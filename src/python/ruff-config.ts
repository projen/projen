// ~~ Generated by projen. To modify, edit .projenrc.ts and run "node ./projen.js".
/**
 * @schema RuffConfiguration
 */
export interface RuffConfiguration {
  /**
   * A list of allowed "confusable" Unicode characters to ignore when
   * enforcing `RUF001`, `RUF002`, and `RUF003`.
   *
   * @schema RuffConfiguration#allowed-confusables
   */
  readonly allowedConfusables?: string[];

  /**
   * Options to configure import map generation.
   *
   * @schema RuffConfiguration#analyze
   */
  readonly analyze?: AnalyzeOptions;

  /**
   * A list of builtins to treat as defined references, in addition to the
   * system builtins.
   *
   * @schema RuffConfiguration#builtins
   */
  readonly builtins?: string[];

  /**
   * A path to the cache directory.
   *
   * By default, Ruff stores cache results in a `.ruff_cache` directory in
   * the current project root.
   *
   * However, Ruff will also respect the `RUFF_CACHE_DIR` environment
   * variable, which takes precedence over that default.
   *
   * This setting will override even the `RUFF_CACHE_DIR` environment
   * variable, if set.
   *
   * @schema RuffConfiguration#cache-dir
   */
  readonly cacheDir?: string;

  /**
   * A regular expression used to identify "dummy" variables, or those which
   * should be ignored when enforcing (e.g.) unused-variable rules. The
   * default expression matches `_`, `__`, and `_var`, but not `_var_`.
   *
   * @schema RuffConfiguration#dummy-variable-rgx
   */
  readonly dummyVariableRgx?: string;

  /**
   * A list of file patterns to exclude from formatting and linting.
   *
   * Exclusions are based on globs, and can be either:
   *
   * - Single-path patterns, like `.mypy_cache` (to exclude any directory
   * named `.mypy_cache` in the tree), `foo.py` (to exclude any file named
   * `foo.py`), or `foo_*.py` (to exclude any file matching `foo_*.py` ).
   * - Relative patterns, like `directory/foo.py` (to exclude that specific
   * file) or `directory/*.py` (to exclude any Python files in
   * `directory`). Note that these paths are relative to the project root
   * (e.g., the directory containing your `pyproject.toml`).
   *
   * For more information on the glob syntax, refer to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).
   *
   * Note that you'll typically want to use
   * [`extend-exclude`](#extend-exclude) to modify the excluded paths.
   *
   * @schema RuffConfiguration#exclude
   */
  readonly exclude?: string[];

  /**
   * Whether to require exact codes to select preview rules. When enabled,
   * preview rules will not be selected by prefixes — the full code of each
   * preview rule will be required to enable the rule.
   *
   * @schema RuffConfiguration#explicit-preview-rules
   */
  readonly explicitPreviewRules?: boolean;

  /**
   * A path to a local `pyproject.toml` or `ruff.toml` file to merge into this
   * configuration. User home directory and environment variables will be
   * expanded.
   *
   * To resolve the current configuration file, Ruff will first load
   * this base configuration file, then merge in properties defined
   * in the current configuration file. Most settings follow simple override
   * behavior where the child value replaces the parent value. However,
   * rule selection (`lint.select` and `lint.ignore`) has special merging
   * behavior: if the child configuration specifies `lint.select`, it
   * establishes a new baseline rule set and the parent's `lint.ignore`
   * rules are discarded; if the child configuration omits `lint.select`,
   * the parent's rule selection is inherited and both parent and child
   * `lint.ignore` rules are accumulated together.
   *
   * @schema RuffConfiguration#extend
   */
  readonly extend?: string;

  /**
   * A list of file patterns to omit from formatting and linting, in addition to those
   * specified by [`exclude`](#exclude).
   *
   * Exclusions are based on globs, and can be either:
   *
   * - Single-path patterns, like `.mypy_cache` (to exclude any directory
   * named `.mypy_cache` in the tree), `foo.py` (to exclude any file named
   * `foo.py`), or `foo_*.py` (to exclude any file matching `foo_*.py` ).
   * - Relative patterns, like `directory/foo.py` (to exclude that specific
   * file) or `directory/*.py` (to exclude any Python files in
   * `directory`). Note that these paths are relative to the project root
   * (e.g., the directory containing your `pyproject.toml`).
   *
   * For more information on the glob syntax, refer to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).
   *
   * @schema RuffConfiguration#extend-exclude
   */
  readonly extendExclude?: string[];

  /**
   * A list of rule codes or prefixes to consider fixable, in addition to those
   * specified by [`fixable`](#lint_fixable).
   *
   * @schema RuffConfiguration#extend-fixable
   */
  readonly extendFixable?: RuleSelector[];

  /**
   * A list of rule codes or prefixes to ignore, in addition to those
   * specified by `ignore`.
   *
   * @schema RuffConfiguration#extend-ignore
   */
  readonly extendIgnore?: RuleSelector[];

  /**
   * A list of file patterns to include when linting, in addition to those
   * specified by [`include`](#include).
   *
   * Inclusion are based on globs, and should be single-path patterns, like
   * `*.pyw`, to include any file with the `.pyw` extension.
   *
   * For more information on the glob syntax, refer to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).
   *
   * @schema RuffConfiguration#extend-include
   */
  readonly extendInclude?: string[];

  /**
   * A list of mappings from file pattern to rule codes or prefixes to
   * exclude, in addition to any rules excluded by [`per-file-ignores`](#lint_per-file-ignores).
   *
   * @schema RuffConfiguration#extend-per-file-ignores
   */
  readonly extendPerFileIgnores?: { [key: string]: RuleSelector[] };

  /**
   * A list of rule codes or prefixes for which unsafe fixes should be considered
   * safe.
   *
   * @schema RuffConfiguration#extend-safe-fixes
   */
  readonly extendSafeFixes?: RuleSelector[];

  /**
   * A list of rule codes or prefixes to enable, in addition to those
   * specified by [`select`](#lint_select).
   *
   * @schema RuffConfiguration#extend-select
   */
  readonly extendSelect?: RuleSelector[];

  /**
   * A list of rule codes or prefixes to consider non-auto-fixable, in addition to those
   * specified by [`unfixable`](#lint_unfixable).
   *
   * @schema RuffConfiguration#extend-unfixable
   */
  readonly extendUnfixable?: RuleSelector[];

  /**
   * A list of rule codes or prefixes for which safe fixes should be considered
   * unsafe.
   *
   * @schema RuffConfiguration#extend-unsafe-fixes
   */
  readonly extendUnsafeFixes?: RuleSelector[];

  /**
   * A list of rule codes or prefixes that are unsupported by Ruff, but should be
   * preserved when (e.g.) validating `# noqa` directives. Useful for
   * retaining `# noqa` directives that cover plugins not yet implemented
   * by Ruff.
   *
   * @schema RuffConfiguration#external
   */
  readonly external?: string[];

  /**
   * Enable fix behavior by-default when running `ruff` (overridden
   * by the `--fix` and `--no-fix` command-line flags).
   * Only includes automatic fixes unless `--unsafe-fixes` is provided.
   *
   * @schema RuffConfiguration#fix
   */
  readonly fix?: boolean;

  /**
   * Like [`fix`](#fix), but disables reporting on leftover violation. Implies [`fix`](#fix).
   *
   * @schema RuffConfiguration#fix-only
   */
  readonly fixOnly?: boolean;

  /**
   * A list of rule codes or prefixes to consider fixable. By default,
   * all rules are considered fixable.
   *
   * @schema RuffConfiguration#fixable
   */
  readonly fixable?: RuleSelector[];

  /**
   * Options for the `flake8-annotations` plugin.
   *
   * @schema RuffConfiguration#flake8-annotations
   */
  readonly flake8Annotations?: Flake8AnnotationsOptions;

  /**
   * Options for the `flake8-bandit` plugin.
   *
   * @schema RuffConfiguration#flake8-bandit
   */
  readonly flake8Bandit?: Flake8BanditOptions;

  /**
   * Options for the `flake8-boolean-trap` plugin.
   *
   * @schema RuffConfiguration#flake8-boolean-trap
   */
  readonly flake8BooleanTrap?: Flake8BooleanTrapOptions;

  /**
   * Options for the `flake8-bugbear` plugin.
   *
   * @schema RuffConfiguration#flake8-bugbear
   */
  readonly flake8Bugbear?: Flake8BugbearOptions;

  /**
   * Options for the `flake8-builtins` plugin.
   *
   * @schema RuffConfiguration#flake8-builtins
   */
  readonly flake8Builtins?: Flake8BuiltinsOptions;

  /**
   * Options for the `flake8-comprehensions` plugin.
   *
   * @schema RuffConfiguration#flake8-comprehensions
   */
  readonly flake8Comprehensions?: Flake8ComprehensionsOptions;

  /**
   * Options for the `flake8-copyright` plugin.
   *
   * @schema RuffConfiguration#flake8-copyright
   */
  readonly flake8Copyright?: Flake8CopyrightOptions;

  /**
   * Options for the `flake8-errmsg` plugin.
   *
   * @schema RuffConfiguration#flake8-errmsg
   */
  readonly flake8Errmsg?: Flake8ErrMsgOptions;

  /**
   * Options for the `flake8-gettext` plugin.
   *
   * @schema RuffConfiguration#flake8-gettext
   */
  readonly flake8Gettext?: Flake8GetTextOptions;

  /**
   * Options for the `flake8-implicit-str-concat` plugin.
   *
   * @schema RuffConfiguration#flake8-implicit-str-concat
   */
  readonly flake8ImplicitStrConcat?: Flake8ImplicitStrConcatOptions;

  /**
   * Options for the `flake8-import-conventions` plugin.
   *
   * @schema RuffConfiguration#flake8-import-conventions
   */
  readonly flake8ImportConventions?: Flake8ImportConventionsOptions;

  /**
   * Options for the `flake8-pytest-style` plugin.
   *
   * @schema RuffConfiguration#flake8-pytest-style
   */
  readonly flake8PytestStyle?: Flake8PytestStyleOptions;

  /**
   * Options for the `flake8-quotes` plugin.
   *
   * @schema RuffConfiguration#flake8-quotes
   */
  readonly flake8Quotes?: Flake8QuotesOptions;

  /**
   * Options for the `flake8_self` plugin.
   *
   * @schema RuffConfiguration#flake8-self
   */
  readonly flake8Self?: Flake8SelfOptions;

  /**
   * Options for the `flake8-tidy-imports` plugin.
   *
   * @schema RuffConfiguration#flake8-tidy-imports
   */
  readonly flake8TidyImports?: Flake8TidyImportsOptions;

  /**
   * Options for the `flake8-type-checking` plugin.
   *
   * @schema RuffConfiguration#flake8-type-checking
   */
  readonly flake8TypeChecking?: Flake8TypeCheckingOptions;

  /**
   * Options for the `flake8-unused-arguments` plugin.
   *
   * @schema RuffConfiguration#flake8-unused-arguments
   */
  readonly flake8UnusedArguments?: Flake8UnusedArgumentsOptions;

  /**
   * Whether to enforce [`exclude`](#exclude) and [`extend-exclude`](#extend-exclude) patterns,
   * even for paths that are passed to Ruff explicitly. Typically, Ruff will lint
   * any paths passed in directly, even if they would typically be
   * excluded. Setting `force-exclude = true` will cause Ruff to
   * respect these exclusions unequivocally.
   *
   * This is useful for [`pre-commit`](https://pre-commit.com/), which explicitly passes all
   * changed files to the [`ruff-pre-commit`](https://github.com/astral-sh/ruff-pre-commit)
   * plugin, regardless of whether they're marked as excluded by Ruff's own
   * settings.
   *
   * @schema RuffConfiguration#force-exclude
   */
  readonly forceExclude?: boolean;

  /**
   * Options to configure code formatting.
   *
   * @schema RuffConfiguration#format
   */
  readonly format?: FormatOptions;

  /**
   * A list of rule codes or prefixes to ignore. Prefixes can specify exact
   * rules (like `F841`), entire categories (like `F`), or anything in
   * between.
   *
   * When breaking ties between enabled and disabled rules (via `select` and
   * `ignore`, respectively), more specific prefixes override less
   * specific prefixes. `ignore` takes precedence over `select` if the same
   * prefix appears in both.
   *
   * @schema RuffConfiguration#ignore
   */
  readonly ignore?: RuleSelector[];

  /**
   * Avoid automatically removing unused imports in `__init__.py` files. Such
   * imports will still be flagged, but with a dedicated message suggesting
   * that the import is either added to the module's `__all__` symbol, or
   * re-exported with a redundant alias (e.g., `import os as os`).
   *
   * This option is enabled by default, but you can opt-in to removal of imports
   * via an unsafe fix.
   *
   * @schema RuffConfiguration#ignore-init-module-imports
   */
  readonly ignoreInitModuleImports?: boolean;

  /**
   * A list of file patterns to include when linting.
   *
   * Inclusion are based on globs, and should be single-path patterns, like
   * `*.pyw`, to include any file with the `.pyw` extension. `pyproject.toml` is
   * included here not for configuration but because we lint whether e.g. the
   * `[project]` matches the schema.
   *
   * Notebook files (`.ipynb` extension) are included by default on Ruff 0.6.0+.
   *
   * For more information on the glob syntax, refer to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).
   *
   * @schema RuffConfiguration#include
   */
  readonly include?: string[];

  /**
   * The number of spaces per indentation level (tab).
   *
   * Used by the formatter and when enforcing long-line violations (like `E501`) to determine the visual
   * width of a tab.
   *
   * This option changes the number of spaces the formatter inserts when
   * using soft-tabs (`indent-style = space`).
   *
   * PEP 8 recommends using 4 spaces per [indentation level](https://peps.python.org/pep-0008/#indentation).
   *
   * @schema RuffConfiguration#indent-width
   */
  readonly indentWidth?: number;

  /**
   * Options for the `isort` plugin.
   *
   * @schema RuffConfiguration#isort
   */
  readonly isort?: IsortOptions;

  /**
   * The line length to use when enforcing long-lines violations (like `E501`)
   * and at which `isort` and the formatter prefers to wrap lines.
   *
   * The length is determined by the number of characters per line, except for lines containing East Asian characters or emojis.
   * For these lines, the [unicode width](https://unicode.org/reports/tr11/) of each character is added up to determine the length.
   *
   * The value must be greater than `0` and less than or equal to `320`.
   *
   * Note: While the formatter will attempt to format lines such that they remain
   * within the `line-length`, it isn't a hard upper bound, and formatted lines may
   * exceed the `line-length`.
   *
   * See [`pycodestyle.max-line-length`](#lint_pycodestyle_max-line-length) to configure different lengths for `E501` and the formatter.
   *
   * @schema RuffConfiguration#line-length
   */
  readonly lineLength?: number;

  /**
   * @schema RuffConfiguration#lint
   */
  readonly lint?: LintOptions;

  /**
   * A list of objects that should be treated equivalently to a
   * `logging.Logger` object.
   *
   * This is useful for ensuring proper diagnostics (e.g., to identify
   * `logging` deprecations and other best-practices) for projects that
   * re-export a `logging.Logger` object from a common module.
   *
   * For example, if you have a module `logging_setup.py` with the following
   * contents:
   * ```python
   * import logging
   *
   * logger = logging.getLogger(__name__)
   * ```
   *
   * Adding `"logging_setup.logger"` to `logger-objects` will ensure that
   * `logging_setup.logger` is treated as a `logging.Logger` object when
   * imported from other modules (e.g., `from logging_setup import logger`).
   *
   * @schema RuffConfiguration#logger-objects
   */
  readonly loggerObjects?: string[];

  /**
   * Options for the `mccabe` plugin.
   *
   * @schema RuffConfiguration#mccabe
   */
  readonly mccabe?: McCabeOptions;

  /**
   * Mark the specified directories as namespace packages. For the purpose of
   * module resolution, Ruff will treat those directories and all their subdirectories
   * as if they contained an `__init__.py` file.
   *
   * @schema RuffConfiguration#namespace-packages
   */
  readonly namespacePackages?: string[];

  /**
   * The style in which violation messages should be formatted: `"full"` (default)
   * (shows source), `"concise"`, `"grouped"` (group messages by file), `"json"`
   * (machine-readable), `"junit"` (machine-readable XML), `"github"` (GitHub
   * Actions annotations), `"gitlab"` (GitLab CI code quality report),
   * `"pylint"` (Pylint text format) or `"azure"` (Azure Pipeline logging commands).
   *
   * @schema RuffConfiguration#output-format
   */
  readonly outputFormat?: OutputFormat;

  /**
   * Options for the `pep8-naming` plugin.
   *
   * @schema RuffConfiguration#pep8-naming
   */
  readonly pep8Naming?: Pep8NamingOptions;

  /**
   * A list of mappings from file pattern to rule codes or prefixes to
   * exclude, when considering any matching files. An initial '!' negates
   * the file pattern.
   *
   * @schema RuffConfiguration#per-file-ignores
   */
  readonly perFileIgnores?: { [key: string]: RuleSelector[] };

  /**
   * A list of mappings from glob-style file pattern to Python version to use when checking the
   * corresponding file(s).
   *
   * This may be useful for overriding the global Python version settings in `target-version` or
   * `requires-python` for a subset of files. For example, if you have a project with a minimum
   * supported Python version of 3.9 but a subdirectory of developer scripts that want to use a
   * newer feature like the `match` statement from Python 3.10, you can use
   * `per-file-target-version` to specify `"developer_scripts/*.py" = "py310"`.
   *
   * This setting is used by the linter to enforce any enabled version-specific lint rules, as
   * well as by the formatter for any version-specific formatting options, such as parenthesizing
   * context managers on Python 3.10+.
   *
   * @schema RuffConfiguration#per-file-target-version
   */
  readonly perFileTargetVersion?: { [key: string]: PythonVersion };

  /**
   * Whether to enable preview mode. When preview mode is enabled, Ruff will
   * use unstable rules, fixes, and formatting.
   *
   * @schema RuffConfiguration#preview
   */
  readonly preview?: boolean;

  /**
   * Options for the `pycodestyle` plugin.
   *
   * @schema RuffConfiguration#pycodestyle
   */
  readonly pycodestyle?: PycodestyleOptions;

  /**
   * Options for the `pydocstyle` plugin.
   *
   * @schema RuffConfiguration#pydocstyle
   */
  readonly pydocstyle?: PydocstyleOptions;

  /**
   * Options for the `pyflakes` plugin.
   *
   * @schema RuffConfiguration#pyflakes
   */
  readonly pyflakes?: PyflakesOptions;

  /**
   * Options for the `pylint` plugin.
   *
   * @schema RuffConfiguration#pylint
   */
  readonly pylint?: PylintOptions;

  /**
   * Options for the `pyupgrade` plugin.
   *
   * @schema RuffConfiguration#pyupgrade
   */
  readonly pyupgrade?: PyUpgradeOptions;

  /**
   * Enforce a requirement on the version of Ruff, to enforce at runtime.
   * If the version of Ruff does not meet the requirement, Ruff will exit
   * with an error.
   *
   * Useful for unifying results across many environments, e.g., with a
   * `pyproject.toml` file.
   *
   * Accepts a [PEP 440](https://peps.python.org/pep-0440/) specifier, like `==0.3.1` or `>=0.3.1`.
   *
   * @schema RuffConfiguration#required-version
   */
  readonly requiredVersion?: string;

  /**
   * Whether to automatically exclude files that are ignored by `.ignore`,
   * `.gitignore`, `.git/info/exclude`, and global `gitignore` files.
   * Enabled by default.
   *
   * @schema RuffConfiguration#respect-gitignore
   */
  readonly respectGitignore?: boolean;

  /**
   * A list of rule codes or prefixes to enable. Prefixes can specify exact
   * rules (like `F841`), entire categories (like `F`), or anything in
   * between.
   *
   * When breaking ties between enabled and disabled rules (via `select` and
   * `ignore`, respectively), more specific prefixes override less
   * specific prefixes. `ignore` takes precedence over `select` if the
   * same prefix appears in both.
   *
   * @schema RuffConfiguration#select
   */
  readonly select?: RuleSelector[];

  /**
   * Whether to show an enumeration of all fixed lint violations
   * (overridden by the `--show-fixes` command-line flag).
   *
   * @schema RuffConfiguration#show-fixes
   */
  readonly showFixes?: boolean;

  /**
   * The directories to consider when resolving first- vs. third-party
   * imports.
   *
   * When omitted, the `src` directory will typically default to including both:
   *
   * 1. The directory containing the nearest `pyproject.toml`, `ruff.toml`, or `.ruff.toml` file (the "project root").
   * 2. The `"src"` subdirectory of the project root.
   *
   * These defaults ensure that Ruff supports both flat layouts and `src` layouts out-of-the-box.
   * (If a configuration file is explicitly provided (e.g., via the `--config` command-line
   * flag), the current working directory will be considered the project root.)
   *
   * As an example, consider an alternative project structure, like:
   *
   * ```text
   * my_project
   * ├── pyproject.toml
   * └── lib
   * └── my_package
   * ├── __init__.py
   * ├── foo.py
   * └── bar.py
   * ```
   *
   * In this case, the `./lib` directory should be included in the `src` option
   * (e.g., `src = ["lib"]`), such that when resolving imports, `my_package.foo`
   * is considered first-party.
   *
   * This field supports globs. For example, if you have a series of Python
   * packages in a `python_modules` directory, `src = ["python_modules/*"]`
   * would expand to incorporate all packages in that directory. User home
   * directory and environment variables will also be expanded.
   *
   * @schema RuffConfiguration#src
   */
  readonly src?: string[];

  /**
   * The minimum Python version to target, e.g., when considering automatic
   * code upgrades, like rewriting type annotations. Ruff will not propose
   * changes using features that are not available in the given version.
   *
   * For example, to represent supporting Python >=3.11 or ==3.11
   * specify `target-version = "py311"`.
   *
   * If you're already using a `pyproject.toml` file, we recommend
   * `project.requires-python` instead, as it's based on Python packaging
   * standards, and will be respected by other tools. For example, Ruff
   * treats the following as identical to `target-version = "py38"`:
   *
   * ```toml
   * [project]
   * requires-python = ">=3.8"
   * ```
   *
   * If both are specified, `target-version` takes precedence over
   * `requires-python`. See [_Inferring the Python version_](https://docs.astral.sh/ruff/configuration/#inferring-the-python-version)
   * for a complete description of how the `target-version` is determined
   * when left unspecified.
   *
   * Note that a stub file can [sometimes make use of a typing feature](https://typing.python.org/en/latest/spec/distributing.html#syntax)
   * before it is available at runtime, as long as the stub does not make
   * use of new *syntax*. For example, a type checker will understand
   * `int | str` in a stub as being a `Union` type annotation, even if the
   * type checker is run using Python 3.9, despite the fact that the `|`
   * operator can only be used to create union types at runtime on Python
   * 3.10+. As such, Ruff will often recommend newer features in a stub
   * file than it would for an equivalent runtime file with the same target
   * version.
   *
   * @schema RuffConfiguration#target-version
   */
  readonly targetVersion?: PythonVersion;

  /**
   * A list of task tags to recognize (e.g., "TODO", "FIXME", "XXX").
   *
   * Comments starting with these tags will be ignored by commented-out code
   * detection (`ERA`), and skipped by line-length rules (`E501`) if
   * [`ignore-overlong-task-comments`](#lint_pycodestyle_ignore-overlong-task-comments) is set to `true`.
   *
   * @schema RuffConfiguration#task-tags
   */
  readonly taskTags?: string[];

  /**
   * A list of modules whose exports should be treated equivalently to
   * members of the `typing` module.
   *
   * This is useful for ensuring proper type annotation inference for
   * projects that re-export `typing` and `typing_extensions` members
   * from a compatibility module. If omitted, any members imported from
   * modules apart from `typing` and `typing_extensions` will be treated
   * as ordinary Python objects.
   *
   * @schema RuffConfiguration#typing-modules
   */
  readonly typingModules?: string[];

  /**
   * A list of rule codes or prefixes to consider non-fixable.
   *
   * @schema RuffConfiguration#unfixable
   */
  readonly unfixable?: RuleSelector[];

  /**
   * Enable application of unsafe fixes.
   * If excluded, a hint will be displayed when unsafe fixes are available.
   * If set to false, the hint will be hidden.
   *
   * @schema RuffConfiguration#unsafe-fixes
   */
  readonly unsafeFixes?: boolean;
}

/**
 * Converts an object of type 'RuffConfiguration' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_RuffConfiguration(obj: RuffConfiguration | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'allowed-confusables': obj.allowedConfusables?.map(y => y),
    'analyze': toJson_AnalyzeOptions(obj.analyze),
    'builtins': obj.builtins?.map(y => y),
    'cache-dir': obj.cacheDir,
    'dummy-variable-rgx': obj.dummyVariableRgx,
    'exclude': obj.exclude?.map(y => y),
    'explicit-preview-rules': obj.explicitPreviewRules,
    'extend': obj.extend,
    'extend-exclude': obj.extendExclude?.map(y => y),
    'extend-fixable': obj.extendFixable?.map(y => y),
    'extend-ignore': obj.extendIgnore?.map(y => y),
    'extend-include': obj.extendInclude?.map(y => y),
    'extend-per-file-ignores': ((obj.extendPerFileIgnores) === undefined) ? undefined : (Object.entries(obj.extendPerFileIgnores).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1]?.map(y => y) }), {})),
    'extend-safe-fixes': obj.extendSafeFixes?.map(y => y),
    'extend-select': obj.extendSelect?.map(y => y),
    'extend-unfixable': obj.extendUnfixable?.map(y => y),
    'extend-unsafe-fixes': obj.extendUnsafeFixes?.map(y => y),
    'external': obj.external?.map(y => y),
    'fix': obj.fix,
    'fix-only': obj.fixOnly,
    'fixable': obj.fixable?.map(y => y),
    'flake8-annotations': toJson_Flake8AnnotationsOptions(obj.flake8Annotations),
    'flake8-bandit': toJson_Flake8BanditOptions(obj.flake8Bandit),
    'flake8-boolean-trap': toJson_Flake8BooleanTrapOptions(obj.flake8BooleanTrap),
    'flake8-bugbear': toJson_Flake8BugbearOptions(obj.flake8Bugbear),
    'flake8-builtins': toJson_Flake8BuiltinsOptions(obj.flake8Builtins),
    'flake8-comprehensions': toJson_Flake8ComprehensionsOptions(obj.flake8Comprehensions),
    'flake8-copyright': toJson_Flake8CopyrightOptions(obj.flake8Copyright),
    'flake8-errmsg': toJson_Flake8ErrMsgOptions(obj.flake8Errmsg),
    'flake8-gettext': toJson_Flake8GetTextOptions(obj.flake8Gettext),
    'flake8-implicit-str-concat': toJson_Flake8ImplicitStrConcatOptions(obj.flake8ImplicitStrConcat),
    'flake8-import-conventions': toJson_Flake8ImportConventionsOptions(obj.flake8ImportConventions),
    'flake8-pytest-style': toJson_Flake8PytestStyleOptions(obj.flake8PytestStyle),
    'flake8-quotes': toJson_Flake8QuotesOptions(obj.flake8Quotes),
    'flake8-self': toJson_Flake8SelfOptions(obj.flake8Self),
    'flake8-tidy-imports': toJson_Flake8TidyImportsOptions(obj.flake8TidyImports),
    'flake8-type-checking': toJson_Flake8TypeCheckingOptions(obj.flake8TypeChecking),
    'flake8-unused-arguments': toJson_Flake8UnusedArgumentsOptions(obj.flake8UnusedArguments),
    'force-exclude': obj.forceExclude,
    'format': toJson_FormatOptions(obj.format),
    'ignore': obj.ignore?.map(y => y),
    'ignore-init-module-imports': obj.ignoreInitModuleImports,
    'include': obj.include?.map(y => y),
    'indent-width': obj.indentWidth,
    'isort': toJson_IsortOptions(obj.isort),
    'line-length': obj.lineLength,
    'lint': toJson_LintOptions(obj.lint),
    'logger-objects': obj.loggerObjects?.map(y => y),
    'mccabe': toJson_McCabeOptions(obj.mccabe),
    'namespace-packages': obj.namespacePackages?.map(y => y),
    'output-format': obj.outputFormat,
    'pep8-naming': toJson_Pep8NamingOptions(obj.pep8Naming),
    'per-file-ignores': ((obj.perFileIgnores) === undefined) ? undefined : (Object.entries(obj.perFileIgnores).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1]?.map(y => y) }), {})),
    'per-file-target-version': ((obj.perFileTargetVersion) === undefined) ? undefined : (Object.entries(obj.perFileTargetVersion).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {})),
    'preview': obj.preview,
    'pycodestyle': toJson_PycodestyleOptions(obj.pycodestyle),
    'pydocstyle': toJson_PydocstyleOptions(obj.pydocstyle),
    'pyflakes': toJson_PyflakesOptions(obj.pyflakes),
    'pylint': toJson_PylintOptions(obj.pylint),
    'pyupgrade': toJson_PyUpgradeOptions(obj.pyupgrade),
    'required-version': obj.requiredVersion,
    'respect-gitignore': obj.respectGitignore,
    'select': obj.select?.map(y => y),
    'show-fixes': obj.showFixes,
    'src': obj.src?.map(y => y),
    'target-version': obj.targetVersion,
    'task-tags': obj.taskTags?.map(y => y),
    'typing-modules': obj.typingModules?.map(y => y),
    'unfixable': obj.unfixable?.map(y => y),
    'unsafe-fixes': obj.unsafeFixes,
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Configures Ruff's `analyze` command.
 *
 * @schema AnalyzeOptions
 */
export interface AnalyzeOptions {
  /**
   * Whether to detect imports from string literals. When enabled, Ruff will search for string
   * literals that "look like" import paths, and include them in the import map, if they resolve
   * to valid Python modules.
   *
   * @schema AnalyzeOptions#detect-string-imports
   */
  readonly detectStringImports?: boolean;

  /**
   * Whether to generate a map from file to files that it depends on (dependencies) or files that
   * depend on it (dependents).
   *
   * @schema AnalyzeOptions#direction
   */
  readonly direction?: string;

  /**
   * A list of file patterns to exclude from analysis in addition to the files excluded globally (see [`exclude`](#exclude), and [`extend-exclude`](#extend-exclude)).
   *
   * Exclusions are based on globs, and can be either:
   *
   * - Single-path patterns, like `.mypy_cache` (to exclude any directory
   * named `.mypy_cache` in the tree), `foo.py` (to exclude any file named
   * `foo.py`), or `foo_*.py` (to exclude any file matching `foo_*.py` ).
   * - Relative patterns, like `directory/foo.py` (to exclude that specific
   * file) or `directory/*.py` (to exclude any Python files in
   * `directory`). Note that these paths are relative to the project root
   * (e.g., the directory containing your `pyproject.toml`).
   *
   * For more information on the glob syntax, refer to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).
   *
   * @schema AnalyzeOptions#exclude
   */
  readonly exclude?: string[];

  /**
   * A map from file path to the list of Python or non-Python file paths or globs that should be
   * considered dependencies of that file, regardless of whether relevant imports are detected.
   *
   * @schema AnalyzeOptions#include-dependencies
   */
  readonly includeDependencies?: { [key: string]: string[] };

  /**
   * Whether to enable preview mode. When preview mode is enabled, Ruff will expose unstable
   * commands.
   *
   * @schema AnalyzeOptions#preview
   */
  readonly preview?: boolean;

  /**
   * The minimum number of dots in a string to consider it a valid import.
   *
   * This setting is only relevant when [`detect-string-imports`](#detect-string-imports) is enabled.
   * For example, if this is set to `2`, then only strings with at least two dots (e.g., `"path.to.module"`)
   * would be considered valid imports.
   *
   * @schema AnalyzeOptions#string-imports-min-dots
   */
  readonly stringImportsMinDots?: number;

  /**
   * Whether to include imports that are only used for type checking (i.e., imports within `if TYPE_CHECKING:` blocks).
   * When enabled (default), type-checking-only imports are included in the import graph.
   * When disabled, they are excluded.
   *
   * @schema AnalyzeOptions#type-checking-imports
   */
  readonly typeCheckingImports?: boolean;
}

/**
 * Converts an object of type 'AnalyzeOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_AnalyzeOptions(obj: AnalyzeOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'detect-string-imports': obj.detectStringImports,
    'direction': obj.direction,
    'exclude': obj.exclude?.map(y => y),
    'include-dependencies': ((obj.includeDependencies) === undefined) ? undefined : (Object.entries(obj.includeDependencies).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1]?.map(y => y) }), {})),
    'preview': obj.preview,
    'string-imports-min-dots': obj.stringImportsMinDots,
    'type-checking-imports': obj.typeCheckingImports,
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * @schema RuleSelector
 */
export enum RuleSelector {
  /** A */
  A = "A",
  /** A0 */
  A0 = "A0",
  /** A00 */
  A00 = "A00",
  /** A001 */
  A001 = "A001",
  /** A002 */
  A002 = "A002",
  /** A003 */
  A003 = "A003",
  /** A004 */
  A004 = "A004",
  /** A005 */
  A005 = "A005",
  /** A006 */
  A006 = "A006",
  /** AIR */
  AIR = "AIR",
  /** AIR0 */
  AIR0 = "AIR0",
  /** AIR00 */
  AIR00 = "AIR00",
  /** AIR001 */
  AIR001 = "AIR001",
  /** AIR002 */
  AIR002 = "AIR002",
  /** AIR3 */
  AIR3 = "AIR3",
  /** AIR30 */
  AIR30 = "AIR30",
  /** AIR301 */
  AIR301 = "AIR301",
  /** AIR302 */
  AIR302 = "AIR302",
  /** AIR31 */
  AIR31 = "AIR31",
  /** AIR311 */
  AIR311 = "AIR311",
  /** AIR312 */
  AIR312 = "AIR312",
  /** ALL */
  ALL = "ALL",
  /** ANN */
  ANN = "ANN",
  /** ANN0 */
  ANN0 = "ANN0",
  /** ANN00 */
  ANN00 = "ANN00",
  /** ANN001 */
  ANN001 = "ANN001",
  /** ANN002 */
  ANN002 = "ANN002",
  /** ANN003 */
  ANN003 = "ANN003",
  /** ANN2 */
  ANN2 = "ANN2",
  /** ANN20 */
  ANN20 = "ANN20",
  /** ANN201 */
  ANN201 = "ANN201",
  /** ANN202 */
  ANN202 = "ANN202",
  /** ANN204 */
  ANN204 = "ANN204",
  /** ANN205 */
  ANN205 = "ANN205",
  /** ANN206 */
  ANN206 = "ANN206",
  /** ANN4 */
  ANN4 = "ANN4",
  /** ANN40 */
  ANN40 = "ANN40",
  /** ANN401 */
  ANN401 = "ANN401",
  /** ARG */
  ARG = "ARG",
  /** ARG0 */
  ARG0 = "ARG0",
  /** ARG00 */
  ARG00 = "ARG00",
  /** ARG001 */
  ARG001 = "ARG001",
  /** ARG002 */
  ARG002 = "ARG002",
  /** ARG003 */
  ARG003 = "ARG003",
  /** ARG004 */
  ARG004 = "ARG004",
  /** ARG005 */
  ARG005 = "ARG005",
  /** ASYNC */
  ASYNC = "ASYNC",
  /** ASYNC1 */
  ASYNC1 = "ASYNC1",
  /** ASYNC10 */
  ASYNC10 = "ASYNC10",
  /** ASYNC100 */
  ASYNC100 = "ASYNC100",
  /** ASYNC105 */
  ASYNC105 = "ASYNC105",
  /** ASYNC109 */
  ASYNC109 = "ASYNC109",
  /** ASYNC11 */
  ASYNC11 = "ASYNC11",
  /** ASYNC110 */
  ASYNC110 = "ASYNC110",
  /** ASYNC115 */
  ASYNC115 = "ASYNC115",
  /** ASYNC116 */
  ASYNC116 = "ASYNC116",
  /** ASYNC2 */
  ASYNC2 = "ASYNC2",
  /** ASYNC21 */
  ASYNC21 = "ASYNC21",
  /** ASYNC210 */
  ASYNC210 = "ASYNC210",
  /** ASYNC212 */
  ASYNC212 = "ASYNC212",
  /** ASYNC22 */
  ASYNC22 = "ASYNC22",
  /** ASYNC220 */
  ASYNC220 = "ASYNC220",
  /** ASYNC221 */
  ASYNC221 = "ASYNC221",
  /** ASYNC222 */
  ASYNC222 = "ASYNC222",
  /** ASYNC23 */
  ASYNC23 = "ASYNC23",
  /** ASYNC230 */
  ASYNC230 = "ASYNC230",
  /** ASYNC24 */
  ASYNC24 = "ASYNC24",
  /** ASYNC240 */
  ASYNC240 = "ASYNC240",
  /** ASYNC25 */
  ASYNC25 = "ASYNC25",
  /** ASYNC250 */
  ASYNC250 = "ASYNC250",
  /** ASYNC251 */
  ASYNC251 = "ASYNC251",
  /** B */
  B = "B",
  /** B0 */
  B0 = "B0",
  /** B00 */
  B00 = "B00",
  /** B002 */
  B002 = "B002",
  /** B003 */
  B003 = "B003",
  /** B004 */
  B004 = "B004",
  /** B005 */
  B005 = "B005",
  /** B006 */
  B006 = "B006",
  /** B007 */
  B007 = "B007",
  /** B008 */
  B008 = "B008",
  /** B009 */
  B009 = "B009",
  /** B01 */
  B01 = "B01",
  /** B010 */
  B010 = "B010",
  /** B011 */
  B011 = "B011",
  /** B012 */
  B012 = "B012",
  /** B013 */
  B013 = "B013",
  /** B014 */
  B014 = "B014",
  /** B015 */
  B015 = "B015",
  /** B016 */
  B016 = "B016",
  /** B017 */
  B017 = "B017",
  /** B018 */
  B018 = "B018",
  /** B019 */
  B019 = "B019",
  /** B02 */
  B02 = "B02",
  /** B020 */
  B020 = "B020",
  /** B021 */
  B021 = "B021",
  /** B022 */
  B022 = "B022",
  /** B023 */
  B023 = "B023",
  /** B024 */
  B024 = "B024",
  /** B025 */
  B025 = "B025",
  /** B026 */
  B026 = "B026",
  /** B027 */
  B027 = "B027",
  /** B028 */
  B028 = "B028",
  /** B029 */
  B029 = "B029",
  /** B03 */
  B03 = "B03",
  /** B030 */
  B030 = "B030",
  /** B031 */
  B031 = "B031",
  /** B032 */
  B032 = "B032",
  /** B033 */
  B033 = "B033",
  /** B034 */
  B034 = "B034",
  /** B035 */
  B035 = "B035",
  /** B039 */
  B039 = "B039",
  /** B9 */
  B9 = "B9",
  /** B90 */
  B90 = "B90",
  /** B901 */
  B901 = "B901",
  /** B903 */
  B903 = "B903",
  /** B904 */
  B904 = "B904",
  /** B905 */
  B905 = "B905",
  /** B909 */
  B909 = "B909",
  /** B91 */
  B91 = "B91",
  /** B911 */
  B911 = "B911",
  /** B912 */
  B912 = "B912",
  /** BLE */
  BLE = "BLE",
  /** BLE0 */
  BLE0 = "BLE0",
  /** BLE00 */
  BLE00 = "BLE00",
  /** BLE001 */
  BLE001 = "BLE001",
  /** C */
  C = "C",
  /** C4 */
  C4 = "C4",
  /** C40 */
  C40 = "C40",
  /** C400 */
  C400 = "C400",
  /** C401 */
  C401 = "C401",
  /** C402 */
  C402 = "C402",
  /** C403 */
  C403 = "C403",
  /** C404 */
  C404 = "C404",
  /** C405 */
  C405 = "C405",
  /** C406 */
  C406 = "C406",
  /** C408 */
  C408 = "C408",
  /** C409 */
  C409 = "C409",
  /** C41 */
  C41 = "C41",
  /** C410 */
  C410 = "C410",
  /** C411 */
  C411 = "C411",
  /** C413 */
  C413 = "C413",
  /** C414 */
  C414 = "C414",
  /** C415 */
  C415 = "C415",
  /** C416 */
  C416 = "C416",
  /** C417 */
  C417 = "C417",
  /** C418 */
  C418 = "C418",
  /** C419 */
  C419 = "C419",
  /** C42 */
  C42 = "C42",
  /** C420 */
  C420 = "C420",
  /** C9 */
  C9 = "C9",
  /** C90 */
  C90 = "C90",
  /** C901 */
  C901 = "C901",
  /** COM */
  COM = "COM",
  /** COM8 */
  COM8 = "COM8",
  /** COM81 */
  COM81 = "COM81",
  /** COM812 */
  COM812 = "COM812",
  /** COM818 */
  COM818 = "COM818",
  /** COM819 */
  COM819 = "COM819",
  /** CPY */
  CPY = "CPY",
  /** CPY0 */
  CPY0 = "CPY0",
  /** CPY00 */
  CPY00 = "CPY00",
  /** CPY001 */
  CPY001 = "CPY001",
  /** D */
  D = "D",
  /** D1 */
  D1 = "D1",
  /** D10 */
  D10 = "D10",
  /** D100 */
  D100 = "D100",
  /** D101 */
  D101 = "D101",
  /** D102 */
  D102 = "D102",
  /** D103 */
  D103 = "D103",
  /** D104 */
  D104 = "D104",
  /** D105 */
  D105 = "D105",
  /** D106 */
  D106 = "D106",
  /** D107 */
  D107 = "D107",
  /** D2 */
  D2 = "D2",
  /** D20 */
  D20 = "D20",
  /** D200 */
  D200 = "D200",
  /** D201 */
  D201 = "D201",
  /** D202 */
  D202 = "D202",
  /** D203 */
  D203 = "D203",
  /** D204 */
  D204 = "D204",
  /** D205 */
  D205 = "D205",
  /** D206 */
  D206 = "D206",
  /** D207 */
  D207 = "D207",
  /** D208 */
  D208 = "D208",
  /** D209 */
  D209 = "D209",
  /** D21 */
  D21 = "D21",
  /** D210 */
  D210 = "D210",
  /** D211 */
  D211 = "D211",
  /** D212 */
  D212 = "D212",
  /** D213 */
  D213 = "D213",
  /** D214 */
  D214 = "D214",
  /** D215 */
  D215 = "D215",
  /** D3 */
  D3 = "D3",
  /** D30 */
  D30 = "D30",
  /** D300 */
  D300 = "D300",
  /** D301 */
  D301 = "D301",
  /** D4 */
  D4 = "D4",
  /** D40 */
  D40 = "D40",
  /** D400 */
  D400 = "D400",
  /** D401 */
  D401 = "D401",
  /** D402 */
  D402 = "D402",
  /** D403 */
  D403 = "D403",
  /** D404 */
  D404 = "D404",
  /** D405 */
  D405 = "D405",
  /** D406 */
  D406 = "D406",
  /** D407 */
  D407 = "D407",
  /** D408 */
  D408 = "D408",
  /** D409 */
  D409 = "D409",
  /** D41 */
  D41 = "D41",
  /** D410 */
  D410 = "D410",
  /** D411 */
  D411 = "D411",
  /** D412 */
  D412 = "D412",
  /** D413 */
  D413 = "D413",
  /** D414 */
  D414 = "D414",
  /** D415 */
  D415 = "D415",
  /** D416 */
  D416 = "D416",
  /** D417 */
  D417 = "D417",
  /** D418 */
  D418 = "D418",
  /** D419 */
  D419 = "D419",
  /** DJ */
  DJ = "DJ",
  /** DJ0 */
  DJ0 = "DJ0",
  /** DJ00 */
  DJ00 = "DJ00",
  /** DJ001 */
  DJ001 = "DJ001",
  /** DJ003 */
  DJ003 = "DJ003",
  /** DJ006 */
  DJ006 = "DJ006",
  /** DJ007 */
  DJ007 = "DJ007",
  /** DJ008 */
  DJ008 = "DJ008",
  /** DJ01 */
  DJ01 = "DJ01",
  /** DJ012 */
  DJ012 = "DJ012",
  /** DJ013 */
  DJ013 = "DJ013",
  /** DOC */
  DOC = "DOC",
  /** DOC1 */
  DOC1 = "DOC1",
  /** DOC10 */
  DOC10 = "DOC10",
  /** DOC102 */
  DOC102 = "DOC102",
  /** DOC2 */
  DOC2 = "DOC2",
  /** DOC20 */
  DOC20 = "DOC20",
  /** DOC201 */
  DOC201 = "DOC201",
  /** DOC202 */
  DOC202 = "DOC202",
  /** DOC4 */
  DOC4 = "DOC4",
  /** DOC40 */
  DOC40 = "DOC40",
  /** DOC402 */
  DOC402 = "DOC402",
  /** DOC403 */
  DOC403 = "DOC403",
  /** DOC5 */
  DOC5 = "DOC5",
  /** DOC50 */
  DOC50 = "DOC50",
  /** DOC501 */
  DOC501 = "DOC501",
  /** DOC502 */
  DOC502 = "DOC502",
  /** DTZ */
  DTZ = "DTZ",
  /** DTZ0 */
  DTZ0 = "DTZ0",
  /** DTZ00 */
  DTZ00 = "DTZ00",
  /** DTZ001 */
  DTZ001 = "DTZ001",
  /** DTZ002 */
  DTZ002 = "DTZ002",
  /** DTZ003 */
  DTZ003 = "DTZ003",
  /** DTZ004 */
  DTZ004 = "DTZ004",
  /** DTZ005 */
  DTZ005 = "DTZ005",
  /** DTZ006 */
  DTZ006 = "DTZ006",
  /** DTZ007 */
  DTZ007 = "DTZ007",
  /** DTZ01 */
  DTZ01 = "DTZ01",
  /** DTZ011 */
  DTZ011 = "DTZ011",
  /** DTZ012 */
  DTZ012 = "DTZ012",
  /** DTZ9 */
  DTZ9 = "DTZ9",
  /** DTZ90 */
  DTZ90 = "DTZ90",
  /** DTZ901 */
  DTZ901 = "DTZ901",
  /** E */
  E = "E",
  /** E1 */
  E1 = "E1",
  /** E10 */
  E10 = "E10",
  /** E101 */
  E101 = "E101",
  /** E11 */
  E11 = "E11",
  /** E111 */
  E111 = "E111",
  /** E112 */
  E112 = "E112",
  /** E113 */
  E113 = "E113",
  /** E114 */
  E114 = "E114",
  /** E115 */
  E115 = "E115",
  /** E116 */
  E116 = "E116",
  /** E117 */
  E117 = "E117",
  /** E2 */
  E2 = "E2",
  /** E20 */
  E20 = "E20",
  /** E201 */
  E201 = "E201",
  /** E202 */
  E202 = "E202",
  /** E203 */
  E203 = "E203",
  /** E204 */
  E204 = "E204",
  /** E21 */
  E21 = "E21",
  /** E211 */
  E211 = "E211",
  /** E22 */
  E22 = "E22",
  /** E221 */
  E221 = "E221",
  /** E222 */
  E222 = "E222",
  /** E223 */
  E223 = "E223",
  /** E224 */
  E224 = "E224",
  /** E225 */
  E225 = "E225",
  /** E226 */
  E226 = "E226",
  /** E227 */
  E227 = "E227",
  /** E228 */
  E228 = "E228",
  /** E23 */
  E23 = "E23",
  /** E231 */
  E231 = "E231",
  /** E24 */
  E24 = "E24",
  /** E241 */
  E241 = "E241",
  /** E242 */
  E242 = "E242",
  /** E25 */
  E25 = "E25",
  /** E251 */
  E251 = "E251",
  /** E252 */
  E252 = "E252",
  /** E26 */
  E26 = "E26",
  /** E261 */
  E261 = "E261",
  /** E262 */
  E262 = "E262",
  /** E265 */
  E265 = "E265",
  /** E266 */
  E266 = "E266",
  /** E27 */
  E27 = "E27",
  /** E271 */
  E271 = "E271",
  /** E272 */
  E272 = "E272",
  /** E273 */
  E273 = "E273",
  /** E274 */
  E274 = "E274",
  /** E275 */
  E275 = "E275",
  /** E3 */
  E3 = "E3",
  /** E30 */
  E30 = "E30",
  /** E301 */
  E301 = "E301",
  /** E302 */
  E302 = "E302",
  /** E303 */
  E303 = "E303",
  /** E304 */
  E304 = "E304",
  /** E305 */
  E305 = "E305",
  /** E306 */
  E306 = "E306",
  /** E4 */
  E4 = "E4",
  /** E40 */
  E40 = "E40",
  /** E401 */
  E401 = "E401",
  /** E402 */
  E402 = "E402",
  /** E5 */
  E5 = "E5",
  /** E50 */
  E50 = "E50",
  /** E501 */
  E501 = "E501",
  /** E502 */
  E502 = "E502",
  /** E7 */
  E7 = "E7",
  /** E70 */
  E70 = "E70",
  /** E701 */
  E701 = "E701",
  /** E702 */
  E702 = "E702",
  /** E703 */
  E703 = "E703",
  /** E71 */
  E71 = "E71",
  /** E711 */
  E711 = "E711",
  /** E712 */
  E712 = "E712",
  /** E713 */
  E713 = "E713",
  /** E714 */
  E714 = "E714",
  /** E72 */
  E72 = "E72",
  /** E721 */
  E721 = "E721",
  /** E722 */
  E722 = "E722",
  /** E73 */
  E73 = "E73",
  /** E731 */
  E731 = "E731",
  /** E74 */
  E74 = "E74",
  /** E741 */
  E741 = "E741",
  /** E742 */
  E742 = "E742",
  /** E743 */
  E743 = "E743",
  /** E9 */
  E9 = "E9",
  /** E90 */
  E90 = "E90",
  /** E902 */
  E902 = "E902",
  /** EM */
  EM = "EM",
  /** EM1 */
  EM1 = "EM1",
  /** EM10 */
  EM10 = "EM10",
  /** EM101 */
  EM101 = "EM101",
  /** EM102 */
  EM102 = "EM102",
  /** EM103 */
  EM103 = "EM103",
  /** ERA */
  ERA = "ERA",
  /** ERA0 */
  ERA0 = "ERA0",
  /** ERA00 */
  ERA00 = "ERA00",
  /** ERA001 */
  ERA001 = "ERA001",
  /** EXE */
  EXE = "EXE",
  /** EXE0 */
  EXE0 = "EXE0",
  /** EXE00 */
  EXE00 = "EXE00",
  /** EXE001 */
  EXE001 = "EXE001",
  /** EXE002 */
  EXE002 = "EXE002",
  /** EXE003 */
  EXE003 = "EXE003",
  /** EXE004 */
  EXE004 = "EXE004",
  /** EXE005 */
  EXE005 = "EXE005",
  /** F */
  F = "F",
  /** F4 */
  F4 = "F4",
  /** F40 */
  F40 = "F40",
  /** F401 */
  F401 = "F401",
  /** F402 */
  F402 = "F402",
  /** F403 */
  F403 = "F403",
  /** F404 */
  F404 = "F404",
  /** F405 */
  F405 = "F405",
  /** F406 */
  F406 = "F406",
  /** F407 */
  F407 = "F407",
  /** F5 */
  F5 = "F5",
  /** F50 */
  F50 = "F50",
  /** F501 */
  F501 = "F501",
  /** F502 */
  F502 = "F502",
  /** F503 */
  F503 = "F503",
  /** F504 */
  F504 = "F504",
  /** F505 */
  F505 = "F505",
  /** F506 */
  F506 = "F506",
  /** F507 */
  F507 = "F507",
  /** F508 */
  F508 = "F508",
  /** F509 */
  F509 = "F509",
  /** F52 */
  F52 = "F52",
  /** F521 */
  F521 = "F521",
  /** F522 */
  F522 = "F522",
  /** F523 */
  F523 = "F523",
  /** F524 */
  F524 = "F524",
  /** F525 */
  F525 = "F525",
  /** F54 */
  F54 = "F54",
  /** F541 */
  F541 = "F541",
  /** F6 */
  F6 = "F6",
  /** F60 */
  F60 = "F60",
  /** F601 */
  F601 = "F601",
  /** F602 */
  F602 = "F602",
  /** F62 */
  F62 = "F62",
  /** F621 */
  F621 = "F621",
  /** F622 */
  F622 = "F622",
  /** F63 */
  F63 = "F63",
  /** F631 */
  F631 = "F631",
  /** F632 */
  F632 = "F632",
  /** F633 */
  F633 = "F633",
  /** F634 */
  F634 = "F634",
  /** F7 */
  F7 = "F7",
  /** F70 */
  F70 = "F70",
  /** F701 */
  F701 = "F701",
  /** F702 */
  F702 = "F702",
  /** F704 */
  F704 = "F704",
  /** F706 */
  F706 = "F706",
  /** F707 */
  F707 = "F707",
  /** F72 */
  F72 = "F72",
  /** F722 */
  F722 = "F722",
  /** F8 */
  F8 = "F8",
  /** F81 */
  F81 = "F81",
  /** F811 */
  F811 = "F811",
  /** F82 */
  F82 = "F82",
  /** F821 */
  F821 = "F821",
  /** F822 */
  F822 = "F822",
  /** F823 */
  F823 = "F823",
  /** F84 */
  F84 = "F84",
  /** F841 */
  F841 = "F841",
  /** F842 */
  F842 = "F842",
  /** F9 */
  F9 = "F9",
  /** F90 */
  F90 = "F90",
  /** F901 */
  F901 = "F901",
  /** FA */
  FA = "FA",
  /** FA1 */
  FA1 = "FA1",
  /** FA10 */
  FA10 = "FA10",
  /** FA100 */
  FA100 = "FA100",
  /** FA102 */
  FA102 = "FA102",
  /** FAST */
  FAST = "FAST",
  /** FAST0 */
  FAST0 = "FAST0",
  /** FAST00 */
  FAST00 = "FAST00",
  /** FAST001 */
  FAST001 = "FAST001",
  /** FAST002 */
  FAST002 = "FAST002",
  /** FAST003 */
  FAST003 = "FAST003",
  /** FBT */
  FBT = "FBT",
  /** FBT0 */
  FBT0 = "FBT0",
  /** FBT00 */
  FBT00 = "FBT00",
  /** FBT001 */
  FBT001 = "FBT001",
  /** FBT002 */
  FBT002 = "FBT002",
  /** FBT003 */
  FBT003 = "FBT003",
  /** FIX */
  FIX = "FIX",
  /** FIX0 */
  FIX0 = "FIX0",
  /** FIX00 */
  FIX00 = "FIX00",
  /** FIX001 */
  FIX001 = "FIX001",
  /** FIX002 */
  FIX002 = "FIX002",
  /** FIX003 */
  FIX003 = "FIX003",
  /** FIX004 */
  FIX004 = "FIX004",
  /** FLY */
  FLY = "FLY",
  /** FLY0 */
  FLY0 = "FLY0",
  /** FLY00 */
  FLY00 = "FLY00",
  /** FLY002 */
  FLY002 = "FLY002",
  /** FURB */
  FURB = "FURB",
  /** FURB1 */
  FURB1 = "FURB1",
  /** FURB10 */
  FURB10 = "FURB10",
  /** FURB101 */
  FURB101 = "FURB101",
  /** FURB103 */
  FURB103 = "FURB103",
  /** FURB105 */
  FURB105 = "FURB105",
  /** FURB11 */
  FURB11 = "FURB11",
  /** FURB110 */
  FURB110 = "FURB110",
  /** FURB113 */
  FURB113 = "FURB113",
  /** FURB116 */
  FURB116 = "FURB116",
  /** FURB118 */
  FURB118 = "FURB118",
  /** FURB12 */
  FURB12 = "FURB12",
  /** FURB122 */
  FURB122 = "FURB122",
  /** FURB129 */
  FURB129 = "FURB129",
  /** FURB13 */
  FURB13 = "FURB13",
  /** FURB131 */
  FURB131 = "FURB131",
  /** FURB132 */
  FURB132 = "FURB132",
  /** FURB136 */
  FURB136 = "FURB136",
  /** FURB14 */
  FURB14 = "FURB14",
  /** FURB140 */
  FURB140 = "FURB140",
  /** FURB142 */
  FURB142 = "FURB142",
  /** FURB145 */
  FURB145 = "FURB145",
  /** FURB148 */
  FURB148 = "FURB148",
  /** FURB15 */
  FURB15 = "FURB15",
  /** FURB152 */
  FURB152 = "FURB152",
  /** FURB154 */
  FURB154 = "FURB154",
  /** FURB156 */
  FURB156 = "FURB156",
  /** FURB157 */
  FURB157 = "FURB157",
  /** FURB16 */
  FURB16 = "FURB16",
  /** FURB161 */
  FURB161 = "FURB161",
  /** FURB162 */
  FURB162 = "FURB162",
  /** FURB163 */
  FURB163 = "FURB163",
  /** FURB164 */
  FURB164 = "FURB164",
  /** FURB166 */
  FURB166 = "FURB166",
  /** FURB167 */
  FURB167 = "FURB167",
  /** FURB168 */
  FURB168 = "FURB168",
  /** FURB169 */
  FURB169 = "FURB169",
  /** FURB17 */
  FURB17 = "FURB17",
  /** FURB171 */
  FURB171 = "FURB171",
  /** FURB177 */
  FURB177 = "FURB177",
  /** FURB18 */
  FURB18 = "FURB18",
  /** FURB180 */
  FURB180 = "FURB180",
  /** FURB181 */
  FURB181 = "FURB181",
  /** FURB187 */
  FURB187 = "FURB187",
  /** FURB188 */
  FURB188 = "FURB188",
  /** FURB189 */
  FURB189 = "FURB189",
  /** FURB19 */
  FURB19 = "FURB19",
  /** FURB192 */
  FURB192 = "FURB192",
  /** G */
  G = "G",
  /** G0 */
  G0 = "G0",
  /** G00 */
  G00 = "G00",
  /** G001 */
  G001 = "G001",
  /** G002 */
  G002 = "G002",
  /** G003 */
  G003 = "G003",
  /** G004 */
  G004 = "G004",
  /** G01 */
  G01 = "G01",
  /** G010 */
  G010 = "G010",
  /** G1 */
  G1 = "G1",
  /** G10 */
  G10 = "G10",
  /** G101 */
  G101 = "G101",
  /** G2 */
  G2 = "G2",
  /** G20 */
  G20 = "G20",
  /** G201 */
  G201 = "G201",
  /** G202 */
  G202 = "G202",
  /** I */
  I = "I",
  /** I0 */
  I0 = "I0",
  /** I00 */
  I00 = "I00",
  /** I001 */
  I001 = "I001",
  /** I002 */
  I002 = "I002",
  /** ICN */
  ICN = "ICN",
  /** ICN0 */
  ICN0 = "ICN0",
  /** ICN00 */
  ICN00 = "ICN00",
  /** ICN001 */
  ICN001 = "ICN001",
  /** ICN002 */
  ICN002 = "ICN002",
  /** ICN003 */
  ICN003 = "ICN003",
  /** INP */
  INP = "INP",
  /** INP0 */
  INP0 = "INP0",
  /** INP00 */
  INP00 = "INP00",
  /** INP001 */
  INP001 = "INP001",
  /** INT */
  INT = "INT",
  /** INT0 */
  INT0 = "INT0",
  /** INT00 */
  INT00 = "INT00",
  /** INT001 */
  INT001 = "INT001",
  /** INT002 */
  INT002 = "INT002",
  /** INT003 */
  INT003 = "INT003",
  /** ISC */
  ISC = "ISC",
  /** ISC0 */
  ISC0 = "ISC0",
  /** ISC00 */
  ISC00 = "ISC00",
  /** ISC001 */
  ISC001 = "ISC001",
  /** ISC002 */
  ISC002 = "ISC002",
  /** ISC003 */
  ISC003 = "ISC003",
  /** LOG */
  LOG = "LOG",
  /** LOG0 */
  LOG0 = "LOG0",
  /** LOG00 */
  LOG00 = "LOG00",
  /** LOG001 */
  LOG001 = "LOG001",
  /** LOG002 */
  LOG002 = "LOG002",
  /** LOG004 */
  LOG004 = "LOG004",
  /** LOG007 */
  LOG007 = "LOG007",
  /** LOG009 */
  LOG009 = "LOG009",
  /** LOG01 */
  LOG01 = "LOG01",
  /** LOG014 */
  LOG014 = "LOG014",
  /** LOG015 */
  LOG015 = "LOG015",
  /** N */
  N = "N",
  /** N8 */
  N8 = "N8",
  /** N80 */
  N80 = "N80",
  /** N801 */
  N801 = "N801",
  /** N802 */
  N802 = "N802",
  /** N803 */
  N803 = "N803",
  /** N804 */
  N804 = "N804",
  /** N805 */
  N805 = "N805",
  /** N806 */
  N806 = "N806",
  /** N807 */
  N807 = "N807",
  /** N81 */
  N81 = "N81",
  /** N811 */
  N811 = "N811",
  /** N812 */
  N812 = "N812",
  /** N813 */
  N813 = "N813",
  /** N814 */
  N814 = "N814",
  /** N815 */
  N815 = "N815",
  /** N816 */
  N816 = "N816",
  /** N817 */
  N817 = "N817",
  /** N818 */
  N818 = "N818",
  /** N9 */
  N9 = "N9",
  /** N99 */
  N99 = "N99",
  /** N999 */
  N999 = "N999",
  /** NPY */
  NPY = "NPY",
  /** NPY0 */
  NPY0 = "NPY0",
  /** NPY00 */
  NPY00 = "NPY00",
  /** NPY001 */
  NPY001 = "NPY001",
  /** NPY002 */
  NPY002 = "NPY002",
  /** NPY003 */
  NPY003 = "NPY003",
  /** NPY2 */
  NPY2 = "NPY2",
  /** NPY20 */
  NPY20 = "NPY20",
  /** NPY201 */
  NPY201 = "NPY201",
  /** PD */
  PD = "PD",
  /** PD0 */
  PD0 = "PD0",
  /** PD00 */
  PD00 = "PD00",
  /** PD002 */
  PD002 = "PD002",
  /** PD003 */
  PD003 = "PD003",
  /** PD004 */
  PD004 = "PD004",
  /** PD007 */
  PD007 = "PD007",
  /** PD008 */
  PD008 = "PD008",
  /** PD009 */
  PD009 = "PD009",
  /** PD01 */
  PD01 = "PD01",
  /** PD010 */
  PD010 = "PD010",
  /** PD011 */
  PD011 = "PD011",
  /** PD012 */
  PD012 = "PD012",
  /** PD013 */
  PD013 = "PD013",
  /** PD015 */
  PD015 = "PD015",
  /** PD1 */
  PD1 = "PD1",
  /** PD10 */
  PD10 = "PD10",
  /** PD101 */
  PD101 = "PD101",
  /** PERF */
  PERF = "PERF",
  /** PERF1 */
  PERF1 = "PERF1",
  /** PERF10 */
  PERF10 = "PERF10",
  /** PERF101 */
  PERF101 = "PERF101",
  /** PERF102 */
  PERF102 = "PERF102",
  /** PERF2 */
  PERF2 = "PERF2",
  /** PERF20 */
  PERF20 = "PERF20",
  /** PERF203 */
  PERF203 = "PERF203",
  /** PERF4 */
  PERF4 = "PERF4",
  /** PERF40 */
  PERF40 = "PERF40",
  /** PERF401 */
  PERF401 = "PERF401",
  /** PERF402 */
  PERF402 = "PERF402",
  /** PERF403 */
  PERF403 = "PERF403",
  /** PGH */
  PGH = "PGH",
  /** PGH0 */
  PGH0 = "PGH0",
  /** PGH00 */
  PGH00 = "PGH00",
  /** PGH003 */
  PGH003 = "PGH003",
  /** PGH004 */
  PGH004 = "PGH004",
  /** PGH005 */
  PGH005 = "PGH005",
  /** PIE */
  PIE = "PIE",
  /** PIE7 */
  PIE7 = "PIE7",
  /** PIE79 */
  PIE79 = "PIE79",
  /** PIE790 */
  PIE790 = "PIE790",
  /** PIE794 */
  PIE794 = "PIE794",
  /** PIE796 */
  PIE796 = "PIE796",
  /** PIE8 */
  PIE8 = "PIE8",
  /** PIE80 */
  PIE80 = "PIE80",
  /** PIE800 */
  PIE800 = "PIE800",
  /** PIE804 */
  PIE804 = "PIE804",
  /** PIE807 */
  PIE807 = "PIE807",
  /** PIE808 */
  PIE808 = "PIE808",
  /** PIE81 */
  PIE81 = "PIE81",
  /** PIE810 */
  PIE810 = "PIE810",
  /** PL */
  PL = "PL",
  /** PLC */
  PLC = "PLC",
  /** PLC0 */
  PLC0 = "PLC0",
  /** PLC01 */
  PLC01 = "PLC01",
  /** PLC010 */
  PLC010 = "PLC010",
  /** PLC0105 */
  PLC0105 = "PLC0105",
  /** PLC013 */
  PLC013 = "PLC013",
  /** PLC0131 */
  PLC0131 = "PLC0131",
  /** PLC0132 */
  PLC0132 = "PLC0132",
  /** PLC02 */
  PLC02 = "PLC02",
  /** PLC020 */
  PLC020 = "PLC020",
  /** PLC0205 */
  PLC0205 = "PLC0205",
  /** PLC0206 */
  PLC0206 = "PLC0206",
  /** PLC0207 */
  PLC0207 = "PLC0207",
  /** PLC0208 */
  PLC0208 = "PLC0208",
  /** PLC04 */
  PLC04 = "PLC04",
  /** PLC041 */
  PLC041 = "PLC041",
  /** PLC0414 */
  PLC0414 = "PLC0414",
  /** PLC0415 */
  PLC0415 = "PLC0415",
  /** PLC1 */
  PLC1 = "PLC1",
  /** PLC18 */
  PLC18 = "PLC18",
  /** PLC180 */
  PLC180 = "PLC180",
  /** PLC1802 */
  PLC1802 = "PLC1802",
  /** PLC19 */
  PLC19 = "PLC19",
  /** PLC190 */
  PLC190 = "PLC190",
  /** PLC1901 */
  PLC1901 = "PLC1901",
  /** PLC2 */
  PLC2 = "PLC2",
  /** PLC24 */
  PLC24 = "PLC24",
  /** PLC240 */
  PLC240 = "PLC240",
  /** PLC2401 */
  PLC2401 = "PLC2401",
  /** PLC2403 */
  PLC2403 = "PLC2403",
  /** PLC27 */
  PLC27 = "PLC27",
  /** PLC270 */
  PLC270 = "PLC270",
  /** PLC2701 */
  PLC2701 = "PLC2701",
  /** PLC28 */
  PLC28 = "PLC28",
  /** PLC280 */
  PLC280 = "PLC280",
  /** PLC2801 */
  PLC2801 = "PLC2801",
  /** PLC3 */
  PLC3 = "PLC3",
  /** PLC30 */
  PLC30 = "PLC30",
  /** PLC300 */
  PLC300 = "PLC300",
  /** PLC3002 */
  PLC3002 = "PLC3002",
  /** PLE */
  PLE = "PLE",
  /** PLE0 */
  PLE0 = "PLE0",
  /** PLE01 */
  PLE01 = "PLE01",
  /** PLE010 */
  PLE010 = "PLE010",
  /** PLE0100 */
  PLE0100 = "PLE0100",
  /** PLE0101 */
  PLE0101 = "PLE0101",
  /** PLE011 */
  PLE011 = "PLE011",
  /** PLE0115 */
  PLE0115 = "PLE0115",
  /** PLE0116 */
  PLE0116 = "PLE0116",
  /** PLE0117 */
  PLE0117 = "PLE0117",
  /** PLE0118 */
  PLE0118 = "PLE0118",
  /** PLE02 */
  PLE02 = "PLE02",
  /** PLE023 */
  PLE023 = "PLE023",
  /** PLE0237 */
  PLE0237 = "PLE0237",
  /** PLE024 */
  PLE024 = "PLE024",
  /** PLE0241 */
  PLE0241 = "PLE0241",
  /** PLE03 */
  PLE03 = "PLE03",
  /** PLE030 */
  PLE030 = "PLE030",
  /** PLE0302 */
  PLE0302 = "PLE0302",
  /** PLE0303 */
  PLE0303 = "PLE0303",
  /** PLE0304 */
  PLE0304 = "PLE0304",
  /** PLE0305 */
  PLE0305 = "PLE0305",
  /** PLE0307 */
  PLE0307 = "PLE0307",
  /** PLE0308 */
  PLE0308 = "PLE0308",
  /** PLE0309 */
  PLE0309 = "PLE0309",
  /** PLE06 */
  PLE06 = "PLE06",
  /** PLE060 */
  PLE060 = "PLE060",
  /** PLE0604 */
  PLE0604 = "PLE0604",
  /** PLE0605 */
  PLE0605 = "PLE0605",
  /** PLE064 */
  PLE064 = "PLE064",
  /** PLE0643 */
  PLE0643 = "PLE0643",
  /** PLE07 */
  PLE07 = "PLE07",
  /** PLE070 */
  PLE070 = "PLE070",
  /** PLE0704 */
  PLE0704 = "PLE0704",
  /** PLE1 */
  PLE1 = "PLE1",
  /** PLE11 */
  PLE11 = "PLE11",
  /** PLE113 */
  PLE113 = "PLE113",
  /** PLE1132 */
  PLE1132 = "PLE1132",
  /** PLE114 */
  PLE114 = "PLE114",
  /** PLE1141 */
  PLE1141 = "PLE1141",
  /** PLE1142 */
  PLE1142 = "PLE1142",
  /** PLE12 */
  PLE12 = "PLE12",
  /** PLE120 */
  PLE120 = "PLE120",
  /** PLE1205 */
  PLE1205 = "PLE1205",
  /** PLE1206 */
  PLE1206 = "PLE1206",
  /** PLE13 */
  PLE13 = "PLE13",
  /** PLE130 */
  PLE130 = "PLE130",
  /** PLE1300 */
  PLE1300 = "PLE1300",
  /** PLE1307 */
  PLE1307 = "PLE1307",
  /** PLE131 */
  PLE131 = "PLE131",
  /** PLE1310 */
  PLE1310 = "PLE1310",
  /** PLE15 */
  PLE15 = "PLE15",
  /** PLE150 */
  PLE150 = "PLE150",
  /** PLE1507 */
  PLE1507 = "PLE1507",
  /** PLE151 */
  PLE151 = "PLE151",
  /** PLE1519 */
  PLE1519 = "PLE1519",
  /** PLE152 */
  PLE152 = "PLE152",
  /** PLE1520 */
  PLE1520 = "PLE1520",
  /** PLE17 */
  PLE17 = "PLE17",
  /** PLE170 */
  PLE170 = "PLE170",
  /** PLE1700 */
  PLE1700 = "PLE1700",
  /** PLE2 */
  PLE2 = "PLE2",
  /** PLE25 */
  PLE25 = "PLE25",
  /** PLE250 */
  PLE250 = "PLE250",
  /** PLE2502 */
  PLE2502 = "PLE2502",
  /** PLE251 */
  PLE251 = "PLE251",
  /** PLE2510 */
  PLE2510 = "PLE2510",
  /** PLE2512 */
  PLE2512 = "PLE2512",
  /** PLE2513 */
  PLE2513 = "PLE2513",
  /** PLE2514 */
  PLE2514 = "PLE2514",
  /** PLE2515 */
  PLE2515 = "PLE2515",
  /** PLE4 */
  PLE4 = "PLE4",
  /** PLE47 */
  PLE47 = "PLE47",
  /** PLE470 */
  PLE470 = "PLE470",
  /** PLE4703 */
  PLE4703 = "PLE4703",
  /** PLR */
  PLR = "PLR",
  /** PLR0 */
  PLR0 = "PLR0",
  /** PLR01 */
  PLR01 = "PLR01",
  /** PLR012 */
  PLR012 = "PLR012",
  /** PLR0124 */
  PLR0124 = "PLR0124",
  /** PLR013 */
  PLR013 = "PLR013",
  /** PLR0133 */
  PLR0133 = "PLR0133",
  /** PLR02 */
  PLR02 = "PLR02",
  /** PLR020 */
  PLR020 = "PLR020",
  /** PLR0202 */
  PLR0202 = "PLR0202",
  /** PLR0203 */
  PLR0203 = "PLR0203",
  /** PLR0206 */
  PLR0206 = "PLR0206",
  /** PLR04 */
  PLR04 = "PLR04",
  /** PLR040 */
  PLR040 = "PLR040",
  /** PLR0402 */
  PLR0402 = "PLR0402",
  /** PLR09 */
  PLR09 = "PLR09",
  /** PLR090 */
  PLR090 = "PLR090",
  /** PLR0904 */
  PLR0904 = "PLR0904",
  /** PLR091 */
  PLR091 = "PLR091",
  /** PLR0911 */
  PLR0911 = "PLR0911",
  /** PLR0912 */
  PLR0912 = "PLR0912",
  /** PLR0913 */
  PLR0913 = "PLR0913",
  /** PLR0914 */
  PLR0914 = "PLR0914",
  /** PLR0915 */
  PLR0915 = "PLR0915",
  /** PLR0916 */
  PLR0916 = "PLR0916",
  /** PLR0917 */
  PLR0917 = "PLR0917",
  /** PLR1 */
  PLR1 = "PLR1",
  /** PLR17 */
  PLR17 = "PLR17",
  /** PLR170 */
  PLR170 = "PLR170",
  /** PLR1702 */
  PLR1702 = "PLR1702",
  /** PLR1704 */
  PLR1704 = "PLR1704",
  /** PLR1708 */
  PLR1708 = "PLR1708",
  /** PLR171 */
  PLR171 = "PLR171",
  /** PLR1711 */
  PLR1711 = "PLR1711",
  /** PLR1714 */
  PLR1714 = "PLR1714",
  /** PLR1716 */
  PLR1716 = "PLR1716",
  /** PLR172 */
  PLR172 = "PLR172",
  /** PLR1722 */
  PLR1722 = "PLR1722",
  /** PLR173 */
  PLR173 = "PLR173",
  /** PLR1730 */
  PLR1730 = "PLR1730",
  /** PLR1733 */
  PLR1733 = "PLR1733",
  /** PLR1736 */
  PLR1736 = "PLR1736",
  /** PLR2 */
  PLR2 = "PLR2",
  /** PLR20 */
  PLR20 = "PLR20",
  /** PLR200 */
  PLR200 = "PLR200",
  /** PLR2004 */
  PLR2004 = "PLR2004",
  /** PLR204 */
  PLR204 = "PLR204",
  /** PLR2044 */
  PLR2044 = "PLR2044",
  /** PLR5 */
  PLR5 = "PLR5",
  /** PLR55 */
  PLR55 = "PLR55",
  /** PLR550 */
  PLR550 = "PLR550",
  /** PLR5501 */
  PLR5501 = "PLR5501",
  /** PLR6 */
  PLR6 = "PLR6",
  /** PLR61 */
  PLR61 = "PLR61",
  /** PLR610 */
  PLR610 = "PLR610",
  /** PLR6104 */
  PLR6104 = "PLR6104",
  /** PLR62 */
  PLR62 = "PLR62",
  /** PLR620 */
  PLR620 = "PLR620",
  /** PLR6201 */
  PLR6201 = "PLR6201",
  /** PLR63 */
  PLR63 = "PLR63",
  /** PLR630 */
  PLR630 = "PLR630",
  /** PLR6301 */
  PLR6301 = "PLR6301",
  /** PLW */
  PLW = "PLW",
  /** PLW0 */
  PLW0 = "PLW0",
  /** PLW01 */
  PLW01 = "PLW01",
  /** PLW010 */
  PLW010 = "PLW010",
  /** PLW0108 */
  PLW0108 = "PLW0108",
  /** PLW012 */
  PLW012 = "PLW012",
  /** PLW0120 */
  PLW0120 = "PLW0120",
  /** PLW0127 */
  PLW0127 = "PLW0127",
  /** PLW0128 */
  PLW0128 = "PLW0128",
  /** PLW0129 */
  PLW0129 = "PLW0129",
  /** PLW013 */
  PLW013 = "PLW013",
  /** PLW0131 */
  PLW0131 = "PLW0131",
  /** PLW0133 */
  PLW0133 = "PLW0133",
  /** PLW017 */
  PLW017 = "PLW017",
  /** PLW0177 */
  PLW0177 = "PLW0177",
  /** PLW02 */
  PLW02 = "PLW02",
  /** PLW021 */
  PLW021 = "PLW021",
  /** PLW0211 */
  PLW0211 = "PLW0211",
  /** PLW024 */
  PLW024 = "PLW024",
  /** PLW0244 */
  PLW0244 = "PLW0244",
  /** PLW0245 */
  PLW0245 = "PLW0245",
  /** PLW04 */
  PLW04 = "PLW04",
  /** PLW040 */
  PLW040 = "PLW040",
  /** PLW0406 */
  PLW0406 = "PLW0406",
  /** PLW06 */
  PLW06 = "PLW06",
  /** PLW060 */
  PLW060 = "PLW060",
  /** PLW0602 */
  PLW0602 = "PLW0602",
  /** PLW0603 */
  PLW0603 = "PLW0603",
  /** PLW0604 */
  PLW0604 = "PLW0604",
  /** PLW064 */
  PLW064 = "PLW064",
  /** PLW0642 */
  PLW0642 = "PLW0642",
  /** PLW07 */
  PLW07 = "PLW07",
  /** PLW071 */
  PLW071 = "PLW071",
  /** PLW0711 */
  PLW0711 = "PLW0711",
  /** PLW1 */
  PLW1 = "PLW1",
  /** PLW15 */
  PLW15 = "PLW15",
  /** PLW150 */
  PLW150 = "PLW150",
  /** PLW1501 */
  PLW1501 = "PLW1501",
  /** PLW1507 */
  PLW1507 = "PLW1507",
  /** PLW1508 */
  PLW1508 = "PLW1508",
  /** PLW1509 */
  PLW1509 = "PLW1509",
  /** PLW151 */
  PLW151 = "PLW151",
  /** PLW1510 */
  PLW1510 = "PLW1510",
  /** PLW1514 */
  PLW1514 = "PLW1514",
  /** PLW16 */
  PLW16 = "PLW16",
  /** PLW164 */
  PLW164 = "PLW164",
  /** PLW1641 */
  PLW1641 = "PLW1641",
  /** PLW2 */
  PLW2 = "PLW2",
  /** PLW21 */
  PLW21 = "PLW21",
  /** PLW210 */
  PLW210 = "PLW210",
  /** PLW2101 */
  PLW2101 = "PLW2101",
  /** PLW29 */
  PLW29 = "PLW29",
  /** PLW290 */
  PLW290 = "PLW290",
  /** PLW2901 */
  PLW2901 = "PLW2901",
  /** PLW3 */
  PLW3 = "PLW3",
  /** PLW32 */
  PLW32 = "PLW32",
  /** PLW320 */
  PLW320 = "PLW320",
  /** PLW3201 */
  PLW3201 = "PLW3201",
  /** PLW33 */
  PLW33 = "PLW33",
  /** PLW330 */
  PLW330 = "PLW330",
  /** PLW3301 */
  PLW3301 = "PLW3301",
  /** PT */
  PT = "PT",
  /** PT0 */
  PT0 = "PT0",
  /** PT00 */
  PT00 = "PT00",
  /** PT001 */
  PT001 = "PT001",
  /** PT002 */
  PT002 = "PT002",
  /** PT003 */
  PT003 = "PT003",
  /** PT006 */
  PT006 = "PT006",
  /** PT007 */
  PT007 = "PT007",
  /** PT008 */
  PT008 = "PT008",
  /** PT009 */
  PT009 = "PT009",
  /** PT01 */
  PT01 = "PT01",
  /** PT010 */
  PT010 = "PT010",
  /** PT011 */
  PT011 = "PT011",
  /** PT012 */
  PT012 = "PT012",
  /** PT013 */
  PT013 = "PT013",
  /** PT014 */
  PT014 = "PT014",
  /** PT015 */
  PT015 = "PT015",
  /** PT016 */
  PT016 = "PT016",
  /** PT017 */
  PT017 = "PT017",
  /** PT018 */
  PT018 = "PT018",
  /** PT019 */
  PT019 = "PT019",
  /** PT02 */
  PT02 = "PT02",
  /** PT020 */
  PT020 = "PT020",
  /** PT021 */
  PT021 = "PT021",
  /** PT022 */
  PT022 = "PT022",
  /** PT023 */
  PT023 = "PT023",
  /** PT024 */
  PT024 = "PT024",
  /** PT025 */
  PT025 = "PT025",
  /** PT026 */
  PT026 = "PT026",
  /** PT027 */
  PT027 = "PT027",
  /** PT028 */
  PT028 = "PT028",
  /** PT029 */
  PT029 = "PT029",
  /** PT03 */
  PT03 = "PT03",
  /** PT030 */
  PT030 = "PT030",
  /** PT031 */
  PT031 = "PT031",
  /** PTH */
  PTH = "PTH",
  /** PTH1 */
  PTH1 = "PTH1",
  /** PTH10 */
  PTH10 = "PTH10",
  /** PTH100 */
  PTH100 = "PTH100",
  /** PTH101 */
  PTH101 = "PTH101",
  /** PTH102 */
  PTH102 = "PTH102",
  /** PTH103 */
  PTH103 = "PTH103",
  /** PTH104 */
  PTH104 = "PTH104",
  /** PTH105 */
  PTH105 = "PTH105",
  /** PTH106 */
  PTH106 = "PTH106",
  /** PTH107 */
  PTH107 = "PTH107",
  /** PTH108 */
  PTH108 = "PTH108",
  /** PTH109 */
  PTH109 = "PTH109",
  /** PTH11 */
  PTH11 = "PTH11",
  /** PTH110 */
  PTH110 = "PTH110",
  /** PTH111 */
  PTH111 = "PTH111",
  /** PTH112 */
  PTH112 = "PTH112",
  /** PTH113 */
  PTH113 = "PTH113",
  /** PTH114 */
  PTH114 = "PTH114",
  /** PTH115 */
  PTH115 = "PTH115",
  /** PTH116 */
  PTH116 = "PTH116",
  /** PTH117 */
  PTH117 = "PTH117",
  /** PTH118 */
  PTH118 = "PTH118",
  /** PTH119 */
  PTH119 = "PTH119",
  /** PTH12 */
  PTH12 = "PTH12",
  /** PTH120 */
  PTH120 = "PTH120",
  /** PTH121 */
  PTH121 = "PTH121",
  /** PTH122 */
  PTH122 = "PTH122",
  /** PTH123 */
  PTH123 = "PTH123",
  /** PTH124 */
  PTH124 = "PTH124",
  /** PTH2 */
  PTH2 = "PTH2",
  /** PTH20 */
  PTH20 = "PTH20",
  /** PTH201 */
  PTH201 = "PTH201",
  /** PTH202 */
  PTH202 = "PTH202",
  /** PTH203 */
  PTH203 = "PTH203",
  /** PTH204 */
  PTH204 = "PTH204",
  /** PTH205 */
  PTH205 = "PTH205",
  /** PTH206 */
  PTH206 = "PTH206",
  /** PTH207 */
  PTH207 = "PTH207",
  /** PTH208 */
  PTH208 = "PTH208",
  /** PTH21 */
  PTH21 = "PTH21",
  /** PTH210 */
  PTH210 = "PTH210",
  /** PTH211 */
  PTH211 = "PTH211",
  /** PYI */
  PYI = "PYI",
  /** PYI0 */
  PYI0 = "PYI0",
  /** PYI00 */
  PYI00 = "PYI00",
  /** PYI001 */
  PYI001 = "PYI001",
  /** PYI002 */
  PYI002 = "PYI002",
  /** PYI003 */
  PYI003 = "PYI003",
  /** PYI004 */
  PYI004 = "PYI004",
  /** PYI005 */
  PYI005 = "PYI005",
  /** PYI006 */
  PYI006 = "PYI006",
  /** PYI007 */
  PYI007 = "PYI007",
  /** PYI008 */
  PYI008 = "PYI008",
  /** PYI009 */
  PYI009 = "PYI009",
  /** PYI01 */
  PYI01 = "PYI01",
  /** PYI010 */
  PYI010 = "PYI010",
  /** PYI011 */
  PYI011 = "PYI011",
  /** PYI012 */
  PYI012 = "PYI012",
  /** PYI013 */
  PYI013 = "PYI013",
  /** PYI014 */
  PYI014 = "PYI014",
  /** PYI015 */
  PYI015 = "PYI015",
  /** PYI016 */
  PYI016 = "PYI016",
  /** PYI017 */
  PYI017 = "PYI017",
  /** PYI018 */
  PYI018 = "PYI018",
  /** PYI019 */
  PYI019 = "PYI019",
  /** PYI02 */
  PYI02 = "PYI02",
  /** PYI020 */
  PYI020 = "PYI020",
  /** PYI021 */
  PYI021 = "PYI021",
  /** PYI024 */
  PYI024 = "PYI024",
  /** PYI025 */
  PYI025 = "PYI025",
  /** PYI026 */
  PYI026 = "PYI026",
  /** PYI029 */
  PYI029 = "PYI029",
  /** PYI03 */
  PYI03 = "PYI03",
  /** PYI030 */
  PYI030 = "PYI030",
  /** PYI032 */
  PYI032 = "PYI032",
  /** PYI033 */
  PYI033 = "PYI033",
  /** PYI034 */
  PYI034 = "PYI034",
  /** PYI035 */
  PYI035 = "PYI035",
  /** PYI036 */
  PYI036 = "PYI036",
  /** PYI04 */
  PYI04 = "PYI04",
  /** PYI041 */
  PYI041 = "PYI041",
  /** PYI042 */
  PYI042 = "PYI042",
  /** PYI043 */
  PYI043 = "PYI043",
  /** PYI044 */
  PYI044 = "PYI044",
  /** PYI045 */
  PYI045 = "PYI045",
  /** PYI046 */
  PYI046 = "PYI046",
  /** PYI047 */
  PYI047 = "PYI047",
  /** PYI048 */
  PYI048 = "PYI048",
  /** PYI049 */
  PYI049 = "PYI049",
  /** PYI05 */
  PYI05 = "PYI05",
  /** PYI050 */
  PYI050 = "PYI050",
  /** PYI051 */
  PYI051 = "PYI051",
  /** PYI052 */
  PYI052 = "PYI052",
  /** PYI053 */
  PYI053 = "PYI053",
  /** PYI054 */
  PYI054 = "PYI054",
  /** PYI055 */
  PYI055 = "PYI055",
  /** PYI056 */
  PYI056 = "PYI056",
  /** PYI057 */
  PYI057 = "PYI057",
  /** PYI058 */
  PYI058 = "PYI058",
  /** PYI059 */
  PYI059 = "PYI059",
  /** PYI06 */
  PYI06 = "PYI06",
  /** PYI061 */
  PYI061 = "PYI061",
  /** PYI062 */
  PYI062 = "PYI062",
  /** PYI063 */
  PYI063 = "PYI063",
  /** PYI064 */
  PYI064 = "PYI064",
  /** PYI066 */
  PYI066 = "PYI066",
  /** Q */
  Q = "Q",
  /** Q0 */
  Q0 = "Q0",
  /** Q00 */
  Q00 = "Q00",
  /** Q000 */
  Q000 = "Q000",
  /** Q001 */
  Q001 = "Q001",
  /** Q002 */
  Q002 = "Q002",
  /** Q003 */
  Q003 = "Q003",
  /** Q004 */
  Q004 = "Q004",
  /** RET */
  RET = "RET",
  /** RET5 */
  RET5 = "RET5",
  /** RET50 */
  RET50 = "RET50",
  /** RET501 */
  RET501 = "RET501",
  /** RET502 */
  RET502 = "RET502",
  /** RET503 */
  RET503 = "RET503",
  /** RET504 */
  RET504 = "RET504",
  /** RET505 */
  RET505 = "RET505",
  /** RET506 */
  RET506 = "RET506",
  /** RET507 */
  RET507 = "RET507",
  /** RET508 */
  RET508 = "RET508",
  /** RSE */
  RSE = "RSE",
  /** RSE1 */
  RSE1 = "RSE1",
  /** RSE10 */
  RSE10 = "RSE10",
  /** RSE102 */
  RSE102 = "RSE102",
  /** RUF */
  RUF = "RUF",
  /** RUF0 */
  RUF0 = "RUF0",
  /** RUF00 */
  RUF00 = "RUF00",
  /** RUF001 */
  RUF001 = "RUF001",
  /** RUF002 */
  RUF002 = "RUF002",
  /** RUF003 */
  RUF003 = "RUF003",
  /** RUF005 */
  RUF005 = "RUF005",
  /** RUF006 */
  RUF006 = "RUF006",
  /** RUF007 */
  RUF007 = "RUF007",
  /** RUF008 */
  RUF008 = "RUF008",
  /** RUF009 */
  RUF009 = "RUF009",
  /** RUF01 */
  RUF01 = "RUF01",
  /** RUF010 */
  RUF010 = "RUF010",
  /** RUF012 */
  RUF012 = "RUF012",
  /** RUF013 */
  RUF013 = "RUF013",
  /** RUF015 */
  RUF015 = "RUF015",
  /** RUF016 */
  RUF016 = "RUF016",
  /** RUF017 */
  RUF017 = "RUF017",
  /** RUF018 */
  RUF018 = "RUF018",
  /** RUF019 */
  RUF019 = "RUF019",
  /** RUF02 */
  RUF02 = "RUF02",
  /** RUF020 */
  RUF020 = "RUF020",
  /** RUF021 */
  RUF021 = "RUF021",
  /** RUF022 */
  RUF022 = "RUF022",
  /** RUF023 */
  RUF023 = "RUF023",
  /** RUF024 */
  RUF024 = "RUF024",
  /** RUF026 */
  RUF026 = "RUF026",
  /** RUF027 */
  RUF027 = "RUF027",
  /** RUF028 */
  RUF028 = "RUF028",
  /** RUF029 */
  RUF029 = "RUF029",
  /** RUF03 */
  RUF03 = "RUF03",
  /** RUF030 */
  RUF030 = "RUF030",
  /** RUF031 */
  RUF031 = "RUF031",
  /** RUF032 */
  RUF032 = "RUF032",
  /** RUF033 */
  RUF033 = "RUF033",
  /** RUF034 */
  RUF034 = "RUF034",
  /** RUF036 */
  RUF036 = "RUF036",
  /** RUF037 */
  RUF037 = "RUF037",
  /** RUF038 */
  RUF038 = "RUF038",
  /** RUF039 */
  RUF039 = "RUF039",
  /** RUF04 */
  RUF04 = "RUF04",
  /** RUF040 */
  RUF040 = "RUF040",
  /** RUF041 */
  RUF041 = "RUF041",
  /** RUF043 */
  RUF043 = "RUF043",
  /** RUF045 */
  RUF045 = "RUF045",
  /** RUF046 */
  RUF046 = "RUF046",
  /** RUF047 */
  RUF047 = "RUF047",
  /** RUF048 */
  RUF048 = "RUF048",
  /** RUF049 */
  RUF049 = "RUF049",
  /** RUF05 */
  RUF05 = "RUF05",
  /** RUF051 */
  RUF051 = "RUF051",
  /** RUF052 */
  RUF052 = "RUF052",
  /** RUF053 */
  RUF053 = "RUF053",
  /** RUF054 */
  RUF054 = "RUF054",
  /** RUF055 */
  RUF055 = "RUF055",
  /** RUF056 */
  RUF056 = "RUF056",
  /** RUF057 */
  RUF057 = "RUF057",
  /** RUF058 */
  RUF058 = "RUF058",
  /** RUF059 */
  RUF059 = "RUF059",
  /** RUF06 */
  RUF06 = "RUF06",
  /** RUF060 */
  RUF060 = "RUF060",
  /** RUF061 */
  RUF061 = "RUF061",
  /** RUF063 */
  RUF063 = "RUF063",
  /** RUF064 */
  RUF064 = "RUF064",
  /** RUF065 */
  RUF065 = "RUF065",
  /** RUF1 */
  RUF1 = "RUF1",
  /** RUF10 */
  RUF10 = "RUF10",
  /** RUF100 */
  RUF100 = "RUF100",
  /** RUF101 */
  RUF101 = "RUF101",
  /** RUF102 */
  RUF102 = "RUF102",
  /** RUF2 */
  RUF2 = "RUF2",
  /** RUF20 */
  RUF20 = "RUF20",
  /** RUF200 */
  RUF200 = "RUF200",
  /** S */
  S = "S",
  /** S1 */
  S1 = "S1",
  /** S10 */
  S10 = "S10",
  /** S101 */
  S101 = "S101",
  /** S102 */
  S102 = "S102",
  /** S103 */
  S103 = "S103",
  /** S104 */
  S104 = "S104",
  /** S105 */
  S105 = "S105",
  /** S106 */
  S106 = "S106",
  /** S107 */
  S107 = "S107",
  /** S108 */
  S108 = "S108",
  /** S11 */
  S11 = "S11",
  /** S110 */
  S110 = "S110",
  /** S112 */
  S112 = "S112",
  /** S113 */
  S113 = "S113",
  /** S2 */
  S2 = "S2",
  /** S20 */
  S20 = "S20",
  /** S201 */
  S201 = "S201",
  /** S202 */
  S202 = "S202",
  /** S3 */
  S3 = "S3",
  /** S30 */
  S30 = "S30",
  /** S301 */
  S301 = "S301",
  /** S302 */
  S302 = "S302",
  /** S303 */
  S303 = "S303",
  /** S304 */
  S304 = "S304",
  /** S305 */
  S305 = "S305",
  /** S306 */
  S306 = "S306",
  /** S307 */
  S307 = "S307",
  /** S308 */
  S308 = "S308",
  /** S31 */
  S31 = "S31",
  /** S310 */
  S310 = "S310",
  /** S311 */
  S311 = "S311",
  /** S312 */
  S312 = "S312",
  /** S313 */
  S313 = "S313",
  /** S314 */
  S314 = "S314",
  /** S315 */
  S315 = "S315",
  /** S316 */
  S316 = "S316",
  /** S317 */
  S317 = "S317",
  /** S318 */
  S318 = "S318",
  /** S319 */
  S319 = "S319",
  /** S32 */
  S32 = "S32",
  /** S321 */
  S321 = "S321",
  /** S323 */
  S323 = "S323",
  /** S324 */
  S324 = "S324",
  /** S4 */
  S4 = "S4",
  /** S40 */
  S40 = "S40",
  /** S401 */
  S401 = "S401",
  /** S402 */
  S402 = "S402",
  /** S403 */
  S403 = "S403",
  /** S404 */
  S404 = "S404",
  /** S405 */
  S405 = "S405",
  /** S406 */
  S406 = "S406",
  /** S407 */
  S407 = "S407",
  /** S408 */
  S408 = "S408",
  /** S409 */
  S409 = "S409",
  /** S41 */
  S41 = "S41",
  /** S411 */
  S411 = "S411",
  /** S412 */
  S412 = "S412",
  /** S413 */
  S413 = "S413",
  /** S415 */
  S415 = "S415",
  /** S5 */
  S5 = "S5",
  /** S50 */
  S50 = "S50",
  /** S501 */
  S501 = "S501",
  /** S502 */
  S502 = "S502",
  /** S503 */
  S503 = "S503",
  /** S504 */
  S504 = "S504",
  /** S505 */
  S505 = "S505",
  /** S506 */
  S506 = "S506",
  /** S507 */
  S507 = "S507",
  /** S508 */
  S508 = "S508",
  /** S509 */
  S509 = "S509",
  /** S6 */
  S6 = "S6",
  /** S60 */
  S60 = "S60",
  /** S601 */
  S601 = "S601",
  /** S602 */
  S602 = "S602",
  /** S603 */
  S603 = "S603",
  /** S604 */
  S604 = "S604",
  /** S605 */
  S605 = "S605",
  /** S606 */
  S606 = "S606",
  /** S607 */
  S607 = "S607",
  /** S608 */
  S608 = "S608",
  /** S609 */
  S609 = "S609",
  /** S61 */
  S61 = "S61",
  /** S610 */
  S610 = "S610",
  /** S611 */
  S611 = "S611",
  /** S612 */
  S612 = "S612",
  /** S7 */
  S7 = "S7",
  /** S70 */
  S70 = "S70",
  /** S701 */
  S701 = "S701",
  /** S702 */
  S702 = "S702",
  /** S704 */
  S704 = "S704",
  /** SIM */
  SIM = "SIM",
  /** SIM1 */
  SIM1 = "SIM1",
  /** SIM10 */
  SIM10 = "SIM10",
  /** SIM101 */
  SIM101 = "SIM101",
  /** SIM102 */
  SIM102 = "SIM102",
  /** SIM103 */
  SIM103 = "SIM103",
  /** SIM105 */
  SIM105 = "SIM105",
  /** SIM107 */
  SIM107 = "SIM107",
  /** SIM108 */
  SIM108 = "SIM108",
  /** SIM109 */
  SIM109 = "SIM109",
  /** SIM11 */
  SIM11 = "SIM11",
  /** SIM110 */
  SIM110 = "SIM110",
  /** SIM112 */
  SIM112 = "SIM112",
  /** SIM113 */
  SIM113 = "SIM113",
  /** SIM114 */
  SIM114 = "SIM114",
  /** SIM115 */
  SIM115 = "SIM115",
  /** SIM116 */
  SIM116 = "SIM116",
  /** SIM117 */
  SIM117 = "SIM117",
  /** SIM118 */
  SIM118 = "SIM118",
  /** SIM2 */
  SIM2 = "SIM2",
  /** SIM20 */
  SIM20 = "SIM20",
  /** SIM201 */
  SIM201 = "SIM201",
  /** SIM202 */
  SIM202 = "SIM202",
  /** SIM208 */
  SIM208 = "SIM208",
  /** SIM21 */
  SIM21 = "SIM21",
  /** SIM210 */
  SIM210 = "SIM210",
  /** SIM211 */
  SIM211 = "SIM211",
  /** SIM212 */
  SIM212 = "SIM212",
  /** SIM22 */
  SIM22 = "SIM22",
  /** SIM220 */
  SIM220 = "SIM220",
  /** SIM221 */
  SIM221 = "SIM221",
  /** SIM222 */
  SIM222 = "SIM222",
  /** SIM223 */
  SIM223 = "SIM223",
  /** SIM3 */
  SIM3 = "SIM3",
  /** SIM30 */
  SIM30 = "SIM30",
  /** SIM300 */
  SIM300 = "SIM300",
  /** SIM4 */
  SIM4 = "SIM4",
  /** SIM40 */
  SIM40 = "SIM40",
  /** SIM401 */
  SIM401 = "SIM401",
  /** SIM9 */
  SIM9 = "SIM9",
  /** SIM90 */
  SIM90 = "SIM90",
  /** SIM905 */
  SIM905 = "SIM905",
  /** SIM91 */
  SIM91 = "SIM91",
  /** SIM910 */
  SIM910 = "SIM910",
  /** SIM911 */
  SIM911 = "SIM911",
  /** SLF */
  SLF = "SLF",
  /** SLF0 */
  SLF0 = "SLF0",
  /** SLF00 */
  SLF00 = "SLF00",
  /** SLF001 */
  SLF001 = "SLF001",
  /** SLOT */
  SLOT = "SLOT",
  /** SLOT0 */
  SLOT0 = "SLOT0",
  /** SLOT00 */
  SLOT00 = "SLOT00",
  /** SLOT000 */
  SLOT000 = "SLOT000",
  /** SLOT001 */
  SLOT001 = "SLOT001",
  /** SLOT002 */
  SLOT002 = "SLOT002",
  /** T */
  T = "T",
  /** T1 */
  T1 = "T1",
  /** T10 */
  T10 = "T10",
  /** T100 */
  T100 = "T100",
  /** T2 */
  T2 = "T2",
  /** T20 */
  T20 = "T20",
  /** T201 */
  T201 = "T201",
  /** T203 */
  T203 = "T203",
  /** TC */
  TC = "TC",
  /** TC0 */
  TC0 = "TC0",
  /** TC00 */
  TC00 = "TC00",
  /** TC001 */
  TC001 = "TC001",
  /** TC002 */
  TC002 = "TC002",
  /** TC003 */
  TC003 = "TC003",
  /** TC004 */
  TC004 = "TC004",
  /** TC005 */
  TC005 = "TC005",
  /** TC006 */
  TC006 = "TC006",
  /** TC007 */
  TC007 = "TC007",
  /** TC008 */
  TC008 = "TC008",
  /** TC01 */
  TC01 = "TC01",
  /** TC010 */
  TC010 = "TC010",
  /** TD */
  TD = "TD",
  /** TD0 */
  TD0 = "TD0",
  /** TD00 */
  TD00 = "TD00",
  /** TD001 */
  TD001 = "TD001",
  /** TD002 */
  TD002 = "TD002",
  /** TD003 */
  TD003 = "TD003",
  /** TD004 */
  TD004 = "TD004",
  /** TD005 */
  TD005 = "TD005",
  /** TD006 */
  TD006 = "TD006",
  /** TD007 */
  TD007 = "TD007",
  /** TID */
  TID = "TID",
  /** TID2 */
  TID2 = "TID2",
  /** TID25 */
  TID25 = "TID25",
  /** TID251 */
  TID251 = "TID251",
  /** TID252 */
  TID252 = "TID252",
  /** TID253 */
  TID253 = "TID253",
  /** TRY */
  TRY = "TRY",
  /** TRY0 */
  TRY0 = "TRY0",
  /** TRY00 */
  TRY00 = "TRY00",
  /** TRY002 */
  TRY002 = "TRY002",
  /** TRY003 */
  TRY003 = "TRY003",
  /** TRY004 */
  TRY004 = "TRY004",
  /** TRY2 */
  TRY2 = "TRY2",
  /** TRY20 */
  TRY20 = "TRY20",
  /** TRY201 */
  TRY201 = "TRY201",
  /** TRY203 */
  TRY203 = "TRY203",
  /** TRY3 */
  TRY3 = "TRY3",
  /** TRY30 */
  TRY30 = "TRY30",
  /** TRY300 */
  TRY300 = "TRY300",
  /** TRY301 */
  TRY301 = "TRY301",
  /** TRY4 */
  TRY4 = "TRY4",
  /** TRY40 */
  TRY40 = "TRY40",
  /** TRY400 */
  TRY400 = "TRY400",
  /** TRY401 */
  TRY401 = "TRY401",
  /** UP */
  UP = "UP",
  /** UP0 */
  UP0 = "UP0",
  /** UP00 */
  UP00 = "UP00",
  /** UP001 */
  UP001 = "UP001",
  /** UP003 */
  UP003 = "UP003",
  /** UP004 */
  UP004 = "UP004",
  /** UP005 */
  UP005 = "UP005",
  /** UP006 */
  UP006 = "UP006",
  /** UP007 */
  UP007 = "UP007",
  /** UP008 */
  UP008 = "UP008",
  /** UP009 */
  UP009 = "UP009",
  /** UP01 */
  UP01 = "UP01",
  /** UP010 */
  UP010 = "UP010",
  /** UP011 */
  UP011 = "UP011",
  /** UP012 */
  UP012 = "UP012",
  /** UP013 */
  UP013 = "UP013",
  /** UP014 */
  UP014 = "UP014",
  /** UP015 */
  UP015 = "UP015",
  /** UP017 */
  UP017 = "UP017",
  /** UP018 */
  UP018 = "UP018",
  /** UP019 */
  UP019 = "UP019",
  /** UP02 */
  UP02 = "UP02",
  /** UP020 */
  UP020 = "UP020",
  /** UP021 */
  UP021 = "UP021",
  /** UP022 */
  UP022 = "UP022",
  /** UP023 */
  UP023 = "UP023",
  /** UP024 */
  UP024 = "UP024",
  /** UP025 */
  UP025 = "UP025",
  /** UP026 */
  UP026 = "UP026",
  /** UP028 */
  UP028 = "UP028",
  /** UP029 */
  UP029 = "UP029",
  /** UP03 */
  UP03 = "UP03",
  /** UP030 */
  UP030 = "UP030",
  /** UP031 */
  UP031 = "UP031",
  /** UP032 */
  UP032 = "UP032",
  /** UP033 */
  UP033 = "UP033",
  /** UP034 */
  UP034 = "UP034",
  /** UP035 */
  UP035 = "UP035",
  /** UP036 */
  UP036 = "UP036",
  /** UP037 */
  UP037 = "UP037",
  /** UP039 */
  UP039 = "UP039",
  /** UP04 */
  UP04 = "UP04",
  /** UP040 */
  UP040 = "UP040",
  /** UP041 */
  UP041 = "UP041",
  /** UP042 */
  UP042 = "UP042",
  /** UP043 */
  UP043 = "UP043",
  /** UP044 */
  UP044 = "UP044",
  /** UP045 */
  UP045 = "UP045",
  /** UP046 */
  UP046 = "UP046",
  /** UP047 */
  UP047 = "UP047",
  /** UP049 */
  UP049 = "UP049",
  /** UP05 */
  UP05 = "UP05",
  /** UP050 */
  UP050 = "UP050",
  /** W */
  W = "W",
  /** W1 */
  W1 = "W1",
  /** W19 */
  W19 = "W19",
  /** W191 */
  W191 = "W191",
  /** W2 */
  W2 = "W2",
  /** W29 */
  W29 = "W29",
  /** W291 */
  W291 = "W291",
  /** W292 */
  W292 = "W292",
  /** W293 */
  W293 = "W293",
  /** W3 */
  W3 = "W3",
  /** W39 */
  W39 = "W39",
  /** W391 */
  W391 = "W391",
  /** W5 */
  W5 = "W5",
  /** W50 */
  W50 = "W50",
  /** W505 */
  W505 = "W505",
  /** W6 */
  W6 = "W6",
  /** W60 */
  W60 = "W60",
  /** W605 */
  W605 = "W605",
  /** YTT */
  YTT = "YTT",
  /** YTT1 */
  YTT1 = "YTT1",
  /** YTT10 */
  YTT10 = "YTT10",
  /** YTT101 */
  YTT101 = "YTT101",
  /** YTT102 */
  YTT102 = "YTT102",
  /** YTT103 */
  YTT103 = "YTT103",
  /** YTT2 */
  YTT2 = "YTT2",
  /** YTT20 */
  YTT20 = "YTT20",
  /** YTT201 */
  YTT201 = "YTT201",
  /** YTT202 */
  YTT202 = "YTT202",
  /** YTT203 */
  YTT203 = "YTT203",
  /** YTT204 */
  YTT204 = "YTT204",
  /** YTT3 */
  YTT3 = "YTT3",
  /** YTT30 */
  YTT30 = "YTT30",
  /** YTT301 */
  YTT301 = "YTT301",
  /** YTT302 */
  YTT302 = "YTT302",
  /** YTT303 */
  YTT303 = "YTT303",
}

/**
 * Options for the `flake8-annotations` plugin.
 *
 * @schema Flake8AnnotationsOptions
 */
export interface Flake8AnnotationsOptions {
  /**
   * Whether to suppress `ANN401` for dynamically typed `*args` and
   * `**kwargs` arguments.
   *
   * @schema Flake8AnnotationsOptions#allow-star-arg-any
   */
  readonly allowStarArgAny?: boolean;

  /**
   * Whether to suppress `ANN*` rules for any declaration
   * that hasn't been typed at all.
   * This makes it easier to gradually add types to a codebase.
   *
   * @schema Flake8AnnotationsOptions#ignore-fully-untyped
   */
  readonly ignoreFullyUntyped?: boolean;

  /**
   * Whether to allow the omission of a return type hint for `__init__` if at
   * least one argument is annotated.
   *
   * @schema Flake8AnnotationsOptions#mypy-init-return
   */
  readonly mypyInitReturn?: boolean;

  /**
   * Whether to suppress `ANN000`-level violations for arguments matching the
   * "dummy" variable regex (like `_`).
   *
   * @schema Flake8AnnotationsOptions#suppress-dummy-args
   */
  readonly suppressDummyArgs?: boolean;

  /**
   * Whether to suppress `ANN200`-level violations for functions that meet
   * either of the following criteria:
   *
   * - Contain no `return` statement.
   * - Explicit `return` statement(s) all return `None` (explicitly or
   * implicitly).
   *
   * @schema Flake8AnnotationsOptions#suppress-none-returning
   */
  readonly suppressNoneReturning?: boolean;
}

/**
 * Converts an object of type 'Flake8AnnotationsOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_Flake8AnnotationsOptions(obj: Flake8AnnotationsOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'allow-star-arg-any': obj.allowStarArgAny,
    'ignore-fully-untyped': obj.ignoreFullyUntyped,
    'mypy-init-return': obj.mypyInitReturn,
    'suppress-dummy-args': obj.suppressDummyArgs,
    'suppress-none-returning': obj.suppressNoneReturning,
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `flake8-bandit` plugin.
 *
 * @schema Flake8BanditOptions
 */
export interface Flake8BanditOptions {
  /**
   * A list of callable names, whose result may be safely passed into
   * [`markupsafe.Markup`](https://markupsafe.palletsprojects.com/en/stable/escaping/#markupsafe.Markup).
   *
   * Expects to receive a list of fully-qualified names (e.g., `bleach.clean`, rather than `clean`).
   *
   * This setting helps you avoid false positives in code like:
   *
   * ```python
   * from bleach import clean
   * from markupsafe import Markup
   *
   * cleaned_markup = Markup(clean(some_user_input))
   * ```
   *
   * Where the use of [`bleach.clean`](https://bleach.readthedocs.io/en/latest/clean.html)
   * usually ensures that there's no XSS vulnerability.
   *
   * Although it is not recommended, you may also use this setting to whitelist other
   * kinds of calls, e.g. calls to i18n translation functions, where how safe that is
   * will depend on the implementation and how well the translations are audited.
   *
   * Another common use-case is to wrap the output of functions that generate markup
   * like [`xml.etree.ElementTree.tostring`](https://docs.python.org/3/library/xml.etree.elementtree.html#xml.etree.ElementTree.tostring)
   * or template rendering engines where sanitization of potential user input is either
   * already baked in or has to happen before rendering.
   *
   * @schema Flake8BanditOptions#allowed-markup-calls
   */
  readonly allowedMarkupCalls?: string[];

  /**
   * Whether to disallow `try`-`except`-`pass` (`S110`) for specific
   * exception types. By default, `try`-`except`-`pass` is only
   * disallowed for `Exception` and `BaseException`.
   *
   * @schema Flake8BanditOptions#check-typed-exception
   */
  readonly checkTypedException?: boolean;

  /**
   * A list of additional callable names that behave like
   * [`markupsafe.Markup`](https://markupsafe.palletsprojects.com/en/stable/escaping/#markupsafe.Markup).
   *
   * Expects to receive a list of fully-qualified names (e.g., `webhelpers.html.literal`, rather than
   * `literal`).
   *
   * @schema Flake8BanditOptions#extend-markup-names
   */
  readonly extendMarkupNames?: string[];

  /**
   * A list of directories to consider temporary (see `S108`).
   *
   * @schema Flake8BanditOptions#hardcoded-tmp-directory
   */
  readonly hardcodedTmpDirectory?: string[];

  /**
   * A list of directories to consider temporary, in addition to those
   * specified by [`hardcoded-tmp-directory`](#lint_flake8-bandit_hardcoded-tmp-directory) (see `S108`).
   *
   * @schema Flake8BanditOptions#hardcoded-tmp-directory-extend
   */
  readonly hardcodedTmpDirectoryExtend?: string[];
}

/**
 * Converts an object of type 'Flake8BanditOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_Flake8BanditOptions(obj: Flake8BanditOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'allowed-markup-calls': obj.allowedMarkupCalls?.map(y => y),
    'check-typed-exception': obj.checkTypedException,
    'extend-markup-names': obj.extendMarkupNames?.map(y => y),
    'hardcoded-tmp-directory': obj.hardcodedTmpDirectory?.map(y => y),
    'hardcoded-tmp-directory-extend': obj.hardcodedTmpDirectoryExtend?.map(y => y),
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `flake8-boolean-trap` plugin
 *
 * @schema Flake8BooleanTrapOptions
 */
export interface Flake8BooleanTrapOptions {
  /**
   * Additional callable functions with which to allow boolean traps.
   *
   * Expects to receive a list of fully-qualified names (e.g., `pydantic.Field`, rather than
   * `Field`).
   *
   * @schema Flake8BooleanTrapOptions#extend-allowed-calls
   */
  readonly extendAllowedCalls?: string[];
}

/**
 * Converts an object of type 'Flake8BooleanTrapOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_Flake8BooleanTrapOptions(obj: Flake8BooleanTrapOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'extend-allowed-calls': obj.extendAllowedCalls?.map(y => y),
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `flake8-bugbear` plugin.
 *
 * @schema Flake8BugbearOptions
 */
export interface Flake8BugbearOptions {
  /**
   * Additional callable functions to consider "immutable" when evaluating, e.g., the
   * `function-call-in-default-argument` rule (`B008`) or `function-call-in-dataclass-defaults`
   * rule (`RUF009`).
   *
   * Expects to receive a list of fully-qualified names (e.g., `fastapi.Query`, rather than
   * `Query`).
   *
   * @schema Flake8BugbearOptions#extend-immutable-calls
   */
  readonly extendImmutableCalls?: string[];
}

/**
 * Converts an object of type 'Flake8BugbearOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_Flake8BugbearOptions(obj: Flake8BugbearOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'extend-immutable-calls': obj.extendImmutableCalls?.map(y => y),
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `flake8-builtins` plugin.
 *
 * @schema Flake8BuiltinsOptions
 */
export interface Flake8BuiltinsOptions {
  /**
   * List of builtin module names to allow.
   *
   * @schema Flake8BuiltinsOptions#allowed-modules
   */
  readonly allowedModules?: string[];

  /**
   * DEPRECATED: This option has been renamed to `allowed-modules`. Use `allowed-modules` instead.
   *
   * List of builtin module names to allow.
   *
   * This option is ignored if both `allowed-modules` and `builtins-allowed-modules` are set.
   *
   * @schema Flake8BuiltinsOptions#builtins-allowed-modules
   */
  readonly builtinsAllowedModules?: string[];

  /**
   * DEPRECATED: This option has been renamed to `ignorelist`. Use `ignorelist` instead.
   *
   * Ignore list of builtins.
   *
   * This option is ignored if both `ignorelist` and `builtins-ignorelist` are set.
   *
   * @schema Flake8BuiltinsOptions#builtins-ignorelist
   */
  readonly builtinsIgnorelist?: string[];

  /**
   * DEPRECATED: This option has been renamed to `strict-checking`. Use `strict-checking` instead.
   *
   * Compare module names instead of full module paths.
   *
   * This option is ignored if both `strict-checking` and `builtins-strict-checking` are set.
   *
   * @schema Flake8BuiltinsOptions#builtins-strict-checking
   */
  readonly builtinsStrictChecking?: boolean;

  /**
   * Ignore list of builtins.
   *
   * @schema Flake8BuiltinsOptions#ignorelist
   */
  readonly ignorelist?: string[];

  /**
   * Compare module names instead of full module paths.
   *
   * Used by [`A005` - `stdlib-module-shadowing`](https://docs.astral.sh/ruff/rules/stdlib-module-shadowing/).
   *
   * @schema Flake8BuiltinsOptions#strict-checking
   */
  readonly strictChecking?: boolean;
}

/**
 * Converts an object of type 'Flake8BuiltinsOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_Flake8BuiltinsOptions(obj: Flake8BuiltinsOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'allowed-modules': obj.allowedModules?.map(y => y),
    'builtins-allowed-modules': obj.builtinsAllowedModules?.map(y => y),
    'builtins-ignorelist': obj.builtinsIgnorelist?.map(y => y),
    'builtins-strict-checking': obj.builtinsStrictChecking,
    'ignorelist': obj.ignorelist?.map(y => y),
    'strict-checking': obj.strictChecking,
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `flake8-comprehensions` plugin.
 *
 * @schema Flake8ComprehensionsOptions
 */
export interface Flake8ComprehensionsOptions {
  /**
   * Allow `dict` calls that make use of keyword arguments (e.g., `dict(a=1, b=2)`).
   *
   * @schema Flake8ComprehensionsOptions#allow-dict-calls-with-keyword-arguments
   */
  readonly allowDictCallsWithKeywordArguments?: boolean;
}

/**
 * Converts an object of type 'Flake8ComprehensionsOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_Flake8ComprehensionsOptions(obj: Flake8ComprehensionsOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'allow-dict-calls-with-keyword-arguments': obj.allowDictCallsWithKeywordArguments,
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `flake8-copyright` plugin.
 *
 * @schema Flake8CopyrightOptions
 */
export interface Flake8CopyrightOptions {
  /**
   * Author to enforce within the copyright notice. If provided, the
   * author must be present immediately following the copyright notice.
   *
   * @schema Flake8CopyrightOptions#author
   */
  readonly author?: string;

  /**
   * A minimum file size (in bytes) required for a copyright notice to
   * be enforced. By default, all files are validated.
   *
   * @schema Flake8CopyrightOptions#min-file-size
   */
  readonly minFileSize?: number;

  /**
   * The regular expression used to match the copyright notice, compiled
   * with the [`regex`](https://docs.rs/regex/latest/regex/) crate.
   * Defaults to `(?i)Copyright\s+((?:\(C\)|©)\s+)?\d{4}((-|,\s)\d{4})*`, which matches
   * the following:
   *
   * - `Copyright 2023`
   * - `Copyright (C) 2023`
   * - `Copyright 2021-2023`
   * - `Copyright (C) 2021-2023`
   * - `Copyright (C) 2021, 2023`
   *
   * @default i)Copyright\s+((?:\(C\)|©)\s+)?\d{4}((-|,\s)\d{4})*`, which matches
   * @schema Flake8CopyrightOptions#notice-rgx
   */
  readonly noticeRgx?: string;
}

/**
 * Converts an object of type 'Flake8CopyrightOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_Flake8CopyrightOptions(obj: Flake8CopyrightOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'author': obj.author,
    'min-file-size': obj.minFileSize,
    'notice-rgx': obj.noticeRgx,
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `flake8-errmsg` plugin.
 *
 * @schema Flake8ErrMsgOptions
 */
export interface Flake8ErrMsgOptions {
  /**
   * Maximum string length for string literals in exception messages.
   *
   * @schema Flake8ErrMsgOptions#max-string-length
   */
  readonly maxStringLength?: number;
}

/**
 * Converts an object of type 'Flake8ErrMsgOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_Flake8ErrMsgOptions(obj: Flake8ErrMsgOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'max-string-length': obj.maxStringLength,
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `flake8-gettext` plugin.
 *
 * @schema Flake8GetTextOptions
 */
export interface Flake8GetTextOptions {
  /**
   * Additional function names to consider as internationalization calls, in addition to those
   * included in [`function-names`](#lint_flake8-gettext_function-names).
   *
   * @schema Flake8GetTextOptions#extend-function-names
   */
  readonly extendFunctionNames?: string[];

  /**
   * The function names to consider as internationalization calls.
   *
   * @schema Flake8GetTextOptions#function-names
   */
  readonly functionNames?: string[];
}

/**
 * Converts an object of type 'Flake8GetTextOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_Flake8GetTextOptions(obj: Flake8GetTextOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'extend-function-names': obj.extendFunctionNames?.map(y => y),
    'function-names': obj.functionNames?.map(y => y),
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `flake8-implicit-str-concat` plugin
 *
 * @schema Flake8ImplicitStrConcatOptions
 */
export interface Flake8ImplicitStrConcatOptions {
  /**
   * Whether to allow implicit string concatenations for multiline strings.
   * By default, implicit concatenations of multiline strings are
   * allowed (but continuation lines, delimited with a backslash, are
   * prohibited).
   *
   * Setting `allow-multiline = false` will automatically disable the
   * `explicit-string-concatenation` (`ISC003`) rule. Otherwise, both
   * implicit and explicit multiline string concatenations would be seen
   * as violations, making it impossible to write a linter-compliant multiline
   * string.
   *
   * @schema Flake8ImplicitStrConcatOptions#allow-multiline
   */
  readonly allowMultiline?: boolean;
}

/**
 * Converts an object of type 'Flake8ImplicitStrConcatOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_Flake8ImplicitStrConcatOptions(obj: Flake8ImplicitStrConcatOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'allow-multiline': obj.allowMultiline,
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `flake8-import-conventions` plugin
 *
 * @schema Flake8ImportConventionsOptions
 */
export interface Flake8ImportConventionsOptions {
  /**
   * The conventional aliases for imports. These aliases can be extended by
   * the [`extend-aliases`](#lint_flake8-import-conventions_extend-aliases) option.
   *
   * @schema Flake8ImportConventionsOptions#aliases
   */
  readonly aliases?: { [key: string]: string };

  /**
   * A mapping from module to its banned import aliases.
   *
   * @schema Flake8ImportConventionsOptions#banned-aliases
   */
  readonly bannedAliases?: { [key: string]: string[] };

  /**
   * A list of modules that should not be imported from using the
   * `from ... import ...` syntax.
   *
   * For example, given `banned-from = ["pandas"]`, `from pandas import DataFrame`
   * would be disallowed, while `import pandas` would be allowed.
   *
   * @schema Flake8ImportConventionsOptions#banned-from
   */
  readonly bannedFrom?: string[];

  /**
   * A mapping from module to conventional import alias. These aliases will
   * be added to the [`aliases`](#lint_flake8-import-conventions_aliases) mapping.
   *
   * @schema Flake8ImportConventionsOptions#extend-aliases
   */
  readonly extendAliases?: { [key: string]: string };
}

/**
 * Converts an object of type 'Flake8ImportConventionsOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_Flake8ImportConventionsOptions(obj: Flake8ImportConventionsOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'aliases': ((obj.aliases) === undefined) ? undefined : (Object.entries(obj.aliases).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {})),
    'banned-aliases': ((obj.bannedAliases) === undefined) ? undefined : (Object.entries(obj.bannedAliases).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1]?.map(y => y) }), {})),
    'banned-from': obj.bannedFrom?.map(y => y),
    'extend-aliases': ((obj.extendAliases) === undefined) ? undefined : (Object.entries(obj.extendAliases).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {})),
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `flake8-pytest-style` plugin
 *
 * @schema Flake8PytestStyleOptions
 */
export interface Flake8PytestStyleOptions {
  /**
   * Boolean flag specifying whether `@pytest.fixture()` without parameters
   * should have parentheses. If the option is set to `false` (the default),
   * `@pytest.fixture` is valid and `@pytest.fixture()` is invalid. If set
   * to `true`, `@pytest.fixture()` is valid and `@pytest.fixture` is
   * invalid.
   *
   * @schema Flake8PytestStyleOptions#fixture-parentheses
   */
  readonly fixtureParentheses?: boolean;

  /**
   * Boolean flag specifying whether `@pytest.mark.foo()` without parameters
   * should have parentheses. If the option is set to `false` (the
   * default), `@pytest.mark.foo` is valid and `@pytest.mark.foo()` is
   * invalid. If set to `true`, `@pytest.mark.foo()` is valid and
   * `@pytest.mark.foo` is invalid.
   *
   * @schema Flake8PytestStyleOptions#mark-parentheses
   */
  readonly markParentheses?: boolean;

  /**
   * Expected type for multiple argument names in `@pytest.mark.parametrize`.
   * The following values are supported:
   *
   * - `csv` — a comma-separated list, e.g.
   * `@pytest.mark.parametrize("name1,name2", ...)`
   * - `tuple` (default) — e.g.
   * `@pytest.mark.parametrize(("name1", "name2"), ...)`
   * - `list` — e.g. `@pytest.mark.parametrize(["name1", "name2"], ...)`
   *
   * @schema Flake8PytestStyleOptions#parametrize-names-type
   */
  readonly parametrizeNamesType?: ParametrizeNameType;

  /**
   * Expected type for each row of values in `@pytest.mark.parametrize` in
   * case of multiple parameters. The following values are supported:
   *
   * - `tuple` (default) — e.g.
   * `@pytest.mark.parametrize(("name1", "name2"), [(1, 2), (3, 4)])`
   * - `list` — e.g.
   * `@pytest.mark.parametrize(("name1", "name2"), [[1, 2], [3, 4]])`
   *
   * @schema Flake8PytestStyleOptions#parametrize-values-row-type
   */
  readonly parametrizeValuesRowType?: ParametrizeValuesRowType;

  /**
   * Expected type for the list of values rows in `@pytest.mark.parametrize`.
   * The following values are supported:
   *
   * - `tuple` — e.g. `@pytest.mark.parametrize("name", (1, 2, 3))`
   * - `list` (default) — e.g. `@pytest.mark.parametrize("name", [1, 2, 3])`
   *
   * @schema Flake8PytestStyleOptions#parametrize-values-type
   */
  readonly parametrizeValuesType?: ParametrizeValuesType;

  /**
   * List of additional exception names that require a match= parameter in a
   * `pytest.raises()` call. This extends the default list of exceptions
   * that require a match= parameter.
   * This option is useful if you want to extend the default list of
   * exceptions that require a match= parameter without having to specify
   * the entire list.
   * Note that this option does not remove any exceptions from the default
   * list.
   *
   * Supports glob patterns. For more information on the glob syntax, refer
   * to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).
   *
   * @schema Flake8PytestStyleOptions#raises-extend-require-match-for
   */
  readonly raisesExtendRequireMatchFor?: string[];

  /**
   * List of exception names that require a match= parameter in a
   * `pytest.raises()` call.
   *
   * Supports glob patterns. For more information on the glob syntax, refer
   * to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).
   *
   * @schema Flake8PytestStyleOptions#raises-require-match-for
   */
  readonly raisesRequireMatchFor?: string[];

  /**
   * List of additional warning names that require a match= parameter in a
   * `pytest.warns()` call. This extends the default list of warnings that
   * require a match= parameter.
   *
   * This option is useful if you want to extend the default list of warnings
   * that require a match= parameter without having to specify the entire
   * list.
   *
   * Note that this option does not remove any warnings from the default
   * list.
   *
   * Supports glob patterns. For more information on the glob syntax, refer
   * to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).
   *
   * @schema Flake8PytestStyleOptions#warns-extend-require-match-for
   */
  readonly warnsExtendRequireMatchFor?: string[];

  /**
   * List of warning names that require a match= parameter in a
   * `pytest.warns()` call.
   *
   * Supports glob patterns. For more information on the glob syntax, refer
   * to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).
   *
   * @schema Flake8PytestStyleOptions#warns-require-match-for
   */
  readonly warnsRequireMatchFor?: string[];
}

/**
 * Converts an object of type 'Flake8PytestStyleOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_Flake8PytestStyleOptions(obj: Flake8PytestStyleOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'fixture-parentheses': obj.fixtureParentheses,
    'mark-parentheses': obj.markParentheses,
    'parametrize-names-type': obj.parametrizeNamesType,
    'parametrize-values-row-type': obj.parametrizeValuesRowType,
    'parametrize-values-type': obj.parametrizeValuesType,
    'raises-extend-require-match-for': obj.raisesExtendRequireMatchFor?.map(y => y),
    'raises-require-match-for': obj.raisesRequireMatchFor?.map(y => y),
    'warns-extend-require-match-for': obj.warnsExtendRequireMatchFor?.map(y => y),
    'warns-require-match-for': obj.warnsRequireMatchFor?.map(y => y),
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `flake8-quotes` plugin.
 *
 * @schema Flake8QuotesOptions
 */
export interface Flake8QuotesOptions {
  /**
   * Whether to avoid using single quotes if a string contains single quotes,
   * or vice-versa with double quotes, as per [PEP 8](https://peps.python.org/pep-0008/#string-quotes).
   * This minimizes the need to escape quotation marks within strings.
   *
   * @schema Flake8QuotesOptions#avoid-escape
   */
  readonly avoidEscape?: boolean;

  /**
   * Quote style to prefer for docstrings (either "single" or "double").
   *
   * When using the formatter, only "double" is compatible, as the formatter
   * enforces double quotes for docstrings strings.
   *
   * @schema Flake8QuotesOptions#docstring-quotes
   */
  readonly docstringQuotes?: string;

  /**
   * Quote style to prefer for inline strings (either "single" or
   * "double").
   *
   * When using the formatter, ensure that [`format.quote-style`](#format_quote-style) is set to
   * the same preferred quote style.
   *
   * @schema Flake8QuotesOptions#inline-quotes
   */
  readonly inlineQuotes?: string;

  /**
   * Quote style to prefer for multiline strings (either "single" or
   * "double").
   *
   * When using the formatter, only "double" is compatible, as the formatter
   * enforces double quotes for multiline strings.
   *
   * @schema Flake8QuotesOptions#multiline-quotes
   */
  readonly multilineQuotes?: string;
}

/**
 * Converts an object of type 'Flake8QuotesOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_Flake8QuotesOptions(obj: Flake8QuotesOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'avoid-escape': obj.avoidEscape,
    'docstring-quotes': obj.docstringQuotes,
    'inline-quotes': obj.inlineQuotes,
    'multiline-quotes': obj.multilineQuotes,
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `flake8_self` plugin.
 *
 * @schema Flake8SelfOptions
 */
export interface Flake8SelfOptions {
  /**
   * Additional names to ignore when considering `flake8-self` violations,
   * in addition to those included in [`ignore-names`](#lint_flake8-self_ignore-names).
   *
   * @schema Flake8SelfOptions#extend-ignore-names
   */
  readonly extendIgnoreNames?: string[];

  /**
   * A list of names to ignore when considering `flake8-self` violations.
   *
   * @schema Flake8SelfOptions#ignore-names
   */
  readonly ignoreNames?: string[];
}

/**
 * Converts an object of type 'Flake8SelfOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_Flake8SelfOptions(obj: Flake8SelfOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'extend-ignore-names': obj.extendIgnoreNames?.map(y => y),
    'ignore-names': obj.ignoreNames?.map(y => y),
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `flake8-tidy-imports` plugin
 *
 * @schema Flake8TidyImportsOptions
 */
export interface Flake8TidyImportsOptions {
  /**
   * Whether to ban all relative imports (`"all"`), or only those imports
   * that extend into the parent module or beyond (`"parents"`).
   *
   * @schema Flake8TidyImportsOptions#ban-relative-imports
   */
  readonly banRelativeImports?: string;

  /**
   * Specific modules or module members that may not be imported or accessed.
   * Note that this rule is only meant to flag accidental uses,
   * and can be circumvented via `eval` or `importlib`.
   *
   * @schema Flake8TidyImportsOptions#banned-api
   */
  readonly bannedApi?: { [key: string]: ApiBan };

  /**
   * List of specific modules that may not be imported at module level, and should instead be
   * imported lazily (e.g., within a function definition, or an `if TYPE_CHECKING:`
   * block, or some other nested context). This also affects the rule `import-outside-top-level`
   * if `banned-module-level-imports` is enabled.
   *
   * @schema Flake8TidyImportsOptions#banned-module-level-imports
   */
  readonly bannedModuleLevelImports?: string[];
}

/**
 * Converts an object of type 'Flake8TidyImportsOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_Flake8TidyImportsOptions(obj: Flake8TidyImportsOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'ban-relative-imports': obj.banRelativeImports,
    'banned-api': ((obj.bannedApi) === undefined) ? undefined : (Object.entries(obj.bannedApi).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: toJson_ApiBan(i[1]) }), {})),
    'banned-module-level-imports': obj.bannedModuleLevelImports?.map(y => y),
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `flake8-type-checking` plugin
 *
 * @schema Flake8TypeCheckingOptions
 */
export interface Flake8TypeCheckingOptions {
  /**
   * Exempt certain modules from needing to be moved into type-checking
   * blocks.
   *
   * @schema Flake8TypeCheckingOptions#exempt-modules
   */
  readonly exemptModules?: string[];

  /**
   * Whether to add quotes around type annotations, if doing so would allow
   * the corresponding import to be moved into a type-checking block.
   *
   * For example, in the following, Python requires that `Sequence` be
   * available at runtime, despite the fact that it's only used in a type
   * annotation:
   *
   * ```python
   * from collections.abc import Sequence
   *
   *
   * def func(value: Sequence[int]) -> None:
   * ...
   * ```
   *
   * In other words, moving `from collections.abc import Sequence` into an
   * `if TYPE_CHECKING:` block above would cause a runtime error, as the
   * type would no longer be available at runtime.
   *
   * By default, Ruff will respect such runtime semantics and avoid moving
   * the import to prevent such runtime errors.
   *
   * Setting `quote-annotations` to `true` will instruct Ruff to add quotes
   * around the annotation (e.g., `"Sequence[int]"`), which in turn enables
   * Ruff to move the import into an `if TYPE_CHECKING:` block, like so:
   *
   * ```python
   * from typing import TYPE_CHECKING
   *
   * if TYPE_CHECKING:
   * from collections.abc import Sequence
   *
   *
   * def func(value: "Sequence[int]") -> None:
   * ...
   * ```
   *
   * Note that this setting has no effect when `from __future__ import annotations`
   * is present, as `__future__` annotations are always treated equivalently
   * to quoted annotations. Similarly, this setting has no effect on Python
   * versions after 3.14 because these annotations are also deferred.
   *
   * @schema Flake8TypeCheckingOptions#quote-annotations
   */
  readonly quoteAnnotations?: boolean;

  /**
   * Exempt classes that list any of the enumerated classes as a base class
   * from needing to be moved into type-checking blocks.
   *
   * Common examples include Pydantic's `pydantic.BaseModel` and SQLAlchemy's
   * `sqlalchemy.orm.DeclarativeBase`, but can also support user-defined
   * classes that inherit from those base classes. For example, if you define
   * a common `DeclarativeBase` subclass that's used throughout your project
   * (e.g., `class Base(DeclarativeBase) ...` in `base.py`), you can add it to
   * this list (`runtime-evaluated-base-classes = ["base.Base"]`) to exempt
   * models from being moved into type-checking blocks.
   *
   * @schema Flake8TypeCheckingOptions#runtime-evaluated-base-classes
   */
  readonly runtimeEvaluatedBaseClasses?: string[];

  /**
   * Exempt classes and functions decorated with any of the enumerated
   * decorators from being moved into type-checking blocks.
   *
   * Common examples include Pydantic's `@pydantic.validate_call` decorator
   * (for functions) and attrs' `@attrs.define` decorator (for classes).
   *
   * This also supports framework decorators like FastAPI's `fastapi.FastAPI.get`
   * which will work across assignments in the same module.
   *
   * For example:
   * ```python
   * import fastapi
   *
   * app = FastAPI("app")
   *
   * @app.get("/home")
   * def home() -> str: ...
   * ```
   *
   * Here `app.get` will correctly be identified as `fastapi.FastAPI.get`.
   *
   * @schema Flake8TypeCheckingOptions#runtime-evaluated-decorators
   */
  readonly runtimeEvaluatedDecorators?: string[];

  /**
   * Enforce `TC001`, `TC002`, and `TC003` rules even when valid runtime imports
   * are present for the same module.
   *
   * See flake8-type-checking's [strict](https://github.com/snok/flake8-type-checking#strict) option.
   *
   * @schema Flake8TypeCheckingOptions#strict
   */
  readonly strict?: boolean;
}

/**
 * Converts an object of type 'Flake8TypeCheckingOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_Flake8TypeCheckingOptions(obj: Flake8TypeCheckingOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'exempt-modules': obj.exemptModules?.map(y => y),
    'quote-annotations': obj.quoteAnnotations,
    'runtime-evaluated-base-classes': obj.runtimeEvaluatedBaseClasses?.map(y => y),
    'runtime-evaluated-decorators': obj.runtimeEvaluatedDecorators?.map(y => y),
    'strict': obj.strict,
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `flake8-unused-arguments` plugin
 *
 * @schema Flake8UnusedArgumentsOptions
 */
export interface Flake8UnusedArgumentsOptions {
  /**
   * Whether to allow unused variadic arguments, like `*args` and `**kwargs`.
   *
   * @schema Flake8UnusedArgumentsOptions#ignore-variadic-names
   */
  readonly ignoreVariadicNames?: boolean;
}

/**
 * Converts an object of type 'Flake8UnusedArgumentsOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_Flake8UnusedArgumentsOptions(obj: Flake8UnusedArgumentsOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'ignore-variadic-names': obj.ignoreVariadicNames,
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Configures the way Ruff formats your code.
 *
 * @schema FormatOptions
 */
export interface FormatOptions {
  /**
   * Whether to format code snippets in docstrings.
   *
   * When this is enabled, Python code examples within docstrings are
   * automatically reformatted.
   *
   * For example, when this is enabled, the following code:
   *
   * ```python
   * def f(x):
   * """
   * Something about `f`. And an example in doctest format:
   *
   * >>> f(  x  )
   *
   * Markdown is also supported:
   *
   * ```py
   * f(  x  )
   * ```
   *
   * As are reStructuredText literal blocks::
   *
   * f(  x  )
   *
   *
   * And reStructuredText code blocks:
   *
   * .. code-block:: python
   *
   * f(  x  )
   * """
   * pass
   * ```
   *
   * ... will be reformatted (assuming the rest of the options are set to
   * their defaults) as:
   *
   * ```python
   * def f(x):
   * """
   * Something about `f`. And an example in doctest format:
   *
   * >>> f(x)
   *
   * Markdown is also supported:
   *
   * ```py
   * f(x)
   * ```
   *
   * As are reStructuredText literal blocks::
   *
   * f(x)
   *
   *
   * And reStructuredText code blocks:
   *
   * .. code-block:: python
   *
   * f(x)
   * """
   * pass
   * ```
   *
   * If a code snippet in a docstring contains invalid Python code or if the
   * formatter would otherwise write invalid Python code, then the code
   * example is ignored by the formatter and kept as-is.
   *
   * Currently, doctest, Markdown, reStructuredText literal blocks, and
   * reStructuredText code blocks are all supported and automatically
   * recognized. In the case of unlabeled fenced code blocks in Markdown and
   * reStructuredText literal blocks, the contents are assumed to be Python
   * and reformatted. As with any other format, if the contents aren't valid
   * Python, then the block is left untouched automatically.
   *
   * @schema FormatOptions#docstring-code-format
   */
  readonly docstringCodeFormat?: boolean;

  /**
   * Set the line length used when formatting code snippets in docstrings.
   *
   * This only has an effect when the `docstring-code-format` setting is
   * enabled.
   *
   * The default value for this setting is `"dynamic"`, which has the effect
   * of ensuring that any reformatted code examples in docstrings adhere to
   * the global line length configuration that is used for the surrounding
   * Python code. The point of this setting is that it takes the indentation
   * of the docstring into account when reformatting code examples.
   *
   * Alternatively, this can be set to a fixed integer, which will result
   * in the same line length limit being applied to all reformatted code
   * examples in docstrings. When set to a fixed integer, the indent of the
   * docstring is not taken into account. That is, this may result in lines
   * in the reformatted code example that exceed the globally configured
   * line length limit.
   *
   * For example, when this is set to `20` and [`docstring-code-format`](#docstring-code-format)
   * is enabled, then this code:
   *
   * ```python
   * def f(x):
   * '''
   * Something about `f`. And an example:
   *
   * .. code-block:: python
   *
   * foo, bar, quux = this_is_a_long_line(lion, hippo, lemur, bear)
   * '''
   * pass
   * ```
   *
   * ... will be reformatted (assuming the rest of the options are set
   * to their defaults) as:
   *
   * ```python
   * def f(x):
   * """
   * Something about `f`. And an example:
   *
   * .. code-block:: python
   *
   * (
   * foo,
   * bar,
   * quux,
   * ) = this_is_a_long_line(
   * lion,
   * hippo,
   * lemur,
   * bear,
   * )
   * """
   * pass
   * ```
   *
   * @schema FormatOptions#docstring-code-line-length
   */
  readonly docstringCodeLineLength?: any;

  /**
   * A list of file patterns to exclude from formatting in addition to the files excluded globally (see [`exclude`](#exclude), and [`extend-exclude`](#extend-exclude)).
   *
   * Exclusions are based on globs, and can be either:
   *
   * - Single-path patterns, like `.mypy_cache` (to exclude any directory
   * named `.mypy_cache` in the tree), `foo.py` (to exclude any file named
   * `foo.py`), or `foo_*.py` (to exclude any file matching `foo_*.py` ).
   * - Relative patterns, like `directory/foo.py` (to exclude that specific
   * file) or `directory/*.py` (to exclude any Python files in
   * `directory`). Note that these paths are relative to the project root
   * (e.g., the directory containing your `pyproject.toml`).
   *
   * For more information on the glob syntax, refer to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).
   *
   * @schema FormatOptions#exclude
   */
  readonly exclude?: string[];

  /**
   * Whether to use spaces or tabs for indentation.
   *
   * `indent-style = "space"` (default):
   *
   * ```python
   * def f():
   * print("Hello") #  Spaces indent the `print` statement.
   * ```
   *
   * `indent-style = "tab"`:
   *
   * ```python
   * def f():
   * print("Hello") #  A tab `\t` indents the `print` statement.
   * ```
   *
   * PEP 8 recommends using spaces for [indentation](https://peps.python.org/pep-0008/#indentation).
   * We care about accessibility; if you do not need tabs for accessibility, we do not recommend you use them.
   *
   * See [`indent-width`](#indent-width) to configure the number of spaces per indentation and the tab width.
   *
   * @schema FormatOptions#indent-style
   */
  readonly indentStyle?: string;

  /**
   * The character Ruff uses at the end of a line.
   *
   * * `auto`: The newline style is detected automatically on a file per file basis. Files with mixed line endings will be converted to the first detected line ending. Defaults to `\n` for files that contain no line endings.
   * * `lf`: Line endings will be converted to `\n`. The default line ending on Unix.
   * * `cr-lf`: Line endings will be converted to `\r\n`. The default line ending on Windows.
   * * `native`: Line endings will be converted to `\n` on Unix and `\r\n` on Windows.
   *
   * @default n` for files that contain no line endings.
   * @schema FormatOptions#line-ending
   */
  readonly lineEnding?: string;

  /**
   * Whether to enable the unstable preview style formatting.
   *
   * @schema FormatOptions#preview
   */
  readonly preview?: boolean;

  /**
   * Configures the preferred quote character for strings. The recommended options are
   *
   * * `double` (default): Use double quotes `"`
   * * `single`: Use single quotes `'`
   *
   * In compliance with [PEP 8](https://peps.python.org/pep-0008/) and [PEP 257](https://peps.python.org/pep-0257/),
   * Ruff prefers double quotes for triple quoted strings and docstrings even when using `quote-style = "single"`.
   *
   * Ruff deviates from using the configured quotes if doing so prevents the need for
   * escaping quote characters inside the string:
   *
   * ```python
   * a = "a string without any quotes"
   * b = "It's monday morning"
   * ```
   *
   * Ruff will change the quotes of the string assigned to `a` to single quotes when using `quote-style = "single"`.
   * However, Ruff uses double quotes for the string assigned to `b` because using single quotes would require escaping the `'`,
   * which leads to the less readable code: `'It\'s monday morning'`.
   *
   * In addition, Ruff supports the quote style `preserve` for projects that already use
   * a mixture of single and double quotes and can't migrate to the `double` or `single` style.
   * The quote style `preserve` leaves the quotes of all strings unchanged.
   *
   * @schema FormatOptions#quote-style
   */
  readonly quoteStyle?: QuoteStyle;

  /**
   * Ruff uses existing trailing commas as an indication that short lines should be left separate.
   * If this option is set to `true`, the magic trailing comma is ignored.
   *
   * For example, Ruff leaves the arguments separate even though
   * collapsing the arguments to a single line doesn't exceed the line length if `skip-magic-trailing-comma = false`:
   *
   * ```python
   * # The arguments remain on separate lines because of the trailing comma after `b`
   * def test(
   * a,
   * b,
   * ): pass
   * ```
   *
   * Setting `skip-magic-trailing-comma = true` changes the formatting to:
   *
   * ```python
   * # The arguments are collapsed to a single line because the trailing comma is ignored
   * def test(a, b):
   * pass
   * ```
   *
   * @schema FormatOptions#skip-magic-trailing-comma
   */
  readonly skipMagicTrailingComma?: boolean;
}

/**
 * Converts an object of type 'FormatOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_FormatOptions(obj: FormatOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'docstring-code-format': obj.docstringCodeFormat,
    'docstring-code-line-length': obj.docstringCodeLineLength,
    'exclude': obj.exclude?.map(y => y),
    'indent-style': obj.indentStyle,
    'line-ending': obj.lineEnding,
    'preview': obj.preview,
    'quote-style': obj.quoteStyle,
    'skip-magic-trailing-comma': obj.skipMagicTrailingComma,
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `isort` plugin.
 *
 * @schema IsortOptions
 */
export interface IsortOptions {
  /**
   * Sort imports taking into account case sensitivity.
   *
   * Note that the [`order-by-type`](#lint_isort_order-by-type) setting will
   * take precedence over this one when enabled.
   *
   * @schema IsortOptions#case-sensitive
   */
  readonly caseSensitive?: boolean;

  /**
   * An override list of tokens to always recognize as a Class for
   * [`order-by-type`](#lint_isort_order-by-type) regardless of casing.
   *
   * @schema IsortOptions#classes
   */
  readonly classes?: string[];

  /**
   * Combines as imports on the same line. See isort's [`combine-as-imports`](https://pycqa.github.io/isort/docs/configuration/options.html#combine-as-imports)
   * option.
   *
   * @schema IsortOptions#combine-as-imports
   */
  readonly combineAsImports?: boolean;

  /**
   * An override list of tokens to always recognize as a CONSTANT
   * for [`order-by-type`](#lint_isort_order-by-type) regardless of casing.
   *
   * @schema IsortOptions#constants
   */
  readonly constants?: string[];

  /**
   * Define a default section for any imports that don't fit into the specified [`section-order`](#lint_isort_section-order).
   *
   * @schema IsortOptions#default-section
   */
  readonly defaultSection?: string;

  /**
   * Whether to automatically mark imports from within the same package as first-party.
   * For example, when `detect-same-package = true`, then when analyzing files within the
   * `foo` package, any imports from within the `foo` package will be considered first-party.
   *
   * This heuristic is often unnecessary when `src` is configured to detect all first-party
   * sources; however, if `src` is _not_ configured, this heuristic can be useful to detect
   * first-party imports from _within_ (but not _across_) first-party packages.
   *
   * @schema IsortOptions#detect-same-package
   */
  readonly detectSamePackage?: boolean;

  /**
   * A list of modules to consider standard-library, in addition to those
   * known to Ruff in advance.
   *
   * Supports glob patterns. For more information on the glob syntax, refer
   * to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).
   *
   * @schema IsortOptions#extra-standard-library
   */
  readonly extraStandardLibrary?: string[];

  /**
   * Forces all from imports to appear on their own line.
   *
   * @schema IsortOptions#force-single-line
   */
  readonly forceSingleLine?: boolean;

  /**
   * Don't sort straight-style imports (like `import sys`) before from-style
   * imports (like `from itertools import groupby`). Instead, sort the
   * imports by module, independent of import style.
   *
   * @schema IsortOptions#force-sort-within-sections
   */
  readonly forceSortWithinSections?: boolean;

  /**
   * Force specific imports to the top of their appropriate section.
   *
   * @schema IsortOptions#force-to-top
   */
  readonly forceToTop?: string[];

  /**
   * Force `import from` statements with multiple members and at least one
   * alias (e.g., `import A as B`) to wrap such that every line contains
   * exactly one member. For example, this formatting would be retained,
   * rather than condensing to a single line:
   *
   * ```python
   * from .utils import (
   * test_directory as test_directory,
   * test_id as test_id
   * )
   * ```
   *
   * Note that this setting is only effective when combined with
   * `combine-as-imports = true`. When [`combine-as-imports`](#lint_isort_combine-as-imports) isn't
   * enabled, every aliased `import from` will be given its own line, in
   * which case, wrapping is not necessary.
   *
   * When using the formatter, ensure that [`format.skip-magic-trailing-comma`](#format_skip-magic-trailing-comma) is set to `false` (default)
   * when enabling `force-wrap-aliases` to avoid that the formatter collapses members if they all fit on a single line.
   *
   * @schema IsortOptions#force-wrap-aliases
   */
  readonly forceWrapAliases?: boolean;

  /**
   * A list of modules to separate into auxiliary block(s) of imports,
   * in the order specified.
   *
   * @schema IsortOptions#forced-separate
   */
  readonly forcedSeparate?: string[];

  /**
   * Whether to place `import from` imports before straight imports when sorting.
   *
   * For example, by default, imports will be sorted such that straight imports appear
   * before `import from` imports, as in:
   * ```python
   * import os
   * import sys
   * from typing import List
   * ```
   *
   * Setting `from-first = true` will instead sort such that `import from` imports appear
   * before straight imports, as in:
   * ```python
   * from typing import List
   * import os
   * import sys
   * ```
   *
   * @schema IsortOptions#from-first
   */
  readonly fromFirst?: boolean;

  /**
   * A list of modules to consider first-party, regardless of whether they
   * can be identified as such via introspection of the local filesystem.
   *
   * Supports glob patterns. For more information on the glob syntax, refer
   * to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).
   *
   * @schema IsortOptions#known-first-party
   */
  readonly knownFirstParty?: string[];

  /**
   * A list of modules to consider being a local folder.
   * Generally, this is reserved for relative imports (`from . import module`).
   *
   * Supports glob patterns. For more information on the glob syntax, refer
   * to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).
   *
   * @schema IsortOptions#known-local-folder
   */
  readonly knownLocalFolder?: string[];

  /**
   * A list of modules to consider third-party, regardless of whether they
   * can be identified as such via introspection of the local filesystem.
   *
   * Supports glob patterns. For more information on the glob syntax, refer
   * to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).
   *
   * @schema IsortOptions#known-third-party
   */
  readonly knownThirdParty?: string[];

  /**
   * Sort imports by their string length, such that shorter imports appear
   * before longer imports. For example, by default, imports will be sorted
   * alphabetically, as in:
   * ```python
   * import collections
   * import os
   * ```
   *
   * Setting `length-sort = true` will instead sort such that shorter imports
   * appear before longer imports, as in:
   * ```python
   * import os
   * import collections
   * ```
   *
   * @schema IsortOptions#length-sort
   */
  readonly lengthSort?: boolean;

  /**
   * Sort straight imports by their string length. Similar to [`length-sort`](#lint_isort_length-sort),
   * but applies only to straight imports and doesn't affect `from` imports.
   *
   * @schema IsortOptions#length-sort-straight
   */
  readonly lengthSortStraight?: boolean;

  /**
   * The number of blank lines to place after imports.
   * Use `-1` for automatic determination.
   *
   * Ruff uses at most one blank line after imports in typing stub files (files with `.pyi` extension) in accordance to
   * the typing style recommendations ([source](https://typing.python.org/en/latest/guides/writing_stubs.html#blank-lines)).
   *
   * When using the formatter, only the values `-1`, `1`, and `2` are compatible because
   * it enforces at least one empty and at most two empty lines after imports.
   *
   * @schema IsortOptions#lines-after-imports
   */
  readonly linesAfterImports?: number;

  /**
   * The number of lines to place between "direct" and `import from` imports.
   *
   * When using the formatter, only the values `0` and `1` are compatible because
   * it preserves up to one empty line after imports in nested blocks.
   *
   * @schema IsortOptions#lines-between-types
   */
  readonly linesBetweenTypes?: number;

  /**
   * A list of sections that should _not_ be delineated from the previous
   * section via empty lines.
   *
   * @schema IsortOptions#no-lines-before
   */
  readonly noLinesBefore?: string[];

  /**
   * Put all imports into the same section bucket.
   *
   * For example, rather than separating standard library and third-party imports, as in:
   * ```python
   * import os
   * import sys
   *
   * import numpy
   * import pandas
   * ```
   *
   * Setting `no-sections = true` will instead group all imports into a single section:
   * ```python
   * import numpy
   * import os
   * import pandas
   * import sys
   * ```
   *
   * @schema IsortOptions#no-sections
   */
  readonly noSections?: boolean;

  /**
   * Order imports by type, which is determined by case, in addition to
   * alphabetically.
   *
   * Note that this option takes precedence over the
   * [`case-sensitive`](#lint_isort_case-sensitive) setting when enabled.
   *
   * @schema IsortOptions#order-by-type
   */
  readonly orderByType?: boolean;

  /**
   * Whether to place "closer" imports (fewer `.` characters, most local)
   * before "further" imports (more `.` characters, least local), or vice
   * versa.
   *
   * The default ("furthest-to-closest") is equivalent to isort's
   * [`reverse-relative`](https://pycqa.github.io/isort/docs/configuration/options.html#reverse-relative) default (`reverse-relative = false`); setting
   * this to "closest-to-furthest" is equivalent to isort's
   * `reverse-relative = true`.
   *
   * @schema IsortOptions#relative-imports-order
   */
  readonly relativeImportsOrder?: string;

  /**
   * Add the specified import line to all files.
   *
   * @schema IsortOptions#required-imports
   */
  readonly requiredImports?: string[];

  /**
   * Override in which order the sections should be output. Can be used to move custom sections.
   *
   * @schema IsortOptions#section-order
   */
  readonly sectionOrder?: string[];

  /**
   * A list of mappings from section names to modules.
   *
   * By default, imports are categorized according to their type (e.g., `future`, `third-party`,
   * and so on). This setting allows you to group modules into custom sections, to augment or
   * override the built-in sections.
   *
   * For example, to group all testing utilities, you could create a `testing` section:
   * ```toml
   * testing = ["pytest", "hypothesis"]
   * ```
   *
   * The values in the list are treated as glob patterns. For example, to match all packages in
   * the LangChain ecosystem (`langchain-core`, `langchain-openai`, etc.):
   * ```toml
   * langchain = ["langchain-*"]
   * ```
   *
   * Custom sections should typically be inserted into the [`section-order`](#lint_isort_section-order) list to ensure that
   * they're displayed as a standalone group and in the intended order, as in:
   * ```toml
   * section-order = [
   * "future",
   * "standard-library",
   * "third-party",
   * "first-party",
   * "local-folder",
   * "testing"
   * ]
   * ```
   *
   * If a custom section is omitted from [`section-order`](#lint_isort_section-order), imports in that section will be
   * assigned to the [`default-section`](#lint_isort_default-section) (which defaults to `third-party`).
   *
   * @schema IsortOptions#sections
   */
  readonly sections?: { [key: string]: string[] };

  /**
   * One or more modules to exclude from the single line rule.
   *
   * @schema IsortOptions#single-line-exclusions
   */
  readonly singleLineExclusions?: string[];

  /**
   * If a comma is placed after the last member in a multi-line import, then
   * the imports will never be folded into one line.
   *
   * See isort's [`split-on-trailing-comma`](https://pycqa.github.io/isort/docs/configuration/options.html#split-on-trailing-comma) option.
   *
   * When using the formatter, ensure that [`format.skip-magic-trailing-comma`](#format_skip-magic-trailing-comma) is set to `false` (default) when enabling `split-on-trailing-comma`
   * to avoid that the formatter removes the trailing commas.
   *
   * @schema IsortOptions#split-on-trailing-comma
   */
  readonly splitOnTrailingComma?: boolean;

  /**
   * An override list of tokens to always recognize as a var
   * for [`order-by-type`](#lint_isort_order-by-type) regardless of casing.
   *
   * @schema IsortOptions#variables
   */
  readonly variables?: string[];
}

/**
 * Converts an object of type 'IsortOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_IsortOptions(obj: IsortOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'case-sensitive': obj.caseSensitive,
    'classes': obj.classes?.map(y => y),
    'combine-as-imports': obj.combineAsImports,
    'constants': obj.constants?.map(y => y),
    'default-section': obj.defaultSection,
    'detect-same-package': obj.detectSamePackage,
    'extra-standard-library': obj.extraStandardLibrary?.map(y => y),
    'force-single-line': obj.forceSingleLine,
    'force-sort-within-sections': obj.forceSortWithinSections,
    'force-to-top': obj.forceToTop?.map(y => y),
    'force-wrap-aliases': obj.forceWrapAliases,
    'forced-separate': obj.forcedSeparate?.map(y => y),
    'from-first': obj.fromFirst,
    'known-first-party': obj.knownFirstParty?.map(y => y),
    'known-local-folder': obj.knownLocalFolder?.map(y => y),
    'known-third-party': obj.knownThirdParty?.map(y => y),
    'length-sort': obj.lengthSort,
    'length-sort-straight': obj.lengthSortStraight,
    'lines-after-imports': obj.linesAfterImports,
    'lines-between-types': obj.linesBetweenTypes,
    'no-lines-before': obj.noLinesBefore?.map(y => y),
    'no-sections': obj.noSections,
    'order-by-type': obj.orderByType,
    'relative-imports-order': obj.relativeImportsOrder,
    'required-imports': obj.requiredImports?.map(y => y),
    'section-order': obj.sectionOrder?.map(y => y),
    'sections': ((obj.sections) === undefined) ? undefined : (Object.entries(obj.sections).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1]?.map(y => y) }), {})),
    'single-line-exclusions': obj.singleLineExclusions?.map(y => y),
    'split-on-trailing-comma': obj.splitOnTrailingComma,
    'variables': obj.variables?.map(y => y),
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Configures how Ruff checks your code.
 *
 * Options specified in the `lint` section take precedence over the deprecated top-level settings.
 *
 * @schema LintOptions
 */
export interface LintOptions {
  /**
   * A list of allowed "confusable" Unicode characters to ignore when
   * enforcing `RUF001`, `RUF002`, and `RUF003`.
   *
   * @schema LintOptions#allowed-confusables
   */
  readonly allowedConfusables?: string[];

  /**
   * A regular expression used to identify "dummy" variables, or those which
   * should be ignored when enforcing (e.g.) unused-variable rules. The
   * default expression matches `_`, `__`, and `_var`, but not `_var_`.
   *
   * @schema LintOptions#dummy-variable-rgx
   */
  readonly dummyVariableRgx?: string;

  /**
   * A list of file patterns to exclude from linting in addition to the files excluded globally (see [`exclude`](#exclude), and [`extend-exclude`](#extend-exclude)).
   *
   * Exclusions are based on globs, and can be either:
   *
   * - Single-path patterns, like `.mypy_cache` (to exclude any directory
   * named `.mypy_cache` in the tree), `foo.py` (to exclude any file named
   * `foo.py`), or `foo_*.py` (to exclude any file matching `foo_*.py` ).
   * - Relative patterns, like `directory/foo.py` (to exclude that specific
   * file) or `directory/*.py` (to exclude any Python files in
   * `directory`). Note that these paths are relative to the project root
   * (e.g., the directory containing your `pyproject.toml`).
   *
   * For more information on the glob syntax, refer to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).
   *
   * @schema LintOptions#exclude
   */
  readonly exclude?: string[];

  /**
   * Whether to require exact codes to select preview rules. When enabled,
   * preview rules will not be selected by prefixes — the full code of each
   * preview rule will be required to enable the rule.
   *
   * @schema LintOptions#explicit-preview-rules
   */
  readonly explicitPreviewRules?: boolean;

  /**
   * A list of rule codes or prefixes to consider fixable, in addition to those
   * specified by [`fixable`](#lint_fixable).
   *
   * @schema LintOptions#extend-fixable
   */
  readonly extendFixable?: RuleSelector[];

  /**
   * A list of rule codes or prefixes to ignore, in addition to those
   * specified by `ignore`.
   *
   * @schema LintOptions#extend-ignore
   */
  readonly extendIgnore?: RuleSelector[];

  /**
   * A list of mappings from file pattern to rule codes or prefixes to
   * exclude, in addition to any rules excluded by [`per-file-ignores`](#lint_per-file-ignores).
   *
   * @schema LintOptions#extend-per-file-ignores
   */
  readonly extendPerFileIgnores?: { [key: string]: RuleSelector[] };

  /**
   * A list of rule codes or prefixes for which unsafe fixes should be considered
   * safe.
   *
   * @schema LintOptions#extend-safe-fixes
   */
  readonly extendSafeFixes?: RuleSelector[];

  /**
   * A list of rule codes or prefixes to enable, in addition to those
   * specified by [`select`](#lint_select).
   *
   * @schema LintOptions#extend-select
   */
  readonly extendSelect?: RuleSelector[];

  /**
   * A list of rule codes or prefixes to consider non-auto-fixable, in addition to those
   * specified by [`unfixable`](#lint_unfixable).
   *
   * @schema LintOptions#extend-unfixable
   */
  readonly extendUnfixable?: RuleSelector[];

  /**
   * A list of rule codes or prefixes for which safe fixes should be considered
   * unsafe.
   *
   * @schema LintOptions#extend-unsafe-fixes
   */
  readonly extendUnsafeFixes?: RuleSelector[];

  /**
   * A list of rule codes or prefixes that are unsupported by Ruff, but should be
   * preserved when (e.g.) validating `# noqa` directives. Useful for
   * retaining `# noqa` directives that cover plugins not yet implemented
   * by Ruff.
   *
   * @schema LintOptions#external
   */
  readonly external?: string[];

  /**
   * A list of rule codes or prefixes to consider fixable. By default,
   * all rules are considered fixable.
   *
   * @schema LintOptions#fixable
   */
  readonly fixable?: RuleSelector[];

  /**
   * Options for the `flake8-annotations` plugin.
   *
   * @schema LintOptions#flake8-annotations
   */
  readonly flake8Annotations?: Flake8AnnotationsOptions;

  /**
   * Options for the `flake8-bandit` plugin.
   *
   * @schema LintOptions#flake8-bandit
   */
  readonly flake8Bandit?: Flake8BanditOptions;

  /**
   * Options for the `flake8-boolean-trap` plugin.
   *
   * @schema LintOptions#flake8-boolean-trap
   */
  readonly flake8BooleanTrap?: Flake8BooleanTrapOptions;

  /**
   * Options for the `flake8-bugbear` plugin.
   *
   * @schema LintOptions#flake8-bugbear
   */
  readonly flake8Bugbear?: Flake8BugbearOptions;

  /**
   * Options for the `flake8-builtins` plugin.
   *
   * @schema LintOptions#flake8-builtins
   */
  readonly flake8Builtins?: Flake8BuiltinsOptions;

  /**
   * Options for the `flake8-comprehensions` plugin.
   *
   * @schema LintOptions#flake8-comprehensions
   */
  readonly flake8Comprehensions?: Flake8ComprehensionsOptions;

  /**
   * Options for the `flake8-copyright` plugin.
   *
   * @schema LintOptions#flake8-copyright
   */
  readonly flake8Copyright?: Flake8CopyrightOptions;

  /**
   * Options for the `flake8-errmsg` plugin.
   *
   * @schema LintOptions#flake8-errmsg
   */
  readonly flake8Errmsg?: Flake8ErrMsgOptions;

  /**
   * Options for the `flake8-gettext` plugin.
   *
   * @schema LintOptions#flake8-gettext
   */
  readonly flake8Gettext?: Flake8GetTextOptions;

  /**
   * Options for the `flake8-implicit-str-concat` plugin.
   *
   * @schema LintOptions#flake8-implicit-str-concat
   */
  readonly flake8ImplicitStrConcat?: Flake8ImplicitStrConcatOptions;

  /**
   * Options for the `flake8-import-conventions` plugin.
   *
   * @schema LintOptions#flake8-import-conventions
   */
  readonly flake8ImportConventions?: Flake8ImportConventionsOptions;

  /**
   * Options for the `flake8-pytest-style` plugin.
   *
   * @schema LintOptions#flake8-pytest-style
   */
  readonly flake8PytestStyle?: Flake8PytestStyleOptions;

  /**
   * Options for the `flake8-quotes` plugin.
   *
   * @schema LintOptions#flake8-quotes
   */
  readonly flake8Quotes?: Flake8QuotesOptions;

  /**
   * Options for the `flake8_self` plugin.
   *
   * @schema LintOptions#flake8-self
   */
  readonly flake8Self?: Flake8SelfOptions;

  /**
   * Options for the `flake8-tidy-imports` plugin.
   *
   * @schema LintOptions#flake8-tidy-imports
   */
  readonly flake8TidyImports?: Flake8TidyImportsOptions;

  /**
   * Options for the `flake8-type-checking` plugin.
   *
   * @schema LintOptions#flake8-type-checking
   */
  readonly flake8TypeChecking?: Flake8TypeCheckingOptions;

  /**
   * Options for the `flake8-unused-arguments` plugin.
   *
   * @schema LintOptions#flake8-unused-arguments
   */
  readonly flake8UnusedArguments?: Flake8UnusedArgumentsOptions;

  /**
   * Whether to allow rules to add `from __future__ import annotations` in cases where this would
   * simplify a fix or enable a new diagnostic.
   *
   * For example, `TC001`, `TC002`, and `TC003` can move more imports into `TYPE_CHECKING` blocks
   * if `__future__` annotations are enabled.
   *
   * @schema LintOptions#future-annotations
   */
  readonly futureAnnotations?: boolean;

  /**
   * A list of rule codes or prefixes to ignore. Prefixes can specify exact
   * rules (like `F841`), entire categories (like `F`), or anything in
   * between.
   *
   * When breaking ties between enabled and disabled rules (via `select` and
   * `ignore`, respectively), more specific prefixes override less
   * specific prefixes. `ignore` takes precedence over `select` if the same
   * prefix appears in both.
   *
   * @schema LintOptions#ignore
   */
  readonly ignore?: RuleSelector[];

  /**
   * Avoid automatically removing unused imports in `__init__.py` files. Such
   * imports will still be flagged, but with a dedicated message suggesting
   * that the import is either added to the module's `__all__` symbol, or
   * re-exported with a redundant alias (e.g., `import os as os`).
   *
   * This option is enabled by default, but you can opt-in to removal of imports
   * via an unsafe fix.
   *
   * @schema LintOptions#ignore-init-module-imports
   */
  readonly ignoreInitModuleImports?: boolean;

  /**
   * Options for the `isort` plugin.
   *
   * @schema LintOptions#isort
   */
  readonly isort?: IsortOptions;

  /**
   * A list of objects that should be treated equivalently to a
   * `logging.Logger` object.
   *
   * This is useful for ensuring proper diagnostics (e.g., to identify
   * `logging` deprecations and other best-practices) for projects that
   * re-export a `logging.Logger` object from a common module.
   *
   * For example, if you have a module `logging_setup.py` with the following
   * contents:
   * ```python
   * import logging
   *
   * logger = logging.getLogger(__name__)
   * ```
   *
   * Adding `"logging_setup.logger"` to `logger-objects` will ensure that
   * `logging_setup.logger` is treated as a `logging.Logger` object when
   * imported from other modules (e.g., `from logging_setup import logger`).
   *
   * @schema LintOptions#logger-objects
   */
  readonly loggerObjects?: string[];

  /**
   * Options for the `mccabe` plugin.
   *
   * @schema LintOptions#mccabe
   */
  readonly mccabe?: McCabeOptions;

  /**
   * Options for the `pep8-naming` plugin.
   *
   * @schema LintOptions#pep8-naming
   */
  readonly pep8Naming?: Pep8NamingOptions;

  /**
   * A list of mappings from file pattern to rule codes or prefixes to
   * exclude, when considering any matching files. An initial '!' negates
   * the file pattern.
   *
   * @schema LintOptions#per-file-ignores
   */
  readonly perFileIgnores?: { [key: string]: RuleSelector[] };

  /**
   * Whether to enable preview mode. When preview mode is enabled, Ruff will
   * use unstable rules and fixes.
   *
   * @schema LintOptions#preview
   */
  readonly preview?: boolean;

  /**
   * Options for the `pycodestyle` plugin.
   *
   * @schema LintOptions#pycodestyle
   */
  readonly pycodestyle?: PycodestyleOptions;

  /**
   * Options for the `pydoclint` plugin.
   *
   * @schema LintOptions#pydoclint
   */
  readonly pydoclint?: PydoclintOptions;

  /**
   * Options for the `pydocstyle` plugin.
   *
   * @schema LintOptions#pydocstyle
   */
  readonly pydocstyle?: PydocstyleOptions;

  /**
   * Options for the `pyflakes` plugin.
   *
   * @schema LintOptions#pyflakes
   */
  readonly pyflakes?: PyflakesOptions;

  /**
   * Options for the `pylint` plugin.
   *
   * @schema LintOptions#pylint
   */
  readonly pylint?: PylintOptions;

  /**
   * Options for the `pyupgrade` plugin.
   *
   * @schema LintOptions#pyupgrade
   */
  readonly pyupgrade?: PyUpgradeOptions;

  /**
   * Options for the `ruff` plugin
   *
   * @schema LintOptions#ruff
   */
  readonly ruff?: RuffOptions;

  /**
   * A list of rule codes or prefixes to enable. Prefixes can specify exact
   * rules (like `F841`), entire categories (like `F`), or anything in
   * between.
   *
   * When breaking ties between enabled and disabled rules (via `select` and
   * `ignore`, respectively), more specific prefixes override less
   * specific prefixes. `ignore` takes precedence over `select` if the
   * same prefix appears in both.
   *
   * @schema LintOptions#select
   */
  readonly select?: RuleSelector[];

  /**
   * A list of task tags to recognize (e.g., "TODO", "FIXME", "XXX").
   *
   * Comments starting with these tags will be ignored by commented-out code
   * detection (`ERA`), and skipped by line-length rules (`E501`) if
   * [`ignore-overlong-task-comments`](#lint_pycodestyle_ignore-overlong-task-comments) is set to `true`.
   *
   * @schema LintOptions#task-tags
   */
  readonly taskTags?: string[];

  /**
   * Whether to allow imports from the third-party `typing_extensions` module for Python versions
   * before a symbol was added to the first-party `typing` module.
   *
   * Many rules try to import symbols from the `typing` module but fall back to
   * `typing_extensions` for earlier versions of Python. This option can be used to disable this
   * fallback behavior in cases where `typing_extensions` is not installed.
   *
   * @schema LintOptions#typing-extensions
   */
  readonly typingExtensions?: boolean;

  /**
   * A list of modules whose exports should be treated equivalently to
   * members of the `typing` module.
   *
   * This is useful for ensuring proper type annotation inference for
   * projects that re-export `typing` and `typing_extensions` members
   * from a compatibility module. If omitted, any members imported from
   * modules apart from `typing` and `typing_extensions` will be treated
   * as ordinary Python objects.
   *
   * @schema LintOptions#typing-modules
   */
  readonly typingModules?: string[];

  /**
   * A list of rule codes or prefixes to consider non-fixable.
   *
   * @schema LintOptions#unfixable
   */
  readonly unfixable?: RuleSelector[];
}

/**
 * Converts an object of type 'LintOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_LintOptions(obj: LintOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'allowed-confusables': obj.allowedConfusables?.map(y => y),
    'dummy-variable-rgx': obj.dummyVariableRgx,
    'exclude': obj.exclude?.map(y => y),
    'explicit-preview-rules': obj.explicitPreviewRules,
    'extend-fixable': obj.extendFixable?.map(y => y),
    'extend-ignore': obj.extendIgnore?.map(y => y),
    'extend-per-file-ignores': ((obj.extendPerFileIgnores) === undefined) ? undefined : (Object.entries(obj.extendPerFileIgnores).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1]?.map(y => y) }), {})),
    'extend-safe-fixes': obj.extendSafeFixes?.map(y => y),
    'extend-select': obj.extendSelect?.map(y => y),
    'extend-unfixable': obj.extendUnfixable?.map(y => y),
    'extend-unsafe-fixes': obj.extendUnsafeFixes?.map(y => y),
    'external': obj.external?.map(y => y),
    'fixable': obj.fixable?.map(y => y),
    'flake8-annotations': toJson_Flake8AnnotationsOptions(obj.flake8Annotations),
    'flake8-bandit': toJson_Flake8BanditOptions(obj.flake8Bandit),
    'flake8-boolean-trap': toJson_Flake8BooleanTrapOptions(obj.flake8BooleanTrap),
    'flake8-bugbear': toJson_Flake8BugbearOptions(obj.flake8Bugbear),
    'flake8-builtins': toJson_Flake8BuiltinsOptions(obj.flake8Builtins),
    'flake8-comprehensions': toJson_Flake8ComprehensionsOptions(obj.flake8Comprehensions),
    'flake8-copyright': toJson_Flake8CopyrightOptions(obj.flake8Copyright),
    'flake8-errmsg': toJson_Flake8ErrMsgOptions(obj.flake8Errmsg),
    'flake8-gettext': toJson_Flake8GetTextOptions(obj.flake8Gettext),
    'flake8-implicit-str-concat': toJson_Flake8ImplicitStrConcatOptions(obj.flake8ImplicitStrConcat),
    'flake8-import-conventions': toJson_Flake8ImportConventionsOptions(obj.flake8ImportConventions),
    'flake8-pytest-style': toJson_Flake8PytestStyleOptions(obj.flake8PytestStyle),
    'flake8-quotes': toJson_Flake8QuotesOptions(obj.flake8Quotes),
    'flake8-self': toJson_Flake8SelfOptions(obj.flake8Self),
    'flake8-tidy-imports': toJson_Flake8TidyImportsOptions(obj.flake8TidyImports),
    'flake8-type-checking': toJson_Flake8TypeCheckingOptions(obj.flake8TypeChecking),
    'flake8-unused-arguments': toJson_Flake8UnusedArgumentsOptions(obj.flake8UnusedArguments),
    'future-annotations': obj.futureAnnotations,
    'ignore': obj.ignore?.map(y => y),
    'ignore-init-module-imports': obj.ignoreInitModuleImports,
    'isort': toJson_IsortOptions(obj.isort),
    'logger-objects': obj.loggerObjects?.map(y => y),
    'mccabe': toJson_McCabeOptions(obj.mccabe),
    'pep8-naming': toJson_Pep8NamingOptions(obj.pep8Naming),
    'per-file-ignores': ((obj.perFileIgnores) === undefined) ? undefined : (Object.entries(obj.perFileIgnores).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1]?.map(y => y) }), {})),
    'preview': obj.preview,
    'pycodestyle': toJson_PycodestyleOptions(obj.pycodestyle),
    'pydoclint': toJson_PydoclintOptions(obj.pydoclint),
    'pydocstyle': toJson_PydocstyleOptions(obj.pydocstyle),
    'pyflakes': toJson_PyflakesOptions(obj.pyflakes),
    'pylint': toJson_PylintOptions(obj.pylint),
    'pyupgrade': toJson_PyUpgradeOptions(obj.pyupgrade),
    'ruff': toJson_RuffOptions(obj.ruff),
    'select': obj.select?.map(y => y),
    'task-tags': obj.taskTags?.map(y => y),
    'typing-extensions': obj.typingExtensions,
    'typing-modules': obj.typingModules?.map(y => y),
    'unfixable': obj.unfixable?.map(y => y),
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `mccabe` plugin.
 *
 * @schema McCabeOptions
 */
export interface McCabeOptions {
  /**
   * The maximum McCabe complexity to allow before triggering `C901` errors.
   *
   * @schema McCabeOptions#max-complexity
   */
  readonly maxComplexity?: number;
}

/**
 * Converts an object of type 'McCabeOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_McCabeOptions(obj: McCabeOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'max-complexity': obj.maxComplexity,
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * @schema OutputFormat
 */
export enum OutputFormat {
  /** concise */
  CONCISE = "concise",
  /** full */
  FULL = "full",
  /** json */
  JSON = "json",
  /** json-lines */
  JSON_HYPHEN_LINES = "json-lines",
  /** junit */
  JUNIT = "junit",
  /** grouped */
  GROUPED = "grouped",
  /** github */
  GITHUB = "github",
  /** gitlab */
  GITLAB = "gitlab",
  /** pylint */
  PYLINT = "pylint",
  /** rdjson */
  RDJSON = "rdjson",
  /** azure */
  AZURE = "azure",
  /** sarif */
  SARIF = "sarif",
}

/**
 * Options for the `pep8-naming` plugin.
 *
 * @schema Pep8NamingOptions
 */
export interface Pep8NamingOptions {
  /**
   * A list of decorators that, when applied to a method, indicate that the
   * method should be treated as a class method (in addition to the builtin
   * `@classmethod`).
   *
   * For example, Ruff will expect that any method decorated by a decorator
   * in this list takes a `cls` argument as its first argument.
   *
   * Expects to receive a list of fully-qualified names (e.g., `pydantic.validator`,
   * rather than `validator`) or alternatively a plain name which is then matched against
   * the last segment in case the decorator itself consists of a dotted name.
   *
   * @schema Pep8NamingOptions#classmethod-decorators
   */
  readonly classmethodDecorators?: string[];

  /**
   * Additional names (or patterns) to ignore when considering `pep8-naming` violations,
   * in addition to those included in [`ignore-names`](#lint_pep8-naming_ignore-names).
   *
   * Supports glob patterns. For example, to ignore all names starting with `test_`
   * or ending with `_test`, you could use `ignore-names = ["test_*", "*_test"]`.
   * For more information on the glob syntax, refer to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).
   *
   * @schema Pep8NamingOptions#extend-ignore-names
   */
  readonly extendIgnoreNames?: string[];

  /**
   * A list of names (or patterns) to ignore when considering `pep8-naming` violations.
   *
   * Supports glob patterns. For example, to ignore all names starting with `test_`
   * or ending with `_test`, you could use `ignore-names = ["test_*", "*_test"]`.
   * For more information on the glob syntax, refer to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).
   *
   * @schema Pep8NamingOptions#ignore-names
   */
  readonly ignoreNames?: string[];

  /**
   * A list of decorators that, when applied to a method, indicate that the
   * method should be treated as a static method (in addition to the builtin
   * `@staticmethod`).
   *
   * For example, Ruff will expect that any method decorated by a decorator
   * in this list has no `self` or `cls` argument.
   *
   * Expects to receive a list of fully-qualified names (e.g., `belay.Device.teardown`,
   * rather than `teardown`) or alternatively a plain name which is then matched against
   * the last segment in case the decorator itself consists of a dotted name.
   *
   * @schema Pep8NamingOptions#staticmethod-decorators
   */
  readonly staticmethodDecorators?: string[];
}

/**
 * Converts an object of type 'Pep8NamingOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_Pep8NamingOptions(obj: Pep8NamingOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'classmethod-decorators': obj.classmethodDecorators?.map(y => y),
    'extend-ignore-names': obj.extendIgnoreNames?.map(y => y),
    'ignore-names': obj.ignoreNames?.map(y => y),
    'staticmethod-decorators': obj.staticmethodDecorators?.map(y => y),
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * @schema PythonVersion
 */
export enum PythonVersion {
  /** py37 */
  PY37 = "py37",
  /** py38 */
  PY38 = "py38",
  /** py39 */
  PY39 = "py39",
  /** py310 */
  PY310 = "py310",
  /** py311 */
  PY311 = "py311",
  /** py312 */
  PY312 = "py312",
  /** py313 */
  PY313 = "py313",
  /** py314 */
  PY314 = "py314",
}

/**
 * Options for the `pycodestyle` plugin.
 *
 * @schema PycodestyleOptions
 */
export interface PycodestyleOptions {
  /**
   * Whether line-length violations (`E501`) should be triggered for
   * comments starting with [`task-tags`](#lint_task-tags) (by default: "TODO", "FIXME",
   * and "XXX").
   *
   * @schema PycodestyleOptions#ignore-overlong-task-comments
   */
  readonly ignoreOverlongTaskComments?: boolean;

  /**
   * The maximum line length to allow for [`doc-line-too-long`](https://docs.astral.sh/ruff/rules/doc-line-too-long/) violations within
   * documentation (`W505`), including standalone comments. By default,
   * this is set to `null` which disables reporting violations.
   *
   * The length is determined by the number of characters per line, except for lines containing Asian characters or emojis.
   * For these lines, the [unicode width](https://unicode.org/reports/tr11/) of each character is added up to determine the length.
   *
   * See the [`doc-line-too-long`](https://docs.astral.sh/ruff/rules/doc-line-too-long/) rule for more information.
   *
   * @schema PycodestyleOptions#max-doc-length
   */
  readonly maxDocLength?: number;

  /**
   * The maximum line length to allow for [`line-too-long`](https://docs.astral.sh/ruff/rules/line-too-long/) violations. By default,
   * this is set to the value of the [`line-length`](#line-length) option.
   *
   * Use this option when you want to detect extra-long lines that the formatter can't automatically split by setting
   * `pycodestyle.line-length` to a value larger than [`line-length`](#line-length).
   *
   * ```toml
   * # The formatter wraps lines at a length of 88.
   * line-length = 88
   *
   * [pycodestyle]
   * # E501 reports lines that exceed the length of 100.
   * max-line-length = 100
   * ```
   *
   * The length is determined by the number of characters per line, except for lines containing East Asian characters or emojis.
   * For these lines, the [unicode width](https://unicode.org/reports/tr11/) of each character is added up to determine the length.
   *
   * See the [`line-too-long`](https://docs.astral.sh/ruff/rules/line-too-long/) rule for more information.
   *
   * @schema PycodestyleOptions#max-line-length
   */
  readonly maxLineLength?: number;
}

/**
 * Converts an object of type 'PycodestyleOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_PycodestyleOptions(obj: PycodestyleOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'ignore-overlong-task-comments': obj.ignoreOverlongTaskComments,
    'max-doc-length': obj.maxDocLength,
    'max-line-length': obj.maxLineLength,
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `pydocstyle` plugin.
 *
 * @schema PydocstyleOptions
 */
export interface PydocstyleOptions {
  /**
   * Whether to use Google-style, NumPy-style conventions, or the [PEP 257](https://peps.python.org/pep-0257/)
   * defaults when analyzing docstring sections.
   *
   * Enabling a convention will disable all rules that are not included in
   * the specified convention. As such, the intended workflow is to enable a
   * convention and then selectively enable or disable any additional rules
   * on top of it.
   *
   * For example, to use Google-style conventions but avoid requiring
   * documentation for every function parameter:
   *
   * ```toml
   * [tool.ruff.lint]
   * # Enable all `pydocstyle` rules, limiting to those that adhere to the
   * # Google convention via `convention = "google"`, below.
   * select = ["D"]
   *
   * # On top of the Google convention, disable `D417`, which requires
   * # documentation for every function parameter.
   * ignore = ["D417"]
   *
   * [tool.ruff.lint.pydocstyle]
   * convention = "google"
   * ```
   *
   * To enable an additional rule that's excluded from the convention,
   * select the desired rule via its fully qualified rule code (e.g.,
   * `D400` instead of `D4` or `D40`):
   *
   * ```toml
   * [tool.ruff.lint]
   * # Enable D400 on top of the Google convention.
   * extend-select = ["D400"]
   *
   * [tool.ruff.lint.pydocstyle]
   * convention = "google"
   * ```
   *
   * @schema PydocstyleOptions#convention
   */
  readonly convention?: string;

  /**
   * Ignore docstrings for functions or methods decorated with the
   * specified fully-qualified decorators.
   *
   * @schema PydocstyleOptions#ignore-decorators
   */
  readonly ignoreDecorators?: string[];

  /**
   * If set to `true`, ignore missing documentation for `*args` and `**kwargs` parameters.
   *
   * @schema PydocstyleOptions#ignore-var-parameters
   */
  readonly ignoreVarParameters?: boolean;

  /**
   * A list of decorators that, when applied to a method, indicate that the
   * method should be treated as a property (in addition to the builtin
   * `@property` and standard-library `@functools.cached_property`).
   *
   * For example, Ruff will expect that any method decorated by a decorator
   * in this list can use a non-imperative summary line.
   *
   * @schema PydocstyleOptions#property-decorators
   */
  readonly propertyDecorators?: string[];
}

/**
 * Converts an object of type 'PydocstyleOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_PydocstyleOptions(obj: PydocstyleOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'convention': obj.convention,
    'ignore-decorators': obj.ignoreDecorators?.map(y => y),
    'ignore-var-parameters': obj.ignoreVarParameters,
    'property-decorators': obj.propertyDecorators?.map(y => y),
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `pyflakes` plugin.
 *
 * @schema PyflakesOptions
 */
export interface PyflakesOptions {
  /**
   * A list of modules to ignore when considering unused imports.
   *
   * Used to prevent violations for specific modules that are known to have side effects on
   * import (e.g., `hvplot.pandas`).
   *
   * Modules in this list are expected to be fully-qualified names (e.g., `hvplot.pandas`). Any
   * submodule of a given module will also be ignored (e.g., given `hvplot`, `hvplot.pandas`
   * will also be ignored).
   *
   * @schema PyflakesOptions#allowed-unused-imports
   */
  readonly allowedUnusedImports?: string[];

  /**
   * Additional functions or classes to consider generic, such that any
   * subscripts should be treated as type annotation (e.g., `ForeignKey` in
   * `django.db.models.ForeignKey["User"]`.
   *
   * Expects to receive a list of fully-qualified names (e.g., `django.db.models.ForeignKey`,
   * rather than `ForeignKey`).
   *
   * @schema PyflakesOptions#extend-generics
   */
  readonly extendGenerics?: string[];
}

/**
 * Converts an object of type 'PyflakesOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_PyflakesOptions(obj: PyflakesOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'allowed-unused-imports': obj.allowedUnusedImports?.map(y => y),
    'extend-generics': obj.extendGenerics?.map(y => y),
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `pylint` plugin.
 *
 * @schema PylintOptions
 */
export interface PylintOptions {
  /**
   * Dunder methods name to allow, in addition to the default set from the
   * Python standard library (see `PLW3201`).
   *
   * @schema PylintOptions#allow-dunder-method-names
   */
  readonly allowDunderMethodNames?: string[];

  /**
   * Constant types to ignore when used as "magic values" (see `PLR2004`).
   *
   * @schema PylintOptions#allow-magic-value-types
   */
  readonly allowMagicValueTypes?: ConstantType[];

  /**
   * Maximum number of arguments allowed for a function or method definition
   * (see `PLR0913`).
   *
   * @schema PylintOptions#max-args
   */
  readonly maxArgs?: number;

  /**
   * Maximum number of Boolean expressions allowed within a single `if` statement
   * (see `PLR0916`).
   *
   * @schema PylintOptions#max-bool-expr
   */
  readonly maxBoolExpr?: number;

  /**
   * Maximum number of branches allowed for a function or method body (see `PLR0912`).
   *
   * @schema PylintOptions#max-branches
   */
  readonly maxBranches?: number;

  /**
   * Maximum number of local variables allowed for a function or method body (see `PLR0914`).
   *
   * @schema PylintOptions#max-locals
   */
  readonly maxLocals?: number;

  /**
   * Maximum number of nested blocks allowed within a function or method body
   * (see `PLR1702`).
   *
   * @schema PylintOptions#max-nested-blocks
   */
  readonly maxNestedBlocks?: number;

  /**
   * Maximum number of positional arguments allowed for a function or method definition
   * (see `PLR0917`).
   *
   * If not specified, defaults to the value of `max-args`.
   *
   * @schema PylintOptions#max-positional-args
   */
  readonly maxPositionalArgs?: number;

  /**
   * Maximum number of public methods allowed for a class (see `PLR0904`).
   *
   * @schema PylintOptions#max-public-methods
   */
  readonly maxPublicMethods?: number;

  /**
   * Maximum number of return statements allowed for a function or method
   * body (see `PLR0911`)
   *
   * @schema PylintOptions#max-returns
   */
  readonly maxReturns?: number;

  /**
   * Maximum number of statements allowed for a function or method body (see `PLR0915`).
   *
   * @schema PylintOptions#max-statements
   */
  readonly maxStatements?: number;
}

/**
 * Converts an object of type 'PylintOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_PylintOptions(obj: PylintOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'allow-dunder-method-names': obj.allowDunderMethodNames?.map(y => y),
    'allow-magic-value-types': obj.allowMagicValueTypes?.map(y => y),
    'max-args': obj.maxArgs,
    'max-bool-expr': obj.maxBoolExpr,
    'max-branches': obj.maxBranches,
    'max-locals': obj.maxLocals,
    'max-nested-blocks': obj.maxNestedBlocks,
    'max-positional-args': obj.maxPositionalArgs,
    'max-public-methods': obj.maxPublicMethods,
    'max-returns': obj.maxReturns,
    'max-statements': obj.maxStatements,
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `pyupgrade` plugin.
 *
 * @schema PyUpgradeOptions
 */
export interface PyUpgradeOptions {
  /**
   * Whether to avoid [PEP 585](https://peps.python.org/pep-0585/) (`List[int]` -> `list[int]`) and [PEP 604](https://peps.python.org/pep-0604/)
   * (`Union[str, int]` -> `str | int`) rewrites even if a file imports
   * `from __future__ import annotations`.
   *
   * This setting is only applicable when the target Python version is below
   * 3.9 and 3.10 respectively, and is most commonly used when working with
   * libraries like Pydantic and FastAPI, which rely on the ability to parse
   * type annotations at runtime. The use of `from __future__ import annotations`
   * causes Python to treat the type annotations as strings, which typically
   * allows for the use of language features that appear in later Python
   * versions but are not yet supported by the current version (e.g., `str |
   * int`). However, libraries that rely on runtime type annotations will
   * break if the annotations are incompatible with the current Python
   * version.
   *
   * For example, while the following is valid Python 3.8 code due to the
   * presence of `from __future__ import annotations`, the use of `str | int`
   * prior to Python 3.10 will cause Pydantic to raise a `TypeError` at
   * runtime:
   *
   * ```python
   * from __future__ import annotations
   *
   * import pydantic
   *
   * class Foo(pydantic.BaseModel):
   * bar: str | int
   * ```
   *
   * @schema PyUpgradeOptions#keep-runtime-typing
   */
  readonly keepRuntimeTyping?: boolean;
}

/**
 * Converts an object of type 'PyUpgradeOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_PyUpgradeOptions(obj: PyUpgradeOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'keep-runtime-typing': obj.keepRuntimeTyping,
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * @schema ParametrizeNameType
 */
export enum ParametrizeNameType {
  /** csv */
  CSV = "csv",
  /** tuple */
  TUPLE = "tuple",
  /** list */
  LIST = "list",
}

/**
 * @schema ParametrizeValuesRowType
 */
export enum ParametrizeValuesRowType {
  /** tuple */
  TUPLE = "tuple",
  /** list */
  LIST = "list",
}

/**
 * @schema ParametrizeValuesType
 */
export enum ParametrizeValuesType {
  /** tuple */
  TUPLE = "tuple",
  /** list */
  LIST = "list",
}

/**
 * @schema ApiBan
 */
export interface ApiBan {
  /**
   * The message to display when the API is used.
   *
   * @schema ApiBan#msg
   */
  readonly msg: string;
}

/**
 * Converts an object of type 'ApiBan' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ApiBan(obj: ApiBan | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'msg': obj.msg,
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * @schema QuoteStyle
 */
export enum QuoteStyle {
  /** single */
  SINGLE = "single",
  /** double */
  DOUBLE = "double",
  /** preserve */
  PRESERVE = "preserve",
}

/**
 * Options for the `pydoclint` plugin.
 *
 * @schema PydoclintOptions
 */
export interface PydoclintOptions {
  /**
   * Skip docstrings which fit on a single line.
   *
   * Note: The corresponding setting in `pydoclint`
   * is named `skip-checking-short-docstrings`.
   *
   * @schema PydoclintOptions#ignore-one-line-docstrings
   */
  readonly ignoreOneLineDocstrings?: boolean;
}

/**
 * Converts an object of type 'PydoclintOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_PydoclintOptions(obj: PydoclintOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'ignore-one-line-docstrings': obj.ignoreOneLineDocstrings,
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Options for the `ruff` plugin
 *
 * @schema RuffOptions
 */
export interface RuffOptions {
  /**
   * A list of callable names, whose result may be safely passed into
   * [`markupsafe.Markup`](https://markupsafe.palletsprojects.com/en/stable/escaping/#markupsafe.Markup).
   *
   * Expects to receive a list of fully-qualified names (e.g., `bleach.clean`, rather than `clean`).
   *
   * This setting helps you avoid false positives in code like:
   *
   * ```python
   * from bleach import clean
   * from markupsafe import Markup
   *
   * cleaned_markup = Markup(clean(some_user_input))
   * ```
   *
   * Where the use of [`bleach.clean`](https://bleach.readthedocs.io/en/latest/clean.html)
   * usually ensures that there's no XSS vulnerability.
   *
   * Although it is not recommended, you may also use this setting to whitelist other
   * kinds of calls, e.g. calls to i18n translation functions, where how safe that is
   * will depend on the implementation and how well the translations are audited.
   *
   * Another common use-case is to wrap the output of functions that generate markup
   * like [`xml.etree.ElementTree.tostring`](https://docs.python.org/3/library/xml.etree.elementtree.html#xml.etree.ElementTree.tostring)
   * or template rendering engines where sanitization of potential user input is either
   * already baked in or has to happen before rendering.
   *
   * @schema RuffOptions#allowed-markup-calls
   */
  readonly allowedMarkupCalls?: string[];

  /**
   * A list of additional callable names that behave like
   * [`markupsafe.Markup`](https://markupsafe.palletsprojects.com/en/stable/escaping/#markupsafe.Markup).
   *
   * Expects to receive a list of fully-qualified names (e.g., `webhelpers.html.literal`, rather than
   * `literal`).
   *
   * @schema RuffOptions#extend-markup-names
   */
  readonly extendMarkupNames?: string[];

  /**
   * Whether to prefer accessing items keyed by tuples with
   * parentheses around the tuple (see `RUF031`).
   *
   * @schema RuffOptions#parenthesize-tuple-in-subscript
   */
  readonly parenthesizeTupleInSubscript?: boolean;
}

/**
 * Converts an object of type 'RuffOptions' to JSON representation.
 * @internal
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_RuffOptions(obj: RuffOptions | undefined): Record<string, any> | undefined {
  if (obj === undefined) { return undefined; }
  const result = {
    'allowed-markup-calls': obj.allowedMarkupCalls?.map(y => y),
    'extend-markup-names': obj.extendMarkupNames?.map(y => y),
    'parenthesize-tuple-in-subscript': obj.parenthesizeTupleInSubscript,
  };
  // filter undefined values
  return Object.entries(result).reduce((r, i) => (i[1] === undefined) ? r : ({ ...r, [i[0]]: i[1] }), {});
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * @schema ConstantType
 */
export enum ConstantType {
  /** bytes */
  BYTES = "bytes",
  /** complex */
  COMPLEX = "complex",
  /** float */
  FLOAT = "float",
  /** int */
  INT = "int",
  /** str */
  STR = "str",
}
