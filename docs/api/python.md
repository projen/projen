# `python` Submodule <a name="`python` Submodule" id="projen.python"></a>

## Constructs <a name="Constructs" id="Constructs"></a>

### Hatch <a name="Hatch" id="projen.python.Hatch"></a>

Manage project dependencies, virtual environments, and packaging through hatch.

#### Initializers <a name="Initializers" id="projen.python.Hatch.Initializer"></a>

```typescript
import { python } from 'projen'

new python.Hatch(scope: IConstruct, options: PythonBaseOptions)
```

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Hatch.Initializer.parameter.scope">scope</a></code> | <code>constructs.IConstruct</code> | *No description.* |
| <code><a href="#projen.python.Hatch.Initializer.parameter.options">options</a></code> | <code><a href="#projen.python.PythonBaseOptions">PythonBaseOptions</a></code> | *No description.* |

---

##### `scope`<sup>Required</sup> <a name="scope" id="projen.python.Hatch.Initializer.parameter.scope"></a>

- *Type:* constructs.IConstruct

---

##### `options`<sup>Required</sup> <a name="options" id="projen.python.Hatch.Initializer.parameter.options"></a>

- *Type:* <a href="#projen.python.PythonBaseOptions">PythonBaseOptions</a>

---

#### Methods <a name="Methods" id="Methods"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.Hatch.toString">toString</a></code> | Returns a string representation of this construct. |
| <code><a href="#projen.python.Hatch.postSynthesize">postSynthesize</a></code> | Called after synthesis. |
| <code><a href="#projen.python.Hatch.preSynthesize">preSynthesize</a></code> | Called before synthesis. |
| <code><a href="#projen.python.Hatch.synthesize">synthesize</a></code> | Synthesizes files to the project output directory. |
| <code><a href="#projen.python.Hatch.addDependency">addDependency</a></code> | Adds a runtime dependency. |
| <code><a href="#projen.python.Hatch.addDevDependency">addDevDependency</a></code> | Adds a dev dependency. |
| <code><a href="#projen.python.Hatch.getRunCommand">getRunCommand</a></code> | *No description.* |
| <code><a href="#projen.python.Hatch.installDependencies">installDependencies</a></code> | Installs dependencies (called during post-synthesis). |
| <code><a href="#projen.python.Hatch.setupEnvironment">setupEnvironment</a></code> | Initializes the virtual environment if it doesn't exist (called during post-synthesis). |
| <code><a href="#projen.python.Hatch.synthDependencies">synthDependencies</a></code> | *No description.* |
| <code><a href="#projen.python.Hatch.synthDependencyGroups">synthDependencyGroups</a></code> | *No description.* |
| <code><a href="#projen.python.Hatch.synthDevDependencies">synthDevDependencies</a></code> | *No description.* |

---

##### `toString` <a name="toString" id="projen.python.Hatch.toString"></a>

```typescript
public toString(): string
```

Returns a string representation of this construct.

##### `postSynthesize` <a name="postSynthesize" id="projen.python.Hatch.postSynthesize"></a>

```typescript
public postSynthesize(): void
```

Called after synthesis.

Order is *not* guaranteed.

##### `preSynthesize` <a name="preSynthesize" id="projen.python.Hatch.preSynthesize"></a>

```typescript
public preSynthesize(): void
```

Called before synthesis.

##### `synthesize` <a name="synthesize" id="projen.python.Hatch.synthesize"></a>

```typescript
public synthesize(): void
```

Synthesizes files to the project output directory.

##### `addDependency` <a name="addDependency" id="projen.python.Hatch.addDependency"></a>

```typescript
public addDependency(spec: string): void
```

Adds a runtime dependency.

###### `spec`<sup>Required</sup> <a name="spec" id="projen.python.Hatch.addDependency.parameter.spec"></a>

- *Type:* string

Format `<module>@<semver>`.

---

##### `addDevDependency` <a name="addDevDependency" id="projen.python.Hatch.addDevDependency"></a>

```typescript
public addDevDependency(spec: string): void
```

Adds a dev dependency.

###### `spec`<sup>Required</sup> <a name="spec" id="projen.python.Hatch.addDevDependency.parameter.spec"></a>

- *Type:* string

Format `<module>@<semver>`.

---

##### `getRunCommand` <a name="getRunCommand" id="projen.python.Hatch.getRunCommand"></a>

```typescript
public getRunCommand(_options: PythonBaseOptions): string
```

###### `_options`<sup>Required</sup> <a name="_options" id="projen.python.Hatch.getRunCommand.parameter._options"></a>

- *Type:* <a href="#projen.python.PythonBaseOptions">PythonBaseOptions</a>

---

##### `installDependencies` <a name="installDependencies" id="projen.python.Hatch.installDependencies"></a>

```typescript
public installDependencies(): void
```

Installs dependencies (called during post-synthesis).

##### `setupEnvironment` <a name="setupEnvironment" id="projen.python.Hatch.setupEnvironment"></a>

```typescript
public setupEnvironment(): void
```

Initializes the virtual environment if it doesn't exist (called during post-synthesis).

##### `synthDependencies` <a name="synthDependencies" id="projen.python.Hatch.synthDependencies"></a>

```typescript
public synthDependencies(): string[]
```

##### `synthDependencyGroups` <a name="synthDependencyGroups" id="projen.python.Hatch.synthDependencyGroups"></a>

```typescript
public synthDependencyGroups(): {[ key: string ]: any}
```

##### `synthDevDependencies` <a name="synthDevDependencies" id="projen.python.Hatch.synthDevDependencies"></a>

```typescript
public synthDevDependencies(): string[]
```

#### Static Functions <a name="Static Functions" id="Static Functions"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.Hatch.isConstruct">isConstruct</a></code> | Checks if `x` is a construct. |
| <code><a href="#projen.python.Hatch.isComponent">isComponent</a></code> | Test whether the given construct is a component. |

---

##### `isConstruct` <a name="isConstruct" id="projen.python.Hatch.isConstruct"></a>

```typescript
import { python } from 'projen'

python.Hatch.isConstruct(x: any)
```

Checks if `x` is a construct.

Use this method instead of `instanceof` to properly detect `Construct`
instances, even when the construct library is symlinked.

Explanation: in JavaScript, multiple copies of the `constructs` library on
disk are seen as independent, completely different libraries. As a
consequence, the class `Construct` in each copy of the `constructs` library
is seen as a different class, and an instance of one class will not test as
`instanceof` the other class. `npm install` will not create installations
like this, but users may manually symlink construct libraries together or
use a monorepo tool: in those cases, multiple copies of the `constructs`
library can be accidentally installed, and `instanceof` will behave
unpredictably. It is safest to avoid using `instanceof`, and using
this type-testing method instead.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.Hatch.isConstruct.parameter.x"></a>

- *Type:* any

Any object.

---

##### `isComponent` <a name="isComponent" id="projen.python.Hatch.isComponent"></a>

```typescript
import { python } from 'projen'

python.Hatch.isComponent(x: any)
```

Test whether the given construct is a component.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.Hatch.isComponent.parameter.x"></a>

- *Type:* any

---

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Hatch.property.node">node</a></code> | <code>constructs.Node</code> | The tree node. |
| <code><a href="#projen.python.Hatch.property.project">project</a></code> | <code><a href="#projen.python.PythonProject">PythonProject</a></code> | *No description.* |
| <code><a href="#projen.python.Hatch.property.defaultBuildSystem">defaultBuildSystem</a></code> | <code><a href="#projen.python.BuildSystem">BuildSystem</a></code> | *No description.* |
| <code><a href="#projen.python.Hatch.property.installCiTask">installCiTask</a></code> | <code>projen.Task</code> | A task that installs and updates dependencies. |
| <code><a href="#projen.python.Hatch.property.installTask">installTask</a></code> | <code>projen.Task</code> | *No description.* |
| <code><a href="#projen.python.Hatch.property.publishTask">publishTask</a></code> | <code>projen.Task</code> | A task that uploads the package to a package repository. |
| <code><a href="#projen.python.Hatch.property.runCommand">runCommand</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.Hatch.property.formatTask">formatTask</a></code> | <code>projen.Task</code> | A task that runs formatting. |
| <code><a href="#projen.python.Hatch.property.lintTask">lintTask</a></code> | <code>projen.Task</code> | A task that runs linting. |
| <code><a href="#projen.python.Hatch.property.typeCheckTask">typeCheckTask</a></code> | <code>projen.Task</code> | A task that runs static type checking. |
| <code><a href="#projen.python.Hatch.property.publishTestTask">publishTestTask</a></code> | <code>projen.Task</code> | *No description.* |

---

##### `node`<sup>Required</sup> <a name="node" id="projen.python.Hatch.property.node"></a>

```typescript
public readonly node: Node;
```

- *Type:* constructs.Node

The tree node.

---

##### `project`<sup>Required</sup> <a name="project" id="projen.python.Hatch.property.project"></a>

```typescript
public readonly project: PythonProject;
```

- *Type:* <a href="#projen.python.PythonProject">PythonProject</a>

---

##### `defaultBuildSystem`<sup>Required</sup> <a name="defaultBuildSystem" id="projen.python.Hatch.property.defaultBuildSystem"></a>

```typescript
public readonly defaultBuildSystem: BuildSystem;
```

- *Type:* <a href="#projen.python.BuildSystem">BuildSystem</a>

---

##### `installCiTask`<sup>Required</sup> <a name="installCiTask" id="projen.python.Hatch.property.installCiTask"></a>

```typescript
public readonly installCiTask: Task;
```

- *Type:* projen.Task

A task that installs and updates dependencies.

---

##### `installTask`<sup>Required</sup> <a name="installTask" id="projen.python.Hatch.property.installTask"></a>

```typescript
public readonly installTask: Task;
```

- *Type:* projen.Task

---

##### `publishTask`<sup>Required</sup> <a name="publishTask" id="projen.python.Hatch.property.publishTask"></a>

```typescript
public readonly publishTask: Task;
```

- *Type:* projen.Task

A task that uploads the package to a package repository.

---

##### `runCommand`<sup>Required</sup> <a name="runCommand" id="projen.python.Hatch.property.runCommand"></a>

```typescript
public readonly runCommand: string;
```

- *Type:* string

---

##### `formatTask`<sup>Optional</sup> <a name="formatTask" id="projen.python.Hatch.property.formatTask"></a>

```typescript
public readonly formatTask: Task;
```

- *Type:* projen.Task

A task that runs formatting.

---

##### `lintTask`<sup>Optional</sup> <a name="lintTask" id="projen.python.Hatch.property.lintTask"></a>

```typescript
public readonly lintTask: Task;
```

- *Type:* projen.Task

A task that runs linting.

---

##### `typeCheckTask`<sup>Optional</sup> <a name="typeCheckTask" id="projen.python.Hatch.property.typeCheckTask"></a>

```typescript
public readonly typeCheckTask: Task;
```

- *Type:* projen.Task

A task that runs static type checking.

---

##### `publishTestTask`<sup>Required</sup> <a name="publishTestTask" id="projen.python.Hatch.property.publishTestTask"></a>

```typescript
public readonly publishTestTask: Task;
```

- *Type:* projen.Task

---


### PackageManagerBase <a name="PackageManagerBase" id="projen.python.PackageManagerBase"></a>

- *Implements:* <a href="#projen.python.IPythonDeps">IPythonDeps</a>, <a href="#projen.python.IPythonEnv">IPythonEnv</a>, <a href="#projen.python.IPythonPackaging">IPythonPackaging</a>, <a href="#projen.python.IPythonLinting">IPythonLinting</a>

#### Initializers <a name="Initializers" id="projen.python.PackageManagerBase.Initializer"></a>

```typescript
import { python } from 'projen'

new python.PackageManagerBase(scope: IConstruct, options: PythonBaseOptions)
```

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PackageManagerBase.Initializer.parameter.scope">scope</a></code> | <code>constructs.IConstruct</code> | *No description.* |
| <code><a href="#projen.python.PackageManagerBase.Initializer.parameter.options">options</a></code> | <code><a href="#projen.python.PythonBaseOptions">PythonBaseOptions</a></code> | *No description.* |

---

##### `scope`<sup>Required</sup> <a name="scope" id="projen.python.PackageManagerBase.Initializer.parameter.scope"></a>

- *Type:* constructs.IConstruct

---

##### `options`<sup>Required</sup> <a name="options" id="projen.python.PackageManagerBase.Initializer.parameter.options"></a>

- *Type:* <a href="#projen.python.PythonBaseOptions">PythonBaseOptions</a>

---

#### Methods <a name="Methods" id="Methods"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PackageManagerBase.toString">toString</a></code> | Returns a string representation of this construct. |
| <code><a href="#projen.python.PackageManagerBase.postSynthesize">postSynthesize</a></code> | Called after synthesis. |
| <code><a href="#projen.python.PackageManagerBase.preSynthesize">preSynthesize</a></code> | Called before synthesis. |
| <code><a href="#projen.python.PackageManagerBase.synthesize">synthesize</a></code> | Synthesizes files to the project output directory. |
| <code><a href="#projen.python.PackageManagerBase.addDependency">addDependency</a></code> | Adds a runtime dependency. |
| <code><a href="#projen.python.PackageManagerBase.addDevDependency">addDevDependency</a></code> | Adds a dev dependency. |
| <code><a href="#projen.python.PackageManagerBase.getRunCommand">getRunCommand</a></code> | *No description.* |
| <code><a href="#projen.python.PackageManagerBase.installDependencies">installDependencies</a></code> | Installs dependencies (called during post-synthesis). |
| <code><a href="#projen.python.PackageManagerBase.setupEnvironment">setupEnvironment</a></code> | Initializes the virtual environment if it doesn't exist (called during post-synthesis). |
| <code><a href="#projen.python.PackageManagerBase.synthDependencies">synthDependencies</a></code> | *No description.* |
| <code><a href="#projen.python.PackageManagerBase.synthDependencyGroups">synthDependencyGroups</a></code> | *No description.* |
| <code><a href="#projen.python.PackageManagerBase.synthDevDependencies">synthDevDependencies</a></code> | *No description.* |

---

##### `toString` <a name="toString" id="projen.python.PackageManagerBase.toString"></a>

```typescript
public toString(): string
```

Returns a string representation of this construct.

##### `postSynthesize` <a name="postSynthesize" id="projen.python.PackageManagerBase.postSynthesize"></a>

```typescript
public postSynthesize(): void
```

Called after synthesis.

Order is *not* guaranteed.

##### `preSynthesize` <a name="preSynthesize" id="projen.python.PackageManagerBase.preSynthesize"></a>

```typescript
public preSynthesize(): void
```

Called before synthesis.

##### `synthesize` <a name="synthesize" id="projen.python.PackageManagerBase.synthesize"></a>

```typescript
public synthesize(): void
```

Synthesizes files to the project output directory.

##### `addDependency` <a name="addDependency" id="projen.python.PackageManagerBase.addDependency"></a>

```typescript
public addDependency(spec: string): void
```

Adds a runtime dependency.

###### `spec`<sup>Required</sup> <a name="spec" id="projen.python.PackageManagerBase.addDependency.parameter.spec"></a>

- *Type:* string

Format `<module>@<semver>`.

---

##### `addDevDependency` <a name="addDevDependency" id="projen.python.PackageManagerBase.addDevDependency"></a>

```typescript
public addDevDependency(spec: string): void
```

Adds a dev dependency.

###### `spec`<sup>Required</sup> <a name="spec" id="projen.python.PackageManagerBase.addDevDependency.parameter.spec"></a>

- *Type:* string

Format `<module>@<semver>`.

---

##### `getRunCommand` <a name="getRunCommand" id="projen.python.PackageManagerBase.getRunCommand"></a>

```typescript
public getRunCommand(_options: PythonBaseOptions): string
```

###### `_options`<sup>Required</sup> <a name="_options" id="projen.python.PackageManagerBase.getRunCommand.parameter._options"></a>

- *Type:* <a href="#projen.python.PythonBaseOptions">PythonBaseOptions</a>

---

##### `installDependencies` <a name="installDependencies" id="projen.python.PackageManagerBase.installDependencies"></a>

```typescript
public installDependencies(): void
```

Installs dependencies (called during post-synthesis).

##### `setupEnvironment` <a name="setupEnvironment" id="projen.python.PackageManagerBase.setupEnvironment"></a>

```typescript
public setupEnvironment(): void
```

Initializes the virtual environment if it doesn't exist (called during post-synthesis).

##### `synthDependencies` <a name="synthDependencies" id="projen.python.PackageManagerBase.synthDependencies"></a>

```typescript
public synthDependencies(): string[]
```

##### `synthDependencyGroups` <a name="synthDependencyGroups" id="projen.python.PackageManagerBase.synthDependencyGroups"></a>

```typescript
public synthDependencyGroups(): {[ key: string ]: any}
```

##### `synthDevDependencies` <a name="synthDevDependencies" id="projen.python.PackageManagerBase.synthDevDependencies"></a>

```typescript
public synthDevDependencies(): string[]
```

#### Static Functions <a name="Static Functions" id="Static Functions"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PackageManagerBase.isConstruct">isConstruct</a></code> | Checks if `x` is a construct. |
| <code><a href="#projen.python.PackageManagerBase.isComponent">isComponent</a></code> | Test whether the given construct is a component. |

---

##### `isConstruct` <a name="isConstruct" id="projen.python.PackageManagerBase.isConstruct"></a>

```typescript
import { python } from 'projen'

python.PackageManagerBase.isConstruct(x: any)
```

Checks if `x` is a construct.

Use this method instead of `instanceof` to properly detect `Construct`
instances, even when the construct library is symlinked.

Explanation: in JavaScript, multiple copies of the `constructs` library on
disk are seen as independent, completely different libraries. As a
consequence, the class `Construct` in each copy of the `constructs` library
is seen as a different class, and an instance of one class will not test as
`instanceof` the other class. `npm install` will not create installations
like this, but users may manually symlink construct libraries together or
use a monorepo tool: in those cases, multiple copies of the `constructs`
library can be accidentally installed, and `instanceof` will behave
unpredictably. It is safest to avoid using `instanceof`, and using
this type-testing method instead.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.PackageManagerBase.isConstruct.parameter.x"></a>

- *Type:* any

Any object.

---

##### `isComponent` <a name="isComponent" id="projen.python.PackageManagerBase.isComponent"></a>

```typescript
import { python } from 'projen'

python.PackageManagerBase.isComponent(x: any)
```

Test whether the given construct is a component.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.PackageManagerBase.isComponent.parameter.x"></a>

- *Type:* any

---

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PackageManagerBase.property.node">node</a></code> | <code>constructs.Node</code> | The tree node. |
| <code><a href="#projen.python.PackageManagerBase.property.project">project</a></code> | <code><a href="#projen.python.PythonProject">PythonProject</a></code> | *No description.* |
| <code><a href="#projen.python.PackageManagerBase.property.defaultBuildSystem">defaultBuildSystem</a></code> | <code><a href="#projen.python.BuildSystem">BuildSystem</a></code> | *No description.* |
| <code><a href="#projen.python.PackageManagerBase.property.installCiTask">installCiTask</a></code> | <code>projen.Task</code> | A task that installs and updates dependencies. |
| <code><a href="#projen.python.PackageManagerBase.property.installTask">installTask</a></code> | <code>projen.Task</code> | *No description.* |
| <code><a href="#projen.python.PackageManagerBase.property.publishTask">publishTask</a></code> | <code>projen.Task</code> | A task that uploads the package to a package repository. |
| <code><a href="#projen.python.PackageManagerBase.property.runCommand">runCommand</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.PackageManagerBase.property.formatTask">formatTask</a></code> | <code>projen.Task</code> | A task that runs formatting. |
| <code><a href="#projen.python.PackageManagerBase.property.lintTask">lintTask</a></code> | <code>projen.Task</code> | A task that runs linting. |
| <code><a href="#projen.python.PackageManagerBase.property.typeCheckTask">typeCheckTask</a></code> | <code>projen.Task</code> | A task that runs static type checking. |

---

##### `node`<sup>Required</sup> <a name="node" id="projen.python.PackageManagerBase.property.node"></a>

```typescript
public readonly node: Node;
```

- *Type:* constructs.Node

The tree node.

---

##### `project`<sup>Required</sup> <a name="project" id="projen.python.PackageManagerBase.property.project"></a>

```typescript
public readonly project: PythonProject;
```

- *Type:* <a href="#projen.python.PythonProject">PythonProject</a>

---

##### `defaultBuildSystem`<sup>Required</sup> <a name="defaultBuildSystem" id="projen.python.PackageManagerBase.property.defaultBuildSystem"></a>

```typescript
public readonly defaultBuildSystem: BuildSystem;
```

- *Type:* <a href="#projen.python.BuildSystem">BuildSystem</a>

---

##### `installCiTask`<sup>Required</sup> <a name="installCiTask" id="projen.python.PackageManagerBase.property.installCiTask"></a>

```typescript
public readonly installCiTask: Task;
```

- *Type:* projen.Task

A task that installs and updates dependencies.

---

##### `installTask`<sup>Required</sup> <a name="installTask" id="projen.python.PackageManagerBase.property.installTask"></a>

```typescript
public readonly installTask: Task;
```

- *Type:* projen.Task

---

##### `publishTask`<sup>Required</sup> <a name="publishTask" id="projen.python.PackageManagerBase.property.publishTask"></a>

```typescript
public readonly publishTask: Task;
```

- *Type:* projen.Task

A task that uploads the package to a package repository.

---

##### `runCommand`<sup>Required</sup> <a name="runCommand" id="projen.python.PackageManagerBase.property.runCommand"></a>

```typescript
public readonly runCommand: string;
```

- *Type:* string

---

##### `formatTask`<sup>Optional</sup> <a name="formatTask" id="projen.python.PackageManagerBase.property.formatTask"></a>

```typescript
public readonly formatTask: Task;
```

- *Type:* projen.Task

A task that runs formatting.

---

##### `lintTask`<sup>Optional</sup> <a name="lintTask" id="projen.python.PackageManagerBase.property.lintTask"></a>

```typescript
public readonly lintTask: Task;
```

- *Type:* projen.Task

A task that runs linting.

---

##### `typeCheckTask`<sup>Optional</sup> <a name="typeCheckTask" id="projen.python.PackageManagerBase.property.typeCheckTask"></a>

```typescript
public readonly typeCheckTask: Task;
```

- *Type:* projen.Task

A task that runs static type checking.

---


### Pdm <a name="Pdm" id="projen.python.Pdm"></a>

Manage project dependencies, virtual environments, and packaging through pdm.

#### Initializers <a name="Initializers" id="projen.python.Pdm.Initializer"></a>

```typescript
import { python } from 'projen'

new python.Pdm(scope: IConstruct, options: PythonBaseOptions)
```

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Pdm.Initializer.parameter.scope">scope</a></code> | <code>constructs.IConstruct</code> | *No description.* |
| <code><a href="#projen.python.Pdm.Initializer.parameter.options">options</a></code> | <code><a href="#projen.python.PythonBaseOptions">PythonBaseOptions</a></code> | *No description.* |

---

##### `scope`<sup>Required</sup> <a name="scope" id="projen.python.Pdm.Initializer.parameter.scope"></a>

- *Type:* constructs.IConstruct

---

##### `options`<sup>Required</sup> <a name="options" id="projen.python.Pdm.Initializer.parameter.options"></a>

- *Type:* <a href="#projen.python.PythonBaseOptions">PythonBaseOptions</a>

---

#### Methods <a name="Methods" id="Methods"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.Pdm.toString">toString</a></code> | Returns a string representation of this construct. |
| <code><a href="#projen.python.Pdm.postSynthesize">postSynthesize</a></code> | Called after synthesis. |
| <code><a href="#projen.python.Pdm.preSynthesize">preSynthesize</a></code> | Called before synthesis. |
| <code><a href="#projen.python.Pdm.synthesize">synthesize</a></code> | Synthesizes files to the project output directory. |
| <code><a href="#projen.python.Pdm.addDependency">addDependency</a></code> | Adds a runtime dependency. |
| <code><a href="#projen.python.Pdm.addDevDependency">addDevDependency</a></code> | Adds a dev dependency. |
| <code><a href="#projen.python.Pdm.getRunCommand">getRunCommand</a></code> | *No description.* |
| <code><a href="#projen.python.Pdm.installDependencies">installDependencies</a></code> | Installs dependencies (called during post-synthesis). |
| <code><a href="#projen.python.Pdm.setupEnvironment">setupEnvironment</a></code> | Initializes the virtual environment if it doesn't exist (called during post-synthesis). |
| <code><a href="#projen.python.Pdm.synthDependencies">synthDependencies</a></code> | *No description.* |
| <code><a href="#projen.python.Pdm.synthDependencyGroups">synthDependencyGroups</a></code> | *No description.* |
| <code><a href="#projen.python.Pdm.synthDevDependencies">synthDevDependencies</a></code> | *No description.* |

---

##### `toString` <a name="toString" id="projen.python.Pdm.toString"></a>

```typescript
public toString(): string
```

Returns a string representation of this construct.

##### `postSynthesize` <a name="postSynthesize" id="projen.python.Pdm.postSynthesize"></a>

```typescript
public postSynthesize(): void
```

Called after synthesis.

Order is *not* guaranteed.

##### `preSynthesize` <a name="preSynthesize" id="projen.python.Pdm.preSynthesize"></a>

```typescript
public preSynthesize(): void
```

Called before synthesis.

##### `synthesize` <a name="synthesize" id="projen.python.Pdm.synthesize"></a>

```typescript
public synthesize(): void
```

Synthesizes files to the project output directory.

##### `addDependency` <a name="addDependency" id="projen.python.Pdm.addDependency"></a>

```typescript
public addDependency(spec: string): void
```

Adds a runtime dependency.

###### `spec`<sup>Required</sup> <a name="spec" id="projen.python.Pdm.addDependency.parameter.spec"></a>

- *Type:* string

Format `<module>@<semver>`.

---

##### `addDevDependency` <a name="addDevDependency" id="projen.python.Pdm.addDevDependency"></a>

```typescript
public addDevDependency(spec: string): void
```

Adds a dev dependency.

###### `spec`<sup>Required</sup> <a name="spec" id="projen.python.Pdm.addDevDependency.parameter.spec"></a>

- *Type:* string

Format `<module>@<semver>`.

---

##### `getRunCommand` <a name="getRunCommand" id="projen.python.Pdm.getRunCommand"></a>

```typescript
public getRunCommand(_options: PythonBaseOptions): string
```

###### `_options`<sup>Required</sup> <a name="_options" id="projen.python.Pdm.getRunCommand.parameter._options"></a>

- *Type:* <a href="#projen.python.PythonBaseOptions">PythonBaseOptions</a>

---

##### `installDependencies` <a name="installDependencies" id="projen.python.Pdm.installDependencies"></a>

```typescript
public installDependencies(): void
```

Installs dependencies (called during post-synthesis).

##### `setupEnvironment` <a name="setupEnvironment" id="projen.python.Pdm.setupEnvironment"></a>

```typescript
public setupEnvironment(): void
```

Initializes the virtual environment if it doesn't exist (called during post-synthesis).

##### `synthDependencies` <a name="synthDependencies" id="projen.python.Pdm.synthDependencies"></a>

```typescript
public synthDependencies(): string[]
```

##### `synthDependencyGroups` <a name="synthDependencyGroups" id="projen.python.Pdm.synthDependencyGroups"></a>

```typescript
public synthDependencyGroups(): {[ key: string ]: any}
```

##### `synthDevDependencies` <a name="synthDevDependencies" id="projen.python.Pdm.synthDevDependencies"></a>

```typescript
public synthDevDependencies(): string[]
```

#### Static Functions <a name="Static Functions" id="Static Functions"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.Pdm.isConstruct">isConstruct</a></code> | Checks if `x` is a construct. |
| <code><a href="#projen.python.Pdm.isComponent">isComponent</a></code> | Test whether the given construct is a component. |

---

##### `isConstruct` <a name="isConstruct" id="projen.python.Pdm.isConstruct"></a>

```typescript
import { python } from 'projen'

python.Pdm.isConstruct(x: any)
```

Checks if `x` is a construct.

Use this method instead of `instanceof` to properly detect `Construct`
instances, even when the construct library is symlinked.

Explanation: in JavaScript, multiple copies of the `constructs` library on
disk are seen as independent, completely different libraries. As a
consequence, the class `Construct` in each copy of the `constructs` library
is seen as a different class, and an instance of one class will not test as
`instanceof` the other class. `npm install` will not create installations
like this, but users may manually symlink construct libraries together or
use a monorepo tool: in those cases, multiple copies of the `constructs`
library can be accidentally installed, and `instanceof` will behave
unpredictably. It is safest to avoid using `instanceof`, and using
this type-testing method instead.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.Pdm.isConstruct.parameter.x"></a>

- *Type:* any

Any object.

---

##### `isComponent` <a name="isComponent" id="projen.python.Pdm.isComponent"></a>

```typescript
import { python } from 'projen'

python.Pdm.isComponent(x: any)
```

Test whether the given construct is a component.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.Pdm.isComponent.parameter.x"></a>

- *Type:* any

---

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Pdm.property.node">node</a></code> | <code>constructs.Node</code> | The tree node. |
| <code><a href="#projen.python.Pdm.property.project">project</a></code> | <code><a href="#projen.python.PythonProject">PythonProject</a></code> | *No description.* |
| <code><a href="#projen.python.Pdm.property.defaultBuildSystem">defaultBuildSystem</a></code> | <code><a href="#projen.python.BuildSystem">BuildSystem</a></code> | *No description.* |
| <code><a href="#projen.python.Pdm.property.installCiTask">installCiTask</a></code> | <code>projen.Task</code> | A task that installs and updates dependencies. |
| <code><a href="#projen.python.Pdm.property.installTask">installTask</a></code> | <code>projen.Task</code> | *No description.* |
| <code><a href="#projen.python.Pdm.property.publishTask">publishTask</a></code> | <code>projen.Task</code> | A task that uploads the package to a package repository. |
| <code><a href="#projen.python.Pdm.property.runCommand">runCommand</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.Pdm.property.formatTask">formatTask</a></code> | <code>projen.Task</code> | A task that runs formatting. |
| <code><a href="#projen.python.Pdm.property.lintTask">lintTask</a></code> | <code>projen.Task</code> | A task that runs linting. |
| <code><a href="#projen.python.Pdm.property.typeCheckTask">typeCheckTask</a></code> | <code>projen.Task</code> | A task that runs static type checking. |

---

##### `node`<sup>Required</sup> <a name="node" id="projen.python.Pdm.property.node"></a>

```typescript
public readonly node: Node;
```

- *Type:* constructs.Node

The tree node.

---

##### `project`<sup>Required</sup> <a name="project" id="projen.python.Pdm.property.project"></a>

```typescript
public readonly project: PythonProject;
```

- *Type:* <a href="#projen.python.PythonProject">PythonProject</a>

---

##### `defaultBuildSystem`<sup>Required</sup> <a name="defaultBuildSystem" id="projen.python.Pdm.property.defaultBuildSystem"></a>

```typescript
public readonly defaultBuildSystem: BuildSystem;
```

- *Type:* <a href="#projen.python.BuildSystem">BuildSystem</a>

---

##### `installCiTask`<sup>Required</sup> <a name="installCiTask" id="projen.python.Pdm.property.installCiTask"></a>

```typescript
public readonly installCiTask: Task;
```

- *Type:* projen.Task

A task that installs and updates dependencies.

---

##### `installTask`<sup>Required</sup> <a name="installTask" id="projen.python.Pdm.property.installTask"></a>

```typescript
public readonly installTask: Task;
```

- *Type:* projen.Task

---

##### `publishTask`<sup>Required</sup> <a name="publishTask" id="projen.python.Pdm.property.publishTask"></a>

```typescript
public readonly publishTask: Task;
```

- *Type:* projen.Task

A task that uploads the package to a package repository.

---

##### `runCommand`<sup>Required</sup> <a name="runCommand" id="projen.python.Pdm.property.runCommand"></a>

```typescript
public readonly runCommand: string;
```

- *Type:* string

---

##### `formatTask`<sup>Optional</sup> <a name="formatTask" id="projen.python.Pdm.property.formatTask"></a>

```typescript
public readonly formatTask: Task;
```

- *Type:* projen.Task

A task that runs formatting.

---

##### `lintTask`<sup>Optional</sup> <a name="lintTask" id="projen.python.Pdm.property.lintTask"></a>

```typescript
public readonly lintTask: Task;
```

- *Type:* projen.Task

A task that runs linting.

---

##### `typeCheckTask`<sup>Optional</sup> <a name="typeCheckTask" id="projen.python.Pdm.property.typeCheckTask"></a>

```typescript
public readonly typeCheckTask: Task;
```

- *Type:* projen.Task

A task that runs static type checking.

---


### Poetry <a name="Poetry" id="projen.python.Poetry"></a>

Manage project dependencies, virtual environments, and packaging through the poetry CLI tool.

#### Initializers <a name="Initializers" id="projen.python.Poetry.Initializer"></a>

```typescript
import { python } from 'projen'

new python.Poetry(scope: IConstruct, options: PythonBaseOptions)
```

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Poetry.Initializer.parameter.scope">scope</a></code> | <code>constructs.IConstruct</code> | *No description.* |
| <code><a href="#projen.python.Poetry.Initializer.parameter.options">options</a></code> | <code><a href="#projen.python.PythonBaseOptions">PythonBaseOptions</a></code> | *No description.* |

---

##### `scope`<sup>Required</sup> <a name="scope" id="projen.python.Poetry.Initializer.parameter.scope"></a>

- *Type:* constructs.IConstruct

---

##### `options`<sup>Required</sup> <a name="options" id="projen.python.Poetry.Initializer.parameter.options"></a>

- *Type:* <a href="#projen.python.PythonBaseOptions">PythonBaseOptions</a>

---

#### Methods <a name="Methods" id="Methods"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.Poetry.toString">toString</a></code> | Returns a string representation of this construct. |
| <code><a href="#projen.python.Poetry.postSynthesize">postSynthesize</a></code> | Called after synthesis. |
| <code><a href="#projen.python.Poetry.preSynthesize">preSynthesize</a></code> | Called before synthesis. |
| <code><a href="#projen.python.Poetry.synthesize">synthesize</a></code> | Synthesizes files to the project output directory. |
| <code><a href="#projen.python.Poetry.addDependency">addDependency</a></code> | Adds a runtime dependency. |
| <code><a href="#projen.python.Poetry.addDevDependency">addDevDependency</a></code> | Adds a dev dependency. |
| <code><a href="#projen.python.Poetry.getRunCommand">getRunCommand</a></code> | *No description.* |
| <code><a href="#projen.python.Poetry.installDependencies">installDependencies</a></code> | Installs dependencies (called during post-synthesis). |
| <code><a href="#projen.python.Poetry.setupEnvironment">setupEnvironment</a></code> | Initializes the virtual environment if it doesn't exist (called during post-synthesis). |
| <code><a href="#projen.python.Poetry.synthDependencies">synthDependencies</a></code> | *No description.* |
| <code><a href="#projen.python.Poetry.synthDependencyGroups">synthDependencyGroups</a></code> | *No description.* |
| <code><a href="#projen.python.Poetry.synthDevDependencies">synthDevDependencies</a></code> | *No description.* |

---

##### `toString` <a name="toString" id="projen.python.Poetry.toString"></a>

```typescript
public toString(): string
```

Returns a string representation of this construct.

##### `postSynthesize` <a name="postSynthesize" id="projen.python.Poetry.postSynthesize"></a>

```typescript
public postSynthesize(): void
```

Called after synthesis.

Order is *not* guaranteed.

##### `preSynthesize` <a name="preSynthesize" id="projen.python.Poetry.preSynthesize"></a>

```typescript
public preSynthesize(): void
```

Called before synthesis.

##### `synthesize` <a name="synthesize" id="projen.python.Poetry.synthesize"></a>

```typescript
public synthesize(): void
```

Synthesizes files to the project output directory.

##### `addDependency` <a name="addDependency" id="projen.python.Poetry.addDependency"></a>

```typescript
public addDependency(spec: string): void
```

Adds a runtime dependency.

###### `spec`<sup>Required</sup> <a name="spec" id="projen.python.Poetry.addDependency.parameter.spec"></a>

- *Type:* string

Format `<module>@<semver>`.

---

##### `addDevDependency` <a name="addDevDependency" id="projen.python.Poetry.addDevDependency"></a>

```typescript
public addDevDependency(spec: string): void
```

Adds a dev dependency.

###### `spec`<sup>Required</sup> <a name="spec" id="projen.python.Poetry.addDevDependency.parameter.spec"></a>

- *Type:* string

Format `<module>@<semver>`.

---

##### `getRunCommand` <a name="getRunCommand" id="projen.python.Poetry.getRunCommand"></a>

```typescript
public getRunCommand(_options: PythonBaseOptions): string
```

###### `_options`<sup>Required</sup> <a name="_options" id="projen.python.Poetry.getRunCommand.parameter._options"></a>

- *Type:* <a href="#projen.python.PythonBaseOptions">PythonBaseOptions</a>

---

##### `installDependencies` <a name="installDependencies" id="projen.python.Poetry.installDependencies"></a>

```typescript
public installDependencies(): void
```

Installs dependencies (called during post-synthesis).

##### `setupEnvironment` <a name="setupEnvironment" id="projen.python.Poetry.setupEnvironment"></a>

```typescript
public setupEnvironment(): void
```

Initializes the virtual environment if it doesn't exist (called during post-synthesis).

##### `synthDependencies` <a name="synthDependencies" id="projen.python.Poetry.synthDependencies"></a>

```typescript
public synthDependencies(): string[]
```

##### `synthDependencyGroups` <a name="synthDependencyGroups" id="projen.python.Poetry.synthDependencyGroups"></a>

```typescript
public synthDependencyGroups(): {[ key: string ]: any}
```

##### `synthDevDependencies` <a name="synthDevDependencies" id="projen.python.Poetry.synthDevDependencies"></a>

```typescript
public synthDevDependencies(): string[]
```

#### Static Functions <a name="Static Functions" id="Static Functions"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.Poetry.isConstruct">isConstruct</a></code> | Checks if `x` is a construct. |
| <code><a href="#projen.python.Poetry.isComponent">isComponent</a></code> | Test whether the given construct is a component. |

---

##### `isConstruct` <a name="isConstruct" id="projen.python.Poetry.isConstruct"></a>

```typescript
import { python } from 'projen'

python.Poetry.isConstruct(x: any)
```

Checks if `x` is a construct.

Use this method instead of `instanceof` to properly detect `Construct`
instances, even when the construct library is symlinked.

Explanation: in JavaScript, multiple copies of the `constructs` library on
disk are seen as independent, completely different libraries. As a
consequence, the class `Construct` in each copy of the `constructs` library
is seen as a different class, and an instance of one class will not test as
`instanceof` the other class. `npm install` will not create installations
like this, but users may manually symlink construct libraries together or
use a monorepo tool: in those cases, multiple copies of the `constructs`
library can be accidentally installed, and `instanceof` will behave
unpredictably. It is safest to avoid using `instanceof`, and using
this type-testing method instead.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.Poetry.isConstruct.parameter.x"></a>

- *Type:* any

Any object.

---

##### `isComponent` <a name="isComponent" id="projen.python.Poetry.isComponent"></a>

```typescript
import { python } from 'projen'

python.Poetry.isComponent(x: any)
```

Test whether the given construct is a component.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.Poetry.isComponent.parameter.x"></a>

- *Type:* any

---

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Poetry.property.node">node</a></code> | <code>constructs.Node</code> | The tree node. |
| <code><a href="#projen.python.Poetry.property.project">project</a></code> | <code><a href="#projen.python.PythonProject">PythonProject</a></code> | *No description.* |
| <code><a href="#projen.python.Poetry.property.defaultBuildSystem">defaultBuildSystem</a></code> | <code><a href="#projen.python.BuildSystem">BuildSystem</a></code> | *No description.* |
| <code><a href="#projen.python.Poetry.property.installCiTask">installCiTask</a></code> | <code>projen.Task</code> | Task for installing dependencies according to the existing lockfile. |
| <code><a href="#projen.python.Poetry.property.installTask">installTask</a></code> | <code>projen.Task</code> | Task for updating the lockfile and installing project dependencies. |
| <code><a href="#projen.python.Poetry.property.publishTask">publishTask</a></code> | <code>projen.Task</code> | Task for publishing the package to a package repository. |
| <code><a href="#projen.python.Poetry.property.runCommand">runCommand</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.Poetry.property.formatTask">formatTask</a></code> | <code>projen.Task</code> | A task that runs formatting. |
| <code><a href="#projen.python.Poetry.property.lintTask">lintTask</a></code> | <code>projen.Task</code> | A task that runs linting. |
| <code><a href="#projen.python.Poetry.property.typeCheckTask">typeCheckTask</a></code> | <code>projen.Task</code> | A task that runs static type checking. |
| <code><a href="#projen.python.Poetry.property.publishTestTask">publishTestTask</a></code> | <code>projen.Task</code> | Task for publishing the package to the Test PyPI repository for testing purposes. |

---

##### `node`<sup>Required</sup> <a name="node" id="projen.python.Poetry.property.node"></a>

```typescript
public readonly node: Node;
```

- *Type:* constructs.Node

The tree node.

---

##### `project`<sup>Required</sup> <a name="project" id="projen.python.Poetry.property.project"></a>

```typescript
public readonly project: PythonProject;
```

- *Type:* <a href="#projen.python.PythonProject">PythonProject</a>

---

##### `defaultBuildSystem`<sup>Required</sup> <a name="defaultBuildSystem" id="projen.python.Poetry.property.defaultBuildSystem"></a>

```typescript
public readonly defaultBuildSystem: BuildSystem;
```

- *Type:* <a href="#projen.python.BuildSystem">BuildSystem</a>

---

##### `installCiTask`<sup>Required</sup> <a name="installCiTask" id="projen.python.Poetry.property.installCiTask"></a>

```typescript
public readonly installCiTask: Task;
```

- *Type:* projen.Task

Task for installing dependencies according to the existing lockfile.

---

##### `installTask`<sup>Required</sup> <a name="installTask" id="projen.python.Poetry.property.installTask"></a>

```typescript
public readonly installTask: Task;
```

- *Type:* projen.Task

Task for updating the lockfile and installing project dependencies.

---

##### `publishTask`<sup>Required</sup> <a name="publishTask" id="projen.python.Poetry.property.publishTask"></a>

```typescript
public readonly publishTask: Task;
```

- *Type:* projen.Task

Task for publishing the package to a package repository.

---

##### `runCommand`<sup>Required</sup> <a name="runCommand" id="projen.python.Poetry.property.runCommand"></a>

```typescript
public readonly runCommand: string;
```

- *Type:* string

---

##### `formatTask`<sup>Optional</sup> <a name="formatTask" id="projen.python.Poetry.property.formatTask"></a>

```typescript
public readonly formatTask: Task;
```

- *Type:* projen.Task

A task that runs formatting.

---

##### `lintTask`<sup>Optional</sup> <a name="lintTask" id="projen.python.Poetry.property.lintTask"></a>

```typescript
public readonly lintTask: Task;
```

- *Type:* projen.Task

A task that runs linting.

---

##### `typeCheckTask`<sup>Optional</sup> <a name="typeCheckTask" id="projen.python.Poetry.property.typeCheckTask"></a>

```typescript
public readonly typeCheckTask: Task;
```

- *Type:* projen.Task

A task that runs static type checking.

---

##### `publishTestTask`<sup>Required</sup> <a name="publishTestTask" id="projen.python.Poetry.property.publishTestTask"></a>

```typescript
public readonly publishTestTask: Task;
```

- *Type:* projen.Task

Task for publishing the package to the Test PyPI repository for testing purposes.

---


### Projenrc <a name="Projenrc" id="projen.python.Projenrc"></a>

Allows writing projenrc files in python.

This will install `projen` as a Python dependency and will add a
`synth` task which will run `.projenrc.py`.

#### Initializers <a name="Initializers" id="projen.python.Projenrc.Initializer"></a>

```typescript
import { python } from 'projen'

new python.Projenrc(project: Project, options?: ProjenrcOptions)
```

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Projenrc.Initializer.parameter.project">project</a></code> | <code>projen.Project</code> | *No description.* |
| <code><a href="#projen.python.Projenrc.Initializer.parameter.options">options</a></code> | <code><a href="#projen.python.ProjenrcOptions">ProjenrcOptions</a></code> | *No description.* |

---

##### `project`<sup>Required</sup> <a name="project" id="projen.python.Projenrc.Initializer.parameter.project"></a>

- *Type:* projen.Project

---

##### `options`<sup>Optional</sup> <a name="options" id="projen.python.Projenrc.Initializer.parameter.options"></a>

- *Type:* <a href="#projen.python.ProjenrcOptions">ProjenrcOptions</a>

---

#### Methods <a name="Methods" id="Methods"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.Projenrc.toString">toString</a></code> | Returns a string representation of this construct. |
| <code><a href="#projen.python.Projenrc.postSynthesize">postSynthesize</a></code> | Called after synthesis. |
| <code><a href="#projen.python.Projenrc.preSynthesize">preSynthesize</a></code> | Called before synthesis. |
| <code><a href="#projen.python.Projenrc.synthesize">synthesize</a></code> | Synthesizes files to the project output directory. |

---

##### `toString` <a name="toString" id="projen.python.Projenrc.toString"></a>

```typescript
public toString(): string
```

Returns a string representation of this construct.

##### `postSynthesize` <a name="postSynthesize" id="projen.python.Projenrc.postSynthesize"></a>

```typescript
public postSynthesize(): void
```

Called after synthesis.

Order is *not* guaranteed.

##### `preSynthesize` <a name="preSynthesize" id="projen.python.Projenrc.preSynthesize"></a>

```typescript
public preSynthesize(): void
```

Called before synthesis.

##### `synthesize` <a name="synthesize" id="projen.python.Projenrc.synthesize"></a>

```typescript
public synthesize(): void
```

Synthesizes files to the project output directory.

#### Static Functions <a name="Static Functions" id="Static Functions"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.Projenrc.isConstruct">isConstruct</a></code> | Checks if `x` is a construct. |
| <code><a href="#projen.python.Projenrc.isComponent">isComponent</a></code> | Test whether the given construct is a component. |
| <code><a href="#projen.python.Projenrc.of">of</a></code> | Returns the `Projenrc` instance associated with a project or `undefined` if there is no Projenrc. |

---

##### `isConstruct` <a name="isConstruct" id="projen.python.Projenrc.isConstruct"></a>

```typescript
import { python } from 'projen'

python.Projenrc.isConstruct(x: any)
```

Checks if `x` is a construct.

Use this method instead of `instanceof` to properly detect `Construct`
instances, even when the construct library is symlinked.

Explanation: in JavaScript, multiple copies of the `constructs` library on
disk are seen as independent, completely different libraries. As a
consequence, the class `Construct` in each copy of the `constructs` library
is seen as a different class, and an instance of one class will not test as
`instanceof` the other class. `npm install` will not create installations
like this, but users may manually symlink construct libraries together or
use a monorepo tool: in those cases, multiple copies of the `constructs`
library can be accidentally installed, and `instanceof` will behave
unpredictably. It is safest to avoid using `instanceof`, and using
this type-testing method instead.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.Projenrc.isConstruct.parameter.x"></a>

- *Type:* any

Any object.

---

##### `isComponent` <a name="isComponent" id="projen.python.Projenrc.isComponent"></a>

```typescript
import { python } from 'projen'

python.Projenrc.isComponent(x: any)
```

Test whether the given construct is a component.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.Projenrc.isComponent.parameter.x"></a>

- *Type:* any

---

##### `of` <a name="of" id="projen.python.Projenrc.of"></a>

```typescript
import { python } from 'projen'

python.Projenrc.of(project: Project)
```

Returns the `Projenrc` instance associated with a project or `undefined` if there is no Projenrc.

###### `project`<sup>Required</sup> <a name="project" id="projen.python.Projenrc.of.parameter.project"></a>

- *Type:* projen.Project

The project.

---

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Projenrc.property.node">node</a></code> | <code>constructs.Node</code> | The tree node. |
| <code><a href="#projen.python.Projenrc.property.project">project</a></code> | <code>projen.Project</code> | *No description.* |
| <code><a href="#projen.python.Projenrc.property.filePath">filePath</a></code> | <code>string</code> | The name of the projenrc file. |
| <code><a href="#projen.python.Projenrc.property.pythonExec">pythonExec</a></code> | <code>string</code> | Path to the python executable to use. |

---

##### `node`<sup>Required</sup> <a name="node" id="projen.python.Projenrc.property.node"></a>

```typescript
public readonly node: Node;
```

- *Type:* constructs.Node

The tree node.

---

##### `project`<sup>Required</sup> <a name="project" id="projen.python.Projenrc.property.project"></a>

```typescript
public readonly project: Project;
```

- *Type:* projen.Project

---

##### `filePath`<sup>Required</sup> <a name="filePath" id="projen.python.Projenrc.property.filePath"></a>

```typescript
public readonly filePath: string;
```

- *Type:* string

The name of the projenrc file.

---

##### `pythonExec`<sup>Required</sup> <a name="pythonExec" id="projen.python.Projenrc.property.pythonExec"></a>

```typescript
public readonly pythonExec: string;
```

- *Type:* string

Path to the python executable to use.

---


### PyprojectTomlFile <a name="PyprojectTomlFile" id="projen.python.PyprojectTomlFile"></a>

Represents configuration of a pyproject.toml file.

> [https://packaging.python.org/en/latest/guides/writing-pyproject-toml/](https://packaging.python.org/en/latest/guides/writing-pyproject-toml/)

#### Initializers <a name="Initializers" id="projen.python.PyprojectTomlFile.Initializer"></a>

```typescript
import { python } from 'projen'

new python.PyprojectTomlFile(scope: IConstruct, config: PyprojectToml)
```

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PyprojectTomlFile.Initializer.parameter.scope">scope</a></code> | <code>constructs.IConstruct</code> | *No description.* |
| <code><a href="#projen.python.PyprojectTomlFile.Initializer.parameter.config">config</a></code> | <code><a href="#projen.python.PyprojectToml">PyprojectToml</a></code> | *No description.* |

---

##### `scope`<sup>Required</sup> <a name="scope" id="projen.python.PyprojectTomlFile.Initializer.parameter.scope"></a>

- *Type:* constructs.IConstruct

---

##### `config`<sup>Required</sup> <a name="config" id="projen.python.PyprojectTomlFile.Initializer.parameter.config"></a>

- *Type:* <a href="#projen.python.PyprojectToml">PyprojectToml</a>

---

#### Methods <a name="Methods" id="Methods"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PyprojectTomlFile.toString">toString</a></code> | Returns a string representation of this construct. |
| <code><a href="#projen.python.PyprojectTomlFile.postSynthesize">postSynthesize</a></code> | Called after synthesis. |
| <code><a href="#projen.python.PyprojectTomlFile.preSynthesize">preSynthesize</a></code> | Called before synthesis. |
| <code><a href="#projen.python.PyprojectTomlFile.synthesize">synthesize</a></code> | Writes the file to the project's output directory. |
| <code><a href="#projen.python.PyprojectTomlFile.addDeletionOverride">addDeletionOverride</a></code> | Syntactic sugar for `addOverride(path, undefined)`. |
| <code><a href="#projen.python.PyprojectTomlFile.addOverride">addOverride</a></code> | Adds an override to the synthesized object file. |
| <code><a href="#projen.python.PyprojectTomlFile.addToArray">addToArray</a></code> | Adds to an array in the synthesized object file. |
| <code><a href="#projen.python.PyprojectTomlFile.patch">patch</a></code> | Applies an RFC 6902 JSON-patch to the synthesized object file. See https://datatracker.ietf.org/doc/html/rfc6902 for more information. |

---

##### `toString` <a name="toString" id="projen.python.PyprojectTomlFile.toString"></a>

```typescript
public toString(): string
```

Returns a string representation of this construct.

##### `postSynthesize` <a name="postSynthesize" id="projen.python.PyprojectTomlFile.postSynthesize"></a>

```typescript
public postSynthesize(): void
```

Called after synthesis.

Order is *not* guaranteed.

##### `preSynthesize` <a name="preSynthesize" id="projen.python.PyprojectTomlFile.preSynthesize"></a>

```typescript
public preSynthesize(): void
```

Called before synthesis.

##### `synthesize` <a name="synthesize" id="projen.python.PyprojectTomlFile.synthesize"></a>

```typescript
public synthesize(): void
```

Writes the file to the project's output directory.

##### `addDeletionOverride` <a name="addDeletionOverride" id="projen.python.PyprojectTomlFile.addDeletionOverride"></a>

```typescript
public addDeletionOverride(path: string): void
```

Syntactic sugar for `addOverride(path, undefined)`.

###### `path`<sup>Required</sup> <a name="path" id="projen.python.PyprojectTomlFile.addDeletionOverride.parameter.path"></a>

- *Type:* string

The path of the value to delete.

---

##### `addOverride` <a name="addOverride" id="projen.python.PyprojectTomlFile.addOverride"></a>

```typescript
public addOverride(path: string, value: any): void
```

Adds an override to the synthesized object file.

If the override is nested, separate each nested level using a dot (.) in the path parameter.
If there is an array as part of the nesting, specify the index in the path.

To include a literal `.` in the property name, prefix with a `\`. In most
programming languages you will need to write this as `"\\."` because the
`\` itself will need to be escaped.

For example,
```typescript
project.tsconfig.file.addOverride('compilerOptions.alwaysStrict', true);
project.tsconfig.file.addOverride('compilerOptions.lib', ['dom', 'dom.iterable', 'esnext']);
```
would add the overrides
```json
"compilerOptions": {
  "alwaysStrict": true,
  "lib": [
    "dom",
    "dom.iterable",
    "esnext"
  ]
  ...
}
...
```

###### `path`<sup>Required</sup> <a name="path" id="projen.python.PyprojectTomlFile.addOverride.parameter.path"></a>

- *Type:* string

The path of the property, you can use dot notation to override values in complex types.

Any intermediate keys
will be created as needed.

---

###### `value`<sup>Required</sup> <a name="value" id="projen.python.PyprojectTomlFile.addOverride.parameter.value"></a>

- *Type:* any

The value.

Could be primitive or complex.

---

##### `addToArray` <a name="addToArray" id="projen.python.PyprojectTomlFile.addToArray"></a>

```typescript
public addToArray(path: string, values: ...any[]): void
```

Adds to an array in the synthesized object file.

If the array is nested, separate each nested level using a dot (.) in the path parameter.
If there is an array as part of the nesting, specify the index in the path.

To include a literal `.` in the property name, prefix with a `\`. In most
programming languages you will need to write this as `"\\."` because the
`\` itself will need to be escaped.

For example, with the following object file
```json
"compilerOptions": {
  "exclude": ["node_modules"],
  "lib": ["es2020"]
  ...
}
...
```

```typescript
project.tsconfig.file.addToArray('compilerOptions.exclude', 'coverage');
project.tsconfig.file.addToArray('compilerOptions.lib', 'dom', 'dom.iterable', 'esnext');
```
would result in the following object file
```json
"compilerOptions": {
  "exclude": ["node_modules", "coverage"],
  "lib": ["es2020", "dom", "dom.iterable", "esnext"]
  ...
}
...
```

###### `path`<sup>Required</sup> <a name="path" id="projen.python.PyprojectTomlFile.addToArray.parameter.path"></a>

- *Type:* string

The path of the property, you can use dot notation to att to arrays in complex types.

Any intermediate keys
will be created as needed.

---

###### `values`<sup>Required</sup> <a name="values" id="projen.python.PyprojectTomlFile.addToArray.parameter.values"></a>

- *Type:* ...any[]

The values to add.

Could be primitive or complex.

---

##### `patch` <a name="patch" id="projen.python.PyprojectTomlFile.patch"></a>

```typescript
public patch(patches: ...JsonPatch[]): void
```

Applies an RFC 6902 JSON-patch to the synthesized object file. See https://datatracker.ietf.org/doc/html/rfc6902 for more information.

For example, with the following object file
```json
"compilerOptions": {
  "exclude": ["node_modules"],
  "lib": ["es2020"]
  ...
}
...
```

```typescript
project.tsconfig.file.patch(JsonPatch.add("/compilerOptions/exclude/-", "coverage"));
project.tsconfig.file.patch(JsonPatch.replace("/compilerOptions/lib", ["dom", "dom.iterable", "esnext"]));
```
would result in the following object file
```json
"compilerOptions": {
  "exclude": ["node_modules", "coverage"],
  "lib": ["dom", "dom.iterable", "esnext"]
  ...
}
...
```

###### `patches`<sup>Required</sup> <a name="patches" id="projen.python.PyprojectTomlFile.patch.parameter.patches"></a>

- *Type:* ...projen.JsonPatch[]

The patch operations to apply.

---

#### Static Functions <a name="Static Functions" id="Static Functions"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PyprojectTomlFile.isConstruct">isConstruct</a></code> | Checks if `x` is a construct. |
| <code><a href="#projen.python.PyprojectTomlFile.isComponent">isComponent</a></code> | Test whether the given construct is a component. |

---

##### `isConstruct` <a name="isConstruct" id="projen.python.PyprojectTomlFile.isConstruct"></a>

```typescript
import { python } from 'projen'

python.PyprojectTomlFile.isConstruct(x: any)
```

Checks if `x` is a construct.

Use this method instead of `instanceof` to properly detect `Construct`
instances, even when the construct library is symlinked.

Explanation: in JavaScript, multiple copies of the `constructs` library on
disk are seen as independent, completely different libraries. As a
consequence, the class `Construct` in each copy of the `constructs` library
is seen as a different class, and an instance of one class will not test as
`instanceof` the other class. `npm install` will not create installations
like this, but users may manually symlink construct libraries together or
use a monorepo tool: in those cases, multiple copies of the `constructs`
library can be accidentally installed, and `instanceof` will behave
unpredictably. It is safest to avoid using `instanceof`, and using
this type-testing method instead.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.PyprojectTomlFile.isConstruct.parameter.x"></a>

- *Type:* any

Any object.

---

##### `isComponent` <a name="isComponent" id="projen.python.PyprojectTomlFile.isComponent"></a>

```typescript
import { python } from 'projen'

python.PyprojectTomlFile.isComponent(x: any)
```

Test whether the given construct is a component.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.PyprojectTomlFile.isComponent.parameter.x"></a>

- *Type:* any

---

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PyprojectTomlFile.property.node">node</a></code> | <code>constructs.Node</code> | The tree node. |
| <code><a href="#projen.python.PyprojectTomlFile.property.project">project</a></code> | <code>projen.Project</code> | *No description.* |
| <code><a href="#projen.python.PyprojectTomlFile.property.absolutePath">absolutePath</a></code> | <code>string</code> | The absolute path of this file. |
| <code><a href="#projen.python.PyprojectTomlFile.property.path">path</a></code> | <code>string</code> | The file path, relative to the project's outdir. |
| <code><a href="#projen.python.PyprojectTomlFile.property.changed">changed</a></code> | <code>boolean</code> | Indicates if the file has been changed during synthesis. |
| <code><a href="#projen.python.PyprojectTomlFile.property.marker">marker</a></code> | <code>string</code> | The projen marker, used to identify files as projen-generated. |
| <code><a href="#projen.python.PyprojectTomlFile.property.executable">executable</a></code> | <code>boolean</code> | Indicates if the file should be marked as executable. |
| <code><a href="#projen.python.PyprojectTomlFile.property.readonly">readonly</a></code> | <code>boolean</code> | Indicates if the file should be read-only or read-write. |
| <code><a href="#projen.python.PyprojectTomlFile.property.omitEmpty">omitEmpty</a></code> | <code>boolean</code> | Indicates if empty objects and arrays are omitted from the output object. |

---

##### `node`<sup>Required</sup> <a name="node" id="projen.python.PyprojectTomlFile.property.node"></a>

```typescript
public readonly node: Node;
```

- *Type:* constructs.Node

The tree node.

---

##### `project`<sup>Required</sup> <a name="project" id="projen.python.PyprojectTomlFile.property.project"></a>

```typescript
public readonly project: Project;
```

- *Type:* projen.Project

---

##### `absolutePath`<sup>Required</sup> <a name="absolutePath" id="projen.python.PyprojectTomlFile.property.absolutePath"></a>

```typescript
public readonly absolutePath: string;
```

- *Type:* string

The absolute path of this file.

---

##### `path`<sup>Required</sup> <a name="path" id="projen.python.PyprojectTomlFile.property.path"></a>

```typescript
public readonly path: string;
```

- *Type:* string

The file path, relative to the project's outdir.

---

##### `changed`<sup>Optional</sup> <a name="changed" id="projen.python.PyprojectTomlFile.property.changed"></a>

```typescript
public readonly changed: boolean;
```

- *Type:* boolean

Indicates if the file has been changed during synthesis.

This property is
only available in `postSynthesize()` hooks. If this is `undefined`, the
file has not been synthesized yet.

---

##### `marker`<sup>Optional</sup> <a name="marker" id="projen.python.PyprojectTomlFile.property.marker"></a>

```typescript
public readonly marker: string;
```

- *Type:* string

The projen marker, used to identify files as projen-generated.

Value is undefined if the project is being ejected.

---

##### `executable`<sup>Required</sup> <a name="executable" id="projen.python.PyprojectTomlFile.property.executable"></a>

```typescript
public readonly executable: boolean;
```

- *Type:* boolean

Indicates if the file should be marked as executable.

---

##### `readonly`<sup>Required</sup> <a name="readonly" id="projen.python.PyprojectTomlFile.property.readonly"></a>

```typescript
public readonly readonly: boolean;
```

- *Type:* boolean

Indicates if the file should be read-only or read-write.

---

##### `omitEmpty`<sup>Required</sup> <a name="omitEmpty" id="projen.python.PyprojectTomlFile.property.omitEmpty"></a>

```typescript
public readonly omitEmpty: boolean;
```

- *Type:* boolean

Indicates if empty objects and arrays are omitted from the output object.

---


### Pytest <a name="Pytest" id="projen.python.Pytest"></a>

#### Initializers <a name="Initializers" id="projen.python.Pytest.Initializer"></a>

```typescript
import { python } from 'projen'

new python.Pytest(project: Project, options?: PytestOptions)
```

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Pytest.Initializer.parameter.project">project</a></code> | <code>projen.Project</code> | *No description.* |
| <code><a href="#projen.python.Pytest.Initializer.parameter.options">options</a></code> | <code><a href="#projen.python.PytestOptions">PytestOptions</a></code> | *No description.* |

---

##### `project`<sup>Required</sup> <a name="project" id="projen.python.Pytest.Initializer.parameter.project"></a>

- *Type:* projen.Project

---

##### `options`<sup>Optional</sup> <a name="options" id="projen.python.Pytest.Initializer.parameter.options"></a>

- *Type:* <a href="#projen.python.PytestOptions">PytestOptions</a>

---

#### Methods <a name="Methods" id="Methods"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.Pytest.toString">toString</a></code> | Returns a string representation of this construct. |
| <code><a href="#projen.python.Pytest.postSynthesize">postSynthesize</a></code> | Called after synthesis. |
| <code><a href="#projen.python.Pytest.preSynthesize">preSynthesize</a></code> | Called before synthesis. |
| <code><a href="#projen.python.Pytest.synthesize">synthesize</a></code> | Synthesizes files to the project output directory. |

---

##### `toString` <a name="toString" id="projen.python.Pytest.toString"></a>

```typescript
public toString(): string
```

Returns a string representation of this construct.

##### `postSynthesize` <a name="postSynthesize" id="projen.python.Pytest.postSynthesize"></a>

```typescript
public postSynthesize(): void
```

Called after synthesis.

Order is *not* guaranteed.

##### `preSynthesize` <a name="preSynthesize" id="projen.python.Pytest.preSynthesize"></a>

```typescript
public preSynthesize(): void
```

Called before synthesis.

##### `synthesize` <a name="synthesize" id="projen.python.Pytest.synthesize"></a>

```typescript
public synthesize(): void
```

Synthesizes files to the project output directory.

#### Static Functions <a name="Static Functions" id="Static Functions"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.Pytest.isConstruct">isConstruct</a></code> | Checks if `x` is a construct. |
| <code><a href="#projen.python.Pytest.isComponent">isComponent</a></code> | Test whether the given construct is a component. |

---

##### `isConstruct` <a name="isConstruct" id="projen.python.Pytest.isConstruct"></a>

```typescript
import { python } from 'projen'

python.Pytest.isConstruct(x: any)
```

Checks if `x` is a construct.

Use this method instead of `instanceof` to properly detect `Construct`
instances, even when the construct library is symlinked.

Explanation: in JavaScript, multiple copies of the `constructs` library on
disk are seen as independent, completely different libraries. As a
consequence, the class `Construct` in each copy of the `constructs` library
is seen as a different class, and an instance of one class will not test as
`instanceof` the other class. `npm install` will not create installations
like this, but users may manually symlink construct libraries together or
use a monorepo tool: in those cases, multiple copies of the `constructs`
library can be accidentally installed, and `instanceof` will behave
unpredictably. It is safest to avoid using `instanceof`, and using
this type-testing method instead.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.Pytest.isConstruct.parameter.x"></a>

- *Type:* any

Any object.

---

##### `isComponent` <a name="isComponent" id="projen.python.Pytest.isComponent"></a>

```typescript
import { python } from 'projen'

python.Pytest.isComponent(x: any)
```

Test whether the given construct is a component.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.Pytest.isComponent.parameter.x"></a>

- *Type:* any

---

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Pytest.property.node">node</a></code> | <code>constructs.Node</code> | The tree node. |
| <code><a href="#projen.python.Pytest.property.project">project</a></code> | <code>projen.Project</code> | *No description.* |
| <code><a href="#projen.python.Pytest.property.testdir">testdir</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.Pytest.property.testMatch">testMatch</a></code> | <code>string[]</code> | *No description.* |

---

##### `node`<sup>Required</sup> <a name="node" id="projen.python.Pytest.property.node"></a>

```typescript
public readonly node: Node;
```

- *Type:* constructs.Node

The tree node.

---

##### `project`<sup>Required</sup> <a name="project" id="projen.python.Pytest.property.project"></a>

```typescript
public readonly project: Project;
```

- *Type:* projen.Project

---

##### ~~`testdir`~~<sup>Required</sup> <a name="testdir" id="projen.python.Pytest.property.testdir"></a>

- *Deprecated:* Use `sampleTestdir` on the project instead.

```typescript
public readonly testdir: string;
```

- *Type:* string

---

##### `testMatch`<sup>Required</sup> <a name="testMatch" id="projen.python.Pytest.property.testMatch"></a>

```typescript
public readonly testMatch: string[];
```

- *Type:* string[]

---


### PytestSample <a name="PytestSample" id="projen.python.PytestSample"></a>

Python test code sample.

#### Initializers <a name="Initializers" id="projen.python.PytestSample.Initializer"></a>

```typescript
import { python } from 'projen'

new python.PytestSample(project: Project, options: PytestSampleOptions)
```

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PytestSample.Initializer.parameter.project">project</a></code> | <code>projen.Project</code> | *No description.* |
| <code><a href="#projen.python.PytestSample.Initializer.parameter.options">options</a></code> | <code><a href="#projen.python.PytestSampleOptions">PytestSampleOptions</a></code> | *No description.* |

---

##### `project`<sup>Required</sup> <a name="project" id="projen.python.PytestSample.Initializer.parameter.project"></a>

- *Type:* projen.Project

---

##### `options`<sup>Required</sup> <a name="options" id="projen.python.PytestSample.Initializer.parameter.options"></a>

- *Type:* <a href="#projen.python.PytestSampleOptions">PytestSampleOptions</a>

---

#### Methods <a name="Methods" id="Methods"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PytestSample.toString">toString</a></code> | Returns a string representation of this construct. |
| <code><a href="#projen.python.PytestSample.postSynthesize">postSynthesize</a></code> | Called after synthesis. |
| <code><a href="#projen.python.PytestSample.preSynthesize">preSynthesize</a></code> | Called before synthesis. |
| <code><a href="#projen.python.PytestSample.synthesize">synthesize</a></code> | Synthesizes files to the project output directory. |

---

##### `toString` <a name="toString" id="projen.python.PytestSample.toString"></a>

```typescript
public toString(): string
```

Returns a string representation of this construct.

##### `postSynthesize` <a name="postSynthesize" id="projen.python.PytestSample.postSynthesize"></a>

```typescript
public postSynthesize(): void
```

Called after synthesis.

Order is *not* guaranteed.

##### `preSynthesize` <a name="preSynthesize" id="projen.python.PytestSample.preSynthesize"></a>

```typescript
public preSynthesize(): void
```

Called before synthesis.

##### `synthesize` <a name="synthesize" id="projen.python.PytestSample.synthesize"></a>

```typescript
public synthesize(): void
```

Synthesizes files to the project output directory.

#### Static Functions <a name="Static Functions" id="Static Functions"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PytestSample.isConstruct">isConstruct</a></code> | Checks if `x` is a construct. |
| <code><a href="#projen.python.PytestSample.isComponent">isComponent</a></code> | Test whether the given construct is a component. |

---

##### `isConstruct` <a name="isConstruct" id="projen.python.PytestSample.isConstruct"></a>

```typescript
import { python } from 'projen'

python.PytestSample.isConstruct(x: any)
```

Checks if `x` is a construct.

Use this method instead of `instanceof` to properly detect `Construct`
instances, even when the construct library is symlinked.

Explanation: in JavaScript, multiple copies of the `constructs` library on
disk are seen as independent, completely different libraries. As a
consequence, the class `Construct` in each copy of the `constructs` library
is seen as a different class, and an instance of one class will not test as
`instanceof` the other class. `npm install` will not create installations
like this, but users may manually symlink construct libraries together or
use a monorepo tool: in those cases, multiple copies of the `constructs`
library can be accidentally installed, and `instanceof` will behave
unpredictably. It is safest to avoid using `instanceof`, and using
this type-testing method instead.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.PytestSample.isConstruct.parameter.x"></a>

- *Type:* any

Any object.

---

##### `isComponent` <a name="isComponent" id="projen.python.PytestSample.isComponent"></a>

```typescript
import { python } from 'projen'

python.PytestSample.isComponent(x: any)
```

Test whether the given construct is a component.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.PytestSample.isComponent.parameter.x"></a>

- *Type:* any

---

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PytestSample.property.node">node</a></code> | <code>constructs.Node</code> | The tree node. |
| <code><a href="#projen.python.PytestSample.property.project">project</a></code> | <code>projen.Project</code> | *No description.* |

---

##### `node`<sup>Required</sup> <a name="node" id="projen.python.PytestSample.property.node"></a>

```typescript
public readonly node: Node;
```

- *Type:* constructs.Node

The tree node.

---

##### `project`<sup>Required</sup> <a name="project" id="projen.python.PytestSample.property.project"></a>

```typescript
public readonly project: Project;
```

- *Type:* projen.Project

---


### PythonProject <a name="PythonProject" id="projen.python.PythonProject"></a>

Python project.

#### Initializers <a name="Initializers" id="projen.python.PythonProject.Initializer"></a>

```typescript
import { python } from 'projen'

new python.PythonProject(options: PythonProjectOptions)
```

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PythonProject.Initializer.parameter.options">options</a></code> | <code><a href="#projen.python.PythonProjectOptions">PythonProjectOptions</a></code> | *No description.* |

---

##### `options`<sup>Required</sup> <a name="options" id="projen.python.PythonProject.Initializer.parameter.options"></a>

- *Type:* <a href="#projen.python.PythonProjectOptions">PythonProjectOptions</a>

---

#### Methods <a name="Methods" id="Methods"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PythonProject.toString">toString</a></code> | Returns a string representation of this construct. |
| <code><a href="#projen.python.PythonProject.addExcludeFromCleanup">addExcludeFromCleanup</a></code> | Exclude the matching files from pre-synth cleanup. |
| <code><a href="#projen.python.PythonProject.addGitIgnore">addGitIgnore</a></code> | Adds a .gitignore pattern. |
| <code><a href="#projen.python.PythonProject.addPackageIgnore">addPackageIgnore</a></code> | Exclude these files from the bundled package. |
| <code><a href="#projen.python.PythonProject.addTask">addTask</a></code> | Adds a new task to this project. |
| <code><a href="#projen.python.PythonProject.addTip">addTip</a></code> | Prints a "tip" message during synthesis. |
| <code><a href="#projen.python.PythonProject.annotateGenerated">annotateGenerated</a></code> | Marks the provided file(s) as being generated. |
| <code><a href="#projen.python.PythonProject.postSynthesize">postSynthesize</a></code> | Called after all components are synthesized. |
| <code><a href="#projen.python.PythonProject.preSynthesize">preSynthesize</a></code> | Called before all components are synthesized. |
| <code><a href="#projen.python.PythonProject.removeTask">removeTask</a></code> | Removes a task from a project. |
| <code><a href="#projen.python.PythonProject.runTaskCommand">runTaskCommand</a></code> | Returns the shell command to execute in order to run a task. |
| <code><a href="#projen.python.PythonProject.synth">synth</a></code> | Synthesize all project files into `outdir`. |
| <code><a href="#projen.python.PythonProject.tryFindFile">tryFindFile</a></code> | Finds a file at the specified relative path within this project and all its subprojects. |
| <code><a href="#projen.python.PythonProject.tryFindJsonFile">tryFindJsonFile</a></code> | Finds a json file by name. |
| <code><a href="#projen.python.PythonProject.tryFindObjectFile">tryFindObjectFile</a></code> | Finds an object file (like JsonFile, YamlFile, etc.) by name. |
| <code><a href="#projen.python.PythonProject.tryRemoveFile">tryRemoveFile</a></code> | Finds a file at the specified relative path within this project and removes it. |
| <code><a href="#projen.python.PythonProject.addDependency">addDependency</a></code> | Adds a runtime dependency. |
| <code><a href="#projen.python.PythonProject.addDevDependency">addDevDependency</a></code> | Adds a dev dependency. |

---

##### `toString` <a name="toString" id="projen.python.PythonProject.toString"></a>

```typescript
public toString(): string
```

Returns a string representation of this construct.

##### `addExcludeFromCleanup` <a name="addExcludeFromCleanup" id="projen.python.PythonProject.addExcludeFromCleanup"></a>

```typescript
public addExcludeFromCleanup(globs: ...string[]): void
```

Exclude the matching files from pre-synth cleanup.

Can be used when, for example, some
source files include the projen marker and we don't want them to be erased during synth.

###### `globs`<sup>Required</sup> <a name="globs" id="projen.python.PythonProject.addExcludeFromCleanup.parameter.globs"></a>

- *Type:* ...string[]

The glob patterns to match.

---

##### `addGitIgnore` <a name="addGitIgnore" id="projen.python.PythonProject.addGitIgnore"></a>

```typescript
public addGitIgnore(pattern: string): void
```

Adds a .gitignore pattern.

###### `pattern`<sup>Required</sup> <a name="pattern" id="projen.python.PythonProject.addGitIgnore.parameter.pattern"></a>

- *Type:* string

The glob pattern to ignore.

---

##### `addPackageIgnore` <a name="addPackageIgnore" id="projen.python.PythonProject.addPackageIgnore"></a>

```typescript
public addPackageIgnore(_pattern: string): void
```

Exclude these files from the bundled package.

Implemented by project types based on the
packaging mechanism. For example, `NodeProject` delegates this to `.npmignore`.

###### `_pattern`<sup>Required</sup> <a name="_pattern" id="projen.python.PythonProject.addPackageIgnore.parameter._pattern"></a>

- *Type:* string

The glob pattern to exclude.

---

##### `addTask` <a name="addTask" id="projen.python.PythonProject.addTask"></a>

```typescript
public addTask(name: string, props?: TaskOptions): Task
```

Adds a new task to this project.

This will fail if the project already has
a task with this name.

###### `name`<sup>Required</sup> <a name="name" id="projen.python.PythonProject.addTask.parameter.name"></a>

- *Type:* string

The task name to add.

---

###### `props`<sup>Optional</sup> <a name="props" id="projen.python.PythonProject.addTask.parameter.props"></a>

- *Type:* projen.TaskOptions

Task properties.

---

##### ~~`addTip`~~ <a name="addTip" id="projen.python.PythonProject.addTip"></a>

```typescript
public addTip(message: string): void
```

Prints a "tip" message during synthesis.

###### `message`<sup>Required</sup> <a name="message" id="projen.python.PythonProject.addTip.parameter.message"></a>

- *Type:* string

The message.

---

##### `annotateGenerated` <a name="annotateGenerated" id="projen.python.PythonProject.annotateGenerated"></a>

```typescript
public annotateGenerated(glob: string): void
```

Marks the provided file(s) as being generated.

This is achieved using the
github-linguist attributes. Generated files do not count against the
repository statistics and language breakdown.

> [https://github.com/github/linguist/blob/master/docs/overrides.md](https://github.com/github/linguist/blob/master/docs/overrides.md)

###### `glob`<sup>Required</sup> <a name="glob" id="projen.python.PythonProject.annotateGenerated.parameter.glob"></a>

- *Type:* string

the glob pattern to match (could be a file path).

---

##### `postSynthesize` <a name="postSynthesize" id="projen.python.PythonProject.postSynthesize"></a>

```typescript
public postSynthesize(): void
```

Called after all components are synthesized.

Order is *not* guaranteed.

##### `preSynthesize` <a name="preSynthesize" id="projen.python.PythonProject.preSynthesize"></a>

```typescript
public preSynthesize(): void
```

Called before all components are synthesized.

##### `removeTask` <a name="removeTask" id="projen.python.PythonProject.removeTask"></a>

```typescript
public removeTask(name: string): Task
```

Removes a task from a project.

###### `name`<sup>Required</sup> <a name="name" id="projen.python.PythonProject.removeTask.parameter.name"></a>

- *Type:* string

The name of the task to remove.

---

##### `runTaskCommand` <a name="runTaskCommand" id="projen.python.PythonProject.runTaskCommand"></a>

```typescript
public runTaskCommand(task: Task): string
```

Returns the shell command to execute in order to run a task.

By default, this is `npx projen@<version> <task>`

###### `task`<sup>Required</sup> <a name="task" id="projen.python.PythonProject.runTaskCommand.parameter.task"></a>

- *Type:* projen.Task

The task for which the command is required.

---

##### `synth` <a name="synth" id="projen.python.PythonProject.synth"></a>

```typescript
public synth(): void
```

Synthesize all project files into `outdir`.

1. Call "this.preSynthesize()"
2. Delete all generated files
3. Synthesize all subprojects
4. Synthesize all components of this project
5. Call "postSynthesize()" for all components of this project
6. Call "this.postSynthesize()"

##### `tryFindFile` <a name="tryFindFile" id="projen.python.PythonProject.tryFindFile"></a>

```typescript
public tryFindFile(filePath: string): FileBase
```

Finds a file at the specified relative path within this project and all its subprojects.

###### `filePath`<sup>Required</sup> <a name="filePath" id="projen.python.PythonProject.tryFindFile.parameter.filePath"></a>

- *Type:* string

The file path.

If this path is relative, it will be resolved
from the root of _this_ project.

---

##### ~~`tryFindJsonFile`~~ <a name="tryFindJsonFile" id="projen.python.PythonProject.tryFindJsonFile"></a>

```typescript
public tryFindJsonFile(filePath: string): JsonFile
```

Finds a json file by name.

###### `filePath`<sup>Required</sup> <a name="filePath" id="projen.python.PythonProject.tryFindJsonFile.parameter.filePath"></a>

- *Type:* string

The file path.

---

##### `tryFindObjectFile` <a name="tryFindObjectFile" id="projen.python.PythonProject.tryFindObjectFile"></a>

```typescript
public tryFindObjectFile(filePath: string): ObjectFile
```

Finds an object file (like JsonFile, YamlFile, etc.) by name.

###### `filePath`<sup>Required</sup> <a name="filePath" id="projen.python.PythonProject.tryFindObjectFile.parameter.filePath"></a>

- *Type:* string

The file path.

---

##### `tryRemoveFile` <a name="tryRemoveFile" id="projen.python.PythonProject.tryRemoveFile"></a>

```typescript
public tryRemoveFile(filePath: string): FileBase
```

Finds a file at the specified relative path within this project and removes it.

###### `filePath`<sup>Required</sup> <a name="filePath" id="projen.python.PythonProject.tryRemoveFile.parameter.filePath"></a>

- *Type:* string

The file path.

If this path is relative, it will be
resolved from the root of _this_ project.

---

##### `addDependency` <a name="addDependency" id="projen.python.PythonProject.addDependency"></a>

```typescript
public addDependency(spec: string): void
```

Adds a runtime dependency.

###### `spec`<sup>Required</sup> <a name="spec" id="projen.python.PythonProject.addDependency.parameter.spec"></a>

- *Type:* string

Format `<module>@<semver>`.

---

##### `addDevDependency` <a name="addDevDependency" id="projen.python.PythonProject.addDevDependency"></a>

```typescript
public addDevDependency(spec: string): void
```

Adds a dev dependency.

###### `spec`<sup>Required</sup> <a name="spec" id="projen.python.PythonProject.addDevDependency.parameter.spec"></a>

- *Type:* string

Format `<module>@<semver>`.

---

#### Static Functions <a name="Static Functions" id="Static Functions"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PythonProject.isConstruct">isConstruct</a></code> | Checks if `x` is a construct. |
| <code><a href="#projen.python.PythonProject.isProject">isProject</a></code> | Test whether the given construct is a project. |
| <code><a href="#projen.python.PythonProject.of">of</a></code> | Find the closest ancestor project for given construct. |

---

##### `isConstruct` <a name="isConstruct" id="projen.python.PythonProject.isConstruct"></a>

```typescript
import { python } from 'projen'

python.PythonProject.isConstruct(x: any)
```

Checks if `x` is a construct.

Use this method instead of `instanceof` to properly detect `Construct`
instances, even when the construct library is symlinked.

Explanation: in JavaScript, multiple copies of the `constructs` library on
disk are seen as independent, completely different libraries. As a
consequence, the class `Construct` in each copy of the `constructs` library
is seen as a different class, and an instance of one class will not test as
`instanceof` the other class. `npm install` will not create installations
like this, but users may manually symlink construct libraries together or
use a monorepo tool: in those cases, multiple copies of the `constructs`
library can be accidentally installed, and `instanceof` will behave
unpredictably. It is safest to avoid using `instanceof`, and using
this type-testing method instead.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.PythonProject.isConstruct.parameter.x"></a>

- *Type:* any

Any object.

---

##### `isProject` <a name="isProject" id="projen.python.PythonProject.isProject"></a>

```typescript
import { python } from 'projen'

python.PythonProject.isProject(x: any)
```

Test whether the given construct is a project.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.PythonProject.isProject.parameter.x"></a>

- *Type:* any

---

##### `of` <a name="of" id="projen.python.PythonProject.of"></a>

```typescript
import { python } from 'projen'

python.PythonProject.of(construct: IConstruct)
```

Find the closest ancestor project for given construct.

When given a project, this it the project itself.

###### `construct`<sup>Required</sup> <a name="construct" id="projen.python.PythonProject.of.parameter.construct"></a>

- *Type:* constructs.IConstruct

---

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PythonProject.property.node">node</a></code> | <code>constructs.Node</code> | The tree node. |
| <code><a href="#projen.python.PythonProject.property.buildTask">buildTask</a></code> | <code>projen.Task</code> | *No description.* |
| <code><a href="#projen.python.PythonProject.property.commitGenerated">commitGenerated</a></code> | <code>boolean</code> | Whether to commit the managed files by default. |
| <code><a href="#projen.python.PythonProject.property.compileTask">compileTask</a></code> | <code>projen.Task</code> | *No description.* |
| <code><a href="#projen.python.PythonProject.property.components">components</a></code> | <code>projen.Component[]</code> | Returns all the components within this project. |
| <code><a href="#projen.python.PythonProject.property.deps">deps</a></code> | <code>projen.Dependencies</code> | Project dependencies. |
| <code><a href="#projen.python.PythonProject.property.ejected">ejected</a></code> | <code>boolean</code> | Whether or not the project is being ejected. |
| <code><a href="#projen.python.PythonProject.property.files">files</a></code> | <code>projen.FileBase[]</code> | All files in this project. |
| <code><a href="#projen.python.PythonProject.property.gitattributes">gitattributes</a></code> | <code>projen.GitAttributesFile</code> | The .gitattributes file for this repository. |
| <code><a href="#projen.python.PythonProject.property.gitignore">gitignore</a></code> | <code>projen.IgnoreFile</code> | .gitignore. |
| <code><a href="#projen.python.PythonProject.property.logger">logger</a></code> | <code>projen.Logger</code> | Logging utilities. |
| <code><a href="#projen.python.PythonProject.property.name">name</a></code> | <code>string</code> | Project name. |
| <code><a href="#projen.python.PythonProject.property.outdir">outdir</a></code> | <code>string</code> | Absolute output directory of this project. |
| <code><a href="#projen.python.PythonProject.property.packageTask">packageTask</a></code> | <code>projen.Task</code> | *No description.* |
| <code><a href="#projen.python.PythonProject.property.postCompileTask">postCompileTask</a></code> | <code>projen.Task</code> | *No description.* |
| <code><a href="#projen.python.PythonProject.property.preCompileTask">preCompileTask</a></code> | <code>projen.Task</code> | *No description.* |
| <code><a href="#projen.python.PythonProject.property.projectBuild">projectBuild</a></code> | <code>projen.ProjectBuild</code> | Manages the build process of the project. |
| <code><a href="#projen.python.PythonProject.property.projenCommand">projenCommand</a></code> | <code>string</code> | The command to use in order to run the projen CLI. |
| <code><a href="#projen.python.PythonProject.property.root">root</a></code> | <code>projen.Project</code> | The root project. |
| <code><a href="#projen.python.PythonProject.property.subprojects">subprojects</a></code> | <code>projen.Project[]</code> | Returns all the subprojects within this project. |
| <code><a href="#projen.python.PythonProject.property.tasks">tasks</a></code> | <code>projen.Tasks</code> | Project tasks. |
| <code><a href="#projen.python.PythonProject.property.testTask">testTask</a></code> | <code>projen.Task</code> | *No description.* |
| <code><a href="#projen.python.PythonProject.property.defaultTask">defaultTask</a></code> | <code>projen.Task</code> | This is the "default" task, the one that executes "projen". |
| <code><a href="#projen.python.PythonProject.property.initProject">initProject</a></code> | <code>projen.InitProject</code> | The options used when this project is bootstrapped via `projen new`. |
| <code><a href="#projen.python.PythonProject.property.parent">parent</a></code> | <code>projen.Project</code> | A parent project. |
| <code><a href="#projen.python.PythonProject.property.projectType">projectType</a></code> | <code>projen.ProjectType</code> | *No description.* |
| <code><a href="#projen.python.PythonProject.property.autoApprove">autoApprove</a></code> | <code>projen.github.AutoApprove</code> | Auto approve set up for this project. |
| <code><a href="#projen.python.PythonProject.property.devContainer">devContainer</a></code> | <code>projen.vscode.DevContainer</code> | Access for .devcontainer.json (used for GitHub Codespaces). |
| <code><a href="#projen.python.PythonProject.property.github">github</a></code> | <code>projen.github.GitHub</code> | Access all github components. |
| <code><a href="#projen.python.PythonProject.property.gitpod">gitpod</a></code> | <code>projen.Gitpod</code> | Access for Gitpod. |
| <code><a href="#projen.python.PythonProject.property.vscode">vscode</a></code> | <code>projen.vscode.VsCode</code> | Access all VSCode components. |
| <code><a href="#projen.python.PythonProject.property.envManager">envManager</a></code> | <code><a href="#projen.python.IPythonEnv">IPythonEnv</a></code> | API for managing the Python runtime environment. |
| <code><a href="#projen.python.PythonProject.property.file">file</a></code> | <code><a href="#projen.python.PyprojectTomlFile">PyprojectTomlFile</a></code> | pyproject.toml file. |
| <code><a href="#projen.python.PythonProject.property.moduleName">moduleName</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.PythonProject.property.packagingManager">packagingManager</a></code> | <code><a href="#projen.python.PackageManagerBase">PackageManagerBase</a></code> | API for managing packaging the project as a library. |
| <code><a href="#projen.python.PythonProject.property.projectOptions">projectOptions</a></code> | <code><a href="#projen.python.PyprojectTomlProject">PyprojectTomlProject</a></code> | *No description.* |
| <code><a href="#projen.python.PythonProject.property.sampleTestdir">sampleTestdir</a></code> | <code>string</code> | Directory where sample tests are located. |
| <code><a href="#projen.python.PythonProject.property.pytest">pytest</a></code> | <code><a href="#projen.python.Pytest">Pytest</a></code> | Pytest component. |

---

##### `node`<sup>Required</sup> <a name="node" id="projen.python.PythonProject.property.node"></a>

```typescript
public readonly node: Node;
```

- *Type:* constructs.Node

The tree node.

---

##### `buildTask`<sup>Required</sup> <a name="buildTask" id="projen.python.PythonProject.property.buildTask"></a>

```typescript
public readonly buildTask: Task;
```

- *Type:* projen.Task

---

##### `commitGenerated`<sup>Required</sup> <a name="commitGenerated" id="projen.python.PythonProject.property.commitGenerated"></a>

```typescript
public readonly commitGenerated: boolean;
```

- *Type:* boolean

Whether to commit the managed files by default.

---

##### `compileTask`<sup>Required</sup> <a name="compileTask" id="projen.python.PythonProject.property.compileTask"></a>

```typescript
public readonly compileTask: Task;
```

- *Type:* projen.Task

---

##### `components`<sup>Required</sup> <a name="components" id="projen.python.PythonProject.property.components"></a>

```typescript
public readonly components: Component[];
```

- *Type:* projen.Component[]

Returns all the components within this project.

---

##### `deps`<sup>Required</sup> <a name="deps" id="projen.python.PythonProject.property.deps"></a>

```typescript
public readonly deps: Dependencies;
```

- *Type:* projen.Dependencies

Project dependencies.

---

##### `ejected`<sup>Required</sup> <a name="ejected" id="projen.python.PythonProject.property.ejected"></a>

```typescript
public readonly ejected: boolean;
```

- *Type:* boolean

Whether or not the project is being ejected.

---

##### `files`<sup>Required</sup> <a name="files" id="projen.python.PythonProject.property.files"></a>

```typescript
public readonly files: FileBase[];
```

- *Type:* projen.FileBase[]

All files in this project.

---

##### `gitattributes`<sup>Required</sup> <a name="gitattributes" id="projen.python.PythonProject.property.gitattributes"></a>

```typescript
public readonly gitattributes: GitAttributesFile;
```

- *Type:* projen.GitAttributesFile

The .gitattributes file for this repository.

---

##### `gitignore`<sup>Required</sup> <a name="gitignore" id="projen.python.PythonProject.property.gitignore"></a>

```typescript
public readonly gitignore: IgnoreFile;
```

- *Type:* projen.IgnoreFile

.gitignore.

---

##### `logger`<sup>Required</sup> <a name="logger" id="projen.python.PythonProject.property.logger"></a>

```typescript
public readonly logger: Logger;
```

- *Type:* projen.Logger

Logging utilities.

---

##### `name`<sup>Required</sup> <a name="name" id="projen.python.PythonProject.property.name"></a>

```typescript
public readonly name: string;
```

- *Type:* string

Project name.

---

##### `outdir`<sup>Required</sup> <a name="outdir" id="projen.python.PythonProject.property.outdir"></a>

```typescript
public readonly outdir: string;
```

- *Type:* string

Absolute output directory of this project.

---

##### `packageTask`<sup>Required</sup> <a name="packageTask" id="projen.python.PythonProject.property.packageTask"></a>

```typescript
public readonly packageTask: Task;
```

- *Type:* projen.Task

---

##### `postCompileTask`<sup>Required</sup> <a name="postCompileTask" id="projen.python.PythonProject.property.postCompileTask"></a>

```typescript
public readonly postCompileTask: Task;
```

- *Type:* projen.Task

---

##### `preCompileTask`<sup>Required</sup> <a name="preCompileTask" id="projen.python.PythonProject.property.preCompileTask"></a>

```typescript
public readonly preCompileTask: Task;
```

- *Type:* projen.Task

---

##### `projectBuild`<sup>Required</sup> <a name="projectBuild" id="projen.python.PythonProject.property.projectBuild"></a>

```typescript
public readonly projectBuild: ProjectBuild;
```

- *Type:* projen.ProjectBuild

Manages the build process of the project.

---

##### `projenCommand`<sup>Required</sup> <a name="projenCommand" id="projen.python.PythonProject.property.projenCommand"></a>

```typescript
public readonly projenCommand: string;
```

- *Type:* string

The command to use in order to run the projen CLI.

---

##### `root`<sup>Required</sup> <a name="root" id="projen.python.PythonProject.property.root"></a>

```typescript
public readonly root: Project;
```

- *Type:* projen.Project

The root project.

---

##### `subprojects`<sup>Required</sup> <a name="subprojects" id="projen.python.PythonProject.property.subprojects"></a>

```typescript
public readonly subprojects: Project[];
```

- *Type:* projen.Project[]

Returns all the subprojects within this project.

---

##### `tasks`<sup>Required</sup> <a name="tasks" id="projen.python.PythonProject.property.tasks"></a>

```typescript
public readonly tasks: Tasks;
```

- *Type:* projen.Tasks

Project tasks.

---

##### `testTask`<sup>Required</sup> <a name="testTask" id="projen.python.PythonProject.property.testTask"></a>

```typescript
public readonly testTask: Task;
```

- *Type:* projen.Task

---

##### `defaultTask`<sup>Optional</sup> <a name="defaultTask" id="projen.python.PythonProject.property.defaultTask"></a>

```typescript
public readonly defaultTask: Task;
```

- *Type:* projen.Task

This is the "default" task, the one that executes "projen".

Undefined if
the project is being ejected.

---

##### `initProject`<sup>Optional</sup> <a name="initProject" id="projen.python.PythonProject.property.initProject"></a>

```typescript
public readonly initProject: InitProject;
```

- *Type:* projen.InitProject

The options used when this project is bootstrapped via `projen new`.

It
includes the original set of options passed to the CLI and also the JSII
FQN of the project type.

---

##### `parent`<sup>Optional</sup> <a name="parent" id="projen.python.PythonProject.property.parent"></a>

```typescript
public readonly parent: Project;
```

- *Type:* projen.Project

A parent project.

If undefined, this is the root project.

---

##### `projectType`<sup>Required</sup> <a name="projectType" id="projen.python.PythonProject.property.projectType"></a>

```typescript
public readonly projectType: ProjectType;
```

- *Type:* projen.ProjectType

---

##### `autoApprove`<sup>Optional</sup> <a name="autoApprove" id="projen.python.PythonProject.property.autoApprove"></a>

```typescript
public readonly autoApprove: AutoApprove;
```

- *Type:* projen.github.AutoApprove

Auto approve set up for this project.

---

##### `devContainer`<sup>Optional</sup> <a name="devContainer" id="projen.python.PythonProject.property.devContainer"></a>

```typescript
public readonly devContainer: DevContainer;
```

- *Type:* projen.vscode.DevContainer

Access for .devcontainer.json (used for GitHub Codespaces).

This will be `undefined` if devContainer boolean is false

---

##### `github`<sup>Optional</sup> <a name="github" id="projen.python.PythonProject.property.github"></a>

```typescript
public readonly github: GitHub;
```

- *Type:* projen.github.GitHub

Access all github components.

This will be `undefined` for subprojects.

---

##### `gitpod`<sup>Optional</sup> <a name="gitpod" id="projen.python.PythonProject.property.gitpod"></a>

```typescript
public readonly gitpod: Gitpod;
```

- *Type:* projen.Gitpod

Access for Gitpod.

This will be `undefined` if gitpod boolean is false

---

##### `vscode`<sup>Optional</sup> <a name="vscode" id="projen.python.PythonProject.property.vscode"></a>

```typescript
public readonly vscode: VsCode;
```

- *Type:* projen.vscode.VsCode

Access all VSCode components.

This will be `undefined` for subprojects.

---

##### `envManager`<sup>Required</sup> <a name="envManager" id="projen.python.PythonProject.property.envManager"></a>

```typescript
public readonly envManager: IPythonEnv;
```

- *Type:* <a href="#projen.python.IPythonEnv">IPythonEnv</a>

API for managing the Python runtime environment.

---

##### `file`<sup>Required</sup> <a name="file" id="projen.python.PythonProject.property.file"></a>

```typescript
public readonly file: PyprojectTomlFile;
```

- *Type:* <a href="#projen.python.PyprojectTomlFile">PyprojectTomlFile</a>

pyproject.toml file.

---

##### `moduleName`<sup>Required</sup> <a name="moduleName" id="projen.python.PythonProject.property.moduleName"></a>

```typescript
public readonly moduleName: string;
```

- *Type:* string

---

##### `packagingManager`<sup>Required</sup> <a name="packagingManager" id="projen.python.PythonProject.property.packagingManager"></a>

```typescript
public readonly packagingManager: PackageManagerBase;
```

- *Type:* <a href="#projen.python.PackageManagerBase">PackageManagerBase</a>

API for managing packaging the project as a library.

Only applies when the `projectType` is LIB.

---

##### `projectOptions`<sup>Required</sup> <a name="projectOptions" id="projen.python.PythonProject.property.projectOptions"></a>

```typescript
public readonly projectOptions: PyprojectTomlProject;
```

- *Type:* <a href="#projen.python.PyprojectTomlProject">PyprojectTomlProject</a>

---

##### `sampleTestdir`<sup>Required</sup> <a name="sampleTestdir" id="projen.python.PythonProject.property.sampleTestdir"></a>

```typescript
public readonly sampleTestdir: string;
```

- *Type:* string
- *Default:* "tests"

Directory where sample tests are located.

---

##### `pytest`<sup>Optional</sup> <a name="pytest" id="projen.python.PythonProject.property.pytest"></a>

```typescript
public readonly pytest: Pytest;
```

- *Type:* <a href="#projen.python.Pytest">Pytest</a>

Pytest component.

---

#### Constants <a name="Constants" id="Constants"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PythonProject.property.DEFAULT_TASK">DEFAULT_TASK</a></code> | <code>string</code> | The name of the default task (the task executed when `projen` is run without arguments). |

---

##### `DEFAULT_TASK`<sup>Required</sup> <a name="DEFAULT_TASK" id="projen.python.PythonProject.property.DEFAULT_TASK"></a>

```typescript
public readonly DEFAULT_TASK: string;
```

- *Type:* string

The name of the default task (the task executed when `projen` is run without arguments).

Normally
this task should synthesize the project files.

---

### PythonSample <a name="PythonSample" id="projen.python.PythonSample"></a>

Python code sample.

#### Initializers <a name="Initializers" id="projen.python.PythonSample.Initializer"></a>

```typescript
import { python } from 'projen'

new python.PythonSample(project: Project, options: PythonSampleOptions)
```

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PythonSample.Initializer.parameter.project">project</a></code> | <code>projen.Project</code> | *No description.* |
| <code><a href="#projen.python.PythonSample.Initializer.parameter.options">options</a></code> | <code><a href="#projen.python.PythonSampleOptions">PythonSampleOptions</a></code> | *No description.* |

---

##### `project`<sup>Required</sup> <a name="project" id="projen.python.PythonSample.Initializer.parameter.project"></a>

- *Type:* projen.Project

---

##### `options`<sup>Required</sup> <a name="options" id="projen.python.PythonSample.Initializer.parameter.options"></a>

- *Type:* <a href="#projen.python.PythonSampleOptions">PythonSampleOptions</a>

---

#### Methods <a name="Methods" id="Methods"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PythonSample.toString">toString</a></code> | Returns a string representation of this construct. |
| <code><a href="#projen.python.PythonSample.postSynthesize">postSynthesize</a></code> | Called after synthesis. |
| <code><a href="#projen.python.PythonSample.preSynthesize">preSynthesize</a></code> | Called before synthesis. |
| <code><a href="#projen.python.PythonSample.synthesize">synthesize</a></code> | Synthesizes files to the project output directory. |

---

##### `toString` <a name="toString" id="projen.python.PythonSample.toString"></a>

```typescript
public toString(): string
```

Returns a string representation of this construct.

##### `postSynthesize` <a name="postSynthesize" id="projen.python.PythonSample.postSynthesize"></a>

```typescript
public postSynthesize(): void
```

Called after synthesis.

Order is *not* guaranteed.

##### `preSynthesize` <a name="preSynthesize" id="projen.python.PythonSample.preSynthesize"></a>

```typescript
public preSynthesize(): void
```

Called before synthesis.

##### `synthesize` <a name="synthesize" id="projen.python.PythonSample.synthesize"></a>

```typescript
public synthesize(): void
```

Synthesizes files to the project output directory.

#### Static Functions <a name="Static Functions" id="Static Functions"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PythonSample.isConstruct">isConstruct</a></code> | Checks if `x` is a construct. |
| <code><a href="#projen.python.PythonSample.isComponent">isComponent</a></code> | Test whether the given construct is a component. |

---

##### `isConstruct` <a name="isConstruct" id="projen.python.PythonSample.isConstruct"></a>

```typescript
import { python } from 'projen'

python.PythonSample.isConstruct(x: any)
```

Checks if `x` is a construct.

Use this method instead of `instanceof` to properly detect `Construct`
instances, even when the construct library is symlinked.

Explanation: in JavaScript, multiple copies of the `constructs` library on
disk are seen as independent, completely different libraries. As a
consequence, the class `Construct` in each copy of the `constructs` library
is seen as a different class, and an instance of one class will not test as
`instanceof` the other class. `npm install` will not create installations
like this, but users may manually symlink construct libraries together or
use a monorepo tool: in those cases, multiple copies of the `constructs`
library can be accidentally installed, and `instanceof` will behave
unpredictably. It is safest to avoid using `instanceof`, and using
this type-testing method instead.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.PythonSample.isConstruct.parameter.x"></a>

- *Type:* any

Any object.

---

##### `isComponent` <a name="isComponent" id="projen.python.PythonSample.isComponent"></a>

```typescript
import { python } from 'projen'

python.PythonSample.isComponent(x: any)
```

Test whether the given construct is a component.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.PythonSample.isComponent.parameter.x"></a>

- *Type:* any

---

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PythonSample.property.node">node</a></code> | <code>constructs.Node</code> | The tree node. |
| <code><a href="#projen.python.PythonSample.property.project">project</a></code> | <code>projen.Project</code> | *No description.* |

---

##### `node`<sup>Required</sup> <a name="node" id="projen.python.PythonSample.property.node"></a>

```typescript
public readonly node: Node;
```

- *Type:* constructs.Node

The tree node.

---

##### `project`<sup>Required</sup> <a name="project" id="projen.python.PythonSample.property.project"></a>

```typescript
public readonly project: Project;
```

- *Type:* projen.Project

---


### Setuptools <a name="Setuptools" id="projen.python.Setuptools"></a>

- *Implements:* <a href="#projen.python.IPythonPackaging">IPythonPackaging</a>, <a href="#projen.python.IPythonEnv">IPythonEnv</a>

Manages packaging through setuptools with a setup.py script.

#### Initializers <a name="Initializers" id="projen.python.Setuptools.Initializer"></a>

```typescript
import { python } from 'projen'

new python.Setuptools(scope: IConstruct, options: PythonBaseOptions)
```

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Setuptools.Initializer.parameter.scope">scope</a></code> | <code>constructs.IConstruct</code> | *No description.* |
| <code><a href="#projen.python.Setuptools.Initializer.parameter.options">options</a></code> | <code><a href="#projen.python.PythonBaseOptions">PythonBaseOptions</a></code> | *No description.* |

---

##### `scope`<sup>Required</sup> <a name="scope" id="projen.python.Setuptools.Initializer.parameter.scope"></a>

- *Type:* constructs.IConstruct

---

##### `options`<sup>Required</sup> <a name="options" id="projen.python.Setuptools.Initializer.parameter.options"></a>

- *Type:* <a href="#projen.python.PythonBaseOptions">PythonBaseOptions</a>

---

#### Methods <a name="Methods" id="Methods"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.Setuptools.toString">toString</a></code> | Returns a string representation of this construct. |
| <code><a href="#projen.python.Setuptools.postSynthesize">postSynthesize</a></code> | Called after synthesis. |
| <code><a href="#projen.python.Setuptools.preSynthesize">preSynthesize</a></code> | Called before synthesis. |
| <code><a href="#projen.python.Setuptools.synthesize">synthesize</a></code> | Synthesizes files to the project output directory. |
| <code><a href="#projen.python.Setuptools.addDependency">addDependency</a></code> | Adds a runtime dependency. |
| <code><a href="#projen.python.Setuptools.addDevDependency">addDevDependency</a></code> | Adds a dev dependency. |
| <code><a href="#projen.python.Setuptools.getRunCommand">getRunCommand</a></code> | *No description.* |
| <code><a href="#projen.python.Setuptools.installDependencies">installDependencies</a></code> | Installs dependencies (called during post-synthesis). |
| <code><a href="#projen.python.Setuptools.setupEnvironment">setupEnvironment</a></code> | Initializes the virtual environment if it doesn't exist (called during post-synthesis). |
| <code><a href="#projen.python.Setuptools.synthDependencies">synthDependencies</a></code> | *No description.* |
| <code><a href="#projen.python.Setuptools.synthDependencyGroups">synthDependencyGroups</a></code> | *No description.* |
| <code><a href="#projen.python.Setuptools.synthDevDependencies">synthDevDependencies</a></code> | *No description.* |

---

##### `toString` <a name="toString" id="projen.python.Setuptools.toString"></a>

```typescript
public toString(): string
```

Returns a string representation of this construct.

##### `postSynthesize` <a name="postSynthesize" id="projen.python.Setuptools.postSynthesize"></a>

```typescript
public postSynthesize(): void
```

Called after synthesis.

Order is *not* guaranteed.

##### `preSynthesize` <a name="preSynthesize" id="projen.python.Setuptools.preSynthesize"></a>

```typescript
public preSynthesize(): void
```

Called before synthesis.

##### `synthesize` <a name="synthesize" id="projen.python.Setuptools.synthesize"></a>

```typescript
public synthesize(): void
```

Synthesizes files to the project output directory.

##### `addDependency` <a name="addDependency" id="projen.python.Setuptools.addDependency"></a>

```typescript
public addDependency(spec: string): void
```

Adds a runtime dependency.

###### `spec`<sup>Required</sup> <a name="spec" id="projen.python.Setuptools.addDependency.parameter.spec"></a>

- *Type:* string

Format `<module>@<semver>`.

---

##### `addDevDependency` <a name="addDevDependency" id="projen.python.Setuptools.addDevDependency"></a>

```typescript
public addDevDependency(spec: string): void
```

Adds a dev dependency.

###### `spec`<sup>Required</sup> <a name="spec" id="projen.python.Setuptools.addDevDependency.parameter.spec"></a>

- *Type:* string

Format `<module>@<semver>`.

---

##### `getRunCommand` <a name="getRunCommand" id="projen.python.Setuptools.getRunCommand"></a>

```typescript
public getRunCommand(_options: PythonBaseOptions): string
```

###### `_options`<sup>Required</sup> <a name="_options" id="projen.python.Setuptools.getRunCommand.parameter._options"></a>

- *Type:* <a href="#projen.python.PythonBaseOptions">PythonBaseOptions</a>

---

##### `installDependencies` <a name="installDependencies" id="projen.python.Setuptools.installDependencies"></a>

```typescript
public installDependencies(): void
```

Installs dependencies (called during post-synthesis).

##### `setupEnvironment` <a name="setupEnvironment" id="projen.python.Setuptools.setupEnvironment"></a>

```typescript
public setupEnvironment(): void
```

Initializes the virtual environment if it doesn't exist (called during post-synthesis).

##### `synthDependencies` <a name="synthDependencies" id="projen.python.Setuptools.synthDependencies"></a>

```typescript
public synthDependencies(): string[]
```

##### `synthDependencyGroups` <a name="synthDependencyGroups" id="projen.python.Setuptools.synthDependencyGroups"></a>

```typescript
public synthDependencyGroups(): {[ key: string ]: any}
```

##### `synthDevDependencies` <a name="synthDevDependencies" id="projen.python.Setuptools.synthDevDependencies"></a>

```typescript
public synthDevDependencies(): string[]
```

#### Static Functions <a name="Static Functions" id="Static Functions"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.Setuptools.isConstruct">isConstruct</a></code> | Checks if `x` is a construct. |
| <code><a href="#projen.python.Setuptools.isComponent">isComponent</a></code> | Test whether the given construct is a component. |

---

##### `isConstruct` <a name="isConstruct" id="projen.python.Setuptools.isConstruct"></a>

```typescript
import { python } from 'projen'

python.Setuptools.isConstruct(x: any)
```

Checks if `x` is a construct.

Use this method instead of `instanceof` to properly detect `Construct`
instances, even when the construct library is symlinked.

Explanation: in JavaScript, multiple copies of the `constructs` library on
disk are seen as independent, completely different libraries. As a
consequence, the class `Construct` in each copy of the `constructs` library
is seen as a different class, and an instance of one class will not test as
`instanceof` the other class. `npm install` will not create installations
like this, but users may manually symlink construct libraries together or
use a monorepo tool: in those cases, multiple copies of the `constructs`
library can be accidentally installed, and `instanceof` will behave
unpredictably. It is safest to avoid using `instanceof`, and using
this type-testing method instead.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.Setuptools.isConstruct.parameter.x"></a>

- *Type:* any

Any object.

---

##### `isComponent` <a name="isComponent" id="projen.python.Setuptools.isComponent"></a>

```typescript
import { python } from 'projen'

python.Setuptools.isComponent(x: any)
```

Test whether the given construct is a component.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.Setuptools.isComponent.parameter.x"></a>

- *Type:* any

---

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Setuptools.property.node">node</a></code> | <code>constructs.Node</code> | The tree node. |
| <code><a href="#projen.python.Setuptools.property.project">project</a></code> | <code><a href="#projen.python.PythonProject">PythonProject</a></code> | *No description.* |
| <code><a href="#projen.python.Setuptools.property.defaultBuildSystem">defaultBuildSystem</a></code> | <code><a href="#projen.python.BuildSystem">BuildSystem</a></code> | *No description.* |
| <code><a href="#projen.python.Setuptools.property.installCiTask">installCiTask</a></code> | <code>projen.Task</code> | A task that installs and updates dependencies. |
| <code><a href="#projen.python.Setuptools.property.installTask">installTask</a></code> | <code>projen.Task</code> | *No description.* |
| <code><a href="#projen.python.Setuptools.property.publishTask">publishTask</a></code> | <code>projen.Task</code> | A task that uploads the package to a package repository. |
| <code><a href="#projen.python.Setuptools.property.runCommand">runCommand</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.Setuptools.property.formatTask">formatTask</a></code> | <code>projen.Task</code> | A task that runs formatting. |
| <code><a href="#projen.python.Setuptools.property.lintTask">lintTask</a></code> | <code>projen.Task</code> | A task that runs linting. |
| <code><a href="#projen.python.Setuptools.property.typeCheckTask">typeCheckTask</a></code> | <code>projen.Task</code> | A task that runs static type checking. |
| <code><a href="#projen.python.Setuptools.property.publishTestTask">publishTestTask</a></code> | <code>projen.Task</code> | *No description.* |

---

##### `node`<sup>Required</sup> <a name="node" id="projen.python.Setuptools.property.node"></a>

```typescript
public readonly node: Node;
```

- *Type:* constructs.Node

The tree node.

---

##### `project`<sup>Required</sup> <a name="project" id="projen.python.Setuptools.property.project"></a>

```typescript
public readonly project: PythonProject;
```

- *Type:* <a href="#projen.python.PythonProject">PythonProject</a>

---

##### `defaultBuildSystem`<sup>Required</sup> <a name="defaultBuildSystem" id="projen.python.Setuptools.property.defaultBuildSystem"></a>

```typescript
public readonly defaultBuildSystem: BuildSystem;
```

- *Type:* <a href="#projen.python.BuildSystem">BuildSystem</a>

---

##### `installCiTask`<sup>Required</sup> <a name="installCiTask" id="projen.python.Setuptools.property.installCiTask"></a>

```typescript
public readonly installCiTask: Task;
```

- *Type:* projen.Task

A task that installs and updates dependencies.

---

##### `installTask`<sup>Required</sup> <a name="installTask" id="projen.python.Setuptools.property.installTask"></a>

```typescript
public readonly installTask: Task;
```

- *Type:* projen.Task

---

##### `publishTask`<sup>Required</sup> <a name="publishTask" id="projen.python.Setuptools.property.publishTask"></a>

```typescript
public readonly publishTask: Task;
```

- *Type:* projen.Task

A task that uploads the package to a package repository.

---

##### `runCommand`<sup>Required</sup> <a name="runCommand" id="projen.python.Setuptools.property.runCommand"></a>

```typescript
public readonly runCommand: string;
```

- *Type:* string

---

##### `formatTask`<sup>Optional</sup> <a name="formatTask" id="projen.python.Setuptools.property.formatTask"></a>

```typescript
public readonly formatTask: Task;
```

- *Type:* projen.Task

A task that runs formatting.

---

##### `lintTask`<sup>Optional</sup> <a name="lintTask" id="projen.python.Setuptools.property.lintTask"></a>

```typescript
public readonly lintTask: Task;
```

- *Type:* projen.Task

A task that runs linting.

---

##### `typeCheckTask`<sup>Optional</sup> <a name="typeCheckTask" id="projen.python.Setuptools.property.typeCheckTask"></a>

```typescript
public readonly typeCheckTask: Task;
```

- *Type:* projen.Task

A task that runs static type checking.

---

##### `publishTestTask`<sup>Required</sup> <a name="publishTestTask" id="projen.python.Setuptools.property.publishTestTask"></a>

```typescript
public readonly publishTestTask: Task;
```

- *Type:* projen.Task

---


### Uv <a name="Uv" id="projen.python.Uv"></a>

Manage project dependencies, virtual environments, and packaging through uv.

#### Initializers <a name="Initializers" id="projen.python.Uv.Initializer"></a>

```typescript
import { python } from 'projen'

new python.Uv(scope: IConstruct, options: PythonBaseOptions)
```

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Uv.Initializer.parameter.scope">scope</a></code> | <code>constructs.IConstruct</code> | *No description.* |
| <code><a href="#projen.python.Uv.Initializer.parameter.options">options</a></code> | <code><a href="#projen.python.PythonBaseOptions">PythonBaseOptions</a></code> | *No description.* |

---

##### `scope`<sup>Required</sup> <a name="scope" id="projen.python.Uv.Initializer.parameter.scope"></a>

- *Type:* constructs.IConstruct

---

##### `options`<sup>Required</sup> <a name="options" id="projen.python.Uv.Initializer.parameter.options"></a>

- *Type:* <a href="#projen.python.PythonBaseOptions">PythonBaseOptions</a>

---

#### Methods <a name="Methods" id="Methods"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.Uv.toString">toString</a></code> | Returns a string representation of this construct. |
| <code><a href="#projen.python.Uv.postSynthesize">postSynthesize</a></code> | Called after synthesis. |
| <code><a href="#projen.python.Uv.preSynthesize">preSynthesize</a></code> | Called before synthesis. |
| <code><a href="#projen.python.Uv.synthesize">synthesize</a></code> | Synthesizes files to the project output directory. |
| <code><a href="#projen.python.Uv.addDependency">addDependency</a></code> | Adds a runtime dependency. |
| <code><a href="#projen.python.Uv.addDevDependency">addDevDependency</a></code> | Adds a dev dependency. |
| <code><a href="#projen.python.Uv.getRunCommand">getRunCommand</a></code> | *No description.* |
| <code><a href="#projen.python.Uv.installDependencies">installDependencies</a></code> | Installs dependencies (called during post-synthesis). |
| <code><a href="#projen.python.Uv.setupEnvironment">setupEnvironment</a></code> | Initializes the virtual environment if it doesn't exist (called during post-synthesis). |
| <code><a href="#projen.python.Uv.synthDependencies">synthDependencies</a></code> | *No description.* |
| <code><a href="#projen.python.Uv.synthDependencyGroups">synthDependencyGroups</a></code> | *No description.* |
| <code><a href="#projen.python.Uv.synthDevDependencies">synthDevDependencies</a></code> | *No description.* |

---

##### `toString` <a name="toString" id="projen.python.Uv.toString"></a>

```typescript
public toString(): string
```

Returns a string representation of this construct.

##### `postSynthesize` <a name="postSynthesize" id="projen.python.Uv.postSynthesize"></a>

```typescript
public postSynthesize(): void
```

Called after synthesis.

Order is *not* guaranteed.

##### `preSynthesize` <a name="preSynthesize" id="projen.python.Uv.preSynthesize"></a>

```typescript
public preSynthesize(): void
```

Called before synthesis.

##### `synthesize` <a name="synthesize" id="projen.python.Uv.synthesize"></a>

```typescript
public synthesize(): void
```

Synthesizes files to the project output directory.

##### `addDependency` <a name="addDependency" id="projen.python.Uv.addDependency"></a>

```typescript
public addDependency(spec: string): void
```

Adds a runtime dependency.

###### `spec`<sup>Required</sup> <a name="spec" id="projen.python.Uv.addDependency.parameter.spec"></a>

- *Type:* string

Format `<module>@<semver>`.

---

##### `addDevDependency` <a name="addDevDependency" id="projen.python.Uv.addDevDependency"></a>

```typescript
public addDevDependency(spec: string): void
```

Adds a dev dependency.

###### `spec`<sup>Required</sup> <a name="spec" id="projen.python.Uv.addDevDependency.parameter.spec"></a>

- *Type:* string

Format `<module>@<semver>`.

---

##### `getRunCommand` <a name="getRunCommand" id="projen.python.Uv.getRunCommand"></a>

```typescript
public getRunCommand(_options: PythonBaseOptions): string
```

###### `_options`<sup>Required</sup> <a name="_options" id="projen.python.Uv.getRunCommand.parameter._options"></a>

- *Type:* <a href="#projen.python.PythonBaseOptions">PythonBaseOptions</a>

---

##### `installDependencies` <a name="installDependencies" id="projen.python.Uv.installDependencies"></a>

```typescript
public installDependencies(): void
```

Installs dependencies (called during post-synthesis).

##### `setupEnvironment` <a name="setupEnvironment" id="projen.python.Uv.setupEnvironment"></a>

```typescript
public setupEnvironment(): void
```

Initializes the virtual environment if it doesn't exist (called during post-synthesis).

##### `synthDependencies` <a name="synthDependencies" id="projen.python.Uv.synthDependencies"></a>

```typescript
public synthDependencies(): string[]
```

##### `synthDependencyGroups` <a name="synthDependencyGroups" id="projen.python.Uv.synthDependencyGroups"></a>

```typescript
public synthDependencyGroups(): {[ key: string ]: any}
```

##### `synthDevDependencies` <a name="synthDevDependencies" id="projen.python.Uv.synthDevDependencies"></a>

```typescript
public synthDevDependencies(): string[]
```

#### Static Functions <a name="Static Functions" id="Static Functions"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.Uv.isConstruct">isConstruct</a></code> | Checks if `x` is a construct. |
| <code><a href="#projen.python.Uv.isComponent">isComponent</a></code> | Test whether the given construct is a component. |

---

##### `isConstruct` <a name="isConstruct" id="projen.python.Uv.isConstruct"></a>

```typescript
import { python } from 'projen'

python.Uv.isConstruct(x: any)
```

Checks if `x` is a construct.

Use this method instead of `instanceof` to properly detect `Construct`
instances, even when the construct library is symlinked.

Explanation: in JavaScript, multiple copies of the `constructs` library on
disk are seen as independent, completely different libraries. As a
consequence, the class `Construct` in each copy of the `constructs` library
is seen as a different class, and an instance of one class will not test as
`instanceof` the other class. `npm install` will not create installations
like this, but users may manually symlink construct libraries together or
use a monorepo tool: in those cases, multiple copies of the `constructs`
library can be accidentally installed, and `instanceof` will behave
unpredictably. It is safest to avoid using `instanceof`, and using
this type-testing method instead.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.Uv.isConstruct.parameter.x"></a>

- *Type:* any

Any object.

---

##### `isComponent` <a name="isComponent" id="projen.python.Uv.isComponent"></a>

```typescript
import { python } from 'projen'

python.Uv.isComponent(x: any)
```

Test whether the given construct is a component.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.Uv.isComponent.parameter.x"></a>

- *Type:* any

---

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Uv.property.node">node</a></code> | <code>constructs.Node</code> | The tree node. |
| <code><a href="#projen.python.Uv.property.project">project</a></code> | <code><a href="#projen.python.PythonProject">PythonProject</a></code> | *No description.* |
| <code><a href="#projen.python.Uv.property.defaultBuildSystem">defaultBuildSystem</a></code> | <code><a href="#projen.python.BuildSystem">BuildSystem</a></code> | *No description.* |
| <code><a href="#projen.python.Uv.property.installCiTask">installCiTask</a></code> | <code>projen.Task</code> | A task that installs and updates dependencies. |
| <code><a href="#projen.python.Uv.property.installTask">installTask</a></code> | <code>projen.Task</code> | *No description.* |
| <code><a href="#projen.python.Uv.property.publishTask">publishTask</a></code> | <code>projen.Task</code> | A task that uploads the package to a package repository. |
| <code><a href="#projen.python.Uv.property.runCommand">runCommand</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.Uv.property.formatTask">formatTask</a></code> | <code>projen.Task</code> | A task that runs formatting. |
| <code><a href="#projen.python.Uv.property.lintTask">lintTask</a></code> | <code>projen.Task</code> | A task that runs linting. |
| <code><a href="#projen.python.Uv.property.typeCheckTask">typeCheckTask</a></code> | <code>projen.Task</code> | A task that runs static type checking. |
| <code><a href="#projen.python.Uv.property.publishTestTask">publishTestTask</a></code> | <code>projen.Task</code> | *No description.* |

---

##### `node`<sup>Required</sup> <a name="node" id="projen.python.Uv.property.node"></a>

```typescript
public readonly node: Node;
```

- *Type:* constructs.Node

The tree node.

---

##### `project`<sup>Required</sup> <a name="project" id="projen.python.Uv.property.project"></a>

```typescript
public readonly project: PythonProject;
```

- *Type:* <a href="#projen.python.PythonProject">PythonProject</a>

---

##### `defaultBuildSystem`<sup>Required</sup> <a name="defaultBuildSystem" id="projen.python.Uv.property.defaultBuildSystem"></a>

```typescript
public readonly defaultBuildSystem: BuildSystem;
```

- *Type:* <a href="#projen.python.BuildSystem">BuildSystem</a>

---

##### `installCiTask`<sup>Required</sup> <a name="installCiTask" id="projen.python.Uv.property.installCiTask"></a>

```typescript
public readonly installCiTask: Task;
```

- *Type:* projen.Task

A task that installs and updates dependencies.

---

##### `installTask`<sup>Required</sup> <a name="installTask" id="projen.python.Uv.property.installTask"></a>

```typescript
public readonly installTask: Task;
```

- *Type:* projen.Task

---

##### `publishTask`<sup>Required</sup> <a name="publishTask" id="projen.python.Uv.property.publishTask"></a>

```typescript
public readonly publishTask: Task;
```

- *Type:* projen.Task

A task that uploads the package to a package repository.

---

##### `runCommand`<sup>Required</sup> <a name="runCommand" id="projen.python.Uv.property.runCommand"></a>

```typescript
public readonly runCommand: string;
```

- *Type:* string

---

##### `formatTask`<sup>Optional</sup> <a name="formatTask" id="projen.python.Uv.property.formatTask"></a>

```typescript
public readonly formatTask: Task;
```

- *Type:* projen.Task

A task that runs formatting.

---

##### `lintTask`<sup>Optional</sup> <a name="lintTask" id="projen.python.Uv.property.lintTask"></a>

```typescript
public readonly lintTask: Task;
```

- *Type:* projen.Task

A task that runs linting.

---

##### `typeCheckTask`<sup>Optional</sup> <a name="typeCheckTask" id="projen.python.Uv.property.typeCheckTask"></a>

```typescript
public readonly typeCheckTask: Task;
```

- *Type:* projen.Task

A task that runs static type checking.

---

##### `publishTestTask`<sup>Required</sup> <a name="publishTestTask" id="projen.python.Uv.property.publishTestTask"></a>

```typescript
public readonly publishTestTask: Task;
```

- *Type:* projen.Task

---


### Venv <a name="Venv" id="projen.python.Venv"></a>

- *Implements:* <a href="#projen.python.IPythonEnv">IPythonEnv</a>

Manages a virtual environment through the Python venv module.

#### Initializers <a name="Initializers" id="projen.python.Venv.Initializer"></a>

```typescript
import { python } from 'projen'

new python.Venv(project: Project, options?: VenvOptions)
```

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Venv.Initializer.parameter.project">project</a></code> | <code>projen.Project</code> | *No description.* |
| <code><a href="#projen.python.Venv.Initializer.parameter.options">options</a></code> | <code><a href="#projen.python.VenvOptions">VenvOptions</a></code> | *No description.* |

---

##### `project`<sup>Required</sup> <a name="project" id="projen.python.Venv.Initializer.parameter.project"></a>

- *Type:* projen.Project

---

##### `options`<sup>Optional</sup> <a name="options" id="projen.python.Venv.Initializer.parameter.options"></a>

- *Type:* <a href="#projen.python.VenvOptions">VenvOptions</a>

---

#### Methods <a name="Methods" id="Methods"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.Venv.toString">toString</a></code> | Returns a string representation of this construct. |
| <code><a href="#projen.python.Venv.postSynthesize">postSynthesize</a></code> | Called after synthesis. |
| <code><a href="#projen.python.Venv.preSynthesize">preSynthesize</a></code> | Called before synthesis. |
| <code><a href="#projen.python.Venv.synthesize">synthesize</a></code> | Synthesizes files to the project output directory. |
| <code><a href="#projen.python.Venv.setupEnvironment">setupEnvironment</a></code> | Initializes the virtual environment if it doesn't exist (called during post-synthesis). |

---

##### `toString` <a name="toString" id="projen.python.Venv.toString"></a>

```typescript
public toString(): string
```

Returns a string representation of this construct.

##### `postSynthesize` <a name="postSynthesize" id="projen.python.Venv.postSynthesize"></a>

```typescript
public postSynthesize(): void
```

Called after synthesis.

Order is *not* guaranteed.

##### `preSynthesize` <a name="preSynthesize" id="projen.python.Venv.preSynthesize"></a>

```typescript
public preSynthesize(): void
```

Called before synthesis.

##### `synthesize` <a name="synthesize" id="projen.python.Venv.synthesize"></a>

```typescript
public synthesize(): void
```

Synthesizes files to the project output directory.

##### `setupEnvironment` <a name="setupEnvironment" id="projen.python.Venv.setupEnvironment"></a>

```typescript
public setupEnvironment(): void
```

Initializes the virtual environment if it doesn't exist (called during post-synthesis).

#### Static Functions <a name="Static Functions" id="Static Functions"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.Venv.isConstruct">isConstruct</a></code> | Checks if `x` is a construct. |
| <code><a href="#projen.python.Venv.isComponent">isComponent</a></code> | Test whether the given construct is a component. |

---

##### `isConstruct` <a name="isConstruct" id="projen.python.Venv.isConstruct"></a>

```typescript
import { python } from 'projen'

python.Venv.isConstruct(x: any)
```

Checks if `x` is a construct.

Use this method instead of `instanceof` to properly detect `Construct`
instances, even when the construct library is symlinked.

Explanation: in JavaScript, multiple copies of the `constructs` library on
disk are seen as independent, completely different libraries. As a
consequence, the class `Construct` in each copy of the `constructs` library
is seen as a different class, and an instance of one class will not test as
`instanceof` the other class. `npm install` will not create installations
like this, but users may manually symlink construct libraries together or
use a monorepo tool: in those cases, multiple copies of the `constructs`
library can be accidentally installed, and `instanceof` will behave
unpredictably. It is safest to avoid using `instanceof`, and using
this type-testing method instead.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.Venv.isConstruct.parameter.x"></a>

- *Type:* any

Any object.

---

##### `isComponent` <a name="isComponent" id="projen.python.Venv.isComponent"></a>

```typescript
import { python } from 'projen'

python.Venv.isComponent(x: any)
```

Test whether the given construct is a component.

###### `x`<sup>Required</sup> <a name="x" id="projen.python.Venv.isComponent.parameter.x"></a>

- *Type:* any

---

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Venv.property.node">node</a></code> | <code>constructs.Node</code> | The tree node. |
| <code><a href="#projen.python.Venv.property.project">project</a></code> | <code>projen.Project</code> | *No description.* |

---

##### `node`<sup>Required</sup> <a name="node" id="projen.python.Venv.property.node"></a>

```typescript
public readonly node: Node;
```

- *Type:* constructs.Node

The tree node.

---

##### `project`<sup>Required</sup> <a name="project" id="projen.python.Venv.property.project"></a>

```typescript
public readonly project: Project;
```

- *Type:* projen.Project

---


## Structs <a name="Structs" id="Structs"></a>

### AnalyzeOptions <a name="AnalyzeOptions" id="projen.python.AnalyzeOptions"></a>

Configures Ruff's `analyze` command.

#### Initializer <a name="Initializer" id="projen.python.AnalyzeOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const analyzeOptions: python.AnalyzeOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.AnalyzeOptions.property.detectStringImports">detectStringImports</a></code> | <code>boolean</code> | Whether to detect imports from string literals. |
| <code><a href="#projen.python.AnalyzeOptions.property.direction">direction</a></code> | <code>string</code> | Whether to generate a map from file to files that it depends on (dependencies) or files that depend on it (dependents). |
| <code><a href="#projen.python.AnalyzeOptions.property.exclude">exclude</a></code> | <code>string[]</code> | A list of file patterns to exclude from analysis in addition to the files excluded globally (see [`exclude`](#exclude), and [`extend-exclude`](#extend-exclude)). |
| <code><a href="#projen.python.AnalyzeOptions.property.includeDependencies">includeDependencies</a></code> | <code>{[ key: string ]: string[]}</code> | A map from file path to the list of Python or non-Python file paths or globs that should be considered dependencies of that file, regardless of whether relevant imports are detected. |
| <code><a href="#projen.python.AnalyzeOptions.property.preview">preview</a></code> | <code>boolean</code> | Whether to enable preview mode. |
| <code><a href="#projen.python.AnalyzeOptions.property.stringImportsMinDots">stringImportsMinDots</a></code> | <code>number</code> | The minimum number of dots in a string to consider it a valid import. |
| <code><a href="#projen.python.AnalyzeOptions.property.typeCheckingImports">typeCheckingImports</a></code> | <code>boolean</code> | Whether to include imports that are only used for type checking (i.e., imports within `if TYPE_CHECKING:` blocks). When enabled (default), type-checking-only imports are included in the import graph. When disabled, they are excluded. |

---

##### `detectStringImports`<sup>Optional</sup> <a name="detectStringImports" id="projen.python.AnalyzeOptions.property.detectStringImports"></a>

```typescript
public readonly detectStringImports: boolean;
```

- *Type:* boolean

Whether to detect imports from string literals.

When enabled, Ruff will search for string
literals that "look like" import paths, and include them in the import map, if they resolve
to valid Python modules.

---

##### `direction`<sup>Optional</sup> <a name="direction" id="projen.python.AnalyzeOptions.property.direction"></a>

```typescript
public readonly direction: string;
```

- *Type:* string

Whether to generate a map from file to files that it depends on (dependencies) or files that depend on it (dependents).

---

##### `exclude`<sup>Optional</sup> <a name="exclude" id="projen.python.AnalyzeOptions.property.exclude"></a>

```typescript
public readonly exclude: string[];
```

- *Type:* string[]

A list of file patterns to exclude from analysis in addition to the files excluded globally (see [`exclude`](#exclude), and [`extend-exclude`](#extend-exclude)).

Exclusions are based on globs, and can be either:

- Single-path patterns, like `.mypy_cache` (to exclude any directory
named `.mypy_cache` in the tree), `foo.py` (to exclude any file named
`foo.py`), or `foo_*.py` (to exclude any file matching `foo_*.py` ).
- Relative patterns, like `directory/foo.py` (to exclude that specific
file) or `directory/*.py` (to exclude any Python files in
`directory`). Note that these paths are relative to the project root
(e.g., the directory containing your `pyproject.toml`).

For more information on the glob syntax, refer to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).

---

##### `includeDependencies`<sup>Optional</sup> <a name="includeDependencies" id="projen.python.AnalyzeOptions.property.includeDependencies"></a>

```typescript
public readonly includeDependencies: {[ key: string ]: string[]};
```

- *Type:* {[ key: string ]: string[]}

A map from file path to the list of Python or non-Python file paths or globs that should be considered dependencies of that file, regardless of whether relevant imports are detected.

---

##### `preview`<sup>Optional</sup> <a name="preview" id="projen.python.AnalyzeOptions.property.preview"></a>

```typescript
public readonly preview: boolean;
```

- *Type:* boolean

Whether to enable preview mode.

When preview mode is enabled, Ruff will expose unstable
commands.

---

##### `stringImportsMinDots`<sup>Optional</sup> <a name="stringImportsMinDots" id="projen.python.AnalyzeOptions.property.stringImportsMinDots"></a>

```typescript
public readonly stringImportsMinDots: number;
```

- *Type:* number

The minimum number of dots in a string to consider it a valid import.

This setting is only relevant when [`detect-string-imports`](#detect-string-imports) is enabled.
For example, if this is set to `2`, then only strings with at least two dots (e.g., `"path.to.module"`)
would be considered valid imports.

---

##### `typeCheckingImports`<sup>Optional</sup> <a name="typeCheckingImports" id="projen.python.AnalyzeOptions.property.typeCheckingImports"></a>

```typescript
public readonly typeCheckingImports: boolean;
```

- *Type:* boolean

Whether to include imports that are only used for type checking (i.e., imports within `if TYPE_CHECKING:` blocks). When enabled (default), type-checking-only imports are included in the import graph. When disabled, they are excluded.

---

### ApiBan <a name="ApiBan" id="projen.python.ApiBan"></a>

#### Initializer <a name="Initializer" id="projen.python.ApiBan.Initializer"></a>

```typescript
import { python } from 'projen'

const apiBan: python.ApiBan = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.ApiBan.property.msg">msg</a></code> | <code>string</code> | The message to display when the API is used. |

---

##### `msg`<sup>Required</sup> <a name="msg" id="projen.python.ApiBan.property.msg"></a>

```typescript
public readonly msg: string;
```

- *Type:* string

The message to display when the API is used.

---

### BlackConfiguration <a name="BlackConfiguration" id="projen.python.BlackConfiguration"></a>

#### Initializer <a name="Initializer" id="projen.python.BlackConfiguration.Initializer"></a>

```typescript
import { python } from 'projen'

const blackConfiguration: python.BlackConfiguration = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.BlackConfiguration.property.check">check</a></code> | <code>boolean</code> | Don't write the files back, just return the status. |
| <code><a href="#projen.python.BlackConfiguration.property.code">code</a></code> | <code>string</code> | Format the code passed in as a string. |
| <code><a href="#projen.python.BlackConfiguration.property.color">color</a></code> | <code>boolean</code> | Show (or do not show) colored diff. |
| <code><a href="#projen.python.BlackConfiguration.property.diff">diff</a></code> | <code>boolean</code> | Don't write the files back, just output a diff to indicate what changes Black would've made. |
| <code><a href="#projen.python.BlackConfiguration.property.enableUnstableFeature">enableUnstableFeature</a></code> | <code>string[]</code> | Enable specific features included in the `--unstable` style. |
| <code><a href="#projen.python.BlackConfiguration.property.exclude">exclude</a></code> | <code>string</code> | A regular expression that matches files and directories that should be excluded on recursive searches. |
| <code><a href="#projen.python.BlackConfiguration.property.extendExclude">extendExclude</a></code> | <code>string</code> | Like --exclude, but adds additional files and directories on top of the default values instead of overriding them. |
| <code><a href="#projen.python.BlackConfiguration.property.fast">fast</a></code> | <code>boolean</code> | By default, Black performs an AST safety check after formatting your code. |
| <code><a href="#projen.python.BlackConfiguration.property.forceExclude">forceExclude</a></code> | <code>string</code> | Like --exclude, but files and directories matching this regex will be excluded even when they are passed explicitly as arguments. |
| <code><a href="#projen.python.BlackConfiguration.property.include">include</a></code> | <code>string</code> | A regular expression that matches files and directories that should be included on recursive searches. |
| <code><a href="#projen.python.BlackConfiguration.property.ipynb">ipynb</a></code> | <code>boolean</code> | Format all input files like Jupyter Notebooks regardless of file extension. |
| <code><a href="#projen.python.BlackConfiguration.property.lineLength">lineLength</a></code> | <code>number</code> | How many characters per line to allow. |
| <code><a href="#projen.python.BlackConfiguration.property.preview">preview</a></code> | <code>boolean</code> | Enable potentially disruptive style changes that may be added to Black's main functionality in the next major release. |
| <code><a href="#projen.python.BlackConfiguration.property.pyi">pyi</a></code> | <code>boolean</code> | Format all input files like typing stubs regardless of file extension. |
| <code><a href="#projen.python.BlackConfiguration.property.pythonCellMagics">pythonCellMagics</a></code> | <code>string[]</code> | When processing Jupyter Notebooks, add the given magic to the list of known python-magics (capture, prun, pypy, python, python3, time, timeit). |
| <code><a href="#projen.python.BlackConfiguration.property.quiet">quiet</a></code> | <code>boolean</code> | Stop emitting all non-critical output. |
| <code><a href="#projen.python.BlackConfiguration.property.requiredVersion">requiredVersion</a></code> | <code>string</code> | Require a specific version of Black to be running. |
| <code><a href="#projen.python.BlackConfiguration.property.skipMagicTrailingComma">skipMagicTrailingComma</a></code> | <code>boolean</code> | Don't use trailing commas as a reason to split lines. |
| <code><a href="#projen.python.BlackConfiguration.property.skipSourceFirstLine">skipSourceFirstLine</a></code> | <code>boolean</code> | Skip the first line of the source code. |
| <code><a href="#projen.python.BlackConfiguration.property.skipStringNormalization">skipStringNormalization</a></code> | <code>boolean</code> | Don't normalize string quotes or prefixes. |
| <code><a href="#projen.python.BlackConfiguration.property.targetVersion">targetVersion</a></code> | <code><a href="#projen.python.BlackConfigurationTargetVersion">BlackConfigurationTargetVersion</a>[]</code> | Python versions that should be supported by Black's output. |
| <code><a href="#projen.python.BlackConfiguration.property.unstable">unstable</a></code> | <code>boolean</code> | Enable potentially disruptive style changes that have known bugs or are not currently expected to make it into the stable style Black's next major release. |
| <code><a href="#projen.python.BlackConfiguration.property.verbose">verbose</a></code> | <code>boolean</code> | Emit messages about files that were not changed or were ignored due to exclusion patterns. |
| <code><a href="#projen.python.BlackConfiguration.property.workers">workers</a></code> | <code>number</code> | When Black formats multiple files, it may use a process pool to speed up formatting. |

---

##### `check`<sup>Optional</sup> <a name="check" id="projen.python.BlackConfiguration.property.check"></a>

```typescript
public readonly check: boolean;
```

- *Type:* boolean

Don't write the files back, just return the status.

Return code 0 means nothing would change. Return code 1 means some files would be reformatted. Return code 123 means there was an internal error.

---

##### `code`<sup>Optional</sup> <a name="code" id="projen.python.BlackConfiguration.property.code"></a>

```typescript
public readonly code: string;
```

- *Type:* string

Format the code passed in as a string.

---

##### `color`<sup>Optional</sup> <a name="color" id="projen.python.BlackConfiguration.property.color"></a>

```typescript
public readonly color: boolean;
```

- *Type:* boolean

Show (or do not show) colored diff.

Only applies when --diff is given.

---

##### `diff`<sup>Optional</sup> <a name="diff" id="projen.python.BlackConfiguration.property.diff"></a>

```typescript
public readonly diff: boolean;
```

- *Type:* boolean

Don't write the files back, just output a diff to indicate what changes Black would've made.

They are printed to stdout so capturing them is simple.

---

##### `enableUnstableFeature`<sup>Optional</sup> <a name="enableUnstableFeature" id="projen.python.BlackConfiguration.property.enableUnstableFeature"></a>

```typescript
public readonly enableUnstableFeature: string[];
```

- *Type:* string[]

Enable specific features included in the `--unstable` style.

Requires `--preview`. No compatibility guarantees are provided on the behavior or existence of any unstable features.

---

##### `exclude`<sup>Optional</sup> <a name="exclude" id="projen.python.BlackConfiguration.property.exclude"></a>

```typescript
public readonly exclude: string;
```

- *Type:* string

A regular expression that matches files and directories that should be excluded on recursive searches.

An empty value means no paths are excluded. Use forward slashes for directories on all platforms (Windows, too). By default, Black also ignores all paths listed in .gitignore. Changing this value will override all default exclusions. [default: /(\.direnv|\.eggs|\.git|\.hg|\.ipynb_checkpoints|\.mypy_cache|\.nox|\.pytest_cache|\.ruff_cache|\.tox|\.svn|\.venv|\.vscode|__pypackages__|_build|buck-out|build|dist|venv)/]

---

##### `extendExclude`<sup>Optional</sup> <a name="extendExclude" id="projen.python.BlackConfiguration.property.extendExclude"></a>

```typescript
public readonly extendExclude: string;
```

- *Type:* string

Like --exclude, but adds additional files and directories on top of the default values instead of overriding them.

---

##### `fast`<sup>Optional</sup> <a name="fast" id="projen.python.BlackConfiguration.property.fast"></a>

```typescript
public readonly fast: boolean;
```

- *Type:* boolean

By default, Black performs an AST safety check after formatting your code.

The --fast flag turns off this check and the --safe flag explicitly enables it. [default: --safe]

---

##### `forceExclude`<sup>Optional</sup> <a name="forceExclude" id="projen.python.BlackConfiguration.property.forceExclude"></a>

```typescript
public readonly forceExclude: string;
```

- *Type:* string

Like --exclude, but files and directories matching this regex will be excluded even when they are passed explicitly as arguments.

This is useful when invoking Black programmatically on changed files, such as in a pre-commit hook or editor plugin.

---

##### `include`<sup>Optional</sup> <a name="include" id="projen.python.BlackConfiguration.property.include"></a>

```typescript
public readonly include: string;
```

- *Type:* string

A regular expression that matches files and directories that should be included on recursive searches.

An empty value means all files are included regardless of the name. Use forward slashes for directories on all platforms (Windows, too). Overrides all exclusions, including from .gitignore and command line options.

---

##### `ipynb`<sup>Optional</sup> <a name="ipynb" id="projen.python.BlackConfiguration.property.ipynb"></a>

```typescript
public readonly ipynb: boolean;
```

- *Type:* boolean

Format all input files like Jupyter Notebooks regardless of file extension.

This is useful when piping source on standard input.

---

##### `lineLength`<sup>Optional</sup> <a name="lineLength" id="projen.python.BlackConfiguration.property.lineLength"></a>

```typescript
public readonly lineLength: number;
```

- *Type:* number

How many characters per line to allow.

---

##### `preview`<sup>Optional</sup> <a name="preview" id="projen.python.BlackConfiguration.property.preview"></a>

```typescript
public readonly preview: boolean;
```

- *Type:* boolean

Enable potentially disruptive style changes that may be added to Black's main functionality in the next major release.

---

##### `pyi`<sup>Optional</sup> <a name="pyi" id="projen.python.BlackConfiguration.property.pyi"></a>

```typescript
public readonly pyi: boolean;
```

- *Type:* boolean

Format all input files like typing stubs regardless of file extension.

This is useful when piping source on standard input.

---

##### `pythonCellMagics`<sup>Optional</sup> <a name="pythonCellMagics" id="projen.python.BlackConfiguration.property.pythonCellMagics"></a>

```typescript
public readonly pythonCellMagics: string[];
```

- *Type:* string[]

When processing Jupyter Notebooks, add the given magic to the list of known python-magics (capture, prun, pypy, python, python3, time, timeit).

Useful for formatting cells with custom python magics.

---

##### `quiet`<sup>Optional</sup> <a name="quiet" id="projen.python.BlackConfiguration.property.quiet"></a>

```typescript
public readonly quiet: boolean;
```

- *Type:* boolean

Stop emitting all non-critical output.

Error messages will still be emitted (which can silenced by 2>/dev/null).

---

##### `requiredVersion`<sup>Optional</sup> <a name="requiredVersion" id="projen.python.BlackConfiguration.property.requiredVersion"></a>

```typescript
public readonly requiredVersion: string;
```

- *Type:* string

Require a specific version of Black to be running.

This is useful for ensuring that all contributors to your project are using the same version, because different versions of Black may format code a little differently. This option can be set in a configuration file for consistent results across environments.

---

##### `skipMagicTrailingComma`<sup>Optional</sup> <a name="skipMagicTrailingComma" id="projen.python.BlackConfiguration.property.skipMagicTrailingComma"></a>

```typescript
public readonly skipMagicTrailingComma: boolean;
```

- *Type:* boolean

Don't use trailing commas as a reason to split lines.

---

##### `skipSourceFirstLine`<sup>Optional</sup> <a name="skipSourceFirstLine" id="projen.python.BlackConfiguration.property.skipSourceFirstLine"></a>

```typescript
public readonly skipSourceFirstLine: boolean;
```

- *Type:* boolean

Skip the first line of the source code.

---

##### `skipStringNormalization`<sup>Optional</sup> <a name="skipStringNormalization" id="projen.python.BlackConfiguration.property.skipStringNormalization"></a>

```typescript
public readonly skipStringNormalization: boolean;
```

- *Type:* boolean

Don't normalize string quotes or prefixes.

---

##### `targetVersion`<sup>Optional</sup> <a name="targetVersion" id="projen.python.BlackConfiguration.property.targetVersion"></a>

```typescript
public readonly targetVersion: BlackConfigurationTargetVersion[];
```

- *Type:* <a href="#projen.python.BlackConfigurationTargetVersion">BlackConfigurationTargetVersion</a>[]

Python versions that should be supported by Black's output.

You should include all versions that your code supports. By default, Black will infer target versions from the project metadata in pyproject.toml. If this does not yield conclusive results, Black will use per-file auto-detection.

---

##### `unstable`<sup>Optional</sup> <a name="unstable" id="projen.python.BlackConfiguration.property.unstable"></a>

```typescript
public readonly unstable: boolean;
```

- *Type:* boolean

Enable potentially disruptive style changes that have known bugs or are not currently expected to make it into the stable style Black's next major release.

Implies --preview.

---

##### `verbose`<sup>Optional</sup> <a name="verbose" id="projen.python.BlackConfiguration.property.verbose"></a>

```typescript
public readonly verbose: boolean;
```

- *Type:* boolean

Emit messages about files that were not changed or were ignored due to exclusion patterns.

If Black is using a configuration file, a message detailing which one it is using will be emitted.

---

##### `workers`<sup>Optional</sup> <a name="workers" id="projen.python.BlackConfiguration.property.workers"></a>

```typescript
public readonly workers: number;
```

- *Type:* number
- *Default:* the number of CPUs in the system.

When Black formats multiple files, it may use a process pool to speed up formatting.

This option controls the number of parallel workers. This can also be specified via the BLACK_NUM_WORKERS environment variable. Defaults to the number of CPUs in the system.

---

### Build <a name="Build" id="projen.python.Build"></a>

Build configuration.

#### Initializer <a name="Initializer" id="projen.python.Build.Initializer"></a>

```typescript
import { python } from 'projen'

const build: python.Build = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Build.property.artifacts">artifacts</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.Build.property.devModeDirs">devModeDirs</a></code> | <code>string[]</code> | List of directories to add to PYTHONPATH in development mode. |
| <code><a href="#projen.python.Build.property.devModeExact">devModeExact</a></code> | <code>boolean</code> | Whether to use an exact dev mode that doesn’t add whole directories to PYTHONPATH. |
| <code><a href="#projen.python.Build.property.directory">directory</a></code> | <code>string</code> | Directory to write build artifacts to. |
| <code><a href="#projen.python.Build.property.exclude">exclude</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.Build.property.hooks">hooks</a></code> | <code>{[ key: string ]: <a href="#projen.python.Hook">Hook</a>}</code> | *No description.* |
| <code><a href="#projen.python.Build.property.ignoreVcs">ignoreVcs</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.Build.property.include">include</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.Build.property.onlyPackages">onlyPackages</a></code> | <code>boolean</code> | Whether to only include non-artifact files in packages. |
| <code><a href="#projen.python.Build.property.reproducible">reproducible</a></code> | <code>boolean</code> | Whether to make the build reproducible. |
| <code><a href="#projen.python.Build.property.skipExcludedDirs">skipExcludedDirs</a></code> | <code>boolean</code> | Whether to skip excluded directories (for performance reasons). |
| <code><a href="#projen.python.Build.property.sources">sources</a></code> | <code>any</code> | Rewrite relative paths. |
| <code><a href="#projen.python.Build.property.targets">targets</a></code> | <code>{[ key: string ]: <a href="#projen.python.Target">Target</a>}</code> | *No description.* |

---

##### `artifacts`<sup>Optional</sup> <a name="artifacts" id="projen.python.Build.property.artifacts"></a>

```typescript
public readonly artifacts: string[];
```

- *Type:* string[]

---

##### `devModeDirs`<sup>Optional</sup> <a name="devModeDirs" id="projen.python.Build.property.devModeDirs"></a>

```typescript
public readonly devModeDirs: string[];
```

- *Type:* string[]

List of directories to add to PYTHONPATH in development mode.

---

##### `devModeExact`<sup>Optional</sup> <a name="devModeExact" id="projen.python.Build.property.devModeExact"></a>

```typescript
public readonly devModeExact: boolean;
```

- *Type:* boolean

Whether to use an exact dev mode that doesn’t add whole directories to PYTHONPATH.

---

##### `directory`<sup>Optional</sup> <a name="directory" id="projen.python.Build.property.directory"></a>

```typescript
public readonly directory: string;
```

- *Type:* string

Directory to write build artifacts to.

---

##### `exclude`<sup>Optional</sup> <a name="exclude" id="projen.python.Build.property.exclude"></a>

```typescript
public readonly exclude: string[];
```

- *Type:* string[]

---

##### `hooks`<sup>Optional</sup> <a name="hooks" id="projen.python.Build.property.hooks"></a>

```typescript
public readonly hooks: {[ key: string ]: Hook};
```

- *Type:* {[ key: string ]: <a href="#projen.python.Hook">Hook</a>}

---

##### `ignoreVcs`<sup>Optional</sup> <a name="ignoreVcs" id="projen.python.Build.property.ignoreVcs"></a>

```typescript
public readonly ignoreVcs: boolean;
```

- *Type:* boolean

---

##### `include`<sup>Optional</sup> <a name="include" id="projen.python.Build.property.include"></a>

```typescript
public readonly include: string[];
```

- *Type:* string[]

---

##### `onlyPackages`<sup>Optional</sup> <a name="onlyPackages" id="projen.python.Build.property.onlyPackages"></a>

```typescript
public readonly onlyPackages: boolean;
```

- *Type:* boolean

Whether to only include non-artifact files in packages.

---

##### `reproducible`<sup>Optional</sup> <a name="reproducible" id="projen.python.Build.property.reproducible"></a>

```typescript
public readonly reproducible: boolean;
```

- *Type:* boolean

Whether to make the build reproducible.

---

##### `skipExcludedDirs`<sup>Optional</sup> <a name="skipExcludedDirs" id="projen.python.Build.property.skipExcludedDirs"></a>

```typescript
public readonly skipExcludedDirs: boolean;
```

- *Type:* boolean

Whether to skip excluded directories (for performance reasons).

---

##### `sources`<sup>Optional</sup> <a name="sources" id="projen.python.Build.property.sources"></a>

```typescript
public readonly sources: any;
```

- *Type:* any

Rewrite relative paths.

---

##### `targets`<sup>Optional</sup> <a name="targets" id="projen.python.Build.property.targets"></a>

```typescript
public readonly targets: {[ key: string ]: Target};
```

- *Type:* {[ key: string ]: <a href="#projen.python.Target">Target</a>}

---

### BuildBackendSettings <a name="BuildBackendSettings" id="projen.python.BuildBackendSettings"></a>

Settings for the uv build backend (`uv_build`).

Note that those settings only apply when using the `uv_build` backend, other build backends
(such as hatchling) have their own configuration.

All options that accept globs use the portable glob patterns from
[PEP 639](https://packaging.python.org/en/latest/specifications/glob-patterns/).

#### Initializer <a name="Initializer" id="projen.python.BuildBackendSettings.Initializer"></a>

```typescript
import { python } from 'projen'

const buildBackendSettings: python.BuildBackendSettings = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.BuildBackendSettings.property.data">data</a></code> | <code><a href="#projen.python.WheelDataIncludes">WheelDataIncludes</a></code> | Data includes for wheels. |
| <code><a href="#projen.python.BuildBackendSettings.property.defaultExcludes">defaultExcludes</a></code> | <code>boolean</code> | If set to `false`, the default excludes aren't applied. |
| <code><a href="#projen.python.BuildBackendSettings.property.moduleName">moduleName</a></code> | <code>any</code> | The name of the module directory inside `module-root`. |
| <code><a href="#projen.python.BuildBackendSettings.property.moduleRoot">moduleRoot</a></code> | <code>string</code> | The directory that contains the module directory. |
| <code><a href="#projen.python.BuildBackendSettings.property.namespace">namespace</a></code> | <code>boolean</code> | Build a namespace package. |
| <code><a href="#projen.python.BuildBackendSettings.property.sourceExclude">sourceExclude</a></code> | <code>string[]</code> | Glob expressions which files and directories to exclude from the source distribution. |
| <code><a href="#projen.python.BuildBackendSettings.property.sourceInclude">sourceInclude</a></code> | <code>string[]</code> | Glob expressions which files and directories to additionally include in the source distribution. |
| <code><a href="#projen.python.BuildBackendSettings.property.wheelExclude">wheelExclude</a></code> | <code>string[]</code> | Glob expressions which files and directories to exclude from the wheel. |

---

##### `data`<sup>Optional</sup> <a name="data" id="projen.python.BuildBackendSettings.property.data"></a>

```typescript
public readonly data: WheelDataIncludes;
```

- *Type:* <a href="#projen.python.WheelDataIncludes">WheelDataIncludes</a>

Data includes for wheels.

Each entry is a directory, whose contents are copied to the matching directory in the wheel
in `<name>-<version>.data/(purelib|platlib|headers|scripts|data)`. Upon installation, this
data is moved to its target location, as defined by
<https://docs.python.org/3.12/library/sysconfig.html#installation-paths>. Usually, small
data files are included by placing them in the Python module instead of using data includes.

- `scripts`: Installed to the directory for executables, `<venv>/bin` on Unix or
`<venv>\Scripts` on Windows. This directory is added to `PATH` when the virtual
environment  is activated or when using `uv run`, so this data type can be used to install
additional binaries. Consider using `project.scripts` instead for Python entrypoints.
- `data`: Installed over the virtualenv environment root.

Warning: This may override existing files!

- `headers`: Installed to the include directory. Compilers building Python packages
with this package as build requirement use the include directory to find additional header
files.
- `purelib` and `platlib`: Installed to the `site-packages` directory. It is not recommended
to use these two options.

---

##### `defaultExcludes`<sup>Optional</sup> <a name="defaultExcludes" id="projen.python.BuildBackendSettings.property.defaultExcludes"></a>

```typescript
public readonly defaultExcludes: boolean;
```

- *Type:* boolean

If set to `false`, the default excludes aren't applied.

Default excludes: `__pycache__`, `*.pyc`, and `*.pyo`.

---

##### `moduleName`<sup>Optional</sup> <a name="moduleName" id="projen.python.BuildBackendSettings.property.moduleName"></a>

```typescript
public readonly moduleName: any;
```

- *Type:* any

The name of the module directory inside `module-root`.

The default module name is the package name with dots and dashes replaced by underscores.

Package names need to be valid Python identifiers, and the directory needs to contain a
`__init__.py`. An exception are stubs packages, whose name ends with `-stubs`, with the stem
being the module name, and which contain a `__init__.pyi` file.

For namespace packages with a single module, the path can be dotted, e.g., `foo.bar` or
`foo-stubs.bar`.

For namespace packages with multiple modules, the path can be a list, e.g.,
`["foo", "bar"]`. We recommend using a single module per package, splitting multiple
packages into a workspace.

Note that using this option runs the risk of creating two packages with different names but
the same module names. Installing such packages together leads to unspecified behavior,
often with corrupted files or directory trees.

---

##### `moduleRoot`<sup>Optional</sup> <a name="moduleRoot" id="projen.python.BuildBackendSettings.property.moduleRoot"></a>

```typescript
public readonly moduleRoot: string;
```

- *Type:* string

The directory that contains the module directory.

Common values are `src` (src layout, the default) or an empty path (flat layout).

---

##### `namespace`<sup>Optional</sup> <a name="namespace" id="projen.python.BuildBackendSettings.property.namespace"></a>

```typescript
public readonly namespace: boolean;
```

- *Type:* boolean

Build a namespace package.

Build a PEP 420 implicit namespace package, allowing more than one root `__init__.py`.

Use this option when the namespace package contains multiple root `__init__.py`, for
namespace packages with a single root `__init__.py` use a dotted `module-name` instead.

To compare dotted `module-name` and `namespace = true`, the first example below can be
expressed with `module-name = "cloud.database"`: There is one root `__init__.py` `database`.
In the second example, we have three roots (`cloud.database`, `cloud.database_pro`,
`billing.modules.database_pro`), so `namespace = true` is required.

```text
src
└── cloud
└── database
├── __init__.py
├── query_builder
│   └── __init__.py
└── sql
├── parser.py
└── __init__.py
```

```text
src
├── cloud
│   ├── database
│   │   ├── __init__.py
│   │   ├── query_builder
│   │   │   └── __init__.py
│   │   └── sql
│   │       ├── __init__.py
│   │       └── parser.py
│   └── database_pro
│       ├── __init__.py
│       └── query_builder.py
└── billing
└── modules
└── database_pro
├── __init__.py
└── sql.py
```

---

##### `sourceExclude`<sup>Optional</sup> <a name="sourceExclude" id="projen.python.BuildBackendSettings.property.sourceExclude"></a>

```typescript
public readonly sourceExclude: string[];
```

- *Type:* string[]

Glob expressions which files and directories to exclude from the source distribution.

---

##### `sourceInclude`<sup>Optional</sup> <a name="sourceInclude" id="projen.python.BuildBackendSettings.property.sourceInclude"></a>

```typescript
public readonly sourceInclude: string[];
```

- *Type:* string[]

Glob expressions which files and directories to additionally include in the source distribution.

`pyproject.toml` and the contents of the module directory are always included.

---

##### `wheelExclude`<sup>Optional</sup> <a name="wheelExclude" id="projen.python.BuildBackendSettings.property.wheelExclude"></a>

```typescript
public readonly wheelExclude: string[];
```

- *Type:* string[]

Glob expressions which files and directories to exclude from the wheel.

---

### BuildSystem <a name="BuildSystem" id="projen.python.BuildSystem"></a>

Declares any Python level dependencies that must be installed in order to run the project’s build system successfully.

#### Initializer <a name="Initializer" id="projen.python.BuildSystem.Initializer"></a>

```typescript
import { python } from 'projen'

const buildSystem: python.BuildSystem = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.BuildSystem.property.requires">requires</a></code> | <code>string[]</code> | List of strings following the version specifier specification, representing dependencies required to execute the build system. |
| <code><a href="#projen.python.BuildSystem.property.backendPath">backendPath</a></code> | <code>string[]</code> | list of directories to prepend to `sys.path` when loading the build backend, relative to project root. |
| <code><a href="#projen.python.BuildSystem.property.buildBackend">buildBackend</a></code> | <code>string</code> | String is formatted following the same `module:object` syntax as a `setuptools` entry point. |

---

##### `requires`<sup>Required</sup> <a name="requires" id="projen.python.BuildSystem.property.requires"></a>

```typescript
public readonly requires: string[];
```

- *Type:* string[]

List of strings following the version specifier specification, representing dependencies required to execute the build system.

---

##### `backendPath`<sup>Optional</sup> <a name="backendPath" id="projen.python.BuildSystem.property.backendPath"></a>

```typescript
public readonly backendPath: string[];
```

- *Type:* string[]

list of directories to prepend to `sys.path` when loading the build backend, relative to project root.

---

##### `buildBackend`<sup>Optional</sup> <a name="buildBackend" id="projen.python.BuildSystem.property.buildBackend"></a>

```typescript
public readonly buildBackend: string;
```

- *Type:* string

String is formatted following the same `module:object` syntax as a `setuptools` entry point.

It’s also legal to leave out the `:object` part.

---

### CollectorEnv <a name="CollectorEnv" id="projen.python.CollectorEnv"></a>

Environment collector plugin configuration.

#### Initializer <a name="Initializer" id="projen.python.CollectorEnv.Initializer"></a>

```typescript
import { python } from 'projen'

const collectorEnv: python.CollectorEnv = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.CollectorEnv.property.collectors">collectors</a></code> | <code>{[ key: string ]: any}</code> | Custom environment collectors. |
| <code><a href="#projen.python.CollectorEnv.property.requires">requires</a></code> | <code>string[]</code> | Required environment collectors for automatic management. |

---

##### `collectors`<sup>Optional</sup> <a name="collectors" id="projen.python.CollectorEnv.property.collectors"></a>

```typescript
public readonly collectors: {[ key: string ]: any};
```

- *Type:* {[ key: string ]: any}

Custom environment collectors.

---

##### `requires`<sup>Optional</sup> <a name="requires" id="projen.python.CollectorEnv.property.requires"></a>

```typescript
public readonly requires: string[];
```

- *Type:* string[]

Required environment collectors for automatic management.

---

### DependencyGroupSettings <a name="DependencyGroupSettings" id="projen.python.DependencyGroupSettings"></a>

#### Initializer <a name="Initializer" id="projen.python.DependencyGroupSettings.Initializer"></a>

```typescript
import { python } from 'projen'

const dependencyGroupSettings: python.DependencyGroupSettings = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.DependencyGroupSettings.property.requiresPython">requiresPython</a></code> | <code>string</code> | Version of python to require when installing this group. |

---

##### `requiresPython`<sup>Optional</sup> <a name="requiresPython" id="projen.python.DependencyGroupSettings.property.requiresPython"></a>

```typescript
public readonly requiresPython: string;
```

- *Type:* string

Version of python to require when installing this group.

---

### Env <a name="Env" id="projen.python.Env"></a>

#### Initializer <a name="Initializer" id="projen.python.Env.Initializer"></a>

```typescript
import { python } from 'projen'

const env: python.Env = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Env.property.dependencies">dependencies</a></code> | <code>string[]</code> | List of dependencies to install in the environment. |
| <code><a href="#projen.python.Env.property.description">description</a></code> | <code>string</code> | Description of the environment for the `env show` command. |
| <code><a href="#projen.python.Env.property.detached">detached</a></code> | <code>boolean</code> | Make the environment self-referential and skip project install. |
| <code><a href="#projen.python.Env.property.devMode">devMode</a></code> | <code>boolean</code> | Whether to install the project in development mode. |
| <code><a href="#projen.python.Env.property.envExclude">envExclude</a></code> | <code>string[]</code> | Glob patterns of environment variables to exclude. |
| <code><a href="#projen.python.Env.property.envInclude">envInclude</a></code> | <code>string[]</code> | Glob patterns of environment variables to include. |
| <code><a href="#projen.python.Env.property.envVars">envVars</a></code> | <code>{[ key: string ]: string}</code> | Environment variables to set. |
| <code><a href="#projen.python.Env.property.extraDependencies">extraDependencies</a></code> | <code>string[]</code> | List of extra dependencies to install in the environment in addition to the template environment’s dependencies. |
| <code><a href="#projen.python.Env.property.features">features</a></code> | <code>string[]</code> | List of optional dependency groups (extras) to install. |
| <code><a href="#projen.python.Env.property.matrix">matrix</a></code> | <code>{[ key: string ]: string[]}[]</code> | Matrix of environments. |
| <code><a href="#projen.python.Env.property.matrixNameFormat">matrixNameFormat</a></code> | <code>string</code> | Format string for matrix names, supporting {variable} and {value} placeholders. |
| <code><a href="#projen.python.Env.property.overrides">overrides</a></code> | <code><a href="#projen.python.Overrides">Overrides</a></code> | *No description.* |
| <code><a href="#projen.python.Env.property.platforms">platforms</a></code> | <code><a href="#projen.python.Platform">Platform</a>[]</code> | List of platforms to build for. |
| <code><a href="#projen.python.Env.property.postInstallCommands">postInstallCommands</a></code> | <code>string[]</code> | List of commands to run after installing the project. |
| <code><a href="#projen.python.Env.property.preInstallCommands">preInstallCommands</a></code> | <code>string[]</code> | List of commands to run before installing the project. |
| <code><a href="#projen.python.Env.property.python">python</a></code> | <code>string</code> | Python version to use or an absolute path to a python interpreter. |
| <code><a href="#projen.python.Env.property.requires">requires</a></code> | <code>string[]</code> | Required environment plugins. |
| <code><a href="#projen.python.Env.property.scripts">scripts</a></code> | <code>{[ key: string ]: string[]}</code> | Dictionary of scripts to run. |
| <code><a href="#projen.python.Env.property.skipInstall">skipInstall</a></code> | <code>boolean</code> | Whether to skip installing the project. |
| <code><a href="#projen.python.Env.property.template">template</a></code> | <code>string</code> | Template environment to inherit from. |
| <code><a href="#projen.python.Env.property.type">type</a></code> | <code>string</code> | Type of environment. |

---

##### `dependencies`<sup>Optional</sup> <a name="dependencies" id="projen.python.Env.property.dependencies"></a>

```typescript
public readonly dependencies: string[];
```

- *Type:* string[]

List of dependencies to install in the environment.

---

##### `description`<sup>Optional</sup> <a name="description" id="projen.python.Env.property.description"></a>

```typescript
public readonly description: string;
```

- *Type:* string

Description of the environment for the `env show` command.

---

##### `detached`<sup>Optional</sup> <a name="detached" id="projen.python.Env.property.detached"></a>

```typescript
public readonly detached: boolean;
```

- *Type:* boolean

Make the environment self-referential and skip project install.

---

##### `devMode`<sup>Optional</sup> <a name="devMode" id="projen.python.Env.property.devMode"></a>

```typescript
public readonly devMode: boolean;
```

- *Type:* boolean

Whether to install the project in development mode.

---

##### `envExclude`<sup>Optional</sup> <a name="envExclude" id="projen.python.Env.property.envExclude"></a>

```typescript
public readonly envExclude: string[];
```

- *Type:* string[]

Glob patterns of environment variables to exclude.

---

##### `envInclude`<sup>Optional</sup> <a name="envInclude" id="projen.python.Env.property.envInclude"></a>

```typescript
public readonly envInclude: string[];
```

- *Type:* string[]

Glob patterns of environment variables to include.

---

##### `envVars`<sup>Optional</sup> <a name="envVars" id="projen.python.Env.property.envVars"></a>

```typescript
public readonly envVars: {[ key: string ]: string};
```

- *Type:* {[ key: string ]: string}

Environment variables to set.

---

##### `extraDependencies`<sup>Optional</sup> <a name="extraDependencies" id="projen.python.Env.property.extraDependencies"></a>

```typescript
public readonly extraDependencies: string[];
```

- *Type:* string[]

List of extra dependencies to install in the environment in addition to the template environment’s dependencies.

---

##### `features`<sup>Optional</sup> <a name="features" id="projen.python.Env.property.features"></a>

```typescript
public readonly features: string[];
```

- *Type:* string[]

List of optional dependency groups (extras) to install.

---

##### `matrix`<sup>Optional</sup> <a name="matrix" id="projen.python.Env.property.matrix"></a>

```typescript
public readonly matrix: {[ key: string ]: string[]}[];
```

- *Type:* {[ key: string ]: string[]}[]

Matrix of environments.

---

##### `matrixNameFormat`<sup>Optional</sup> <a name="matrixNameFormat" id="projen.python.Env.property.matrixNameFormat"></a>

```typescript
public readonly matrixNameFormat: string;
```

- *Type:* string

Format string for matrix names, supporting {variable} and {value} placeholders.

---

##### `overrides`<sup>Optional</sup> <a name="overrides" id="projen.python.Env.property.overrides"></a>

```typescript
public readonly overrides: Overrides;
```

- *Type:* <a href="#projen.python.Overrides">Overrides</a>

---

##### `platforms`<sup>Optional</sup> <a name="platforms" id="projen.python.Env.property.platforms"></a>

```typescript
public readonly platforms: Platform[];
```

- *Type:* <a href="#projen.python.Platform">Platform</a>[]

List of platforms to build for.

---

##### `postInstallCommands`<sup>Optional</sup> <a name="postInstallCommands" id="projen.python.Env.property.postInstallCommands"></a>

```typescript
public readonly postInstallCommands: string[];
```

- *Type:* string[]

List of commands to run after installing the project.

---

##### `preInstallCommands`<sup>Optional</sup> <a name="preInstallCommands" id="projen.python.Env.property.preInstallCommands"></a>

```typescript
public readonly preInstallCommands: string[];
```

- *Type:* string[]

List of commands to run before installing the project.

---

##### `python`<sup>Optional</sup> <a name="python" id="projen.python.Env.property.python"></a>

```typescript
public readonly python: string;
```

- *Type:* string

Python version to use or an absolute path to a python interpreter.

---

##### `requires`<sup>Optional</sup> <a name="requires" id="projen.python.Env.property.requires"></a>

```typescript
public readonly requires: string[];
```

- *Type:* string[]

Required environment plugins.

---

##### `scripts`<sup>Optional</sup> <a name="scripts" id="projen.python.Env.property.scripts"></a>

```typescript
public readonly scripts: {[ key: string ]: string[]};
```

- *Type:* {[ key: string ]: string[]}

Dictionary of scripts to run.

---

##### `skipInstall`<sup>Optional</sup> <a name="skipInstall" id="projen.python.Env.property.skipInstall"></a>

```typescript
public readonly skipInstall: boolean;
```

- *Type:* boolean

Whether to skip installing the project.

---

##### `template`<sup>Optional</sup> <a name="template" id="projen.python.Env.property.template"></a>

```typescript
public readonly template: string;
```

- *Type:* string

Template environment to inherit from.

Set to own name to make self referential (disable inheritance from “default”)

---

##### `type`<sup>Optional</sup> <a name="type" id="projen.python.Env.property.type"></a>

```typescript
public readonly type: string;
```

- *Type:* string

Type of environment.

Without plugins, the only supported type is "virtual"

---

### EnvironmentOptions <a name="EnvironmentOptions" id="projen.python.EnvironmentOptions"></a>

#### Initializer <a name="Initializer" id="projen.python.EnvironmentOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const environmentOptions: python.EnvironmentOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.EnvironmentOptions.property.extraPaths">extraPaths</a></code> | <code>string[]</code> | User-provided paths that should take first priority in module resolution. |
| <code><a href="#projen.python.EnvironmentOptions.property.python">python</a></code> | <code>string</code> | Path to your project's Python environment or interpreter. |
| <code><a href="#projen.python.EnvironmentOptions.property.pythonPlatform">pythonPlatform</a></code> | <code>any</code> | Specifies the target platform that will be used to analyze the source code. |
| <code><a href="#projen.python.EnvironmentOptions.property.pythonVersion">pythonVersion</a></code> | <code>any</code> | Specifies the version of Python that will be used to analyze the source code. |
| <code><a href="#projen.python.EnvironmentOptions.property.root">root</a></code> | <code>string[]</code> | The root paths of the project, used for finding first-party modules. |
| <code><a href="#projen.python.EnvironmentOptions.property.typeshed">typeshed</a></code> | <code>string</code> | Optional path to a "typeshed" directory on disk for us to use for standard-library types. |

---

##### `extraPaths`<sup>Optional</sup> <a name="extraPaths" id="projen.python.EnvironmentOptions.property.extraPaths"></a>

```typescript
public readonly extraPaths: string[];
```

- *Type:* string[]

User-provided paths that should take first priority in module resolution.

This is an advanced option that should usually only be used for first-party or third-party
modules that are not installed into your Python environment in a conventional way.
Use the `python` option to specify the location of your Python environment.

This option is similar to mypy's `MYPYPATH` environment variable and pyright's `stubPath`
configuration setting.

---

##### `python`<sup>Optional</sup> <a name="python" id="projen.python.EnvironmentOptions.property.python"></a>

```typescript
public readonly python: string;
```

- *Type:* string

Path to your project's Python environment or interpreter.

ty uses the `site-packages` directory of your project's Python environment
to resolve third-party (and, in some cases, first-party) imports in your code.

If you're using a project management tool such as uv, you should not generally need
to specify this option, as commands such as `uv run` will set the `VIRTUAL_ENV`
environment variable to point to your project's virtual environment. ty can also infer
the location of your environment from an activated Conda environment, and will look for
a `.venv` directory in the project root if none of the above apply.

Passing a path to a Python executable is supported, but passing a path to a dynamic executable
(such as a shim) is not currently supported.

This option can be used to point to virtual or system Python environments.

---

##### `pythonPlatform`<sup>Optional</sup> <a name="pythonPlatform" id="projen.python.EnvironmentOptions.property.pythonPlatform"></a>

```typescript
public readonly pythonPlatform: any;
```

- *Type:* any

Specifies the target platform that will be used to analyze the source code.

If specified, ty will understand conditions based on comparisons with `sys.platform`, such
as are commonly found in typeshed to reflect the differing contents of the standard library across platforms.
If `all` is specified, ty will assume that the source code can run on any platform.

If no platform is specified, ty will use the current platform:
- `win32` for Windows
- `darwin` for macOS
- `android` for Android
- `ios` for iOS
- `linux` for everything else

---

##### `pythonVersion`<sup>Optional</sup> <a name="pythonVersion" id="projen.python.EnvironmentOptions.property.pythonVersion"></a>

```typescript
public readonly pythonVersion: any;
```

- *Type:* any

Specifies the version of Python that will be used to analyze the source code.

The version should be specified as a string in the format `M.m` where `M` is the major version
and `m` is the minor (e.g. `"3.0"` or `"3.6"`).
If a version is provided, ty will generate errors if the source code makes use of language features
that are not supported in that version.

If a version is not specified, ty will try the following techniques in order of preference
to determine a value:
1. Check for the `project.requires-python` setting in a `pyproject.toml` file
and use the minimum version from the specified range
2. Check for an activated or configured Python environment
and attempt to infer the Python version of that environment
3. Fall back to the default value (see below)

For some language features, ty can also understand conditionals based on comparisons
with `sys.version_info`. These are commonly found in typeshed, for example,
to reflect the differing contents of the standard library across Python versions.

---

##### `root`<sup>Optional</sup> <a name="root" id="projen.python.EnvironmentOptions.property.root"></a>

```typescript
public readonly root: string[];
```

- *Type:* string[]

The root paths of the project, used for finding first-party modules.

Accepts a list of directory paths searched in priority order (first has highest priority).

If left unspecified, ty will try to detect common project layouts and initialize `root` accordingly:

* if a `./src` directory exists, include `.` and `./src` in the first party search path (src layout or flat)
* if a `./<project-name>/<project-name>` directory exists, include `.` and `./<project-name>` in the first party search path
* otherwise, default to `.` (flat layout)

Besides, if a `./python` or `./tests` directory exists and is not a package (i.e. it does not contain an `__init__.py` or `__init__.pyi` file),
it will also be included in the first party search path.

---

##### `typeshed`<sup>Optional</sup> <a name="typeshed" id="projen.python.EnvironmentOptions.property.typeshed"></a>

```typescript
public readonly typeshed: string;
```

- *Type:* string

Optional path to a "typeshed" directory on disk for us to use for standard-library types.

If this is not provided, we will fallback to our vendored typeshed stubs for the stdlib,
bundled as a zip file in the binary

---

### FileDirective <a name="FileDirective" id="projen.python.FileDirective"></a>

Value is read from a file (or list of files and then concatenated).

#### Initializer <a name="Initializer" id="projen.python.FileDirective.Initializer"></a>

```typescript
import { python } from 'projen'

const fileDirective: python.FileDirective = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.FileDirective.property.file">file</a></code> | <code>string[]</code> | *No description.* |

---

##### `file`<sup>Required</sup> <a name="file" id="projen.python.FileDirective.property.file"></a>

```typescript
public readonly file: string[];
```

- *Type:* string[]

---

### Flake8AnnotationsOptions <a name="Flake8AnnotationsOptions" id="projen.python.Flake8AnnotationsOptions"></a>

Options for the `flake8-annotations` plugin.

#### Initializer <a name="Initializer" id="projen.python.Flake8AnnotationsOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const flake8AnnotationsOptions: python.Flake8AnnotationsOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Flake8AnnotationsOptions.property.allowStarArgAny">allowStarArgAny</a></code> | <code>boolean</code> | Whether to suppress `ANN401` for dynamically typed `*args` and `**kwargs` arguments. |
| <code><a href="#projen.python.Flake8AnnotationsOptions.property.ignoreFullyUntyped">ignoreFullyUntyped</a></code> | <code>boolean</code> | Whether to suppress `ANN*` rules for any declaration that hasn't been typed at all. |
| <code><a href="#projen.python.Flake8AnnotationsOptions.property.mypyInitReturn">mypyInitReturn</a></code> | <code>boolean</code> | Whether to allow the omission of a return type hint for `__init__` if at least one argument is annotated. |
| <code><a href="#projen.python.Flake8AnnotationsOptions.property.suppressDummyArgs">suppressDummyArgs</a></code> | <code>boolean</code> | Whether to suppress `ANN000`-level violations for arguments matching the "dummy" variable regex (like `_`). |
| <code><a href="#projen.python.Flake8AnnotationsOptions.property.suppressNoneReturning">suppressNoneReturning</a></code> | <code>boolean</code> | Whether to suppress `ANN200`-level violations for functions that meet either of the following criteria:. |

---

##### `allowStarArgAny`<sup>Optional</sup> <a name="allowStarArgAny" id="projen.python.Flake8AnnotationsOptions.property.allowStarArgAny"></a>

```typescript
public readonly allowStarArgAny: boolean;
```

- *Type:* boolean

Whether to suppress `ANN401` for dynamically typed `*args` and `**kwargs` arguments.

---

##### `ignoreFullyUntyped`<sup>Optional</sup> <a name="ignoreFullyUntyped" id="projen.python.Flake8AnnotationsOptions.property.ignoreFullyUntyped"></a>

```typescript
public readonly ignoreFullyUntyped: boolean;
```

- *Type:* boolean

Whether to suppress `ANN*` rules for any declaration that hasn't been typed at all.

This makes it easier to gradually add types to a codebase.

---

##### `mypyInitReturn`<sup>Optional</sup> <a name="mypyInitReturn" id="projen.python.Flake8AnnotationsOptions.property.mypyInitReturn"></a>

```typescript
public readonly mypyInitReturn: boolean;
```

- *Type:* boolean

Whether to allow the omission of a return type hint for `__init__` if at least one argument is annotated.

---

##### `suppressDummyArgs`<sup>Optional</sup> <a name="suppressDummyArgs" id="projen.python.Flake8AnnotationsOptions.property.suppressDummyArgs"></a>

```typescript
public readonly suppressDummyArgs: boolean;
```

- *Type:* boolean

Whether to suppress `ANN000`-level violations for arguments matching the "dummy" variable regex (like `_`).

---

##### `suppressNoneReturning`<sup>Optional</sup> <a name="suppressNoneReturning" id="projen.python.Flake8AnnotationsOptions.property.suppressNoneReturning"></a>

```typescript
public readonly suppressNoneReturning: boolean;
```

- *Type:* boolean

Whether to suppress `ANN200`-level violations for functions that meet either of the following criteria:.

Contain no `return` statement.
- Explicit `return` statement(s) all return `None` (explicitly or
implicitly).

---

### Flake8BanditOptions <a name="Flake8BanditOptions" id="projen.python.Flake8BanditOptions"></a>

Options for the `flake8-bandit` plugin.

#### Initializer <a name="Initializer" id="projen.python.Flake8BanditOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const flake8BanditOptions: python.Flake8BanditOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Flake8BanditOptions.property.allowedMarkupCalls">allowedMarkupCalls</a></code> | <code>string[]</code> | A list of callable names, whose result may be safely passed into [`markupsafe.Markup`](https://markupsafe.palletsprojects.com/en/stable/escaping/#markupsafe.Markup). |
| <code><a href="#projen.python.Flake8BanditOptions.property.checkTypedException">checkTypedException</a></code> | <code>boolean</code> | Whether to disallow `try`-`except`-`pass` (`S110`) for specific exception types. |
| <code><a href="#projen.python.Flake8BanditOptions.property.extendMarkupNames">extendMarkupNames</a></code> | <code>string[]</code> | A list of additional callable names that behave like [`markupsafe.Markup`](https://markupsafe.palletsprojects.com/en/stable/escaping/#markupsafe.Markup). |
| <code><a href="#projen.python.Flake8BanditOptions.property.hardcodedTmpDirectory">hardcodedTmpDirectory</a></code> | <code>string[]</code> | A list of directories to consider temporary (see `S108`). |
| <code><a href="#projen.python.Flake8BanditOptions.property.hardcodedTmpDirectoryExtend">hardcodedTmpDirectoryExtend</a></code> | <code>string[]</code> | A list of directories to consider temporary, in addition to those specified by [`hardcoded-tmp-directory`](#lint_flake8-bandit_hardcoded-tmp-directory) (see `S108`). |

---

##### `allowedMarkupCalls`<sup>Optional</sup> <a name="allowedMarkupCalls" id="projen.python.Flake8BanditOptions.property.allowedMarkupCalls"></a>

```typescript
public readonly allowedMarkupCalls: string[];
```

- *Type:* string[]

A list of callable names, whose result may be safely passed into [`markupsafe.Markup`](https://markupsafe.palletsprojects.com/en/stable/escaping/#markupsafe.Markup).

Expects to receive a list of fully-qualified names (e.g., `bleach.clean`, rather than `clean`).

This setting helps you avoid false positives in code like:

```python
from bleach import clean
from markupsafe import Markup

cleaned_markup = Markup(clean(some_user_input))
```

Where the use of [`bleach.clean`](https://bleach.readthedocs.io/en/latest/clean.html)
usually ensures that there's no XSS vulnerability.

Although it is not recommended, you may also use this setting to whitelist other
kinds of calls, e.g. calls to i18n translation functions, where how safe that is
will depend on the implementation and how well the translations are audited.

Another common use-case is to wrap the output of functions that generate markup
like [`xml.etree.ElementTree.tostring`](https://docs.python.org/3/library/xml.etree.elementtree.html#xml.etree.ElementTree.tostring)
or template rendering engines where sanitization of potential user input is either
already baked in or has to happen before rendering.

---

##### `checkTypedException`<sup>Optional</sup> <a name="checkTypedException" id="projen.python.Flake8BanditOptions.property.checkTypedException"></a>

```typescript
public readonly checkTypedException: boolean;
```

- *Type:* boolean

Whether to disallow `try`-`except`-`pass` (`S110`) for specific exception types.

By default, `try`-`except`-`pass` is only
disallowed for `Exception` and `BaseException`.

---

##### `extendMarkupNames`<sup>Optional</sup> <a name="extendMarkupNames" id="projen.python.Flake8BanditOptions.property.extendMarkupNames"></a>

```typescript
public readonly extendMarkupNames: string[];
```

- *Type:* string[]

A list of additional callable names that behave like [`markupsafe.Markup`](https://markupsafe.palletsprojects.com/en/stable/escaping/#markupsafe.Markup).

Expects to receive a list of fully-qualified names (e.g., `webhelpers.html.literal`, rather than
`literal`).

---

##### `hardcodedTmpDirectory`<sup>Optional</sup> <a name="hardcodedTmpDirectory" id="projen.python.Flake8BanditOptions.property.hardcodedTmpDirectory"></a>

```typescript
public readonly hardcodedTmpDirectory: string[];
```

- *Type:* string[]

A list of directories to consider temporary (see `S108`).

---

##### `hardcodedTmpDirectoryExtend`<sup>Optional</sup> <a name="hardcodedTmpDirectoryExtend" id="projen.python.Flake8BanditOptions.property.hardcodedTmpDirectoryExtend"></a>

```typescript
public readonly hardcodedTmpDirectoryExtend: string[];
```

- *Type:* string[]

A list of directories to consider temporary, in addition to those specified by [`hardcoded-tmp-directory`](#lint_flake8-bandit_hardcoded-tmp-directory) (see `S108`).

---

### Flake8BooleanTrapOptions <a name="Flake8BooleanTrapOptions" id="projen.python.Flake8BooleanTrapOptions"></a>

Options for the `flake8-boolean-trap` plugin.

#### Initializer <a name="Initializer" id="projen.python.Flake8BooleanTrapOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const flake8BooleanTrapOptions: python.Flake8BooleanTrapOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Flake8BooleanTrapOptions.property.extendAllowedCalls">extendAllowedCalls</a></code> | <code>string[]</code> | Additional callable functions with which to allow boolean traps. |

---

##### `extendAllowedCalls`<sup>Optional</sup> <a name="extendAllowedCalls" id="projen.python.Flake8BooleanTrapOptions.property.extendAllowedCalls"></a>

```typescript
public readonly extendAllowedCalls: string[];
```

- *Type:* string[]

Additional callable functions with which to allow boolean traps.

Expects to receive a list of fully-qualified names (e.g., `pydantic.Field`, rather than
`Field`).

---

### Flake8BugbearOptions <a name="Flake8BugbearOptions" id="projen.python.Flake8BugbearOptions"></a>

Options for the `flake8-bugbear` plugin.

#### Initializer <a name="Initializer" id="projen.python.Flake8BugbearOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const flake8BugbearOptions: python.Flake8BugbearOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Flake8BugbearOptions.property.extendImmutableCalls">extendImmutableCalls</a></code> | <code>string[]</code> | Additional callable functions to consider "immutable" when evaluating, e.g., the `function-call-in-default-argument` rule (`B008`) or `function-call-in-dataclass-defaults` rule (`RUF009`). |

---

##### `extendImmutableCalls`<sup>Optional</sup> <a name="extendImmutableCalls" id="projen.python.Flake8BugbearOptions.property.extendImmutableCalls"></a>

```typescript
public readonly extendImmutableCalls: string[];
```

- *Type:* string[]

Additional callable functions to consider "immutable" when evaluating, e.g., the `function-call-in-default-argument` rule (`B008`) or `function-call-in-dataclass-defaults` rule (`RUF009`).

Expects to receive a list of fully-qualified names (e.g., `fastapi.Query`, rather than
`Query`).

---

### Flake8BuiltinsOptions <a name="Flake8BuiltinsOptions" id="projen.python.Flake8BuiltinsOptions"></a>

Options for the `flake8-builtins` plugin.

#### Initializer <a name="Initializer" id="projen.python.Flake8BuiltinsOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const flake8BuiltinsOptions: python.Flake8BuiltinsOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Flake8BuiltinsOptions.property.allowedModules">allowedModules</a></code> | <code>string[]</code> | List of builtin module names to allow. |
| <code><a href="#projen.python.Flake8BuiltinsOptions.property.builtinsAllowedModules">builtinsAllowedModules</a></code> | <code>string[]</code> | DEPRECATED: This option has been renamed to `allowed-modules`. Use `allowed-modules` instead. |
| <code><a href="#projen.python.Flake8BuiltinsOptions.property.builtinsIgnorelist">builtinsIgnorelist</a></code> | <code>string[]</code> | DEPRECATED: This option has been renamed to `ignorelist`. Use `ignorelist` instead. |
| <code><a href="#projen.python.Flake8BuiltinsOptions.property.builtinsStrictChecking">builtinsStrictChecking</a></code> | <code>boolean</code> | DEPRECATED: This option has been renamed to `strict-checking`. Use `strict-checking` instead. |
| <code><a href="#projen.python.Flake8BuiltinsOptions.property.ignorelist">ignorelist</a></code> | <code>string[]</code> | Ignore list of builtins. |
| <code><a href="#projen.python.Flake8BuiltinsOptions.property.strictChecking">strictChecking</a></code> | <code>boolean</code> | Compare module names instead of full module paths. |

---

##### `allowedModules`<sup>Optional</sup> <a name="allowedModules" id="projen.python.Flake8BuiltinsOptions.property.allowedModules"></a>

```typescript
public readonly allowedModules: string[];
```

- *Type:* string[]

List of builtin module names to allow.

---

##### `builtinsAllowedModules`<sup>Optional</sup> <a name="builtinsAllowedModules" id="projen.python.Flake8BuiltinsOptions.property.builtinsAllowedModules"></a>

```typescript
public readonly builtinsAllowedModules: string[];
```

- *Type:* string[]

DEPRECATED: This option has been renamed to `allowed-modules`. Use `allowed-modules` instead.

List of builtin module names to allow.

This option is ignored if both `allowed-modules` and `builtins-allowed-modules` are set.

---

##### `builtinsIgnorelist`<sup>Optional</sup> <a name="builtinsIgnorelist" id="projen.python.Flake8BuiltinsOptions.property.builtinsIgnorelist"></a>

```typescript
public readonly builtinsIgnorelist: string[];
```

- *Type:* string[]

DEPRECATED: This option has been renamed to `ignorelist`. Use `ignorelist` instead.

Ignore list of builtins.

This option is ignored if both `ignorelist` and `builtins-ignorelist` are set.

---

##### `builtinsStrictChecking`<sup>Optional</sup> <a name="builtinsStrictChecking" id="projen.python.Flake8BuiltinsOptions.property.builtinsStrictChecking"></a>

```typescript
public readonly builtinsStrictChecking: boolean;
```

- *Type:* boolean

DEPRECATED: This option has been renamed to `strict-checking`. Use `strict-checking` instead.

Compare module names instead of full module paths.

This option is ignored if both `strict-checking` and `builtins-strict-checking` are set.

---

##### `ignorelist`<sup>Optional</sup> <a name="ignorelist" id="projen.python.Flake8BuiltinsOptions.property.ignorelist"></a>

```typescript
public readonly ignorelist: string[];
```

- *Type:* string[]

Ignore list of builtins.

---

##### `strictChecking`<sup>Optional</sup> <a name="strictChecking" id="projen.python.Flake8BuiltinsOptions.property.strictChecking"></a>

```typescript
public readonly strictChecking: boolean;
```

- *Type:* boolean

Compare module names instead of full module paths.

Used by [`A005` - `stdlib-module-shadowing`](https://docs.astral.sh/ruff/rules/stdlib-module-shadowing/).

---

### Flake8ComprehensionsOptions <a name="Flake8ComprehensionsOptions" id="projen.python.Flake8ComprehensionsOptions"></a>

Options for the `flake8-comprehensions` plugin.

#### Initializer <a name="Initializer" id="projen.python.Flake8ComprehensionsOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const flake8ComprehensionsOptions: python.Flake8ComprehensionsOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Flake8ComprehensionsOptions.property.allowDictCallsWithKeywordArguments">allowDictCallsWithKeywordArguments</a></code> | <code>boolean</code> | Allow `dict` calls that make use of keyword arguments (e.g., `dict(a=1, b=2)`). |

---

##### `allowDictCallsWithKeywordArguments`<sup>Optional</sup> <a name="allowDictCallsWithKeywordArguments" id="projen.python.Flake8ComprehensionsOptions.property.allowDictCallsWithKeywordArguments"></a>

```typescript
public readonly allowDictCallsWithKeywordArguments: boolean;
```

- *Type:* boolean

Allow `dict` calls that make use of keyword arguments (e.g., `dict(a=1, b=2)`).

---

### Flake8CopyrightOptions <a name="Flake8CopyrightOptions" id="projen.python.Flake8CopyrightOptions"></a>

Options for the `flake8-copyright` plugin.

#### Initializer <a name="Initializer" id="projen.python.Flake8CopyrightOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const flake8CopyrightOptions: python.Flake8CopyrightOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Flake8CopyrightOptions.property.author">author</a></code> | <code>string</code> | Author to enforce within the copyright notice. |
| <code><a href="#projen.python.Flake8CopyrightOptions.property.minFileSize">minFileSize</a></code> | <code>number</code> | A minimum file size (in bytes) required for a copyright notice to be enforced. |
| <code><a href="#projen.python.Flake8CopyrightOptions.property.noticeRgx">noticeRgx</a></code> | <code>string</code> | The regular expression used to match the copyright notice, compiled with the [`regex`](https://docs.rs/regex/latest/regex/) crate. Defaults to `(?i)Copyright\s+((?:\(C\)\|©)\s+)?\d{4}((-\|,\s)\d{4})*`, which matches the following:. |

---

##### `author`<sup>Optional</sup> <a name="author" id="projen.python.Flake8CopyrightOptions.property.author"></a>

```typescript
public readonly author: string;
```

- *Type:* string

Author to enforce within the copyright notice.

If provided, the
author must be present immediately following the copyright notice.

---

##### `minFileSize`<sup>Optional</sup> <a name="minFileSize" id="projen.python.Flake8CopyrightOptions.property.minFileSize"></a>

```typescript
public readonly minFileSize: number;
```

- *Type:* number

A minimum file size (in bytes) required for a copyright notice to be enforced.

By default, all files are validated.

---

##### `noticeRgx`<sup>Optional</sup> <a name="noticeRgx" id="projen.python.Flake8CopyrightOptions.property.noticeRgx"></a>

```typescript
public readonly noticeRgx: string;
```

- *Type:* string
- *Default:* i)Copyright\s+((?:\(C\)|©)\s+)?\d{4}((-|,\s)\d{4})*`, which matches

The regular expression used to match the copyright notice, compiled with the [`regex`](https://docs.rs/regex/latest/regex/) crate. Defaults to `(?i)Copyright\s+((?:\(C\)|©)\s+)?\d{4}((-|,\s)\d{4})*`, which matches the following:.

`Copyright 2023`
- `Copyright (C) 2023`
- `Copyright 2021-2023`
- `Copyright (C) 2021-2023`
- `Copyright (C) 2021, 2023`

---

### Flake8ErrMsgOptions <a name="Flake8ErrMsgOptions" id="projen.python.Flake8ErrMsgOptions"></a>

Options for the `flake8-errmsg` plugin.

#### Initializer <a name="Initializer" id="projen.python.Flake8ErrMsgOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const flake8ErrMsgOptions: python.Flake8ErrMsgOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Flake8ErrMsgOptions.property.maxStringLength">maxStringLength</a></code> | <code>number</code> | Maximum string length for string literals in exception messages. |

---

##### `maxStringLength`<sup>Optional</sup> <a name="maxStringLength" id="projen.python.Flake8ErrMsgOptions.property.maxStringLength"></a>

```typescript
public readonly maxStringLength: number;
```

- *Type:* number

Maximum string length for string literals in exception messages.

---

### Flake8GetTextOptions <a name="Flake8GetTextOptions" id="projen.python.Flake8GetTextOptions"></a>

Options for the `flake8-gettext` plugin.

#### Initializer <a name="Initializer" id="projen.python.Flake8GetTextOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const flake8GetTextOptions: python.Flake8GetTextOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Flake8GetTextOptions.property.extendFunctionNames">extendFunctionNames</a></code> | <code>string[]</code> | Additional function names to consider as internationalization calls, in addition to those included in [`function-names`](#lint_flake8-gettext_function-names). |
| <code><a href="#projen.python.Flake8GetTextOptions.property.functionNames">functionNames</a></code> | <code>string[]</code> | The function names to consider as internationalization calls. |

---

##### `extendFunctionNames`<sup>Optional</sup> <a name="extendFunctionNames" id="projen.python.Flake8GetTextOptions.property.extendFunctionNames"></a>

```typescript
public readonly extendFunctionNames: string[];
```

- *Type:* string[]

Additional function names to consider as internationalization calls, in addition to those included in [`function-names`](#lint_flake8-gettext_function-names).

---

##### `functionNames`<sup>Optional</sup> <a name="functionNames" id="projen.python.Flake8GetTextOptions.property.functionNames"></a>

```typescript
public readonly functionNames: string[];
```

- *Type:* string[]

The function names to consider as internationalization calls.

---

### Flake8ImplicitStrConcatOptions <a name="Flake8ImplicitStrConcatOptions" id="projen.python.Flake8ImplicitStrConcatOptions"></a>

Options for the `flake8-implicit-str-concat` plugin.

#### Initializer <a name="Initializer" id="projen.python.Flake8ImplicitStrConcatOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const flake8ImplicitStrConcatOptions: python.Flake8ImplicitStrConcatOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Flake8ImplicitStrConcatOptions.property.allowMultiline">allowMultiline</a></code> | <code>boolean</code> | Whether to allow implicit string concatenations for multiline strings. |

---

##### `allowMultiline`<sup>Optional</sup> <a name="allowMultiline" id="projen.python.Flake8ImplicitStrConcatOptions.property.allowMultiline"></a>

```typescript
public readonly allowMultiline: boolean;
```

- *Type:* boolean

Whether to allow implicit string concatenations for multiline strings.

By default, implicit concatenations of multiline strings are
allowed (but continuation lines, delimited with a backslash, are
prohibited).

Setting `allow-multiline = false` will automatically disable the
`explicit-string-concatenation` (`ISC003`) rule. Otherwise, both
implicit and explicit multiline string concatenations would be seen
as violations, making it impossible to write a linter-compliant multiline
string.

---

### Flake8ImportConventionsOptions <a name="Flake8ImportConventionsOptions" id="projen.python.Flake8ImportConventionsOptions"></a>

Options for the `flake8-import-conventions` plugin.

#### Initializer <a name="Initializer" id="projen.python.Flake8ImportConventionsOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const flake8ImportConventionsOptions: python.Flake8ImportConventionsOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Flake8ImportConventionsOptions.property.aliases">aliases</a></code> | <code>{[ key: string ]: string}</code> | The conventional aliases for imports. |
| <code><a href="#projen.python.Flake8ImportConventionsOptions.property.bannedAliases">bannedAliases</a></code> | <code>{[ key: string ]: string[]}</code> | A mapping from module to its banned import aliases. |
| <code><a href="#projen.python.Flake8ImportConventionsOptions.property.bannedFrom">bannedFrom</a></code> | <code>string[]</code> | A list of modules that should not be imported from using the `from ... import ...` syntax. |
| <code><a href="#projen.python.Flake8ImportConventionsOptions.property.extendAliases">extendAliases</a></code> | <code>{[ key: string ]: string}</code> | A mapping from module to conventional import alias. |

---

##### `aliases`<sup>Optional</sup> <a name="aliases" id="projen.python.Flake8ImportConventionsOptions.property.aliases"></a>

```typescript
public readonly aliases: {[ key: string ]: string};
```

- *Type:* {[ key: string ]: string}

The conventional aliases for imports.

These aliases can be extended by
the [`extend-aliases`](#lint_flake8-import-conventions_extend-aliases) option.

---

##### `bannedAliases`<sup>Optional</sup> <a name="bannedAliases" id="projen.python.Flake8ImportConventionsOptions.property.bannedAliases"></a>

```typescript
public readonly bannedAliases: {[ key: string ]: string[]};
```

- *Type:* {[ key: string ]: string[]}

A mapping from module to its banned import aliases.

---

##### `bannedFrom`<sup>Optional</sup> <a name="bannedFrom" id="projen.python.Flake8ImportConventionsOptions.property.bannedFrom"></a>

```typescript
public readonly bannedFrom: string[];
```

- *Type:* string[]

A list of modules that should not be imported from using the `from ... import ...` syntax.

For example, given `banned-from = ["pandas"]`, `from pandas import DataFrame`
would be disallowed, while `import pandas` would be allowed.

---

##### `extendAliases`<sup>Optional</sup> <a name="extendAliases" id="projen.python.Flake8ImportConventionsOptions.property.extendAliases"></a>

```typescript
public readonly extendAliases: {[ key: string ]: string};
```

- *Type:* {[ key: string ]: string}

A mapping from module to conventional import alias.

These aliases will
be added to the [`aliases`](#lint_flake8-import-conventions_aliases) mapping.

---

### Flake8PytestStyleOptions <a name="Flake8PytestStyleOptions" id="projen.python.Flake8PytestStyleOptions"></a>

Options for the `flake8-pytest-style` plugin.

#### Initializer <a name="Initializer" id="projen.python.Flake8PytestStyleOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const flake8PytestStyleOptions: python.Flake8PytestStyleOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Flake8PytestStyleOptions.property.fixtureParentheses">fixtureParentheses</a></code> | <code>boolean</code> | Boolean flag specifying whether `@pytest.fixture()` without parameters should have parentheses. If the option is set to `false` (the default), `@pytest.fixture` is valid and `@pytest.fixture()` is invalid. If set to `true`, `@pytest.fixture()` is valid and `@pytest.fixture` is invalid. |
| <code><a href="#projen.python.Flake8PytestStyleOptions.property.markParentheses">markParentheses</a></code> | <code>boolean</code> | Boolean flag specifying whether `@pytest.mark.foo()` without parameters should have parentheses. If the option is set to `false` (the default), `@pytest.mark.foo` is valid and `@pytest.mark.foo()` is invalid. If set to `true`, `@pytest.mark.foo()` is valid and `@pytest.mark.foo` is invalid. |
| <code><a href="#projen.python.Flake8PytestStyleOptions.property.parametrizeNamesType">parametrizeNamesType</a></code> | <code><a href="#projen.python.ParametrizeNameType">ParametrizeNameType</a></code> | Expected type for multiple argument names in `@pytest.mark.parametrize`. The following values are supported:. |
| <code><a href="#projen.python.Flake8PytestStyleOptions.property.parametrizeValuesRowType">parametrizeValuesRowType</a></code> | <code><a href="#projen.python.ParametrizeValuesRowType">ParametrizeValuesRowType</a></code> | Expected type for each row of values in `@pytest.mark.parametrize` in case of multiple parameters. The following values are supported:. |
| <code><a href="#projen.python.Flake8PytestStyleOptions.property.parametrizeValuesType">parametrizeValuesType</a></code> | <code><a href="#projen.python.ParametrizeValuesType">ParametrizeValuesType</a></code> | Expected type for the list of values rows in `@pytest.mark.parametrize`. The following values are supported:. |
| <code><a href="#projen.python.Flake8PytestStyleOptions.property.raisesExtendRequireMatchFor">raisesExtendRequireMatchFor</a></code> | <code>string[]</code> | List of additional exception names that require a match= parameter in a `pytest.raises()` call. This extends the default list of exceptions that require a match= parameter. This option is useful if you want to extend the default list of exceptions that require a match= parameter without having to specify the entire list. Note that this option does not remove any exceptions from the default list. |
| <code><a href="#projen.python.Flake8PytestStyleOptions.property.raisesRequireMatchFor">raisesRequireMatchFor</a></code> | <code>string[]</code> | List of exception names that require a match= parameter in a `pytest.raises()` call. |
| <code><a href="#projen.python.Flake8PytestStyleOptions.property.warnsExtendRequireMatchFor">warnsExtendRequireMatchFor</a></code> | <code>string[]</code> | List of additional warning names that require a match= parameter in a `pytest.warns()` call. This extends the default list of warnings that require a match= parameter. |
| <code><a href="#projen.python.Flake8PytestStyleOptions.property.warnsRequireMatchFor">warnsRequireMatchFor</a></code> | <code>string[]</code> | List of warning names that require a match= parameter in a `pytest.warns()` call. |

---

##### `fixtureParentheses`<sup>Optional</sup> <a name="fixtureParentheses" id="projen.python.Flake8PytestStyleOptions.property.fixtureParentheses"></a>

```typescript
public readonly fixtureParentheses: boolean;
```

- *Type:* boolean

Boolean flag specifying whether `@pytest.fixture()` without parameters should have parentheses. If the option is set to `false` (the default), `@pytest.fixture` is valid and `@pytest.fixture()` is invalid. If set to `true`, `@pytest.fixture()` is valid and `@pytest.fixture` is invalid.

---

##### `markParentheses`<sup>Optional</sup> <a name="markParentheses" id="projen.python.Flake8PytestStyleOptions.property.markParentheses"></a>

```typescript
public readonly markParentheses: boolean;
```

- *Type:* boolean

Boolean flag specifying whether `@pytest.mark.foo()` without parameters should have parentheses. If the option is set to `false` (the default), `@pytest.mark.foo` is valid and `@pytest.mark.foo()` is invalid. If set to `true`, `@pytest.mark.foo()` is valid and `@pytest.mark.foo` is invalid.

---

##### `parametrizeNamesType`<sup>Optional</sup> <a name="parametrizeNamesType" id="projen.python.Flake8PytestStyleOptions.property.parametrizeNamesType"></a>

```typescript
public readonly parametrizeNamesType: ParametrizeNameType;
```

- *Type:* <a href="#projen.python.ParametrizeNameType">ParametrizeNameType</a>

Expected type for multiple argument names in `@pytest.mark.parametrize`. The following values are supported:.

`csv` — a comma-separated list, e.g.
`@pytest.mark.parametrize("name1,name2", ...)`
- `tuple` (default) — e.g.
`@pytest.mark.parametrize(("name1", "name2"), ...)`
- `list` — e.g. `@pytest.mark.parametrize(["name1", "name2"], ...)`

---

##### `parametrizeValuesRowType`<sup>Optional</sup> <a name="parametrizeValuesRowType" id="projen.python.Flake8PytestStyleOptions.property.parametrizeValuesRowType"></a>

```typescript
public readonly parametrizeValuesRowType: ParametrizeValuesRowType;
```

- *Type:* <a href="#projen.python.ParametrizeValuesRowType">ParametrizeValuesRowType</a>

Expected type for each row of values in `@pytest.mark.parametrize` in case of multiple parameters. The following values are supported:.

`tuple` (default) — e.g.
`@pytest.mark.parametrize(("name1", "name2"), [(1, 2), (3, 4)])`
- `list` — e.g.
`@pytest.mark.parametrize(("name1", "name2"), [[1, 2], [3, 4]])`

---

##### `parametrizeValuesType`<sup>Optional</sup> <a name="parametrizeValuesType" id="projen.python.Flake8PytestStyleOptions.property.parametrizeValuesType"></a>

```typescript
public readonly parametrizeValuesType: ParametrizeValuesType;
```

- *Type:* <a href="#projen.python.ParametrizeValuesType">ParametrizeValuesType</a>

Expected type for the list of values rows in `@pytest.mark.parametrize`. The following values are supported:.

`tuple` — e.g. `@pytest.mark.parametrize("name", (1, 2, 3))`
- `list` (default) — e.g. `@pytest.mark.parametrize("name", [1, 2, 3])`

---

##### `raisesExtendRequireMatchFor`<sup>Optional</sup> <a name="raisesExtendRequireMatchFor" id="projen.python.Flake8PytestStyleOptions.property.raisesExtendRequireMatchFor"></a>

```typescript
public readonly raisesExtendRequireMatchFor: string[];
```

- *Type:* string[]

List of additional exception names that require a match= parameter in a `pytest.raises()` call. This extends the default list of exceptions that require a match= parameter. This option is useful if you want to extend the default list of exceptions that require a match= parameter without having to specify the entire list. Note that this option does not remove any exceptions from the default list.

Supports glob patterns. For more information on the glob syntax, refer
to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).

---

##### `raisesRequireMatchFor`<sup>Optional</sup> <a name="raisesRequireMatchFor" id="projen.python.Flake8PytestStyleOptions.property.raisesRequireMatchFor"></a>

```typescript
public readonly raisesRequireMatchFor: string[];
```

- *Type:* string[]

List of exception names that require a match= parameter in a `pytest.raises()` call.

Supports glob patterns. For more information on the glob syntax, refer
to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).

---

##### `warnsExtendRequireMatchFor`<sup>Optional</sup> <a name="warnsExtendRequireMatchFor" id="projen.python.Flake8PytestStyleOptions.property.warnsExtendRequireMatchFor"></a>

```typescript
public readonly warnsExtendRequireMatchFor: string[];
```

- *Type:* string[]

List of additional warning names that require a match= parameter in a `pytest.warns()` call. This extends the default list of warnings that require a match= parameter.

This option is useful if you want to extend the default list of warnings
that require a match= parameter without having to specify the entire
list.

Note that this option does not remove any warnings from the default
list.

Supports glob patterns. For more information on the glob syntax, refer
to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).

---

##### `warnsRequireMatchFor`<sup>Optional</sup> <a name="warnsRequireMatchFor" id="projen.python.Flake8PytestStyleOptions.property.warnsRequireMatchFor"></a>

```typescript
public readonly warnsRequireMatchFor: string[];
```

- *Type:* string[]

List of warning names that require a match= parameter in a `pytest.warns()` call.

Supports glob patterns. For more information on the glob syntax, refer
to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).

---

### Flake8QuotesOptions <a name="Flake8QuotesOptions" id="projen.python.Flake8QuotesOptions"></a>

Options for the `flake8-quotes` plugin.

#### Initializer <a name="Initializer" id="projen.python.Flake8QuotesOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const flake8QuotesOptions: python.Flake8QuotesOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Flake8QuotesOptions.property.avoidEscape">avoidEscape</a></code> | <code>boolean</code> | Whether to avoid using single quotes if a string contains single quotes, or vice-versa with double quotes, as per [PEP 8](https://peps.python.org/pep-0008/#string-quotes). This minimizes the need to escape quotation marks within strings. |
| <code><a href="#projen.python.Flake8QuotesOptions.property.docstringQuotes">docstringQuotes</a></code> | <code>string</code> | Quote style to prefer for docstrings (either "single" or "double"). |
| <code><a href="#projen.python.Flake8QuotesOptions.property.inlineQuotes">inlineQuotes</a></code> | <code>string</code> | Quote style to prefer for inline strings (either "single" or "double"). |
| <code><a href="#projen.python.Flake8QuotesOptions.property.multilineQuotes">multilineQuotes</a></code> | <code>string</code> | Quote style to prefer for multiline strings (either "single" or "double"). |

---

##### `avoidEscape`<sup>Optional</sup> <a name="avoidEscape" id="projen.python.Flake8QuotesOptions.property.avoidEscape"></a>

```typescript
public readonly avoidEscape: boolean;
```

- *Type:* boolean

Whether to avoid using single quotes if a string contains single quotes, or vice-versa with double quotes, as per [PEP 8](https://peps.python.org/pep-0008/#string-quotes). This minimizes the need to escape quotation marks within strings.

---

##### `docstringQuotes`<sup>Optional</sup> <a name="docstringQuotes" id="projen.python.Flake8QuotesOptions.property.docstringQuotes"></a>

```typescript
public readonly docstringQuotes: string;
```

- *Type:* string

Quote style to prefer for docstrings (either "single" or "double").

When using the formatter, only "double" is compatible, as the formatter
enforces double quotes for docstrings strings.

---

##### `inlineQuotes`<sup>Optional</sup> <a name="inlineQuotes" id="projen.python.Flake8QuotesOptions.property.inlineQuotes"></a>

```typescript
public readonly inlineQuotes: string;
```

- *Type:* string

Quote style to prefer for inline strings (either "single" or "double").

When using the formatter, ensure that [`format.quote-style`](#format_quote-style) is set to
the same preferred quote style.

---

##### `multilineQuotes`<sup>Optional</sup> <a name="multilineQuotes" id="projen.python.Flake8QuotesOptions.property.multilineQuotes"></a>

```typescript
public readonly multilineQuotes: string;
```

- *Type:* string

Quote style to prefer for multiline strings (either "single" or "double").

When using the formatter, only "double" is compatible, as the formatter
enforces double quotes for multiline strings.

---

### Flake8SelfOptions <a name="Flake8SelfOptions" id="projen.python.Flake8SelfOptions"></a>

Options for the `flake8_self` plugin.

#### Initializer <a name="Initializer" id="projen.python.Flake8SelfOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const flake8SelfOptions: python.Flake8SelfOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Flake8SelfOptions.property.extendIgnoreNames">extendIgnoreNames</a></code> | <code>string[]</code> | Additional names to ignore when considering `flake8-self` violations, in addition to those included in [`ignore-names`](#lint_flake8-self_ignore-names). |
| <code><a href="#projen.python.Flake8SelfOptions.property.ignoreNames">ignoreNames</a></code> | <code>string[]</code> | A list of names to ignore when considering `flake8-self` violations. |

---

##### `extendIgnoreNames`<sup>Optional</sup> <a name="extendIgnoreNames" id="projen.python.Flake8SelfOptions.property.extendIgnoreNames"></a>

```typescript
public readonly extendIgnoreNames: string[];
```

- *Type:* string[]

Additional names to ignore when considering `flake8-self` violations, in addition to those included in [`ignore-names`](#lint_flake8-self_ignore-names).

---

##### `ignoreNames`<sup>Optional</sup> <a name="ignoreNames" id="projen.python.Flake8SelfOptions.property.ignoreNames"></a>

```typescript
public readonly ignoreNames: string[];
```

- *Type:* string[]

A list of names to ignore when considering `flake8-self` violations.

---

### Flake8TidyImportsOptions <a name="Flake8TidyImportsOptions" id="projen.python.Flake8TidyImportsOptions"></a>

Options for the `flake8-tidy-imports` plugin.

#### Initializer <a name="Initializer" id="projen.python.Flake8TidyImportsOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const flake8TidyImportsOptions: python.Flake8TidyImportsOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Flake8TidyImportsOptions.property.bannedApi">bannedApi</a></code> | <code>{[ key: string ]: <a href="#projen.python.ApiBan">ApiBan</a>}</code> | Specific modules or module members that may not be imported or accessed. |
| <code><a href="#projen.python.Flake8TidyImportsOptions.property.bannedModuleLevelImports">bannedModuleLevelImports</a></code> | <code>string[]</code> | List of specific modules that may not be imported at module level, and should instead be imported lazily (e.g., within a function definition, or an `if TYPE_CHECKING:` block, or some other nested context). This also affects the rule `import-outside-top-level` if `banned-module-level-imports` is enabled. |
| <code><a href="#projen.python.Flake8TidyImportsOptions.property.banRelativeImports">banRelativeImports</a></code> | <code>string</code> | Whether to ban all relative imports (`"all"`), or only those imports that extend into the parent module or beyond (`"parents"`). |

---

##### `bannedApi`<sup>Optional</sup> <a name="bannedApi" id="projen.python.Flake8TidyImportsOptions.property.bannedApi"></a>

```typescript
public readonly bannedApi: {[ key: string ]: ApiBan};
```

- *Type:* {[ key: string ]: <a href="#projen.python.ApiBan">ApiBan</a>}

Specific modules or module members that may not be imported or accessed.

Note that this rule is only meant to flag accidental uses,
and can be circumvented via `eval` or `importlib`.

---

##### `bannedModuleLevelImports`<sup>Optional</sup> <a name="bannedModuleLevelImports" id="projen.python.Flake8TidyImportsOptions.property.bannedModuleLevelImports"></a>

```typescript
public readonly bannedModuleLevelImports: string[];
```

- *Type:* string[]

List of specific modules that may not be imported at module level, and should instead be imported lazily (e.g., within a function definition, or an `if TYPE_CHECKING:` block, or some other nested context). This also affects the rule `import-outside-top-level` if `banned-module-level-imports` is enabled.

---

##### `banRelativeImports`<sup>Optional</sup> <a name="banRelativeImports" id="projen.python.Flake8TidyImportsOptions.property.banRelativeImports"></a>

```typescript
public readonly banRelativeImports: string;
```

- *Type:* string

Whether to ban all relative imports (`"all"`), or only those imports that extend into the parent module or beyond (`"parents"`).

---

### Flake8TypeCheckingOptions <a name="Flake8TypeCheckingOptions" id="projen.python.Flake8TypeCheckingOptions"></a>

Options for the `flake8-type-checking` plugin.

#### Initializer <a name="Initializer" id="projen.python.Flake8TypeCheckingOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const flake8TypeCheckingOptions: python.Flake8TypeCheckingOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Flake8TypeCheckingOptions.property.exemptModules">exemptModules</a></code> | <code>string[]</code> | Exempt certain modules from needing to be moved into type-checking blocks. |
| <code><a href="#projen.python.Flake8TypeCheckingOptions.property.quoteAnnotations">quoteAnnotations</a></code> | <code>boolean</code> | Whether to add quotes around type annotations, if doing so would allow the corresponding import to be moved into a type-checking block. |
| <code><a href="#projen.python.Flake8TypeCheckingOptions.property.runtimeEvaluatedBaseClasses">runtimeEvaluatedBaseClasses</a></code> | <code>string[]</code> | Exempt classes that list any of the enumerated classes as a base class from needing to be moved into type-checking blocks. |
| <code><a href="#projen.python.Flake8TypeCheckingOptions.property.runtimeEvaluatedDecorators">runtimeEvaluatedDecorators</a></code> | <code>string[]</code> | Exempt classes and functions decorated with any of the enumerated decorators from being moved into type-checking blocks. |
| <code><a href="#projen.python.Flake8TypeCheckingOptions.property.strict">strict</a></code> | <code>boolean</code> | Enforce `TC001`, `TC002`, and `TC003` rules even when valid runtime imports are present for the same module. |

---

##### `exemptModules`<sup>Optional</sup> <a name="exemptModules" id="projen.python.Flake8TypeCheckingOptions.property.exemptModules"></a>

```typescript
public readonly exemptModules: string[];
```

- *Type:* string[]

Exempt certain modules from needing to be moved into type-checking blocks.

---

##### `quoteAnnotations`<sup>Optional</sup> <a name="quoteAnnotations" id="projen.python.Flake8TypeCheckingOptions.property.quoteAnnotations"></a>

```typescript
public readonly quoteAnnotations: boolean;
```

- *Type:* boolean

Whether to add quotes around type annotations, if doing so would allow the corresponding import to be moved into a type-checking block.

For example, in the following, Python requires that `Sequence` be
available at runtime, despite the fact that it's only used in a type
annotation:

```python
from collections.abc import Sequence


def func(value: Sequence[int]) -> None:
...
```

In other words, moving `from collections.abc import Sequence` into an
`if TYPE_CHECKING:` block above would cause a runtime error, as the
type would no longer be available at runtime.

By default, Ruff will respect such runtime semantics and avoid moving
the import to prevent such runtime errors.

Setting `quote-annotations` to `true` will instruct Ruff to add quotes
around the annotation (e.g., `"Sequence[int]"`), which in turn enables
Ruff to move the import into an `if TYPE_CHECKING:` block, like so:

```python
from typing import TYPE_CHECKING

if TYPE_CHECKING:
from collections.abc import Sequence


def func(value: "Sequence[int]") -> None:
...
```

Note that this setting has no effect when `from __future__ import annotations`
is present, as `__future__` annotations are always treated equivalently
to quoted annotations. Similarly, this setting has no effect on Python
versions after 3.14 because these annotations are also deferred.

---

##### `runtimeEvaluatedBaseClasses`<sup>Optional</sup> <a name="runtimeEvaluatedBaseClasses" id="projen.python.Flake8TypeCheckingOptions.property.runtimeEvaluatedBaseClasses"></a>

```typescript
public readonly runtimeEvaluatedBaseClasses: string[];
```

- *Type:* string[]

Exempt classes that list any of the enumerated classes as a base class from needing to be moved into type-checking blocks.

Common examples include Pydantic's `pydantic.BaseModel` and SQLAlchemy's
`sqlalchemy.orm.DeclarativeBase`, but can also support user-defined
classes that inherit from those base classes. For example, if you define
a common `DeclarativeBase` subclass that's used throughout your project
(e.g., `class Base(DeclarativeBase) ...` in `base.py`), you can add it to
this list (`runtime-evaluated-base-classes = ["base.Base"]`) to exempt
models from being moved into type-checking blocks.

---

##### `runtimeEvaluatedDecorators`<sup>Optional</sup> <a name="runtimeEvaluatedDecorators" id="projen.python.Flake8TypeCheckingOptions.property.runtimeEvaluatedDecorators"></a>

```typescript
public readonly runtimeEvaluatedDecorators: string[];
```

- *Type:* string[]

Exempt classes and functions decorated with any of the enumerated decorators from being moved into type-checking blocks.

Common examples include Pydantic's `@pydantic.validate_call` decorator
(for functions) and attrs' `@attrs.define` decorator (for classes).

This also supports framework decorators like FastAPI's `fastapi.FastAPI.get`
which will work across assignments in the same module.

For example:
```python
import fastapi

app = FastAPI("app")

---

##### `strict`<sup>Optional</sup> <a name="strict" id="projen.python.Flake8TypeCheckingOptions.property.strict"></a>

```typescript
public readonly strict: boolean;
```

- *Type:* boolean

Enforce `TC001`, `TC002`, and `TC003` rules even when valid runtime imports are present for the same module.

See flake8-type-checking's [strict](https://github.com/snok/flake8-type-checking#strict) option.

---

### Flake8UnusedArgumentsOptions <a name="Flake8UnusedArgumentsOptions" id="projen.python.Flake8UnusedArgumentsOptions"></a>

Options for the `flake8-unused-arguments` plugin.

#### Initializer <a name="Initializer" id="projen.python.Flake8UnusedArgumentsOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const flake8UnusedArgumentsOptions: python.Flake8UnusedArgumentsOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Flake8UnusedArgumentsOptions.property.ignoreVariadicNames">ignoreVariadicNames</a></code> | <code>boolean</code> | Whether to allow unused variadic arguments, like `*args` and `**kwargs`. |

---

##### `ignoreVariadicNames`<sup>Optional</sup> <a name="ignoreVariadicNames" id="projen.python.Flake8UnusedArgumentsOptions.property.ignoreVariadicNames"></a>

```typescript
public readonly ignoreVariadicNames: boolean;
```

- *Type:* boolean

Whether to allow unused variadic arguments, like `*args` and `**kwargs`.

---

### FormatOptions <a name="FormatOptions" id="projen.python.FormatOptions"></a>

Configures the way Ruff formats your code.

#### Initializer <a name="Initializer" id="projen.python.FormatOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const formatOptions: python.FormatOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.FormatOptions.property.docstringCodeFormat">docstringCodeFormat</a></code> | <code>boolean</code> | Whether to format code snippets in docstrings. |
| <code><a href="#projen.python.FormatOptions.property.docstringCodeLineLength">docstringCodeLineLength</a></code> | <code>any</code> | Set the line length used when formatting code snippets in docstrings. |
| <code><a href="#projen.python.FormatOptions.property.exclude">exclude</a></code> | <code>string[]</code> | A list of file patterns to exclude from formatting in addition to the files excluded globally (see [`exclude`](#exclude), and [`extend-exclude`](#extend-exclude)). |
| <code><a href="#projen.python.FormatOptions.property.indentStyle">indentStyle</a></code> | <code>string</code> | Whether to use spaces or tabs for indentation. |
| <code><a href="#projen.python.FormatOptions.property.lineEnding">lineEnding</a></code> | <code>string</code> | The character Ruff uses at the end of a line. |
| <code><a href="#projen.python.FormatOptions.property.preview">preview</a></code> | <code>boolean</code> | Whether to enable the unstable preview style formatting. |
| <code><a href="#projen.python.FormatOptions.property.quoteStyle">quoteStyle</a></code> | <code><a href="#projen.python.QuoteStyle">QuoteStyle</a></code> | Configures the preferred quote character for strings. The recommended options are. |
| <code><a href="#projen.python.FormatOptions.property.skipMagicTrailingComma">skipMagicTrailingComma</a></code> | <code>boolean</code> | Ruff uses existing trailing commas as an indication that short lines should be left separate. |

---

##### `docstringCodeFormat`<sup>Optional</sup> <a name="docstringCodeFormat" id="projen.python.FormatOptions.property.docstringCodeFormat"></a>

```typescript
public readonly docstringCodeFormat: boolean;
```

- *Type:* boolean

Whether to format code snippets in docstrings.

When this is enabled, Python code examples within docstrings are
automatically reformatted.

For example, when this is enabled, the following code:

```python
def f(x):
"""
Something about `f`. And an example in doctest format:

>>> f(  x  )

Markdown is also supported:

```py
f(  x  )
```

As are reStructuredText literal blocks::

f(  x  )


And reStructuredText code blocks:

.. code-block:: python

f(  x  )
"""
pass
```

... will be reformatted (assuming the rest of the options are set to
their defaults) as:

```python
def f(x):
"""
Something about `f`. And an example in doctest format:

>>> f(x)

Markdown is also supported:

```py
f(x)
```

As are reStructuredText literal blocks::

f(x)


And reStructuredText code blocks:

.. code-block:: python

f(x)
"""
pass
```

If a code snippet in a docstring contains invalid Python code or if the
formatter would otherwise write invalid Python code, then the code
example is ignored by the formatter and kept as-is.

Currently, doctest, Markdown, reStructuredText literal blocks, and
reStructuredText code blocks are all supported and automatically
recognized. In the case of unlabeled fenced code blocks in Markdown and
reStructuredText literal blocks, the contents are assumed to be Python
and reformatted. As with any other format, if the contents aren't valid
Python, then the block is left untouched automatically.

---

##### `docstringCodeLineLength`<sup>Optional</sup> <a name="docstringCodeLineLength" id="projen.python.FormatOptions.property.docstringCodeLineLength"></a>

```typescript
public readonly docstringCodeLineLength: any;
```

- *Type:* any

Set the line length used when formatting code snippets in docstrings.

This only has an effect when the `docstring-code-format` setting is
enabled.

The default value for this setting is `"dynamic"`, which has the effect
of ensuring that any reformatted code examples in docstrings adhere to
the global line length configuration that is used for the surrounding
Python code. The point of this setting is that it takes the indentation
of the docstring into account when reformatting code examples.

Alternatively, this can be set to a fixed integer, which will result
in the same line length limit being applied to all reformatted code
examples in docstrings. When set to a fixed integer, the indent of the
docstring is not taken into account. That is, this may result in lines
in the reformatted code example that exceed the globally configured
line length limit.

For example, when this is set to `20` and [`docstring-code-format`](#docstring-code-format)
is enabled, then this code:

```python
def f(x):
'''
Something about `f`. And an example:

.. code-block:: python

foo, bar, quux = this_is_a_long_line(lion, hippo, lemur, bear)
'''
pass
```

... will be reformatted (assuming the rest of the options are set
to their defaults) as:

```python
def f(x):
"""
Something about `f`. And an example:

.. code-block:: python

(
foo,
bar,
quux,
) = this_is_a_long_line(
lion,
hippo,
lemur,
bear,
)
"""
pass
```

---

##### `exclude`<sup>Optional</sup> <a name="exclude" id="projen.python.FormatOptions.property.exclude"></a>

```typescript
public readonly exclude: string[];
```

- *Type:* string[]

A list of file patterns to exclude from formatting in addition to the files excluded globally (see [`exclude`](#exclude), and [`extend-exclude`](#extend-exclude)).

Exclusions are based on globs, and can be either:

- Single-path patterns, like `.mypy_cache` (to exclude any directory
named `.mypy_cache` in the tree), `foo.py` (to exclude any file named
`foo.py`), or `foo_*.py` (to exclude any file matching `foo_*.py` ).
- Relative patterns, like `directory/foo.py` (to exclude that specific
file) or `directory/*.py` (to exclude any Python files in
`directory`). Note that these paths are relative to the project root
(e.g., the directory containing your `pyproject.toml`).

For more information on the glob syntax, refer to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).

---

##### `indentStyle`<sup>Optional</sup> <a name="indentStyle" id="projen.python.FormatOptions.property.indentStyle"></a>

```typescript
public readonly indentStyle: string;
```

- *Type:* string

Whether to use spaces or tabs for indentation.

`indent-style = "space"` (default):

```python
def f():
print("Hello") #  Spaces indent the `print` statement.
```

`indent-style = "tab"`:

```python
def f():
print("Hello") #  A tab `\t` indents the `print` statement.
```

PEP 8 recommends using spaces for [indentation](https://peps.python.org/pep-0008/#indentation).
We care about accessibility; if you do not need tabs for accessibility, we do not recommend you use them.

See [`indent-width`](#indent-width) to configure the number of spaces per indentation and the tab width.

---

##### `lineEnding`<sup>Optional</sup> <a name="lineEnding" id="projen.python.FormatOptions.property.lineEnding"></a>

```typescript
public readonly lineEnding: string;
```

- *Type:* string
- *Default:* n` for files that contain no line endings.

The character Ruff uses at the end of a line.

* `auto`: The newline style is detected automatically on a file per file basis. Files with mixed line endings will be converted to the first detected line ending. Defaults to `\n` for files that contain no line endings.
* `lf`: Line endings will be converted to `\n`. The default line ending on Unix.
* `cr-lf`: Line endings will be converted to `\r\n`. The default line ending on Windows.
* `native`: Line endings will be converted to `\n` on Unix and `\r\n` on Windows.

---

##### `preview`<sup>Optional</sup> <a name="preview" id="projen.python.FormatOptions.property.preview"></a>

```typescript
public readonly preview: boolean;
```

- *Type:* boolean

Whether to enable the unstable preview style formatting.

---

##### `quoteStyle`<sup>Optional</sup> <a name="quoteStyle" id="projen.python.FormatOptions.property.quoteStyle"></a>

```typescript
public readonly quoteStyle: QuoteStyle;
```

- *Type:* <a href="#projen.python.QuoteStyle">QuoteStyle</a>

Configures the preferred quote character for strings. The recommended options are.

* `double` (default): Use double quotes `"`
* `single`: Use single quotes `'`

In compliance with [PEP 8](https://peps.python.org/pep-0008/) and [PEP 257](https://peps.python.org/pep-0257/),
Ruff prefers double quotes for triple quoted strings and docstrings even when using `quote-style = "single"`.

Ruff deviates from using the configured quotes if doing so prevents the need for
escaping quote characters inside the string:

```python
a = "a string without any quotes"
b = "It's monday morning"
```

Ruff will change the quotes of the string assigned to `a` to single quotes when using `quote-style = "single"`.
However, Ruff uses double quotes for the string assigned to `b` because using single quotes would require escaping the `'`,
which leads to the less readable code: `'It\'s monday morning'`.

In addition, Ruff supports the quote style `preserve` for projects that already use
a mixture of single and double quotes and can't migrate to the `double` or `single` style.
The quote style `preserve` leaves the quotes of all strings unchanged.

---

##### `skipMagicTrailingComma`<sup>Optional</sup> <a name="skipMagicTrailingComma" id="projen.python.FormatOptions.property.skipMagicTrailingComma"></a>

```typescript
public readonly skipMagicTrailingComma: boolean;
```

- *Type:* boolean

Ruff uses existing trailing commas as an indication that short lines should be left separate.

If this option is set to `true`, the magic trailing comma is ignored.

For example, Ruff leaves the arguments separate even though
collapsing the arguments to a single line doesn't exceed the line length if `skip-magic-trailing-comma = false`:

```python
# The arguments remain on separate lines because of the trailing comma after `b`
def test(
a,
b,
): pass
```

Setting `skip-magic-trailing-comma = true` changes the formatting to:

```python
# The arguments are collapsed to a single line because the trailing comma is ignored
def test(a, b):
pass
```

---

### HatchConfiguration <a name="HatchConfiguration" id="projen.python.HatchConfiguration"></a>

#### Initializer <a name="Initializer" id="projen.python.HatchConfiguration.Initializer"></a>

```typescript
import { python } from 'projen'

const hatchConfiguration: python.HatchConfiguration = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.HatchConfiguration.property.buildConfig">buildConfig</a></code> | <code><a href="#projen.python.Build">Build</a></code> | *No description.* |
| <code><a href="#projen.python.HatchConfiguration.property.env">env</a></code> | <code><a href="#projen.python.CollectorEnv">CollectorEnv</a></code> | *No description.* |
| <code><a href="#projen.python.HatchConfiguration.property.envs">envs</a></code> | <code>{[ key: string ]: <a href="#projen.python.Env">Env</a>}</code> | *No description.* |
| <code><a href="#projen.python.HatchConfiguration.property.metadata">metadata</a></code> | <code><a href="#projen.python.Metadata">Metadata</a></code> | *No description.* |
| <code><a href="#projen.python.HatchConfiguration.property.publish">publish</a></code> | <code><a href="#projen.python.Publish">Publish</a></code> | *No description.* |
| <code><a href="#projen.python.HatchConfiguration.property.version">version</a></code> | <code><a href="#projen.python.Version">Version</a></code> | *No description.* |

---

##### `buildConfig`<sup>Optional</sup> <a name="buildConfig" id="projen.python.HatchConfiguration.property.buildConfig"></a>

```typescript
public readonly buildConfig: Build;
```

- *Type:* <a href="#projen.python.Build">Build</a>

---

##### `env`<sup>Optional</sup> <a name="env" id="projen.python.HatchConfiguration.property.env"></a>

```typescript
public readonly env: CollectorEnv;
```

- *Type:* <a href="#projen.python.CollectorEnv">CollectorEnv</a>

---

##### `envs`<sup>Optional</sup> <a name="envs" id="projen.python.HatchConfiguration.property.envs"></a>

```typescript
public readonly envs: {[ key: string ]: Env};
```

- *Type:* {[ key: string ]: <a href="#projen.python.Env">Env</a>}

---

##### `metadata`<sup>Optional</sup> <a name="metadata" id="projen.python.HatchConfiguration.property.metadata"></a>

```typescript
public readonly metadata: Metadata;
```

- *Type:* <a href="#projen.python.Metadata">Metadata</a>

---

##### `publish`<sup>Optional</sup> <a name="publish" id="projen.python.HatchConfiguration.property.publish"></a>

```typescript
public readonly publish: Publish;
```

- *Type:* <a href="#projen.python.Publish">Publish</a>

---

##### `version`<sup>Optional</sup> <a name="version" id="projen.python.HatchConfiguration.property.version"></a>

```typescript
public readonly version: Version;
```

- *Type:* <a href="#projen.python.Version">Version</a>

---

### Hook <a name="Hook" id="projen.python.Hook"></a>

#### Initializer <a name="Initializer" id="projen.python.Hook.Initializer"></a>

```typescript
import { python } from 'projen'

const hook: python.Hook = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Hook.property.dependencies">dependencies</a></code> | <code>string[]</code> | Additional dependencies installed in the build environment. |
| <code><a href="#projen.python.Hook.property.enableByDefault">enableByDefault</a></code> | <code>boolean</code> | Whether to enable current hook (disable to control activation using environment variables). |
| <code><a href="#projen.python.Hook.property.requireRuntimeDependencies">requireRuntimeDependencies</a></code> | <code>boolean</code> | Whether to install the project’s runtime dependencies. |
| <code><a href="#projen.python.Hook.property.requireRuntimeFeatures">requireRuntimeFeatures</a></code> | <code>string[]</code> | A list of the project’s runtime features to install. |

---

##### `dependencies`<sup>Optional</sup> <a name="dependencies" id="projen.python.Hook.property.dependencies"></a>

```typescript
public readonly dependencies: string[];
```

- *Type:* string[]

Additional dependencies installed in the build environment.

---

##### `enableByDefault`<sup>Optional</sup> <a name="enableByDefault" id="projen.python.Hook.property.enableByDefault"></a>

```typescript
public readonly enableByDefault: boolean;
```

- *Type:* boolean

Whether to enable current hook (disable to control activation using environment variables).

---

##### `requireRuntimeDependencies`<sup>Optional</sup> <a name="requireRuntimeDependencies" id="projen.python.Hook.property.requireRuntimeDependencies"></a>

```typescript
public readonly requireRuntimeDependencies: boolean;
```

- *Type:* boolean

Whether to install the project’s runtime dependencies.

---

##### `requireRuntimeFeatures`<sup>Optional</sup> <a name="requireRuntimeFeatures" id="projen.python.Hook.property.requireRuntimeFeatures"></a>

```typescript
public readonly requireRuntimeFeatures: string[];
```

- *Type:* string[]

A list of the project’s runtime features to install.

---

### Index <a name="Index" id="projen.python.Index"></a>

#### Initializer <a name="Initializer" id="projen.python.Index.Initializer"></a>

```typescript
import { python } from 'projen'

const index: python.Index = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Index.property.url">url</a></code> | <code>string</code> | The URL of the index. |
| <code><a href="#projen.python.Index.property.authenticate">authenticate</a></code> | <code>string</code> | When uv should use authentication for requests to the index. |
| <code><a href="#projen.python.Index.property.cacheControl">cacheControl</a></code> | <code><a href="#projen.python.IndexCacheControl">IndexCacheControl</a></code> | Cache control configuration for this index. |
| <code><a href="#projen.python.Index.property.default">default</a></code> | <code>boolean</code> | Mark the index as the default index. |
| <code><a href="#projen.python.Index.property.explicit">explicit</a></code> | <code>boolean</code> | Mark the index as explicit. |
| <code><a href="#projen.python.Index.property.format">format</a></code> | <code>string</code> | The format used by the index. |
| <code><a href="#projen.python.Index.property.ignoreErrorCodes">ignoreErrorCodes</a></code> | <code>number[]</code> | Status codes that uv should ignore when deciding whether to continue searching in the next index after a failure. |
| <code><a href="#projen.python.Index.property.name">name</a></code> | <code>string</code> | The name of the index. |
| <code><a href="#projen.python.Index.property.publishUrl">publishUrl</a></code> | <code>string</code> | The URL of the upload endpoint. |

---

##### `url`<sup>Required</sup> <a name="url" id="projen.python.Index.property.url"></a>

```typescript
public readonly url: string;
```

- *Type:* string

The URL of the index.

Expects to receive a URL (e.g., `https://pypi.org/simple`) or a local path.

---

##### `authenticate`<sup>Optional</sup> <a name="authenticate" id="projen.python.Index.property.authenticate"></a>

```typescript
public readonly authenticate: string;
```

- *Type:* string

When uv should use authentication for requests to the index.

```toml
[[tool.uv.index]]
name = "my-index"
url = "https://<omitted>/simple"
authenticate = "always"
```

---

##### `cacheControl`<sup>Optional</sup> <a name="cacheControl" id="projen.python.Index.property.cacheControl"></a>

```typescript
public readonly cacheControl: IndexCacheControl;
```

- *Type:* <a href="#projen.python.IndexCacheControl">IndexCacheControl</a>

Cache control configuration for this index.

When set, these headers will override the server's cache control headers
for both package metadata requests and artifact downloads.

```toml
[[tool.uv.index]]
name = "my-index"
url = "https://<omitted>/simple"
cache-control = { api = "max-age=600", files = "max-age=3600" }
```

---

##### `default`<sup>Optional</sup> <a name="default" id="projen.python.Index.property.default"></a>

```typescript
public readonly default: boolean;
```

- *Type:* boolean

Mark the index as the default index.

By default, uv uses PyPI as the default index, such that even if additional indexes are
defined via `[[tool.uv.index]]`, PyPI will still be used as a fallback for packages that
aren't found elsewhere. To disable the PyPI default, set `default = true` on at least one
other index.

Marking an index as default will move it to the front of the list of indexes, such that it
is given the highest priority when resolving packages.

---

##### `explicit`<sup>Optional</sup> <a name="explicit" id="projen.python.Index.property.explicit"></a>

```typescript
public readonly explicit: boolean;
```

- *Type:* boolean

Mark the index as explicit.

Explicit indexes will _only_ be used when explicitly requested via a `[tool.uv.sources]`
definition, as in:

```toml
[[tool.uv.index]]
name = "pytorch"
url = "https://download.pytorch.org/whl/cu121"
explicit = true

[tool.uv.sources]
torch = { index = "pytorch" }
```

---

##### `format`<sup>Optional</sup> <a name="format" id="projen.python.Index.property.format"></a>

```typescript
public readonly format: string;
```

- *Type:* string

The format used by the index.

Indexes can either be PEP 503-compliant (i.e., a PyPI-style registry implementing the Simple
API) or structured as a flat list of distributions (e.g., `--find-links`). In both cases,
indexes can point to either local or remote resources.

---

##### `ignoreErrorCodes`<sup>Optional</sup> <a name="ignoreErrorCodes" id="projen.python.Index.property.ignoreErrorCodes"></a>

```typescript
public readonly ignoreErrorCodes: number[];
```

- *Type:* number[]

Status codes that uv should ignore when deciding whether to continue searching in the next index after a failure.

```toml
[[tool.uv.index]]
name = "my-index"
url = "https://<omitted>/simple"
ignore-error-codes = [401, 403]
```

---

##### `name`<sup>Optional</sup> <a name="name" id="projen.python.Index.property.name"></a>

```typescript
public readonly name: string;
```

- *Type:* string

The name of the index.

Index names can be used to reference indexes elsewhere in the configuration. For example,
you can pin a package to a specific index by name:

```toml
[[tool.uv.index]]
name = "pytorch"
url = "https://download.pytorch.org/whl/cu121"

[tool.uv.sources]
torch = { index = "pytorch" }
```

---

##### `publishUrl`<sup>Optional</sup> <a name="publishUrl" id="projen.python.Index.property.publishUrl"></a>

```typescript
public readonly publishUrl: string;
```

- *Type:* string

The URL of the upload endpoint.

When using `uv publish --index <name>`, this URL is used for publishing.

A configuration for the default index PyPI would look as follows:

```toml
[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
publish-url = "https://upload.pypi.org/legacy/"
```

---

### IndexCacheControl <a name="IndexCacheControl" id="projen.python.IndexCacheControl"></a>

Cache control configuration for an index.

#### Initializer <a name="Initializer" id="projen.python.IndexCacheControl.Initializer"></a>

```typescript
import { python } from 'projen'

const indexCacheControl: python.IndexCacheControl = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.IndexCacheControl.property.api">api</a></code> | <code>string</code> | Cache control header for Simple API requests. |
| <code><a href="#projen.python.IndexCacheControl.property.files">files</a></code> | <code>string</code> | Cache control header for file downloads. |

---

##### `api`<sup>Optional</sup> <a name="api" id="projen.python.IndexCacheControl.property.api"></a>

```typescript
public readonly api: string;
```

- *Type:* string

Cache control header for Simple API requests.

---

##### `files`<sup>Optional</sup> <a name="files" id="projen.python.IndexCacheControl.property.files"></a>

```typescript
public readonly files: string;
```

- *Type:* string

Cache control header for file downloads.

---

### IsortOptions <a name="IsortOptions" id="projen.python.IsortOptions"></a>

Options for the `isort` plugin.

#### Initializer <a name="Initializer" id="projen.python.IsortOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const isortOptions: python.IsortOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.IsortOptions.property.caseSensitive">caseSensitive</a></code> | <code>boolean</code> | Sort imports taking into account case sensitivity. |
| <code><a href="#projen.python.IsortOptions.property.classes">classes</a></code> | <code>string[]</code> | An override list of tokens to always recognize as a Class for [`order-by-type`](#lint_isort_order-by-type) regardless of casing. |
| <code><a href="#projen.python.IsortOptions.property.combineAsImports">combineAsImports</a></code> | <code>boolean</code> | Combines as imports on the same line. |
| <code><a href="#projen.python.IsortOptions.property.constants">constants</a></code> | <code>string[]</code> | An override list of tokens to always recognize as a CONSTANT for [`order-by-type`](#lint_isort_order-by-type) regardless of casing. |
| <code><a href="#projen.python.IsortOptions.property.defaultSection">defaultSection</a></code> | <code><a href="#projen.python.ImportSection">ImportSection</a></code> | Define a default section for any imports that don't fit into the specified [`section-order`](#lint_isort_section-order). |
| <code><a href="#projen.python.IsortOptions.property.detectSamePackage">detectSamePackage</a></code> | <code>boolean</code> | Whether to automatically mark imports from within the same package as first-party. |
| <code><a href="#projen.python.IsortOptions.property.extraStandardLibrary">extraStandardLibrary</a></code> | <code>string[]</code> | A list of modules to consider standard-library, in addition to those known to Ruff in advance. |
| <code><a href="#projen.python.IsortOptions.property.forcedSeparate">forcedSeparate</a></code> | <code>string[]</code> | A list of modules to separate into auxiliary block(s) of imports, in the order specified. |
| <code><a href="#projen.python.IsortOptions.property.forceSingleLine">forceSingleLine</a></code> | <code>boolean</code> | Forces all from imports to appear on their own line. |
| <code><a href="#projen.python.IsortOptions.property.forceSortWithinSections">forceSortWithinSections</a></code> | <code>boolean</code> | Don't sort straight-style imports (like `import sys`) before from-style imports (like `from itertools import groupby`). |
| <code><a href="#projen.python.IsortOptions.property.forceToTop">forceToTop</a></code> | <code>string[]</code> | Force specific imports to the top of their appropriate section. |
| <code><a href="#projen.python.IsortOptions.property.forceWrapAliases">forceWrapAliases</a></code> | <code>boolean</code> | Force `import from` statements with multiple members and at least one alias (e.g., `import A as B`) to wrap such that every line contains exactly one member. For example, this formatting would be retained, rather than condensing to a single line:. |
| <code><a href="#projen.python.IsortOptions.property.fromFirst">fromFirst</a></code> | <code>boolean</code> | Whether to place `import from` imports before straight imports when sorting. |
| <code><a href="#projen.python.IsortOptions.property.knownFirstParty">knownFirstParty</a></code> | <code>string[]</code> | A list of modules to consider first-party, regardless of whether they can be identified as such via introspection of the local filesystem. |
| <code><a href="#projen.python.IsortOptions.property.knownLocalFolder">knownLocalFolder</a></code> | <code>string[]</code> | A list of modules to consider being a local folder. |
| <code><a href="#projen.python.IsortOptions.property.knownThirdParty">knownThirdParty</a></code> | <code>string[]</code> | A list of modules to consider third-party, regardless of whether they can be identified as such via introspection of the local filesystem. |
| <code><a href="#projen.python.IsortOptions.property.lengthSort">lengthSort</a></code> | <code>boolean</code> | Sort imports by their string length, such that shorter imports appear before longer imports. |
| <code><a href="#projen.python.IsortOptions.property.lengthSortStraight">lengthSortStraight</a></code> | <code>boolean</code> | Sort straight imports by their string length. |
| <code><a href="#projen.python.IsortOptions.property.linesAfterImports">linesAfterImports</a></code> | <code>number</code> | The number of blank lines to place after imports. Use `-1` for automatic determination. |
| <code><a href="#projen.python.IsortOptions.property.linesBetweenTypes">linesBetweenTypes</a></code> | <code>number</code> | The number of lines to place between "direct" and `import from` imports. |
| <code><a href="#projen.python.IsortOptions.property.noLinesBefore">noLinesBefore</a></code> | <code><a href="#projen.python.ImportSection">ImportSection</a>[]</code> | A list of sections that should _not_ be delineated from the previous section via empty lines. |
| <code><a href="#projen.python.IsortOptions.property.noSections">noSections</a></code> | <code>boolean</code> | Put all imports into the same section bucket. |
| <code><a href="#projen.python.IsortOptions.property.orderByType">orderByType</a></code> | <code>boolean</code> | Order imports by type, which is determined by case, in addition to alphabetically. |
| <code><a href="#projen.python.IsortOptions.property.relativeImportsOrder">relativeImportsOrder</a></code> | <code>string</code> | Whether to place "closer" imports (fewer `.` characters, most local) before "further" imports (more `.` characters, least local), or vice versa. |
| <code><a href="#projen.python.IsortOptions.property.requiredImports">requiredImports</a></code> | <code>string[]</code> | Add the specified import line to all files. |
| <code><a href="#projen.python.IsortOptions.property.sectionOrder">sectionOrder</a></code> | <code><a href="#projen.python.ImportSection">ImportSection</a>[]</code> | Override in which order the sections should be output. |
| <code><a href="#projen.python.IsortOptions.property.sections">sections</a></code> | <code>{[ key: string ]: string[]}</code> | A list of mappings from section names to modules. |
| <code><a href="#projen.python.IsortOptions.property.singleLineExclusions">singleLineExclusions</a></code> | <code>string[]</code> | One or more modules to exclude from the single line rule. |
| <code><a href="#projen.python.IsortOptions.property.splitOnTrailingComma">splitOnTrailingComma</a></code> | <code>boolean</code> | If a comma is placed after the last member in a multi-line import, then the imports will never be folded into one line. |
| <code><a href="#projen.python.IsortOptions.property.variables">variables</a></code> | <code>string[]</code> | An override list of tokens to always recognize as a var for [`order-by-type`](#lint_isort_order-by-type) regardless of casing. |

---

##### `caseSensitive`<sup>Optional</sup> <a name="caseSensitive" id="projen.python.IsortOptions.property.caseSensitive"></a>

```typescript
public readonly caseSensitive: boolean;
```

- *Type:* boolean

Sort imports taking into account case sensitivity.

Note that the [`order-by-type`](#lint_isort_order-by-type) setting will
take precedence over this one when enabled.

---

##### `classes`<sup>Optional</sup> <a name="classes" id="projen.python.IsortOptions.property.classes"></a>

```typescript
public readonly classes: string[];
```

- *Type:* string[]

An override list of tokens to always recognize as a Class for [`order-by-type`](#lint_isort_order-by-type) regardless of casing.

---

##### `combineAsImports`<sup>Optional</sup> <a name="combineAsImports" id="projen.python.IsortOptions.property.combineAsImports"></a>

```typescript
public readonly combineAsImports: boolean;
```

- *Type:* boolean

Combines as imports on the same line.

See isort's [`combine-as-imports`](https://pycqa.github.io/isort/docs/configuration/options.html#combine-as-imports)
option.

---

##### `constants`<sup>Optional</sup> <a name="constants" id="projen.python.IsortOptions.property.constants"></a>

```typescript
public readonly constants: string[];
```

- *Type:* string[]

An override list of tokens to always recognize as a CONSTANT for [`order-by-type`](#lint_isort_order-by-type) regardless of casing.

---

##### `defaultSection`<sup>Optional</sup> <a name="defaultSection" id="projen.python.IsortOptions.property.defaultSection"></a>

```typescript
public readonly defaultSection: ImportSection;
```

- *Type:* <a href="#projen.python.ImportSection">ImportSection</a>

Define a default section for any imports that don't fit into the specified [`section-order`](#lint_isort_section-order).

---

##### `detectSamePackage`<sup>Optional</sup> <a name="detectSamePackage" id="projen.python.IsortOptions.property.detectSamePackage"></a>

```typescript
public readonly detectSamePackage: boolean;
```

- *Type:* boolean

Whether to automatically mark imports from within the same package as first-party.

For example, when `detect-same-package = true`, then when analyzing files within the
`foo` package, any imports from within the `foo` package will be considered first-party.

This heuristic is often unnecessary when `src` is configured to detect all first-party
sources; however, if `src` is _not_ configured, this heuristic can be useful to detect
first-party imports from _within_ (but not _across_) first-party packages.

---

##### `extraStandardLibrary`<sup>Optional</sup> <a name="extraStandardLibrary" id="projen.python.IsortOptions.property.extraStandardLibrary"></a>

```typescript
public readonly extraStandardLibrary: string[];
```

- *Type:* string[]

A list of modules to consider standard-library, in addition to those known to Ruff in advance.

Supports glob patterns. For more information on the glob syntax, refer
to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).

---

##### `forcedSeparate`<sup>Optional</sup> <a name="forcedSeparate" id="projen.python.IsortOptions.property.forcedSeparate"></a>

```typescript
public readonly forcedSeparate: string[];
```

- *Type:* string[]

A list of modules to separate into auxiliary block(s) of imports, in the order specified.

---

##### `forceSingleLine`<sup>Optional</sup> <a name="forceSingleLine" id="projen.python.IsortOptions.property.forceSingleLine"></a>

```typescript
public readonly forceSingleLine: boolean;
```

- *Type:* boolean

Forces all from imports to appear on their own line.

---

##### `forceSortWithinSections`<sup>Optional</sup> <a name="forceSortWithinSections" id="projen.python.IsortOptions.property.forceSortWithinSections"></a>

```typescript
public readonly forceSortWithinSections: boolean;
```

- *Type:* boolean

Don't sort straight-style imports (like `import sys`) before from-style imports (like `from itertools import groupby`).

Instead, sort the
imports by module, independent of import style.

---

##### `forceToTop`<sup>Optional</sup> <a name="forceToTop" id="projen.python.IsortOptions.property.forceToTop"></a>

```typescript
public readonly forceToTop: string[];
```

- *Type:* string[]

Force specific imports to the top of their appropriate section.

---

##### `forceWrapAliases`<sup>Optional</sup> <a name="forceWrapAliases" id="projen.python.IsortOptions.property.forceWrapAliases"></a>

```typescript
public readonly forceWrapAliases: boolean;
```

- *Type:* boolean

Force `import from` statements with multiple members and at least one alias (e.g., `import A as B`) to wrap such that every line contains exactly one member. For example, this formatting would be retained, rather than condensing to a single line:.

```python
from .utils import (
test_directory as test_directory,
test_id as test_id
)
```

Note that this setting is only effective when combined with
`combine-as-imports = true`. When [`combine-as-imports`](#lint_isort_combine-as-imports) isn't
enabled, every aliased `import from` will be given its own line, in
which case, wrapping is not necessary.

When using the formatter, ensure that [`format.skip-magic-trailing-comma`](#format_skip-magic-trailing-comma) is set to `false` (default)
when enabling `force-wrap-aliases` to avoid that the formatter collapses members if they all fit on a single line.

---

##### `fromFirst`<sup>Optional</sup> <a name="fromFirst" id="projen.python.IsortOptions.property.fromFirst"></a>

```typescript
public readonly fromFirst: boolean;
```

- *Type:* boolean

Whether to place `import from` imports before straight imports when sorting.

For example, by default, imports will be sorted such that straight imports appear
before `import from` imports, as in:
```python
import os
import sys
from typing import List
```

Setting `from-first = true` will instead sort such that `import from` imports appear
before straight imports, as in:
```python
from typing import List
import os
import sys
```

---

##### `knownFirstParty`<sup>Optional</sup> <a name="knownFirstParty" id="projen.python.IsortOptions.property.knownFirstParty"></a>

```typescript
public readonly knownFirstParty: string[];
```

- *Type:* string[]

A list of modules to consider first-party, regardless of whether they can be identified as such via introspection of the local filesystem.

Supports glob patterns. For more information on the glob syntax, refer
to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).

---

##### `knownLocalFolder`<sup>Optional</sup> <a name="knownLocalFolder" id="projen.python.IsortOptions.property.knownLocalFolder"></a>

```typescript
public readonly knownLocalFolder: string[];
```

- *Type:* string[]

A list of modules to consider being a local folder.

Generally, this is reserved for relative imports (`from . import module`).

Supports glob patterns. For more information on the glob syntax, refer
to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).

---

##### `knownThirdParty`<sup>Optional</sup> <a name="knownThirdParty" id="projen.python.IsortOptions.property.knownThirdParty"></a>

```typescript
public readonly knownThirdParty: string[];
```

- *Type:* string[]

A list of modules to consider third-party, regardless of whether they can be identified as such via introspection of the local filesystem.

Supports glob patterns. For more information on the glob syntax, refer
to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).

---

##### `lengthSort`<sup>Optional</sup> <a name="lengthSort" id="projen.python.IsortOptions.property.lengthSort"></a>

```typescript
public readonly lengthSort: boolean;
```

- *Type:* boolean

Sort imports by their string length, such that shorter imports appear before longer imports.

For example, by default, imports will be sorted
alphabetically, as in:
```python
import collections
import os
```

Setting `length-sort = true` will instead sort such that shorter imports
appear before longer imports, as in:
```python
import os
import collections
```

---

##### `lengthSortStraight`<sup>Optional</sup> <a name="lengthSortStraight" id="projen.python.IsortOptions.property.lengthSortStraight"></a>

```typescript
public readonly lengthSortStraight: boolean;
```

- *Type:* boolean

Sort straight imports by their string length.

Similar to [`length-sort`](#lint_isort_length-sort),
but applies only to straight imports and doesn't affect `from` imports.

---

##### `linesAfterImports`<sup>Optional</sup> <a name="linesAfterImports" id="projen.python.IsortOptions.property.linesAfterImports"></a>

```typescript
public readonly linesAfterImports: number;
```

- *Type:* number

The number of blank lines to place after imports. Use `-1` for automatic determination.

Ruff uses at most one blank line after imports in typing stub files (files with `.pyi` extension) in accordance to
the typing style recommendations ([source](https://typing.python.org/en/latest/guides/writing_stubs.html#blank-lines)).

When using the formatter, only the values `-1`, `1`, and `2` are compatible because
it enforces at least one empty and at most two empty lines after imports.

---

##### `linesBetweenTypes`<sup>Optional</sup> <a name="linesBetweenTypes" id="projen.python.IsortOptions.property.linesBetweenTypes"></a>

```typescript
public readonly linesBetweenTypes: number;
```

- *Type:* number

The number of lines to place between "direct" and `import from` imports.

When using the formatter, only the values `0` and `1` are compatible because
it preserves up to one empty line after imports in nested blocks.

---

##### `noLinesBefore`<sup>Optional</sup> <a name="noLinesBefore" id="projen.python.IsortOptions.property.noLinesBefore"></a>

```typescript
public readonly noLinesBefore: ImportSection[];
```

- *Type:* <a href="#projen.python.ImportSection">ImportSection</a>[]

A list of sections that should _not_ be delineated from the previous section via empty lines.

---

##### `noSections`<sup>Optional</sup> <a name="noSections" id="projen.python.IsortOptions.property.noSections"></a>

```typescript
public readonly noSections: boolean;
```

- *Type:* boolean

Put all imports into the same section bucket.

For example, rather than separating standard library and third-party imports, as in:
```python
import os
import sys

import numpy
import pandas
```

Setting `no-sections = true` will instead group all imports into a single section:
```python
import numpy
import os
import pandas
import sys
```

---

##### `orderByType`<sup>Optional</sup> <a name="orderByType" id="projen.python.IsortOptions.property.orderByType"></a>

```typescript
public readonly orderByType: boolean;
```

- *Type:* boolean

Order imports by type, which is determined by case, in addition to alphabetically.

Note that this option takes precedence over the
[`case-sensitive`](#lint_isort_case-sensitive) setting when enabled.

---

##### `relativeImportsOrder`<sup>Optional</sup> <a name="relativeImportsOrder" id="projen.python.IsortOptions.property.relativeImportsOrder"></a>

```typescript
public readonly relativeImportsOrder: string;
```

- *Type:* string

Whether to place "closer" imports (fewer `.` characters, most local) before "further" imports (more `.` characters, least local), or vice versa.

The default ("furthest-to-closest") is equivalent to isort's
[`reverse-relative`](https://pycqa.github.io/isort/docs/configuration/options.html#reverse-relative) default (`reverse-relative = false`); setting
this to "closest-to-furthest" is equivalent to isort's
`reverse-relative = true`.

---

##### `requiredImports`<sup>Optional</sup> <a name="requiredImports" id="projen.python.IsortOptions.property.requiredImports"></a>

```typescript
public readonly requiredImports: string[];
```

- *Type:* string[]

Add the specified import line to all files.

---

##### `sectionOrder`<sup>Optional</sup> <a name="sectionOrder" id="projen.python.IsortOptions.property.sectionOrder"></a>

```typescript
public readonly sectionOrder: ImportSection[];
```

- *Type:* <a href="#projen.python.ImportSection">ImportSection</a>[]

Override in which order the sections should be output.

Can be used to move custom sections.

---

##### `sections`<sup>Optional</sup> <a name="sections" id="projen.python.IsortOptions.property.sections"></a>

```typescript
public readonly sections: {[ key: string ]: string[]};
```

- *Type:* {[ key: string ]: string[]}

A list of mappings from section names to modules.

By default, imports are categorized according to their type (e.g., `future`, `third-party`,
and so on). This setting allows you to group modules into custom sections, to augment or
override the built-in sections.

For example, to group all testing utilities, you could create a `testing` section:
```toml
testing = ["pytest", "hypothesis"]
```

The values in the list are treated as glob patterns. For example, to match all packages in
the LangChain ecosystem (`langchain-core`, `langchain-openai`, etc.):
```toml
langchain = ["langchain-*"]
```

Custom sections should typically be inserted into the [`section-order`](#lint_isort_section-order) list to ensure that
they're displayed as a standalone group and in the intended order, as in:
```toml
section-order = [
"future",
"standard-library",
"third-party",
"first-party",
"local-folder",
"testing"
]
```

If a custom section is omitted from [`section-order`](#lint_isort_section-order), imports in that section will be
assigned to the [`default-section`](#lint_isort_default-section) (which defaults to `third-party`).

---

##### `singleLineExclusions`<sup>Optional</sup> <a name="singleLineExclusions" id="projen.python.IsortOptions.property.singleLineExclusions"></a>

```typescript
public readonly singleLineExclusions: string[];
```

- *Type:* string[]

One or more modules to exclude from the single line rule.

---

##### `splitOnTrailingComma`<sup>Optional</sup> <a name="splitOnTrailingComma" id="projen.python.IsortOptions.property.splitOnTrailingComma"></a>

```typescript
public readonly splitOnTrailingComma: boolean;
```

- *Type:* boolean

If a comma is placed after the last member in a multi-line import, then the imports will never be folded into one line.

See isort's [`split-on-trailing-comma`](https://pycqa.github.io/isort/docs/configuration/options.html#split-on-trailing-comma) option.

When using the formatter, ensure that [`format.skip-magic-trailing-comma`](#format_skip-magic-trailing-comma) is set to `false` (default) when enabling `split-on-trailing-comma`
to avoid that the formatter removes the trailing commas.

---

##### `variables`<sup>Optional</sup> <a name="variables" id="projen.python.IsortOptions.property.variables"></a>

```typescript
public readonly variables: string[];
```

- *Type:* string[]

An override list of tokens to always recognize as a var for [`order-by-type`](#lint_isort_order-by-type) regardless of casing.

---

### LintOptions <a name="LintOptions" id="projen.python.LintOptions"></a>

Configures how Ruff checks your code.

Options specified in the `lint` section take precedence over the deprecated top-level settings.

#### Initializer <a name="Initializer" id="projen.python.LintOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const lintOptions: python.LintOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.LintOptions.property.allowedConfusables">allowedConfusables</a></code> | <code>string[]</code> | A list of allowed "confusable" Unicode characters to ignore when enforcing `RUF001`, `RUF002`, and `RUF003`. |
| <code><a href="#projen.python.LintOptions.property.dummyVariableRgx">dummyVariableRgx</a></code> | <code>string</code> | A regular expression used to identify "dummy" variables, or those which should be ignored when enforcing (e.g.) unused-variable rules. The default expression matches `_`, `__`, and `_var`, but not `_var_`. |
| <code><a href="#projen.python.LintOptions.property.exclude">exclude</a></code> | <code>string[]</code> | A list of file patterns to exclude from linting in addition to the files excluded globally (see [`exclude`](#exclude), and [`extend-exclude`](#extend-exclude)). |
| <code><a href="#projen.python.LintOptions.property.explicitPreviewRules">explicitPreviewRules</a></code> | <code>boolean</code> | Whether to require exact codes to select preview rules. |
| <code><a href="#projen.python.LintOptions.property.extendFixable">extendFixable</a></code> | <code><a href="#projen.python.RuleSelector">RuleSelector</a>[]</code> | A list of rule codes or prefixes to consider fixable, in addition to those specified by [`fixable`](#lint_fixable). |
| <code><a href="#projen.python.LintOptions.property.extendIgnore">extendIgnore</a></code> | <code><a href="#projen.python.RuleSelector">RuleSelector</a>[]</code> | A list of rule codes or prefixes to ignore, in addition to those specified by `ignore`. |
| <code><a href="#projen.python.LintOptions.property.extendPerFileIgnores">extendPerFileIgnores</a></code> | <code>{[ key: string ]: <a href="#projen.python.RuleSelector">RuleSelector</a>[]}</code> | A list of mappings from file pattern to rule codes or prefixes to exclude, in addition to any rules excluded by [`per-file-ignores`](#lint_per-file-ignores). |
| <code><a href="#projen.python.LintOptions.property.extendSafeFixes">extendSafeFixes</a></code> | <code><a href="#projen.python.RuleSelector">RuleSelector</a>[]</code> | A list of rule codes or prefixes for which unsafe fixes should be considered safe. |
| <code><a href="#projen.python.LintOptions.property.extendSelect">extendSelect</a></code> | <code><a href="#projen.python.RuleSelector">RuleSelector</a>[]</code> | A list of rule codes or prefixes to enable, in addition to those specified by [`select`](#lint_select). |
| <code><a href="#projen.python.LintOptions.property.extendUnfixable">extendUnfixable</a></code> | <code><a href="#projen.python.RuleSelector">RuleSelector</a>[]</code> | A list of rule codes or prefixes to consider non-auto-fixable, in addition to those specified by [`unfixable`](#lint_unfixable). |
| <code><a href="#projen.python.LintOptions.property.extendUnsafeFixes">extendUnsafeFixes</a></code> | <code><a href="#projen.python.RuleSelector">RuleSelector</a>[]</code> | A list of rule codes or prefixes for which safe fixes should be considered unsafe. |
| <code><a href="#projen.python.LintOptions.property.external">external</a></code> | <code>string[]</code> | A list of rule codes or prefixes that are unsupported by Ruff, but should be preserved when (e.g.) validating `# noqa` directives. Useful for retaining `# noqa` directives that cover plugins not yet implemented by Ruff. |
| <code><a href="#projen.python.LintOptions.property.fixable">fixable</a></code> | <code><a href="#projen.python.RuleSelector">RuleSelector</a>[]</code> | A list of rule codes or prefixes to consider fixable. |
| <code><a href="#projen.python.LintOptions.property.flake8Annotations">flake8Annotations</a></code> | <code><a href="#projen.python.Flake8AnnotationsOptions">Flake8AnnotationsOptions</a></code> | Options for the `flake8-annotations` plugin. |
| <code><a href="#projen.python.LintOptions.property.flake8Bandit">flake8Bandit</a></code> | <code><a href="#projen.python.Flake8BanditOptions">Flake8BanditOptions</a></code> | Options for the `flake8-bandit` plugin. |
| <code><a href="#projen.python.LintOptions.property.flake8BooleanTrap">flake8BooleanTrap</a></code> | <code><a href="#projen.python.Flake8BooleanTrapOptions">Flake8BooleanTrapOptions</a></code> | Options for the `flake8-boolean-trap` plugin. |
| <code><a href="#projen.python.LintOptions.property.flake8Bugbear">flake8Bugbear</a></code> | <code><a href="#projen.python.Flake8BugbearOptions">Flake8BugbearOptions</a></code> | Options for the `flake8-bugbear` plugin. |
| <code><a href="#projen.python.LintOptions.property.flake8Builtins">flake8Builtins</a></code> | <code><a href="#projen.python.Flake8BuiltinsOptions">Flake8BuiltinsOptions</a></code> | Options for the `flake8-builtins` plugin. |
| <code><a href="#projen.python.LintOptions.property.flake8Comprehensions">flake8Comprehensions</a></code> | <code><a href="#projen.python.Flake8ComprehensionsOptions">Flake8ComprehensionsOptions</a></code> | Options for the `flake8-comprehensions` plugin. |
| <code><a href="#projen.python.LintOptions.property.flake8Copyright">flake8Copyright</a></code> | <code><a href="#projen.python.Flake8CopyrightOptions">Flake8CopyrightOptions</a></code> | Options for the `flake8-copyright` plugin. |
| <code><a href="#projen.python.LintOptions.property.flake8Errmsg">flake8Errmsg</a></code> | <code><a href="#projen.python.Flake8ErrMsgOptions">Flake8ErrMsgOptions</a></code> | Options for the `flake8-errmsg` plugin. |
| <code><a href="#projen.python.LintOptions.property.flake8Gettext">flake8Gettext</a></code> | <code><a href="#projen.python.Flake8GetTextOptions">Flake8GetTextOptions</a></code> | Options for the `flake8-gettext` plugin. |
| <code><a href="#projen.python.LintOptions.property.flake8ImplicitStrConcat">flake8ImplicitStrConcat</a></code> | <code><a href="#projen.python.Flake8ImplicitStrConcatOptions">Flake8ImplicitStrConcatOptions</a></code> | Options for the `flake8-implicit-str-concat` plugin. |
| <code><a href="#projen.python.LintOptions.property.flake8ImportConventions">flake8ImportConventions</a></code> | <code><a href="#projen.python.Flake8ImportConventionsOptions">Flake8ImportConventionsOptions</a></code> | Options for the `flake8-import-conventions` plugin. |
| <code><a href="#projen.python.LintOptions.property.flake8PytestStyle">flake8PytestStyle</a></code> | <code><a href="#projen.python.Flake8PytestStyleOptions">Flake8PytestStyleOptions</a></code> | Options for the `flake8-pytest-style` plugin. |
| <code><a href="#projen.python.LintOptions.property.flake8Quotes">flake8Quotes</a></code> | <code><a href="#projen.python.Flake8QuotesOptions">Flake8QuotesOptions</a></code> | Options for the `flake8-quotes` plugin. |
| <code><a href="#projen.python.LintOptions.property.flake8Self">flake8Self</a></code> | <code><a href="#projen.python.Flake8SelfOptions">Flake8SelfOptions</a></code> | Options for the `flake8_self` plugin. |
| <code><a href="#projen.python.LintOptions.property.flake8TidyImports">flake8TidyImports</a></code> | <code><a href="#projen.python.Flake8TidyImportsOptions">Flake8TidyImportsOptions</a></code> | Options for the `flake8-tidy-imports` plugin. |
| <code><a href="#projen.python.LintOptions.property.flake8TypeChecking">flake8TypeChecking</a></code> | <code><a href="#projen.python.Flake8TypeCheckingOptions">Flake8TypeCheckingOptions</a></code> | Options for the `flake8-type-checking` plugin. |
| <code><a href="#projen.python.LintOptions.property.flake8UnusedArguments">flake8UnusedArguments</a></code> | <code><a href="#projen.python.Flake8UnusedArgumentsOptions">Flake8UnusedArgumentsOptions</a></code> | Options for the `flake8-unused-arguments` plugin. |
| <code><a href="#projen.python.LintOptions.property.futureAnnotations">futureAnnotations</a></code> | <code>boolean</code> | Whether to allow rules to add `from __future__ import annotations` in cases where this would simplify a fix or enable a new diagnostic. |
| <code><a href="#projen.python.LintOptions.property.ignore">ignore</a></code> | <code><a href="#projen.python.RuleSelector">RuleSelector</a>[]</code> | A list of rule codes or prefixes to ignore. |
| <code><a href="#projen.python.LintOptions.property.ignoreInitModuleImports">ignoreInitModuleImports</a></code> | <code>boolean</code> | Avoid automatically removing unused imports in `__init__.py` files. Such imports will still be flagged, but with a dedicated message suggesting that the import is either added to the module's `__all__` symbol, or re-exported with a redundant alias (e.g., `import os as os`). |
| <code><a href="#projen.python.LintOptions.property.isort">isort</a></code> | <code><a href="#projen.python.IsortOptions">IsortOptions</a></code> | Options for the `isort` plugin. |
| <code><a href="#projen.python.LintOptions.property.loggerObjects">loggerObjects</a></code> | <code>string[]</code> | A list of objects that should be treated equivalently to a `logging.Logger` object. |
| <code><a href="#projen.python.LintOptions.property.mccabe">mccabe</a></code> | <code><a href="#projen.python.McCabeOptions">McCabeOptions</a></code> | Options for the `mccabe` plugin. |
| <code><a href="#projen.python.LintOptions.property.pep8Naming">pep8Naming</a></code> | <code><a href="#projen.python.Pep8NamingOptions">Pep8NamingOptions</a></code> | Options for the `pep8-naming` plugin. |
| <code><a href="#projen.python.LintOptions.property.perFileIgnores">perFileIgnores</a></code> | <code>{[ key: string ]: <a href="#projen.python.RuleSelector">RuleSelector</a>[]}</code> | A list of mappings from file pattern to rule codes or prefixes to exclude, when considering any matching files. |
| <code><a href="#projen.python.LintOptions.property.preview">preview</a></code> | <code>boolean</code> | Whether to enable preview mode. |
| <code><a href="#projen.python.LintOptions.property.pycodestyle">pycodestyle</a></code> | <code><a href="#projen.python.PycodestyleOptions">PycodestyleOptions</a></code> | Options for the `pycodestyle` plugin. |
| <code><a href="#projen.python.LintOptions.property.pydoclint">pydoclint</a></code> | <code><a href="#projen.python.PydoclintOptions">PydoclintOptions</a></code> | Options for the `pydoclint` plugin. |
| <code><a href="#projen.python.LintOptions.property.pydocstyle">pydocstyle</a></code> | <code><a href="#projen.python.PydocstyleOptions">PydocstyleOptions</a></code> | Options for the `pydocstyle` plugin. |
| <code><a href="#projen.python.LintOptions.property.pyflakes">pyflakes</a></code> | <code><a href="#projen.python.PyflakesOptions">PyflakesOptions</a></code> | Options for the `pyflakes` plugin. |
| <code><a href="#projen.python.LintOptions.property.pylint">pylint</a></code> | <code><a href="#projen.python.PylintOptions">PylintOptions</a></code> | Options for the `pylint` plugin. |
| <code><a href="#projen.python.LintOptions.property.pyupgrade">pyupgrade</a></code> | <code><a href="#projen.python.PyUpgradeOptions">PyUpgradeOptions</a></code> | Options for the `pyupgrade` plugin. |
| <code><a href="#projen.python.LintOptions.property.ruff">ruff</a></code> | <code><a href="#projen.python.RuffOptions">RuffOptions</a></code> | Options for the `ruff` plugin. |
| <code><a href="#projen.python.LintOptions.property.select">select</a></code> | <code><a href="#projen.python.RuleSelector">RuleSelector</a>[]</code> | A list of rule codes or prefixes to enable. |
| <code><a href="#projen.python.LintOptions.property.taskTags">taskTags</a></code> | <code>string[]</code> | A list of task tags to recognize (e.g., "TODO", "FIXME", "XXX"). |
| <code><a href="#projen.python.LintOptions.property.typingExtensions">typingExtensions</a></code> | <code>boolean</code> | Whether to allow imports from the third-party `typing_extensions` module for Python versions before a symbol was added to the first-party `typing` module. |
| <code><a href="#projen.python.LintOptions.property.typingModules">typingModules</a></code> | <code>string[]</code> | A list of modules whose exports should be treated equivalently to members of the `typing` module. |
| <code><a href="#projen.python.LintOptions.property.unfixable">unfixable</a></code> | <code><a href="#projen.python.RuleSelector">RuleSelector</a>[]</code> | A list of rule codes or prefixes to consider non-fixable. |

---

##### `allowedConfusables`<sup>Optional</sup> <a name="allowedConfusables" id="projen.python.LintOptions.property.allowedConfusables"></a>

```typescript
public readonly allowedConfusables: string[];
```

- *Type:* string[]

A list of allowed "confusable" Unicode characters to ignore when enforcing `RUF001`, `RUF002`, and `RUF003`.

---

##### `dummyVariableRgx`<sup>Optional</sup> <a name="dummyVariableRgx" id="projen.python.LintOptions.property.dummyVariableRgx"></a>

```typescript
public readonly dummyVariableRgx: string;
```

- *Type:* string

A regular expression used to identify "dummy" variables, or those which should be ignored when enforcing (e.g.) unused-variable rules. The default expression matches `_`, `__`, and `_var`, but not `_var_`.

---

##### `exclude`<sup>Optional</sup> <a name="exclude" id="projen.python.LintOptions.property.exclude"></a>

```typescript
public readonly exclude: string[];
```

- *Type:* string[]

A list of file patterns to exclude from linting in addition to the files excluded globally (see [`exclude`](#exclude), and [`extend-exclude`](#extend-exclude)).

Exclusions are based on globs, and can be either:

- Single-path patterns, like `.mypy_cache` (to exclude any directory
named `.mypy_cache` in the tree), `foo.py` (to exclude any file named
`foo.py`), or `foo_*.py` (to exclude any file matching `foo_*.py` ).
- Relative patterns, like `directory/foo.py` (to exclude that specific
file) or `directory/*.py` (to exclude any Python files in
`directory`). Note that these paths are relative to the project root
(e.g., the directory containing your `pyproject.toml`).

For more information on the glob syntax, refer to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).

---

##### `explicitPreviewRules`<sup>Optional</sup> <a name="explicitPreviewRules" id="projen.python.LintOptions.property.explicitPreviewRules"></a>

```typescript
public readonly explicitPreviewRules: boolean;
```

- *Type:* boolean

Whether to require exact codes to select preview rules.

When enabled,
preview rules will not be selected by prefixes — the full code of each
preview rule will be required to enable the rule.

---

##### `extendFixable`<sup>Optional</sup> <a name="extendFixable" id="projen.python.LintOptions.property.extendFixable"></a>

```typescript
public readonly extendFixable: RuleSelector[];
```

- *Type:* <a href="#projen.python.RuleSelector">RuleSelector</a>[]

A list of rule codes or prefixes to consider fixable, in addition to those specified by [`fixable`](#lint_fixable).

---

##### `extendIgnore`<sup>Optional</sup> <a name="extendIgnore" id="projen.python.LintOptions.property.extendIgnore"></a>

```typescript
public readonly extendIgnore: RuleSelector[];
```

- *Type:* <a href="#projen.python.RuleSelector">RuleSelector</a>[]

A list of rule codes or prefixes to ignore, in addition to those specified by `ignore`.

---

##### `extendPerFileIgnores`<sup>Optional</sup> <a name="extendPerFileIgnores" id="projen.python.LintOptions.property.extendPerFileIgnores"></a>

```typescript
public readonly extendPerFileIgnores: {[ key: string ]: RuleSelector[]};
```

- *Type:* {[ key: string ]: <a href="#projen.python.RuleSelector">RuleSelector</a>[]}

A list of mappings from file pattern to rule codes or prefixes to exclude, in addition to any rules excluded by [`per-file-ignores`](#lint_per-file-ignores).

---

##### `extendSafeFixes`<sup>Optional</sup> <a name="extendSafeFixes" id="projen.python.LintOptions.property.extendSafeFixes"></a>

```typescript
public readonly extendSafeFixes: RuleSelector[];
```

- *Type:* <a href="#projen.python.RuleSelector">RuleSelector</a>[]

A list of rule codes or prefixes for which unsafe fixes should be considered safe.

---

##### `extendSelect`<sup>Optional</sup> <a name="extendSelect" id="projen.python.LintOptions.property.extendSelect"></a>

```typescript
public readonly extendSelect: RuleSelector[];
```

- *Type:* <a href="#projen.python.RuleSelector">RuleSelector</a>[]

A list of rule codes or prefixes to enable, in addition to those specified by [`select`](#lint_select).

---

##### `extendUnfixable`<sup>Optional</sup> <a name="extendUnfixable" id="projen.python.LintOptions.property.extendUnfixable"></a>

```typescript
public readonly extendUnfixable: RuleSelector[];
```

- *Type:* <a href="#projen.python.RuleSelector">RuleSelector</a>[]

A list of rule codes or prefixes to consider non-auto-fixable, in addition to those specified by [`unfixable`](#lint_unfixable).

---

##### `extendUnsafeFixes`<sup>Optional</sup> <a name="extendUnsafeFixes" id="projen.python.LintOptions.property.extendUnsafeFixes"></a>

```typescript
public readonly extendUnsafeFixes: RuleSelector[];
```

- *Type:* <a href="#projen.python.RuleSelector">RuleSelector</a>[]

A list of rule codes or prefixes for which safe fixes should be considered unsafe.

---

##### `external`<sup>Optional</sup> <a name="external" id="projen.python.LintOptions.property.external"></a>

```typescript
public readonly external: string[];
```

- *Type:* string[]

A list of rule codes or prefixes that are unsupported by Ruff, but should be preserved when (e.g.) validating `# noqa` directives. Useful for retaining `# noqa` directives that cover plugins not yet implemented by Ruff.

---

##### `fixable`<sup>Optional</sup> <a name="fixable" id="projen.python.LintOptions.property.fixable"></a>

```typescript
public readonly fixable: RuleSelector[];
```

- *Type:* <a href="#projen.python.RuleSelector">RuleSelector</a>[]

A list of rule codes or prefixes to consider fixable.

By default,
all rules are considered fixable.

---

##### `flake8Annotations`<sup>Optional</sup> <a name="flake8Annotations" id="projen.python.LintOptions.property.flake8Annotations"></a>

```typescript
public readonly flake8Annotations: Flake8AnnotationsOptions;
```

- *Type:* <a href="#projen.python.Flake8AnnotationsOptions">Flake8AnnotationsOptions</a>

Options for the `flake8-annotations` plugin.

---

##### `flake8Bandit`<sup>Optional</sup> <a name="flake8Bandit" id="projen.python.LintOptions.property.flake8Bandit"></a>

```typescript
public readonly flake8Bandit: Flake8BanditOptions;
```

- *Type:* <a href="#projen.python.Flake8BanditOptions">Flake8BanditOptions</a>

Options for the `flake8-bandit` plugin.

---

##### `flake8BooleanTrap`<sup>Optional</sup> <a name="flake8BooleanTrap" id="projen.python.LintOptions.property.flake8BooleanTrap"></a>

```typescript
public readonly flake8BooleanTrap: Flake8BooleanTrapOptions;
```

- *Type:* <a href="#projen.python.Flake8BooleanTrapOptions">Flake8BooleanTrapOptions</a>

Options for the `flake8-boolean-trap` plugin.

---

##### `flake8Bugbear`<sup>Optional</sup> <a name="flake8Bugbear" id="projen.python.LintOptions.property.flake8Bugbear"></a>

```typescript
public readonly flake8Bugbear: Flake8BugbearOptions;
```

- *Type:* <a href="#projen.python.Flake8BugbearOptions">Flake8BugbearOptions</a>

Options for the `flake8-bugbear` plugin.

---

##### `flake8Builtins`<sup>Optional</sup> <a name="flake8Builtins" id="projen.python.LintOptions.property.flake8Builtins"></a>

```typescript
public readonly flake8Builtins: Flake8BuiltinsOptions;
```

- *Type:* <a href="#projen.python.Flake8BuiltinsOptions">Flake8BuiltinsOptions</a>

Options for the `flake8-builtins` plugin.

---

##### `flake8Comprehensions`<sup>Optional</sup> <a name="flake8Comprehensions" id="projen.python.LintOptions.property.flake8Comprehensions"></a>

```typescript
public readonly flake8Comprehensions: Flake8ComprehensionsOptions;
```

- *Type:* <a href="#projen.python.Flake8ComprehensionsOptions">Flake8ComprehensionsOptions</a>

Options for the `flake8-comprehensions` plugin.

---

##### `flake8Copyright`<sup>Optional</sup> <a name="flake8Copyright" id="projen.python.LintOptions.property.flake8Copyright"></a>

```typescript
public readonly flake8Copyright: Flake8CopyrightOptions;
```

- *Type:* <a href="#projen.python.Flake8CopyrightOptions">Flake8CopyrightOptions</a>

Options for the `flake8-copyright` plugin.

---

##### `flake8Errmsg`<sup>Optional</sup> <a name="flake8Errmsg" id="projen.python.LintOptions.property.flake8Errmsg"></a>

```typescript
public readonly flake8Errmsg: Flake8ErrMsgOptions;
```

- *Type:* <a href="#projen.python.Flake8ErrMsgOptions">Flake8ErrMsgOptions</a>

Options for the `flake8-errmsg` plugin.

---

##### `flake8Gettext`<sup>Optional</sup> <a name="flake8Gettext" id="projen.python.LintOptions.property.flake8Gettext"></a>

```typescript
public readonly flake8Gettext: Flake8GetTextOptions;
```

- *Type:* <a href="#projen.python.Flake8GetTextOptions">Flake8GetTextOptions</a>

Options for the `flake8-gettext` plugin.

---

##### `flake8ImplicitStrConcat`<sup>Optional</sup> <a name="flake8ImplicitStrConcat" id="projen.python.LintOptions.property.flake8ImplicitStrConcat"></a>

```typescript
public readonly flake8ImplicitStrConcat: Flake8ImplicitStrConcatOptions;
```

- *Type:* <a href="#projen.python.Flake8ImplicitStrConcatOptions">Flake8ImplicitStrConcatOptions</a>

Options for the `flake8-implicit-str-concat` plugin.

---

##### `flake8ImportConventions`<sup>Optional</sup> <a name="flake8ImportConventions" id="projen.python.LintOptions.property.flake8ImportConventions"></a>

```typescript
public readonly flake8ImportConventions: Flake8ImportConventionsOptions;
```

- *Type:* <a href="#projen.python.Flake8ImportConventionsOptions">Flake8ImportConventionsOptions</a>

Options for the `flake8-import-conventions` plugin.

---

##### `flake8PytestStyle`<sup>Optional</sup> <a name="flake8PytestStyle" id="projen.python.LintOptions.property.flake8PytestStyle"></a>

```typescript
public readonly flake8PytestStyle: Flake8PytestStyleOptions;
```

- *Type:* <a href="#projen.python.Flake8PytestStyleOptions">Flake8PytestStyleOptions</a>

Options for the `flake8-pytest-style` plugin.

---

##### `flake8Quotes`<sup>Optional</sup> <a name="flake8Quotes" id="projen.python.LintOptions.property.flake8Quotes"></a>

```typescript
public readonly flake8Quotes: Flake8QuotesOptions;
```

- *Type:* <a href="#projen.python.Flake8QuotesOptions">Flake8QuotesOptions</a>

Options for the `flake8-quotes` plugin.

---

##### `flake8Self`<sup>Optional</sup> <a name="flake8Self" id="projen.python.LintOptions.property.flake8Self"></a>

```typescript
public readonly flake8Self: Flake8SelfOptions;
```

- *Type:* <a href="#projen.python.Flake8SelfOptions">Flake8SelfOptions</a>

Options for the `flake8_self` plugin.

---

##### `flake8TidyImports`<sup>Optional</sup> <a name="flake8TidyImports" id="projen.python.LintOptions.property.flake8TidyImports"></a>

```typescript
public readonly flake8TidyImports: Flake8TidyImportsOptions;
```

- *Type:* <a href="#projen.python.Flake8TidyImportsOptions">Flake8TidyImportsOptions</a>

Options for the `flake8-tidy-imports` plugin.

---

##### `flake8TypeChecking`<sup>Optional</sup> <a name="flake8TypeChecking" id="projen.python.LintOptions.property.flake8TypeChecking"></a>

```typescript
public readonly flake8TypeChecking: Flake8TypeCheckingOptions;
```

- *Type:* <a href="#projen.python.Flake8TypeCheckingOptions">Flake8TypeCheckingOptions</a>

Options for the `flake8-type-checking` plugin.

---

##### `flake8UnusedArguments`<sup>Optional</sup> <a name="flake8UnusedArguments" id="projen.python.LintOptions.property.flake8UnusedArguments"></a>

```typescript
public readonly flake8UnusedArguments: Flake8UnusedArgumentsOptions;
```

- *Type:* <a href="#projen.python.Flake8UnusedArgumentsOptions">Flake8UnusedArgumentsOptions</a>

Options for the `flake8-unused-arguments` plugin.

---

##### `futureAnnotations`<sup>Optional</sup> <a name="futureAnnotations" id="projen.python.LintOptions.property.futureAnnotations"></a>

```typescript
public readonly futureAnnotations: boolean;
```

- *Type:* boolean

Whether to allow rules to add `from __future__ import annotations` in cases where this would simplify a fix or enable a new diagnostic.

For example, `TC001`, `TC002`, and `TC003` can move more imports into `TYPE_CHECKING` blocks
if `__future__` annotations are enabled.

---

##### `ignore`<sup>Optional</sup> <a name="ignore" id="projen.python.LintOptions.property.ignore"></a>

```typescript
public readonly ignore: RuleSelector[];
```

- *Type:* <a href="#projen.python.RuleSelector">RuleSelector</a>[]

A list of rule codes or prefixes to ignore.

Prefixes can specify exact
rules (like `F841`), entire categories (like `F`), or anything in
between.

When breaking ties between enabled and disabled rules (via `select` and
`ignore`, respectively), more specific prefixes override less
specific prefixes. `ignore` takes precedence over `select` if the same
prefix appears in both.

---

##### `ignoreInitModuleImports`<sup>Optional</sup> <a name="ignoreInitModuleImports" id="projen.python.LintOptions.property.ignoreInitModuleImports"></a>

```typescript
public readonly ignoreInitModuleImports: boolean;
```

- *Type:* boolean

Avoid automatically removing unused imports in `__init__.py` files. Such imports will still be flagged, but with a dedicated message suggesting that the import is either added to the module's `__all__` symbol, or re-exported with a redundant alias (e.g., `import os as os`).

This option is enabled by default, but you can opt-in to removal of imports
via an unsafe fix.

---

##### `isort`<sup>Optional</sup> <a name="isort" id="projen.python.LintOptions.property.isort"></a>

```typescript
public readonly isort: IsortOptions;
```

- *Type:* <a href="#projen.python.IsortOptions">IsortOptions</a>

Options for the `isort` plugin.

---

##### `loggerObjects`<sup>Optional</sup> <a name="loggerObjects" id="projen.python.LintOptions.property.loggerObjects"></a>

```typescript
public readonly loggerObjects: string[];
```

- *Type:* string[]

A list of objects that should be treated equivalently to a `logging.Logger` object.

This is useful for ensuring proper diagnostics (e.g., to identify
`logging` deprecations and other best-practices) for projects that
re-export a `logging.Logger` object from a common module.

For example, if you have a module `logging_setup.py` with the following
contents:
```python
import logging

logger = logging.getLogger(__name__)
```

Adding `"logging_setup.logger"` to `logger-objects` will ensure that
`logging_setup.logger` is treated as a `logging.Logger` object when
imported from other modules (e.g., `from logging_setup import logger`).

---

##### `mccabe`<sup>Optional</sup> <a name="mccabe" id="projen.python.LintOptions.property.mccabe"></a>

```typescript
public readonly mccabe: McCabeOptions;
```

- *Type:* <a href="#projen.python.McCabeOptions">McCabeOptions</a>

Options for the `mccabe` plugin.

---

##### `pep8Naming`<sup>Optional</sup> <a name="pep8Naming" id="projen.python.LintOptions.property.pep8Naming"></a>

```typescript
public readonly pep8Naming: Pep8NamingOptions;
```

- *Type:* <a href="#projen.python.Pep8NamingOptions">Pep8NamingOptions</a>

Options for the `pep8-naming` plugin.

---

##### `perFileIgnores`<sup>Optional</sup> <a name="perFileIgnores" id="projen.python.LintOptions.property.perFileIgnores"></a>

```typescript
public readonly perFileIgnores: {[ key: string ]: RuleSelector[]};
```

- *Type:* {[ key: string ]: <a href="#projen.python.RuleSelector">RuleSelector</a>[]}

A list of mappings from file pattern to rule codes or prefixes to exclude, when considering any matching files.

An initial '!' negates
the file pattern.

---

##### `preview`<sup>Optional</sup> <a name="preview" id="projen.python.LintOptions.property.preview"></a>

```typescript
public readonly preview: boolean;
```

- *Type:* boolean

Whether to enable preview mode.

When preview mode is enabled, Ruff will
use unstable rules and fixes.

---

##### `pycodestyle`<sup>Optional</sup> <a name="pycodestyle" id="projen.python.LintOptions.property.pycodestyle"></a>

```typescript
public readonly pycodestyle: PycodestyleOptions;
```

- *Type:* <a href="#projen.python.PycodestyleOptions">PycodestyleOptions</a>

Options for the `pycodestyle` plugin.

---

##### `pydoclint`<sup>Optional</sup> <a name="pydoclint" id="projen.python.LintOptions.property.pydoclint"></a>

```typescript
public readonly pydoclint: PydoclintOptions;
```

- *Type:* <a href="#projen.python.PydoclintOptions">PydoclintOptions</a>

Options for the `pydoclint` plugin.

---

##### `pydocstyle`<sup>Optional</sup> <a name="pydocstyle" id="projen.python.LintOptions.property.pydocstyle"></a>

```typescript
public readonly pydocstyle: PydocstyleOptions;
```

- *Type:* <a href="#projen.python.PydocstyleOptions">PydocstyleOptions</a>

Options for the `pydocstyle` plugin.

---

##### `pyflakes`<sup>Optional</sup> <a name="pyflakes" id="projen.python.LintOptions.property.pyflakes"></a>

```typescript
public readonly pyflakes: PyflakesOptions;
```

- *Type:* <a href="#projen.python.PyflakesOptions">PyflakesOptions</a>

Options for the `pyflakes` plugin.

---

##### `pylint`<sup>Optional</sup> <a name="pylint" id="projen.python.LintOptions.property.pylint"></a>

```typescript
public readonly pylint: PylintOptions;
```

- *Type:* <a href="#projen.python.PylintOptions">PylintOptions</a>

Options for the `pylint` plugin.

---

##### `pyupgrade`<sup>Optional</sup> <a name="pyupgrade" id="projen.python.LintOptions.property.pyupgrade"></a>

```typescript
public readonly pyupgrade: PyUpgradeOptions;
```

- *Type:* <a href="#projen.python.PyUpgradeOptions">PyUpgradeOptions</a>

Options for the `pyupgrade` plugin.

---

##### `ruff`<sup>Optional</sup> <a name="ruff" id="projen.python.LintOptions.property.ruff"></a>

```typescript
public readonly ruff: RuffOptions;
```

- *Type:* <a href="#projen.python.RuffOptions">RuffOptions</a>

Options for the `ruff` plugin.

---

##### `select`<sup>Optional</sup> <a name="select" id="projen.python.LintOptions.property.select"></a>

```typescript
public readonly select: RuleSelector[];
```

- *Type:* <a href="#projen.python.RuleSelector">RuleSelector</a>[]

A list of rule codes or prefixes to enable.

Prefixes can specify exact
rules (like `F841`), entire categories (like `F`), or anything in
between.

When breaking ties between enabled and disabled rules (via `select` and
`ignore`, respectively), more specific prefixes override less
specific prefixes. `ignore` takes precedence over `select` if the
same prefix appears in both.

---

##### `taskTags`<sup>Optional</sup> <a name="taskTags" id="projen.python.LintOptions.property.taskTags"></a>

```typescript
public readonly taskTags: string[];
```

- *Type:* string[]

A list of task tags to recognize (e.g., "TODO", "FIXME", "XXX").

Comments starting with these tags will be ignored by commented-out code
detection (`ERA`), and skipped by line-length rules (`E501`) if
[`ignore-overlong-task-comments`](#lint_pycodestyle_ignore-overlong-task-comments) is set to `true`.

---

##### `typingExtensions`<sup>Optional</sup> <a name="typingExtensions" id="projen.python.LintOptions.property.typingExtensions"></a>

```typescript
public readonly typingExtensions: boolean;
```

- *Type:* boolean

Whether to allow imports from the third-party `typing_extensions` module for Python versions before a symbol was added to the first-party `typing` module.

Many rules try to import symbols from the `typing` module but fall back to
`typing_extensions` for earlier versions of Python. This option can be used to disable this
fallback behavior in cases where `typing_extensions` is not installed.

---

##### `typingModules`<sup>Optional</sup> <a name="typingModules" id="projen.python.LintOptions.property.typingModules"></a>

```typescript
public readonly typingModules: string[];
```

- *Type:* string[]

A list of modules whose exports should be treated equivalently to members of the `typing` module.

This is useful for ensuring proper type annotation inference for
projects that re-export `typing` and `typing_extensions` members
from a compatibility module. If omitted, any members imported from
modules apart from `typing` and `typing_extensions` will be treated
as ordinary Python objects.

---

##### `unfixable`<sup>Optional</sup> <a name="unfixable" id="projen.python.LintOptions.property.unfixable"></a>

```typescript
public readonly unfixable: RuleSelector[];
```

- *Type:* <a href="#projen.python.RuleSelector">RuleSelector</a>[]

A list of rule codes or prefixes to consider non-fixable.

---

### McCabeOptions <a name="McCabeOptions" id="projen.python.McCabeOptions"></a>

Options for the `mccabe` plugin.

#### Initializer <a name="Initializer" id="projen.python.McCabeOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const mcCabeOptions: python.McCabeOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.McCabeOptions.property.maxComplexity">maxComplexity</a></code> | <code>number</code> | The maximum McCabe complexity to allow before triggering `C901` errors. |

---

##### `maxComplexity`<sup>Optional</sup> <a name="maxComplexity" id="projen.python.McCabeOptions.property.maxComplexity"></a>

```typescript
public readonly maxComplexity: number;
```

- *Type:* number

The maximum McCabe complexity to allow before triggering `C901` errors.

---

### Metadata <a name="Metadata" id="projen.python.Metadata"></a>

Metadata for the project.

#### Initializer <a name="Initializer" id="projen.python.Metadata.Initializer"></a>

```typescript
import { python } from 'projen'

const metadata: python.Metadata = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Metadata.property.allowAmbiguousFeatures">allowAmbiguousFeatures</a></code> | <code>boolean</code> | Whether to allow ambiguous features. |
| <code><a href="#projen.python.Metadata.property.allowDirectReferences">allowDirectReferences</a></code> | <code>boolean</code> | Whether to allow direct references. |
| <code><a href="#projen.python.Metadata.property.hooks">hooks</a></code> | <code>any</code> | Configuration for plugin hooks that allow for the modification of project metadata after it has been loaded. |

---

##### `allowAmbiguousFeatures`<sup>Optional</sup> <a name="allowAmbiguousFeatures" id="projen.python.Metadata.property.allowAmbiguousFeatures"></a>

```typescript
public readonly allowAmbiguousFeatures: boolean;
```

- *Type:* boolean

Whether to allow ambiguous features.

---

##### `allowDirectReferences`<sup>Optional</sup> <a name="allowDirectReferences" id="projen.python.Metadata.property.allowDirectReferences"></a>

```typescript
public readonly allowDirectReferences: boolean;
```

- *Type:* boolean

Whether to allow direct references.

---

##### `hooks`<sup>Optional</sup> <a name="hooks" id="projen.python.Metadata.property.hooks"></a>

```typescript
public readonly hooks: any;
```

- *Type:* any

Configuration for plugin hooks that allow for the modification of project metadata after it has been loaded.

---

### MypyConfiguration <a name="MypyConfiguration" id="projen.python.MypyConfiguration"></a>

#### Initializer <a name="Initializer" id="projen.python.MypyConfiguration.Initializer"></a>

```typescript
import { python } from 'projen'

const mypyConfiguration: python.MypyConfiguration = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.MypyConfiguration.property.allowRedefinition">allowRedefinition</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.allowRedefinitionNew">allowRedefinitionNew</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.allowUntypedGlobals">allowUntypedGlobals</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.alwaysFalse">alwaysFalse</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.alwaysTrue">alwaysTrue</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.anyExprsReport">anyExprsReport</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.cacheDir">cacheDir</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.cacheFineGrained">cacheFineGrained</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.checkUntypedDefs">checkUntypedDefs</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.coberturaXmlReport">coberturaXmlReport</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.colorOutput">colorOutput</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.customTypeshedDir">customTypeshedDir</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.customTypingModule">customTypingModule</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.disableBytearrayPromotion">disableBytearrayPromotion</a></code> | <code>boolean</code> | UNDOCUMENTED. |
| <code><a href="#projen.python.MypyConfiguration.property.disableErrorCode">disableErrorCode</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.disableMemoryviewPromotion">disableMemoryviewPromotion</a></code> | <code>boolean</code> | UNDOCUMENTED. |
| <code><a href="#projen.python.MypyConfiguration.property.disallowAnyDecorated">disallowAnyDecorated</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.disallowAnyExplicit">disallowAnyExplicit</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.disallowAnyExpr">disallowAnyExpr</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.disallowAnyGenerics">disallowAnyGenerics</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.disallowAnyUnimported">disallowAnyUnimported</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.disallowIncompleteDefs">disallowIncompleteDefs</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.disallowSubclassingAny">disallowSubclassingAny</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.disallowUntypedCalls">disallowUntypedCalls</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.disallowUntypedDecorators">disallowUntypedDecorators</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.disallowUntypedDefs">disallowUntypedDefs</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.enableErrorCode">enableErrorCode</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.enableIncompleteFeature">enableIncompleteFeature</a></code> | <code>string[]</code> | Enable a preview of incomplete features that are not yet enabled by default by the current version of mypy. |
| <code><a href="#projen.python.MypyConfiguration.property.errorSummary">errorSummary</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.exclude">exclude</a></code> | <code>string[]</code> | A regular expression that matches file names, directory names and paths which mypy should ignore while recursively discovering files to check. |
| <code><a href="#projen.python.MypyConfiguration.property.excludeGitignore">excludeGitignore</a></code> | <code>boolean</code> | This flag will add everything that matches `.gitignore` file(s) to `exclude` (https://mypy.readthedocs.io/en/stable/config_file.html#confval-exclude). This option may only be set in the global section (`[mypy]`). |
| <code><a href="#projen.python.MypyConfiguration.property.explicitPackageBases">explicitPackageBases</a></code> | <code>boolean</code> | This flag tells mypy that top-level packages will be based in either the current directory, or a member of the `MYPYPATH` environment variable or `mypy_path` config option. |
| <code><a href="#projen.python.MypyConfiguration.property.extraChecks">extraChecks</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.files">files</a></code> | <code>string[]</code> | A comma-separated list of paths which should be checked by mypy if none are given on the command line. |
| <code><a href="#projen.python.MypyConfiguration.property.fixedFormatCache">fixedFormatCache</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.followImports">followImports</a></code> | <code><a href="#projen.python.FollowImports">FollowImports</a></code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.followImportsForStubs">followImportsForStubs</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.followUntypedImports">followUntypedImports</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.forceUnionSyntax">forceUnionSyntax</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.forceUppercaseBuiltins">forceUppercaseBuiltins</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.hideErrorCodes">hideErrorCodes</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.ignoreErrors">ignoreErrors</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.ignoreMissingImports">ignoreMissingImports</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.implicitOptional">implicitOptional</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.implicitReexport">implicitReexport</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.incremental">incremental</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.installTypes">installTypes</a></code> | <code>boolean</code> | Install detected missing library stub packages using pip. |
| <code><a href="#projen.python.MypyConfiguration.property.junitXml">junitXml</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.linecountReport">linecountReport</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.linecoverageReport">linecoverageReport</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.lineprecisionReport">lineprecisionReport</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.localPartialTypes">localPartialTypes</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.modules">modules</a></code> | <code>string[]</code> | A comma-separated list of packages which should be checked by mypy if none are given on the command line. |
| <code><a href="#projen.python.MypyConfiguration.property.mypyPath">mypyPath</a></code> | <code>string[]</code> | Specifies the paths to use, after trying the paths from `MYPYPATH` environment variable. |
| <code><a href="#projen.python.MypyConfiguration.property.namespacePackages">namespacePackages</a></code> | <code>boolean</code> | Enables PEP 420 style namespace packages. |
| <code><a href="#projen.python.MypyConfiguration.property.noImplicitOptional">noImplicitOptional</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.noImplicitReexport">noImplicitReexport</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.nonInteractive">nonInteractive</a></code> | <code>boolean</code> | Install stubs without asking for confirmation and hide errors, with --install-types. |
| <code><a href="#projen.python.MypyConfiguration.property.noSilenceSitePackages">noSilenceSitePackages</a></code> | <code>boolean</code> | Enables reporting error messages generated within installed packages (see PEP 561 for more details on distributing type information). |
| <code><a href="#projen.python.MypyConfiguration.property.noSitePackages">noSitePackages</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.overrides">overrides</a></code> | <code><a href="#projen.python.MypyConfigurationOverrides">MypyConfigurationOverrides</a>[]</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.packages">packages</a></code> | <code>string[]</code> | A comma-separated list of packages which should be checked by mypy if none are given on the command line. |
| <code><a href="#projen.python.MypyConfiguration.property.pdb">pdb</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.platform">platform</a></code> | <code>string</code> | Specifies the OS platform for the target program, for example `darwin` or `win32` (meaning OS X or Windows, respectively). |
| <code><a href="#projen.python.MypyConfiguration.property.plugins">plugins</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.pretty">pretty</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.pythonExecutable">pythonExecutable</a></code> | <code>string</code> | Specifies the path to the Python executable to inspect to collect a list of available PEP 561 packages (https://mypy.readthedocs.io/en/stable/installed_packages.html#installed-packages). User home directory and environment variables will be expanded. Defaults to the executable used to run mypy. |
| <code><a href="#projen.python.MypyConfiguration.property.pythonVersion">pythonVersion</a></code> | <code>string</code> | Specifies the Python version used to parse and check the target program. |
| <code><a href="#projen.python.MypyConfiguration.property.raiseExceptions">raiseExceptions</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.scriptsAreModules">scriptsAreModules</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.showAbsolutePath">showAbsolutePath</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.showColumnNumbers">showColumnNumbers</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.showErrorCodeLinks">showErrorCodeLinks</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.showErrorCodes">showErrorCodes</a></code> | <code>boolean</code> | DEPRECATED and UNDOCUMENTED: Now defaults to true, use `hide_error_codes` if you need to disable error codes instead. |
| <code><a href="#projen.python.MypyConfiguration.property.showErrorContext">showErrorContext</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.showTraceback">showTraceback</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.skipCacheMtimeChecks">skipCacheMtimeChecks</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.skipVersionCheck">skipVersionCheck</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.sqliteCache">sqliteCache</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.strict">strict</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.strictBytes">strictBytes</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.strictConcatenate">strictConcatenate</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.strictEquality">strictEquality</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.strictEqualityForNone">strictEqualityForNone</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.strictOptional">strictOptional</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.untypedCallsExclude">untypedCallsExclude</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.verbosity">verbosity</a></code> | <code>number</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.warnIncompleteStub">warnIncompleteStub</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.warnNoReturn">warnNoReturn</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.warnRedundantCasts">warnRedundantCasts</a></code> | <code>boolean</code> | Warns about casting an expression to its inferred type. |
| <code><a href="#projen.python.MypyConfiguration.property.warnReturnAny">warnReturnAny</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.warnUnreachable">warnUnreachable</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.warnUnusedConfigs">warnUnusedConfigs</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.warnUnusedIgnores">warnUnusedIgnores</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfiguration.property.xmlReport">xmlReport</a></code> | <code>string</code> | *No description.* |

---

##### `allowRedefinition`<sup>Optional</sup> <a name="allowRedefinition" id="projen.python.MypyConfiguration.property.allowRedefinition"></a>

```typescript
public readonly allowRedefinition: boolean;
```

- *Type:* boolean

---

##### `allowRedefinitionNew`<sup>Optional</sup> <a name="allowRedefinitionNew" id="projen.python.MypyConfiguration.property.allowRedefinitionNew"></a>

```typescript
public readonly allowRedefinitionNew: boolean;
```

- *Type:* boolean

---

##### `allowUntypedGlobals`<sup>Optional</sup> <a name="allowUntypedGlobals" id="projen.python.MypyConfiguration.property.allowUntypedGlobals"></a>

```typescript
public readonly allowUntypedGlobals: boolean;
```

- *Type:* boolean

---

##### `alwaysFalse`<sup>Optional</sup> <a name="alwaysFalse" id="projen.python.MypyConfiguration.property.alwaysFalse"></a>

```typescript
public readonly alwaysFalse: string[];
```

- *Type:* string[]

---

##### `alwaysTrue`<sup>Optional</sup> <a name="alwaysTrue" id="projen.python.MypyConfiguration.property.alwaysTrue"></a>

```typescript
public readonly alwaysTrue: string[];
```

- *Type:* string[]

---

##### `anyExprsReport`<sup>Optional</sup> <a name="anyExprsReport" id="projen.python.MypyConfiguration.property.anyExprsReport"></a>

```typescript
public readonly anyExprsReport: string;
```

- *Type:* string

---

##### `cacheDir`<sup>Optional</sup> <a name="cacheDir" id="projen.python.MypyConfiguration.property.cacheDir"></a>

```typescript
public readonly cacheDir: string;
```

- *Type:* string

---

##### `cacheFineGrained`<sup>Optional</sup> <a name="cacheFineGrained" id="projen.python.MypyConfiguration.property.cacheFineGrained"></a>

```typescript
public readonly cacheFineGrained: boolean;
```

- *Type:* boolean

---

##### `checkUntypedDefs`<sup>Optional</sup> <a name="checkUntypedDefs" id="projen.python.MypyConfiguration.property.checkUntypedDefs"></a>

```typescript
public readonly checkUntypedDefs: boolean;
```

- *Type:* boolean

---

##### `coberturaXmlReport`<sup>Optional</sup> <a name="coberturaXmlReport" id="projen.python.MypyConfiguration.property.coberturaXmlReport"></a>

```typescript
public readonly coberturaXmlReport: string;
```

- *Type:* string

---

##### `colorOutput`<sup>Optional</sup> <a name="colorOutput" id="projen.python.MypyConfiguration.property.colorOutput"></a>

```typescript
public readonly colorOutput: boolean;
```

- *Type:* boolean

---

##### `customTypeshedDir`<sup>Optional</sup> <a name="customTypeshedDir" id="projen.python.MypyConfiguration.property.customTypeshedDir"></a>

```typescript
public readonly customTypeshedDir: string;
```

- *Type:* string

---

##### `customTypingModule`<sup>Optional</sup> <a name="customTypingModule" id="projen.python.MypyConfiguration.property.customTypingModule"></a>

```typescript
public readonly customTypingModule: string;
```

- *Type:* string

---

##### `disableBytearrayPromotion`<sup>Optional</sup> <a name="disableBytearrayPromotion" id="projen.python.MypyConfiguration.property.disableBytearrayPromotion"></a>

```typescript
public readonly disableBytearrayPromotion: boolean;
```

- *Type:* boolean

UNDOCUMENTED.

Disables automatic promotion of `bytearray` to `bytes` type. Is set to `true` in strict mode.

---

##### `disableErrorCode`<sup>Optional</sup> <a name="disableErrorCode" id="projen.python.MypyConfiguration.property.disableErrorCode"></a>

```typescript
public readonly disableErrorCode: string[];
```

- *Type:* string[]

---

##### `disableMemoryviewPromotion`<sup>Optional</sup> <a name="disableMemoryviewPromotion" id="projen.python.MypyConfiguration.property.disableMemoryviewPromotion"></a>

```typescript
public readonly disableMemoryviewPromotion: boolean;
```

- *Type:* boolean

UNDOCUMENTED.

Disables automatic promotion of `memoryview` to `bytes` type. Is set to `true` in strict mode.

---

##### `disallowAnyDecorated`<sup>Optional</sup> <a name="disallowAnyDecorated" id="projen.python.MypyConfiguration.property.disallowAnyDecorated"></a>

```typescript
public readonly disallowAnyDecorated: boolean;
```

- *Type:* boolean

---

##### `disallowAnyExplicit`<sup>Optional</sup> <a name="disallowAnyExplicit" id="projen.python.MypyConfiguration.property.disallowAnyExplicit"></a>

```typescript
public readonly disallowAnyExplicit: boolean;
```

- *Type:* boolean

---

##### `disallowAnyExpr`<sup>Optional</sup> <a name="disallowAnyExpr" id="projen.python.MypyConfiguration.property.disallowAnyExpr"></a>

```typescript
public readonly disallowAnyExpr: boolean;
```

- *Type:* boolean

---

##### `disallowAnyGenerics`<sup>Optional</sup> <a name="disallowAnyGenerics" id="projen.python.MypyConfiguration.property.disallowAnyGenerics"></a>

```typescript
public readonly disallowAnyGenerics: boolean;
```

- *Type:* boolean

---

##### `disallowAnyUnimported`<sup>Optional</sup> <a name="disallowAnyUnimported" id="projen.python.MypyConfiguration.property.disallowAnyUnimported"></a>

```typescript
public readonly disallowAnyUnimported: boolean;
```

- *Type:* boolean

---

##### `disallowIncompleteDefs`<sup>Optional</sup> <a name="disallowIncompleteDefs" id="projen.python.MypyConfiguration.property.disallowIncompleteDefs"></a>

```typescript
public readonly disallowIncompleteDefs: boolean;
```

- *Type:* boolean

---

##### `disallowSubclassingAny`<sup>Optional</sup> <a name="disallowSubclassingAny" id="projen.python.MypyConfiguration.property.disallowSubclassingAny"></a>

```typescript
public readonly disallowSubclassingAny: boolean;
```

- *Type:* boolean

---

##### `disallowUntypedCalls`<sup>Optional</sup> <a name="disallowUntypedCalls" id="projen.python.MypyConfiguration.property.disallowUntypedCalls"></a>

```typescript
public readonly disallowUntypedCalls: boolean;
```

- *Type:* boolean

---

##### `disallowUntypedDecorators`<sup>Optional</sup> <a name="disallowUntypedDecorators" id="projen.python.MypyConfiguration.property.disallowUntypedDecorators"></a>

```typescript
public readonly disallowUntypedDecorators: boolean;
```

- *Type:* boolean

---

##### `disallowUntypedDefs`<sup>Optional</sup> <a name="disallowUntypedDefs" id="projen.python.MypyConfiguration.property.disallowUntypedDefs"></a>

```typescript
public readonly disallowUntypedDefs: boolean;
```

- *Type:* boolean

---

##### `enableErrorCode`<sup>Optional</sup> <a name="enableErrorCode" id="projen.python.MypyConfiguration.property.enableErrorCode"></a>

```typescript
public readonly enableErrorCode: string[];
```

- *Type:* string[]

---

##### `enableIncompleteFeature`<sup>Optional</sup> <a name="enableIncompleteFeature" id="projen.python.MypyConfiguration.property.enableIncompleteFeature"></a>

```typescript
public readonly enableIncompleteFeature: string[];
```

- *Type:* string[]

Enable a preview of incomplete features that are not yet enabled by default by the current version of mypy.

Note that it is not guaranteed that all features will be ultimately enabled by default.

---

##### `errorSummary`<sup>Optional</sup> <a name="errorSummary" id="projen.python.MypyConfiguration.property.errorSummary"></a>

```typescript
public readonly errorSummary: boolean;
```

- *Type:* boolean

---

##### `exclude`<sup>Optional</sup> <a name="exclude" id="projen.python.MypyConfiguration.property.exclude"></a>

```typescript
public readonly exclude: string[];
```

- *Type:* string[]

A regular expression that matches file names, directory names and paths which mypy should ignore while recursively discovering files to check.

Use forward slashes (`/`) as directory separators on all platforms.

---

##### `excludeGitignore`<sup>Optional</sup> <a name="excludeGitignore" id="projen.python.MypyConfiguration.property.excludeGitignore"></a>

```typescript
public readonly excludeGitignore: boolean;
```

- *Type:* boolean

This flag will add everything that matches `.gitignore` file(s) to `exclude` (https://mypy.readthedocs.io/en/stable/config_file.html#confval-exclude). This option may only be set in the global section (`[mypy]`).

---

##### `explicitPackageBases`<sup>Optional</sup> <a name="explicitPackageBases" id="projen.python.MypyConfiguration.property.explicitPackageBases"></a>

```typescript
public readonly explicitPackageBases: boolean;
```

- *Type:* boolean

This flag tells mypy that top-level packages will be based in either the current directory, or a member of the `MYPYPATH` environment variable or `mypy_path` config option.

This option is only useful in the absence of `__init__.py`. See Mapping file paths to modules (https://mypy.readthedocs.io/en/stable/running_mypy.html#mapping-paths-to-modules) for details.

---

##### `extraChecks`<sup>Optional</sup> <a name="extraChecks" id="projen.python.MypyConfiguration.property.extraChecks"></a>

```typescript
public readonly extraChecks: boolean;
```

- *Type:* boolean

---

##### `files`<sup>Optional</sup> <a name="files" id="projen.python.MypyConfiguration.property.files"></a>

```typescript
public readonly files: string[];
```

- *Type:* string[]

A comma-separated list of paths which should be checked by mypy if none are given on the command line.

Supports recursive file globbing using `glob`, where `*` (e.g. `*.py`) matches files in the current directory and `*_/` (e.g. `*_/*.py`) matches files in any directories below the current one. User home directory and environment variables will be expanded.

---

##### `fixedFormatCache`<sup>Optional</sup> <a name="fixedFormatCache" id="projen.python.MypyConfiguration.property.fixedFormatCache"></a>

```typescript
public readonly fixedFormatCache: boolean;
```

- *Type:* boolean

---

##### `followImports`<sup>Optional</sup> <a name="followImports" id="projen.python.MypyConfiguration.property.followImports"></a>

```typescript
public readonly followImports: FollowImports;
```

- *Type:* <a href="#projen.python.FollowImports">FollowImports</a>

---

##### `followImportsForStubs`<sup>Optional</sup> <a name="followImportsForStubs" id="projen.python.MypyConfiguration.property.followImportsForStubs"></a>

```typescript
public readonly followImportsForStubs: boolean;
```

- *Type:* boolean

---

##### `followUntypedImports`<sup>Optional</sup> <a name="followUntypedImports" id="projen.python.MypyConfiguration.property.followUntypedImports"></a>

```typescript
public readonly followUntypedImports: boolean;
```

- *Type:* boolean

---

##### `forceUnionSyntax`<sup>Optional</sup> <a name="forceUnionSyntax" id="projen.python.MypyConfiguration.property.forceUnionSyntax"></a>

```typescript
public readonly forceUnionSyntax: boolean;
```

- *Type:* boolean

---

##### `forceUppercaseBuiltins`<sup>Optional</sup> <a name="forceUppercaseBuiltins" id="projen.python.MypyConfiguration.property.forceUppercaseBuiltins"></a>

```typescript
public readonly forceUppercaseBuiltins: boolean;
```

- *Type:* boolean

---

##### `hideErrorCodes`<sup>Optional</sup> <a name="hideErrorCodes" id="projen.python.MypyConfiguration.property.hideErrorCodes"></a>

```typescript
public readonly hideErrorCodes: boolean;
```

- *Type:* boolean

---

##### `ignoreErrors`<sup>Optional</sup> <a name="ignoreErrors" id="projen.python.MypyConfiguration.property.ignoreErrors"></a>

```typescript
public readonly ignoreErrors: boolean;
```

- *Type:* boolean

---

##### `ignoreMissingImports`<sup>Optional</sup> <a name="ignoreMissingImports" id="projen.python.MypyConfiguration.property.ignoreMissingImports"></a>

```typescript
public readonly ignoreMissingImports: boolean;
```

- *Type:* boolean

---

##### `implicitOptional`<sup>Optional</sup> <a name="implicitOptional" id="projen.python.MypyConfiguration.property.implicitOptional"></a>

```typescript
public readonly implicitOptional: boolean;
```

- *Type:* boolean

---

##### `implicitReexport`<sup>Optional</sup> <a name="implicitReexport" id="projen.python.MypyConfiguration.property.implicitReexport"></a>

```typescript
public readonly implicitReexport: boolean;
```

- *Type:* boolean

---

##### `incremental`<sup>Optional</sup> <a name="incremental" id="projen.python.MypyConfiguration.property.incremental"></a>

```typescript
public readonly incremental: boolean;
```

- *Type:* boolean

---

##### `installTypes`<sup>Optional</sup> <a name="installTypes" id="projen.python.MypyConfiguration.property.installTypes"></a>

```typescript
public readonly installTypes: boolean;
```

- *Type:* boolean

Install detected missing library stub packages using pip.

---

##### `junitXml`<sup>Optional</sup> <a name="junitXml" id="projen.python.MypyConfiguration.property.junitXml"></a>

```typescript
public readonly junitXml: string;
```

- *Type:* string

---

##### `linecountReport`<sup>Optional</sup> <a name="linecountReport" id="projen.python.MypyConfiguration.property.linecountReport"></a>

```typescript
public readonly linecountReport: string;
```

- *Type:* string

---

##### `linecoverageReport`<sup>Optional</sup> <a name="linecoverageReport" id="projen.python.MypyConfiguration.property.linecoverageReport"></a>

```typescript
public readonly linecoverageReport: string;
```

- *Type:* string

---

##### `lineprecisionReport`<sup>Optional</sup> <a name="lineprecisionReport" id="projen.python.MypyConfiguration.property.lineprecisionReport"></a>

```typescript
public readonly lineprecisionReport: string;
```

- *Type:* string

---

##### `localPartialTypes`<sup>Optional</sup> <a name="localPartialTypes" id="projen.python.MypyConfiguration.property.localPartialTypes"></a>

```typescript
public readonly localPartialTypes: boolean;
```

- *Type:* boolean

---

##### `modules`<sup>Optional</sup> <a name="modules" id="projen.python.MypyConfiguration.property.modules"></a>

```typescript
public readonly modules: string[];
```

- *Type:* string[]

A comma-separated list of packages which should be checked by mypy if none are given on the command line.

Mypy WILL NOT recursively type check any submodules of the provided module.

---

##### `mypyPath`<sup>Optional</sup> <a name="mypyPath" id="projen.python.MypyConfiguration.property.mypyPath"></a>

```typescript
public readonly mypyPath: string[];
```

- *Type:* string[]

Specifies the paths to use, after trying the paths from `MYPYPATH` environment variable.

Useful if you'd like to keep stubs in your repo, along with the config file. Multiple paths are always separated with a `:` or `,` regardless of the platform. User home directory and environment variables will be expanded.

---

##### `namespacePackages`<sup>Optional</sup> <a name="namespacePackages" id="projen.python.MypyConfiguration.property.namespacePackages"></a>

```typescript
public readonly namespacePackages: boolean;
```

- *Type:* boolean

Enables PEP 420 style namespace packages.

See the corresponding flag `--no-namespace-packages` (https://mypy.readthedocs.io/en/stable/command_line.html#cmdoption-mypy-no-namespace-packages) for more information.

---

##### `noImplicitOptional`<sup>Optional</sup> <a name="noImplicitOptional" id="projen.python.MypyConfiguration.property.noImplicitOptional"></a>

```typescript
public readonly noImplicitOptional: boolean;
```

- *Type:* boolean

---

##### `noImplicitReexport`<sup>Optional</sup> <a name="noImplicitReexport" id="projen.python.MypyConfiguration.property.noImplicitReexport"></a>

```typescript
public readonly noImplicitReexport: boolean;
```

- *Type:* boolean

---

##### `nonInteractive`<sup>Optional</sup> <a name="nonInteractive" id="projen.python.MypyConfiguration.property.nonInteractive"></a>

```typescript
public readonly nonInteractive: boolean;
```

- *Type:* boolean

Install stubs without asking for confirmation and hide errors, with --install-types.

---

##### `noSilenceSitePackages`<sup>Optional</sup> <a name="noSilenceSitePackages" id="projen.python.MypyConfiguration.property.noSilenceSitePackages"></a>

```typescript
public readonly noSilenceSitePackages: boolean;
```

- *Type:* boolean

Enables reporting error messages generated within installed packages (see PEP 561 for more details on distributing type information).

Those error messages are suppressed by default, since you are usually not able to control errors in 3rd party code.

---

##### `noSitePackages`<sup>Optional</sup> <a name="noSitePackages" id="projen.python.MypyConfiguration.property.noSitePackages"></a>

```typescript
public readonly noSitePackages: boolean;
```

- *Type:* boolean

---

##### `overrides`<sup>Optional</sup> <a name="overrides" id="projen.python.MypyConfiguration.property.overrides"></a>

```typescript
public readonly overrides: MypyConfigurationOverrides[];
```

- *Type:* <a href="#projen.python.MypyConfigurationOverrides">MypyConfigurationOverrides</a>[]

---

##### `packages`<sup>Optional</sup> <a name="packages" id="projen.python.MypyConfiguration.property.packages"></a>

```typescript
public readonly packages: string[];
```

- *Type:* string[]

A comma-separated list of packages which should be checked by mypy if none are given on the command line.

Mypy WILL recursively type check any submodules of the provided package. This flag is identical to `modules` apart from this behavior.

---

##### `pdb`<sup>Optional</sup> <a name="pdb" id="projen.python.MypyConfiguration.property.pdb"></a>

```typescript
public readonly pdb: boolean;
```

- *Type:* boolean

---

##### `platform`<sup>Optional</sup> <a name="platform" id="projen.python.MypyConfiguration.property.platform"></a>

```typescript
public readonly platform: string;
```

- *Type:* string

Specifies the OS platform for the target program, for example `darwin` or `win32` (meaning OS X or Windows, respectively).

The default is the current platform as revealed by Python's `sys.platform` variable.

---

##### `plugins`<sup>Optional</sup> <a name="plugins" id="projen.python.MypyConfiguration.property.plugins"></a>

```typescript
public readonly plugins: string[];
```

- *Type:* string[]

---

##### `pretty`<sup>Optional</sup> <a name="pretty" id="projen.python.MypyConfiguration.property.pretty"></a>

```typescript
public readonly pretty: boolean;
```

- *Type:* boolean

---

##### `pythonExecutable`<sup>Optional</sup> <a name="pythonExecutable" id="projen.python.MypyConfiguration.property.pythonExecutable"></a>

```typescript
public readonly pythonExecutable: string;
```

- *Type:* string
- *Default:* the executable used to run mypy.

Specifies the path to the Python executable to inspect to collect a list of available PEP 561 packages (https://mypy.readthedocs.io/en/stable/installed_packages.html#installed-packages). User home directory and environment variables will be expanded. Defaults to the executable used to run mypy.

---

##### `pythonVersion`<sup>Optional</sup> <a name="pythonVersion" id="projen.python.MypyConfiguration.property.pythonVersion"></a>

```typescript
public readonly pythonVersion: string;
```

- *Type:* string

Specifies the Python version used to parse and check the target program.

The string should be in the format `MAJOR.MINOR` (for example, `2.7`). The default is the version of the Python interpreter used to run mypy.

---

##### `raiseExceptions`<sup>Optional</sup> <a name="raiseExceptions" id="projen.python.MypyConfiguration.property.raiseExceptions"></a>

```typescript
public readonly raiseExceptions: boolean;
```

- *Type:* boolean

---

##### `scriptsAreModules`<sup>Optional</sup> <a name="scriptsAreModules" id="projen.python.MypyConfiguration.property.scriptsAreModules"></a>

```typescript
public readonly scriptsAreModules: boolean;
```

- *Type:* boolean

---

##### `showAbsolutePath`<sup>Optional</sup> <a name="showAbsolutePath" id="projen.python.MypyConfiguration.property.showAbsolutePath"></a>

```typescript
public readonly showAbsolutePath: boolean;
```

- *Type:* boolean

---

##### `showColumnNumbers`<sup>Optional</sup> <a name="showColumnNumbers" id="projen.python.MypyConfiguration.property.showColumnNumbers"></a>

```typescript
public readonly showColumnNumbers: boolean;
```

- *Type:* boolean

---

##### `showErrorCodeLinks`<sup>Optional</sup> <a name="showErrorCodeLinks" id="projen.python.MypyConfiguration.property.showErrorCodeLinks"></a>

```typescript
public readonly showErrorCodeLinks: boolean;
```

- *Type:* boolean

---

##### `showErrorCodes`<sup>Optional</sup> <a name="showErrorCodes" id="projen.python.MypyConfiguration.property.showErrorCodes"></a>

```typescript
public readonly showErrorCodes: boolean;
```

- *Type:* boolean

DEPRECATED and UNDOCUMENTED: Now defaults to true, use `hide_error_codes` if you need to disable error codes instead.

---

##### `showErrorContext`<sup>Optional</sup> <a name="showErrorContext" id="projen.python.MypyConfiguration.property.showErrorContext"></a>

```typescript
public readonly showErrorContext: boolean;
```

- *Type:* boolean

---

##### `showTraceback`<sup>Optional</sup> <a name="showTraceback" id="projen.python.MypyConfiguration.property.showTraceback"></a>

```typescript
public readonly showTraceback: boolean;
```

- *Type:* boolean

---

##### `skipCacheMtimeChecks`<sup>Optional</sup> <a name="skipCacheMtimeChecks" id="projen.python.MypyConfiguration.property.skipCacheMtimeChecks"></a>

```typescript
public readonly skipCacheMtimeChecks: boolean;
```

- *Type:* boolean

---

##### `skipVersionCheck`<sup>Optional</sup> <a name="skipVersionCheck" id="projen.python.MypyConfiguration.property.skipVersionCheck"></a>

```typescript
public readonly skipVersionCheck: boolean;
```

- *Type:* boolean

---

##### `sqliteCache`<sup>Optional</sup> <a name="sqliteCache" id="projen.python.MypyConfiguration.property.sqliteCache"></a>

```typescript
public readonly sqliteCache: boolean;
```

- *Type:* boolean

---

##### `strict`<sup>Optional</sup> <a name="strict" id="projen.python.MypyConfiguration.property.strict"></a>

```typescript
public readonly strict: boolean;
```

- *Type:* boolean

---

##### `strictBytes`<sup>Optional</sup> <a name="strictBytes" id="projen.python.MypyConfiguration.property.strictBytes"></a>

```typescript
public readonly strictBytes: boolean;
```

- *Type:* boolean

---

##### `strictConcatenate`<sup>Optional</sup> <a name="strictConcatenate" id="projen.python.MypyConfiguration.property.strictConcatenate"></a>

```typescript
public readonly strictConcatenate: boolean;
```

- *Type:* boolean

---

##### `strictEquality`<sup>Optional</sup> <a name="strictEquality" id="projen.python.MypyConfiguration.property.strictEquality"></a>

```typescript
public readonly strictEquality: boolean;
```

- *Type:* boolean

---

##### `strictEqualityForNone`<sup>Optional</sup> <a name="strictEqualityForNone" id="projen.python.MypyConfiguration.property.strictEqualityForNone"></a>

```typescript
public readonly strictEqualityForNone: boolean;
```

- *Type:* boolean

---

##### `strictOptional`<sup>Optional</sup> <a name="strictOptional" id="projen.python.MypyConfiguration.property.strictOptional"></a>

```typescript
public readonly strictOptional: boolean;
```

- *Type:* boolean

---

##### `untypedCallsExclude`<sup>Optional</sup> <a name="untypedCallsExclude" id="projen.python.MypyConfiguration.property.untypedCallsExclude"></a>

```typescript
public readonly untypedCallsExclude: string[];
```

- *Type:* string[]

---

##### `verbosity`<sup>Optional</sup> <a name="verbosity" id="projen.python.MypyConfiguration.property.verbosity"></a>

```typescript
public readonly verbosity: number;
```

- *Type:* number

---

##### `warnIncompleteStub`<sup>Optional</sup> <a name="warnIncompleteStub" id="projen.python.MypyConfiguration.property.warnIncompleteStub"></a>

```typescript
public readonly warnIncompleteStub: boolean;
```

- *Type:* boolean

---

##### `warnNoReturn`<sup>Optional</sup> <a name="warnNoReturn" id="projen.python.MypyConfiguration.property.warnNoReturn"></a>

```typescript
public readonly warnNoReturn: boolean;
```

- *Type:* boolean

---

##### `warnRedundantCasts`<sup>Optional</sup> <a name="warnRedundantCasts" id="projen.python.MypyConfiguration.property.warnRedundantCasts"></a>

```typescript
public readonly warnRedundantCasts: boolean;
```

- *Type:* boolean

Warns about casting an expression to its inferred type.

---

##### `warnReturnAny`<sup>Optional</sup> <a name="warnReturnAny" id="projen.python.MypyConfiguration.property.warnReturnAny"></a>

```typescript
public readonly warnReturnAny: boolean;
```

- *Type:* boolean

---

##### `warnUnreachable`<sup>Optional</sup> <a name="warnUnreachable" id="projen.python.MypyConfiguration.property.warnUnreachable"></a>

```typescript
public readonly warnUnreachable: boolean;
```

- *Type:* boolean

---

##### `warnUnusedConfigs`<sup>Optional</sup> <a name="warnUnusedConfigs" id="projen.python.MypyConfiguration.property.warnUnusedConfigs"></a>

```typescript
public readonly warnUnusedConfigs: boolean;
```

- *Type:* boolean

---

##### `warnUnusedIgnores`<sup>Optional</sup> <a name="warnUnusedIgnores" id="projen.python.MypyConfiguration.property.warnUnusedIgnores"></a>

```typescript
public readonly warnUnusedIgnores: boolean;
```

- *Type:* boolean

---

##### `xmlReport`<sup>Optional</sup> <a name="xmlReport" id="projen.python.MypyConfiguration.property.xmlReport"></a>

```typescript
public readonly xmlReport: string;
```

- *Type:* string

---

### MypyConfigurationOverrides <a name="MypyConfigurationOverrides" id="projen.python.MypyConfigurationOverrides"></a>

#### Initializer <a name="Initializer" id="projen.python.MypyConfigurationOverrides.Initializer"></a>

```typescript
import { python } from 'projen'

const mypyConfigurationOverrides: python.MypyConfigurationOverrides = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.module">module</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.allowRedefinition">allowRedefinition</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.allowRedefinitionNew">allowRedefinitionNew</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.allowUntypedGlobals">allowUntypedGlobals</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.alwaysFalse">alwaysFalse</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.alwaysTrue">alwaysTrue</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.anyExprsReport">anyExprsReport</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.cacheDir">cacheDir</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.cacheFineGrained">cacheFineGrained</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.checkUntypedDefs">checkUntypedDefs</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.coberturaXmlReport">coberturaXmlReport</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.colorOutput">colorOutput</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.customTypeshedDir">customTypeshedDir</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.customTypingModule">customTypingModule</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.disableErrorCode">disableErrorCode</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.disallowAnyDecorated">disallowAnyDecorated</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.disallowAnyExplicit">disallowAnyExplicit</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.disallowAnyExpr">disallowAnyExpr</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.disallowAnyGenerics">disallowAnyGenerics</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.disallowAnyUnimported">disallowAnyUnimported</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.disallowIncompleteDefs">disallowIncompleteDefs</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.disallowSubclassingAny">disallowSubclassingAny</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.disallowUntypedCalls">disallowUntypedCalls</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.disallowUntypedDecorators">disallowUntypedDecorators</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.disallowUntypedDefs">disallowUntypedDefs</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.enableErrorCode">enableErrorCode</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.errorSummary">errorSummary</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.extraChecks">extraChecks</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.fixedFormatCache">fixedFormatCache</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.followImports">followImports</a></code> | <code><a href="#projen.python.FollowImports">FollowImports</a></code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.followImportsForStubs">followImportsForStubs</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.followUntypedImports">followUntypedImports</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.forceUnionSyntax">forceUnionSyntax</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.forceUppercaseBuiltins">forceUppercaseBuiltins</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.hideErrorCodes">hideErrorCodes</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.ignoreErrors">ignoreErrors</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.ignoreMissingImports">ignoreMissingImports</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.implicitOptional">implicitOptional</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.implicitReexport">implicitReexport</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.incremental">incremental</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.junitXml">junitXml</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.linecountReport">linecountReport</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.linecoverageReport">linecoverageReport</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.lineprecisionReport">lineprecisionReport</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.localPartialTypes">localPartialTypes</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.noImplicitOptional">noImplicitOptional</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.noImplicitReexport">noImplicitReexport</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.noSitePackages">noSitePackages</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.pdb">pdb</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.plugins">plugins</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.pretty">pretty</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.raiseExceptions">raiseExceptions</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.scriptsAreModules">scriptsAreModules</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.showAbsolutePath">showAbsolutePath</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.showColumnNumbers">showColumnNumbers</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.showErrorCodeLinks">showErrorCodeLinks</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.showErrorContext">showErrorContext</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.showTraceback">showTraceback</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.skipCacheMtimeChecks">skipCacheMtimeChecks</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.skipVersionCheck">skipVersionCheck</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.sqliteCache">sqliteCache</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.strict">strict</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.strictBytes">strictBytes</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.strictConcatenate">strictConcatenate</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.strictEquality">strictEquality</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.strictEqualityForNone">strictEqualityForNone</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.strictOptional">strictOptional</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.untypedCallsExclude">untypedCallsExclude</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.verbosity">verbosity</a></code> | <code>number</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.warnIncompleteStub">warnIncompleteStub</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.warnNoReturn">warnNoReturn</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.warnReturnAny">warnReturnAny</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.warnUnreachable">warnUnreachable</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.warnUnusedConfigs">warnUnusedConfigs</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.warnUnusedIgnores">warnUnusedIgnores</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.MypyConfigurationOverrides.property.xmlReport">xmlReport</a></code> | <code>string</code> | *No description.* |

---

##### `module`<sup>Required</sup> <a name="module" id="projen.python.MypyConfigurationOverrides.property.module"></a>

```typescript
public readonly module: string[];
```

- *Type:* string[]

---

##### `allowRedefinition`<sup>Optional</sup> <a name="allowRedefinition" id="projen.python.MypyConfigurationOverrides.property.allowRedefinition"></a>

```typescript
public readonly allowRedefinition: boolean;
```

- *Type:* boolean

---

##### `allowRedefinitionNew`<sup>Optional</sup> <a name="allowRedefinitionNew" id="projen.python.MypyConfigurationOverrides.property.allowRedefinitionNew"></a>

```typescript
public readonly allowRedefinitionNew: boolean;
```

- *Type:* boolean

---

##### `allowUntypedGlobals`<sup>Optional</sup> <a name="allowUntypedGlobals" id="projen.python.MypyConfigurationOverrides.property.allowUntypedGlobals"></a>

```typescript
public readonly allowUntypedGlobals: boolean;
```

- *Type:* boolean

---

##### `alwaysFalse`<sup>Optional</sup> <a name="alwaysFalse" id="projen.python.MypyConfigurationOverrides.property.alwaysFalse"></a>

```typescript
public readonly alwaysFalse: string[];
```

- *Type:* string[]

---

##### `alwaysTrue`<sup>Optional</sup> <a name="alwaysTrue" id="projen.python.MypyConfigurationOverrides.property.alwaysTrue"></a>

```typescript
public readonly alwaysTrue: string[];
```

- *Type:* string[]

---

##### `anyExprsReport`<sup>Optional</sup> <a name="anyExprsReport" id="projen.python.MypyConfigurationOverrides.property.anyExprsReport"></a>

```typescript
public readonly anyExprsReport: string;
```

- *Type:* string

---

##### `cacheDir`<sup>Optional</sup> <a name="cacheDir" id="projen.python.MypyConfigurationOverrides.property.cacheDir"></a>

```typescript
public readonly cacheDir: string;
```

- *Type:* string

---

##### `cacheFineGrained`<sup>Optional</sup> <a name="cacheFineGrained" id="projen.python.MypyConfigurationOverrides.property.cacheFineGrained"></a>

```typescript
public readonly cacheFineGrained: boolean;
```

- *Type:* boolean

---

##### `checkUntypedDefs`<sup>Optional</sup> <a name="checkUntypedDefs" id="projen.python.MypyConfigurationOverrides.property.checkUntypedDefs"></a>

```typescript
public readonly checkUntypedDefs: boolean;
```

- *Type:* boolean

---

##### `coberturaXmlReport`<sup>Optional</sup> <a name="coberturaXmlReport" id="projen.python.MypyConfigurationOverrides.property.coberturaXmlReport"></a>

```typescript
public readonly coberturaXmlReport: string;
```

- *Type:* string

---

##### `colorOutput`<sup>Optional</sup> <a name="colorOutput" id="projen.python.MypyConfigurationOverrides.property.colorOutput"></a>

```typescript
public readonly colorOutput: boolean;
```

- *Type:* boolean

---

##### `customTypeshedDir`<sup>Optional</sup> <a name="customTypeshedDir" id="projen.python.MypyConfigurationOverrides.property.customTypeshedDir"></a>

```typescript
public readonly customTypeshedDir: string;
```

- *Type:* string

---

##### `customTypingModule`<sup>Optional</sup> <a name="customTypingModule" id="projen.python.MypyConfigurationOverrides.property.customTypingModule"></a>

```typescript
public readonly customTypingModule: string;
```

- *Type:* string

---

##### `disableErrorCode`<sup>Optional</sup> <a name="disableErrorCode" id="projen.python.MypyConfigurationOverrides.property.disableErrorCode"></a>

```typescript
public readonly disableErrorCode: string[];
```

- *Type:* string[]

---

##### `disallowAnyDecorated`<sup>Optional</sup> <a name="disallowAnyDecorated" id="projen.python.MypyConfigurationOverrides.property.disallowAnyDecorated"></a>

```typescript
public readonly disallowAnyDecorated: boolean;
```

- *Type:* boolean

---

##### `disallowAnyExplicit`<sup>Optional</sup> <a name="disallowAnyExplicit" id="projen.python.MypyConfigurationOverrides.property.disallowAnyExplicit"></a>

```typescript
public readonly disallowAnyExplicit: boolean;
```

- *Type:* boolean

---

##### `disallowAnyExpr`<sup>Optional</sup> <a name="disallowAnyExpr" id="projen.python.MypyConfigurationOverrides.property.disallowAnyExpr"></a>

```typescript
public readonly disallowAnyExpr: boolean;
```

- *Type:* boolean

---

##### `disallowAnyGenerics`<sup>Optional</sup> <a name="disallowAnyGenerics" id="projen.python.MypyConfigurationOverrides.property.disallowAnyGenerics"></a>

```typescript
public readonly disallowAnyGenerics: boolean;
```

- *Type:* boolean

---

##### `disallowAnyUnimported`<sup>Optional</sup> <a name="disallowAnyUnimported" id="projen.python.MypyConfigurationOverrides.property.disallowAnyUnimported"></a>

```typescript
public readonly disallowAnyUnimported: boolean;
```

- *Type:* boolean

---

##### `disallowIncompleteDefs`<sup>Optional</sup> <a name="disallowIncompleteDefs" id="projen.python.MypyConfigurationOverrides.property.disallowIncompleteDefs"></a>

```typescript
public readonly disallowIncompleteDefs: boolean;
```

- *Type:* boolean

---

##### `disallowSubclassingAny`<sup>Optional</sup> <a name="disallowSubclassingAny" id="projen.python.MypyConfigurationOverrides.property.disallowSubclassingAny"></a>

```typescript
public readonly disallowSubclassingAny: boolean;
```

- *Type:* boolean

---

##### `disallowUntypedCalls`<sup>Optional</sup> <a name="disallowUntypedCalls" id="projen.python.MypyConfigurationOverrides.property.disallowUntypedCalls"></a>

```typescript
public readonly disallowUntypedCalls: boolean;
```

- *Type:* boolean

---

##### `disallowUntypedDecorators`<sup>Optional</sup> <a name="disallowUntypedDecorators" id="projen.python.MypyConfigurationOverrides.property.disallowUntypedDecorators"></a>

```typescript
public readonly disallowUntypedDecorators: boolean;
```

- *Type:* boolean

---

##### `disallowUntypedDefs`<sup>Optional</sup> <a name="disallowUntypedDefs" id="projen.python.MypyConfigurationOverrides.property.disallowUntypedDefs"></a>

```typescript
public readonly disallowUntypedDefs: boolean;
```

- *Type:* boolean

---

##### `enableErrorCode`<sup>Optional</sup> <a name="enableErrorCode" id="projen.python.MypyConfigurationOverrides.property.enableErrorCode"></a>

```typescript
public readonly enableErrorCode: string[];
```

- *Type:* string[]

---

##### `errorSummary`<sup>Optional</sup> <a name="errorSummary" id="projen.python.MypyConfigurationOverrides.property.errorSummary"></a>

```typescript
public readonly errorSummary: boolean;
```

- *Type:* boolean

---

##### `extraChecks`<sup>Optional</sup> <a name="extraChecks" id="projen.python.MypyConfigurationOverrides.property.extraChecks"></a>

```typescript
public readonly extraChecks: boolean;
```

- *Type:* boolean

---

##### `fixedFormatCache`<sup>Optional</sup> <a name="fixedFormatCache" id="projen.python.MypyConfigurationOverrides.property.fixedFormatCache"></a>

```typescript
public readonly fixedFormatCache: boolean;
```

- *Type:* boolean

---

##### `followImports`<sup>Optional</sup> <a name="followImports" id="projen.python.MypyConfigurationOverrides.property.followImports"></a>

```typescript
public readonly followImports: FollowImports;
```

- *Type:* <a href="#projen.python.FollowImports">FollowImports</a>

---

##### `followImportsForStubs`<sup>Optional</sup> <a name="followImportsForStubs" id="projen.python.MypyConfigurationOverrides.property.followImportsForStubs"></a>

```typescript
public readonly followImportsForStubs: boolean;
```

- *Type:* boolean

---

##### `followUntypedImports`<sup>Optional</sup> <a name="followUntypedImports" id="projen.python.MypyConfigurationOverrides.property.followUntypedImports"></a>

```typescript
public readonly followUntypedImports: boolean;
```

- *Type:* boolean

---

##### `forceUnionSyntax`<sup>Optional</sup> <a name="forceUnionSyntax" id="projen.python.MypyConfigurationOverrides.property.forceUnionSyntax"></a>

```typescript
public readonly forceUnionSyntax: boolean;
```

- *Type:* boolean

---

##### `forceUppercaseBuiltins`<sup>Optional</sup> <a name="forceUppercaseBuiltins" id="projen.python.MypyConfigurationOverrides.property.forceUppercaseBuiltins"></a>

```typescript
public readonly forceUppercaseBuiltins: boolean;
```

- *Type:* boolean

---

##### `hideErrorCodes`<sup>Optional</sup> <a name="hideErrorCodes" id="projen.python.MypyConfigurationOverrides.property.hideErrorCodes"></a>

```typescript
public readonly hideErrorCodes: boolean;
```

- *Type:* boolean

---

##### `ignoreErrors`<sup>Optional</sup> <a name="ignoreErrors" id="projen.python.MypyConfigurationOverrides.property.ignoreErrors"></a>

```typescript
public readonly ignoreErrors: boolean;
```

- *Type:* boolean

---

##### `ignoreMissingImports`<sup>Optional</sup> <a name="ignoreMissingImports" id="projen.python.MypyConfigurationOverrides.property.ignoreMissingImports"></a>

```typescript
public readonly ignoreMissingImports: boolean;
```

- *Type:* boolean

---

##### `implicitOptional`<sup>Optional</sup> <a name="implicitOptional" id="projen.python.MypyConfigurationOverrides.property.implicitOptional"></a>

```typescript
public readonly implicitOptional: boolean;
```

- *Type:* boolean

---

##### `implicitReexport`<sup>Optional</sup> <a name="implicitReexport" id="projen.python.MypyConfigurationOverrides.property.implicitReexport"></a>

```typescript
public readonly implicitReexport: boolean;
```

- *Type:* boolean

---

##### `incremental`<sup>Optional</sup> <a name="incremental" id="projen.python.MypyConfigurationOverrides.property.incremental"></a>

```typescript
public readonly incremental: boolean;
```

- *Type:* boolean

---

##### `junitXml`<sup>Optional</sup> <a name="junitXml" id="projen.python.MypyConfigurationOverrides.property.junitXml"></a>

```typescript
public readonly junitXml: string;
```

- *Type:* string

---

##### `linecountReport`<sup>Optional</sup> <a name="linecountReport" id="projen.python.MypyConfigurationOverrides.property.linecountReport"></a>

```typescript
public readonly linecountReport: string;
```

- *Type:* string

---

##### `linecoverageReport`<sup>Optional</sup> <a name="linecoverageReport" id="projen.python.MypyConfigurationOverrides.property.linecoverageReport"></a>

```typescript
public readonly linecoverageReport: string;
```

- *Type:* string

---

##### `lineprecisionReport`<sup>Optional</sup> <a name="lineprecisionReport" id="projen.python.MypyConfigurationOverrides.property.lineprecisionReport"></a>

```typescript
public readonly lineprecisionReport: string;
```

- *Type:* string

---

##### `localPartialTypes`<sup>Optional</sup> <a name="localPartialTypes" id="projen.python.MypyConfigurationOverrides.property.localPartialTypes"></a>

```typescript
public readonly localPartialTypes: boolean;
```

- *Type:* boolean

---

##### `noImplicitOptional`<sup>Optional</sup> <a name="noImplicitOptional" id="projen.python.MypyConfigurationOverrides.property.noImplicitOptional"></a>

```typescript
public readonly noImplicitOptional: boolean;
```

- *Type:* boolean

---

##### `noImplicitReexport`<sup>Optional</sup> <a name="noImplicitReexport" id="projen.python.MypyConfigurationOverrides.property.noImplicitReexport"></a>

```typescript
public readonly noImplicitReexport: boolean;
```

- *Type:* boolean

---

##### `noSitePackages`<sup>Optional</sup> <a name="noSitePackages" id="projen.python.MypyConfigurationOverrides.property.noSitePackages"></a>

```typescript
public readonly noSitePackages: boolean;
```

- *Type:* boolean

---

##### `pdb`<sup>Optional</sup> <a name="pdb" id="projen.python.MypyConfigurationOverrides.property.pdb"></a>

```typescript
public readonly pdb: boolean;
```

- *Type:* boolean

---

##### `plugins`<sup>Optional</sup> <a name="plugins" id="projen.python.MypyConfigurationOverrides.property.plugins"></a>

```typescript
public readonly plugins: string[];
```

- *Type:* string[]

---

##### `pretty`<sup>Optional</sup> <a name="pretty" id="projen.python.MypyConfigurationOverrides.property.pretty"></a>

```typescript
public readonly pretty: boolean;
```

- *Type:* boolean

---

##### `raiseExceptions`<sup>Optional</sup> <a name="raiseExceptions" id="projen.python.MypyConfigurationOverrides.property.raiseExceptions"></a>

```typescript
public readonly raiseExceptions: boolean;
```

- *Type:* boolean

---

##### `scriptsAreModules`<sup>Optional</sup> <a name="scriptsAreModules" id="projen.python.MypyConfigurationOverrides.property.scriptsAreModules"></a>

```typescript
public readonly scriptsAreModules: boolean;
```

- *Type:* boolean

---

##### `showAbsolutePath`<sup>Optional</sup> <a name="showAbsolutePath" id="projen.python.MypyConfigurationOverrides.property.showAbsolutePath"></a>

```typescript
public readonly showAbsolutePath: boolean;
```

- *Type:* boolean

---

##### `showColumnNumbers`<sup>Optional</sup> <a name="showColumnNumbers" id="projen.python.MypyConfigurationOverrides.property.showColumnNumbers"></a>

```typescript
public readonly showColumnNumbers: boolean;
```

- *Type:* boolean

---

##### `showErrorCodeLinks`<sup>Optional</sup> <a name="showErrorCodeLinks" id="projen.python.MypyConfigurationOverrides.property.showErrorCodeLinks"></a>

```typescript
public readonly showErrorCodeLinks: boolean;
```

- *Type:* boolean

---

##### `showErrorContext`<sup>Optional</sup> <a name="showErrorContext" id="projen.python.MypyConfigurationOverrides.property.showErrorContext"></a>

```typescript
public readonly showErrorContext: boolean;
```

- *Type:* boolean

---

##### `showTraceback`<sup>Optional</sup> <a name="showTraceback" id="projen.python.MypyConfigurationOverrides.property.showTraceback"></a>

```typescript
public readonly showTraceback: boolean;
```

- *Type:* boolean

---

##### `skipCacheMtimeChecks`<sup>Optional</sup> <a name="skipCacheMtimeChecks" id="projen.python.MypyConfigurationOverrides.property.skipCacheMtimeChecks"></a>

```typescript
public readonly skipCacheMtimeChecks: boolean;
```

- *Type:* boolean

---

##### `skipVersionCheck`<sup>Optional</sup> <a name="skipVersionCheck" id="projen.python.MypyConfigurationOverrides.property.skipVersionCheck"></a>

```typescript
public readonly skipVersionCheck: boolean;
```

- *Type:* boolean

---

##### `sqliteCache`<sup>Optional</sup> <a name="sqliteCache" id="projen.python.MypyConfigurationOverrides.property.sqliteCache"></a>

```typescript
public readonly sqliteCache: boolean;
```

- *Type:* boolean

---

##### `strict`<sup>Optional</sup> <a name="strict" id="projen.python.MypyConfigurationOverrides.property.strict"></a>

```typescript
public readonly strict: boolean;
```

- *Type:* boolean

---

##### `strictBytes`<sup>Optional</sup> <a name="strictBytes" id="projen.python.MypyConfigurationOverrides.property.strictBytes"></a>

```typescript
public readonly strictBytes: boolean;
```

- *Type:* boolean

---

##### `strictConcatenate`<sup>Optional</sup> <a name="strictConcatenate" id="projen.python.MypyConfigurationOverrides.property.strictConcatenate"></a>

```typescript
public readonly strictConcatenate: boolean;
```

- *Type:* boolean

---

##### `strictEquality`<sup>Optional</sup> <a name="strictEquality" id="projen.python.MypyConfigurationOverrides.property.strictEquality"></a>

```typescript
public readonly strictEquality: boolean;
```

- *Type:* boolean

---

##### `strictEqualityForNone`<sup>Optional</sup> <a name="strictEqualityForNone" id="projen.python.MypyConfigurationOverrides.property.strictEqualityForNone"></a>

```typescript
public readonly strictEqualityForNone: boolean;
```

- *Type:* boolean

---

##### `strictOptional`<sup>Optional</sup> <a name="strictOptional" id="projen.python.MypyConfigurationOverrides.property.strictOptional"></a>

```typescript
public readonly strictOptional: boolean;
```

- *Type:* boolean

---

##### `untypedCallsExclude`<sup>Optional</sup> <a name="untypedCallsExclude" id="projen.python.MypyConfigurationOverrides.property.untypedCallsExclude"></a>

```typescript
public readonly untypedCallsExclude: string[];
```

- *Type:* string[]

---

##### `verbosity`<sup>Optional</sup> <a name="verbosity" id="projen.python.MypyConfigurationOverrides.property.verbosity"></a>

```typescript
public readonly verbosity: number;
```

- *Type:* number

---

##### `warnIncompleteStub`<sup>Optional</sup> <a name="warnIncompleteStub" id="projen.python.MypyConfigurationOverrides.property.warnIncompleteStub"></a>

```typescript
public readonly warnIncompleteStub: boolean;
```

- *Type:* boolean

---

##### `warnNoReturn`<sup>Optional</sup> <a name="warnNoReturn" id="projen.python.MypyConfigurationOverrides.property.warnNoReturn"></a>

```typescript
public readonly warnNoReturn: boolean;
```

- *Type:* boolean

---

##### `warnReturnAny`<sup>Optional</sup> <a name="warnReturnAny" id="projen.python.MypyConfigurationOverrides.property.warnReturnAny"></a>

```typescript
public readonly warnReturnAny: boolean;
```

- *Type:* boolean

---

##### `warnUnreachable`<sup>Optional</sup> <a name="warnUnreachable" id="projen.python.MypyConfigurationOverrides.property.warnUnreachable"></a>

```typescript
public readonly warnUnreachable: boolean;
```

- *Type:* boolean

---

##### `warnUnusedConfigs`<sup>Optional</sup> <a name="warnUnusedConfigs" id="projen.python.MypyConfigurationOverrides.property.warnUnusedConfigs"></a>

```typescript
public readonly warnUnusedConfigs: boolean;
```

- *Type:* boolean

---

##### `warnUnusedIgnores`<sup>Optional</sup> <a name="warnUnusedIgnores" id="projen.python.MypyConfigurationOverrides.property.warnUnusedIgnores"></a>

```typescript
public readonly warnUnusedIgnores: boolean;
```

- *Type:* boolean

---

##### `xmlReport`<sup>Optional</sup> <a name="xmlReport" id="projen.python.MypyConfigurationOverrides.property.xmlReport"></a>

```typescript
public readonly xmlReport: string;
```

- *Type:* string

---

### OverrideOptions <a name="OverrideOptions" id="projen.python.OverrideOptions"></a>

#### Initializer <a name="Initializer" id="projen.python.OverrideOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const overrideOptions: python.OverrideOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.OverrideOptions.property.exclude">exclude</a></code> | <code>string[]</code> | A list of file and directory patterns to exclude from this override. |
| <code><a href="#projen.python.OverrideOptions.property.include">include</a></code> | <code>string[]</code> | A list of file and directory patterns to include for this override. |
| <code><a href="#projen.python.OverrideOptions.property.rules">rules</a></code> | <code><a href="#projen.python.Rules">Rules</a></code> | Rule overrides for files matching the include/exclude patterns. |

---

##### `exclude`<sup>Optional</sup> <a name="exclude" id="projen.python.OverrideOptions.property.exclude"></a>

```typescript
public readonly exclude: string[];
```

- *Type:* string[]

A list of file and directory patterns to exclude from this override.

Patterns follow a syntax similar to `.gitignore`.
Exclude patterns take precedence over include patterns within the same override.

If not specified, defaults to `[]` (excludes no files).

---

##### `include`<sup>Optional</sup> <a name="include" id="projen.python.OverrideOptions.property.include"></a>

```typescript
public readonly include: string[];
```

- *Type:* string[]

A list of file and directory patterns to include for this override.

The `include` option follows a similar syntax to `.gitignore` but reversed:
Including a file or directory will make it so that it (and its contents)
are affected by this override.

If not specified, defaults to `["**"]` (matches all files).

---

##### `rules`<sup>Optional</sup> <a name="rules" id="projen.python.OverrideOptions.property.rules"></a>

```typescript
public readonly rules: Rules;
```

- *Type:* <a href="#projen.python.Rules">Rules</a>

Rule overrides for files matching the include/exclude patterns.

These rules will be merged with the global rules, with override rules
taking precedence for matching files. You can set rules to different
severity levels or disable them entirely.

---

### Overrides <a name="Overrides" id="projen.python.Overrides"></a>

Overrides depending on things like platform, matrix variables, or environment variables.

#### Initializer <a name="Initializer" id="projen.python.Overrides.Initializer"></a>

```typescript
import { python } from 'projen'

const overrides: python.Overrides = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Overrides.property.platform">platform</a></code> | <code>{[ key: string ]: any}</code> | *No description.* |

---

##### `platform`<sup>Optional</sup> <a name="platform" id="projen.python.Overrides.property.platform"></a>

```typescript
public readonly platform: {[ key: string ]: any};
```

- *Type:* {[ key: string ]: any}

---

### PackageSource <a name="PackageSource" id="projen.python.PackageSource"></a>

Package source.

#### Initializer <a name="Initializer" id="projen.python.PackageSource.Initializer"></a>

```typescript
import { python } from 'projen'

const packageSource: python.PackageSource = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PackageSource.property.name">name</a></code> | <code>string</code> | Name of the package source. |
| <code><a href="#projen.python.PackageSource.property.url">url</a></code> | <code>string</code> | HTTP url for the package source. |
| <code><a href="#projen.python.PackageSource.property.excludePackages">excludePackages</a></code> | <code>string[]</code> | Glob patterns of packages that should not be used from this source. |
| <code><a href="#projen.python.PackageSource.property.includePackages">includePackages</a></code> | <code>string[]</code> | Glob patterns of the only packages that should be used from this source. |
| <code><a href="#projen.python.PackageSource.property.password">password</a></code> | <code>string</code> | Password for the package source. |
| <code><a href="#projen.python.PackageSource.property.type">type</a></code> | <code><a href="#projen.python.PackageSourceType">PackageSourceType</a></code> | index or find_links. |
| <code><a href="#projen.python.PackageSource.property.username">username</a></code> | <code>string</code> | Username for the package source. |
| <code><a href="#projen.python.PackageSource.property.verifySsl">verifySsl</a></code> | <code>boolean</code> | Whether to verify certificates when using the package source. |

---

##### `name`<sup>Required</sup> <a name="name" id="projen.python.PackageSource.property.name"></a>

```typescript
public readonly name: string;
```

- *Type:* string

Name of the package source.

---

##### `url`<sup>Required</sup> <a name="url" id="projen.python.PackageSource.property.url"></a>

```typescript
public readonly url: string;
```

- *Type:* string

HTTP url for the package source.

---

##### `excludePackages`<sup>Optional</sup> <a name="excludePackages" id="projen.python.PackageSource.property.excludePackages"></a>

```typescript
public readonly excludePackages: string[];
```

- *Type:* string[]

Glob patterns of packages that should not be used from this source.

Ignored if include_packages configuration is present

---

##### `includePackages`<sup>Optional</sup> <a name="includePackages" id="projen.python.PackageSource.property.includePackages"></a>

```typescript
public readonly includePackages: string[];
```

- *Type:* string[]

Glob patterns of the only packages that should be used from this source.

---

##### `password`<sup>Optional</sup> <a name="password" id="projen.python.PackageSource.property.password"></a>

```typescript
public readonly password: string;
```

- *Type:* string

Password for the package source.

---

##### `type`<sup>Optional</sup> <a name="type" id="projen.python.PackageSource.property.type"></a>

```typescript
public readonly type: PackageSourceType;
```

- *Type:* <a href="#projen.python.PackageSourceType">PackageSourceType</a>

index or find_links.

---

##### `username`<sup>Optional</sup> <a name="username" id="projen.python.PackageSource.property.username"></a>

```typescript
public readonly username: string;
```

- *Type:* string

Username for the package source.

---

##### `verifySsl`<sup>Optional</sup> <a name="verifySsl" id="projen.python.PackageSource.property.verifySsl"></a>

```typescript
public readonly verifySsl: boolean;
```

- *Type:* boolean

Whether to verify certificates when using the package source.

---

### PdmConfiguration <a name="PdmConfiguration" id="projen.python.PdmConfiguration"></a>

#### Initializer <a name="Initializer" id="projen.python.PdmConfiguration.Initializer"></a>

```typescript
import { python } from 'projen'

const pdmConfiguration: python.PdmConfiguration = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PdmConfiguration.property.buildConfig">buildConfig</a></code> | <code><a href="#projen.python.PdmConfigurationBuild">PdmConfigurationBuild</a></code> | Configuration related to the build process with pdm-backend. |
| <code><a href="#projen.python.PdmConfiguration.property.devDependencies">devDependencies</a></code> | <code><a href="#projen.python.PdmConfigurationDevDependencies">PdmConfigurationDevDependencies</a></code> | Development dependencies. |
| <code><a href="#projen.python.PdmConfiguration.property.distribution">distribution</a></code> | <code>boolean</code> | Whether the project needs to be distributed. |
| <code><a href="#projen.python.PdmConfiguration.property.dockerize">dockerize</a></code> | <code><a href="#projen.python.PdmConfigurationDockerize">PdmConfigurationDockerize</a></code> | PDM Dockerize plugin table in pyproject.toml. |
| <code><a href="#projen.python.PdmConfiguration.property.ignorePackageWarnings">ignorePackageWarnings</a></code> | <code>string[]</code> | Ignore package resolution warnings for packages matching the given patterns (PDM v2.10.0). |
| <code><a href="#projen.python.PdmConfiguration.property.options">options</a></code> | <code><a href="#projen.python.PdmConfigurationOptions">PdmConfigurationOptions</a></code> | Default options applied to a PDM command. |
| <code><a href="#projen.python.PdmConfiguration.property.plugins">plugins</a></code> | <code>string[]</code> | List of PDM plugins required for this project. |
| <code><a href="#projen.python.PdmConfiguration.property.resolution">resolution</a></code> | <code><a href="#projen.python.PdmConfigurationResolution">PdmConfigurationResolution</a></code> | Configuration related to package resolution. |
| <code><a href="#projen.python.PdmConfiguration.property.scripts">scripts</a></code> | <code><a href="#projen.python.PdmConfigurationScripts">PdmConfigurationScripts</a></code> | Definition of PDM scripts. |
| <code><a href="#projen.python.PdmConfiguration.property.source">source</a></code> | <code><a href="#projen.python.PackageSource">PackageSource</a>[]</code> | Sources used for package resolution. |
| <code><a href="#projen.python.PdmConfiguration.property.version">version</a></code> | <code><a href="#projen.python.PdmConfigurationVersion">PdmConfigurationVersion</a></code> | Dynamic version options for PDM-backend. |

---

##### `buildConfig`<sup>Optional</sup> <a name="buildConfig" id="projen.python.PdmConfiguration.property.buildConfig"></a>

```typescript
public readonly buildConfig: PdmConfigurationBuild;
```

- *Type:* <a href="#projen.python.PdmConfigurationBuild">PdmConfigurationBuild</a>

Configuration related to the build process with pdm-backend.

---

##### `devDependencies`<sup>Optional</sup> <a name="devDependencies" id="projen.python.PdmConfiguration.property.devDependencies"></a>

```typescript
public readonly devDependencies: PdmConfigurationDevDependencies;
```

- *Type:* <a href="#projen.python.PdmConfigurationDevDependencies">PdmConfigurationDevDependencies</a>

Development dependencies.

---

##### `distribution`<sup>Optional</sup> <a name="distribution" id="projen.python.PdmConfiguration.property.distribution"></a>

```typescript
public readonly distribution: boolean;
```

- *Type:* boolean

Whether the project needs to be distributed.

---

##### `dockerize`<sup>Optional</sup> <a name="dockerize" id="projen.python.PdmConfiguration.property.dockerize"></a>

```typescript
public readonly dockerize: PdmConfigurationDockerize;
```

- *Type:* <a href="#projen.python.PdmConfigurationDockerize">PdmConfigurationDockerize</a>

PDM Dockerize plugin table in pyproject.toml.

---

##### `ignorePackageWarnings`<sup>Optional</sup> <a name="ignorePackageWarnings" id="projen.python.PdmConfiguration.property.ignorePackageWarnings"></a>

```typescript
public readonly ignorePackageWarnings: string[];
```

- *Type:* string[]

Ignore package resolution warnings for packages matching the given patterns (PDM v2.10.0).

---

##### `options`<sup>Optional</sup> <a name="options" id="projen.python.PdmConfiguration.property.options"></a>

```typescript
public readonly options: PdmConfigurationOptions;
```

- *Type:* <a href="#projen.python.PdmConfigurationOptions">PdmConfigurationOptions</a>

Default options applied to a PDM command.

---

##### `plugins`<sup>Optional</sup> <a name="plugins" id="projen.python.PdmConfiguration.property.plugins"></a>

```typescript
public readonly plugins: string[];
```

- *Type:* string[]

List of PDM plugins required for this project.

---

##### `resolution`<sup>Optional</sup> <a name="resolution" id="projen.python.PdmConfiguration.property.resolution"></a>

```typescript
public readonly resolution: PdmConfigurationResolution;
```

- *Type:* <a href="#projen.python.PdmConfigurationResolution">PdmConfigurationResolution</a>

Configuration related to package resolution.

---

##### `scripts`<sup>Optional</sup> <a name="scripts" id="projen.python.PdmConfiguration.property.scripts"></a>

```typescript
public readonly scripts: PdmConfigurationScripts;
```

- *Type:* <a href="#projen.python.PdmConfigurationScripts">PdmConfigurationScripts</a>

Definition of PDM scripts.

---

##### `source`<sup>Optional</sup> <a name="source" id="projen.python.PdmConfiguration.property.source"></a>

```typescript
public readonly source: PackageSource[];
```

- *Type:* <a href="#projen.python.PackageSource">PackageSource</a>[]

Sources used for package resolution.

---

##### `version`<sup>Optional</sup> <a name="version" id="projen.python.PdmConfiguration.property.version"></a>

```typescript
public readonly version: PdmConfigurationVersion;
```

- *Type:* <a href="#projen.python.PdmConfigurationVersion">PdmConfigurationVersion</a>

Dynamic version options for PDM-backend.

---

### PdmConfigurationBuild <a name="PdmConfigurationBuild" id="projen.python.PdmConfigurationBuild"></a>

Configuration related to the build process with pdm-backend.

#### Initializer <a name="Initializer" id="projen.python.PdmConfigurationBuild.Initializer"></a>

```typescript
import { python } from 'projen'

const pdmConfigurationBuild: python.PdmConfigurationBuild = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PdmConfigurationBuild.property.customHook">customHook</a></code> | <code>string</code> | Python file that should be run before every build. |
| <code><a href="#projen.python.PdmConfigurationBuild.property.editableBackend">editableBackend</a></code> | <code><a href="#projen.python.PdmConfigurationBuildEditableBackend">PdmConfigurationBuildEditableBackend</a></code> | Format of editable wheels. |
| <code><a href="#projen.python.PdmConfigurationBuild.property.excludes">excludes</a></code> | <code>string[]</code> | Path patterns of files to be excluded from package. |
| <code><a href="#projen.python.PdmConfigurationBuild.property.includes">includes</a></code> | <code>string[]</code> | Path patterns of files to be included in the package. |
| <code><a href="#projen.python.PdmConfigurationBuild.property.isPurelib">isPurelib</a></code> | <code>boolean</code> | Whether builds should be platform specific. |
| <code><a href="#projen.python.PdmConfigurationBuild.property.packageDir">packageDir</a></code> | <code>string</code> | Directory where packages are located. |
| <code><a href="#projen.python.PdmConfigurationBuild.property.runSetuptools">runSetuptools</a></code> | <code>boolean</code> | Whether to auto-generate setup.py and run setuptools with it. Useful for building C extensions. |
| <code><a href="#projen.python.PdmConfigurationBuild.property.sourceIncludes">sourceIncludes</a></code> | <code>string[]</code> | Path patterns of files that are needed for building but not meant to be copied to the site-packages directory. |
| <code><a href="#projen.python.PdmConfigurationBuild.property.wheelData">wheelData</a></code> | <code><a href="#projen.python.PdmConfigurationBuildWheelData">PdmConfigurationBuildWheelData</a></code> | Configuration for additional files to be written to a user's site-packages directory. |

---

##### `customHook`<sup>Optional</sup> <a name="customHook" id="projen.python.PdmConfigurationBuild.property.customHook"></a>

```typescript
public readonly customHook: string;
```

- *Type:* string

Python file that should be run before every build.

---

##### `editableBackend`<sup>Optional</sup> <a name="editableBackend" id="projen.python.PdmConfigurationBuild.property.editableBackend"></a>

```typescript
public readonly editableBackend: PdmConfigurationBuildEditableBackend;
```

- *Type:* <a href="#projen.python.PdmConfigurationBuildEditableBackend">PdmConfigurationBuildEditableBackend</a>

Format of editable wheels.

---

##### `excludes`<sup>Optional</sup> <a name="excludes" id="projen.python.PdmConfigurationBuild.property.excludes"></a>

```typescript
public readonly excludes: string[];
```

- *Type:* string[]

Path patterns of files to be excluded from package.

---

##### `includes`<sup>Optional</sup> <a name="includes" id="projen.python.PdmConfigurationBuild.property.includes"></a>

```typescript
public readonly includes: string[];
```

- *Type:* string[]

Path patterns of files to be included in the package.

---

##### `isPurelib`<sup>Optional</sup> <a name="isPurelib" id="projen.python.PdmConfigurationBuild.property.isPurelib"></a>

```typescript
public readonly isPurelib: boolean;
```

- *Type:* boolean

Whether builds should be platform specific.

---

##### `packageDir`<sup>Optional</sup> <a name="packageDir" id="projen.python.PdmConfigurationBuild.property.packageDir"></a>

```typescript
public readonly packageDir: string;
```

- *Type:* string

Directory where packages are located.

The default value is src if src/ is found and . otherwise

---

##### `runSetuptools`<sup>Optional</sup> <a name="runSetuptools" id="projen.python.PdmConfigurationBuild.property.runSetuptools"></a>

```typescript
public readonly runSetuptools: boolean;
```

- *Type:* boolean

Whether to auto-generate setup.py and run setuptools with it. Useful for building C extensions.

---

##### `sourceIncludes`<sup>Optional</sup> <a name="sourceIncludes" id="projen.python.PdmConfigurationBuild.property.sourceIncludes"></a>

```typescript
public readonly sourceIncludes: string[];
```

- *Type:* string[]

Path patterns of files that are needed for building but not meant to be copied to the site-packages directory.

---

##### `wheelData`<sup>Optional</sup> <a name="wheelData" id="projen.python.PdmConfigurationBuild.property.wheelData"></a>

```typescript
public readonly wheelData: PdmConfigurationBuildWheelData;
```

- *Type:* <a href="#projen.python.PdmConfigurationBuildWheelData">PdmConfigurationBuildWheelData</a>

Configuration for additional files to be written to a user's site-packages directory.

---

### PdmConfigurationBuildWheelData <a name="PdmConfigurationBuildWheelData" id="projen.python.PdmConfigurationBuildWheelData"></a>

Configuration for additional files to be written to a user's site-packages directory.

#### Initializer <a name="Initializer" id="projen.python.PdmConfigurationBuildWheelData.Initializer"></a>

```typescript
import { python } from 'projen'

const pdmConfigurationBuildWheelData: python.PdmConfigurationBuildWheelData = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PdmConfigurationBuildWheelData.property.data">data</a></code> | <code>any[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationBuildWheelData.property.include">include</a></code> | <code>any[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationBuildWheelData.property.platinclude">platinclude</a></code> | <code>any[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationBuildWheelData.property.platlib">platlib</a></code> | <code>any[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationBuildWheelData.property.purelib">purelib</a></code> | <code>any[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationBuildWheelData.property.scripts">scripts</a></code> | <code>any[]</code> | *No description.* |

---

##### `data`<sup>Optional</sup> <a name="data" id="projen.python.PdmConfigurationBuildWheelData.property.data"></a>

```typescript
public readonly data: any[];
```

- *Type:* any[]

---

##### `include`<sup>Optional</sup> <a name="include" id="projen.python.PdmConfigurationBuildWheelData.property.include"></a>

```typescript
public readonly include: any[];
```

- *Type:* any[]

---

##### `platinclude`<sup>Optional</sup> <a name="platinclude" id="projen.python.PdmConfigurationBuildWheelData.property.platinclude"></a>

```typescript
public readonly platinclude: any[];
```

- *Type:* any[]

---

##### `platlib`<sup>Optional</sup> <a name="platlib" id="projen.python.PdmConfigurationBuildWheelData.property.platlib"></a>

```typescript
public readonly platlib: any[];
```

- *Type:* any[]

---

##### `purelib`<sup>Optional</sup> <a name="purelib" id="projen.python.PdmConfigurationBuildWheelData.property.purelib"></a>

```typescript
public readonly purelib: any[];
```

- *Type:* any[]

---

##### `scripts`<sup>Optional</sup> <a name="scripts" id="projen.python.PdmConfigurationBuildWheelData.property.scripts"></a>

```typescript
public readonly scripts: any[];
```

- *Type:* any[]

---

### PdmConfigurationDevDependencies <a name="PdmConfigurationDevDependencies" id="projen.python.PdmConfigurationDevDependencies"></a>

Development dependencies.

#### Initializer <a name="Initializer" id="projen.python.PdmConfigurationDevDependencies.Initializer"></a>

```typescript
import { python } from 'projen'

const pdmConfigurationDevDependencies: python.PdmConfigurationDevDependencies = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PdmConfigurationDevDependencies.property.dev">dev</a></code> | <code>string[]</code> | The default dev dependencies. |

---

##### `dev`<sup>Optional</sup> <a name="dev" id="projen.python.PdmConfigurationDevDependencies.property.dev"></a>

```typescript
public readonly dev: string[];
```

- *Type:* string[]

The default dev dependencies.

---

### PdmConfigurationDockerize <a name="PdmConfigurationDockerize" id="projen.python.PdmConfigurationDockerize"></a>

PDM Dockerize plugin table in pyproject.toml.

#### Initializer <a name="Initializer" id="projen.python.PdmConfigurationDockerize.Initializer"></a>

```typescript
import { python } from 'projen'

const pdmConfigurationDockerize: python.PdmConfigurationDockerize = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PdmConfigurationDockerize.property.env">env</a></code> | <code>{[ key: string ]: string}</code> | Environment variables applied when running the script. |
| <code><a href="#projen.python.PdmConfigurationDockerize.property.envFile">envFile</a></code> | <code>string</code> | Path to a file with environment variables. |
| <code><a href="#projen.python.PdmConfigurationDockerize.property.exclude">exclude</a></code> | <code>string[]</code> | fnmatch filter patterns for excluded PDM scripts. |
| <code><a href="#projen.python.PdmConfigurationDockerize.property.excludeBins">excludeBins</a></code> | <code>string[]</code> | fnmatch filter patterns for excluded binaries. |
| <code><a href="#projen.python.PdmConfigurationDockerize.property.include">include</a></code> | <code>string[]</code> | fnmatch filter patterns for included PDM scripts. |
| <code><a href="#projen.python.PdmConfigurationDockerize.property.includeBins">includeBins</a></code> | <code>string[]</code> | fnmatch filter patterns for included binaries. |

---

##### `env`<sup>Optional</sup> <a name="env" id="projen.python.PdmConfigurationDockerize.property.env"></a>

```typescript
public readonly env: {[ key: string ]: string};
```

- *Type:* {[ key: string ]: string}

Environment variables applied when running the script.

---

##### `envFile`<sup>Optional</sup> <a name="envFile" id="projen.python.PdmConfigurationDockerize.property.envFile"></a>

```typescript
public readonly envFile: string;
```

- *Type:* string

Path to a file with environment variables.

---

##### `exclude`<sup>Optional</sup> <a name="exclude" id="projen.python.PdmConfigurationDockerize.property.exclude"></a>

```typescript
public readonly exclude: string[];
```

- *Type:* string[]

fnmatch filter patterns for excluded PDM scripts.

---

##### `excludeBins`<sup>Optional</sup> <a name="excludeBins" id="projen.python.PdmConfigurationDockerize.property.excludeBins"></a>

```typescript
public readonly excludeBins: string[];
```

- *Type:* string[]

fnmatch filter patterns for excluded binaries.

---

##### `include`<sup>Optional</sup> <a name="include" id="projen.python.PdmConfigurationDockerize.property.include"></a>

```typescript
public readonly include: string[];
```

- *Type:* string[]

fnmatch filter patterns for included PDM scripts.

---

##### `includeBins`<sup>Optional</sup> <a name="includeBins" id="projen.python.PdmConfigurationDockerize.property.includeBins"></a>

```typescript
public readonly includeBins: string[];
```

- *Type:* string[]

fnmatch filter patterns for included binaries.

---

### PdmConfigurationOptions <a name="PdmConfigurationOptions" id="projen.python.PdmConfigurationOptions"></a>

Default options applied to a PDM command.

#### Initializer <a name="Initializer" id="projen.python.PdmConfigurationOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const pdmConfigurationOptions: python.PdmConfigurationOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PdmConfigurationOptions.property.add">add</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.buildConfig">buildConfig</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.cache">cache</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.completion">completion</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.config">config</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.export">export</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.fix">fix</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.import">import</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.info">info</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.init">init</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.install">install</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.list">list</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.lock">lock</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.publish">publish</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.remove">remove</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.run">run</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.search">search</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.self">self</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.show">show</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.sync">sync</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.update">update</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.use">use</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationOptions.property.venv">venv</a></code> | <code>string[]</code> | *No description.* |

---

##### `add`<sup>Optional</sup> <a name="add" id="projen.python.PdmConfigurationOptions.property.add"></a>

```typescript
public readonly add: string[];
```

- *Type:* string[]

---

##### `buildConfig`<sup>Optional</sup> <a name="buildConfig" id="projen.python.PdmConfigurationOptions.property.buildConfig"></a>

```typescript
public readonly buildConfig: string[];
```

- *Type:* string[]

---

##### `cache`<sup>Optional</sup> <a name="cache" id="projen.python.PdmConfigurationOptions.property.cache"></a>

```typescript
public readonly cache: string[];
```

- *Type:* string[]

---

##### `completion`<sup>Optional</sup> <a name="completion" id="projen.python.PdmConfigurationOptions.property.completion"></a>

```typescript
public readonly completion: string[];
```

- *Type:* string[]

---

##### `config`<sup>Optional</sup> <a name="config" id="projen.python.PdmConfigurationOptions.property.config"></a>

```typescript
public readonly config: string[];
```

- *Type:* string[]

---

##### `export`<sup>Optional</sup> <a name="export" id="projen.python.PdmConfigurationOptions.property.export"></a>

```typescript
public readonly export: string[];
```

- *Type:* string[]

---

##### `fix`<sup>Optional</sup> <a name="fix" id="projen.python.PdmConfigurationOptions.property.fix"></a>

```typescript
public readonly fix: string[];
```

- *Type:* string[]

---

##### `import`<sup>Optional</sup> <a name="import" id="projen.python.PdmConfigurationOptions.property.import"></a>

```typescript
public readonly import: string[];
```

- *Type:* string[]

---

##### `info`<sup>Optional</sup> <a name="info" id="projen.python.PdmConfigurationOptions.property.info"></a>

```typescript
public readonly info: string[];
```

- *Type:* string[]

---

##### `init`<sup>Optional</sup> <a name="init" id="projen.python.PdmConfigurationOptions.property.init"></a>

```typescript
public readonly init: string[];
```

- *Type:* string[]

---

##### `install`<sup>Optional</sup> <a name="install" id="projen.python.PdmConfigurationOptions.property.install"></a>

```typescript
public readonly install: string[];
```

- *Type:* string[]

---

##### `list`<sup>Optional</sup> <a name="list" id="projen.python.PdmConfigurationOptions.property.list"></a>

```typescript
public readonly list: string[];
```

- *Type:* string[]

---

##### `lock`<sup>Optional</sup> <a name="lock" id="projen.python.PdmConfigurationOptions.property.lock"></a>

```typescript
public readonly lock: string[];
```

- *Type:* string[]

---

##### `publish`<sup>Optional</sup> <a name="publish" id="projen.python.PdmConfigurationOptions.property.publish"></a>

```typescript
public readonly publish: string[];
```

- *Type:* string[]

---

##### `remove`<sup>Optional</sup> <a name="remove" id="projen.python.PdmConfigurationOptions.property.remove"></a>

```typescript
public readonly remove: string[];
```

- *Type:* string[]

---

##### `run`<sup>Optional</sup> <a name="run" id="projen.python.PdmConfigurationOptions.property.run"></a>

```typescript
public readonly run: string[];
```

- *Type:* string[]

---

##### `search`<sup>Optional</sup> <a name="search" id="projen.python.PdmConfigurationOptions.property.search"></a>

```typescript
public readonly search: string[];
```

- *Type:* string[]

---

##### `self`<sup>Optional</sup> <a name="self" id="projen.python.PdmConfigurationOptions.property.self"></a>

```typescript
public readonly self: string[];
```

- *Type:* string[]

---

##### `show`<sup>Optional</sup> <a name="show" id="projen.python.PdmConfigurationOptions.property.show"></a>

```typescript
public readonly show: string[];
```

- *Type:* string[]

---

##### `sync`<sup>Optional</sup> <a name="sync" id="projen.python.PdmConfigurationOptions.property.sync"></a>

```typescript
public readonly sync: string[];
```

- *Type:* string[]

---

##### `update`<sup>Optional</sup> <a name="update" id="projen.python.PdmConfigurationOptions.property.update"></a>

```typescript
public readonly update: string[];
```

- *Type:* string[]

---

##### `use`<sup>Optional</sup> <a name="use" id="projen.python.PdmConfigurationOptions.property.use"></a>

```typescript
public readonly use: string[];
```

- *Type:* string[]

---

##### `venv`<sup>Optional</sup> <a name="venv" id="projen.python.PdmConfigurationOptions.property.venv"></a>

```typescript
public readonly venv: string[];
```

- *Type:* string[]

---

### PdmConfigurationResolution <a name="PdmConfigurationResolution" id="projen.python.PdmConfigurationResolution"></a>

Configuration related to package resolution.

#### Initializer <a name="Initializer" id="projen.python.PdmConfigurationResolution.Initializer"></a>

```typescript
import { python } from 'projen'

const pdmConfigurationResolution: python.PdmConfigurationResolution = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PdmConfigurationResolution.property.allowPrereleases">allowPrereleases</a></code> | <code>boolean</code> | Whether to allow the use of pre-releases when resolving packages. |
| <code><a href="#projen.python.PdmConfigurationResolution.property.excludes">excludes</a></code> | <code>string[]</code> | Package names to be excluded from the resolution result. |
| <code><a href="#projen.python.PdmConfigurationResolution.property.overrides">overrides</a></code> | <code>{[ key: string ]: string}</code> | Resolution overrides for forcing specific dependency versions. |
| <code><a href="#projen.python.PdmConfigurationResolution.property.respectSourceOrder">respectSourceOrder</a></code> | <code>boolean</code> | Whether PDM prefers package sources by order. |

---

##### `allowPrereleases`<sup>Optional</sup> <a name="allowPrereleases" id="projen.python.PdmConfigurationResolution.property.allowPrereleases"></a>

```typescript
public readonly allowPrereleases: boolean;
```

- *Type:* boolean

Whether to allow the use of pre-releases when resolving packages.

---

##### `excludes`<sup>Optional</sup> <a name="excludes" id="projen.python.PdmConfigurationResolution.property.excludes"></a>

```typescript
public readonly excludes: string[];
```

- *Type:* string[]

Package names to be excluded from the resolution result.

---

##### `overrides`<sup>Optional</sup> <a name="overrides" id="projen.python.PdmConfigurationResolution.property.overrides"></a>

```typescript
public readonly overrides: {[ key: string ]: string};
```

- *Type:* {[ key: string ]: string}

Resolution overrides for forcing specific dependency versions.

---

##### `respectSourceOrder`<sup>Optional</sup> <a name="respectSourceOrder" id="projen.python.PdmConfigurationResolution.property.respectSourceOrder"></a>

```typescript
public readonly respectSourceOrder: boolean;
```

- *Type:* boolean

Whether PDM prefers package sources by order.

---

### PdmConfigurationScripts <a name="PdmConfigurationScripts" id="projen.python.PdmConfigurationScripts"></a>

Definition of PDM scripts.

#### Initializer <a name="Initializer" id="projen.python.PdmConfigurationScripts.Initializer"></a>

```typescript
import { python } from 'projen'

const pdmConfigurationScripts: python.PdmConfigurationScripts = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PdmConfigurationScripts.property.postBuild">postBuild</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationScripts.property.postInit">postInit</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationScripts.property.postInstall">postInstall</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationScripts.property.postLock">postLock</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationScripts.property.postPublish">postPublish</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationScripts.property.postRun">postRun</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationScripts.property.postScript">postScript</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationScripts.property.preBuild">preBuild</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationScripts.property.preInstall">preInstall</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationScripts.property.preLock">preLock</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationScripts.property.prePublish">prePublish</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationScripts.property.preRun">preRun</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationScripts.property.preScript">preScript</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationScripts.property.scripts">scripts</a></code> | <code><a href="#projen.python.PdmConfigurationScriptsEnv">PdmConfigurationScriptsEnv</a></code> | Shared options for all scripts https://pdm-project.org/latest/usage/scripts/#shared-options. |

---

##### `postBuild`<sup>Optional</sup> <a name="postBuild" id="projen.python.PdmConfigurationScripts.property.postBuild"></a>

```typescript
public readonly postBuild: any;
```

- *Type:* any

---

##### `postInit`<sup>Optional</sup> <a name="postInit" id="projen.python.PdmConfigurationScripts.property.postInit"></a>

```typescript
public readonly postInit: any;
```

- *Type:* any

---

##### `postInstall`<sup>Optional</sup> <a name="postInstall" id="projen.python.PdmConfigurationScripts.property.postInstall"></a>

```typescript
public readonly postInstall: any;
```

- *Type:* any

---

##### `postLock`<sup>Optional</sup> <a name="postLock" id="projen.python.PdmConfigurationScripts.property.postLock"></a>

```typescript
public readonly postLock: any;
```

- *Type:* any

---

##### `postPublish`<sup>Optional</sup> <a name="postPublish" id="projen.python.PdmConfigurationScripts.property.postPublish"></a>

```typescript
public readonly postPublish: any;
```

- *Type:* any

---

##### `postRun`<sup>Optional</sup> <a name="postRun" id="projen.python.PdmConfigurationScripts.property.postRun"></a>

```typescript
public readonly postRun: any;
```

- *Type:* any

---

##### `postScript`<sup>Optional</sup> <a name="postScript" id="projen.python.PdmConfigurationScripts.property.postScript"></a>

```typescript
public readonly postScript: any;
```

- *Type:* any

---

##### `preBuild`<sup>Optional</sup> <a name="preBuild" id="projen.python.PdmConfigurationScripts.property.preBuild"></a>

```typescript
public readonly preBuild: any;
```

- *Type:* any

---

##### `preInstall`<sup>Optional</sup> <a name="preInstall" id="projen.python.PdmConfigurationScripts.property.preInstall"></a>

```typescript
public readonly preInstall: any;
```

- *Type:* any

---

##### `preLock`<sup>Optional</sup> <a name="preLock" id="projen.python.PdmConfigurationScripts.property.preLock"></a>

```typescript
public readonly preLock: any;
```

- *Type:* any

---

##### `prePublish`<sup>Optional</sup> <a name="prePublish" id="projen.python.PdmConfigurationScripts.property.prePublish"></a>

```typescript
public readonly prePublish: any;
```

- *Type:* any

---

##### `preRun`<sup>Optional</sup> <a name="preRun" id="projen.python.PdmConfigurationScripts.property.preRun"></a>

```typescript
public readonly preRun: any;
```

- *Type:* any

---

##### `preScript`<sup>Optional</sup> <a name="preScript" id="projen.python.PdmConfigurationScripts.property.preScript"></a>

```typescript
public readonly preScript: any;
```

- *Type:* any

---

##### `scripts`<sup>Optional</sup> <a name="scripts" id="projen.python.PdmConfigurationScripts.property.scripts"></a>

```typescript
public readonly scripts: PdmConfigurationScriptsEnv;
```

- *Type:* <a href="#projen.python.PdmConfigurationScriptsEnv">PdmConfigurationScriptsEnv</a>

Shared options for all scripts https://pdm-project.org/latest/usage/scripts/#shared-options.

---

### PdmConfigurationScriptsEnv <a name="PdmConfigurationScriptsEnv" id="projen.python.PdmConfigurationScriptsEnv"></a>

Shared options for all scripts https://pdm-project.org/latest/usage/scripts/#shared-options.

#### Initializer <a name="Initializer" id="projen.python.PdmConfigurationScriptsEnv.Initializer"></a>

```typescript
import { python } from 'projen'

const pdmConfigurationScriptsEnv: python.PdmConfigurationScriptsEnv = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PdmConfigurationScriptsEnv.property.env">env</a></code> | <code>{[ key: string ]: string}</code> | *No description.* |
| <code><a href="#projen.python.PdmConfigurationScriptsEnv.property.envFile">envFile</a></code> | <code>any</code> | *No description.* |

---

##### `env`<sup>Optional</sup> <a name="env" id="projen.python.PdmConfigurationScriptsEnv.property.env"></a>

```typescript
public readonly env: {[ key: string ]: string};
```

- *Type:* {[ key: string ]: string}

---

##### `envFile`<sup>Optional</sup> <a name="envFile" id="projen.python.PdmConfigurationScriptsEnv.property.envFile"></a>

```typescript
public readonly envFile: any;
```

- *Type:* any

---

### PdmConfigurationVersion <a name="PdmConfigurationVersion" id="projen.python.PdmConfigurationVersion"></a>

Dynamic version options for PDM-backend.

#### Initializer <a name="Initializer" id="projen.python.PdmConfigurationVersion.Initializer"></a>

```typescript
import { python } from 'projen'

const pdmConfigurationVersion: python.PdmConfigurationVersion = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PdmConfigurationVersion.property.source">source</a></code> | <code><a href="#projen.python.PdmConfigurationVersionSource">PdmConfigurationVersionSource</a></code> | Dynamic version source. |
| <code><a href="#projen.python.PdmConfigurationVersion.property.fallbackVersion">fallbackVersion</a></code> | <code>string</code> | Specify a default version to be used when building from a source tree where SCM is not available (since pdm-backend v2.2.0). |
| <code><a href="#projen.python.PdmConfigurationVersion.property.getter">getter</a></code> | <code>string</code> | Python callable for reading version. |
| <code><a href="#projen.python.PdmConfigurationVersion.property.path">path</a></code> | <code>string</code> | Path to python file to read version from (file source). |
| <code><a href="#projen.python.PdmConfigurationVersion.property.tagFilter">tagFilter</a></code> | <code>string</code> | Filters the set of tags which are considered as candidates to capture your project's version (scm source). |
| <code><a href="#projen.python.PdmConfigurationVersion.property.tagRegex">tagRegex</a></code> | <code>string</code> | Regex for reading version from source control tag (scm source). |
| <code><a href="#projen.python.PdmConfigurationVersion.property.versionFormat">versionFormat</a></code> | <code>string</code> | Used to customize the format of the version string (since pdm-backend v2.2.0). |
| <code><a href="#projen.python.PdmConfigurationVersion.property.writeTemplate">writeTemplate</a></code> | <code>string</code> | Template to use for generated version file. |
| <code><a href="#projen.python.PdmConfigurationVersion.property.writeTo">writeTo</a></code> | <code>string</code> | File path a generated version should be written to. |

---

##### `source`<sup>Required</sup> <a name="source" id="projen.python.PdmConfigurationVersion.property.source"></a>

```typescript
public readonly source: PdmConfigurationVersionSource;
```

- *Type:* <a href="#projen.python.PdmConfigurationVersionSource">PdmConfigurationVersionSource</a>

Dynamic version source.

---

##### `fallbackVersion`<sup>Optional</sup> <a name="fallbackVersion" id="projen.python.PdmConfigurationVersion.property.fallbackVersion"></a>

```typescript
public readonly fallbackVersion: string;
```

- *Type:* string

Specify a default version to be used when building from a source tree where SCM is not available (since pdm-backend v2.2.0).

---

##### `getter`<sup>Optional</sup> <a name="getter" id="projen.python.PdmConfigurationVersion.property.getter"></a>

```typescript
public readonly getter: string;
```

- *Type:* string

Python callable for reading version.

---

##### `path`<sup>Optional</sup> <a name="path" id="projen.python.PdmConfigurationVersion.property.path"></a>

```typescript
public readonly path: string;
```

- *Type:* string

Path to python file to read version from (file source).

---

##### `tagFilter`<sup>Optional</sup> <a name="tagFilter" id="projen.python.PdmConfigurationVersion.property.tagFilter"></a>

```typescript
public readonly tagFilter: string;
```

- *Type:* string

Filters the set of tags which are considered as candidates to capture your project's version (scm source).

---

##### `tagRegex`<sup>Optional</sup> <a name="tagRegex" id="projen.python.PdmConfigurationVersion.property.tagRegex"></a>

```typescript
public readonly tagRegex: string;
```

- *Type:* string

Regex for reading version from source control tag (scm source).

---

##### `versionFormat`<sup>Optional</sup> <a name="versionFormat" id="projen.python.PdmConfigurationVersion.property.versionFormat"></a>

```typescript
public readonly versionFormat: string;
```

- *Type:* string

Used to customize the format of the version string (since pdm-backend v2.2.0).

---

##### `writeTemplate`<sup>Optional</sup> <a name="writeTemplate" id="projen.python.PdmConfigurationVersion.property.writeTemplate"></a>

```typescript
public readonly writeTemplate: string;
```

- *Type:* string

Template to use for generated version file.

---

##### `writeTo`<sup>Optional</sup> <a name="writeTo" id="projen.python.PdmConfigurationVersion.property.writeTo"></a>

```typescript
public readonly writeTo: string;
```

- *Type:* string

File path a generated version should be written to.

---

### Pep8NamingOptions <a name="Pep8NamingOptions" id="projen.python.Pep8NamingOptions"></a>

Options for the `pep8-naming` plugin.

#### Initializer <a name="Initializer" id="projen.python.Pep8NamingOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const pep8NamingOptions: python.Pep8NamingOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Pep8NamingOptions.property.classmethodDecorators">classmethodDecorators</a></code> | <code>string[]</code> | A list of decorators that, when applied to a method, indicate that the method should be treated as a class method (in addition to the builtin `@classmethod`). |
| <code><a href="#projen.python.Pep8NamingOptions.property.extendIgnoreNames">extendIgnoreNames</a></code> | <code>string[]</code> | Additional names (or patterns) to ignore when considering `pep8-naming` violations, in addition to those included in [`ignore-names`](#lint_pep8-naming_ignore-names). |
| <code><a href="#projen.python.Pep8NamingOptions.property.ignoreNames">ignoreNames</a></code> | <code>string[]</code> | A list of names (or patterns) to ignore when considering `pep8-naming` violations. |
| <code><a href="#projen.python.Pep8NamingOptions.property.staticmethodDecorators">staticmethodDecorators</a></code> | <code>string[]</code> | A list of decorators that, when applied to a method, indicate that the method should be treated as a static method (in addition to the builtin `@staticmethod`). |

---

##### `classmethodDecorators`<sup>Optional</sup> <a name="classmethodDecorators" id="projen.python.Pep8NamingOptions.property.classmethodDecorators"></a>

```typescript
public readonly classmethodDecorators: string[];
```

- *Type:* string[]

A list of decorators that, when applied to a method, indicate that the method should be treated as a class method (in addition to the builtin `@classmethod`).

For example, Ruff will expect that any method decorated by a decorator
in this list takes a `cls` argument as its first argument.

Expects to receive a list of fully-qualified names (e.g., `pydantic.validator`,
rather than `validator`) or alternatively a plain name which is then matched against
the last segment in case the decorator itself consists of a dotted name.

---

##### `extendIgnoreNames`<sup>Optional</sup> <a name="extendIgnoreNames" id="projen.python.Pep8NamingOptions.property.extendIgnoreNames"></a>

```typescript
public readonly extendIgnoreNames: string[];
```

- *Type:* string[]

Additional names (or patterns) to ignore when considering `pep8-naming` violations, in addition to those included in [`ignore-names`](#lint_pep8-naming_ignore-names).

Supports glob patterns. For example, to ignore all names starting with `test_`
or ending with `_test`, you could use `ignore-names = ["test_*", "*_test"]`.
For more information on the glob syntax, refer to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).

---

##### `ignoreNames`<sup>Optional</sup> <a name="ignoreNames" id="projen.python.Pep8NamingOptions.property.ignoreNames"></a>

```typescript
public readonly ignoreNames: string[];
```

- *Type:* string[]

A list of names (or patterns) to ignore when considering `pep8-naming` violations.

Supports glob patterns. For example, to ignore all names starting with `test_`
or ending with `_test`, you could use `ignore-names = ["test_*", "*_test"]`.
For more information on the glob syntax, refer to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).

---

##### `staticmethodDecorators`<sup>Optional</sup> <a name="staticmethodDecorators" id="projen.python.Pep8NamingOptions.property.staticmethodDecorators"></a>

```typescript
public readonly staticmethodDecorators: string[];
```

- *Type:* string[]

A list of decorators that, when applied to a method, indicate that the method should be treated as a static method (in addition to the builtin `@staticmethod`).

For example, Ruff will expect that any method decorated by a decorator
in this list has no `self` or `cls` argument.

Expects to receive a list of fully-qualified names (e.g., `belay.Device.teardown`,
rather than `teardown`) or alternatively a plain name which is then matched against
the last segment in case the decorator itself consists of a dotted name.

---

### PipGroupName <a name="PipGroupName" id="projen.python.PipGroupName"></a>

The pip-compatible variant of a [`GroupName`].

Either <groupname> or <path>:<groupname>.
If <path> is omitted it defaults to "pyproject.toml".

#### Initializer <a name="Initializer" id="projen.python.PipGroupName.Initializer"></a>

```typescript
import { python } from 'projen'

const pipGroupName: python.PipGroupName = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PipGroupName.property.name">name</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.PipGroupName.property.path">path</a></code> | <code>string</code> | *No description.* |

---

##### `name`<sup>Required</sup> <a name="name" id="projen.python.PipGroupName.property.name"></a>

```typescript
public readonly name: string;
```

- *Type:* string

---

##### `path`<sup>Optional</sup> <a name="path" id="projen.python.PipGroupName.property.path"></a>

```typescript
public readonly path: string;
```

- *Type:* string

---

### PipOptions <a name="PipOptions" id="projen.python.PipOptions"></a>

Settings that are specific to the `uv pip` command-line interface.

These values will be ignored when running commands outside the `uv pip` namespace (e.g.,
`uv lock`, `uvx`).

#### Initializer <a name="Initializer" id="projen.python.PipOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const pipOptions: python.PipOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PipOptions.property.allExtras">allExtras</a></code> | <code>boolean</code> | Include all optional dependencies. |
| <code><a href="#projen.python.PipOptions.property.allowEmptyRequirements">allowEmptyRequirements</a></code> | <code>boolean</code> | Allow `uv pip sync` with empty requirements, which will clear the environment of all packages. |
| <code><a href="#projen.python.PipOptions.property.annotationStyle">annotationStyle</a></code> | <code>string</code> | The style of the annotation comments included in the output file, used to indicate the source of each package. |
| <code><a href="#projen.python.PipOptions.property.breakSystemPackages">breakSystemPackages</a></code> | <code>boolean</code> | Allow uv to modify an `EXTERNALLY-MANAGED` Python installation. |
| <code><a href="#projen.python.PipOptions.property.compileBytecode">compileBytecode</a></code> | <code>boolean</code> | Compile Python files to bytecode after installation. |
| <code><a href="#projen.python.PipOptions.property.configSettings">configSettings</a></code> | <code>{[ key: string ]: any}</code> | Settings to pass to the [PEP 517](https://peps.python.org/pep-0517/) build backend, specified as `KEY=VALUE` pairs. |
| <code><a href="#projen.python.PipOptions.property.configSettingsPackage">configSettingsPackage</a></code> | <code>{[ key: string ]: {[ key: string ]: any}}</code> | Settings to pass to the [PEP 517](https://peps.python.org/pep-0517/) build backend for specific packages, specified as `KEY=VALUE` pairs. |
| <code><a href="#projen.python.PipOptions.property.customCompileCommand">customCompileCommand</a></code> | <code>string</code> | The header comment to include at the top of the output file generated by `uv pip compile`. |
| <code><a href="#projen.python.PipOptions.property.dependencyMetadata">dependencyMetadata</a></code> | <code><a href="#projen.python.StaticMetadata">StaticMetadata</a>[]</code> | Pre-defined static metadata for dependencies of the project (direct or transitive). |
| <code><a href="#projen.python.PipOptions.property.emitBuildOptions">emitBuildOptions</a></code> | <code>boolean</code> | Include `--no-binary` and `--only-binary` entries in the output file generated by `uv pip compile`. |
| <code><a href="#projen.python.PipOptions.property.emitFindLinks">emitFindLinks</a></code> | <code>boolean</code> | Include `--find-links` entries in the output file generated by `uv pip compile`. |
| <code><a href="#projen.python.PipOptions.property.emitIndexAnnotation">emitIndexAnnotation</a></code> | <code>boolean</code> | Include comment annotations indicating the index used to resolve each package (e.g., `# from https://pypi.org/simple`). |
| <code><a href="#projen.python.PipOptions.property.emitIndexUrl">emitIndexUrl</a></code> | <code>boolean</code> | Include `--index-url` and `--extra-index-url` entries in the output file generated by `uv pip compile`. |
| <code><a href="#projen.python.PipOptions.property.emitMarkerExpression">emitMarkerExpression</a></code> | <code>boolean</code> | Whether to emit a marker string indicating the conditions under which the set of pinned dependencies is valid. |
| <code><a href="#projen.python.PipOptions.property.excludeNewer">excludeNewer</a></code> | <code>string</code> | Limit candidate packages to those that were uploaded prior to a given point in time. |
| <code><a href="#projen.python.PipOptions.property.excludeNewerPackage">excludeNewerPackage</a></code> | <code>{[ key: string ]: string}</code> | Limit candidate packages for specific packages to those that were uploaded prior to the given date. |
| <code><a href="#projen.python.PipOptions.property.extra">extra</a></code> | <code>string[]</code> | Include optional dependencies from the specified extra; may be provided more than once. |
| <code><a href="#projen.python.PipOptions.property.extraBuildDependencies">extraBuildDependencies</a></code> | <code>{[ key: string ]: any[]}</code> | Additional build dependencies for packages. |
| <code><a href="#projen.python.PipOptions.property.extraBuildVariables">extraBuildVariables</a></code> | <code>{[ key: string ]: {[ key: string ]: string}}</code> | Extra environment variables to set when building certain packages. |
| <code><a href="#projen.python.PipOptions.property.extraIndexUrl">extraIndexUrl</a></code> | <code>string[]</code> | Extra URLs of package indexes to use, in addition to `--index-url`. |
| <code><a href="#projen.python.PipOptions.property.findLinks">findLinks</a></code> | <code>string[]</code> | Locations to search for candidate distributions, in addition to those found in the registry indexes. |
| <code><a href="#projen.python.PipOptions.property.forkStrategy">forkStrategy</a></code> | <code>string</code> | The strategy to use when selecting multiple versions of a given package across Python versions and platforms. |
| <code><a href="#projen.python.PipOptions.property.generateHashes">generateHashes</a></code> | <code>boolean</code> | Include distribution hashes in the output file. |
| <code><a href="#projen.python.PipOptions.property.group">group</a></code> | <code><a href="#projen.python.PipGroupName">PipGroupName</a>[]</code> | Include the following dependency groups. |
| <code><a href="#projen.python.PipOptions.property.indexStrategy">indexStrategy</a></code> | <code>string</code> | The strategy to use when resolving against multiple index URLs. |
| <code><a href="#projen.python.PipOptions.property.indexUrl">indexUrl</a></code> | <code>string</code> | The URL of the Python package index (by default: <https://pypi.org/simple>). |
| <code><a href="#projen.python.PipOptions.property.keyringProvider">keyringProvider</a></code> | <code>string</code> | Attempt to use `keyring` for authentication for index URLs. |
| <code><a href="#projen.python.PipOptions.property.linkMode">linkMode</a></code> | <code>string</code> | The method to use when installing packages from the global cache. |
| <code><a href="#projen.python.PipOptions.property.noAnnotate">noAnnotate</a></code> | <code>boolean</code> | Exclude comment annotations indicating the source of each package from the output file generated by `uv pip compile`. |
| <code><a href="#projen.python.PipOptions.property.noBinary">noBinary</a></code> | <code>string[]</code> | Don't install pre-built wheels. |
| <code><a href="#projen.python.PipOptions.property.noBuild">noBuild</a></code> | <code>boolean</code> | Don't build source distributions. |
| <code><a href="#projen.python.PipOptions.property.noBuildIsolation">noBuildIsolation</a></code> | <code>boolean</code> | Disable isolation when building source distributions. |
| <code><a href="#projen.python.PipOptions.property.noBuildIsolationPackage">noBuildIsolationPackage</a></code> | <code>string[]</code> | Disable isolation when building source distributions for a specific package. |
| <code><a href="#projen.python.PipOptions.property.noDeps">noDeps</a></code> | <code>boolean</code> | Ignore package dependencies, instead only add those packages explicitly listed on the command line to the resulting requirements file. |
| <code><a href="#projen.python.PipOptions.property.noEmitPackage">noEmitPackage</a></code> | <code>string[]</code> | Specify a package to omit from the output resolution. |
| <code><a href="#projen.python.PipOptions.property.noExtra">noExtra</a></code> | <code>string[]</code> | Exclude the specified optional dependencies if `all-extras` is supplied. |
| <code><a href="#projen.python.PipOptions.property.noHeader">noHeader</a></code> | <code>boolean</code> | Exclude the comment header at the top of output file generated by `uv pip compile`. |
| <code><a href="#projen.python.PipOptions.property.noIndex">noIndex</a></code> | <code>boolean</code> | Ignore all registry indexes (e.g., PyPI), instead relying on direct URL dependencies and those provided via `--find-links`. |
| <code><a href="#projen.python.PipOptions.property.noSources">noSources</a></code> | <code>boolean</code> | Ignore the `tool.uv.sources` table when resolving dependencies. Used to lock against the standards-compliant, publishable package metadata, as opposed to using any local or Git sources. |
| <code><a href="#projen.python.PipOptions.property.noStripExtras">noStripExtras</a></code> | <code>boolean</code> | Include extras in the output file. |
| <code><a href="#projen.python.PipOptions.property.noStripMarkers">noStripMarkers</a></code> | <code>boolean</code> | Include environment markers in the output file generated by `uv pip compile`. |
| <code><a href="#projen.python.PipOptions.property.onlyBinary">onlyBinary</a></code> | <code>string[]</code> | Only use pre-built wheels; don't build source distributions. |
| <code><a href="#projen.python.PipOptions.property.outputFile">outputFile</a></code> | <code>string</code> | Write the requirements generated by `uv pip compile` to the given `requirements.txt` file. |
| <code><a href="#projen.python.PipOptions.property.prefix">prefix</a></code> | <code>string</code> | Install packages into `lib`, `bin`, and other top-level folders under the specified directory, as if a virtual environment were present at that location. |
| <code><a href="#projen.python.PipOptions.property.prerelease">prerelease</a></code> | <code>string</code> | The strategy to use when considering pre-release versions. |
| <code><a href="#projen.python.PipOptions.property.python">python</a></code> | <code>string</code> | The Python interpreter into which packages should be installed. |
| <code><a href="#projen.python.PipOptions.property.pythonPlatform">pythonPlatform</a></code> | <code>string</code> | The platform for which requirements should be resolved. |
| <code><a href="#projen.python.PipOptions.property.pythonVersion">pythonVersion</a></code> | <code>string</code> | The minimum Python version that should be supported by the resolved requirements (e.g., `3.8` or `3.8.17`). |
| <code><a href="#projen.python.PipOptions.property.reinstall">reinstall</a></code> | <code>boolean</code> | Reinstall all packages, regardless of whether they're already installed. |
| <code><a href="#projen.python.PipOptions.property.reinstallPackage">reinstallPackage</a></code> | <code>string[]</code> | Reinstall a specific package, regardless of whether it's already installed. |
| <code><a href="#projen.python.PipOptions.property.requireHashes">requireHashes</a></code> | <code>boolean</code> | Require a matching hash for each requirement. |
| <code><a href="#projen.python.PipOptions.property.resolution">resolution</a></code> | <code>string</code> | The strategy to use when selecting between the different compatible versions for a given package requirement. |
| <code><a href="#projen.python.PipOptions.property.strict">strict</a></code> | <code>boolean</code> | Validate the Python environment, to detect packages with missing dependencies and other issues. |
| <code><a href="#projen.python.PipOptions.property.system">system</a></code> | <code>boolean</code> | Install packages into the system Python environment. |
| <code><a href="#projen.python.PipOptions.property.target">target</a></code> | <code>string</code> | Install packages into the specified directory, rather than into the virtual or system Python environment. |
| <code><a href="#projen.python.PipOptions.property.torchBackend">torchBackend</a></code> | <code>string</code> | The backend to use when fetching packages in the PyTorch ecosystem. |
| <code><a href="#projen.python.PipOptions.property.universal">universal</a></code> | <code>boolean</code> | Perform a universal resolution, attempting to generate a single `requirements.txt` output file that is compatible with all operating systems, architectures, and Python implementations. |
| <code><a href="#projen.python.PipOptions.property.upgrade">upgrade</a></code> | <code>boolean</code> | Allow package upgrades, ignoring pinned versions in any existing output file. |
| <code><a href="#projen.python.PipOptions.property.upgradePackage">upgradePackage</a></code> | <code>string[]</code> | Allow upgrades for a specific package, ignoring pinned versions in any existing output file. |
| <code><a href="#projen.python.PipOptions.property.verifyHashes">verifyHashes</a></code> | <code>boolean</code> | Validate any hashes provided in the requirements file. |

---

##### `allExtras`<sup>Optional</sup> <a name="allExtras" id="projen.python.PipOptions.property.allExtras"></a>

```typescript
public readonly allExtras: boolean;
```

- *Type:* boolean

Include all optional dependencies.

Only applies to `pyproject.toml`, `setup.py`, and `setup.cfg` sources.

---

##### `allowEmptyRequirements`<sup>Optional</sup> <a name="allowEmptyRequirements" id="projen.python.PipOptions.property.allowEmptyRequirements"></a>

```typescript
public readonly allowEmptyRequirements: boolean;
```

- *Type:* boolean

Allow `uv pip sync` with empty requirements, which will clear the environment of all packages.

---

##### `annotationStyle`<sup>Optional</sup> <a name="annotationStyle" id="projen.python.PipOptions.property.annotationStyle"></a>

```typescript
public readonly annotationStyle: string;
```

- *Type:* string

The style of the annotation comments included in the output file, used to indicate the source of each package.

---

##### `breakSystemPackages`<sup>Optional</sup> <a name="breakSystemPackages" id="projen.python.PipOptions.property.breakSystemPackages"></a>

```typescript
public readonly breakSystemPackages: boolean;
```

- *Type:* boolean

Allow uv to modify an `EXTERNALLY-MANAGED` Python installation.

WARNING: `--break-system-packages` is intended for use in continuous integration (CI)
environments, when installing into Python installations that are managed by an external
package manager, like `apt`. It should be used with caution, as such Python installations
explicitly recommend against modifications by other package managers (like uv or pip).

---

##### `compileBytecode`<sup>Optional</sup> <a name="compileBytecode" id="projen.python.PipOptions.property.compileBytecode"></a>

```typescript
public readonly compileBytecode: boolean;
```

- *Type:* boolean

Compile Python files to bytecode after installation.

By default, uv does not compile Python (`.py`) files to bytecode (`__pycache__/*.pyc`);
instead, compilation is performed lazily the first time a module is imported. For use-cases
in which start time is critical, such as CLI applications and Docker containers, this option
can be enabled to trade longer installation times for faster start times.

When enabled, uv will process the entire site-packages directory (including packages that
are not being modified by the current operation) for consistency. Like pip, it will also
ignore errors.

---

##### `configSettings`<sup>Optional</sup> <a name="configSettings" id="projen.python.PipOptions.property.configSettings"></a>

```typescript
public readonly configSettings: {[ key: string ]: any};
```

- *Type:* {[ key: string ]: any}

Settings to pass to the [PEP 517](https://peps.python.org/pep-0517/) build backend, specified as `KEY=VALUE` pairs.

---

##### `configSettingsPackage`<sup>Optional</sup> <a name="configSettingsPackage" id="projen.python.PipOptions.property.configSettingsPackage"></a>

```typescript
public readonly configSettingsPackage: {[ key: string ]: {[ key: string ]: any}};
```

- *Type:* {[ key: string ]: {[ key: string ]: any}}

Settings to pass to the [PEP 517](https://peps.python.org/pep-0517/) build backend for specific packages, specified as `KEY=VALUE` pairs.

---

##### `customCompileCommand`<sup>Optional</sup> <a name="customCompileCommand" id="projen.python.PipOptions.property.customCompileCommand"></a>

```typescript
public readonly customCompileCommand: string;
```

- *Type:* string

The header comment to include at the top of the output file generated by `uv pip compile`.

Used to reflect custom build scripts and commands that wrap `uv pip compile`.

---

##### `dependencyMetadata`<sup>Optional</sup> <a name="dependencyMetadata" id="projen.python.PipOptions.property.dependencyMetadata"></a>

```typescript
public readonly dependencyMetadata: StaticMetadata[];
```

- *Type:* <a href="#projen.python.StaticMetadata">StaticMetadata</a>[]

Pre-defined static metadata for dependencies of the project (direct or transitive).

When
provided, enables the resolver to use the specified metadata instead of querying the
registry or building the relevant package from source.

Metadata should be provided in adherence with the [Metadata 2.3](https://packaging.python.org/en/latest/specifications/core-metadata/)
standard, though only the following fields are respected:

- `name`: The name of the package.
- (Optional) `version`: The version of the package. If omitted, the metadata will be applied
to all versions of the package.
- (Optional) `requires-dist`: The dependencies of the package (e.g., `werkzeug>=0.14`).
- (Optional) `requires-python`: The Python version required by the package (e.g., `>=3.10`).
- (Optional) `provides-extra`: The extras provided by the package.

---

##### `emitBuildOptions`<sup>Optional</sup> <a name="emitBuildOptions" id="projen.python.PipOptions.property.emitBuildOptions"></a>

```typescript
public readonly emitBuildOptions: boolean;
```

- *Type:* boolean

Include `--no-binary` and `--only-binary` entries in the output file generated by `uv pip compile`.

---

##### `emitFindLinks`<sup>Optional</sup> <a name="emitFindLinks" id="projen.python.PipOptions.property.emitFindLinks"></a>

```typescript
public readonly emitFindLinks: boolean;
```

- *Type:* boolean

Include `--find-links` entries in the output file generated by `uv pip compile`.

---

##### `emitIndexAnnotation`<sup>Optional</sup> <a name="emitIndexAnnotation" id="projen.python.PipOptions.property.emitIndexAnnotation"></a>

```typescript
public readonly emitIndexAnnotation: boolean;
```

- *Type:* boolean

Include comment annotations indicating the index used to resolve each package (e.g., `# from https://pypi.org/simple`).

---

##### `emitIndexUrl`<sup>Optional</sup> <a name="emitIndexUrl" id="projen.python.PipOptions.property.emitIndexUrl"></a>

```typescript
public readonly emitIndexUrl: boolean;
```

- *Type:* boolean

Include `--index-url` and `--extra-index-url` entries in the output file generated by `uv pip compile`.

---

##### `emitMarkerExpression`<sup>Optional</sup> <a name="emitMarkerExpression" id="projen.python.PipOptions.property.emitMarkerExpression"></a>

```typescript
public readonly emitMarkerExpression: boolean;
```

- *Type:* boolean

Whether to emit a marker string indicating the conditions under which the set of pinned dependencies is valid.

The pinned dependencies may be valid even when the marker expression is
false, but when the expression is true, the requirements are known to
be correct.

---

##### `excludeNewer`<sup>Optional</sup> <a name="excludeNewer" id="projen.python.PipOptions.property.excludeNewer"></a>

```typescript
public readonly excludeNewer: string;
```

- *Type:* string

Limit candidate packages to those that were uploaded prior to a given point in time.

Accepts a superset of [RFC 3339](https://www.rfc-editor.org/rfc/rfc3339.html) (e.g.,
`2006-12-02T02:07:43Z`). A full timestamp is required to ensure that the resolver will
behave consistently across timezones.

---

##### `excludeNewerPackage`<sup>Optional</sup> <a name="excludeNewerPackage" id="projen.python.PipOptions.property.excludeNewerPackage"></a>

```typescript
public readonly excludeNewerPackage: {[ key: string ]: string};
```

- *Type:* {[ key: string ]: string}

Limit candidate packages for specific packages to those that were uploaded prior to the given date.

Accepts package-date pairs in a dictionary format.

---

##### `extra`<sup>Optional</sup> <a name="extra" id="projen.python.PipOptions.property.extra"></a>

```typescript
public readonly extra: string[];
```

- *Type:* string[]

Include optional dependencies from the specified extra; may be provided more than once.

Only applies to `pyproject.toml`, `setup.py`, and `setup.cfg` sources.

---

##### `extraBuildDependencies`<sup>Optional</sup> <a name="extraBuildDependencies" id="projen.python.PipOptions.property.extraBuildDependencies"></a>

```typescript
public readonly extraBuildDependencies: {[ key: string ]: any[]};
```

- *Type:* {[ key: string ]: any[]}

Additional build dependencies for packages.

This allows extending the PEP 517 build environment for the project's dependencies with
additional packages. This is useful for packages that assume the presence of packages like
`pip`, and do not declare them as build dependencies.

---

##### `extraBuildVariables`<sup>Optional</sup> <a name="extraBuildVariables" id="projen.python.PipOptions.property.extraBuildVariables"></a>

```typescript
public readonly extraBuildVariables: {[ key: string ]: {[ key: string ]: string}};
```

- *Type:* {[ key: string ]: {[ key: string ]: string}}

Extra environment variables to set when building certain packages.

Environment variables will be added to the environment when building the
specified packages.

---

##### `extraIndexUrl`<sup>Optional</sup> <a name="extraIndexUrl" id="projen.python.PipOptions.property.extraIndexUrl"></a>

```typescript
public readonly extraIndexUrl: string[];
```

- *Type:* string[]

Extra URLs of package indexes to use, in addition to `--index-url`.

Accepts either a repository compliant with [PEP 503](https://peps.python.org/pep-0503/)
(the simple repository API), or a local directory laid out in the same format.

All indexes provided via this flag take priority over the index specified by
[`index_url`](#index-url). When multiple indexes are provided, earlier values take priority.

To control uv's resolution strategy when multiple indexes are present, see
[`index_strategy`](#index-strategy).

---

##### `findLinks`<sup>Optional</sup> <a name="findLinks" id="projen.python.PipOptions.property.findLinks"></a>

```typescript
public readonly findLinks: string[];
```

- *Type:* string[]

Locations to search for candidate distributions, in addition to those found in the registry indexes.

If a path, the target must be a directory that contains packages as wheel files (`.whl`) or
source distributions (e.g., `.tar.gz` or `.zip`) at the top level.

If a URL, the page must contain a flat list of links to package files adhering to the
formats described above.

---

##### `forkStrategy`<sup>Optional</sup> <a name="forkStrategy" id="projen.python.PipOptions.property.forkStrategy"></a>

```typescript
public readonly forkStrategy: string;
```

- *Type:* string

The strategy to use when selecting multiple versions of a given package across Python versions and platforms.

By default, uv will optimize for selecting the latest version of each package for each
supported Python version (`requires-python`), while minimizing the number of selected
versions across platforms.

Under `fewest`, uv will minimize the number of selected versions for each package,
preferring older versions that are compatible with a wider range of supported Python
versions or platforms.

---

##### `generateHashes`<sup>Optional</sup> <a name="generateHashes" id="projen.python.PipOptions.property.generateHashes"></a>

```typescript
public readonly generateHashes: boolean;
```

- *Type:* boolean

Include distribution hashes in the output file.

---

##### `group`<sup>Optional</sup> <a name="group" id="projen.python.PipOptions.property.group"></a>

```typescript
public readonly group: PipGroupName[];
```

- *Type:* <a href="#projen.python.PipGroupName">PipGroupName</a>[]

Include the following dependency groups.

---

##### `indexStrategy`<sup>Optional</sup> <a name="indexStrategy" id="projen.python.PipOptions.property.indexStrategy"></a>

```typescript
public readonly indexStrategy: string;
```

- *Type:* string

The strategy to use when resolving against multiple index URLs.

By default, uv will stop at the first index on which a given package is available, and
limit resolutions to those present on that first index (`first-index`). This prevents
"dependency confusion" attacks, whereby an attacker can upload a malicious package under the
same name to an alternate index.

---

##### `indexUrl`<sup>Optional</sup> <a name="indexUrl" id="projen.python.PipOptions.property.indexUrl"></a>

```typescript
public readonly indexUrl: string;
```

- *Type:* string

The URL of the Python package index (by default: <https://pypi.org/simple>).

Accepts either a repository compliant with [PEP 503](https://peps.python.org/pep-0503/)
(the simple repository API), or a local directory laid out in the same format.

The index provided by this setting is given lower priority than any indexes specified via
[`extra_index_url`](#extra-index-url).

---

##### `keyringProvider`<sup>Optional</sup> <a name="keyringProvider" id="projen.python.PipOptions.property.keyringProvider"></a>

```typescript
public readonly keyringProvider: string;
```

- *Type:* string

Attempt to use `keyring` for authentication for index URLs.

At present, only `--keyring-provider subprocess` is supported, which configures uv to
use the `keyring` CLI to handle authentication.

---

##### `linkMode`<sup>Optional</sup> <a name="linkMode" id="projen.python.PipOptions.property.linkMode"></a>

```typescript
public readonly linkMode: string;
```

- *Type:* string
- *Default:* clone` (also known as Copy-on-Write) on macOS, and `hardlink` on Linux and

The method to use when installing packages from the global cache.

Defaults to `clone` (also known as Copy-on-Write) on macOS, and `hardlink` on Linux and
Windows.

WARNING: The use of symlink link mode is discouraged, as they create tight coupling between
the cache and the target environment. For example, clearing the cache (`uv cache clean`)
will break all installed packages by way of removing the underlying source files. Use
symlinks with caution.

---

##### `noAnnotate`<sup>Optional</sup> <a name="noAnnotate" id="projen.python.PipOptions.property.noAnnotate"></a>

```typescript
public readonly noAnnotate: boolean;
```

- *Type:* boolean

Exclude comment annotations indicating the source of each package from the output file generated by `uv pip compile`.

---

##### `noBinary`<sup>Optional</sup> <a name="noBinary" id="projen.python.PipOptions.property.noBinary"></a>

```typescript
public readonly noBinary: string[];
```

- *Type:* string[]

Don't install pre-built wheels.

The given packages will be built and installed from source. The resolver will still use
pre-built wheels to extract package metadata, if available.

Multiple packages may be provided. Disable binaries for all packages with `:all:`.
Clear previously specified packages with `:none:`.

---

##### `noBuild`<sup>Optional</sup> <a name="noBuild" id="projen.python.PipOptions.property.noBuild"></a>

```typescript
public readonly noBuild: boolean;
```

- *Type:* boolean

Don't build source distributions.

When enabled, resolving will not run arbitrary Python code. The cached wheels of
already-built source distributions will be reused, but operations that require building
distributions will exit with an error.

Alias for `--only-binary :all:`.

---

##### `noBuildIsolation`<sup>Optional</sup> <a name="noBuildIsolation" id="projen.python.PipOptions.property.noBuildIsolation"></a>

```typescript
public readonly noBuildIsolation: boolean;
```

- *Type:* boolean

Disable isolation when building source distributions.

Assumes that build dependencies specified by [PEP 518](https://peps.python.org/pep-0518/)
are already installed.

---

##### `noBuildIsolationPackage`<sup>Optional</sup> <a name="noBuildIsolationPackage" id="projen.python.PipOptions.property.noBuildIsolationPackage"></a>

```typescript
public readonly noBuildIsolationPackage: string[];
```

- *Type:* string[]

Disable isolation when building source distributions for a specific package.

Assumes that the packages' build dependencies specified by [PEP 518](https://peps.python.org/pep-0518/)
are already installed.

---

##### `noDeps`<sup>Optional</sup> <a name="noDeps" id="projen.python.PipOptions.property.noDeps"></a>

```typescript
public readonly noDeps: boolean;
```

- *Type:* boolean

Ignore package dependencies, instead only add those packages explicitly listed on the command line to the resulting requirements file.

---

##### `noEmitPackage`<sup>Optional</sup> <a name="noEmitPackage" id="projen.python.PipOptions.property.noEmitPackage"></a>

```typescript
public readonly noEmitPackage: string[];
```

- *Type:* string[]

Specify a package to omit from the output resolution.

Its dependencies will still be
included in the resolution. Equivalent to pip-compile's `--unsafe-package` option.

---

##### `noExtra`<sup>Optional</sup> <a name="noExtra" id="projen.python.PipOptions.property.noExtra"></a>

```typescript
public readonly noExtra: string[];
```

- *Type:* string[]

Exclude the specified optional dependencies if `all-extras` is supplied.

---

##### `noHeader`<sup>Optional</sup> <a name="noHeader" id="projen.python.PipOptions.property.noHeader"></a>

```typescript
public readonly noHeader: boolean;
```

- *Type:* boolean

Exclude the comment header at the top of output file generated by `uv pip compile`.

---

##### `noIndex`<sup>Optional</sup> <a name="noIndex" id="projen.python.PipOptions.property.noIndex"></a>

```typescript
public readonly noIndex: boolean;
```

- *Type:* boolean

Ignore all registry indexes (e.g., PyPI), instead relying on direct URL dependencies and those provided via `--find-links`.

---

##### `noSources`<sup>Optional</sup> <a name="noSources" id="projen.python.PipOptions.property.noSources"></a>

```typescript
public readonly noSources: boolean;
```

- *Type:* boolean

Ignore the `tool.uv.sources` table when resolving dependencies. Used to lock against the standards-compliant, publishable package metadata, as opposed to using any local or Git sources.

---

##### `noStripExtras`<sup>Optional</sup> <a name="noStripExtras" id="projen.python.PipOptions.property.noStripExtras"></a>

```typescript
public readonly noStripExtras: boolean;
```

- *Type:* boolean

Include extras in the output file.

By default, uv strips extras, as any packages pulled in by the extras are already included
as dependencies in the output file directly. Further, output files generated with
`--no-strip-extras` cannot be used as constraints files in `install` and `sync` invocations.

---

##### `noStripMarkers`<sup>Optional</sup> <a name="noStripMarkers" id="projen.python.PipOptions.property.noStripMarkers"></a>

```typescript
public readonly noStripMarkers: boolean;
```

- *Type:* boolean

Include environment markers in the output file generated by `uv pip compile`.

By default, uv strips environment markers, as the resolution generated by `compile` is
only guaranteed to be correct for the target environment.

---

##### `onlyBinary`<sup>Optional</sup> <a name="onlyBinary" id="projen.python.PipOptions.property.onlyBinary"></a>

```typescript
public readonly onlyBinary: string[];
```

- *Type:* string[]

Only use pre-built wheels; don't build source distributions.

When enabled, resolving will not run code from the given packages. The cached wheels of already-built
source distributions will be reused, but operations that require building distributions will
exit with an error.

Multiple packages may be provided. Disable binaries for all packages with `:all:`.
Clear previously specified packages with `:none:`.

---

##### `outputFile`<sup>Optional</sup> <a name="outputFile" id="projen.python.PipOptions.property.outputFile"></a>

```typescript
public readonly outputFile: string;
```

- *Type:* string

Write the requirements generated by `uv pip compile` to the given `requirements.txt` file.

If the file already exists, the existing versions will be preferred when resolving
dependencies, unless `--upgrade` is also specified.

---

##### `prefix`<sup>Optional</sup> <a name="prefix" id="projen.python.PipOptions.property.prefix"></a>

```typescript
public readonly prefix: string;
```

- *Type:* string

Install packages into `lib`, `bin`, and other top-level folders under the specified directory, as if a virtual environment were present at that location.

In general, prefer the use of `--python` to install into an alternate environment, as
scripts and other artifacts installed via `--prefix` will reference the installing
interpreter, rather than any interpreter added to the `--prefix` directory, rendering them
non-portable.

---

##### `prerelease`<sup>Optional</sup> <a name="prerelease" id="projen.python.PipOptions.property.prerelease"></a>

```typescript
public readonly prerelease: string;
```

- *Type:* string

The strategy to use when considering pre-release versions.

By default, uv will accept pre-releases for packages that _only_ publish pre-releases,
along with first-party requirements that contain an explicit pre-release marker in the
declared specifiers (`if-necessary-or-explicit`).

---

##### `python`<sup>Optional</sup> <a name="python" id="projen.python.PipOptions.property.python"></a>

```typescript
public readonly python: string;
```

- *Type:* string

The Python interpreter into which packages should be installed.

By default, uv installs into the virtual environment in the current working directory or
any parent directory. The `--python` option allows you to specify a different interpreter,
which is intended for use in continuous integration (CI) environments or other automated
workflows.

Supported formats:
- `3.10` looks for an installed Python 3.10 in the registry on Windows (see
`py --list-paths`), or `python3.10` on Linux and macOS.
- `python3.10` or `python.exe` looks for a binary with the given name in `PATH`.
- `/home/ferris/.local/bin/python3.10` uses the exact Python at the given path.

---

##### `pythonPlatform`<sup>Optional</sup> <a name="pythonPlatform" id="projen.python.PipOptions.property.pythonPlatform"></a>

```typescript
public readonly pythonPlatform: string;
```

- *Type:* string

The platform for which requirements should be resolved.

Represented as a "target triple", a string that describes the target platform in terms of
its CPU, vendor, and operating system name, like `x86_64-unknown-linux-gnu` or
`aarch64-apple-darwin`.

---

##### `pythonVersion`<sup>Optional</sup> <a name="pythonVersion" id="projen.python.PipOptions.property.pythonVersion"></a>

```typescript
public readonly pythonVersion: string;
```

- *Type:* string

The minimum Python version that should be supported by the resolved requirements (e.g., `3.8` or `3.8.17`).

If a patch version is omitted, the minimum patch version is assumed. For example, `3.8` is
mapped to `3.8.0`.

---

##### `reinstall`<sup>Optional</sup> <a name="reinstall" id="projen.python.PipOptions.property.reinstall"></a>

```typescript
public readonly reinstall: boolean;
```

- *Type:* boolean

Reinstall all packages, regardless of whether they're already installed.

Implies `refresh`.

---

##### `reinstallPackage`<sup>Optional</sup> <a name="reinstallPackage" id="projen.python.PipOptions.property.reinstallPackage"></a>

```typescript
public readonly reinstallPackage: string[];
```

- *Type:* string[]

Reinstall a specific package, regardless of whether it's already installed.

Implies
`refresh-package`.

---

##### `requireHashes`<sup>Optional</sup> <a name="requireHashes" id="projen.python.PipOptions.property.requireHashes"></a>

```typescript
public readonly requireHashes: boolean;
```

- *Type:* boolean

Require a matching hash for each requirement.

Hash-checking mode is all or nothing. If enabled, _all_ requirements must be provided
with a corresponding hash or set of hashes. Additionally, if enabled, _all_ requirements
must either be pinned to exact versions (e.g., `==1.0.0`), or be specified via direct URL.

Hash-checking mode introduces a number of additional constraints:

- Git dependencies are not supported.
- Editable installations are not supported.
- Local dependencies are not supported, unless they point to a specific wheel (`.whl`) or
source archive (`.zip`, `.tar.gz`), as opposed to a directory.

---

##### `resolution`<sup>Optional</sup> <a name="resolution" id="projen.python.PipOptions.property.resolution"></a>

```typescript
public readonly resolution: string;
```

- *Type:* string

The strategy to use when selecting between the different compatible versions for a given package requirement.

By default, uv will use the latest compatible version of each package (`highest`).

---

##### `strict`<sup>Optional</sup> <a name="strict" id="projen.python.PipOptions.property.strict"></a>

```typescript
public readonly strict: boolean;
```

- *Type:* boolean

Validate the Python environment, to detect packages with missing dependencies and other issues.

---

##### `system`<sup>Optional</sup> <a name="system" id="projen.python.PipOptions.property.system"></a>

```typescript
public readonly system: boolean;
```

- *Type:* boolean

Install packages into the system Python environment.

By default, uv installs into the virtual environment in the current working directory or
any parent directory. The `--system` option instructs uv to instead use the first Python
found in the system `PATH`.

WARNING: `--system` is intended for use in continuous integration (CI) environments and
should be used with caution, as it can modify the system Python installation.

---

##### `target`<sup>Optional</sup> <a name="target" id="projen.python.PipOptions.property.target"></a>

```typescript
public readonly target: string;
```

- *Type:* string

Install packages into the specified directory, rather than into the virtual or system Python environment.

The packages will be installed at the top-level of the directory.

---

##### `torchBackend`<sup>Optional</sup> <a name="torchBackend" id="projen.python.PipOptions.property.torchBackend"></a>

```typescript
public readonly torchBackend: string;
```

- *Type:* string

The backend to use when fetching packages in the PyTorch ecosystem.

When set, uv will ignore the configured index URLs for packages in the PyTorch ecosystem,
and will instead use the defined backend.

For example, when set to `cpu`, uv will use the CPU-only PyTorch index; when set to `cu126`,
uv will use the PyTorch index for CUDA 12.6.

The `auto` mode will attempt to detect the appropriate PyTorch index based on the currently
installed CUDA drivers.

This option is in preview and may change in any future release.

---

##### `universal`<sup>Optional</sup> <a name="universal" id="projen.python.PipOptions.property.universal"></a>

```typescript
public readonly universal: boolean;
```

- *Type:* boolean

Perform a universal resolution, attempting to generate a single `requirements.txt` output file that is compatible with all operating systems, architectures, and Python implementations.

In universal mode, the current Python version (or user-provided `--python-version`) will be
treated as a lower bound. For example, `--universal --python-version 3.7` would produce a
universal resolution for Python 3.7 and later.

---

##### `upgrade`<sup>Optional</sup> <a name="upgrade" id="projen.python.PipOptions.property.upgrade"></a>

```typescript
public readonly upgrade: boolean;
```

- *Type:* boolean

Allow package upgrades, ignoring pinned versions in any existing output file.

---

##### `upgradePackage`<sup>Optional</sup> <a name="upgradePackage" id="projen.python.PipOptions.property.upgradePackage"></a>

```typescript
public readonly upgradePackage: string[];
```

- *Type:* string[]

Allow upgrades for a specific package, ignoring pinned versions in any existing output file.

Accepts both standalone package names (`ruff`) and version specifiers (`ruff<0.5.0`).

---

##### `verifyHashes`<sup>Optional</sup> <a name="verifyHashes" id="projen.python.PipOptions.property.verifyHashes"></a>

```typescript
public readonly verifyHashes: boolean;
```

- *Type:* boolean

Validate any hashes provided in the requirements file.

Unlike `--require-hashes`, `--verify-hashes` does not require that all requirements have
hashes; instead, it will limit itself to verifying the hashes of those requirements that do
include them.

---

### PoetryConfiguration <a name="PoetryConfiguration" id="projen.python.PoetryConfiguration"></a>

#### Initializer <a name="Initializer" id="projen.python.PoetryConfiguration.Initializer"></a>

```typescript
import { python } from 'projen'

const poetryConfiguration: python.PoetryConfiguration = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PoetryConfiguration.property.authors">authors</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PoetryConfiguration.property.buildConfig">buildConfig</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PoetryConfiguration.property.classifiers">classifiers</a></code> | <code>string[]</code> | A list of trove classifiers. |
| <code><a href="#projen.python.PoetryConfiguration.property.dependencies">dependencies</a></code> | <code><a href="#projen.python.PoetryConfigurationDependencies">PoetryConfigurationDependencies</a></code> | This is a hash of package name (keys) and version constraints (values) that are required to run this package. |
| <code><a href="#projen.python.PoetryConfiguration.property.description">description</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.PoetryConfiguration.property.devDependencies">devDependencies</a></code> | <code>any</code> | This is a hash of package name (keys) and version constraints (values) that this package requires for developing it (testing tools and such). |
| <code><a href="#projen.python.PoetryConfiguration.property.documentation">documentation</a></code> | <code>string</code> | Documentation URL for the project. |
| <code><a href="#projen.python.PoetryConfiguration.property.exclude">exclude</a></code> | <code>string[]</code> | A list of files and folders to exclude. |
| <code><a href="#projen.python.PoetryConfiguration.property.extras">extras</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PoetryConfiguration.property.group">group</a></code> | <code>any</code> | This represents groups of dependencies. |
| <code><a href="#projen.python.PoetryConfiguration.property.homepage">homepage</a></code> | <code>string</code> | Homepage URL for the project. |
| <code><a href="#projen.python.PoetryConfiguration.property.include">include</a></code> | <code>string[]</code> | A list of files and folders to include. |
| <code><a href="#projen.python.PoetryConfiguration.property.keywords">keywords</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PoetryConfiguration.property.license">license</a></code> | <code>string</code> | License name. |
| <code><a href="#projen.python.PoetryConfiguration.property.maintainers">maintainers</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PoetryConfiguration.property.name">name</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.PoetryConfiguration.property.packageMode">packageMode</a></code> | <code>boolean</code> | Whether Poetry operates in package mode or not. |
| <code><a href="#projen.python.PoetryConfiguration.property.packages">packages</a></code> | <code><a href="#projen.python.PoetryConfigurationPackages">PoetryConfigurationPackages</a>[]</code> | A list of packages to include in the final distribution. |
| <code><a href="#projen.python.PoetryConfiguration.property.plugins">plugins</a></code> | <code>any</code> | A hash of hashes representing plugins. |
| <code><a href="#projen.python.PoetryConfiguration.property.readme">readme</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.PoetryConfiguration.property.repository">repository</a></code> | <code>string</code> | Repository URL for the project. |
| <code><a href="#projen.python.PoetryConfiguration.property.scripts">scripts</a></code> | <code>any</code> | A hash of scripts to be installed. |
| <code><a href="#projen.python.PoetryConfiguration.property.source">source</a></code> | <code>any[]</code> | *No description.* |
| <code><a href="#projen.python.PoetryConfiguration.property.urls">urls</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PoetryConfiguration.property.version">version</a></code> | <code>string</code> | *No description.* |

---

##### `authors`<sup>Optional</sup> <a name="authors" id="projen.python.PoetryConfiguration.property.authors"></a>

```typescript
public readonly authors: string[];
```

- *Type:* string[]

---

##### `buildConfig`<sup>Optional</sup> <a name="buildConfig" id="projen.python.PoetryConfiguration.property.buildConfig"></a>

```typescript
public readonly buildConfig: any;
```

- *Type:* any

---

##### `classifiers`<sup>Optional</sup> <a name="classifiers" id="projen.python.PoetryConfiguration.property.classifiers"></a>

```typescript
public readonly classifiers: string[];
```

- *Type:* string[]

A list of trove classifiers.

---

##### `dependencies`<sup>Optional</sup> <a name="dependencies" id="projen.python.PoetryConfiguration.property.dependencies"></a>

```typescript
public readonly dependencies: PoetryConfigurationDependencies;
```

- *Type:* <a href="#projen.python.PoetryConfigurationDependencies">PoetryConfigurationDependencies</a>

This is a hash of package name (keys) and version constraints (values) that are required to run this package.

---

##### `description`<sup>Optional</sup> <a name="description" id="projen.python.PoetryConfiguration.property.description"></a>

```typescript
public readonly description: string;
```

- *Type:* string

---

##### `devDependencies`<sup>Optional</sup> <a name="devDependencies" id="projen.python.PoetryConfiguration.property.devDependencies"></a>

```typescript
public readonly devDependencies: any;
```

- *Type:* any

This is a hash of package name (keys) and version constraints (values) that this package requires for developing it (testing tools and such).

---

##### `documentation`<sup>Optional</sup> <a name="documentation" id="projen.python.PoetryConfiguration.property.documentation"></a>

```typescript
public readonly documentation: string;
```

- *Type:* string

Documentation URL for the project.

---

##### `exclude`<sup>Optional</sup> <a name="exclude" id="projen.python.PoetryConfiguration.property.exclude"></a>

```typescript
public readonly exclude: string[];
```

- *Type:* string[]

A list of files and folders to exclude.

---

##### `extras`<sup>Optional</sup> <a name="extras" id="projen.python.PoetryConfiguration.property.extras"></a>

```typescript
public readonly extras: any;
```

- *Type:* any

---

##### `group`<sup>Optional</sup> <a name="group" id="projen.python.PoetryConfiguration.property.group"></a>

```typescript
public readonly group: any;
```

- *Type:* any

This represents groups of dependencies.

---

##### `homepage`<sup>Optional</sup> <a name="homepage" id="projen.python.PoetryConfiguration.property.homepage"></a>

```typescript
public readonly homepage: string;
```

- *Type:* string

Homepage URL for the project.

---

##### `include`<sup>Optional</sup> <a name="include" id="projen.python.PoetryConfiguration.property.include"></a>

```typescript
public readonly include: string[];
```

- *Type:* string[]

A list of files and folders to include.

---

##### `keywords`<sup>Optional</sup> <a name="keywords" id="projen.python.PoetryConfiguration.property.keywords"></a>

```typescript
public readonly keywords: string[];
```

- *Type:* string[]

---

##### `license`<sup>Optional</sup> <a name="license" id="projen.python.PoetryConfiguration.property.license"></a>

```typescript
public readonly license: string;
```

- *Type:* string

License name.

---

##### `maintainers`<sup>Optional</sup> <a name="maintainers" id="projen.python.PoetryConfiguration.property.maintainers"></a>

```typescript
public readonly maintainers: string[];
```

- *Type:* string[]

---

##### `name`<sup>Optional</sup> <a name="name" id="projen.python.PoetryConfiguration.property.name"></a>

```typescript
public readonly name: string;
```

- *Type:* string

---

##### `packageMode`<sup>Optional</sup> <a name="packageMode" id="projen.python.PoetryConfiguration.property.packageMode"></a>

```typescript
public readonly packageMode: boolean;
```

- *Type:* boolean

Whether Poetry operates in package mode or not.

---

##### `packages`<sup>Optional</sup> <a name="packages" id="projen.python.PoetryConfiguration.property.packages"></a>

```typescript
public readonly packages: PoetryConfigurationPackages[];
```

- *Type:* <a href="#projen.python.PoetryConfigurationPackages">PoetryConfigurationPackages</a>[]

A list of packages to include in the final distribution.

---

##### `plugins`<sup>Optional</sup> <a name="plugins" id="projen.python.PoetryConfiguration.property.plugins"></a>

```typescript
public readonly plugins: any;
```

- *Type:* any

A hash of hashes representing plugins.

---

##### `readme`<sup>Optional</sup> <a name="readme" id="projen.python.PoetryConfiguration.property.readme"></a>

```typescript
public readonly readme: string;
```

- *Type:* string

---

##### `repository`<sup>Optional</sup> <a name="repository" id="projen.python.PoetryConfiguration.property.repository"></a>

```typescript
public readonly repository: string;
```

- *Type:* string

Repository URL for the project.

---

##### `scripts`<sup>Optional</sup> <a name="scripts" id="projen.python.PoetryConfiguration.property.scripts"></a>

```typescript
public readonly scripts: any;
```

- *Type:* any

A hash of scripts to be installed.

---

##### `source`<sup>Optional</sup> <a name="source" id="projen.python.PoetryConfiguration.property.source"></a>

```typescript
public readonly source: any[];
```

- *Type:* any[]

---

##### `urls`<sup>Optional</sup> <a name="urls" id="projen.python.PoetryConfiguration.property.urls"></a>

```typescript
public readonly urls: any;
```

- *Type:* any

---

##### `version`<sup>Optional</sup> <a name="version" id="projen.python.PoetryConfiguration.property.version"></a>

```typescript
public readonly version: string;
```

- *Type:* string

---

### PoetryConfigurationDependencies <a name="PoetryConfigurationDependencies" id="projen.python.PoetryConfigurationDependencies"></a>

This is a hash of package name (keys) and version constraints (values) that are required to run this package.

#### Initializer <a name="Initializer" id="projen.python.PoetryConfigurationDependencies.Initializer"></a>

```typescript
import { python } from 'projen'

const poetryConfigurationDependencies: python.PoetryConfigurationDependencies = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PoetryConfigurationDependencies.property.python">python</a></code> | <code>string</code> | The Python versions the package is compatible with. |

---

##### `python`<sup>Optional</sup> <a name="python" id="projen.python.PoetryConfigurationDependencies.property.python"></a>

```typescript
public readonly python: string;
```

- *Type:* string

The Python versions the package is compatible with.

---

### PoetryConfigurationPackages <a name="PoetryConfigurationPackages" id="projen.python.PoetryConfigurationPackages"></a>

Information about where the package resides.

#### Initializer <a name="Initializer" id="projen.python.PoetryConfigurationPackages.Initializer"></a>

```typescript
import { python } from 'projen'

const poetryConfigurationPackages: python.PoetryConfigurationPackages = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PoetryConfigurationPackages.property.include">include</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.PoetryConfigurationPackages.property.format">format</a></code> | <code><a href="#projen.python.PoetryPackageFormat">PoetryPackageFormat</a>[]</code> | *No description.* |
| <code><a href="#projen.python.PoetryConfigurationPackages.property.from">from</a></code> | <code>string</code> | Where the source directory of the package resides. |
| <code><a href="#projen.python.PoetryConfigurationPackages.property.to">to</a></code> | <code>string</code> | Where the package should be installed in the final distribution. |

---

##### `include`<sup>Required</sup> <a name="include" id="projen.python.PoetryConfigurationPackages.property.include"></a>

```typescript
public readonly include: string;
```

- *Type:* string

---

##### `format`<sup>Optional</sup> <a name="format" id="projen.python.PoetryConfigurationPackages.property.format"></a>

```typescript
public readonly format: PoetryPackageFormat[];
```

- *Type:* <a href="#projen.python.PoetryPackageFormat">PoetryPackageFormat</a>[]

---

##### `from`<sup>Optional</sup> <a name="from" id="projen.python.PoetryConfigurationPackages.property.from"></a>

```typescript
public readonly from: string;
```

- *Type:* string

Where the source directory of the package resides.

---

##### `to`<sup>Optional</sup> <a name="to" id="projen.python.PoetryConfigurationPackages.property.to"></a>

```typescript
public readonly to: string;
```

- *Type:* string

Where the package should be installed in the final distribution.

---

### ProjectAuthor <a name="ProjectAuthor" id="projen.python.ProjectAuthor"></a>

#### Initializer <a name="Initializer" id="projen.python.ProjectAuthor.Initializer"></a>

```typescript
import { python } from 'projen'

const projectAuthor: python.ProjectAuthor = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.ProjectAuthor.property.email">email</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.ProjectAuthor.property.name">name</a></code> | <code>string</code> | *No description.* |

---

##### `email`<sup>Optional</sup> <a name="email" id="projen.python.ProjectAuthor.property.email"></a>

```typescript
public readonly email: string;
```

- *Type:* string

---

##### `name`<sup>Optional</sup> <a name="name" id="projen.python.ProjectAuthor.property.name"></a>

```typescript
public readonly name: string;
```

- *Type:* string

---

### ProjenrcOptions <a name="ProjenrcOptions" id="projen.python.ProjenrcOptions"></a>

Options for `Projenrc`.

#### Initializer <a name="Initializer" id="projen.python.ProjenrcOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const projenrcOptions: python.ProjenrcOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.ProjenrcOptions.property.filename">filename</a></code> | <code>string</code> | The name of the projenrc file. |
| <code><a href="#projen.python.ProjenrcOptions.property.projenVersion">projenVersion</a></code> | <code>string</code> | The projen version to use. |
| <code><a href="#projen.python.ProjenrcOptions.property.pythonExec">pythonExec</a></code> | <code>string</code> | Path to the python executable to use. |

---

##### `filename`<sup>Optional</sup> <a name="filename" id="projen.python.ProjenrcOptions.property.filename"></a>

```typescript
public readonly filename: string;
```

- *Type:* string
- *Default:* ".projenrc.py"

The name of the projenrc file.

---

##### `projenVersion`<sup>Optional</sup> <a name="projenVersion" id="projen.python.ProjenrcOptions.property.projenVersion"></a>

```typescript
public readonly projenVersion: string;
```

- *Type:* string
- *Default:* current version

The projen version to use.

---

##### `pythonExec`<sup>Optional</sup> <a name="pythonExec" id="projen.python.ProjenrcOptions.property.pythonExec"></a>

```typescript
public readonly pythonExec: string;
```

- *Type:* string
- *Default:* "python"

Path to the python executable to use.

---

### Publish <a name="Publish" id="projen.python.Publish"></a>

Publish configuration.

#### Initializer <a name="Initializer" id="projen.python.Publish.Initializer"></a>

```typescript
import { python } from 'projen'

const publish: python.Publish = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Publish.property.index">index</a></code> | <code><a href="#projen.python.PublishIndex">PublishIndex</a></code> | *No description.* |

---

##### `index`<sup>Optional</sup> <a name="index" id="projen.python.Publish.property.index"></a>

```typescript
public readonly index: PublishIndex;
```

- *Type:* <a href="#projen.python.PublishIndex">PublishIndex</a>

---

### PublishIndex <a name="PublishIndex" id="projen.python.PublishIndex"></a>

Publishing index configuration.

#### Initializer <a name="Initializer" id="projen.python.PublishIndex.Initializer"></a>

```typescript
import { python } from 'projen'

const publishIndex: python.PublishIndex = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PublishIndex.property.disable">disable</a></code> | <code>boolean</code> | Ask for confirmation when publishing to index. |
| <code><a href="#projen.python.PublishIndex.property.repos">repos</a></code> | <code><a href="#projen.python.PublishIndexRepos">PublishIndexRepos</a></code> | Define named repositories to publish to. |

---

##### `disable`<sup>Optional</sup> <a name="disable" id="projen.python.PublishIndex.property.disable"></a>

```typescript
public readonly disable: boolean;
```

- *Type:* boolean

Ask for confirmation when publishing to index.

---

##### `repos`<sup>Optional</sup> <a name="repos" id="projen.python.PublishIndex.property.repos"></a>

```typescript
public readonly repos: PublishIndexRepos;
```

- *Type:* <a href="#projen.python.PublishIndexRepos">PublishIndexRepos</a>

Define named repositories to publish to.

---

### PublishIndexRepos <a name="PublishIndexRepos" id="projen.python.PublishIndexRepos"></a>

Define named repositories to publish to.

#### Initializer <a name="Initializer" id="projen.python.PublishIndexRepos.Initializer"></a>

```typescript
import { python } from 'projen'

const publishIndexRepos: python.PublishIndexRepos = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PublishIndexRepos.property.url">url</a></code> | <code>string</code> | *No description.* |

---

##### `url`<sup>Optional</sup> <a name="url" id="projen.python.PublishIndexRepos.property.url"></a>

```typescript
public readonly url: string;
```

- *Type:* string

---

### PycodestyleOptions <a name="PycodestyleOptions" id="projen.python.PycodestyleOptions"></a>

Options for the `pycodestyle` plugin.

#### Initializer <a name="Initializer" id="projen.python.PycodestyleOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const pycodestyleOptions: python.PycodestyleOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PycodestyleOptions.property.ignoreOverlongTaskComments">ignoreOverlongTaskComments</a></code> | <code>boolean</code> | Whether line-length violations (`E501`) should be triggered for comments starting with [`task-tags`](#lint_task-tags) (by default: "TODO", "FIXME", and "XXX"). |
| <code><a href="#projen.python.PycodestyleOptions.property.maxDocLength">maxDocLength</a></code> | <code>number</code> | The maximum line length to allow for [`doc-line-too-long`](https://docs.astral.sh/ruff/rules/doc-line-too-long/) violations within documentation (`W505`), including standalone comments. By default, this is set to `null` which disables reporting violations. |
| <code><a href="#projen.python.PycodestyleOptions.property.maxLineLength">maxLineLength</a></code> | <code>number</code> | The maximum line length to allow for [`line-too-long`](https://docs.astral.sh/ruff/rules/line-too-long/) violations. By default, this is set to the value of the [`line-length`](#line-length) option. |

---

##### `ignoreOverlongTaskComments`<sup>Optional</sup> <a name="ignoreOverlongTaskComments" id="projen.python.PycodestyleOptions.property.ignoreOverlongTaskComments"></a>

```typescript
public readonly ignoreOverlongTaskComments: boolean;
```

- *Type:* boolean

Whether line-length violations (`E501`) should be triggered for comments starting with [`task-tags`](#lint_task-tags) (by default: "TODO", "FIXME", and "XXX").

---

##### `maxDocLength`<sup>Optional</sup> <a name="maxDocLength" id="projen.python.PycodestyleOptions.property.maxDocLength"></a>

```typescript
public readonly maxDocLength: number;
```

- *Type:* number

The maximum line length to allow for [`doc-line-too-long`](https://docs.astral.sh/ruff/rules/doc-line-too-long/) violations within documentation (`W505`), including standalone comments. By default, this is set to `null` which disables reporting violations.

The length is determined by the number of characters per line, except for lines containing Asian characters or emojis.
For these lines, the [unicode width](https://unicode.org/reports/tr11/) of each character is added up to determine the length.

See the [`doc-line-too-long`](https://docs.astral.sh/ruff/rules/doc-line-too-long/) rule for more information.

---

##### `maxLineLength`<sup>Optional</sup> <a name="maxLineLength" id="projen.python.PycodestyleOptions.property.maxLineLength"></a>

```typescript
public readonly maxLineLength: number;
```

- *Type:* number

The maximum line length to allow for [`line-too-long`](https://docs.astral.sh/ruff/rules/line-too-long/) violations. By default, this is set to the value of the [`line-length`](#line-length) option.

Use this option when you want to detect extra-long lines that the formatter can't automatically split by setting
`pycodestyle.line-length` to a value larger than [`line-length`](#line-length).

```toml
# The formatter wraps lines at a length of 88.
line-length = 88

[pycodestyle]
# E501 reports lines that exceed the length of 100.
max-line-length = 100
```

The length is determined by the number of characters per line, except for lines containing East Asian characters or emojis.
For these lines, the [unicode width](https://unicode.org/reports/tr11/) of each character is added up to determine the length.

See the [`line-too-long`](https://docs.astral.sh/ruff/rules/line-too-long/) rule for more information.

---

### PydoclintOptions <a name="PydoclintOptions" id="projen.python.PydoclintOptions"></a>

Options for the `pydoclint` plugin.

#### Initializer <a name="Initializer" id="projen.python.PydoclintOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const pydoclintOptions: python.PydoclintOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PydoclintOptions.property.ignoreOneLineDocstrings">ignoreOneLineDocstrings</a></code> | <code>boolean</code> | Skip docstrings which fit on a single line. |

---

##### `ignoreOneLineDocstrings`<sup>Optional</sup> <a name="ignoreOneLineDocstrings" id="projen.python.PydoclintOptions.property.ignoreOneLineDocstrings"></a>

```typescript
public readonly ignoreOneLineDocstrings: boolean;
```

- *Type:* boolean

Skip docstrings which fit on a single line.

Note: The corresponding setting in `pydoclint`
is named `skip-checking-short-docstrings`.

---

### PydocstyleOptions <a name="PydocstyleOptions" id="projen.python.PydocstyleOptions"></a>

Options for the `pydocstyle` plugin.

#### Initializer <a name="Initializer" id="projen.python.PydocstyleOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const pydocstyleOptions: python.PydocstyleOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PydocstyleOptions.property.convention">convention</a></code> | <code>string</code> | Whether to use Google-style, NumPy-style conventions, or the [PEP 257](https://peps.python.org/pep-0257/) defaults when analyzing docstring sections. |
| <code><a href="#projen.python.PydocstyleOptions.property.ignoreDecorators">ignoreDecorators</a></code> | <code>string[]</code> | Ignore docstrings for functions or methods decorated with the specified fully-qualified decorators. |
| <code><a href="#projen.python.PydocstyleOptions.property.ignoreVarParameters">ignoreVarParameters</a></code> | <code>boolean</code> | If set to `true`, ignore missing documentation for `*args` and `**kwargs` parameters. |
| <code><a href="#projen.python.PydocstyleOptions.property.propertyDecorators">propertyDecorators</a></code> | <code>string[]</code> | A list of decorators that, when applied to a method, indicate that the method should be treated as a property (in addition to the builtin `@property` and standard-library `@functools.cached_property`). |

---

##### `convention`<sup>Optional</sup> <a name="convention" id="projen.python.PydocstyleOptions.property.convention"></a>

```typescript
public readonly convention: string;
```

- *Type:* string

Whether to use Google-style, NumPy-style conventions, or the [PEP 257](https://peps.python.org/pep-0257/) defaults when analyzing docstring sections.

Enabling a convention will disable all rules that are not included in
the specified convention. As such, the intended workflow is to enable a
convention and then selectively enable or disable any additional rules
on top of it.

For example, to use Google-style conventions but avoid requiring
documentation for every function parameter:

```toml
[tool.ruff.lint]
# Enable all `pydocstyle` rules, limiting to those that adhere to the
# Google convention via `convention = "google"`, below.
select = ["D"]

# On top of the Google convention, disable `D417`, which requires
# documentation for every function parameter.
ignore = ["D417"]

[tool.ruff.lint.pydocstyle]
convention = "google"
```

To enable an additional rule that's excluded from the convention,
select the desired rule via its fully qualified rule code (e.g.,
`D400` instead of `D4` or `D40`):

```toml
[tool.ruff.lint]
# Enable D400 on top of the Google convention.
extend-select = ["D400"]

[tool.ruff.lint.pydocstyle]
convention = "google"
```

---

##### `ignoreDecorators`<sup>Optional</sup> <a name="ignoreDecorators" id="projen.python.PydocstyleOptions.property.ignoreDecorators"></a>

```typescript
public readonly ignoreDecorators: string[];
```

- *Type:* string[]

Ignore docstrings for functions or methods decorated with the specified fully-qualified decorators.

---

##### `ignoreVarParameters`<sup>Optional</sup> <a name="ignoreVarParameters" id="projen.python.PydocstyleOptions.property.ignoreVarParameters"></a>

```typescript
public readonly ignoreVarParameters: boolean;
```

- *Type:* boolean

If set to `true`, ignore missing documentation for `*args` and `**kwargs` parameters.

---

##### `propertyDecorators`<sup>Optional</sup> <a name="propertyDecorators" id="projen.python.PydocstyleOptions.property.propertyDecorators"></a>

```typescript
public readonly propertyDecorators: string[];
```

- *Type:* string[]

A list of decorators that, when applied to a method, indicate that the method should be treated as a property (in addition to the builtin `@property` and standard-library `@functools.cached_property`).

For example, Ruff will expect that any method decorated by a decorator
in this list can use a non-imperative summary line.

---

### PyflakesOptions <a name="PyflakesOptions" id="projen.python.PyflakesOptions"></a>

Options for the `pyflakes` plugin.

#### Initializer <a name="Initializer" id="projen.python.PyflakesOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const pyflakesOptions: python.PyflakesOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PyflakesOptions.property.allowedUnusedImports">allowedUnusedImports</a></code> | <code>string[]</code> | A list of modules to ignore when considering unused imports. |
| <code><a href="#projen.python.PyflakesOptions.property.extendGenerics">extendGenerics</a></code> | <code>string[]</code> | Additional functions or classes to consider generic, such that any subscripts should be treated as type annotation (e.g., `ForeignKey` in `django.db.models.ForeignKey["User"]`. |

---

##### `allowedUnusedImports`<sup>Optional</sup> <a name="allowedUnusedImports" id="projen.python.PyflakesOptions.property.allowedUnusedImports"></a>

```typescript
public readonly allowedUnusedImports: string[];
```

- *Type:* string[]

A list of modules to ignore when considering unused imports.

Used to prevent violations for specific modules that are known to have side effects on
import (e.g., `hvplot.pandas`).

Modules in this list are expected to be fully-qualified names (e.g., `hvplot.pandas`). Any
submodule of a given module will also be ignored (e.g., given `hvplot`, `hvplot.pandas`
will also be ignored).

---

##### `extendGenerics`<sup>Optional</sup> <a name="extendGenerics" id="projen.python.PyflakesOptions.property.extendGenerics"></a>

```typescript
public readonly extendGenerics: string[];
```

- *Type:* string[]

Additional functions or classes to consider generic, such that any subscripts should be treated as type annotation (e.g., `ForeignKey` in `django.db.models.ForeignKey["User"]`.

Expects to receive a list of fully-qualified names (e.g., `django.db.models.ForeignKey`,
rather than `ForeignKey`).

---

### PylintOptions <a name="PylintOptions" id="projen.python.PylintOptions"></a>

Options for the `pylint` plugin.

#### Initializer <a name="Initializer" id="projen.python.PylintOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const pylintOptions: python.PylintOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PylintOptions.property.allowDunderMethodNames">allowDunderMethodNames</a></code> | <code>string[]</code> | Dunder methods name to allow, in addition to the default set from the Python standard library (see `PLW3201`). |
| <code><a href="#projen.python.PylintOptions.property.allowMagicValueTypes">allowMagicValueTypes</a></code> | <code><a href="#projen.python.ConstantType">ConstantType</a>[]</code> | Constant types to ignore when used as "magic values" (see `PLR2004`). |
| <code><a href="#projen.python.PylintOptions.property.maxArgs">maxArgs</a></code> | <code>number</code> | Maximum number of arguments allowed for a function or method definition (see `PLR0913`). |
| <code><a href="#projen.python.PylintOptions.property.maxBoolExpr">maxBoolExpr</a></code> | <code>number</code> | Maximum number of Boolean expressions allowed within a single `if` statement (see `PLR0916`). |
| <code><a href="#projen.python.PylintOptions.property.maxBranches">maxBranches</a></code> | <code>number</code> | Maximum number of branches allowed for a function or method body (see `PLR0912`). |
| <code><a href="#projen.python.PylintOptions.property.maxLocals">maxLocals</a></code> | <code>number</code> | Maximum number of local variables allowed for a function or method body (see `PLR0914`). |
| <code><a href="#projen.python.PylintOptions.property.maxNestedBlocks">maxNestedBlocks</a></code> | <code>number</code> | Maximum number of nested blocks allowed within a function or method body (see `PLR1702`). |
| <code><a href="#projen.python.PylintOptions.property.maxPositionalArgs">maxPositionalArgs</a></code> | <code>number</code> | Maximum number of positional arguments allowed for a function or method definition (see `PLR0917`). |
| <code><a href="#projen.python.PylintOptions.property.maxPublicMethods">maxPublicMethods</a></code> | <code>number</code> | Maximum number of public methods allowed for a class (see `PLR0904`). |
| <code><a href="#projen.python.PylintOptions.property.maxReturns">maxReturns</a></code> | <code>number</code> | Maximum number of return statements allowed for a function or method body (see `PLR0911`). |
| <code><a href="#projen.python.PylintOptions.property.maxStatements">maxStatements</a></code> | <code>number</code> | Maximum number of statements allowed for a function or method body (see `PLR0915`). |

---

##### `allowDunderMethodNames`<sup>Optional</sup> <a name="allowDunderMethodNames" id="projen.python.PylintOptions.property.allowDunderMethodNames"></a>

```typescript
public readonly allowDunderMethodNames: string[];
```

- *Type:* string[]

Dunder methods name to allow, in addition to the default set from the Python standard library (see `PLW3201`).

---

##### `allowMagicValueTypes`<sup>Optional</sup> <a name="allowMagicValueTypes" id="projen.python.PylintOptions.property.allowMagicValueTypes"></a>

```typescript
public readonly allowMagicValueTypes: ConstantType[];
```

- *Type:* <a href="#projen.python.ConstantType">ConstantType</a>[]

Constant types to ignore when used as "magic values" (see `PLR2004`).

---

##### `maxArgs`<sup>Optional</sup> <a name="maxArgs" id="projen.python.PylintOptions.property.maxArgs"></a>

```typescript
public readonly maxArgs: number;
```

- *Type:* number

Maximum number of arguments allowed for a function or method definition (see `PLR0913`).

---

##### `maxBoolExpr`<sup>Optional</sup> <a name="maxBoolExpr" id="projen.python.PylintOptions.property.maxBoolExpr"></a>

```typescript
public readonly maxBoolExpr: number;
```

- *Type:* number

Maximum number of Boolean expressions allowed within a single `if` statement (see `PLR0916`).

---

##### `maxBranches`<sup>Optional</sup> <a name="maxBranches" id="projen.python.PylintOptions.property.maxBranches"></a>

```typescript
public readonly maxBranches: number;
```

- *Type:* number

Maximum number of branches allowed for a function or method body (see `PLR0912`).

---

##### `maxLocals`<sup>Optional</sup> <a name="maxLocals" id="projen.python.PylintOptions.property.maxLocals"></a>

```typescript
public readonly maxLocals: number;
```

- *Type:* number

Maximum number of local variables allowed for a function or method body (see `PLR0914`).

---

##### `maxNestedBlocks`<sup>Optional</sup> <a name="maxNestedBlocks" id="projen.python.PylintOptions.property.maxNestedBlocks"></a>

```typescript
public readonly maxNestedBlocks: number;
```

- *Type:* number

Maximum number of nested blocks allowed within a function or method body (see `PLR1702`).

---

##### `maxPositionalArgs`<sup>Optional</sup> <a name="maxPositionalArgs" id="projen.python.PylintOptions.property.maxPositionalArgs"></a>

```typescript
public readonly maxPositionalArgs: number;
```

- *Type:* number

Maximum number of positional arguments allowed for a function or method definition (see `PLR0917`).

If not specified, defaults to the value of `max-args`.

---

##### `maxPublicMethods`<sup>Optional</sup> <a name="maxPublicMethods" id="projen.python.PylintOptions.property.maxPublicMethods"></a>

```typescript
public readonly maxPublicMethods: number;
```

- *Type:* number

Maximum number of public methods allowed for a class (see `PLR0904`).

---

##### `maxReturns`<sup>Optional</sup> <a name="maxReturns" id="projen.python.PylintOptions.property.maxReturns"></a>

```typescript
public readonly maxReturns: number;
```

- *Type:* number

Maximum number of return statements allowed for a function or method body (see `PLR0911`).

---

##### `maxStatements`<sup>Optional</sup> <a name="maxStatements" id="projen.python.PylintOptions.property.maxStatements"></a>

```typescript
public readonly maxStatements: number;
```

- *Type:* number

Maximum number of statements allowed for a function or method body (see `PLR0915`).

---

### PyprojectToml <a name="PyprojectToml" id="projen.python.PyprojectToml"></a>

#### Initializer <a name="Initializer" id="projen.python.PyprojectToml.Initializer"></a>

```typescript
import { python } from 'projen'

const pyprojectToml: python.PyprojectToml = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PyprojectToml.property.buildSystem">buildSystem</a></code> | <code><a href="#projen.python.BuildSystem">BuildSystem</a></code> | *No description.* |
| <code><a href="#projen.python.PyprojectToml.property.dependencyGroups">dependencyGroups</a></code> | <code><a href="#projen.python.PyprojectTomlDependencyGroups">PyprojectTomlDependencyGroups</a></code> | Named groups of dependencies, similar to `requirements.txt` files, which launchers, IDEs, and other tools can find and identify by name. Each item in `[dependency-groups]` is defined as mapping of group name to list of [dependency specifiers](https://packaging.python.org/en/latest/specifications/dependency-specifiers/). |
| <code><a href="#projen.python.PyprojectToml.property.project">project</a></code> | <code><a href="#projen.python.PyprojectTomlProject">PyprojectTomlProject</a></code> | There are two kinds of metadata: _static_ and _dynamic_. |
| <code><a href="#projen.python.PyprojectToml.property.tool">tool</a></code> | <code><a href="#projen.python.PyprojectTomlTool">PyprojectTomlTool</a></code> | Every tool that is used by the project can have users specify configuration data as long as they use a sub-table within `[tool]`. |

---

##### `buildSystem`<sup>Optional</sup> <a name="buildSystem" id="projen.python.PyprojectToml.property.buildSystem"></a>

```typescript
public readonly buildSystem: BuildSystem;
```

- *Type:* <a href="#projen.python.BuildSystem">BuildSystem</a>

---

##### `dependencyGroups`<sup>Optional</sup> <a name="dependencyGroups" id="projen.python.PyprojectToml.property.dependencyGroups"></a>

```typescript
public readonly dependencyGroups: PyprojectTomlDependencyGroups;
```

- *Type:* <a href="#projen.python.PyprojectTomlDependencyGroups">PyprojectTomlDependencyGroups</a>

Named groups of dependencies, similar to `requirements.txt` files, which launchers, IDEs, and other tools can find and identify by name. Each item in `[dependency-groups]` is defined as mapping of group name to list of [dependency specifiers](https://packaging.python.org/en/latest/specifications/dependency-specifiers/).

---

##### `project`<sup>Optional</sup> <a name="project" id="projen.python.PyprojectToml.property.project"></a>

```typescript
public readonly project: PyprojectTomlProject;
```

- *Type:* <a href="#projen.python.PyprojectTomlProject">PyprojectTomlProject</a>

There are two kinds of metadata: _static_ and _dynamic_.

Static metadata is listed in the `[project]` table directly and cannot be specified or changed by a tool.
- Dynamic metadata key names are listed inside the `dynamic` key and represents metadata that a tool will later provide.

---

##### `tool`<sup>Optional</sup> <a name="tool" id="projen.python.PyprojectToml.property.tool"></a>

```typescript
public readonly tool: PyprojectTomlTool;
```

- *Type:* <a href="#projen.python.PyprojectTomlTool">PyprojectTomlTool</a>

Every tool that is used by the project can have users specify configuration data as long as they use a sub-table within `[tool]`.

Generally a project can use the subtable `tool.$NAME` if, and only if, they own the entry for `$NAME` in the Cheeseshop/PyPI.

---

### PyprojectTomlDependencyGroups <a name="PyprojectTomlDependencyGroups" id="projen.python.PyprojectTomlDependencyGroups"></a>

Named groups of dependencies, similar to `requirements.txt` files, which launchers, IDEs, and other tools can find and identify by name. Each item in `[dependency-groups]` is defined as mapping of group name to list of [dependency specifiers](https://packaging.python.org/en/latest/specifications/dependency-specifiers/).

#### Initializer <a name="Initializer" id="projen.python.PyprojectTomlDependencyGroups.Initializer"></a>

```typescript
import { python } from 'projen'

const pyprojectTomlDependencyGroups: python.PyprojectTomlDependencyGroups = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PyprojectTomlDependencyGroups.property.dev">dev</a></code> | <code>any[]</code> | *No description.* |

---

##### `dev`<sup>Optional</sup> <a name="dev" id="projen.python.PyprojectTomlDependencyGroups.property.dev"></a>

```typescript
public readonly dev: any[];
```

- *Type:* any[]

---

### PyprojectTomlProject <a name="PyprojectTomlProject" id="projen.python.PyprojectTomlProject"></a>

There are two kinds of metadata: _static_ and _dynamic_.

Static metadata is listed in the `[project]` table directly and cannot be specified or changed by a tool.
- Dynamic metadata key names are listed inside the `dynamic` key and represents metadata that a tool will later provide.

#### Initializer <a name="Initializer" id="projen.python.PyprojectTomlProject.Initializer"></a>

```typescript
import { python } from 'projen'

const pyprojectTomlProject: python.PyprojectTomlProject = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PyprojectTomlProject.property.name">name</a></code> | <code>string</code> | Valid name consists only of ASCII letters and numbers, period, underscore and hyphen. |
| <code><a href="#projen.python.PyprojectTomlProject.property.authors">authors</a></code> | <code><a href="#projen.python.ProjectAuthor">ProjectAuthor</a>[]</code> | People or organizations considered as 'authors' of the project. |
| <code><a href="#projen.python.PyprojectTomlProject.property.classifiers">classifiers</a></code> | <code>string[]</code> | List of [Trove classifiers](https://pypi.org/classifiers/) that describe the project. PyPI use the classifiers to categorize projects. |
| <code><a href="#projen.python.PyprojectTomlProject.property.dependencies">dependencies</a></code> | <code>string[]</code> | An array of [dependency specifier](https://packaging.python.org/en/latest/specifications/dependency-specifiers/) strings, each representing a mandatory dependent package of the project. |
| <code><a href="#projen.python.PyprojectTomlProject.property.description">description</a></code> | <code>string</code> | Summary description of the project in one line. |
| <code><a href="#projen.python.PyprojectTomlProject.property.dynamic">dynamic</a></code> | <code><a href="#projen.python.PyprojectTomlProjectDynamic">PyprojectTomlProjectDynamic</a>[]</code> | Specifies which keys are intentionally unspecified under `[project]` table so build backend can/will provide such metadata dynamically. |
| <code><a href="#projen.python.PyprojectTomlProject.property.entryPoints">entryPoints</a></code> | <code>any</code> | Extra [entry point groups](https://packaging.python.org/en/latest/specifications/entry-points/) that allow applications to load plugins. For example, Pygments (a syntax highlighting tool) can use additional styles from separately installed packages through `[project.entry-points."pygments.styles"]`. Each key is the name of the entry-point group, and each value is a table of entry points. |
| <code><a href="#projen.python.PyprojectTomlProject.property.guiScripts">guiScripts</a></code> | <code>{[ key: string ]: string}</code> | Table of [entry points](https://packaging.python.org/en/latest/specifications/entry-points/) that allows package installers to create a GUI wrapper for. Each key is the name of the script to be created, and each value is the function or object to all, in form of either `importable.module` or `importable.module:object.attr`. Windows platform treats `gui_scripts` specially in that they are wrapped in a GUI executable, so they can be started without a console, but cannot use standard streams unless application code redirects them. |
| <code><a href="#projen.python.PyprojectTomlProject.property.importNames">importNames</a></code> | <code>string[]</code> | An array of strings specifying the import names that the project exclusively provides when installed. |
| <code><a href="#projen.python.PyprojectTomlProject.property.importNamespaces">importNamespaces</a></code> | <code>string[]</code> | An array of strings specifying the import names that the project provides when installed, but not exclusively. |
| <code><a href="#projen.python.PyprojectTomlProject.property.keywords">keywords</a></code> | <code>string[]</code> | List of keywords or tags that describe the project. |
| <code><a href="#projen.python.PyprojectTomlProject.property.license">license</a></code> | <code>any</code> | For now it is a table with either: - `file` key specifying a relative path to a license file, or - `text` key containing full license content. |
| <code><a href="#projen.python.PyprojectTomlProject.property.licenseFiles">licenseFiles</a></code> | <code>string[]</code> | Relative paths or globs to paths of license files. |
| <code><a href="#projen.python.PyprojectTomlProject.property.maintainers">maintainers</a></code> | <code><a href="#projen.python.ProjectAuthor">ProjectAuthor</a>[]</code> | People or organizations considered as 'maintainers' of the project. |
| <code><a href="#projen.python.PyprojectTomlProject.property.optionalDependencies">optionalDependencies</a></code> | <code>any</code> | Each entry is a key/value pair, with the key specifying [extra feature name](https://packaging.python.org/en/latest/specifications/core-metadata/#provides-extra-multiple-use) (such as `socks` in `requests[socks]`), and value is an array of [dependency specifier](https://packaging.python.org/en/latest/specifications/dependency-specifiers/) strings. |
| <code><a href="#projen.python.PyprojectTomlProject.property.readme">readme</a></code> | <code>any</code> | Value can be a relative path to text / markdown (`.md` suffix) / reStructuredText (`.rst` suffix) readme file, or a table with either: - `file` key containing path of aforementioned readme file, or - `text` key containing the full readme text embedded inside `pyproject.toml`. |
| <code><a href="#projen.python.PyprojectTomlProject.property.requiresPython">requiresPython</a></code> | <code>string</code> | Specifies the Python version(s) that the distribution is compatible with. |
| <code><a href="#projen.python.PyprojectTomlProject.property.scripts">scripts</a></code> | <code>{[ key: string ]: string}</code> | Table of [entry points](https://packaging.python.org/en/latest/specifications/entry-points/) that allows package installers to create a command-line wrapper for. Each key is the name of the script to be created, and each value is the function or object to all, in form of either `importable.module` or `importable.module:object.attr`. Windows platform treats `console_scripts` specially in that they are wrapped in a console executable, so they are attached to a console and can use `sys.stdin`, `sys.stdout` and `sys.stderr` for I/O. |
| <code><a href="#projen.python.PyprojectTomlProject.property.urls">urls</a></code> | <code>{[ key: string ]: string}</code> | Table consisting one or multiple `label: URL` pairs. |
| <code><a href="#projen.python.PyprojectTomlProject.property.version">version</a></code> | <code>string</code> | Version of the project, as defined in the [Version specifier specification](https://packaging.python.org/en/latest/specifications/version-specifiers/), and preferably [already normalized](https://packaging.python.org/en/latest/specifications/version-specifiers/#normalization). |

---

##### `name`<sup>Required</sup> <a name="name" id="projen.python.PyprojectTomlProject.property.name"></a>

```typescript
public readonly name: string;
```

- *Type:* string

Valid name consists only of ASCII letters and numbers, period, underscore and hyphen.

It must start and end with a letter or number.

---

##### `authors`<sup>Optional</sup> <a name="authors" id="projen.python.PyprojectTomlProject.property.authors"></a>

```typescript
public readonly authors: ProjectAuthor[];
```

- *Type:* <a href="#projen.python.ProjectAuthor">ProjectAuthor</a>[]

People or organizations considered as 'authors' of the project.

Each author is a table with `name` key, `email` key, or both.

---

##### `classifiers`<sup>Optional</sup> <a name="classifiers" id="projen.python.PyprojectTomlProject.property.classifiers"></a>

```typescript
public readonly classifiers: string[];
```

- *Type:* string[]

List of [Trove classifiers](https://pypi.org/classifiers/) that describe the project. PyPI use the classifiers to categorize projects.

---

##### `dependencies`<sup>Optional</sup> <a name="dependencies" id="projen.python.PyprojectTomlProject.property.dependencies"></a>

```typescript
public readonly dependencies: string[];
```

- *Type:* string[]

An array of [dependency specifier](https://packaging.python.org/en/latest/specifications/dependency-specifiers/) strings, each representing a mandatory dependent package of the project.

---

##### `description`<sup>Optional</sup> <a name="description" id="projen.python.PyprojectTomlProject.property.description"></a>

```typescript
public readonly description: string;
```

- *Type:* string

Summary description of the project in one line.

Tools may not accept multiple lines.

---

##### `dynamic`<sup>Optional</sup> <a name="dynamic" id="projen.python.PyprojectTomlProject.property.dynamic"></a>

```typescript
public readonly dynamic: PyprojectTomlProjectDynamic[];
```

- *Type:* <a href="#projen.python.PyprojectTomlProjectDynamic">PyprojectTomlProjectDynamic</a>[]

Specifies which keys are intentionally unspecified under `[project]` table so build backend can/will provide such metadata dynamically.

Each key must be listed only once. It is an error to both list a key in `dynamic` and use the key directly in `[project]`.
One of the most common usage is `version`, which allows build backend to retrieve project version from source code or version control system instead of hardcoding it in `pyproject.toml`.

---

##### `entryPoints`<sup>Optional</sup> <a name="entryPoints" id="projen.python.PyprojectTomlProject.property.entryPoints"></a>

```typescript
public readonly entryPoints: any;
```

- *Type:* any

Extra [entry point groups](https://packaging.python.org/en/latest/specifications/entry-points/) that allow applications to load plugins. For example, Pygments (a syntax highlighting tool) can use additional styles from separately installed packages through `[project.entry-points."pygments.styles"]`. Each key is the name of the entry-point group, and each value is a table of entry points.

---

##### `guiScripts`<sup>Optional</sup> <a name="guiScripts" id="projen.python.PyprojectTomlProject.property.guiScripts"></a>

```typescript
public readonly guiScripts: {[ key: string ]: string};
```

- *Type:* {[ key: string ]: string}

Table of [entry points](https://packaging.python.org/en/latest/specifications/entry-points/) that allows package installers to create a GUI wrapper for. Each key is the name of the script to be created, and each value is the function or object to all, in form of either `importable.module` or `importable.module:object.attr`. Windows platform treats `gui_scripts` specially in that they are wrapped in a GUI executable, so they can be started without a console, but cannot use standard streams unless application code redirects them.

---

##### `importNames`<sup>Optional</sup> <a name="importNames" id="projen.python.PyprojectTomlProject.property.importNames"></a>

```typescript
public readonly importNames: string[];
```

- *Type:* string[]

An array of strings specifying the import names that the project exclusively provides when installed.

---

##### `importNamespaces`<sup>Optional</sup> <a name="importNamespaces" id="projen.python.PyprojectTomlProject.property.importNamespaces"></a>

```typescript
public readonly importNamespaces: string[];
```

- *Type:* string[]

An array of strings specifying the import names that the project provides when installed, but not exclusively.

---

##### `keywords`<sup>Optional</sup> <a name="keywords" id="projen.python.PyprojectTomlProject.property.keywords"></a>

```typescript
public readonly keywords: string[];
```

- *Type:* string[]

List of keywords or tags that describe the project.

They could be used by search engines to categorize the project.

---

##### `license`<sup>Optional</sup> <a name="license" id="projen.python.PyprojectTomlProject.property.license"></a>

```typescript
public readonly license: any;
```

- *Type:* any

For now it is a table with either: - `file` key specifying a relative path to a license file, or - `text` key containing full license content.

Newer tool may accept a single [SPDX license expression](https://spdx.github.io/spdx-spec/v2.2.2/SPDX-license-expressions/) string instead of a table.

---

##### `licenseFiles`<sup>Optional</sup> <a name="licenseFiles" id="projen.python.PyprojectTomlProject.property.licenseFiles"></a>

```typescript
public readonly licenseFiles: string[];
```

- *Type:* string[]

Relative paths or globs to paths of license files.

Can be an empty list.

---

##### `maintainers`<sup>Optional</sup> <a name="maintainers" id="projen.python.PyprojectTomlProject.property.maintainers"></a>

```typescript
public readonly maintainers: ProjectAuthor[];
```

- *Type:* <a href="#projen.python.ProjectAuthor">ProjectAuthor</a>[]

People or organizations considered as 'maintainers' of the project.

Each maintainer is a table with `name` key, `email` key, or both.

---

##### `optionalDependencies`<sup>Optional</sup> <a name="optionalDependencies" id="projen.python.PyprojectTomlProject.property.optionalDependencies"></a>

```typescript
public readonly optionalDependencies: any;
```

- *Type:* any

Each entry is a key/value pair, with the key specifying [extra feature name](https://packaging.python.org/en/latest/specifications/core-metadata/#provides-extra-multiple-use) (such as `socks` in `requests[socks]`), and value is an array of [dependency specifier](https://packaging.python.org/en/latest/specifications/dependency-specifiers/) strings.

---

##### `readme`<sup>Optional</sup> <a name="readme" id="projen.python.PyprojectTomlProject.property.readme"></a>

```typescript
public readonly readme: any;
```

- *Type:* any

Value can be a relative path to text / markdown (`.md` suffix) / reStructuredText (`.rst` suffix) readme file, or a table with either: - `file` key containing path of aforementioned readme file, or - `text` key containing the full readme text embedded inside `pyproject.toml`.

---

##### `requiresPython`<sup>Optional</sup> <a name="requiresPython" id="projen.python.PyprojectTomlProject.property.requiresPython"></a>

```typescript
public readonly requiresPython: string;
```

- *Type:* string

Specifies the Python version(s) that the distribution is compatible with.

Must be in the format specified in [Version specifiers](https://packaging.python.org/en/latest/specifications/version-specifiers/).

---

##### `scripts`<sup>Optional</sup> <a name="scripts" id="projen.python.PyprojectTomlProject.property.scripts"></a>

```typescript
public readonly scripts: {[ key: string ]: string};
```

- *Type:* {[ key: string ]: string}

Table of [entry points](https://packaging.python.org/en/latest/specifications/entry-points/) that allows package installers to create a command-line wrapper for. Each key is the name of the script to be created, and each value is the function or object to all, in form of either `importable.module` or `importable.module:object.attr`. Windows platform treats `console_scripts` specially in that they are wrapped in a console executable, so they are attached to a console and can use `sys.stdin`, `sys.stdout` and `sys.stderr` for I/O.

---

##### `urls`<sup>Optional</sup> <a name="urls" id="projen.python.PyprojectTomlProject.property.urls"></a>

```typescript
public readonly urls: {[ key: string ]: string};
```

- *Type:* {[ key: string ]: string}

Table consisting one or multiple `label: URL` pairs.

Common indexes like PyPI uses [well-known Project URLs](https://packaging.python.org/en/latest/specifications/well-known-project-urls/#well-known-labels) when presenting project pages.

---

##### `version`<sup>Optional</sup> <a name="version" id="projen.python.PyprojectTomlProject.property.version"></a>

```typescript
public readonly version: string;
```

- *Type:* string

Version of the project, as defined in the [Version specifier specification](https://packaging.python.org/en/latest/specifications/version-specifiers/), and preferably [already normalized](https://packaging.python.org/en/latest/specifications/version-specifiers/#normalization).

---

### PyprojectTomlTool <a name="PyprojectTomlTool" id="projen.python.PyprojectTomlTool"></a>

Every tool that is used by the project can have users specify configuration data as long as they use a sub-table within `[tool]`.

Generally a project can use the subtable `tool.$NAME` if, and only if, they own the entry for `$NAME` in the Cheeseshop/PyPI.

#### Initializer <a name="Initializer" id="projen.python.PyprojectTomlTool.Initializer"></a>

```typescript
import { python } from 'projen'

const pyprojectTomlTool: python.PyprojectTomlTool = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PyprojectTomlTool.property.black">black</a></code> | <code>any</code> | The uncompromising Python code formatter. |
| <code><a href="#projen.python.PyprojectTomlTool.property.cibuildwheel">cibuildwheel</a></code> | <code>any</code> | Build Python wheels for all platforms. |
| <code><a href="#projen.python.PyprojectTomlTool.property.hatch">hatch</a></code> | <code>any</code> | Modern, extensible Python project management. |
| <code><a href="#projen.python.PyprojectTomlTool.property.maturin">maturin</a></code> | <code>any</code> | Build and publish crates with pyo3, cffi and uniffi bindings as well as rust binaries as python packages. |
| <code><a href="#projen.python.PyprojectTomlTool.property.mypy">mypy</a></code> | <code>any</code> | Optional static typing for Python. |
| <code><a href="#projen.python.PyprojectTomlTool.property.pdm">pdm</a></code> | <code>any</code> | A modern Python package manager with PEP 621 support. |
| <code><a href="#projen.python.PyprojectTomlTool.property.poe">poe</a></code> | <code>any</code> | A task runner that works well with pyproject.toml files. |
| <code><a href="#projen.python.PyprojectTomlTool.property.poetry">poetry</a></code> | <code>any</code> | Python dependency management and packaging made easy. |
| <code><a href="#projen.python.PyprojectTomlTool.property.pyright">pyright</a></code> | <code>any</code> | Static type checker for Python. |
| <code><a href="#projen.python.PyprojectTomlTool.property.pytest">pytest</a></code> | <code>any</code> | Standardized automated testing of Python packages. |
| <code><a href="#projen.python.PyprojectTomlTool.property.repoReview">repoReview</a></code> | <code>any</code> | Review a repository for best practices. |
| <code><a href="#projen.python.PyprojectTomlTool.property.ruff">ruff</a></code> | <code>any</code> | An extremely fast Python linter and formatter, written in Rust. |
| <code><a href="#projen.python.PyprojectTomlTool.property.scikitBuild">scikitBuild</a></code> | <code>any</code> | Improved build system generator for Python C/C++/Fortran extensions. |
| <code><a href="#projen.python.PyprojectTomlTool.property.setuptools">setuptools</a></code> | <code>any</code> | Easily download, build, install, upgrade, and uninstall Python packages. |
| <code><a href="#projen.python.PyprojectTomlTool.property.setuptoolsScm">setuptoolsScm</a></code> | <code>any</code> | Manage Python package versions using SCM (e.g. Git). |
| <code><a href="#projen.python.PyprojectTomlTool.property.taskipy">taskipy</a></code> | <code>any</code> | The complementary task runner for python. |
| <code><a href="#projen.python.PyprojectTomlTool.property.tombi">tombi</a></code> | <code>any</code> | Tombi is a toolkit for TOML; |
| <code><a href="#projen.python.PyprojectTomlTool.property.tox">tox</a></code> | <code>any</code> | Standardized automated testing of Python packages. |
| <code><a href="#projen.python.PyprojectTomlTool.property.ty">ty</a></code> | <code>any</code> | An extremely fast Python type checker, written in Rust. |
| <code><a href="#projen.python.PyprojectTomlTool.property.uv">uv</a></code> | <code>any</code> | An extremely fast Python package installer and resolver, written in Rust. |

---

##### `black`<sup>Optional</sup> <a name="black" id="projen.python.PyprojectTomlTool.property.black"></a>

```typescript
public readonly black: any;
```

- *Type:* any

The uncompromising Python code formatter.

---

##### `cibuildwheel`<sup>Optional</sup> <a name="cibuildwheel" id="projen.python.PyprojectTomlTool.property.cibuildwheel"></a>

```typescript
public readonly cibuildwheel: any;
```

- *Type:* any

Build Python wheels for all platforms.

---

##### `hatch`<sup>Optional</sup> <a name="hatch" id="projen.python.PyprojectTomlTool.property.hatch"></a>

```typescript
public readonly hatch: any;
```

- *Type:* any

Modern, extensible Python project management.

---

##### `maturin`<sup>Optional</sup> <a name="maturin" id="projen.python.PyprojectTomlTool.property.maturin"></a>

```typescript
public readonly maturin: any;
```

- *Type:* any

Build and publish crates with pyo3, cffi and uniffi bindings as well as rust binaries as python packages.

---

##### `mypy`<sup>Optional</sup> <a name="mypy" id="projen.python.PyprojectTomlTool.property.mypy"></a>

```typescript
public readonly mypy: any;
```

- *Type:* any

Optional static typing for Python.

---

##### `pdm`<sup>Optional</sup> <a name="pdm" id="projen.python.PyprojectTomlTool.property.pdm"></a>

```typescript
public readonly pdm: any;
```

- *Type:* any

A modern Python package manager with PEP 621 support.

---

##### `poe`<sup>Optional</sup> <a name="poe" id="projen.python.PyprojectTomlTool.property.poe"></a>

```typescript
public readonly poe: any;
```

- *Type:* any

A task runner that works well with pyproject.toml files.

---

##### `poetry`<sup>Optional</sup> <a name="poetry" id="projen.python.PyprojectTomlTool.property.poetry"></a>

```typescript
public readonly poetry: any;
```

- *Type:* any

Python dependency management and packaging made easy.

---

##### `pyright`<sup>Optional</sup> <a name="pyright" id="projen.python.PyprojectTomlTool.property.pyright"></a>

```typescript
public readonly pyright: any;
```

- *Type:* any

Static type checker for Python.

---

##### `pytest`<sup>Optional</sup> <a name="pytest" id="projen.python.PyprojectTomlTool.property.pytest"></a>

```typescript
public readonly pytest: any;
```

- *Type:* any

Standardized automated testing of Python packages.

---

##### `repoReview`<sup>Optional</sup> <a name="repoReview" id="projen.python.PyprojectTomlTool.property.repoReview"></a>

```typescript
public readonly repoReview: any;
```

- *Type:* any

Review a repository for best practices.

---

##### `ruff`<sup>Optional</sup> <a name="ruff" id="projen.python.PyprojectTomlTool.property.ruff"></a>

```typescript
public readonly ruff: any;
```

- *Type:* any

An extremely fast Python linter and formatter, written in Rust.

---

##### `scikitBuild`<sup>Optional</sup> <a name="scikitBuild" id="projen.python.PyprojectTomlTool.property.scikitBuild"></a>

```typescript
public readonly scikitBuild: any;
```

- *Type:* any

Improved build system generator for Python C/C++/Fortran extensions.

---

##### `setuptools`<sup>Optional</sup> <a name="setuptools" id="projen.python.PyprojectTomlTool.property.setuptools"></a>

```typescript
public readonly setuptools: any;
```

- *Type:* any

Easily download, build, install, upgrade, and uninstall Python packages.

---

##### `setuptoolsScm`<sup>Optional</sup> <a name="setuptoolsScm" id="projen.python.PyprojectTomlTool.property.setuptoolsScm"></a>

```typescript
public readonly setuptoolsScm: any;
```

- *Type:* any

Manage Python package versions using SCM (e.g. Git).

---

##### `taskipy`<sup>Optional</sup> <a name="taskipy" id="projen.python.PyprojectTomlTool.property.taskipy"></a>

```typescript
public readonly taskipy: any;
```

- *Type:* any

The complementary task runner for python.

---

##### `tombi`<sup>Optional</sup> <a name="tombi" id="projen.python.PyprojectTomlTool.property.tombi"></a>

```typescript
public readonly tombi: any;
```

- *Type:* any

Tombi is a toolkit for TOML;

providing a formatter/linter and language server

---

##### `tox`<sup>Optional</sup> <a name="tox" id="projen.python.PyprojectTomlTool.property.tox"></a>

```typescript
public readonly tox: any;
```

- *Type:* any

Standardized automated testing of Python packages.

---

##### `ty`<sup>Optional</sup> <a name="ty" id="projen.python.PyprojectTomlTool.property.ty"></a>

```typescript
public readonly ty: any;
```

- *Type:* any

An extremely fast Python type checker, written in Rust.

---

##### `uv`<sup>Optional</sup> <a name="uv" id="projen.python.PyprojectTomlTool.property.uv"></a>

```typescript
public readonly uv: any;
```

- *Type:* any

An extremely fast Python package installer and resolver, written in Rust.

---

### PyrightConfiguration <a name="PyrightConfiguration" id="projen.python.PyrightConfiguration"></a>

Pyright Configuration Schema.

#### Initializer <a name="Initializer" id="projen.python.PyrightConfiguration.Initializer"></a>

```typescript
import { python } from 'projen'

const pyrightConfiguration: python.PyrightConfiguration = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PyrightConfiguration.property.analyzeUnannotatedFunctions">analyzeUnannotatedFunctions</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.defineConstant">defineConstant</a></code> | <code><a href="#projen.python.PyrightConfigurationDefineConstant">PyrightConfigurationDefineConstant</a></code> | Set of identifiers that should be assumed to contain a constant value wherever used within this program. |
| <code><a href="#projen.python.PyrightConfiguration.property.deprecateTypingAliases">deprecateTypingAliases</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.disableBytesTypePromotions">disableBytesTypePromotions</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.enableExperimentalFeatures">enableExperimentalFeatures</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.enableReachabilityAnalysis">enableReachabilityAnalysis</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.enableTypeIgnoreComments">enableTypeIgnoreComments</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.exclude">exclude</a></code> | <code>string[]</code> | Paths of directories or files that should not be considered part of the project. |
| <code><a href="#projen.python.PyrightConfiguration.property.executionEnvironments">executionEnvironments</a></code> | <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments">PyrightConfigurationExecutionEnvironments</a>[]</code> | Specifies a list of execution environments. |
| <code><a href="#projen.python.PyrightConfiguration.property.extends">extends</a></code> | <code>string</code> | Path to another `.json` or `.toml` file that is used as a "base configuration", allowing this configuration to inherit configuration settings. Top-level keys within this configuration overwrite top-level keys in the base configuration. Multiple levels of inheritance are supported. Relative paths specified in a configuration file are resolved relative to the location of that configuration file. |
| <code><a href="#projen.python.PyrightConfiguration.property.extraPaths">extraPaths</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.ignore">ignore</a></code> | <code>string[]</code> | Paths of directories or files whose diagnostic output (errors and warnings) should be suppressed even if they are an included file or within the transitive closure of an included file. |
| <code><a href="#projen.python.PyrightConfiguration.property.include">include</a></code> | <code>string[]</code> | Paths of directories or files that should be considered part of the project. |
| <code><a href="#projen.python.PyrightConfiguration.property.pythonPlatform">pythonPlatform</a></code> | <code><a href="#projen.python.PythonPlatform">PythonPlatform</a></code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.pythonVersion">pythonVersion</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportAbstractUsage">reportAbstractUsage</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportArgumentType">reportArgumentType</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportAssertAlwaysTrue">reportAssertAlwaysTrue</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportAssertTypeFailure">reportAssertTypeFailure</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportAssignmentType">reportAssignmentType</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportAttributeAccessIssue">reportAttributeAccessIssue</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportCallInDefaultInitializer">reportCallInDefaultInitializer</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportCallIssue">reportCallIssue</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportConstantRedefinition">reportConstantRedefinition</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportDeprecated">reportDeprecated</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportDuplicateImport">reportDuplicateImport</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportFunctionMemberAccess">reportFunctionMemberAccess</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportGeneralTypeIssues">reportGeneralTypeIssues</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportImplicitOverride">reportImplicitOverride</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportImplicitStringConcatenation">reportImplicitStringConcatenation</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportImportCycles">reportImportCycles</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportIncompatibleMethodOverride">reportIncompatibleMethodOverride</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportIncompatibleVariableOverride">reportIncompatibleVariableOverride</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportIncompleteStub">reportIncompleteStub</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportInconsistentConstructor">reportInconsistentConstructor</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportInconsistentOverload">reportInconsistentOverload</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportIndexIssue">reportIndexIssue</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportInvalidStringEscapeSequence">reportInvalidStringEscapeSequence</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportInvalidStubStatement">reportInvalidStubStatement</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportInvalidTypeArguments">reportInvalidTypeArguments</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportInvalidTypeForm">reportInvalidTypeForm</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportInvalidTypeVarUse">reportInvalidTypeVarUse</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportMatchNotExhaustive">reportMatchNotExhaustive</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportMissingImports">reportMissingImports</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportMissingModuleSource">reportMissingModuleSource</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportMissingParameterType">reportMissingParameterType</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportMissingSuperCall">reportMissingSuperCall</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportMissingTypeArgument">reportMissingTypeArgument</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportMissingTypeStubs">reportMissingTypeStubs</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportNoOverloadImplementation">reportNoOverloadImplementation</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportOperatorIssue">reportOperatorIssue</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportOptionalCall">reportOptionalCall</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportOptionalContextManager">reportOptionalContextManager</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportOptionalIterable">reportOptionalIterable</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportOptionalMemberAccess">reportOptionalMemberAccess</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportOptionalOperand">reportOptionalOperand</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportOptionalSubscript">reportOptionalSubscript</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportOverlappingOverload">reportOverlappingOverload</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportPossiblyUnboundVariable">reportPossiblyUnboundVariable</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportPrivateImportUsage">reportPrivateImportUsage</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportPrivateUsage">reportPrivateUsage</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportPropertyTypeMismatch">reportPropertyTypeMismatch</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportRedeclaration">reportRedeclaration</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportReturnType">reportReturnType</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportSelfClsParameterName">reportSelfClsParameterName</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportTypeCommentUsage">reportTypeCommentUsage</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportTypedDictNotRequiredAccess">reportTypedDictNotRequiredAccess</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnboundVariable">reportUnboundVariable</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUndefinedVariable">reportUndefinedVariable</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnhashable">reportUnhashable</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUninitializedInstanceVariable">reportUninitializedInstanceVariable</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnknownArgumentType">reportUnknownArgumentType</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnknownLambdaType">reportUnknownLambdaType</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnknownMemberType">reportUnknownMemberType</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnknownParameterType">reportUnknownParameterType</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnknownVariableType">reportUnknownVariableType</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnnecessaryCast">reportUnnecessaryCast</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnnecessaryComparison">reportUnnecessaryComparison</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnnecessaryContains">reportUnnecessaryContains</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnnecessaryIsInstance">reportUnnecessaryIsInstance</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnnecessaryTypeIgnoreComment">reportUnnecessaryTypeIgnoreComment</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnreachable">reportUnreachable</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnsupportedDunderAll">reportUnsupportedDunderAll</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUntypedBaseClass">reportUntypedBaseClass</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUntypedClassDecorator">reportUntypedClassDecorator</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUntypedFunctionDecorator">reportUntypedFunctionDecorator</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUntypedNamedTuple">reportUntypedNamedTuple</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnusedCallResult">reportUnusedCallResult</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnusedClass">reportUnusedClass</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnusedCoroutine">reportUnusedCoroutine</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnusedExcept">reportUnusedExcept</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnusedExpression">reportUnusedExpression</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnusedFunction">reportUnusedFunction</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnusedImport">reportUnusedImport</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportUnusedVariable">reportUnusedVariable</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.reportWildcardImportFromLibrary">reportWildcardImportFromLibrary</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.strict">strict</a></code> | <code>string[]</code> | Paths of directories or files that should use "strict" analysis if they are included. |
| <code><a href="#projen.python.PyrightConfiguration.property.strictDictionaryInference">strictDictionaryInference</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.strictListInference">strictListInference</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.strictParameterNoneValue">strictParameterNoneValue</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.strictSetInference">strictSetInference</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfiguration.property.stubPath">stubPath</a></code> | <code>string</code> | Path to a directory that contains custom type stubs. |
| <code><a href="#projen.python.PyrightConfiguration.property.typeCheckingMode">typeCheckingMode</a></code> | <code><a href="#projen.python.PyrightConfigurationTypeCheckingMode">PyrightConfigurationTypeCheckingMode</a></code> | Specifies the default rule set to use. |
| <code><a href="#projen.python.PyrightConfiguration.property.typeshedPath">typeshedPath</a></code> | <code>string</code> | Path to a directory that contains `typeshed` type stub files. |
| <code><a href="#projen.python.PyrightConfiguration.property.useLibraryCodeForTypes">useLibraryCodeForTypes</a></code> | <code>boolean</code> | Determines whether pyright reads, parses and analyzes library code to extract type information in the absence of type stub files. |
| <code><a href="#projen.python.PyrightConfiguration.property.venv">venv</a></code> | <code>string</code> | Used in conjunction with the `venvPath`, specifies the virtual environment to use. |
| <code><a href="#projen.python.PyrightConfiguration.property.venvPath">venvPath</a></code> | <code>string</code> | Path to a directory containing one or more subdirectories, each of which contains a virtual environment. |
| <code><a href="#projen.python.PyrightConfiguration.property.verboseOutput">verboseOutput</a></code> | <code>boolean</code> | Specifies whether output logs should be verbose. |

---

##### `analyzeUnannotatedFunctions`<sup>Optional</sup> <a name="analyzeUnannotatedFunctions" id="projen.python.PyrightConfiguration.property.analyzeUnannotatedFunctions"></a>

```typescript
public readonly analyzeUnannotatedFunctions: boolean;
```

- *Type:* boolean

---

##### `defineConstant`<sup>Optional</sup> <a name="defineConstant" id="projen.python.PyrightConfiguration.property.defineConstant"></a>

```typescript
public readonly defineConstant: PyrightConfigurationDefineConstant;
```

- *Type:* <a href="#projen.python.PyrightConfigurationDefineConstant">PyrightConfigurationDefineConstant</a>

Set of identifiers that should be assumed to contain a constant value wherever used within this program.

For example, `{ "DEBUG": true }` indicates that pyright should assume that the identifier `DEBUG` will always be equal to `True`. If this identifier is used within a conditional expression (such as `if not DEBUG:`) pyright will use the indicated value to determine whether the guarded block is reachable or not. Member expressions that reference one of these constants (e.g. `my_module.DEBUG`) are also supported.

---

##### `deprecateTypingAliases`<sup>Optional</sup> <a name="deprecateTypingAliases" id="projen.python.PyrightConfiguration.property.deprecateTypingAliases"></a>

```typescript
public readonly deprecateTypingAliases: boolean;
```

- *Type:* boolean

---

##### `disableBytesTypePromotions`<sup>Optional</sup> <a name="disableBytesTypePromotions" id="projen.python.PyrightConfiguration.property.disableBytesTypePromotions"></a>

```typescript
public readonly disableBytesTypePromotions: boolean;
```

- *Type:* boolean

---

##### `enableExperimentalFeatures`<sup>Optional</sup> <a name="enableExperimentalFeatures" id="projen.python.PyrightConfiguration.property.enableExperimentalFeatures"></a>

```typescript
public readonly enableExperimentalFeatures: boolean;
```

- *Type:* boolean

---

##### `enableReachabilityAnalysis`<sup>Optional</sup> <a name="enableReachabilityAnalysis" id="projen.python.PyrightConfiguration.property.enableReachabilityAnalysis"></a>

```typescript
public readonly enableReachabilityAnalysis: boolean;
```

- *Type:* boolean

---

##### `enableTypeIgnoreComments`<sup>Optional</sup> <a name="enableTypeIgnoreComments" id="projen.python.PyrightConfiguration.property.enableTypeIgnoreComments"></a>

```typescript
public readonly enableTypeIgnoreComments: boolean;
```

- *Type:* boolean

---

##### `exclude`<sup>Optional</sup> <a name="exclude" id="projen.python.PyrightConfiguration.property.exclude"></a>

```typescript
public readonly exclude: string[];
```

- *Type:* string[]

Paths of directories or files that should not be considered part of the project.

These override the includes directories and files, allowing specific subdirectories to be excluded. Note that files in the exclude paths may still be included in the analysis if they are referenced (imported) by source files that are not excluded. Paths may contain wildcard characters: `**` (a directory or multiple levels of directories), `*` (a sequence of zero or more characters), or `?` (a single character). If no exclude paths are specified, Pyright automatically excludes the following: `*_/node_modules`, `*_/__pycache__`, `*_/.*` and any virtual environment directories.

---

##### `executionEnvironments`<sup>Optional</sup> <a name="executionEnvironments" id="projen.python.PyrightConfiguration.property.executionEnvironments"></a>

```typescript
public readonly executionEnvironments: PyrightConfigurationExecutionEnvironments[];
```

- *Type:* <a href="#projen.python.PyrightConfigurationExecutionEnvironments">PyrightConfigurationExecutionEnvironments</a>[]

Specifies a list of execution environments.

Execution environments are searched from start to finish by comparing the path of a source file with the root path specified in the execution environment.

---

##### `extends`<sup>Optional</sup> <a name="extends" id="projen.python.PyrightConfiguration.property.extends"></a>

```typescript
public readonly extends: string;
```

- *Type:* string

Path to another `.json` or `.toml` file that is used as a "base configuration", allowing this configuration to inherit configuration settings. Top-level keys within this configuration overwrite top-level keys in the base configuration. Multiple levels of inheritance are supported. Relative paths specified in a configuration file are resolved relative to the location of that configuration file.

---

##### `extraPaths`<sup>Optional</sup> <a name="extraPaths" id="projen.python.PyrightConfiguration.property.extraPaths"></a>

```typescript
public readonly extraPaths: string[];
```

- *Type:* string[]

---

##### `ignore`<sup>Optional</sup> <a name="ignore" id="projen.python.PyrightConfiguration.property.ignore"></a>

```typescript
public readonly ignore: string[];
```

- *Type:* string[]

Paths of directories or files whose diagnostic output (errors and warnings) should be suppressed even if they are an included file or within the transitive closure of an included file.

Paths may contain wildcard characters: `**` (a directory or multiple levels of directories), `*` (a sequence of zero or more characters), or `?` (a single character).

---

##### `include`<sup>Optional</sup> <a name="include" id="projen.python.PyrightConfiguration.property.include"></a>

```typescript
public readonly include: string[];
```

- *Type:* string[]

Paths of directories or files that should be considered part of the project.

If no paths are specified, pyright defaults to the directory that contains the config file. Paths may contain wildcard characters: `**` (a directory or multiple levels of directories), `*` (a sequence of zero or more characters), or `?` (a single character). If no include paths are specified, the root path for the workspace is assumed.

---

##### `pythonPlatform`<sup>Optional</sup> <a name="pythonPlatform" id="projen.python.PyrightConfiguration.property.pythonPlatform"></a>

```typescript
public readonly pythonPlatform: PythonPlatform;
```

- *Type:* <a href="#projen.python.PythonPlatform">PythonPlatform</a>

---

##### `pythonVersion`<sup>Optional</sup> <a name="pythonVersion" id="projen.python.PyrightConfiguration.property.pythonVersion"></a>

```typescript
public readonly pythonVersion: string;
```

- *Type:* string

---

##### `reportAbstractUsage`<sup>Optional</sup> <a name="reportAbstractUsage" id="projen.python.PyrightConfiguration.property.reportAbstractUsage"></a>

```typescript
public readonly reportAbstractUsage: any;
```

- *Type:* any

---

##### `reportArgumentType`<sup>Optional</sup> <a name="reportArgumentType" id="projen.python.PyrightConfiguration.property.reportArgumentType"></a>

```typescript
public readonly reportArgumentType: any;
```

- *Type:* any

---

##### `reportAssertAlwaysTrue`<sup>Optional</sup> <a name="reportAssertAlwaysTrue" id="projen.python.PyrightConfiguration.property.reportAssertAlwaysTrue"></a>

```typescript
public readonly reportAssertAlwaysTrue: any;
```

- *Type:* any

---

##### `reportAssertTypeFailure`<sup>Optional</sup> <a name="reportAssertTypeFailure" id="projen.python.PyrightConfiguration.property.reportAssertTypeFailure"></a>

```typescript
public readonly reportAssertTypeFailure: any;
```

- *Type:* any

---

##### `reportAssignmentType`<sup>Optional</sup> <a name="reportAssignmentType" id="projen.python.PyrightConfiguration.property.reportAssignmentType"></a>

```typescript
public readonly reportAssignmentType: any;
```

- *Type:* any

---

##### `reportAttributeAccessIssue`<sup>Optional</sup> <a name="reportAttributeAccessIssue" id="projen.python.PyrightConfiguration.property.reportAttributeAccessIssue"></a>

```typescript
public readonly reportAttributeAccessIssue: any;
```

- *Type:* any

---

##### `reportCallInDefaultInitializer`<sup>Optional</sup> <a name="reportCallInDefaultInitializer" id="projen.python.PyrightConfiguration.property.reportCallInDefaultInitializer"></a>

```typescript
public readonly reportCallInDefaultInitializer: any;
```

- *Type:* any

---

##### `reportCallIssue`<sup>Optional</sup> <a name="reportCallIssue" id="projen.python.PyrightConfiguration.property.reportCallIssue"></a>

```typescript
public readonly reportCallIssue: any;
```

- *Type:* any

---

##### `reportConstantRedefinition`<sup>Optional</sup> <a name="reportConstantRedefinition" id="projen.python.PyrightConfiguration.property.reportConstantRedefinition"></a>

```typescript
public readonly reportConstantRedefinition: any;
```

- *Type:* any

---

##### `reportDeprecated`<sup>Optional</sup> <a name="reportDeprecated" id="projen.python.PyrightConfiguration.property.reportDeprecated"></a>

```typescript
public readonly reportDeprecated: any;
```

- *Type:* any

---

##### `reportDuplicateImport`<sup>Optional</sup> <a name="reportDuplicateImport" id="projen.python.PyrightConfiguration.property.reportDuplicateImport"></a>

```typescript
public readonly reportDuplicateImport: any;
```

- *Type:* any

---

##### `reportFunctionMemberAccess`<sup>Optional</sup> <a name="reportFunctionMemberAccess" id="projen.python.PyrightConfiguration.property.reportFunctionMemberAccess"></a>

```typescript
public readonly reportFunctionMemberAccess: any;
```

- *Type:* any

---

##### `reportGeneralTypeIssues`<sup>Optional</sup> <a name="reportGeneralTypeIssues" id="projen.python.PyrightConfiguration.property.reportGeneralTypeIssues"></a>

```typescript
public readonly reportGeneralTypeIssues: any;
```

- *Type:* any

---

##### `reportImplicitOverride`<sup>Optional</sup> <a name="reportImplicitOverride" id="projen.python.PyrightConfiguration.property.reportImplicitOverride"></a>

```typescript
public readonly reportImplicitOverride: any;
```

- *Type:* any

---

##### `reportImplicitStringConcatenation`<sup>Optional</sup> <a name="reportImplicitStringConcatenation" id="projen.python.PyrightConfiguration.property.reportImplicitStringConcatenation"></a>

```typescript
public readonly reportImplicitStringConcatenation: any;
```

- *Type:* any

---

##### `reportImportCycles`<sup>Optional</sup> <a name="reportImportCycles" id="projen.python.PyrightConfiguration.property.reportImportCycles"></a>

```typescript
public readonly reportImportCycles: any;
```

- *Type:* any

---

##### `reportIncompatibleMethodOverride`<sup>Optional</sup> <a name="reportIncompatibleMethodOverride" id="projen.python.PyrightConfiguration.property.reportIncompatibleMethodOverride"></a>

```typescript
public readonly reportIncompatibleMethodOverride: any;
```

- *Type:* any

---

##### `reportIncompatibleVariableOverride`<sup>Optional</sup> <a name="reportIncompatibleVariableOverride" id="projen.python.PyrightConfiguration.property.reportIncompatibleVariableOverride"></a>

```typescript
public readonly reportIncompatibleVariableOverride: any;
```

- *Type:* any

---

##### `reportIncompleteStub`<sup>Optional</sup> <a name="reportIncompleteStub" id="projen.python.PyrightConfiguration.property.reportIncompleteStub"></a>

```typescript
public readonly reportIncompleteStub: any;
```

- *Type:* any

---

##### `reportInconsistentConstructor`<sup>Optional</sup> <a name="reportInconsistentConstructor" id="projen.python.PyrightConfiguration.property.reportInconsistentConstructor"></a>

```typescript
public readonly reportInconsistentConstructor: any;
```

- *Type:* any

---

##### `reportInconsistentOverload`<sup>Optional</sup> <a name="reportInconsistentOverload" id="projen.python.PyrightConfiguration.property.reportInconsistentOverload"></a>

```typescript
public readonly reportInconsistentOverload: any;
```

- *Type:* any

---

##### `reportIndexIssue`<sup>Optional</sup> <a name="reportIndexIssue" id="projen.python.PyrightConfiguration.property.reportIndexIssue"></a>

```typescript
public readonly reportIndexIssue: any;
```

- *Type:* any

---

##### `reportInvalidStringEscapeSequence`<sup>Optional</sup> <a name="reportInvalidStringEscapeSequence" id="projen.python.PyrightConfiguration.property.reportInvalidStringEscapeSequence"></a>

```typescript
public readonly reportInvalidStringEscapeSequence: any;
```

- *Type:* any

---

##### `reportInvalidStubStatement`<sup>Optional</sup> <a name="reportInvalidStubStatement" id="projen.python.PyrightConfiguration.property.reportInvalidStubStatement"></a>

```typescript
public readonly reportInvalidStubStatement: any;
```

- *Type:* any

---

##### `reportInvalidTypeArguments`<sup>Optional</sup> <a name="reportInvalidTypeArguments" id="projen.python.PyrightConfiguration.property.reportInvalidTypeArguments"></a>

```typescript
public readonly reportInvalidTypeArguments: any;
```

- *Type:* any

---

##### `reportInvalidTypeForm`<sup>Optional</sup> <a name="reportInvalidTypeForm" id="projen.python.PyrightConfiguration.property.reportInvalidTypeForm"></a>

```typescript
public readonly reportInvalidTypeForm: any;
```

- *Type:* any

---

##### `reportInvalidTypeVarUse`<sup>Optional</sup> <a name="reportInvalidTypeVarUse" id="projen.python.PyrightConfiguration.property.reportInvalidTypeVarUse"></a>

```typescript
public readonly reportInvalidTypeVarUse: any;
```

- *Type:* any

---

##### `reportMatchNotExhaustive`<sup>Optional</sup> <a name="reportMatchNotExhaustive" id="projen.python.PyrightConfiguration.property.reportMatchNotExhaustive"></a>

```typescript
public readonly reportMatchNotExhaustive: any;
```

- *Type:* any

---

##### `reportMissingImports`<sup>Optional</sup> <a name="reportMissingImports" id="projen.python.PyrightConfiguration.property.reportMissingImports"></a>

```typescript
public readonly reportMissingImports: any;
```

- *Type:* any

---

##### `reportMissingModuleSource`<sup>Optional</sup> <a name="reportMissingModuleSource" id="projen.python.PyrightConfiguration.property.reportMissingModuleSource"></a>

```typescript
public readonly reportMissingModuleSource: any;
```

- *Type:* any

---

##### `reportMissingParameterType`<sup>Optional</sup> <a name="reportMissingParameterType" id="projen.python.PyrightConfiguration.property.reportMissingParameterType"></a>

```typescript
public readonly reportMissingParameterType: any;
```

- *Type:* any

---

##### `reportMissingSuperCall`<sup>Optional</sup> <a name="reportMissingSuperCall" id="projen.python.PyrightConfiguration.property.reportMissingSuperCall"></a>

```typescript
public readonly reportMissingSuperCall: any;
```

- *Type:* any

---

##### `reportMissingTypeArgument`<sup>Optional</sup> <a name="reportMissingTypeArgument" id="projen.python.PyrightConfiguration.property.reportMissingTypeArgument"></a>

```typescript
public readonly reportMissingTypeArgument: any;
```

- *Type:* any

---

##### `reportMissingTypeStubs`<sup>Optional</sup> <a name="reportMissingTypeStubs" id="projen.python.PyrightConfiguration.property.reportMissingTypeStubs"></a>

```typescript
public readonly reportMissingTypeStubs: any;
```

- *Type:* any

---

##### `reportNoOverloadImplementation`<sup>Optional</sup> <a name="reportNoOverloadImplementation" id="projen.python.PyrightConfiguration.property.reportNoOverloadImplementation"></a>

```typescript
public readonly reportNoOverloadImplementation: any;
```

- *Type:* any

---

##### `reportOperatorIssue`<sup>Optional</sup> <a name="reportOperatorIssue" id="projen.python.PyrightConfiguration.property.reportOperatorIssue"></a>

```typescript
public readonly reportOperatorIssue: any;
```

- *Type:* any

---

##### `reportOptionalCall`<sup>Optional</sup> <a name="reportOptionalCall" id="projen.python.PyrightConfiguration.property.reportOptionalCall"></a>

```typescript
public readonly reportOptionalCall: any;
```

- *Type:* any

---

##### `reportOptionalContextManager`<sup>Optional</sup> <a name="reportOptionalContextManager" id="projen.python.PyrightConfiguration.property.reportOptionalContextManager"></a>

```typescript
public readonly reportOptionalContextManager: any;
```

- *Type:* any

---

##### `reportOptionalIterable`<sup>Optional</sup> <a name="reportOptionalIterable" id="projen.python.PyrightConfiguration.property.reportOptionalIterable"></a>

```typescript
public readonly reportOptionalIterable: any;
```

- *Type:* any

---

##### `reportOptionalMemberAccess`<sup>Optional</sup> <a name="reportOptionalMemberAccess" id="projen.python.PyrightConfiguration.property.reportOptionalMemberAccess"></a>

```typescript
public readonly reportOptionalMemberAccess: any;
```

- *Type:* any

---

##### `reportOptionalOperand`<sup>Optional</sup> <a name="reportOptionalOperand" id="projen.python.PyrightConfiguration.property.reportOptionalOperand"></a>

```typescript
public readonly reportOptionalOperand: any;
```

- *Type:* any

---

##### `reportOptionalSubscript`<sup>Optional</sup> <a name="reportOptionalSubscript" id="projen.python.PyrightConfiguration.property.reportOptionalSubscript"></a>

```typescript
public readonly reportOptionalSubscript: any;
```

- *Type:* any

---

##### `reportOverlappingOverload`<sup>Optional</sup> <a name="reportOverlappingOverload" id="projen.python.PyrightConfiguration.property.reportOverlappingOverload"></a>

```typescript
public readonly reportOverlappingOverload: any;
```

- *Type:* any

---

##### `reportPossiblyUnboundVariable`<sup>Optional</sup> <a name="reportPossiblyUnboundVariable" id="projen.python.PyrightConfiguration.property.reportPossiblyUnboundVariable"></a>

```typescript
public readonly reportPossiblyUnboundVariable: any;
```

- *Type:* any

---

##### `reportPrivateImportUsage`<sup>Optional</sup> <a name="reportPrivateImportUsage" id="projen.python.PyrightConfiguration.property.reportPrivateImportUsage"></a>

```typescript
public readonly reportPrivateImportUsage: any;
```

- *Type:* any

---

##### `reportPrivateUsage`<sup>Optional</sup> <a name="reportPrivateUsage" id="projen.python.PyrightConfiguration.property.reportPrivateUsage"></a>

```typescript
public readonly reportPrivateUsage: any;
```

- *Type:* any

---

##### `reportPropertyTypeMismatch`<sup>Optional</sup> <a name="reportPropertyTypeMismatch" id="projen.python.PyrightConfiguration.property.reportPropertyTypeMismatch"></a>

```typescript
public readonly reportPropertyTypeMismatch: any;
```

- *Type:* any

---

##### `reportRedeclaration`<sup>Optional</sup> <a name="reportRedeclaration" id="projen.python.PyrightConfiguration.property.reportRedeclaration"></a>

```typescript
public readonly reportRedeclaration: any;
```

- *Type:* any

---

##### `reportReturnType`<sup>Optional</sup> <a name="reportReturnType" id="projen.python.PyrightConfiguration.property.reportReturnType"></a>

```typescript
public readonly reportReturnType: any;
```

- *Type:* any

---

##### `reportSelfClsParameterName`<sup>Optional</sup> <a name="reportSelfClsParameterName" id="projen.python.PyrightConfiguration.property.reportSelfClsParameterName"></a>

```typescript
public readonly reportSelfClsParameterName: any;
```

- *Type:* any

---

##### `reportTypeCommentUsage`<sup>Optional</sup> <a name="reportTypeCommentUsage" id="projen.python.PyrightConfiguration.property.reportTypeCommentUsage"></a>

```typescript
public readonly reportTypeCommentUsage: any;
```

- *Type:* any

---

##### `reportTypedDictNotRequiredAccess`<sup>Optional</sup> <a name="reportTypedDictNotRequiredAccess" id="projen.python.PyrightConfiguration.property.reportTypedDictNotRequiredAccess"></a>

```typescript
public readonly reportTypedDictNotRequiredAccess: any;
```

- *Type:* any

---

##### `reportUnboundVariable`<sup>Optional</sup> <a name="reportUnboundVariable" id="projen.python.PyrightConfiguration.property.reportUnboundVariable"></a>

```typescript
public readonly reportUnboundVariable: any;
```

- *Type:* any

---

##### `reportUndefinedVariable`<sup>Optional</sup> <a name="reportUndefinedVariable" id="projen.python.PyrightConfiguration.property.reportUndefinedVariable"></a>

```typescript
public readonly reportUndefinedVariable: any;
```

- *Type:* any

---

##### `reportUnhashable`<sup>Optional</sup> <a name="reportUnhashable" id="projen.python.PyrightConfiguration.property.reportUnhashable"></a>

```typescript
public readonly reportUnhashable: any;
```

- *Type:* any

---

##### `reportUninitializedInstanceVariable`<sup>Optional</sup> <a name="reportUninitializedInstanceVariable" id="projen.python.PyrightConfiguration.property.reportUninitializedInstanceVariable"></a>

```typescript
public readonly reportUninitializedInstanceVariable: any;
```

- *Type:* any

---

##### `reportUnknownArgumentType`<sup>Optional</sup> <a name="reportUnknownArgumentType" id="projen.python.PyrightConfiguration.property.reportUnknownArgumentType"></a>

```typescript
public readonly reportUnknownArgumentType: any;
```

- *Type:* any

---

##### `reportUnknownLambdaType`<sup>Optional</sup> <a name="reportUnknownLambdaType" id="projen.python.PyrightConfiguration.property.reportUnknownLambdaType"></a>

```typescript
public readonly reportUnknownLambdaType: any;
```

- *Type:* any

---

##### `reportUnknownMemberType`<sup>Optional</sup> <a name="reportUnknownMemberType" id="projen.python.PyrightConfiguration.property.reportUnknownMemberType"></a>

```typescript
public readonly reportUnknownMemberType: any;
```

- *Type:* any

---

##### `reportUnknownParameterType`<sup>Optional</sup> <a name="reportUnknownParameterType" id="projen.python.PyrightConfiguration.property.reportUnknownParameterType"></a>

```typescript
public readonly reportUnknownParameterType: any;
```

- *Type:* any

---

##### `reportUnknownVariableType`<sup>Optional</sup> <a name="reportUnknownVariableType" id="projen.python.PyrightConfiguration.property.reportUnknownVariableType"></a>

```typescript
public readonly reportUnknownVariableType: any;
```

- *Type:* any

---

##### `reportUnnecessaryCast`<sup>Optional</sup> <a name="reportUnnecessaryCast" id="projen.python.PyrightConfiguration.property.reportUnnecessaryCast"></a>

```typescript
public readonly reportUnnecessaryCast: any;
```

- *Type:* any

---

##### `reportUnnecessaryComparison`<sup>Optional</sup> <a name="reportUnnecessaryComparison" id="projen.python.PyrightConfiguration.property.reportUnnecessaryComparison"></a>

```typescript
public readonly reportUnnecessaryComparison: any;
```

- *Type:* any

---

##### `reportUnnecessaryContains`<sup>Optional</sup> <a name="reportUnnecessaryContains" id="projen.python.PyrightConfiguration.property.reportUnnecessaryContains"></a>

```typescript
public readonly reportUnnecessaryContains: any;
```

- *Type:* any

---

##### `reportUnnecessaryIsInstance`<sup>Optional</sup> <a name="reportUnnecessaryIsInstance" id="projen.python.PyrightConfiguration.property.reportUnnecessaryIsInstance"></a>

```typescript
public readonly reportUnnecessaryIsInstance: any;
```

- *Type:* any

---

##### `reportUnnecessaryTypeIgnoreComment`<sup>Optional</sup> <a name="reportUnnecessaryTypeIgnoreComment" id="projen.python.PyrightConfiguration.property.reportUnnecessaryTypeIgnoreComment"></a>

```typescript
public readonly reportUnnecessaryTypeIgnoreComment: any;
```

- *Type:* any

---

##### `reportUnreachable`<sup>Optional</sup> <a name="reportUnreachable" id="projen.python.PyrightConfiguration.property.reportUnreachable"></a>

```typescript
public readonly reportUnreachable: any;
```

- *Type:* any

---

##### `reportUnsupportedDunderAll`<sup>Optional</sup> <a name="reportUnsupportedDunderAll" id="projen.python.PyrightConfiguration.property.reportUnsupportedDunderAll"></a>

```typescript
public readonly reportUnsupportedDunderAll: any;
```

- *Type:* any

---

##### `reportUntypedBaseClass`<sup>Optional</sup> <a name="reportUntypedBaseClass" id="projen.python.PyrightConfiguration.property.reportUntypedBaseClass"></a>

```typescript
public readonly reportUntypedBaseClass: any;
```

- *Type:* any

---

##### `reportUntypedClassDecorator`<sup>Optional</sup> <a name="reportUntypedClassDecorator" id="projen.python.PyrightConfiguration.property.reportUntypedClassDecorator"></a>

```typescript
public readonly reportUntypedClassDecorator: any;
```

- *Type:* any

---

##### `reportUntypedFunctionDecorator`<sup>Optional</sup> <a name="reportUntypedFunctionDecorator" id="projen.python.PyrightConfiguration.property.reportUntypedFunctionDecorator"></a>

```typescript
public readonly reportUntypedFunctionDecorator: any;
```

- *Type:* any

---

##### `reportUntypedNamedTuple`<sup>Optional</sup> <a name="reportUntypedNamedTuple" id="projen.python.PyrightConfiguration.property.reportUntypedNamedTuple"></a>

```typescript
public readonly reportUntypedNamedTuple: any;
```

- *Type:* any

---

##### `reportUnusedCallResult`<sup>Optional</sup> <a name="reportUnusedCallResult" id="projen.python.PyrightConfiguration.property.reportUnusedCallResult"></a>

```typescript
public readonly reportUnusedCallResult: any;
```

- *Type:* any

---

##### `reportUnusedClass`<sup>Optional</sup> <a name="reportUnusedClass" id="projen.python.PyrightConfiguration.property.reportUnusedClass"></a>

```typescript
public readonly reportUnusedClass: any;
```

- *Type:* any

---

##### `reportUnusedCoroutine`<sup>Optional</sup> <a name="reportUnusedCoroutine" id="projen.python.PyrightConfiguration.property.reportUnusedCoroutine"></a>

```typescript
public readonly reportUnusedCoroutine: any;
```

- *Type:* any

---

##### `reportUnusedExcept`<sup>Optional</sup> <a name="reportUnusedExcept" id="projen.python.PyrightConfiguration.property.reportUnusedExcept"></a>

```typescript
public readonly reportUnusedExcept: any;
```

- *Type:* any

---

##### `reportUnusedExpression`<sup>Optional</sup> <a name="reportUnusedExpression" id="projen.python.PyrightConfiguration.property.reportUnusedExpression"></a>

```typescript
public readonly reportUnusedExpression: any;
```

- *Type:* any

---

##### `reportUnusedFunction`<sup>Optional</sup> <a name="reportUnusedFunction" id="projen.python.PyrightConfiguration.property.reportUnusedFunction"></a>

```typescript
public readonly reportUnusedFunction: any;
```

- *Type:* any

---

##### `reportUnusedImport`<sup>Optional</sup> <a name="reportUnusedImport" id="projen.python.PyrightConfiguration.property.reportUnusedImport"></a>

```typescript
public readonly reportUnusedImport: any;
```

- *Type:* any

---

##### `reportUnusedVariable`<sup>Optional</sup> <a name="reportUnusedVariable" id="projen.python.PyrightConfiguration.property.reportUnusedVariable"></a>

```typescript
public readonly reportUnusedVariable: any;
```

- *Type:* any

---

##### `reportWildcardImportFromLibrary`<sup>Optional</sup> <a name="reportWildcardImportFromLibrary" id="projen.python.PyrightConfiguration.property.reportWildcardImportFromLibrary"></a>

```typescript
public readonly reportWildcardImportFromLibrary: any;
```

- *Type:* any

---

##### `strict`<sup>Optional</sup> <a name="strict" id="projen.python.PyrightConfiguration.property.strict"></a>

```typescript
public readonly strict: string[];
```

- *Type:* string[]

Paths of directories or files that should use "strict" analysis if they are included.

This is the same as manually adding a `# pyright: strict` comment. In strict mode, most type-checking rules are enabled. Paths may contain wildcard characters: `**` (a directory or multiple levels of directories), `*` (a sequence of zero or more characters), or `?` (a single character).

---

##### `strictDictionaryInference`<sup>Optional</sup> <a name="strictDictionaryInference" id="projen.python.PyrightConfiguration.property.strictDictionaryInference"></a>

```typescript
public readonly strictDictionaryInference: boolean;
```

- *Type:* boolean

---

##### `strictListInference`<sup>Optional</sup> <a name="strictListInference" id="projen.python.PyrightConfiguration.property.strictListInference"></a>

```typescript
public readonly strictListInference: boolean;
```

- *Type:* boolean

---

##### `strictParameterNoneValue`<sup>Optional</sup> <a name="strictParameterNoneValue" id="projen.python.PyrightConfiguration.property.strictParameterNoneValue"></a>

```typescript
public readonly strictParameterNoneValue: boolean;
```

- *Type:* boolean

---

##### `strictSetInference`<sup>Optional</sup> <a name="strictSetInference" id="projen.python.PyrightConfiguration.property.strictSetInference"></a>

```typescript
public readonly strictSetInference: boolean;
```

- *Type:* boolean

---

##### `stubPath`<sup>Optional</sup> <a name="stubPath" id="projen.python.PyrightConfiguration.property.stubPath"></a>

```typescript
public readonly stubPath: string;
```

- *Type:* string

Path to a directory that contains custom type stubs.

Each package's type stub file(s) are expected to be in its own subdirectory.

---

##### `typeCheckingMode`<sup>Optional</sup> <a name="typeCheckingMode" id="projen.python.PyrightConfiguration.property.typeCheckingMode"></a>

```typescript
public readonly typeCheckingMode: PyrightConfigurationTypeCheckingMode;
```

- *Type:* <a href="#projen.python.PyrightConfigurationTypeCheckingMode">PyrightConfigurationTypeCheckingMode</a>

Specifies the default rule set to use.

Some rules can be overridden using additional configuration flags documented below. If set to `off`, all type-checking rules are disabled, but Python syntax and semantic errors are still reported.

---

##### `typeshedPath`<sup>Optional</sup> <a name="typeshedPath" id="projen.python.PyrightConfiguration.property.typeshedPath"></a>

```typescript
public readonly typeshedPath: string;
```

- *Type:* string

Path to a directory that contains `typeshed` type stub files.

Pyright ships with a bundled copy of `typeshed` type stubs. If you want to use a different version of `typeshed` stubs, you can clone the `typeshed` GitHub repo (https://github.com/python/typeshed) to a local directory and reference the location with this path. This option is useful if you're actively contributing updates to `typeshed`.

---

##### `useLibraryCodeForTypes`<sup>Optional</sup> <a name="useLibraryCodeForTypes" id="projen.python.PyrightConfiguration.property.useLibraryCodeForTypes"></a>

```typescript
public readonly useLibraryCodeForTypes: boolean;
```

- *Type:* boolean

Determines whether pyright reads, parses and analyzes library code to extract type information in the absence of type stub files.

Type information will typically be incomplete. We recommend using type stubs where possible.

---

##### `venv`<sup>Optional</sup> <a name="venv" id="projen.python.PyrightConfiguration.property.venv"></a>

```typescript
public readonly venv: string;
```

- *Type:* string

Used in conjunction with the `venvPath`, specifies the virtual environment to use.

---

##### `venvPath`<sup>Optional</sup> <a name="venvPath" id="projen.python.PyrightConfiguration.property.venvPath"></a>

```typescript
public readonly venvPath: string;
```

- *Type:* string

Path to a directory containing one or more subdirectories, each of which contains a virtual environment.

When used in conjunction with a `venv` setting, pyright will search for imports in the virtual environment's site-packages directory rather than the paths specified by the default Python interpreter. If you are working on a project with other developers, it is best not to specify this setting in the config file, since this path will typically differ for each developer. Instead, it can be specified on the command line or in a per-user setting.

---

##### `verboseOutput`<sup>Optional</sup> <a name="verboseOutput" id="projen.python.PyrightConfiguration.property.verboseOutput"></a>

```typescript
public readonly verboseOutput: boolean;
```

- *Type:* boolean

Specifies whether output logs should be verbose.

This is useful when diagnosing certain problems like import resolution issues.

---

### PyrightConfigurationDefineConstant <a name="PyrightConfigurationDefineConstant" id="projen.python.PyrightConfigurationDefineConstant"></a>

Set of identifiers that should be assumed to contain a constant value wherever used within this program.

For example, `{ "DEBUG": true }` indicates that pyright should assume that the identifier `DEBUG` will always be equal to `True`. If this identifier is used within a conditional expression (such as `if not DEBUG:`) pyright will use the indicated value to determine whether the guarded block is reachable or not. Member expressions that reference one of these constants (e.g. `my_module.DEBUG`) are also supported.

#### Initializer <a name="Initializer" id="projen.python.PyrightConfigurationDefineConstant.Initializer"></a>

```typescript
import { python } from 'projen'

const pyrightConfigurationDefineConstant: python.PyrightConfigurationDefineConstant = { ... }
```


### PyrightConfigurationExecutionEnvironments <a name="PyrightConfigurationExecutionEnvironments" id="projen.python.PyrightConfigurationExecutionEnvironments"></a>

#### Initializer <a name="Initializer" id="projen.python.PyrightConfigurationExecutionEnvironments.Initializer"></a>

```typescript
import { python } from 'projen'

const pyrightConfigurationExecutionEnvironments: python.PyrightConfigurationExecutionEnvironments = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.root">root</a></code> | <code>string</code> | Root path for the code that will execute within this execution environment. |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.analyzeUnannotatedFunctions">analyzeUnannotatedFunctions</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.deprecateTypingAliases">deprecateTypingAliases</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.disableBytesTypePromotions">disableBytesTypePromotions</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.enableExperimentalFeatures">enableExperimentalFeatures</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.enableReachabilityAnalysis">enableReachabilityAnalysis</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.enableTypeIgnoreComments">enableTypeIgnoreComments</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.extraPaths">extraPaths</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.pythonPlatform">pythonPlatform</a></code> | <code><a href="#projen.python.PythonPlatform">PythonPlatform</a></code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.pythonVersion">pythonVersion</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportAbstractUsage">reportAbstractUsage</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportArgumentType">reportArgumentType</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportAssertAlwaysTrue">reportAssertAlwaysTrue</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportAssertTypeFailure">reportAssertTypeFailure</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportAssignmentType">reportAssignmentType</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportAttributeAccessIssue">reportAttributeAccessIssue</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportCallInDefaultInitializer">reportCallInDefaultInitializer</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportCallIssue">reportCallIssue</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportConstantRedefinition">reportConstantRedefinition</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportDeprecated">reportDeprecated</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportDuplicateImport">reportDuplicateImport</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportFunctionMemberAccess">reportFunctionMemberAccess</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportGeneralTypeIssues">reportGeneralTypeIssues</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportImplicitOverride">reportImplicitOverride</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportImplicitStringConcatenation">reportImplicitStringConcatenation</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportImportCycles">reportImportCycles</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportIncompatibleMethodOverride">reportIncompatibleMethodOverride</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportIncompatibleVariableOverride">reportIncompatibleVariableOverride</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportIncompleteStub">reportIncompleteStub</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportInconsistentConstructor">reportInconsistentConstructor</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportInconsistentOverload">reportInconsistentOverload</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportIndexIssue">reportIndexIssue</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportInvalidStringEscapeSequence">reportInvalidStringEscapeSequence</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportInvalidStubStatement">reportInvalidStubStatement</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportInvalidTypeArguments">reportInvalidTypeArguments</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportInvalidTypeForm">reportInvalidTypeForm</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportInvalidTypeVarUse">reportInvalidTypeVarUse</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportMatchNotExhaustive">reportMatchNotExhaustive</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportMissingImports">reportMissingImports</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportMissingModuleSource">reportMissingModuleSource</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportMissingParameterType">reportMissingParameterType</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportMissingSuperCall">reportMissingSuperCall</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportMissingTypeArgument">reportMissingTypeArgument</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportMissingTypeStubs">reportMissingTypeStubs</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportNoOverloadImplementation">reportNoOverloadImplementation</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportOperatorIssue">reportOperatorIssue</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportOptionalCall">reportOptionalCall</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportOptionalContextManager">reportOptionalContextManager</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportOptionalIterable">reportOptionalIterable</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportOptionalMemberAccess">reportOptionalMemberAccess</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportOptionalOperand">reportOptionalOperand</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportOptionalSubscript">reportOptionalSubscript</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportOverlappingOverload">reportOverlappingOverload</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportPossiblyUnboundVariable">reportPossiblyUnboundVariable</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportPrivateImportUsage">reportPrivateImportUsage</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportPrivateUsage">reportPrivateUsage</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportPropertyTypeMismatch">reportPropertyTypeMismatch</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportRedeclaration">reportRedeclaration</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportReturnType">reportReturnType</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportSelfClsParameterName">reportSelfClsParameterName</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportTypeCommentUsage">reportTypeCommentUsage</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportTypedDictNotRequiredAccess">reportTypedDictNotRequiredAccess</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnboundVariable">reportUnboundVariable</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUndefinedVariable">reportUndefinedVariable</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnhashable">reportUnhashable</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUninitializedInstanceVariable">reportUninitializedInstanceVariable</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnknownArgumentType">reportUnknownArgumentType</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnknownLambdaType">reportUnknownLambdaType</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnknownMemberType">reportUnknownMemberType</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnknownParameterType">reportUnknownParameterType</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnknownVariableType">reportUnknownVariableType</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnnecessaryCast">reportUnnecessaryCast</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnnecessaryComparison">reportUnnecessaryComparison</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnnecessaryContains">reportUnnecessaryContains</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnnecessaryIsInstance">reportUnnecessaryIsInstance</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnnecessaryTypeIgnoreComment">reportUnnecessaryTypeIgnoreComment</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnreachable">reportUnreachable</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnsupportedDunderAll">reportUnsupportedDunderAll</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUntypedBaseClass">reportUntypedBaseClass</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUntypedClassDecorator">reportUntypedClassDecorator</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUntypedFunctionDecorator">reportUntypedFunctionDecorator</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUntypedNamedTuple">reportUntypedNamedTuple</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnusedCallResult">reportUnusedCallResult</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnusedClass">reportUnusedClass</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnusedCoroutine">reportUnusedCoroutine</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnusedExcept">reportUnusedExcept</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnusedExpression">reportUnusedExpression</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnusedFunction">reportUnusedFunction</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnusedImport">reportUnusedImport</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnusedVariable">reportUnusedVariable</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.reportWildcardImportFromLibrary">reportWildcardImportFromLibrary</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.strictDictionaryInference">strictDictionaryInference</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.strictListInference">strictListInference</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.strictParameterNoneValue">strictParameterNoneValue</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.PyrightConfigurationExecutionEnvironments.property.strictSetInference">strictSetInference</a></code> | <code>boolean</code> | *No description.* |

---

##### `root`<sup>Required</sup> <a name="root" id="projen.python.PyrightConfigurationExecutionEnvironments.property.root"></a>

```typescript
public readonly root: string;
```

- *Type:* string

Root path for the code that will execute within this execution environment.

---

##### `analyzeUnannotatedFunctions`<sup>Optional</sup> <a name="analyzeUnannotatedFunctions" id="projen.python.PyrightConfigurationExecutionEnvironments.property.analyzeUnannotatedFunctions"></a>

```typescript
public readonly analyzeUnannotatedFunctions: boolean;
```

- *Type:* boolean

---

##### `deprecateTypingAliases`<sup>Optional</sup> <a name="deprecateTypingAliases" id="projen.python.PyrightConfigurationExecutionEnvironments.property.deprecateTypingAliases"></a>

```typescript
public readonly deprecateTypingAliases: boolean;
```

- *Type:* boolean

---

##### `disableBytesTypePromotions`<sup>Optional</sup> <a name="disableBytesTypePromotions" id="projen.python.PyrightConfigurationExecutionEnvironments.property.disableBytesTypePromotions"></a>

```typescript
public readonly disableBytesTypePromotions: boolean;
```

- *Type:* boolean

---

##### `enableExperimentalFeatures`<sup>Optional</sup> <a name="enableExperimentalFeatures" id="projen.python.PyrightConfigurationExecutionEnvironments.property.enableExperimentalFeatures"></a>

```typescript
public readonly enableExperimentalFeatures: boolean;
```

- *Type:* boolean

---

##### `enableReachabilityAnalysis`<sup>Optional</sup> <a name="enableReachabilityAnalysis" id="projen.python.PyrightConfigurationExecutionEnvironments.property.enableReachabilityAnalysis"></a>

```typescript
public readonly enableReachabilityAnalysis: boolean;
```

- *Type:* boolean

---

##### `enableTypeIgnoreComments`<sup>Optional</sup> <a name="enableTypeIgnoreComments" id="projen.python.PyrightConfigurationExecutionEnvironments.property.enableTypeIgnoreComments"></a>

```typescript
public readonly enableTypeIgnoreComments: boolean;
```

- *Type:* boolean

---

##### `extraPaths`<sup>Optional</sup> <a name="extraPaths" id="projen.python.PyrightConfigurationExecutionEnvironments.property.extraPaths"></a>

```typescript
public readonly extraPaths: string[];
```

- *Type:* string[]

---

##### `pythonPlatform`<sup>Optional</sup> <a name="pythonPlatform" id="projen.python.PyrightConfigurationExecutionEnvironments.property.pythonPlatform"></a>

```typescript
public readonly pythonPlatform: PythonPlatform;
```

- *Type:* <a href="#projen.python.PythonPlatform">PythonPlatform</a>

---

##### `pythonVersion`<sup>Optional</sup> <a name="pythonVersion" id="projen.python.PyrightConfigurationExecutionEnvironments.property.pythonVersion"></a>

```typescript
public readonly pythonVersion: string;
```

- *Type:* string

---

##### `reportAbstractUsage`<sup>Optional</sup> <a name="reportAbstractUsage" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportAbstractUsage"></a>

```typescript
public readonly reportAbstractUsage: any;
```

- *Type:* any

---

##### `reportArgumentType`<sup>Optional</sup> <a name="reportArgumentType" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportArgumentType"></a>

```typescript
public readonly reportArgumentType: any;
```

- *Type:* any

---

##### `reportAssertAlwaysTrue`<sup>Optional</sup> <a name="reportAssertAlwaysTrue" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportAssertAlwaysTrue"></a>

```typescript
public readonly reportAssertAlwaysTrue: any;
```

- *Type:* any

---

##### `reportAssertTypeFailure`<sup>Optional</sup> <a name="reportAssertTypeFailure" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportAssertTypeFailure"></a>

```typescript
public readonly reportAssertTypeFailure: any;
```

- *Type:* any

---

##### `reportAssignmentType`<sup>Optional</sup> <a name="reportAssignmentType" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportAssignmentType"></a>

```typescript
public readonly reportAssignmentType: any;
```

- *Type:* any

---

##### `reportAttributeAccessIssue`<sup>Optional</sup> <a name="reportAttributeAccessIssue" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportAttributeAccessIssue"></a>

```typescript
public readonly reportAttributeAccessIssue: any;
```

- *Type:* any

---

##### `reportCallInDefaultInitializer`<sup>Optional</sup> <a name="reportCallInDefaultInitializer" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportCallInDefaultInitializer"></a>

```typescript
public readonly reportCallInDefaultInitializer: any;
```

- *Type:* any

---

##### `reportCallIssue`<sup>Optional</sup> <a name="reportCallIssue" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportCallIssue"></a>

```typescript
public readonly reportCallIssue: any;
```

- *Type:* any

---

##### `reportConstantRedefinition`<sup>Optional</sup> <a name="reportConstantRedefinition" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportConstantRedefinition"></a>

```typescript
public readonly reportConstantRedefinition: any;
```

- *Type:* any

---

##### `reportDeprecated`<sup>Optional</sup> <a name="reportDeprecated" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportDeprecated"></a>

```typescript
public readonly reportDeprecated: any;
```

- *Type:* any

---

##### `reportDuplicateImport`<sup>Optional</sup> <a name="reportDuplicateImport" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportDuplicateImport"></a>

```typescript
public readonly reportDuplicateImport: any;
```

- *Type:* any

---

##### `reportFunctionMemberAccess`<sup>Optional</sup> <a name="reportFunctionMemberAccess" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportFunctionMemberAccess"></a>

```typescript
public readonly reportFunctionMemberAccess: any;
```

- *Type:* any

---

##### `reportGeneralTypeIssues`<sup>Optional</sup> <a name="reportGeneralTypeIssues" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportGeneralTypeIssues"></a>

```typescript
public readonly reportGeneralTypeIssues: any;
```

- *Type:* any

---

##### `reportImplicitOverride`<sup>Optional</sup> <a name="reportImplicitOverride" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportImplicitOverride"></a>

```typescript
public readonly reportImplicitOverride: any;
```

- *Type:* any

---

##### `reportImplicitStringConcatenation`<sup>Optional</sup> <a name="reportImplicitStringConcatenation" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportImplicitStringConcatenation"></a>

```typescript
public readonly reportImplicitStringConcatenation: any;
```

- *Type:* any

---

##### `reportImportCycles`<sup>Optional</sup> <a name="reportImportCycles" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportImportCycles"></a>

```typescript
public readonly reportImportCycles: any;
```

- *Type:* any

---

##### `reportIncompatibleMethodOverride`<sup>Optional</sup> <a name="reportIncompatibleMethodOverride" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportIncompatibleMethodOverride"></a>

```typescript
public readonly reportIncompatibleMethodOverride: any;
```

- *Type:* any

---

##### `reportIncompatibleVariableOverride`<sup>Optional</sup> <a name="reportIncompatibleVariableOverride" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportIncompatibleVariableOverride"></a>

```typescript
public readonly reportIncompatibleVariableOverride: any;
```

- *Type:* any

---

##### `reportIncompleteStub`<sup>Optional</sup> <a name="reportIncompleteStub" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportIncompleteStub"></a>

```typescript
public readonly reportIncompleteStub: any;
```

- *Type:* any

---

##### `reportInconsistentConstructor`<sup>Optional</sup> <a name="reportInconsistentConstructor" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportInconsistentConstructor"></a>

```typescript
public readonly reportInconsistentConstructor: any;
```

- *Type:* any

---

##### `reportInconsistentOverload`<sup>Optional</sup> <a name="reportInconsistentOverload" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportInconsistentOverload"></a>

```typescript
public readonly reportInconsistentOverload: any;
```

- *Type:* any

---

##### `reportIndexIssue`<sup>Optional</sup> <a name="reportIndexIssue" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportIndexIssue"></a>

```typescript
public readonly reportIndexIssue: any;
```

- *Type:* any

---

##### `reportInvalidStringEscapeSequence`<sup>Optional</sup> <a name="reportInvalidStringEscapeSequence" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportInvalidStringEscapeSequence"></a>

```typescript
public readonly reportInvalidStringEscapeSequence: any;
```

- *Type:* any

---

##### `reportInvalidStubStatement`<sup>Optional</sup> <a name="reportInvalidStubStatement" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportInvalidStubStatement"></a>

```typescript
public readonly reportInvalidStubStatement: any;
```

- *Type:* any

---

##### `reportInvalidTypeArguments`<sup>Optional</sup> <a name="reportInvalidTypeArguments" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportInvalidTypeArguments"></a>

```typescript
public readonly reportInvalidTypeArguments: any;
```

- *Type:* any

---

##### `reportInvalidTypeForm`<sup>Optional</sup> <a name="reportInvalidTypeForm" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportInvalidTypeForm"></a>

```typescript
public readonly reportInvalidTypeForm: any;
```

- *Type:* any

---

##### `reportInvalidTypeVarUse`<sup>Optional</sup> <a name="reportInvalidTypeVarUse" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportInvalidTypeVarUse"></a>

```typescript
public readonly reportInvalidTypeVarUse: any;
```

- *Type:* any

---

##### `reportMatchNotExhaustive`<sup>Optional</sup> <a name="reportMatchNotExhaustive" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportMatchNotExhaustive"></a>

```typescript
public readonly reportMatchNotExhaustive: any;
```

- *Type:* any

---

##### `reportMissingImports`<sup>Optional</sup> <a name="reportMissingImports" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportMissingImports"></a>

```typescript
public readonly reportMissingImports: any;
```

- *Type:* any

---

##### `reportMissingModuleSource`<sup>Optional</sup> <a name="reportMissingModuleSource" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportMissingModuleSource"></a>

```typescript
public readonly reportMissingModuleSource: any;
```

- *Type:* any

---

##### `reportMissingParameterType`<sup>Optional</sup> <a name="reportMissingParameterType" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportMissingParameterType"></a>

```typescript
public readonly reportMissingParameterType: any;
```

- *Type:* any

---

##### `reportMissingSuperCall`<sup>Optional</sup> <a name="reportMissingSuperCall" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportMissingSuperCall"></a>

```typescript
public readonly reportMissingSuperCall: any;
```

- *Type:* any

---

##### `reportMissingTypeArgument`<sup>Optional</sup> <a name="reportMissingTypeArgument" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportMissingTypeArgument"></a>

```typescript
public readonly reportMissingTypeArgument: any;
```

- *Type:* any

---

##### `reportMissingTypeStubs`<sup>Optional</sup> <a name="reportMissingTypeStubs" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportMissingTypeStubs"></a>

```typescript
public readonly reportMissingTypeStubs: any;
```

- *Type:* any

---

##### `reportNoOverloadImplementation`<sup>Optional</sup> <a name="reportNoOverloadImplementation" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportNoOverloadImplementation"></a>

```typescript
public readonly reportNoOverloadImplementation: any;
```

- *Type:* any

---

##### `reportOperatorIssue`<sup>Optional</sup> <a name="reportOperatorIssue" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportOperatorIssue"></a>

```typescript
public readonly reportOperatorIssue: any;
```

- *Type:* any

---

##### `reportOptionalCall`<sup>Optional</sup> <a name="reportOptionalCall" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportOptionalCall"></a>

```typescript
public readonly reportOptionalCall: any;
```

- *Type:* any

---

##### `reportOptionalContextManager`<sup>Optional</sup> <a name="reportOptionalContextManager" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportOptionalContextManager"></a>

```typescript
public readonly reportOptionalContextManager: any;
```

- *Type:* any

---

##### `reportOptionalIterable`<sup>Optional</sup> <a name="reportOptionalIterable" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportOptionalIterable"></a>

```typescript
public readonly reportOptionalIterable: any;
```

- *Type:* any

---

##### `reportOptionalMemberAccess`<sup>Optional</sup> <a name="reportOptionalMemberAccess" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportOptionalMemberAccess"></a>

```typescript
public readonly reportOptionalMemberAccess: any;
```

- *Type:* any

---

##### `reportOptionalOperand`<sup>Optional</sup> <a name="reportOptionalOperand" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportOptionalOperand"></a>

```typescript
public readonly reportOptionalOperand: any;
```

- *Type:* any

---

##### `reportOptionalSubscript`<sup>Optional</sup> <a name="reportOptionalSubscript" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportOptionalSubscript"></a>

```typescript
public readonly reportOptionalSubscript: any;
```

- *Type:* any

---

##### `reportOverlappingOverload`<sup>Optional</sup> <a name="reportOverlappingOverload" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportOverlappingOverload"></a>

```typescript
public readonly reportOverlappingOverload: any;
```

- *Type:* any

---

##### `reportPossiblyUnboundVariable`<sup>Optional</sup> <a name="reportPossiblyUnboundVariable" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportPossiblyUnboundVariable"></a>

```typescript
public readonly reportPossiblyUnboundVariable: any;
```

- *Type:* any

---

##### `reportPrivateImportUsage`<sup>Optional</sup> <a name="reportPrivateImportUsage" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportPrivateImportUsage"></a>

```typescript
public readonly reportPrivateImportUsage: any;
```

- *Type:* any

---

##### `reportPrivateUsage`<sup>Optional</sup> <a name="reportPrivateUsage" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportPrivateUsage"></a>

```typescript
public readonly reportPrivateUsage: any;
```

- *Type:* any

---

##### `reportPropertyTypeMismatch`<sup>Optional</sup> <a name="reportPropertyTypeMismatch" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportPropertyTypeMismatch"></a>

```typescript
public readonly reportPropertyTypeMismatch: any;
```

- *Type:* any

---

##### `reportRedeclaration`<sup>Optional</sup> <a name="reportRedeclaration" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportRedeclaration"></a>

```typescript
public readonly reportRedeclaration: any;
```

- *Type:* any

---

##### `reportReturnType`<sup>Optional</sup> <a name="reportReturnType" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportReturnType"></a>

```typescript
public readonly reportReturnType: any;
```

- *Type:* any

---

##### `reportSelfClsParameterName`<sup>Optional</sup> <a name="reportSelfClsParameterName" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportSelfClsParameterName"></a>

```typescript
public readonly reportSelfClsParameterName: any;
```

- *Type:* any

---

##### `reportTypeCommentUsage`<sup>Optional</sup> <a name="reportTypeCommentUsage" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportTypeCommentUsage"></a>

```typescript
public readonly reportTypeCommentUsage: any;
```

- *Type:* any

---

##### `reportTypedDictNotRequiredAccess`<sup>Optional</sup> <a name="reportTypedDictNotRequiredAccess" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportTypedDictNotRequiredAccess"></a>

```typescript
public readonly reportTypedDictNotRequiredAccess: any;
```

- *Type:* any

---

##### `reportUnboundVariable`<sup>Optional</sup> <a name="reportUnboundVariable" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnboundVariable"></a>

```typescript
public readonly reportUnboundVariable: any;
```

- *Type:* any

---

##### `reportUndefinedVariable`<sup>Optional</sup> <a name="reportUndefinedVariable" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUndefinedVariable"></a>

```typescript
public readonly reportUndefinedVariable: any;
```

- *Type:* any

---

##### `reportUnhashable`<sup>Optional</sup> <a name="reportUnhashable" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnhashable"></a>

```typescript
public readonly reportUnhashable: any;
```

- *Type:* any

---

##### `reportUninitializedInstanceVariable`<sup>Optional</sup> <a name="reportUninitializedInstanceVariable" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUninitializedInstanceVariable"></a>

```typescript
public readonly reportUninitializedInstanceVariable: any;
```

- *Type:* any

---

##### `reportUnknownArgumentType`<sup>Optional</sup> <a name="reportUnknownArgumentType" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnknownArgumentType"></a>

```typescript
public readonly reportUnknownArgumentType: any;
```

- *Type:* any

---

##### `reportUnknownLambdaType`<sup>Optional</sup> <a name="reportUnknownLambdaType" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnknownLambdaType"></a>

```typescript
public readonly reportUnknownLambdaType: any;
```

- *Type:* any

---

##### `reportUnknownMemberType`<sup>Optional</sup> <a name="reportUnknownMemberType" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnknownMemberType"></a>

```typescript
public readonly reportUnknownMemberType: any;
```

- *Type:* any

---

##### `reportUnknownParameterType`<sup>Optional</sup> <a name="reportUnknownParameterType" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnknownParameterType"></a>

```typescript
public readonly reportUnknownParameterType: any;
```

- *Type:* any

---

##### `reportUnknownVariableType`<sup>Optional</sup> <a name="reportUnknownVariableType" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnknownVariableType"></a>

```typescript
public readonly reportUnknownVariableType: any;
```

- *Type:* any

---

##### `reportUnnecessaryCast`<sup>Optional</sup> <a name="reportUnnecessaryCast" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnnecessaryCast"></a>

```typescript
public readonly reportUnnecessaryCast: any;
```

- *Type:* any

---

##### `reportUnnecessaryComparison`<sup>Optional</sup> <a name="reportUnnecessaryComparison" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnnecessaryComparison"></a>

```typescript
public readonly reportUnnecessaryComparison: any;
```

- *Type:* any

---

##### `reportUnnecessaryContains`<sup>Optional</sup> <a name="reportUnnecessaryContains" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnnecessaryContains"></a>

```typescript
public readonly reportUnnecessaryContains: any;
```

- *Type:* any

---

##### `reportUnnecessaryIsInstance`<sup>Optional</sup> <a name="reportUnnecessaryIsInstance" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnnecessaryIsInstance"></a>

```typescript
public readonly reportUnnecessaryIsInstance: any;
```

- *Type:* any

---

##### `reportUnnecessaryTypeIgnoreComment`<sup>Optional</sup> <a name="reportUnnecessaryTypeIgnoreComment" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnnecessaryTypeIgnoreComment"></a>

```typescript
public readonly reportUnnecessaryTypeIgnoreComment: any;
```

- *Type:* any

---

##### `reportUnreachable`<sup>Optional</sup> <a name="reportUnreachable" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnreachable"></a>

```typescript
public readonly reportUnreachable: any;
```

- *Type:* any

---

##### `reportUnsupportedDunderAll`<sup>Optional</sup> <a name="reportUnsupportedDunderAll" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnsupportedDunderAll"></a>

```typescript
public readonly reportUnsupportedDunderAll: any;
```

- *Type:* any

---

##### `reportUntypedBaseClass`<sup>Optional</sup> <a name="reportUntypedBaseClass" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUntypedBaseClass"></a>

```typescript
public readonly reportUntypedBaseClass: any;
```

- *Type:* any

---

##### `reportUntypedClassDecorator`<sup>Optional</sup> <a name="reportUntypedClassDecorator" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUntypedClassDecorator"></a>

```typescript
public readonly reportUntypedClassDecorator: any;
```

- *Type:* any

---

##### `reportUntypedFunctionDecorator`<sup>Optional</sup> <a name="reportUntypedFunctionDecorator" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUntypedFunctionDecorator"></a>

```typescript
public readonly reportUntypedFunctionDecorator: any;
```

- *Type:* any

---

##### `reportUntypedNamedTuple`<sup>Optional</sup> <a name="reportUntypedNamedTuple" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUntypedNamedTuple"></a>

```typescript
public readonly reportUntypedNamedTuple: any;
```

- *Type:* any

---

##### `reportUnusedCallResult`<sup>Optional</sup> <a name="reportUnusedCallResult" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnusedCallResult"></a>

```typescript
public readonly reportUnusedCallResult: any;
```

- *Type:* any

---

##### `reportUnusedClass`<sup>Optional</sup> <a name="reportUnusedClass" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnusedClass"></a>

```typescript
public readonly reportUnusedClass: any;
```

- *Type:* any

---

##### `reportUnusedCoroutine`<sup>Optional</sup> <a name="reportUnusedCoroutine" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnusedCoroutine"></a>

```typescript
public readonly reportUnusedCoroutine: any;
```

- *Type:* any

---

##### `reportUnusedExcept`<sup>Optional</sup> <a name="reportUnusedExcept" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnusedExcept"></a>

```typescript
public readonly reportUnusedExcept: any;
```

- *Type:* any

---

##### `reportUnusedExpression`<sup>Optional</sup> <a name="reportUnusedExpression" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnusedExpression"></a>

```typescript
public readonly reportUnusedExpression: any;
```

- *Type:* any

---

##### `reportUnusedFunction`<sup>Optional</sup> <a name="reportUnusedFunction" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnusedFunction"></a>

```typescript
public readonly reportUnusedFunction: any;
```

- *Type:* any

---

##### `reportUnusedImport`<sup>Optional</sup> <a name="reportUnusedImport" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnusedImport"></a>

```typescript
public readonly reportUnusedImport: any;
```

- *Type:* any

---

##### `reportUnusedVariable`<sup>Optional</sup> <a name="reportUnusedVariable" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportUnusedVariable"></a>

```typescript
public readonly reportUnusedVariable: any;
```

- *Type:* any

---

##### `reportWildcardImportFromLibrary`<sup>Optional</sup> <a name="reportWildcardImportFromLibrary" id="projen.python.PyrightConfigurationExecutionEnvironments.property.reportWildcardImportFromLibrary"></a>

```typescript
public readonly reportWildcardImportFromLibrary: any;
```

- *Type:* any

---

##### `strictDictionaryInference`<sup>Optional</sup> <a name="strictDictionaryInference" id="projen.python.PyrightConfigurationExecutionEnvironments.property.strictDictionaryInference"></a>

```typescript
public readonly strictDictionaryInference: boolean;
```

- *Type:* boolean

---

##### `strictListInference`<sup>Optional</sup> <a name="strictListInference" id="projen.python.PyrightConfigurationExecutionEnvironments.property.strictListInference"></a>

```typescript
public readonly strictListInference: boolean;
```

- *Type:* boolean

---

##### `strictParameterNoneValue`<sup>Optional</sup> <a name="strictParameterNoneValue" id="projen.python.PyrightConfigurationExecutionEnvironments.property.strictParameterNoneValue"></a>

```typescript
public readonly strictParameterNoneValue: boolean;
```

- *Type:* boolean

---

##### `strictSetInference`<sup>Optional</sup> <a name="strictSetInference" id="projen.python.PyrightConfigurationExecutionEnvironments.property.strictSetInference"></a>

```typescript
public readonly strictSetInference: boolean;
```

- *Type:* boolean

---

### PytestConfiguration <a name="PytestConfiguration" id="projen.python.PytestConfiguration"></a>

JSON schema for pytest configuration options under `[tool.pytest]` in `pyproject.toml`.

#### Initializer <a name="Initializer" id="projen.python.PytestConfiguration.Initializer"></a>

```typescript
import { python } from 'projen'

const pytestConfiguration: python.PytestConfiguration = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PytestConfiguration.property.addopts">addopts</a></code> | <code>string[]</code> | Add the specified OPTS to the set of command line arguments as if they had been specified by the user. |
| <code><a href="#projen.python.PytestConfiguration.property.cacheDir">cacheDir</a></code> | <code>string</code> | Sets the directory where the cache plugin’s content is stored. |
| <code><a href="#projen.python.PytestConfiguration.property.collectImportedTests">collectImportedTests</a></code> | <code>boolean</code> | Settings this to false will make pytest collect classes/functions from test files _only_ if they are defined in that file (as opposed to imported there). |
| <code><a href="#projen.python.PytestConfiguration.property.considerNamespacePackages">considerNamespacePackages</a></code> | <code>boolean</code> | Controls if pytest should attempt to identify namespace packages when collecting Python modules. |
| <code><a href="#projen.python.PytestConfiguration.property.consoleOutputStyle">consoleOutputStyle</a></code> | <code><a href="#projen.python.PytestConfigurationConsoleOutputStyle">PytestConfigurationConsoleOutputStyle</a></code> | Sets console output style during test execution. |
| <code><a href="#projen.python.PytestConfiguration.property.disableTestIdEscapingAndForfeitAllRightsToCommunitySupport">disableTestIdEscapingAndForfeitAllRightsToCommunitySupport</a></code> | <code>boolean</code> | pytest by default escapes any non-ascii characters used in unicode strings for the parametrization because it has several downsides. |
| <code><a href="#projen.python.PytestConfiguration.property.doctestEncoding">doctestEncoding</a></code> | <code>string</code> | Default encoding to use to decode text files with docstrings. |
| <code><a href="#projen.python.PytestConfiguration.property.doctestOptionflags">doctestOptionflags</a></code> | <code>string[]</code> | One or more doctest flag names from the standard doctest module. |
| <code><a href="#projen.python.PytestConfiguration.property.emptyParameterSetMark">emptyParameterSetMark</a></code> | <code><a href="#projen.python.PytestConfigurationEmptyParameterSetMark">PytestConfigurationEmptyParameterSetMark</a></code> | Allows to pick the action for empty parametersets in parameterization. |
| <code><a href="#projen.python.PytestConfiguration.property.enableAssertionPassHook">enableAssertionPassHook</a></code> | <code>boolean</code> | Enables the pytest_assertion_pass hook. |
| <code><a href="#projen.python.PytestConfiguration.property.faulthandlerExitOnTimeout">faulthandlerExitOnTimeout</a></code> | <code>boolean</code> | Exit the pytest process after the per-test timeout is reached by passing exit=True to the faulthandler.dump_traceback_later() function. This is particularly useful to avoid wasting CI resources for test suites that are prone to putting the main Python interpreter into a deadlock state. |
| <code><a href="#projen.python.PytestConfiguration.property.faulthandlerTimeout">faulthandlerTimeout</a></code> | <code>number</code> | Dumps the tracebacks of all threads if a test takes longer than X seconds to run (including fixture setup and teardown). |
| <code><a href="#projen.python.PytestConfiguration.property.filterwarnings">filterwarnings</a></code> | <code>string[]</code> | Sets a list of filters and actions that should be taken for matched warnings. |
| <code><a href="#projen.python.PytestConfiguration.property.junitDurationReport">junitDurationReport</a></code> | <code><a href="#projen.python.PytestConfigurationJunitDurationReport">PytestConfigurationJunitDurationReport</a></code> | Configures how durations are recorded into the JUnit XML report:. |
| <code><a href="#projen.python.PytestConfiguration.property.junitFamily">junitFamily</a></code> | <code><a href="#projen.python.PytestConfigurationJunitFamily">PytestConfigurationJunitFamily</a></code> | Configures the format of the generated JUnit XML file. |
| <code><a href="#projen.python.PytestConfiguration.property.junitLogging">junitLogging</a></code> | <code><a href="#projen.python.PytestConfigurationJunitLogging">PytestConfigurationJunitLogging</a></code> | Configures if captured output should be written to the JUnit XML file. |
| <code><a href="#projen.python.PytestConfiguration.property.junitLogPassingTests">junitLogPassingTests</a></code> | <code>boolean</code> | If junit_logging is enabled, configures if the captured output should be written to the JUnit XML file for passing tests. |
| <code><a href="#projen.python.PytestConfiguration.property.junitSuiteName">junitSuiteName</a></code> | <code>string</code> | To set the name of the root test suite xml item, you can configure the junit_suite_name option in your config file. |
| <code><a href="#projen.python.PytestConfiguration.property.logAutoIndent">logAutoIndent</a></code> | <code><a href="#projen.python.PytestConfigurationLogAutoIndent">PytestConfigurationLogAutoIndent</a></code> | Allow selective auto-indentation of multiline log messages. |
| <code><a href="#projen.python.PytestConfiguration.property.logCli">logCli</a></code> | <code>boolean</code> | Enable log display during test run (also known as “live logging”). |
| <code><a href="#projen.python.PytestConfiguration.property.logCliDateFormat">logCliDateFormat</a></code> | <code>string</code> | Sets a time.strftime()-compatible string that will be used when formatting dates for live logging. |
| <code><a href="#projen.python.PytestConfiguration.property.logCliFormat">logCliFormat</a></code> | <code>string</code> | Sets a logging-compatible string used to format live logging messages. |
| <code><a href="#projen.python.PytestConfiguration.property.logCliLevel">logCliLevel</a></code> | <code><a href="#projen.python.LogLevel">LogLevel</a></code> | Sets the minimum log message level that should be captured for live logging. |
| <code><a href="#projen.python.PytestConfiguration.property.logDateFormat">logDateFormat</a></code> | <code>string</code> | Sets a time.strftime()-compatible string that will be used when formatting dates for logging capture. |
| <code><a href="#projen.python.PytestConfiguration.property.logFile">logFile</a></code> | <code>string</code> | Sets a file name relative to the current working directory where log messages should be written to, in addition to the other logging facilities that are active. |
| <code><a href="#projen.python.PytestConfiguration.property.logFileDateFormat">logFileDateFormat</a></code> | <code>string</code> | Sets a time.strftime()-compatible string that will be used when formatting dates for the logging file. |
| <code><a href="#projen.python.PytestConfiguration.property.logFileFormat">logFileFormat</a></code> | <code>string</code> | Sets a logging-compatible string used to format logging messages redirected to the logging file. |
| <code><a href="#projen.python.PytestConfiguration.property.logFileLevel">logFileLevel</a></code> | <code><a href="#projen.python.LogLevel">LogLevel</a></code> | Sets the minimum log message level that should be captured for the logging file. |
| <code><a href="#projen.python.PytestConfiguration.property.logFileMode">logFileMode</a></code> | <code><a href="#projen.python.PytestConfigurationLogFileMode">PytestConfigurationLogFileMode</a></code> | Sets the mode that the logging file is opened with. |
| <code><a href="#projen.python.PytestConfiguration.property.logFormat">logFormat</a></code> | <code>string</code> | Sets a logging-compatible string used to format captured logging messages. |
| <code><a href="#projen.python.PytestConfiguration.property.logLevel">logLevel</a></code> | <code><a href="#projen.python.LogLevel">LogLevel</a></code> | Sets the minimum log message level that should be captured for logging capture. |
| <code><a href="#projen.python.PytestConfiguration.property.markers">markers</a></code> | <code>string[]</code> | When the strict_markers configuration option is set, only known markers - defined in code by core pytest or some plugin - are allowed. |
| <code><a href="#projen.python.PytestConfiguration.property.minversion">minversion</a></code> | <code>string</code> | Specifies a minimal pytest version required for running tests. |
| <code><a href="#projen.python.PytestConfiguration.property.norecursedirs">norecursedirs</a></code> | <code>string[]</code> | Set the directory basename patterns to avoid when recursing for test discovery. |
| <code><a href="#projen.python.PytestConfiguration.property.pythonClasses">pythonClasses</a></code> | <code>string[]</code> | One or more name prefixes or glob-style patterns determining which classes are considered for test collection. |
| <code><a href="#projen.python.PytestConfiguration.property.pythonFiles">pythonFiles</a></code> | <code>string[]</code> | One or more Glob-style file patterns determining which python files are considered as test modules. |
| <code><a href="#projen.python.PytestConfiguration.property.pythonFunctions">pythonFunctions</a></code> | <code>string[]</code> | One or more name prefixes or glob-patterns determining which test functions and methods are considered tests. |
| <code><a href="#projen.python.PytestConfiguration.property.pythonpath">pythonpath</a></code> | <code>string[]</code> | Sets list of directories that should be added to the python search path. |
| <code><a href="#projen.python.PytestConfiguration.property.requiredPlugins">requiredPlugins</a></code> | <code>string[]</code> | A list of plugins that must be present for pytest to run. |
| <code><a href="#projen.python.PytestConfiguration.property.strict">strict</a></code> | <code>boolean</code> | If set to true, enable 'strict mode', which enables a number of other strict options. |
| <code><a href="#projen.python.PytestConfiguration.property.strictConfig">strictConfig</a></code> | <code>boolean</code> | If set to true, any warnings encountered while parsing the pytest section of the configuration file will raise errors. |
| <code><a href="#projen.python.PytestConfiguration.property.strictMarkers">strictMarkers</a></code> | <code>boolean</code> | If set to true, markers not registered in the markers section of the configuration file will raise errors. |
| <code><a href="#projen.python.PytestConfiguration.property.strictParametrizationIds">strictParametrizationIds</a></code> | <code>boolean</code> | If set to true, pytest emits an error if it detects non-unique parameter set IDs. |
| <code><a href="#projen.python.PytestConfiguration.property.strictXfail">strictXfail</a></code> | <code>boolean</code> | If set to true, tests marked with. |
| <code><a href="#projen.python.PytestConfiguration.property.testpaths">testpaths</a></code> | <code>string[]</code> | Sets list of directories that should be searched for tests when no specific directories, files or test ids are given in the command line when executing pytest from the rootdir directory. |
| <code><a href="#projen.python.PytestConfiguration.property.tmpPathRetentionCount">tmpPathRetentionCount</a></code> | <code>number</code> | How many sessions should we keep the tmp_path directories, according to tmp_path_retention_policy. |
| <code><a href="#projen.python.PytestConfiguration.property.tmpPathRetentionPolicy">tmpPathRetentionPolicy</a></code> | <code><a href="#projen.python.PytestConfigurationTmpPathRetentionPolicy">PytestConfigurationTmpPathRetentionPolicy</a></code> | Controls which directories created by the tmp_path fixture are kept around, based on test outcome. |
| <code><a href="#projen.python.PytestConfiguration.property.truncationLimitChars">truncationLimitChars</a></code> | <code>number</code> | Controls maximum number of characters to truncate assertion message contents. |
| <code><a href="#projen.python.PytestConfiguration.property.truncationLimitLines">truncationLimitLines</a></code> | <code>number</code> | Controls maximum number of linesto truncate assertion message contents. |
| <code><a href="#projen.python.PytestConfiguration.property.usefixtures">usefixtures</a></code> | <code>string[]</code> | List of fixtures that will be applied to all test functions; |
| <code><a href="#projen.python.PytestConfiguration.property.verbosityAssertions">verbosityAssertions</a></code> | <code><a href="#projen.python.PytestConfigurationVerbosityAssertions">PytestConfigurationVerbosityAssertions</a></code> | Set a verbosity level specifically for assertion related output, overriding the application wide level. |
| <code><a href="#projen.python.PytestConfiguration.property.verbositySubtests">verbositySubtests</a></code> | <code><a href="#projen.python.PytestConfigurationVerbositySubtests">PytestConfigurationVerbositySubtests</a></code> | Set the verbosity level specifically for passed subtests. |
| <code><a href="#projen.python.PytestConfiguration.property.verbosityTestCases">verbosityTestCases</a></code> | <code><a href="#projen.python.PytestConfigurationVerbosityTestCases">PytestConfigurationVerbosityTestCases</a></code> | Set a verbosity level specifically for test case execution related output, overriding the application wide level. |

---

##### `addopts`<sup>Optional</sup> <a name="addopts" id="projen.python.PytestConfiguration.property.addopts"></a>

```typescript
public readonly addopts: string[];
```

- *Type:* string[]

Add the specified OPTS to the set of command line arguments as if they had been specified by the user.

---

##### `cacheDir`<sup>Optional</sup> <a name="cacheDir" id="projen.python.PytestConfiguration.property.cacheDir"></a>

```typescript
public readonly cacheDir: string;
```

- *Type:* string

Sets the directory where the cache plugin’s content is stored.

---

##### `collectImportedTests`<sup>Optional</sup> <a name="collectImportedTests" id="projen.python.PytestConfiguration.property.collectImportedTests"></a>

```typescript
public readonly collectImportedTests: boolean;
```

- *Type:* boolean

Settings this to false will make pytest collect classes/functions from test files _only_ if they are defined in that file (as opposed to imported there).

---

##### `considerNamespacePackages`<sup>Optional</sup> <a name="considerNamespacePackages" id="projen.python.PytestConfiguration.property.considerNamespacePackages"></a>

```typescript
public readonly considerNamespacePackages: boolean;
```

- *Type:* boolean

Controls if pytest should attempt to identify namespace packages when collecting Python modules.

---

##### `consoleOutputStyle`<sup>Optional</sup> <a name="consoleOutputStyle" id="projen.python.PytestConfiguration.property.consoleOutputStyle"></a>

```typescript
public readonly consoleOutputStyle: PytestConfigurationConsoleOutputStyle;
```

- *Type:* <a href="#projen.python.PytestConfigurationConsoleOutputStyle">PytestConfigurationConsoleOutputStyle</a>

Sets console output style during test execution.

---

##### `disableTestIdEscapingAndForfeitAllRightsToCommunitySupport`<sup>Optional</sup> <a name="disableTestIdEscapingAndForfeitAllRightsToCommunitySupport" id="projen.python.PytestConfiguration.property.disableTestIdEscapingAndForfeitAllRightsToCommunitySupport"></a>

```typescript
public readonly disableTestIdEscapingAndForfeitAllRightsToCommunitySupport: boolean;
```

- *Type:* boolean

pytest by default escapes any non-ascii characters used in unicode strings for the parametrization because it has several downsides.

If however you would like to use unicode strings in parametrization and see them in the terminal as is (non-escaped), use this option in your configuration file

---

##### `doctestEncoding`<sup>Optional</sup> <a name="doctestEncoding" id="projen.python.PytestConfiguration.property.doctestEncoding"></a>

```typescript
public readonly doctestEncoding: string;
```

- *Type:* string

Default encoding to use to decode text files with docstrings.

---

##### `doctestOptionflags`<sup>Optional</sup> <a name="doctestOptionflags" id="projen.python.PytestConfiguration.property.doctestOptionflags"></a>

```typescript
public readonly doctestOptionflags: string[];
```

- *Type:* string[]

One or more doctest flag names from the standard doctest module.

---

##### `emptyParameterSetMark`<sup>Optional</sup> <a name="emptyParameterSetMark" id="projen.python.PytestConfiguration.property.emptyParameterSetMark"></a>

```typescript
public readonly emptyParameterSetMark: PytestConfigurationEmptyParameterSetMark;
```

- *Type:* <a href="#projen.python.PytestConfigurationEmptyParameterSetMark">PytestConfigurationEmptyParameterSetMark</a>

Allows to pick the action for empty parametersets in parameterization.

---

##### `enableAssertionPassHook`<sup>Optional</sup> <a name="enableAssertionPassHook" id="projen.python.PytestConfiguration.property.enableAssertionPassHook"></a>

```typescript
public readonly enableAssertionPassHook: boolean;
```

- *Type:* boolean

Enables the pytest_assertion_pass hook.

Make sure to delete any previously generated .pyc cache files.

---

##### `faulthandlerExitOnTimeout`<sup>Optional</sup> <a name="faulthandlerExitOnTimeout" id="projen.python.PytestConfiguration.property.faulthandlerExitOnTimeout"></a>

```typescript
public readonly faulthandlerExitOnTimeout: boolean;
```

- *Type:* boolean

Exit the pytest process after the per-test timeout is reached by passing exit=True to the faulthandler.dump_traceback_later() function. This is particularly useful to avoid wasting CI resources for test suites that are prone to putting the main Python interpreter into a deadlock state.

---

##### `faulthandlerTimeout`<sup>Optional</sup> <a name="faulthandlerTimeout" id="projen.python.PytestConfiguration.property.faulthandlerTimeout"></a>

```typescript
public readonly faulthandlerTimeout: number;
```

- *Type:* number

Dumps the tracebacks of all threads if a test takes longer than X seconds to run (including fixture setup and teardown).

Implemented using the faulthandler.dump_traceback_later() function, so all caveats there apply.

---

##### `filterwarnings`<sup>Optional</sup> <a name="filterwarnings" id="projen.python.PytestConfiguration.property.filterwarnings"></a>

```typescript
public readonly filterwarnings: string[];
```

- *Type:* string[]

Sets a list of filters and actions that should be taken for matched warnings.

By default all warnings emitted during the test session will be displayed in a summary at the end of the test session.

---

##### `junitDurationReport`<sup>Optional</sup> <a name="junitDurationReport" id="projen.python.PytestConfiguration.property.junitDurationReport"></a>

```typescript
public readonly junitDurationReport: PytestConfigurationJunitDurationReport;
```

- *Type:* <a href="#projen.python.PytestConfigurationJunitDurationReport">PytestConfigurationJunitDurationReport</a>

Configures how durations are recorded into the JUnit XML report:.

---

##### `junitFamily`<sup>Optional</sup> <a name="junitFamily" id="projen.python.PytestConfiguration.property.junitFamily"></a>

```typescript
public readonly junitFamily: PytestConfigurationJunitFamily;
```

- *Type:* <a href="#projen.python.PytestConfigurationJunitFamily">PytestConfigurationJunitFamily</a>

Configures the format of the generated JUnit XML file.

---

##### `junitLogging`<sup>Optional</sup> <a name="junitLogging" id="projen.python.PytestConfiguration.property.junitLogging"></a>

```typescript
public readonly junitLogging: PytestConfigurationJunitLogging;
```

- *Type:* <a href="#projen.python.PytestConfigurationJunitLogging">PytestConfigurationJunitLogging</a>

Configures if captured output should be written to the JUnit XML file.

---

##### `junitLogPassingTests`<sup>Optional</sup> <a name="junitLogPassingTests" id="projen.python.PytestConfiguration.property.junitLogPassingTests"></a>

```typescript
public readonly junitLogPassingTests: boolean;
```

- *Type:* boolean

If junit_logging is enabled, configures if the captured output should be written to the JUnit XML file for passing tests.

---

##### `junitSuiteName`<sup>Optional</sup> <a name="junitSuiteName" id="projen.python.PytestConfiguration.property.junitSuiteName"></a>

```typescript
public readonly junitSuiteName: string;
```

- *Type:* string

To set the name of the root test suite xml item, you can configure the junit_suite_name option in your config file.

---

##### `logAutoIndent`<sup>Optional</sup> <a name="logAutoIndent" id="projen.python.PytestConfiguration.property.logAutoIndent"></a>

```typescript
public readonly logAutoIndent: PytestConfigurationLogAutoIndent;
```

- *Type:* <a href="#projen.python.PytestConfigurationLogAutoIndent">PytestConfigurationLogAutoIndent</a>

Allow selective auto-indentation of multiline log messages.

---

##### `logCli`<sup>Optional</sup> <a name="logCli" id="projen.python.PytestConfiguration.property.logCli"></a>

```typescript
public readonly logCli: boolean;
```

- *Type:* boolean

Enable log display during test run (also known as “live logging”).

---

##### `logCliDateFormat`<sup>Optional</sup> <a name="logCliDateFormat" id="projen.python.PytestConfiguration.property.logCliDateFormat"></a>

```typescript
public readonly logCliDateFormat: string;
```

- *Type:* string

Sets a time.strftime()-compatible string that will be used when formatting dates for live logging.

---

##### `logCliFormat`<sup>Optional</sup> <a name="logCliFormat" id="projen.python.PytestConfiguration.property.logCliFormat"></a>

```typescript
public readonly logCliFormat: string;
```

- *Type:* string

Sets a logging-compatible string used to format live logging messages.

---

##### `logCliLevel`<sup>Optional</sup> <a name="logCliLevel" id="projen.python.PytestConfiguration.property.logCliLevel"></a>

```typescript
public readonly logCliLevel: LogLevel;
```

- *Type:* <a href="#projen.python.LogLevel">LogLevel</a>

Sets the minimum log message level that should be captured for live logging.

The integer value or the names of the levels can be used.

---

##### `logDateFormat`<sup>Optional</sup> <a name="logDateFormat" id="projen.python.PytestConfiguration.property.logDateFormat"></a>

```typescript
public readonly logDateFormat: string;
```

- *Type:* string

Sets a time.strftime()-compatible string that will be used when formatting dates for logging capture.

---

##### `logFile`<sup>Optional</sup> <a name="logFile" id="projen.python.PytestConfiguration.property.logFile"></a>

```typescript
public readonly logFile: string;
```

- *Type:* string

Sets a file name relative to the current working directory where log messages should be written to, in addition to the other logging facilities that are active.

---

##### `logFileDateFormat`<sup>Optional</sup> <a name="logFileDateFormat" id="projen.python.PytestConfiguration.property.logFileDateFormat"></a>

```typescript
public readonly logFileDateFormat: string;
```

- *Type:* string

Sets a time.strftime()-compatible string that will be used when formatting dates for the logging file.

---

##### `logFileFormat`<sup>Optional</sup> <a name="logFileFormat" id="projen.python.PytestConfiguration.property.logFileFormat"></a>

```typescript
public readonly logFileFormat: string;
```

- *Type:* string

Sets a logging-compatible string used to format logging messages redirected to the logging file.

---

##### `logFileLevel`<sup>Optional</sup> <a name="logFileLevel" id="projen.python.PytestConfiguration.property.logFileLevel"></a>

```typescript
public readonly logFileLevel: LogLevel;
```

- *Type:* <a href="#projen.python.LogLevel">LogLevel</a>

Sets the minimum log message level that should be captured for the logging file.

The integer value or the names of the levels can be used.

---

##### `logFileMode`<sup>Optional</sup> <a name="logFileMode" id="projen.python.PytestConfiguration.property.logFileMode"></a>

```typescript
public readonly logFileMode: PytestConfigurationLogFileMode;
```

- *Type:* <a href="#projen.python.PytestConfigurationLogFileMode">PytestConfigurationLogFileMode</a>

Sets the mode that the logging file is opened with.

The options are 'w' to recreate the file (the default) or 'a' to append to the file.

---

##### `logFormat`<sup>Optional</sup> <a name="logFormat" id="projen.python.PytestConfiguration.property.logFormat"></a>

```typescript
public readonly logFormat: string;
```

- *Type:* string

Sets a logging-compatible string used to format captured logging messages.

---

##### `logLevel`<sup>Optional</sup> <a name="logLevel" id="projen.python.PytestConfiguration.property.logLevel"></a>

```typescript
public readonly logLevel: LogLevel;
```

- *Type:* <a href="#projen.python.LogLevel">LogLevel</a>

Sets the minimum log message level that should be captured for logging capture.

The integer value or the names of the levels can be used.

---

##### `markers`<sup>Optional</sup> <a name="markers" id="projen.python.PytestConfiguration.property.markers"></a>

```typescript
public readonly markers: string[];
```

- *Type:* string[]

When the strict_markers configuration option is set, only known markers - defined in code by core pytest or some plugin - are allowed.

You can list additional markers in this setting to add them to the whitelist, in which case you probably want to set strict_markers to true to avoid future regressions.

---

##### `minversion`<sup>Optional</sup> <a name="minversion" id="projen.python.PytestConfiguration.property.minversion"></a>

```typescript
public readonly minversion: string;
```

- *Type:* string

Specifies a minimal pytest version required for running tests.

---

##### `norecursedirs`<sup>Optional</sup> <a name="norecursedirs" id="projen.python.PytestConfiguration.property.norecursedirs"></a>

```typescript
public readonly norecursedirs: string[];
```

- *Type:* string[]

Set the directory basename patterns to avoid when recursing for test discovery.

The individual (fnmatch-style) patterns are applied to the basename of a directory to decide if to recurse into it.

---

##### `pythonClasses`<sup>Optional</sup> <a name="pythonClasses" id="projen.python.PytestConfiguration.property.pythonClasses"></a>

```typescript
public readonly pythonClasses: string[];
```

- *Type:* string[]

One or more name prefixes or glob-style patterns determining which classes are considered for test collection.

Search for multiple glob patterns by adding a space between patterns. By default, pytest will consider any class prefixed with Test as a test collection.

---

##### `pythonFiles`<sup>Optional</sup> <a name="pythonFiles" id="projen.python.PytestConfiguration.property.pythonFiles"></a>

```typescript
public readonly pythonFiles: string[];
```

- *Type:* string[]

One or more Glob-style file patterns determining which python files are considered as test modules.

---

##### `pythonFunctions`<sup>Optional</sup> <a name="pythonFunctions" id="projen.python.PytestConfiguration.property.pythonFunctions"></a>

```typescript
public readonly pythonFunctions: string[];
```

- *Type:* string[]

One or more name prefixes or glob-patterns determining which test functions and methods are considered tests.

Search for multiple glob patterns by adding a space between patterns. By default, pytest will consider any function prefixed with test as a test.

---

##### `pythonpath`<sup>Optional</sup> <a name="pythonpath" id="projen.python.PytestConfiguration.property.pythonpath"></a>

```typescript
public readonly pythonpath: string[];
```

- *Type:* string[]

Sets list of directories that should be added to the python search path.

Directories will be added to the head of sys.path. Similar to the PYTHONPATH environment variable, the directories will be included in where Python will look for imported modules. Paths are relative to the rootdir directory.

---

##### `requiredPlugins`<sup>Optional</sup> <a name="requiredPlugins" id="projen.python.PytestConfiguration.property.requiredPlugins"></a>

```typescript
public readonly requiredPlugins: string[];
```

- *Type:* string[]

A list of plugins that must be present for pytest to run.

Plugins can be listed with or without version specifiers directly following their name.

---

##### `strict`<sup>Optional</sup> <a name="strict" id="projen.python.PytestConfiguration.property.strict"></a>

```typescript
public readonly strict: boolean;
```

- *Type:* boolean

If set to true, enable 'strict mode', which enables a number of other strict options.

---

##### `strictConfig`<sup>Optional</sup> <a name="strictConfig" id="projen.python.PytestConfiguration.property.strictConfig"></a>

```typescript
public readonly strictConfig: boolean;
```

- *Type:* boolean

If set to true, any warnings encountered while parsing the pytest section of the configuration file will raise errors.

---

##### `strictMarkers`<sup>Optional</sup> <a name="strictMarkers" id="projen.python.PytestConfiguration.property.strictMarkers"></a>

```typescript
public readonly strictMarkers: boolean;
```

- *Type:* boolean

If set to true, markers not registered in the markers section of the configuration file will raise errors.

---

##### `strictParametrizationIds`<sup>Optional</sup> <a name="strictParametrizationIds" id="projen.python.PytestConfiguration.property.strictParametrizationIds"></a>

```typescript
public readonly strictParametrizationIds: boolean;
```

- *Type:* boolean

If set to true, pytest emits an error if it detects non-unique parameter set IDs.

If not set (the default), pytest automatically handles this by adding 0, 1, … to duplicate IDs, making them unique.

---

##### `strictXfail`<sup>Optional</sup> <a name="strictXfail" id="projen.python.PytestConfiguration.property.strictXfail"></a>

```typescript
public readonly strictXfail: boolean;
```

- *Type:* boolean

If set to true, tests marked with.

---

##### `testpaths`<sup>Optional</sup> <a name="testpaths" id="projen.python.PytestConfiguration.property.testpaths"></a>

```typescript
public readonly testpaths: string[];
```

- *Type:* string[]

Sets list of directories that should be searched for tests when no specific directories, files or test ids are given in the command line when executing pytest from the rootdir directory.

File system paths may use shell-style wildcards, including the recursive ** pattern. Useful when all project tests are in a known location to speed up test collection and to avoid picking up undesired tests by accident.

---

##### `tmpPathRetentionCount`<sup>Optional</sup> <a name="tmpPathRetentionCount" id="projen.python.PytestConfiguration.property.tmpPathRetentionCount"></a>

```typescript
public readonly tmpPathRetentionCount: number;
```

- *Type:* number

How many sessions should we keep the tmp_path directories, according to tmp_path_retention_policy.

---

##### `tmpPathRetentionPolicy`<sup>Optional</sup> <a name="tmpPathRetentionPolicy" id="projen.python.PytestConfiguration.property.tmpPathRetentionPolicy"></a>

```typescript
public readonly tmpPathRetentionPolicy: PytestConfigurationTmpPathRetentionPolicy;
```

- *Type:* <a href="#projen.python.PytestConfigurationTmpPathRetentionPolicy">PytestConfigurationTmpPathRetentionPolicy</a>

Controls which directories created by the tmp_path fixture are kept around, based on test outcome.

---

##### `truncationLimitChars`<sup>Optional</sup> <a name="truncationLimitChars" id="projen.python.PytestConfiguration.property.truncationLimitChars"></a>

```typescript
public readonly truncationLimitChars: number;
```

- *Type:* number

Controls maximum number of characters to truncate assertion message contents.

Setting value to 0 disables the character limit for truncation.

---

##### `truncationLimitLines`<sup>Optional</sup> <a name="truncationLimitLines" id="projen.python.PytestConfiguration.property.truncationLimitLines"></a>

```typescript
public readonly truncationLimitLines: number;
```

- *Type:* number

Controls maximum number of linesto truncate assertion message contents.

Setting value to 0 disables the lines limit for truncation.

---

##### `usefixtures`<sup>Optional</sup> <a name="usefixtures" id="projen.python.PytestConfiguration.property.usefixtures"></a>

```typescript
public readonly usefixtures: string[];
```

- *Type:* string[]

List of fixtures that will be applied to all test functions;

this is semantically the same to apply the

---

##### `verbosityAssertions`<sup>Optional</sup> <a name="verbosityAssertions" id="projen.python.PytestConfiguration.property.verbosityAssertions"></a>

```typescript
public readonly verbosityAssertions: PytestConfigurationVerbosityAssertions;
```

- *Type:* <a href="#projen.python.PytestConfigurationVerbosityAssertions">PytestConfigurationVerbosityAssertions</a>

Set a verbosity level specifically for assertion related output, overriding the application wide level.

---

##### `verbositySubtests`<sup>Optional</sup> <a name="verbositySubtests" id="projen.python.PytestConfiguration.property.verbositySubtests"></a>

```typescript
public readonly verbositySubtests: PytestConfigurationVerbositySubtests;
```

- *Type:* <a href="#projen.python.PytestConfigurationVerbositySubtests">PytestConfigurationVerbositySubtests</a>

Set the verbosity level specifically for passed subtests.

A value of 1 or higher will show output for passed subtests (failed subtests are always reported). Passed subtests output can be suppressed with the value 0, which overwrites the -v command-line option.

---

##### `verbosityTestCases`<sup>Optional</sup> <a name="verbosityTestCases" id="projen.python.PytestConfiguration.property.verbosityTestCases"></a>

```typescript
public readonly verbosityTestCases: PytestConfigurationVerbosityTestCases;
```

- *Type:* <a href="#projen.python.PytestConfigurationVerbosityTestCases">PytestConfigurationVerbosityTestCases</a>

Set a verbosity level specifically for test case execution related output, overriding the application wide level.

---

### PytestOptions <a name="PytestOptions" id="projen.python.PytestOptions"></a>

#### Initializer <a name="Initializer" id="projen.python.PytestOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const pytestOptions: python.PytestOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PytestOptions.property.maxFailures">maxFailures</a></code> | <code>number</code> | Stop the testing process after the first N failures. |
| <code><a href="#projen.python.PytestOptions.property.testdir">testdir</a></code> | <code>string</code> | Location of sample tests. |
| <code><a href="#projen.python.PytestOptions.property.testMatch">testMatch</a></code> | <code>string[]</code> | List of paths to test files or directories. |
| <code><a href="#projen.python.PytestOptions.property.version">version</a></code> | <code>string</code> | Pytest version. |

---

##### `maxFailures`<sup>Optional</sup> <a name="maxFailures" id="projen.python.PytestOptions.property.maxFailures"></a>

```typescript
public readonly maxFailures: number;
```

- *Type:* number

Stop the testing process after the first N failures.

---

##### ~~`testdir`~~<sup>Optional</sup> <a name="testdir" id="projen.python.PytestOptions.property.testdir"></a>

- *Deprecated:* Reference `sampleTestdir` on the project instead; to change the directory where tests are discovered from, use `testMatch`.

```typescript
public readonly testdir: string;
```

- *Type:* string
- *Default:* "tests"

Location of sample tests.

Typically the same directory where project tests will be located.

---

##### `testMatch`<sup>Optional</sup> <a name="testMatch" id="projen.python.PytestOptions.property.testMatch"></a>

```typescript
public readonly testMatch: string[];
```

- *Type:* string[]
- *Default:* []

List of paths to test files or directories.

Useful when all project tests are in a known location to speed up
test collection and to avoid picking up undesired tests by accident.

Leave empty to discover all test_*.py or *_test.py files, per Pytest default.

The array will be concatenated and passed as a single argument to pytest.

---

*Example*

```typescript
["tests/unit", "tests/qa"]
```


##### `version`<sup>Optional</sup> <a name="version" id="projen.python.PytestOptions.property.version"></a>

```typescript
public readonly version: string;
```

- *Type:* string
- *Default:* "7.4.3"

Pytest version.

---

### PytestSampleOptions <a name="PytestSampleOptions" id="projen.python.PytestSampleOptions"></a>

Options for python test code sample.

#### Initializer <a name="Initializer" id="projen.python.PytestSampleOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const pytestSampleOptions: python.PytestSampleOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PytestSampleOptions.property.moduleName">moduleName</a></code> | <code>string</code> | Name of the python package as used in imports and filenames. |
| <code><a href="#projen.python.PytestSampleOptions.property.testdir">testdir</a></code> | <code>string</code> | Test directory. |

---

##### `moduleName`<sup>Required</sup> <a name="moduleName" id="projen.python.PytestSampleOptions.property.moduleName"></a>

```typescript
public readonly moduleName: string;
```

- *Type:* string

Name of the python package as used in imports and filenames.

---

##### `testdir`<sup>Required</sup> <a name="testdir" id="projen.python.PytestSampleOptions.property.testdir"></a>

```typescript
public readonly testdir: string;
```

- *Type:* string

Test directory.

---

### PythonBaseOptions <a name="PythonBaseOptions" id="projen.python.PythonBaseOptions"></a>

#### Initializer <a name="Initializer" id="projen.python.PythonBaseOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const pythonBaseOptions: python.PythonBaseOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PythonBaseOptions.property.buildSystem">buildSystem</a></code> | <code><a href="#projen.python.BuildSystem">BuildSystem</a></code> | Declares any Python level dependencies that must be installed in order to run the project’s build system successfully. |
| <code><a href="#projen.python.PythonBaseOptions.property.formatter">formatter</a></code> | <code><a href="#projen.python.PythonFormatter">PythonFormatter</a></code> | The formatter to use for the project. |
| <code><a href="#projen.python.PythonBaseOptions.property.linter">linter</a></code> | <code><a href="#projen.python.PythonLinter">PythonLinter</a></code> | The linter to use for the project. |
| <code><a href="#projen.python.PythonBaseOptions.property.pythonExec">pythonExec</a></code> | <code>string</code> | Path to the python executable to use. |
| <code><a href="#projen.python.PythonBaseOptions.property.typeChecker">typeChecker</a></code> | <code><a href="#projen.python.PythonTypeChecker">PythonTypeChecker</a></code> | The type checker to use for the project. |

---

##### `buildSystem`<sup>Optional</sup> <a name="buildSystem" id="projen.python.PythonBaseOptions.property.buildSystem"></a>

```typescript
public readonly buildSystem: BuildSystem;
```

- *Type:* <a href="#projen.python.BuildSystem">BuildSystem</a>
- *Default:* no build system

Declares any Python level dependencies that must be installed in order to run the project’s build system successfully.

---

##### `formatter`<sup>Optional</sup> <a name="formatter" id="projen.python.PythonBaseOptions.property.formatter"></a>

```typescript
public readonly formatter: PythonFormatter;
```

- *Type:* <a href="#projen.python.PythonFormatter">PythonFormatter</a>

The formatter to use for the project.

---

##### `linter`<sup>Optional</sup> <a name="linter" id="projen.python.PythonBaseOptions.property.linter"></a>

```typescript
public readonly linter: PythonLinter;
```

- *Type:* <a href="#projen.python.PythonLinter">PythonLinter</a>

The linter to use for the project.

---

##### `pythonExec`<sup>Optional</sup> <a name="pythonExec" id="projen.python.PythonBaseOptions.property.pythonExec"></a>

```typescript
public readonly pythonExec: string;
```

- *Type:* string
- *Default:* "python"

Path to the python executable to use.

---

##### `typeChecker`<sup>Optional</sup> <a name="typeChecker" id="projen.python.PythonBaseOptions.property.typeChecker"></a>

```typescript
public readonly typeChecker: PythonTypeChecker;
```

- *Type:* <a href="#projen.python.PythonTypeChecker">PythonTypeChecker</a>

The type checker to use for the project.

---

### PythonProjectOptions <a name="PythonProjectOptions" id="projen.python.PythonProjectOptions"></a>

Options for `PythonProject`.

#### Initializer <a name="Initializer" id="projen.python.PythonProjectOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const pythonProjectOptions: python.PythonProjectOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PythonProjectOptions.property.name">name</a></code> | <code>string</code> | Valid name consists only of ASCII letters and numbers, period, underscore and hyphen. |
| <code><a href="#projen.python.PythonProjectOptions.property.commitGenerated">commitGenerated</a></code> | <code>boolean</code> | Whether to commit the managed files by default. |
| <code><a href="#projen.python.PythonProjectOptions.property.gitIgnoreOptions">gitIgnoreOptions</a></code> | <code>projen.IgnoreFileOptions</code> | Configuration options for .gitignore file. |
| <code><a href="#projen.python.PythonProjectOptions.property.gitOptions">gitOptions</a></code> | <code>projen.GitOptions</code> | Configuration options for git. |
| <code><a href="#projen.python.PythonProjectOptions.property.logging">logging</a></code> | <code>projen.LoggerOptions</code> | Configure logging options such as verbosity. |
| <code><a href="#projen.python.PythonProjectOptions.property.outdir">outdir</a></code> | <code>string</code> | The root directory of the project. |
| <code><a href="#projen.python.PythonProjectOptions.property.parent">parent</a></code> | <code>projen.Project</code> | The parent project, if this project is part of a bigger project. |
| <code><a href="#projen.python.PythonProjectOptions.property.projenCommand">projenCommand</a></code> | <code>string</code> | The shell command to use in order to run the projen CLI. |
| <code><a href="#projen.python.PythonProjectOptions.property.projenrcJson">projenrcJson</a></code> | <code>boolean</code> | Generate (once) .projenrc.json (in JSON). Set to `false` in order to disable .projenrc.json generation. |
| <code><a href="#projen.python.PythonProjectOptions.property.projenrcJsonOptions">projenrcJsonOptions</a></code> | <code>projen.ProjenrcJsonOptions</code> | Options for .projenrc.json. |
| <code><a href="#projen.python.PythonProjectOptions.property.renovatebot">renovatebot</a></code> | <code>boolean</code> | Use renovatebot to handle dependency upgrades. |
| <code><a href="#projen.python.PythonProjectOptions.property.renovatebotOptions">renovatebotOptions</a></code> | <code>projen.RenovatebotOptions</code> | Options for renovatebot. |
| <code><a href="#projen.python.PythonProjectOptions.property.autoApproveOptions">autoApproveOptions</a></code> | <code>projen.github.AutoApproveOptions</code> | Enable and configure the 'auto approve' workflow. |
| <code><a href="#projen.python.PythonProjectOptions.property.autoMerge">autoMerge</a></code> | <code>boolean</code> | Enable automatic merging on GitHub. |
| <code><a href="#projen.python.PythonProjectOptions.property.autoMergeOptions">autoMergeOptions</a></code> | <code>projen.github.AutoMergeOptions</code> | Configure options for automatic merging on GitHub. |
| <code><a href="#projen.python.PythonProjectOptions.property.clobber">clobber</a></code> | <code>boolean</code> | Add a `clobber` task which resets the repo to origin. |
| <code><a href="#projen.python.PythonProjectOptions.property.devContainer">devContainer</a></code> | <code>boolean</code> | Add a VSCode development environment (used for GitHub Codespaces). |
| <code><a href="#projen.python.PythonProjectOptions.property.github">github</a></code> | <code>boolean</code> | Enable GitHub integration. |
| <code><a href="#projen.python.PythonProjectOptions.property.githubOptions">githubOptions</a></code> | <code>projen.github.GitHubOptions</code> | Options for GitHub integration. |
| <code><a href="#projen.python.PythonProjectOptions.property.gitpod">gitpod</a></code> | <code>boolean</code> | Add a Gitpod development environment. |
| <code><a href="#projen.python.PythonProjectOptions.property.mergify">mergify</a></code> | <code>boolean</code> | Whether mergify should be enabled on this repository or not. |
| <code><a href="#projen.python.PythonProjectOptions.property.mergifyOptions">mergifyOptions</a></code> | <code>projen.github.MergifyOptions</code> | Options for mergify. |
| <code><a href="#projen.python.PythonProjectOptions.property.projectType">projectType</a></code> | <code>projen.ProjectType</code> | Which type of project this is (library/app). |
| <code><a href="#projen.python.PythonProjectOptions.property.projenCredentials">projenCredentials</a></code> | <code>projen.github.GithubCredentials</code> | Choose a method of providing GitHub API access for projen workflows. |
| <code><a href="#projen.python.PythonProjectOptions.property.projenTokenSecret">projenTokenSecret</a></code> | <code>string</code> | The name of a secret which includes a GitHub Personal Access Token to be used by projen workflows. |
| <code><a href="#projen.python.PythonProjectOptions.property.stale">stale</a></code> | <code>boolean</code> | Auto-close of stale issues and pull request. |
| <code><a href="#projen.python.PythonProjectOptions.property.staleOptions">staleOptions</a></code> | <code>projen.github.StaleOptions</code> | Auto-close stale issues and pull requests. |
| <code><a href="#projen.python.PythonProjectOptions.property.vscode">vscode</a></code> | <code>boolean</code> | Enable VSCode integration. |
| <code><a href="#projen.python.PythonProjectOptions.property.authors">authors</a></code> | <code><a href="#projen.python.ProjectAuthor">ProjectAuthor</a>[]</code> | People or organizations considered as 'authors' of the project. |
| <code><a href="#projen.python.PythonProjectOptions.property.classifiers">classifiers</a></code> | <code>string[]</code> | List of [Trove classifiers](https://pypi.org/classifiers/) that describe the project. PyPI use the classifiers to categorize projects. |
| <code><a href="#projen.python.PythonProjectOptions.property.dependencies">dependencies</a></code> | <code>string[]</code> | An array of [dependency specifier](https://packaging.python.org/en/latest/specifications/dependency-specifiers/) strings, each representing a mandatory dependent package of the project. |
| <code><a href="#projen.python.PythonProjectOptions.property.description">description</a></code> | <code>string</code> | Summary description of the project in one line. |
| <code><a href="#projen.python.PythonProjectOptions.property.dynamic">dynamic</a></code> | <code><a href="#projen.python.PyprojectTomlProjectDynamic">PyprojectTomlProjectDynamic</a>[]</code> | Specifies which keys are intentionally unspecified under `[project]` table so build backend can/will provide such metadata dynamically. |
| <code><a href="#projen.python.PythonProjectOptions.property.entryPoints">entryPoints</a></code> | <code>any</code> | Extra [entry point groups](https://packaging.python.org/en/latest/specifications/entry-points/) that allow applications to load plugins. For example, Pygments (a syntax highlighting tool) can use additional styles from separately installed packages through `[project.entry-points."pygments.styles"]`. Each key is the name of the entry-point group, and each value is a table of entry points. |
| <code><a href="#projen.python.PythonProjectOptions.property.guiScripts">guiScripts</a></code> | <code>{[ key: string ]: string}</code> | Table of [entry points](https://packaging.python.org/en/latest/specifications/entry-points/) that allows package installers to create a GUI wrapper for. Each key is the name of the script to be created, and each value is the function or object to all, in form of either `importable.module` or `importable.module:object.attr`. Windows platform treats `gui_scripts` specially in that they are wrapped in a GUI executable, so they can be started without a console, but cannot use standard streams unless application code redirects them. |
| <code><a href="#projen.python.PythonProjectOptions.property.importNames">importNames</a></code> | <code>string[]</code> | An array of strings specifying the import names that the project exclusively provides when installed. |
| <code><a href="#projen.python.PythonProjectOptions.property.importNamespaces">importNamespaces</a></code> | <code>string[]</code> | An array of strings specifying the import names that the project provides when installed, but not exclusively. |
| <code><a href="#projen.python.PythonProjectOptions.property.keywords">keywords</a></code> | <code>string[]</code> | List of keywords or tags that describe the project. |
| <code><a href="#projen.python.PythonProjectOptions.property.license">license</a></code> | <code>any</code> | For now it is a table with either: - `file` key specifying a relative path to a license file, or - `text` key containing full license content. |
| <code><a href="#projen.python.PythonProjectOptions.property.licenseFiles">licenseFiles</a></code> | <code>string[]</code> | Relative paths or globs to paths of license files. |
| <code><a href="#projen.python.PythonProjectOptions.property.maintainers">maintainers</a></code> | <code><a href="#projen.python.ProjectAuthor">ProjectAuthor</a>[]</code> | People or organizations considered as 'maintainers' of the project. |
| <code><a href="#projen.python.PythonProjectOptions.property.optionalDependencies">optionalDependencies</a></code> | <code>any</code> | Each entry is a key/value pair, with the key specifying [extra feature name](https://packaging.python.org/en/latest/specifications/core-metadata/#provides-extra-multiple-use) (such as `socks` in `requests[socks]`), and value is an array of [dependency specifier](https://packaging.python.org/en/latest/specifications/dependency-specifiers/) strings. |
| <code><a href="#projen.python.PythonProjectOptions.property.readme">readme</a></code> | <code>any</code> | Value can be a relative path to text / markdown (`.md` suffix) / reStructuredText (`.rst` suffix) readme file, or a table with either: - `file` key containing path of aforementioned readme file, or - `text` key containing the full readme text embedded inside `pyproject.toml`. |
| <code><a href="#projen.python.PythonProjectOptions.property.requiresPython">requiresPython</a></code> | <code>string</code> | Specifies the Python version(s) that the distribution is compatible with. |
| <code><a href="#projen.python.PythonProjectOptions.property.scripts">scripts</a></code> | <code>{[ key: string ]: string}</code> | Table of [entry points](https://packaging.python.org/en/latest/specifications/entry-points/) that allows package installers to create a command-line wrapper for. Each key is the name of the script to be created, and each value is the function or object to all, in form of either `importable.module` or `importable.module:object.attr`. Windows platform treats `console_scripts` specially in that they are wrapped in a console executable, so they are attached to a console and can use `sys.stdin`, `sys.stdout` and `sys.stderr` for I/O. |
| <code><a href="#projen.python.PythonProjectOptions.property.urls">urls</a></code> | <code>{[ key: string ]: string}</code> | Table consisting one or multiple `label: URL` pairs. |
| <code><a href="#projen.python.PythonProjectOptions.property.version">version</a></code> | <code>string</code> | Version of the project, as defined in the [Version specifier specification](https://packaging.python.org/en/latest/specifications/version-specifiers/), and preferably [already normalized](https://packaging.python.org/en/latest/specifications/version-specifiers/#normalization). |
| <code><a href="#projen.python.PythonProjectOptions.property.buildSystem">buildSystem</a></code> | <code><a href="#projen.python.BuildSystem">BuildSystem</a></code> | Declares any Python level dependencies that must be installed in order to run the project’s build system successfully. |
| <code><a href="#projen.python.PythonProjectOptions.property.formatter">formatter</a></code> | <code><a href="#projen.python.PythonFormatter">PythonFormatter</a></code> | The formatter to use for the project. |
| <code><a href="#projen.python.PythonProjectOptions.property.linter">linter</a></code> | <code><a href="#projen.python.PythonLinter">PythonLinter</a></code> | The linter to use for the project. |
| <code><a href="#projen.python.PythonProjectOptions.property.pythonExec">pythonExec</a></code> | <code>string</code> | Path to the python executable to use. |
| <code><a href="#projen.python.PythonProjectOptions.property.typeChecker">typeChecker</a></code> | <code><a href="#projen.python.PythonTypeChecker">PythonTypeChecker</a></code> | The type checker to use for the project. |
| <code><a href="#projen.python.PythonProjectOptions.property.devDeps">devDeps</a></code> | <code>string[]</code> | List of dev dependencies for this project. |
| <code><a href="#projen.python.PythonProjectOptions.property.packageManager">packageManager</a></code> | <code><a href="#projen.python.PythonPackageManager">PythonPackageManager</a></code> | The package manager to use for the project. |
| <code><a href="#projen.python.PythonProjectOptions.property.projenrcJs">projenrcJs</a></code> | <code>boolean</code> | Use projenrc in javascript. |
| <code><a href="#projen.python.PythonProjectOptions.property.projenrcJsOptions">projenrcJsOptions</a></code> | <code>projen.javascript.ProjenrcOptions</code> | Options related to projenrc in JavaScript. |
| <code><a href="#projen.python.PythonProjectOptions.property.projenrcPython">projenrcPython</a></code> | <code>boolean</code> | Use projenrc in Python. |
| <code><a href="#projen.python.PythonProjectOptions.property.projenrcPythonOptions">projenrcPythonOptions</a></code> | <code><a href="#projen.python.ProjenrcOptions">ProjenrcOptions</a></code> | Options related to projenrc in python. |
| <code><a href="#projen.python.PythonProjectOptions.property.projenrcTs">projenrcTs</a></code> | <code>boolean</code> | Use projenrc in TypeScript. |
| <code><a href="#projen.python.PythonProjectOptions.property.projenrcTsOptions">projenrcTsOptions</a></code> | <code>projen.typescript.ProjenrcTsOptions</code> | Options related to projenrc in TypeScript. |
| <code><a href="#projen.python.PythonProjectOptions.property.pytest">pytest</a></code> | <code>boolean</code> | Include pytest tests. |
| <code><a href="#projen.python.PythonProjectOptions.property.pytestOptions">pytestOptions</a></code> | <code><a href="#projen.python.PytestOptions">PytestOptions</a></code> | pytest options. |
| <code><a href="#projen.python.PythonProjectOptions.property.sample">sample</a></code> | <code>boolean</code> | Include sample code and test if the relevant directories don't exist. |
| <code><a href="#projen.python.PythonProjectOptions.property.sampleTestdir">sampleTestdir</a></code> | <code>string</code> | Location of sample tests. |
| <code><a href="#projen.python.PythonProjectOptions.property.tool">tool</a></code> | <code><a href="#projen.python.PythonProjectTool">PythonProjectTool</a></code> | Every tool that is used by the project can have users specify configuration data as long as they use a sub-table within `[tool]`. |
| <code><a href="#projen.python.PythonProjectOptions.property.venv">venv</a></code> | <code>boolean</code> | Use venv to manage a virtual environment for installing dependencies inside. |
| <code><a href="#projen.python.PythonProjectOptions.property.venvOptions">venvOptions</a></code> | <code><a href="#projen.python.VenvOptions">VenvOptions</a></code> | Venv options. |

---

##### `name`<sup>Required</sup> <a name="name" id="projen.python.PythonProjectOptions.property.name"></a>

```typescript
public readonly name: string;
```

- *Type:* string

Valid name consists only of ASCII letters and numbers, period, underscore and hyphen.

It must start and end with a letter or number.

---

##### `commitGenerated`<sup>Optional</sup> <a name="commitGenerated" id="projen.python.PythonProjectOptions.property.commitGenerated"></a>

```typescript
public readonly commitGenerated: boolean;
```

- *Type:* boolean
- *Default:* true

Whether to commit the managed files by default.

---

##### `gitIgnoreOptions`<sup>Optional</sup> <a name="gitIgnoreOptions" id="projen.python.PythonProjectOptions.property.gitIgnoreOptions"></a>

```typescript
public readonly gitIgnoreOptions: IgnoreFileOptions;
```

- *Type:* projen.IgnoreFileOptions

Configuration options for .gitignore file.

---

##### `gitOptions`<sup>Optional</sup> <a name="gitOptions" id="projen.python.PythonProjectOptions.property.gitOptions"></a>

```typescript
public readonly gitOptions: GitOptions;
```

- *Type:* projen.GitOptions

Configuration options for git.

---

##### `logging`<sup>Optional</sup> <a name="logging" id="projen.python.PythonProjectOptions.property.logging"></a>

```typescript
public readonly logging: LoggerOptions;
```

- *Type:* projen.LoggerOptions
- *Default:* {}

Configure logging options such as verbosity.

---

##### `outdir`<sup>Optional</sup> <a name="outdir" id="projen.python.PythonProjectOptions.property.outdir"></a>

```typescript
public readonly outdir: string;
```

- *Type:* string
- *Default:* "."

The root directory of the project.

Relative to this directory, all files are synthesized.

If this project has a parent, this directory is relative to the parent
directory and it cannot be the same as the parent or any of it's other
subprojects.

---

##### `parent`<sup>Optional</sup> <a name="parent" id="projen.python.PythonProjectOptions.property.parent"></a>

```typescript
public readonly parent: Project;
```

- *Type:* projen.Project

The parent project, if this project is part of a bigger project.

---

##### `projenCommand`<sup>Optional</sup> <a name="projenCommand" id="projen.python.PythonProjectOptions.property.projenCommand"></a>

```typescript
public readonly projenCommand: string;
```

- *Type:* string
- *Default:* "npx projen"

The shell command to use in order to run the projen CLI.

Can be used to customize in special environments.

---

##### `projenrcJson`<sup>Optional</sup> <a name="projenrcJson" id="projen.python.PythonProjectOptions.property.projenrcJson"></a>

```typescript
public readonly projenrcJson: boolean;
```

- *Type:* boolean
- *Default:* false

Generate (once) .projenrc.json (in JSON). Set to `false` in order to disable .projenrc.json generation.

---

##### `projenrcJsonOptions`<sup>Optional</sup> <a name="projenrcJsonOptions" id="projen.python.PythonProjectOptions.property.projenrcJsonOptions"></a>

```typescript
public readonly projenrcJsonOptions: ProjenrcJsonOptions;
```

- *Type:* projen.ProjenrcJsonOptions
- *Default:* default options

Options for .projenrc.json.

---

##### `renovatebot`<sup>Optional</sup> <a name="renovatebot" id="projen.python.PythonProjectOptions.property.renovatebot"></a>

```typescript
public readonly renovatebot: boolean;
```

- *Type:* boolean
- *Default:* false

Use renovatebot to handle dependency upgrades.

---

##### `renovatebotOptions`<sup>Optional</sup> <a name="renovatebotOptions" id="projen.python.PythonProjectOptions.property.renovatebotOptions"></a>

```typescript
public readonly renovatebotOptions: RenovatebotOptions;
```

- *Type:* projen.RenovatebotOptions
- *Default:* default options

Options for renovatebot.

---

##### `autoApproveOptions`<sup>Optional</sup> <a name="autoApproveOptions" id="projen.python.PythonProjectOptions.property.autoApproveOptions"></a>

```typescript
public readonly autoApproveOptions: AutoApproveOptions;
```

- *Type:* projen.github.AutoApproveOptions
- *Default:* auto approve is disabled

Enable and configure the 'auto approve' workflow.

---

##### `autoMerge`<sup>Optional</sup> <a name="autoMerge" id="projen.python.PythonProjectOptions.property.autoMerge"></a>

```typescript
public readonly autoMerge: boolean;
```

- *Type:* boolean
- *Default:* true

Enable automatic merging on GitHub.

Has no effect if `github.mergify`
is set to false.

---

##### `autoMergeOptions`<sup>Optional</sup> <a name="autoMergeOptions" id="projen.python.PythonProjectOptions.property.autoMergeOptions"></a>

```typescript
public readonly autoMergeOptions: AutoMergeOptions;
```

- *Type:* projen.github.AutoMergeOptions
- *Default:* see defaults in `AutoMergeOptions`

Configure options for automatic merging on GitHub.

Has no effect if
`github.mergify` or `autoMerge` is set to false.

---

##### `clobber`<sup>Optional</sup> <a name="clobber" id="projen.python.PythonProjectOptions.property.clobber"></a>

```typescript
public readonly clobber: boolean;
```

- *Type:* boolean
- *Default:* true, but false for subprojects

Add a `clobber` task which resets the repo to origin.

---

##### `devContainer`<sup>Optional</sup> <a name="devContainer" id="projen.python.PythonProjectOptions.property.devContainer"></a>

```typescript
public readonly devContainer: boolean;
```

- *Type:* boolean
- *Default:* false

Add a VSCode development environment (used for GitHub Codespaces).

---

##### `github`<sup>Optional</sup> <a name="github" id="projen.python.PythonProjectOptions.property.github"></a>

```typescript
public readonly github: boolean;
```

- *Type:* boolean
- *Default:* true

Enable GitHub integration.

Enabled by default for root projects. Disabled for non-root projects.

---

##### `githubOptions`<sup>Optional</sup> <a name="githubOptions" id="projen.python.PythonProjectOptions.property.githubOptions"></a>

```typescript
public readonly githubOptions: GitHubOptions;
```

- *Type:* projen.github.GitHubOptions
- *Default:* see GitHubOptions

Options for GitHub integration.

---

##### `gitpod`<sup>Optional</sup> <a name="gitpod" id="projen.python.PythonProjectOptions.property.gitpod"></a>

```typescript
public readonly gitpod: boolean;
```

- *Type:* boolean
- *Default:* false

Add a Gitpod development environment.

---

##### ~~`mergify`~~<sup>Optional</sup> <a name="mergify" id="projen.python.PythonProjectOptions.property.mergify"></a>

- *Deprecated:* use `githubOptions.mergify` instead

```typescript
public readonly mergify: boolean;
```

- *Type:* boolean
- *Default:* true

Whether mergify should be enabled on this repository or not.

---

##### ~~`mergifyOptions`~~<sup>Optional</sup> <a name="mergifyOptions" id="projen.python.PythonProjectOptions.property.mergifyOptions"></a>

- *Deprecated:* use `githubOptions.mergifyOptions` instead

```typescript
public readonly mergifyOptions: MergifyOptions;
```

- *Type:* projen.github.MergifyOptions
- *Default:* default options

Options for mergify.

---

##### ~~`projectType`~~<sup>Optional</sup> <a name="projectType" id="projen.python.PythonProjectOptions.property.projectType"></a>

- *Deprecated:* no longer supported at the base project level

```typescript
public readonly projectType: ProjectType;
```

- *Type:* projen.ProjectType
- *Default:* ProjectType.UNKNOWN

Which type of project this is (library/app).

---

##### `projenCredentials`<sup>Optional</sup> <a name="projenCredentials" id="projen.python.PythonProjectOptions.property.projenCredentials"></a>

```typescript
public readonly projenCredentials: GithubCredentials;
```

- *Type:* projen.github.GithubCredentials
- *Default:* use a personal access token named PROJEN_GITHUB_TOKEN

Choose a method of providing GitHub API access for projen workflows.

---

##### ~~`projenTokenSecret`~~<sup>Optional</sup> <a name="projenTokenSecret" id="projen.python.PythonProjectOptions.property.projenTokenSecret"></a>

- *Deprecated:* use `projenCredentials`

```typescript
public readonly projenTokenSecret: string;
```

- *Type:* string
- *Default:* "PROJEN_GITHUB_TOKEN"

The name of a secret which includes a GitHub Personal Access Token to be used by projen workflows.

This token needs to have the `repo`, `workflows`
and `packages` scope.

---

##### `stale`<sup>Optional</sup> <a name="stale" id="projen.python.PythonProjectOptions.property.stale"></a>

```typescript
public readonly stale: boolean;
```

- *Type:* boolean
- *Default:* false

Auto-close of stale issues and pull request.

See `staleOptions` for options.

---

##### `staleOptions`<sup>Optional</sup> <a name="staleOptions" id="projen.python.PythonProjectOptions.property.staleOptions"></a>

```typescript
public readonly staleOptions: StaleOptions;
```

- *Type:* projen.github.StaleOptions
- *Default:* see defaults in `StaleOptions`

Auto-close stale issues and pull requests.

To disable set `stale` to `false`.

---

##### `vscode`<sup>Optional</sup> <a name="vscode" id="projen.python.PythonProjectOptions.property.vscode"></a>

```typescript
public readonly vscode: boolean;
```

- *Type:* boolean
- *Default:* true

Enable VSCode integration.

Enabled by default for root projects. Disabled for non-root projects.

---

##### `authors`<sup>Optional</sup> <a name="authors" id="projen.python.PythonProjectOptions.property.authors"></a>

```typescript
public readonly authors: ProjectAuthor[];
```

- *Type:* <a href="#projen.python.ProjectAuthor">ProjectAuthor</a>[]

People or organizations considered as 'authors' of the project.

Each author is a table with `name` key, `email` key, or both.

---

##### `classifiers`<sup>Optional</sup> <a name="classifiers" id="projen.python.PythonProjectOptions.property.classifiers"></a>

```typescript
public readonly classifiers: string[];
```

- *Type:* string[]

List of [Trove classifiers](https://pypi.org/classifiers/) that describe the project. PyPI use the classifiers to categorize projects.

---

##### `dependencies`<sup>Optional</sup> <a name="dependencies" id="projen.python.PythonProjectOptions.property.dependencies"></a>

```typescript
public readonly dependencies: string[];
```

- *Type:* string[]

An array of [dependency specifier](https://packaging.python.org/en/latest/specifications/dependency-specifiers/) strings, each representing a mandatory dependent package of the project.

---

##### `description`<sup>Optional</sup> <a name="description" id="projen.python.PythonProjectOptions.property.description"></a>

```typescript
public readonly description: string;
```

- *Type:* string

Summary description of the project in one line.

Tools may not accept multiple lines.

---

##### `dynamic`<sup>Optional</sup> <a name="dynamic" id="projen.python.PythonProjectOptions.property.dynamic"></a>

```typescript
public readonly dynamic: PyprojectTomlProjectDynamic[];
```

- *Type:* <a href="#projen.python.PyprojectTomlProjectDynamic">PyprojectTomlProjectDynamic</a>[]

Specifies which keys are intentionally unspecified under `[project]` table so build backend can/will provide such metadata dynamically.

Each key must be listed only once. It is an error to both list a key in `dynamic` and use the key directly in `[project]`.
One of the most common usage is `version`, which allows build backend to retrieve project version from source code or version control system instead of hardcoding it in `pyproject.toml`.

---

##### `entryPoints`<sup>Optional</sup> <a name="entryPoints" id="projen.python.PythonProjectOptions.property.entryPoints"></a>

```typescript
public readonly entryPoints: any;
```

- *Type:* any

Extra [entry point groups](https://packaging.python.org/en/latest/specifications/entry-points/) that allow applications to load plugins. For example, Pygments (a syntax highlighting tool) can use additional styles from separately installed packages through `[project.entry-points."pygments.styles"]`. Each key is the name of the entry-point group, and each value is a table of entry points.

---

##### `guiScripts`<sup>Optional</sup> <a name="guiScripts" id="projen.python.PythonProjectOptions.property.guiScripts"></a>

```typescript
public readonly guiScripts: {[ key: string ]: string};
```

- *Type:* {[ key: string ]: string}

Table of [entry points](https://packaging.python.org/en/latest/specifications/entry-points/) that allows package installers to create a GUI wrapper for. Each key is the name of the script to be created, and each value is the function or object to all, in form of either `importable.module` or `importable.module:object.attr`. Windows platform treats `gui_scripts` specially in that they are wrapped in a GUI executable, so they can be started without a console, but cannot use standard streams unless application code redirects them.

---

##### `importNames`<sup>Optional</sup> <a name="importNames" id="projen.python.PythonProjectOptions.property.importNames"></a>

```typescript
public readonly importNames: string[];
```

- *Type:* string[]

An array of strings specifying the import names that the project exclusively provides when installed.

---

##### `importNamespaces`<sup>Optional</sup> <a name="importNamespaces" id="projen.python.PythonProjectOptions.property.importNamespaces"></a>

```typescript
public readonly importNamespaces: string[];
```

- *Type:* string[]

An array of strings specifying the import names that the project provides when installed, but not exclusively.

---

##### `keywords`<sup>Optional</sup> <a name="keywords" id="projen.python.PythonProjectOptions.property.keywords"></a>

```typescript
public readonly keywords: string[];
```

- *Type:* string[]

List of keywords or tags that describe the project.

They could be used by search engines to categorize the project.

---

##### `license`<sup>Optional</sup> <a name="license" id="projen.python.PythonProjectOptions.property.license"></a>

```typescript
public readonly license: any;
```

- *Type:* any

For now it is a table with either: - `file` key specifying a relative path to a license file, or - `text` key containing full license content.

Newer tool may accept a single [SPDX license expression](https://spdx.github.io/spdx-spec/v2.2.2/SPDX-license-expressions/) string instead of a table.

---

##### `licenseFiles`<sup>Optional</sup> <a name="licenseFiles" id="projen.python.PythonProjectOptions.property.licenseFiles"></a>

```typescript
public readonly licenseFiles: string[];
```

- *Type:* string[]

Relative paths or globs to paths of license files.

Can be an empty list.

---

##### `maintainers`<sup>Optional</sup> <a name="maintainers" id="projen.python.PythonProjectOptions.property.maintainers"></a>

```typescript
public readonly maintainers: ProjectAuthor[];
```

- *Type:* <a href="#projen.python.ProjectAuthor">ProjectAuthor</a>[]

People or organizations considered as 'maintainers' of the project.

Each maintainer is a table with `name` key, `email` key, or both.

---

##### `optionalDependencies`<sup>Optional</sup> <a name="optionalDependencies" id="projen.python.PythonProjectOptions.property.optionalDependencies"></a>

```typescript
public readonly optionalDependencies: any;
```

- *Type:* any

Each entry is a key/value pair, with the key specifying [extra feature name](https://packaging.python.org/en/latest/specifications/core-metadata/#provides-extra-multiple-use) (such as `socks` in `requests[socks]`), and value is an array of [dependency specifier](https://packaging.python.org/en/latest/specifications/dependency-specifiers/) strings.

---

##### `readme`<sup>Optional</sup> <a name="readme" id="projen.python.PythonProjectOptions.property.readme"></a>

```typescript
public readonly readme: any;
```

- *Type:* any

Value can be a relative path to text / markdown (`.md` suffix) / reStructuredText (`.rst` suffix) readme file, or a table with either: - `file` key containing path of aforementioned readme file, or - `text` key containing the full readme text embedded inside `pyproject.toml`.

---

##### `requiresPython`<sup>Optional</sup> <a name="requiresPython" id="projen.python.PythonProjectOptions.property.requiresPython"></a>

```typescript
public readonly requiresPython: string;
```

- *Type:* string

Specifies the Python version(s) that the distribution is compatible with.

Must be in the format specified in [Version specifiers](https://packaging.python.org/en/latest/specifications/version-specifiers/).

---

##### `scripts`<sup>Optional</sup> <a name="scripts" id="projen.python.PythonProjectOptions.property.scripts"></a>

```typescript
public readonly scripts: {[ key: string ]: string};
```

- *Type:* {[ key: string ]: string}

Table of [entry points](https://packaging.python.org/en/latest/specifications/entry-points/) that allows package installers to create a command-line wrapper for. Each key is the name of the script to be created, and each value is the function or object to all, in form of either `importable.module` or `importable.module:object.attr`. Windows platform treats `console_scripts` specially in that they are wrapped in a console executable, so they are attached to a console and can use `sys.stdin`, `sys.stdout` and `sys.stderr` for I/O.

---

##### `urls`<sup>Optional</sup> <a name="urls" id="projen.python.PythonProjectOptions.property.urls"></a>

```typescript
public readonly urls: {[ key: string ]: string};
```

- *Type:* {[ key: string ]: string}

Table consisting one or multiple `label: URL` pairs.

Common indexes like PyPI uses [well-known Project URLs](https://packaging.python.org/en/latest/specifications/well-known-project-urls/#well-known-labels) when presenting project pages.

---

##### `version`<sup>Optional</sup> <a name="version" id="projen.python.PythonProjectOptions.property.version"></a>

```typescript
public readonly version: string;
```

- *Type:* string

Version of the project, as defined in the [Version specifier specification](https://packaging.python.org/en/latest/specifications/version-specifiers/), and preferably [already normalized](https://packaging.python.org/en/latest/specifications/version-specifiers/#normalization).

---

##### `buildSystem`<sup>Optional</sup> <a name="buildSystem" id="projen.python.PythonProjectOptions.property.buildSystem"></a>

```typescript
public readonly buildSystem: BuildSystem;
```

- *Type:* <a href="#projen.python.BuildSystem">BuildSystem</a>
- *Default:* no build system

Declares any Python level dependencies that must be installed in order to run the project’s build system successfully.

---

##### `formatter`<sup>Optional</sup> <a name="formatter" id="projen.python.PythonProjectOptions.property.formatter"></a>

```typescript
public readonly formatter: PythonFormatter;
```

- *Type:* <a href="#projen.python.PythonFormatter">PythonFormatter</a>

The formatter to use for the project.

---

##### `linter`<sup>Optional</sup> <a name="linter" id="projen.python.PythonProjectOptions.property.linter"></a>

```typescript
public readonly linter: PythonLinter;
```

- *Type:* <a href="#projen.python.PythonLinter">PythonLinter</a>

The linter to use for the project.

---

##### `pythonExec`<sup>Optional</sup> <a name="pythonExec" id="projen.python.PythonProjectOptions.property.pythonExec"></a>

```typescript
public readonly pythonExec: string;
```

- *Type:* string
- *Default:* "python"

Path to the python executable to use.

---

##### `typeChecker`<sup>Optional</sup> <a name="typeChecker" id="projen.python.PythonProjectOptions.property.typeChecker"></a>

```typescript
public readonly typeChecker: PythonTypeChecker;
```

- *Type:* <a href="#projen.python.PythonTypeChecker">PythonTypeChecker</a>

The type checker to use for the project.

---

##### `devDeps`<sup>Optional</sup> <a name="devDeps" id="projen.python.PythonProjectOptions.property.devDeps"></a>

```typescript
public readonly devDeps: string[];
```

- *Type:* string[]
- *Default:* []

List of dev dependencies for this project.

Dependencies use the format: `<module>@<semver>`

Additional dependencies can be added via `project.addDevDependency()`.

---

##### `packageManager`<sup>Optional</sup> <a name="packageManager" id="projen.python.PythonProjectOptions.property.packageManager"></a>

```typescript
public readonly packageManager: PythonPackageManager;
```

- *Type:* <a href="#projen.python.PythonPackageManager">PythonPackageManager</a>
- *Default:* PackageManager.UV

The package manager to use for the project.

---

##### `projenrcJs`<sup>Optional</sup> <a name="projenrcJs" id="projen.python.PythonProjectOptions.property.projenrcJs"></a>

```typescript
public readonly projenrcJs: boolean;
```

- *Type:* boolean
- *Default:* false

Use projenrc in javascript.

This will install `projen` as a JavaScript dependency and add a `synth`
task which will run `.projenrc.js`.

---

##### `projenrcJsOptions`<sup>Optional</sup> <a name="projenrcJsOptions" id="projen.python.PythonProjectOptions.property.projenrcJsOptions"></a>

```typescript
public readonly projenrcJsOptions: ProjenrcOptions;
```

- *Type:* projen.javascript.ProjenrcOptions
- *Default:* default options

Options related to projenrc in JavaScript.

---

##### `projenrcPython`<sup>Optional</sup> <a name="projenrcPython" id="projen.python.PythonProjectOptions.property.projenrcPython"></a>

```typescript
public readonly projenrcPython: boolean;
```

- *Type:* boolean
- *Default:* true

Use projenrc in Python.

This will install `projen` as a Python dependency and add a `synth`
task which will run `.projenrc.py`.

---

##### `projenrcPythonOptions`<sup>Optional</sup> <a name="projenrcPythonOptions" id="projen.python.PythonProjectOptions.property.projenrcPythonOptions"></a>

```typescript
public readonly projenrcPythonOptions: ProjenrcOptions;
```

- *Type:* <a href="#projen.python.ProjenrcOptions">ProjenrcOptions</a>
- *Default:* default options

Options related to projenrc in python.

---

##### `projenrcTs`<sup>Optional</sup> <a name="projenrcTs" id="projen.python.PythonProjectOptions.property.projenrcTs"></a>

```typescript
public readonly projenrcTs: boolean;
```

- *Type:* boolean
- *Default:* false

Use projenrc in TypeScript.

This will create a tsconfig file (default: `tsconfig.projen.json`)
and use `ts-node` in the default task to parse the project source files.

---

##### `projenrcTsOptions`<sup>Optional</sup> <a name="projenrcTsOptions" id="projen.python.PythonProjectOptions.property.projenrcTsOptions"></a>

```typescript
public readonly projenrcTsOptions: ProjenrcTsOptions;
```

- *Type:* projen.typescript.ProjenrcTsOptions
- *Default:* default options

Options related to projenrc in TypeScript.

---

##### `pytest`<sup>Optional</sup> <a name="pytest" id="projen.python.PythonProjectOptions.property.pytest"></a>

```typescript
public readonly pytest: boolean;
```

- *Type:* boolean
- *Default:* true

Include pytest tests.

---

##### `pytestOptions`<sup>Optional</sup> <a name="pytestOptions" id="projen.python.PythonProjectOptions.property.pytestOptions"></a>

```typescript
public readonly pytestOptions: PytestOptions;
```

- *Type:* <a href="#projen.python.PytestOptions">PytestOptions</a>
- *Default:* defaults

pytest options.

---

##### `sample`<sup>Optional</sup> <a name="sample" id="projen.python.PythonProjectOptions.property.sample"></a>

```typescript
public readonly sample: boolean;
```

- *Type:* boolean
- *Default:* true

Include sample code and test if the relevant directories don't exist.

---

##### `sampleTestdir`<sup>Optional</sup> <a name="sampleTestdir" id="projen.python.PythonProjectOptions.property.sampleTestdir"></a>

```typescript
public readonly sampleTestdir: string;
```

- *Type:* string
- *Default:* "tests"

Location of sample tests.

Typically the same directory where project tests will be located.

---

##### `tool`<sup>Optional</sup> <a name="tool" id="projen.python.PythonProjectOptions.property.tool"></a>

```typescript
public readonly tool: PythonProjectTool;
```

- *Type:* <a href="#projen.python.PythonProjectTool">PythonProjectTool</a>

Every tool that is used by the project can have users specify configuration data as long as they use a sub-table within `[tool]`.

Generally a project can use the subtable `tool.$NAME` if, and only if, they own the entry for `$NAME` in the Cheeseshop/PyPI.

---

##### `venv`<sup>Optional</sup> <a name="venv" id="projen.python.PythonProjectOptions.property.venv"></a>

```typescript
public readonly venv: boolean;
```

- *Type:* boolean
- *Default:* true when using setuptools

Use venv to manage a virtual environment for installing dependencies inside.

---

##### `venvOptions`<sup>Optional</sup> <a name="venvOptions" id="projen.python.PythonProjectOptions.property.venvOptions"></a>

```typescript
public readonly venvOptions: VenvOptions;
```

- *Type:* <a href="#projen.python.VenvOptions">VenvOptions</a>
- *Default:* defaults

Venv options.

---

### PythonProjectTool <a name="PythonProjectTool" id="projen.python.PythonProjectTool"></a>

#### Initializer <a name="Initializer" id="projen.python.PythonProjectTool.Initializer"></a>

```typescript
import { python } from 'projen'

const pythonProjectTool: python.PythonProjectTool = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PythonProjectTool.property.black">black</a></code> | <code><a href="#projen.python.BlackConfiguration">BlackConfiguration</a></code> | The uncompromising Python code formatter. |
| <code><a href="#projen.python.PythonProjectTool.property.cibuildwheel">cibuildwheel</a></code> | <code>any</code> | Build Python wheels for all platforms. |
| <code><a href="#projen.python.PythonProjectTool.property.hatch">hatch</a></code> | <code><a href="#projen.python.HatchConfiguration">HatchConfiguration</a></code> | Modern, extensible Python project management. |
| <code><a href="#projen.python.PythonProjectTool.property.maturin">maturin</a></code> | <code>any</code> | Build and publish crates with pyo3, cffi and uniffi bindings as well as rust binaries as python packages. |
| <code><a href="#projen.python.PythonProjectTool.property.mypy">mypy</a></code> | <code><a href="#projen.python.MypyConfiguration">MypyConfiguration</a></code> | Optional static typing for Python. |
| <code><a href="#projen.python.PythonProjectTool.property.pdm">pdm</a></code> | <code><a href="#projen.python.PdmConfiguration">PdmConfiguration</a></code> | A modern Python package manager with PEP 621 support. |
| <code><a href="#projen.python.PythonProjectTool.property.poe">poe</a></code> | <code>any</code> | A task runner that works well with pyproject.toml files. |
| <code><a href="#projen.python.PythonProjectTool.property.poetry">poetry</a></code> | <code><a href="#projen.python.PoetryConfiguration">PoetryConfiguration</a></code> | Python dependency management and packaging made easy. |
| <code><a href="#projen.python.PythonProjectTool.property.pyright">pyright</a></code> | <code><a href="#projen.python.PyrightConfiguration">PyrightConfiguration</a></code> | Static type checker for Python. |
| <code><a href="#projen.python.PythonProjectTool.property.pytest">pytest</a></code> | <code><a href="#projen.python.PytestConfiguration">PytestConfiguration</a></code> | Standardized automated testing of Python packages. |
| <code><a href="#projen.python.PythonProjectTool.property.repoReview">repoReview</a></code> | <code>any</code> | Review a repository for best practices. |
| <code><a href="#projen.python.PythonProjectTool.property.ruff">ruff</a></code> | <code><a href="#projen.python.RuffConfiguration">RuffConfiguration</a></code> | An extremely fast Python linter and formatter, written in Rust. |
| <code><a href="#projen.python.PythonProjectTool.property.scikitBuild">scikitBuild</a></code> | <code>any</code> | Improved build system generator for Python C/C++/Fortran extensions. |
| <code><a href="#projen.python.PythonProjectTool.property.setuptools">setuptools</a></code> | <code><a href="#projen.python.SetuptoolsConfiguration">SetuptoolsConfiguration</a></code> | Easily download, build, install, upgrade, and uninstall Python packages. |
| <code><a href="#projen.python.PythonProjectTool.property.setuptoolsScm">setuptoolsScm</a></code> | <code>any</code> | Manage Python package versions using SCM (e.g. Git). |
| <code><a href="#projen.python.PythonProjectTool.property.taskipy">taskipy</a></code> | <code>any</code> | The complementary task runner for python. |
| <code><a href="#projen.python.PythonProjectTool.property.tombi">tombi</a></code> | <code>any</code> | Tombi is a toolkit for TOML; |
| <code><a href="#projen.python.PythonProjectTool.property.tox">tox</a></code> | <code>any</code> | Standardized automated testing of Python packages. |
| <code><a href="#projen.python.PythonProjectTool.property.ty">ty</a></code> | <code><a href="#projen.python.TyConfiguration">TyConfiguration</a></code> | An extremely fast Python type checker, written in Rust. |
| <code><a href="#projen.python.PythonProjectTool.property.uv">uv</a></code> | <code><a href="#projen.python.UvConfiguration">UvConfiguration</a></code> | An extremely fast Python package installer and resolver, written in Rust. |

---

##### `black`<sup>Optional</sup> <a name="black" id="projen.python.PythonProjectTool.property.black"></a>

```typescript
public readonly black: BlackConfiguration;
```

- *Type:* <a href="#projen.python.BlackConfiguration">BlackConfiguration</a>

The uncompromising Python code formatter.

---

##### `cibuildwheel`<sup>Optional</sup> <a name="cibuildwheel" id="projen.python.PythonProjectTool.property.cibuildwheel"></a>

```typescript
public readonly cibuildwheel: any;
```

- *Type:* any

Build Python wheels for all platforms.

---

##### `hatch`<sup>Optional</sup> <a name="hatch" id="projen.python.PythonProjectTool.property.hatch"></a>

```typescript
public readonly hatch: HatchConfiguration;
```

- *Type:* <a href="#projen.python.HatchConfiguration">HatchConfiguration</a>

Modern, extensible Python project management.

---

##### `maturin`<sup>Optional</sup> <a name="maturin" id="projen.python.PythonProjectTool.property.maturin"></a>

```typescript
public readonly maturin: any;
```

- *Type:* any

Build and publish crates with pyo3, cffi and uniffi bindings as well as rust binaries as python packages.

---

##### `mypy`<sup>Optional</sup> <a name="mypy" id="projen.python.PythonProjectTool.property.mypy"></a>

```typescript
public readonly mypy: MypyConfiguration;
```

- *Type:* <a href="#projen.python.MypyConfiguration">MypyConfiguration</a>

Optional static typing for Python.

---

##### `pdm`<sup>Optional</sup> <a name="pdm" id="projen.python.PythonProjectTool.property.pdm"></a>

```typescript
public readonly pdm: PdmConfiguration;
```

- *Type:* <a href="#projen.python.PdmConfiguration">PdmConfiguration</a>

A modern Python package manager with PEP 621 support.

---

##### `poe`<sup>Optional</sup> <a name="poe" id="projen.python.PythonProjectTool.property.poe"></a>

```typescript
public readonly poe: any;
```

- *Type:* any

A task runner that works well with pyproject.toml files.

---

##### `poetry`<sup>Optional</sup> <a name="poetry" id="projen.python.PythonProjectTool.property.poetry"></a>

```typescript
public readonly poetry: PoetryConfiguration;
```

- *Type:* <a href="#projen.python.PoetryConfiguration">PoetryConfiguration</a>

Python dependency management and packaging made easy.

---

##### `pyright`<sup>Optional</sup> <a name="pyright" id="projen.python.PythonProjectTool.property.pyright"></a>

```typescript
public readonly pyright: PyrightConfiguration;
```

- *Type:* <a href="#projen.python.PyrightConfiguration">PyrightConfiguration</a>

Static type checker for Python.

---

##### `pytest`<sup>Optional</sup> <a name="pytest" id="projen.python.PythonProjectTool.property.pytest"></a>

```typescript
public readonly pytest: PytestConfiguration;
```

- *Type:* <a href="#projen.python.PytestConfiguration">PytestConfiguration</a>

Standardized automated testing of Python packages.

---

##### `repoReview`<sup>Optional</sup> <a name="repoReview" id="projen.python.PythonProjectTool.property.repoReview"></a>

```typescript
public readonly repoReview: any;
```

- *Type:* any

Review a repository for best practices.

---

##### `ruff`<sup>Optional</sup> <a name="ruff" id="projen.python.PythonProjectTool.property.ruff"></a>

```typescript
public readonly ruff: RuffConfiguration;
```

- *Type:* <a href="#projen.python.RuffConfiguration">RuffConfiguration</a>

An extremely fast Python linter and formatter, written in Rust.

---

##### `scikitBuild`<sup>Optional</sup> <a name="scikitBuild" id="projen.python.PythonProjectTool.property.scikitBuild"></a>

```typescript
public readonly scikitBuild: any;
```

- *Type:* any

Improved build system generator for Python C/C++/Fortran extensions.

---

##### `setuptools`<sup>Optional</sup> <a name="setuptools" id="projen.python.PythonProjectTool.property.setuptools"></a>

```typescript
public readonly setuptools: SetuptoolsConfiguration;
```

- *Type:* <a href="#projen.python.SetuptoolsConfiguration">SetuptoolsConfiguration</a>

Easily download, build, install, upgrade, and uninstall Python packages.

---

##### `setuptoolsScm`<sup>Optional</sup> <a name="setuptoolsScm" id="projen.python.PythonProjectTool.property.setuptoolsScm"></a>

```typescript
public readonly setuptoolsScm: any;
```

- *Type:* any

Manage Python package versions using SCM (e.g. Git).

---

##### `taskipy`<sup>Optional</sup> <a name="taskipy" id="projen.python.PythonProjectTool.property.taskipy"></a>

```typescript
public readonly taskipy: any;
```

- *Type:* any

The complementary task runner for python.

---

##### `tombi`<sup>Optional</sup> <a name="tombi" id="projen.python.PythonProjectTool.property.tombi"></a>

```typescript
public readonly tombi: any;
```

- *Type:* any

Tombi is a toolkit for TOML;

providing a formatter/linter and language server

---

##### `tox`<sup>Optional</sup> <a name="tox" id="projen.python.PythonProjectTool.property.tox"></a>

```typescript
public readonly tox: any;
```

- *Type:* any

Standardized automated testing of Python packages.

---

##### `ty`<sup>Optional</sup> <a name="ty" id="projen.python.PythonProjectTool.property.ty"></a>

```typescript
public readonly ty: TyConfiguration;
```

- *Type:* <a href="#projen.python.TyConfiguration">TyConfiguration</a>

An extremely fast Python type checker, written in Rust.

---

##### `uv`<sup>Optional</sup> <a name="uv" id="projen.python.PythonProjectTool.property.uv"></a>

```typescript
public readonly uv: UvConfiguration;
```

- *Type:* <a href="#projen.python.UvConfiguration">UvConfiguration</a>

An extremely fast Python package installer and resolver, written in Rust.

---

### PythonSampleOptions <a name="PythonSampleOptions" id="projen.python.PythonSampleOptions"></a>

Options for python sample code.

#### Initializer <a name="Initializer" id="projen.python.PythonSampleOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const pythonSampleOptions: python.PythonSampleOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PythonSampleOptions.property.dir">dir</a></code> | <code>string</code> | Sample code directory. |

---

##### `dir`<sup>Required</sup> <a name="dir" id="projen.python.PythonSampleOptions.property.dir"></a>

```typescript
public readonly dir: string;
```

- *Type:* string

Sample code directory.

---

### PyUpgradeOptions <a name="PyUpgradeOptions" id="projen.python.PyUpgradeOptions"></a>

Options for the `pyupgrade` plugin.

#### Initializer <a name="Initializer" id="projen.python.PyUpgradeOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const pyUpgradeOptions: python.PyUpgradeOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PyUpgradeOptions.property.keepRuntimeTyping">keepRuntimeTyping</a></code> | <code>boolean</code> | Whether to avoid [PEP 585](https://peps.python.org/pep-0585/) (`List[int]` -> `list[int]`) and [PEP 604](https://peps.python.org/pep-0604/) (`Union[str, int]` -> `str \| int`) rewrites even if a file imports `from __future__ import annotations`. |

---

##### `keepRuntimeTyping`<sup>Optional</sup> <a name="keepRuntimeTyping" id="projen.python.PyUpgradeOptions.property.keepRuntimeTyping"></a>

```typescript
public readonly keepRuntimeTyping: boolean;
```

- *Type:* boolean

Whether to avoid [PEP 585](https://peps.python.org/pep-0585/) (`List[int]` -> `list[int]`) and [PEP 604](https://peps.python.org/pep-0604/) (`Union[str, int]` -> `str | int`) rewrites even if a file imports `from __future__ import annotations`.

This setting is only applicable when the target Python version is below
3.9 and 3.10 respectively, and is most commonly used when working with
libraries like Pydantic and FastAPI, which rely on the ability to parse
type annotations at runtime. The use of `from __future__ import annotations`
causes Python to treat the type annotations as strings, which typically
allows for the use of language features that appear in later Python
versions but are not yet supported by the current version (e.g., `str |
int`). However, libraries that rely on runtime type annotations will
break if the annotations are incompatible with the current Python
version.

For example, while the following is valid Python 3.8 code due to the
presence of `from __future__ import annotations`, the use of `str | int`
prior to Python 3.10 will cause Pydantic to raise a `TypeError` at
runtime:

```python
from __future__ import annotations

import pydantic

class Foo(pydantic.BaseModel):
bar: str | int
```

---

### RuffConfiguration <a name="RuffConfiguration" id="projen.python.RuffConfiguration"></a>

#### Initializer <a name="Initializer" id="projen.python.RuffConfiguration.Initializer"></a>

```typescript
import { python } from 'projen'

const ruffConfiguration: python.RuffConfiguration = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.RuffConfiguration.property.allowedConfusables">allowedConfusables</a></code> | <code>string[]</code> | A list of allowed "confusable" Unicode characters to ignore when enforcing `RUF001`, `RUF002`, and `RUF003`. |
| <code><a href="#projen.python.RuffConfiguration.property.analyze">analyze</a></code> | <code><a href="#projen.python.AnalyzeOptions">AnalyzeOptions</a></code> | Options to configure import map generation. |
| <code><a href="#projen.python.RuffConfiguration.property.builtins">builtins</a></code> | <code>string[]</code> | A list of builtins to treat as defined references, in addition to the system builtins. |
| <code><a href="#projen.python.RuffConfiguration.property.cacheDir">cacheDir</a></code> | <code>string</code> | A path to the cache directory. |
| <code><a href="#projen.python.RuffConfiguration.property.dummyVariableRgx">dummyVariableRgx</a></code> | <code>string</code> | A regular expression used to identify "dummy" variables, or those which should be ignored when enforcing (e.g.) unused-variable rules. The default expression matches `_`, `__`, and `_var`, but not `_var_`. |
| <code><a href="#projen.python.RuffConfiguration.property.exclude">exclude</a></code> | <code>string[]</code> | A list of file patterns to exclude from formatting and linting. |
| <code><a href="#projen.python.RuffConfiguration.property.explicitPreviewRules">explicitPreviewRules</a></code> | <code>boolean</code> | Whether to require exact codes to select preview rules. |
| <code><a href="#projen.python.RuffConfiguration.property.extend">extend</a></code> | <code>string</code> | A path to a local `pyproject.toml` or `ruff.toml` file to merge into this configuration. User home directory and environment variables will be expanded. |
| <code><a href="#projen.python.RuffConfiguration.property.extendExclude">extendExclude</a></code> | <code>string[]</code> | A list of file patterns to omit from formatting and linting, in addition to those specified by [`exclude`](#exclude). |
| <code><a href="#projen.python.RuffConfiguration.property.extendFixable">extendFixable</a></code> | <code><a href="#projen.python.RuleSelector">RuleSelector</a>[]</code> | A list of rule codes or prefixes to consider fixable, in addition to those specified by [`fixable`](#lint_fixable). |
| <code><a href="#projen.python.RuffConfiguration.property.extendIgnore">extendIgnore</a></code> | <code><a href="#projen.python.RuleSelector">RuleSelector</a>[]</code> | A list of rule codes or prefixes to ignore, in addition to those specified by `ignore`. |
| <code><a href="#projen.python.RuffConfiguration.property.extendInclude">extendInclude</a></code> | <code>string[]</code> | A list of file patterns to include when linting, in addition to those specified by [`include`](#include). |
| <code><a href="#projen.python.RuffConfiguration.property.extendPerFileIgnores">extendPerFileIgnores</a></code> | <code>{[ key: string ]: <a href="#projen.python.RuleSelector">RuleSelector</a>[]}</code> | A list of mappings from file pattern to rule codes or prefixes to exclude, in addition to any rules excluded by [`per-file-ignores`](#lint_per-file-ignores). |
| <code><a href="#projen.python.RuffConfiguration.property.extendSafeFixes">extendSafeFixes</a></code> | <code><a href="#projen.python.RuleSelector">RuleSelector</a>[]</code> | A list of rule codes or prefixes for which unsafe fixes should be considered safe. |
| <code><a href="#projen.python.RuffConfiguration.property.extendSelect">extendSelect</a></code> | <code><a href="#projen.python.RuleSelector">RuleSelector</a>[]</code> | A list of rule codes or prefixes to enable, in addition to those specified by [`select`](#lint_select). |
| <code><a href="#projen.python.RuffConfiguration.property.extendUnfixable">extendUnfixable</a></code> | <code><a href="#projen.python.RuleSelector">RuleSelector</a>[]</code> | A list of rule codes or prefixes to consider non-auto-fixable, in addition to those specified by [`unfixable`](#lint_unfixable). |
| <code><a href="#projen.python.RuffConfiguration.property.extendUnsafeFixes">extendUnsafeFixes</a></code> | <code><a href="#projen.python.RuleSelector">RuleSelector</a>[]</code> | A list of rule codes or prefixes for which safe fixes should be considered unsafe. |
| <code><a href="#projen.python.RuffConfiguration.property.external">external</a></code> | <code>string[]</code> | A list of rule codes or prefixes that are unsupported by Ruff, but should be preserved when (e.g.) validating `# noqa` directives. Useful for retaining `# noqa` directives that cover plugins not yet implemented by Ruff. |
| <code><a href="#projen.python.RuffConfiguration.property.fix">fix</a></code> | <code>boolean</code> | Enable fix behavior by-default when running `ruff` (overridden by the `--fix` and `--no-fix` command-line flags). |
| <code><a href="#projen.python.RuffConfiguration.property.fixable">fixable</a></code> | <code><a href="#projen.python.RuleSelector">RuleSelector</a>[]</code> | A list of rule codes or prefixes to consider fixable. |
| <code><a href="#projen.python.RuffConfiguration.property.fixOnly">fixOnly</a></code> | <code>boolean</code> | Like [`fix`](#fix), but disables reporting on leftover violation. |
| <code><a href="#projen.python.RuffConfiguration.property.flake8Annotations">flake8Annotations</a></code> | <code><a href="#projen.python.Flake8AnnotationsOptions">Flake8AnnotationsOptions</a></code> | Options for the `flake8-annotations` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.flake8Bandit">flake8Bandit</a></code> | <code><a href="#projen.python.Flake8BanditOptions">Flake8BanditOptions</a></code> | Options for the `flake8-bandit` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.flake8BooleanTrap">flake8BooleanTrap</a></code> | <code><a href="#projen.python.Flake8BooleanTrapOptions">Flake8BooleanTrapOptions</a></code> | Options for the `flake8-boolean-trap` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.flake8Bugbear">flake8Bugbear</a></code> | <code><a href="#projen.python.Flake8BugbearOptions">Flake8BugbearOptions</a></code> | Options for the `flake8-bugbear` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.flake8Builtins">flake8Builtins</a></code> | <code><a href="#projen.python.Flake8BuiltinsOptions">Flake8BuiltinsOptions</a></code> | Options for the `flake8-builtins` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.flake8Comprehensions">flake8Comprehensions</a></code> | <code><a href="#projen.python.Flake8ComprehensionsOptions">Flake8ComprehensionsOptions</a></code> | Options for the `flake8-comprehensions` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.flake8Copyright">flake8Copyright</a></code> | <code><a href="#projen.python.Flake8CopyrightOptions">Flake8CopyrightOptions</a></code> | Options for the `flake8-copyright` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.flake8Errmsg">flake8Errmsg</a></code> | <code><a href="#projen.python.Flake8ErrMsgOptions">Flake8ErrMsgOptions</a></code> | Options for the `flake8-errmsg` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.flake8Gettext">flake8Gettext</a></code> | <code><a href="#projen.python.Flake8GetTextOptions">Flake8GetTextOptions</a></code> | Options for the `flake8-gettext` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.flake8ImplicitStrConcat">flake8ImplicitStrConcat</a></code> | <code><a href="#projen.python.Flake8ImplicitStrConcatOptions">Flake8ImplicitStrConcatOptions</a></code> | Options for the `flake8-implicit-str-concat` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.flake8ImportConventions">flake8ImportConventions</a></code> | <code><a href="#projen.python.Flake8ImportConventionsOptions">Flake8ImportConventionsOptions</a></code> | Options for the `flake8-import-conventions` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.flake8PytestStyle">flake8PytestStyle</a></code> | <code><a href="#projen.python.Flake8PytestStyleOptions">Flake8PytestStyleOptions</a></code> | Options for the `flake8-pytest-style` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.flake8Quotes">flake8Quotes</a></code> | <code><a href="#projen.python.Flake8QuotesOptions">Flake8QuotesOptions</a></code> | Options for the `flake8-quotes` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.flake8Self">flake8Self</a></code> | <code><a href="#projen.python.Flake8SelfOptions">Flake8SelfOptions</a></code> | Options for the `flake8_self` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.flake8TidyImports">flake8TidyImports</a></code> | <code><a href="#projen.python.Flake8TidyImportsOptions">Flake8TidyImportsOptions</a></code> | Options for the `flake8-tidy-imports` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.flake8TypeChecking">flake8TypeChecking</a></code> | <code><a href="#projen.python.Flake8TypeCheckingOptions">Flake8TypeCheckingOptions</a></code> | Options for the `flake8-type-checking` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.flake8UnusedArguments">flake8UnusedArguments</a></code> | <code><a href="#projen.python.Flake8UnusedArgumentsOptions">Flake8UnusedArgumentsOptions</a></code> | Options for the `flake8-unused-arguments` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.forceExclude">forceExclude</a></code> | <code>boolean</code> | Whether to enforce [`exclude`](#exclude) and [`extend-exclude`](#extend-exclude) patterns, even for paths that are passed to Ruff explicitly. |
| <code><a href="#projen.python.RuffConfiguration.property.format">format</a></code> | <code><a href="#projen.python.FormatOptions">FormatOptions</a></code> | Options to configure code formatting. |
| <code><a href="#projen.python.RuffConfiguration.property.ignore">ignore</a></code> | <code><a href="#projen.python.RuleSelector">RuleSelector</a>[]</code> | A list of rule codes or prefixes to ignore. |
| <code><a href="#projen.python.RuffConfiguration.property.ignoreInitModuleImports">ignoreInitModuleImports</a></code> | <code>boolean</code> | Avoid automatically removing unused imports in `__init__.py` files. Such imports will still be flagged, but with a dedicated message suggesting that the import is either added to the module's `__all__` symbol, or re-exported with a redundant alias (e.g., `import os as os`). |
| <code><a href="#projen.python.RuffConfiguration.property.include">include</a></code> | <code>string[]</code> | A list of file patterns to include when linting. |
| <code><a href="#projen.python.RuffConfiguration.property.indentWidth">indentWidth</a></code> | <code>number</code> | The number of spaces per indentation level (tab). |
| <code><a href="#projen.python.RuffConfiguration.property.isort">isort</a></code> | <code><a href="#projen.python.IsortOptions">IsortOptions</a></code> | Options for the `isort` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.lineLength">lineLength</a></code> | <code>number</code> | The line length to use when enforcing long-lines violations (like `E501`) and at which `isort` and the formatter prefers to wrap lines. |
| <code><a href="#projen.python.RuffConfiguration.property.lint">lint</a></code> | <code><a href="#projen.python.LintOptions">LintOptions</a></code> | *No description.* |
| <code><a href="#projen.python.RuffConfiguration.property.loggerObjects">loggerObjects</a></code> | <code>string[]</code> | A list of objects that should be treated equivalently to a `logging.Logger` object. |
| <code><a href="#projen.python.RuffConfiguration.property.mccabe">mccabe</a></code> | <code><a href="#projen.python.McCabeOptions">McCabeOptions</a></code> | Options for the `mccabe` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.namespacePackages">namespacePackages</a></code> | <code>string[]</code> | Mark the specified directories as namespace packages. |
| <code><a href="#projen.python.RuffConfiguration.property.outputFormat">outputFormat</a></code> | <code><a href="#projen.python.OutputFormat">OutputFormat</a></code> | The style in which violation messages should be formatted: `"full"` (default) (shows source), `"concise"`, `"grouped"` (group messages by file), `"json"` (machine-readable), `"junit"` (machine-readable XML), `"github"` (GitHub Actions annotations), `"gitlab"` (GitLab CI code quality report), `"pylint"` (Pylint text format) or `"azure"` (Azure Pipeline logging commands). |
| <code><a href="#projen.python.RuffConfiguration.property.pep8Naming">pep8Naming</a></code> | <code><a href="#projen.python.Pep8NamingOptions">Pep8NamingOptions</a></code> | Options for the `pep8-naming` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.perFileIgnores">perFileIgnores</a></code> | <code>{[ key: string ]: <a href="#projen.python.RuleSelector">RuleSelector</a>[]}</code> | A list of mappings from file pattern to rule codes or prefixes to exclude, when considering any matching files. |
| <code><a href="#projen.python.RuffConfiguration.property.perFileTargetVersion">perFileTargetVersion</a></code> | <code>{[ key: string ]: <a href="#projen.python.PythonVersion">PythonVersion</a>}</code> | A list of mappings from glob-style file pattern to Python version to use when checking the corresponding file(s). |
| <code><a href="#projen.python.RuffConfiguration.property.preview">preview</a></code> | <code>boolean</code> | Whether to enable preview mode. |
| <code><a href="#projen.python.RuffConfiguration.property.pycodestyle">pycodestyle</a></code> | <code><a href="#projen.python.PycodestyleOptions">PycodestyleOptions</a></code> | Options for the `pycodestyle` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.pydocstyle">pydocstyle</a></code> | <code><a href="#projen.python.PydocstyleOptions">PydocstyleOptions</a></code> | Options for the `pydocstyle` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.pyflakes">pyflakes</a></code> | <code><a href="#projen.python.PyflakesOptions">PyflakesOptions</a></code> | Options for the `pyflakes` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.pylint">pylint</a></code> | <code><a href="#projen.python.PylintOptions">PylintOptions</a></code> | Options for the `pylint` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.pyupgrade">pyupgrade</a></code> | <code><a href="#projen.python.PyUpgradeOptions">PyUpgradeOptions</a></code> | Options for the `pyupgrade` plugin. |
| <code><a href="#projen.python.RuffConfiguration.property.requiredVersion">requiredVersion</a></code> | <code>string</code> | Enforce a requirement on the version of Ruff, to enforce at runtime. |
| <code><a href="#projen.python.RuffConfiguration.property.respectGitignore">respectGitignore</a></code> | <code>boolean</code> | Whether to automatically exclude files that are ignored by `.ignore`, `.gitignore`, `.git/info/exclude`, and global `gitignore` files. Enabled by default. |
| <code><a href="#projen.python.RuffConfiguration.property.select">select</a></code> | <code><a href="#projen.python.RuleSelector">RuleSelector</a>[]</code> | A list of rule codes or prefixes to enable. |
| <code><a href="#projen.python.RuffConfiguration.property.showFixes">showFixes</a></code> | <code>boolean</code> | Whether to show an enumeration of all fixed lint violations (overridden by the `--show-fixes` command-line flag). |
| <code><a href="#projen.python.RuffConfiguration.property.src">src</a></code> | <code>string[]</code> | The directories to consider when resolving first- vs. third-party imports. |
| <code><a href="#projen.python.RuffConfiguration.property.targetVersion">targetVersion</a></code> | <code><a href="#projen.python.PythonVersion">PythonVersion</a></code> | The minimum Python version to target, e.g., when considering automatic code upgrades, like rewriting type annotations. Ruff will not propose changes using features that are not available in the given version. |
| <code><a href="#projen.python.RuffConfiguration.property.taskTags">taskTags</a></code> | <code>string[]</code> | A list of task tags to recognize (e.g., "TODO", "FIXME", "XXX"). |
| <code><a href="#projen.python.RuffConfiguration.property.typingModules">typingModules</a></code> | <code>string[]</code> | A list of modules whose exports should be treated equivalently to members of the `typing` module. |
| <code><a href="#projen.python.RuffConfiguration.property.unfixable">unfixable</a></code> | <code><a href="#projen.python.RuleSelector">RuleSelector</a>[]</code> | A list of rule codes or prefixes to consider non-fixable. |
| <code><a href="#projen.python.RuffConfiguration.property.unsafeFixes">unsafeFixes</a></code> | <code>boolean</code> | Enable application of unsafe fixes. |

---

##### `allowedConfusables`<sup>Optional</sup> <a name="allowedConfusables" id="projen.python.RuffConfiguration.property.allowedConfusables"></a>

```typescript
public readonly allowedConfusables: string[];
```

- *Type:* string[]

A list of allowed "confusable" Unicode characters to ignore when enforcing `RUF001`, `RUF002`, and `RUF003`.

---

##### `analyze`<sup>Optional</sup> <a name="analyze" id="projen.python.RuffConfiguration.property.analyze"></a>

```typescript
public readonly analyze: AnalyzeOptions;
```

- *Type:* <a href="#projen.python.AnalyzeOptions">AnalyzeOptions</a>

Options to configure import map generation.

---

##### `builtins`<sup>Optional</sup> <a name="builtins" id="projen.python.RuffConfiguration.property.builtins"></a>

```typescript
public readonly builtins: string[];
```

- *Type:* string[]

A list of builtins to treat as defined references, in addition to the system builtins.

---

##### `cacheDir`<sup>Optional</sup> <a name="cacheDir" id="projen.python.RuffConfiguration.property.cacheDir"></a>

```typescript
public readonly cacheDir: string;
```

- *Type:* string

A path to the cache directory.

By default, Ruff stores cache results in a `.ruff_cache` directory in
the current project root.

However, Ruff will also respect the `RUFF_CACHE_DIR` environment
variable, which takes precedence over that default.

This setting will override even the `RUFF_CACHE_DIR` environment
variable, if set.

---

##### `dummyVariableRgx`<sup>Optional</sup> <a name="dummyVariableRgx" id="projen.python.RuffConfiguration.property.dummyVariableRgx"></a>

```typescript
public readonly dummyVariableRgx: string;
```

- *Type:* string

A regular expression used to identify "dummy" variables, or those which should be ignored when enforcing (e.g.) unused-variable rules. The default expression matches `_`, `__`, and `_var`, but not `_var_`.

---

##### `exclude`<sup>Optional</sup> <a name="exclude" id="projen.python.RuffConfiguration.property.exclude"></a>

```typescript
public readonly exclude: string[];
```

- *Type:* string[]

A list of file patterns to exclude from formatting and linting.

Exclusions are based on globs, and can be either:

- Single-path patterns, like `.mypy_cache` (to exclude any directory
named `.mypy_cache` in the tree), `foo.py` (to exclude any file named
`foo.py`), or `foo_*.py` (to exclude any file matching `foo_*.py` ).
- Relative patterns, like `directory/foo.py` (to exclude that specific
file) or `directory/*.py` (to exclude any Python files in
`directory`). Note that these paths are relative to the project root
(e.g., the directory containing your `pyproject.toml`).

For more information on the glob syntax, refer to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).

Note that you'll typically want to use
[`extend-exclude`](#extend-exclude) to modify the excluded paths.

---

##### `explicitPreviewRules`<sup>Optional</sup> <a name="explicitPreviewRules" id="projen.python.RuffConfiguration.property.explicitPreviewRules"></a>

```typescript
public readonly explicitPreviewRules: boolean;
```

- *Type:* boolean

Whether to require exact codes to select preview rules.

When enabled,
preview rules will not be selected by prefixes — the full code of each
preview rule will be required to enable the rule.

---

##### `extend`<sup>Optional</sup> <a name="extend" id="projen.python.RuffConfiguration.property.extend"></a>

```typescript
public readonly extend: string;
```

- *Type:* string

A path to a local `pyproject.toml` or `ruff.toml` file to merge into this configuration. User home directory and environment variables will be expanded.

To resolve the current configuration file, Ruff will first load
this base configuration file, then merge in properties defined
in the current configuration file. Most settings follow simple override
behavior where the child value replaces the parent value. However,
rule selection (`lint.select` and `lint.ignore`) has special merging
behavior: if the child configuration specifies `lint.select`, it
establishes a new baseline rule set and the parent's `lint.ignore`
rules are discarded; if the child configuration omits `lint.select`,
the parent's rule selection is inherited and both parent and child
`lint.ignore` rules are accumulated together.

---

##### `extendExclude`<sup>Optional</sup> <a name="extendExclude" id="projen.python.RuffConfiguration.property.extendExclude"></a>

```typescript
public readonly extendExclude: string[];
```

- *Type:* string[]

A list of file patterns to omit from formatting and linting, in addition to those specified by [`exclude`](#exclude).

Exclusions are based on globs, and can be either:

- Single-path patterns, like `.mypy_cache` (to exclude any directory
named `.mypy_cache` in the tree), `foo.py` (to exclude any file named
`foo.py`), or `foo_*.py` (to exclude any file matching `foo_*.py` ).
- Relative patterns, like `directory/foo.py` (to exclude that specific
file) or `directory/*.py` (to exclude any Python files in
`directory`). Note that these paths are relative to the project root
(e.g., the directory containing your `pyproject.toml`).

For more information on the glob syntax, refer to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).

---

##### `extendFixable`<sup>Optional</sup> <a name="extendFixable" id="projen.python.RuffConfiguration.property.extendFixable"></a>

```typescript
public readonly extendFixable: RuleSelector[];
```

- *Type:* <a href="#projen.python.RuleSelector">RuleSelector</a>[]

A list of rule codes or prefixes to consider fixable, in addition to those specified by [`fixable`](#lint_fixable).

---

##### `extendIgnore`<sup>Optional</sup> <a name="extendIgnore" id="projen.python.RuffConfiguration.property.extendIgnore"></a>

```typescript
public readonly extendIgnore: RuleSelector[];
```

- *Type:* <a href="#projen.python.RuleSelector">RuleSelector</a>[]

A list of rule codes or prefixes to ignore, in addition to those specified by `ignore`.

---

##### `extendInclude`<sup>Optional</sup> <a name="extendInclude" id="projen.python.RuffConfiguration.property.extendInclude"></a>

```typescript
public readonly extendInclude: string[];
```

- *Type:* string[]

A list of file patterns to include when linting, in addition to those specified by [`include`](#include).

Inclusion are based on globs, and should be single-path patterns, like
`*.pyw`, to include any file with the `.pyw` extension.

For more information on the glob syntax, refer to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).

---

##### `extendPerFileIgnores`<sup>Optional</sup> <a name="extendPerFileIgnores" id="projen.python.RuffConfiguration.property.extendPerFileIgnores"></a>

```typescript
public readonly extendPerFileIgnores: {[ key: string ]: RuleSelector[]};
```

- *Type:* {[ key: string ]: <a href="#projen.python.RuleSelector">RuleSelector</a>[]}

A list of mappings from file pattern to rule codes or prefixes to exclude, in addition to any rules excluded by [`per-file-ignores`](#lint_per-file-ignores).

---

##### `extendSafeFixes`<sup>Optional</sup> <a name="extendSafeFixes" id="projen.python.RuffConfiguration.property.extendSafeFixes"></a>

```typescript
public readonly extendSafeFixes: RuleSelector[];
```

- *Type:* <a href="#projen.python.RuleSelector">RuleSelector</a>[]

A list of rule codes or prefixes for which unsafe fixes should be considered safe.

---

##### `extendSelect`<sup>Optional</sup> <a name="extendSelect" id="projen.python.RuffConfiguration.property.extendSelect"></a>

```typescript
public readonly extendSelect: RuleSelector[];
```

- *Type:* <a href="#projen.python.RuleSelector">RuleSelector</a>[]

A list of rule codes or prefixes to enable, in addition to those specified by [`select`](#lint_select).

---

##### `extendUnfixable`<sup>Optional</sup> <a name="extendUnfixable" id="projen.python.RuffConfiguration.property.extendUnfixable"></a>

```typescript
public readonly extendUnfixable: RuleSelector[];
```

- *Type:* <a href="#projen.python.RuleSelector">RuleSelector</a>[]

A list of rule codes or prefixes to consider non-auto-fixable, in addition to those specified by [`unfixable`](#lint_unfixable).

---

##### `extendUnsafeFixes`<sup>Optional</sup> <a name="extendUnsafeFixes" id="projen.python.RuffConfiguration.property.extendUnsafeFixes"></a>

```typescript
public readonly extendUnsafeFixes: RuleSelector[];
```

- *Type:* <a href="#projen.python.RuleSelector">RuleSelector</a>[]

A list of rule codes or prefixes for which safe fixes should be considered unsafe.

---

##### `external`<sup>Optional</sup> <a name="external" id="projen.python.RuffConfiguration.property.external"></a>

```typescript
public readonly external: string[];
```

- *Type:* string[]

A list of rule codes or prefixes that are unsupported by Ruff, but should be preserved when (e.g.) validating `# noqa` directives. Useful for retaining `# noqa` directives that cover plugins not yet implemented by Ruff.

---

##### `fix`<sup>Optional</sup> <a name="fix" id="projen.python.RuffConfiguration.property.fix"></a>

```typescript
public readonly fix: boolean;
```

- *Type:* boolean

Enable fix behavior by-default when running `ruff` (overridden by the `--fix` and `--no-fix` command-line flags).

Only includes automatic fixes unless `--unsafe-fixes` is provided.

---

##### `fixable`<sup>Optional</sup> <a name="fixable" id="projen.python.RuffConfiguration.property.fixable"></a>

```typescript
public readonly fixable: RuleSelector[];
```

- *Type:* <a href="#projen.python.RuleSelector">RuleSelector</a>[]

A list of rule codes or prefixes to consider fixable.

By default,
all rules are considered fixable.

---

##### `fixOnly`<sup>Optional</sup> <a name="fixOnly" id="projen.python.RuffConfiguration.property.fixOnly"></a>

```typescript
public readonly fixOnly: boolean;
```

- *Type:* boolean

Like [`fix`](#fix), but disables reporting on leftover violation.

Implies [`fix`](#fix).

---

##### `flake8Annotations`<sup>Optional</sup> <a name="flake8Annotations" id="projen.python.RuffConfiguration.property.flake8Annotations"></a>

```typescript
public readonly flake8Annotations: Flake8AnnotationsOptions;
```

- *Type:* <a href="#projen.python.Flake8AnnotationsOptions">Flake8AnnotationsOptions</a>

Options for the `flake8-annotations` plugin.

---

##### `flake8Bandit`<sup>Optional</sup> <a name="flake8Bandit" id="projen.python.RuffConfiguration.property.flake8Bandit"></a>

```typescript
public readonly flake8Bandit: Flake8BanditOptions;
```

- *Type:* <a href="#projen.python.Flake8BanditOptions">Flake8BanditOptions</a>

Options for the `flake8-bandit` plugin.

---

##### `flake8BooleanTrap`<sup>Optional</sup> <a name="flake8BooleanTrap" id="projen.python.RuffConfiguration.property.flake8BooleanTrap"></a>

```typescript
public readonly flake8BooleanTrap: Flake8BooleanTrapOptions;
```

- *Type:* <a href="#projen.python.Flake8BooleanTrapOptions">Flake8BooleanTrapOptions</a>

Options for the `flake8-boolean-trap` plugin.

---

##### `flake8Bugbear`<sup>Optional</sup> <a name="flake8Bugbear" id="projen.python.RuffConfiguration.property.flake8Bugbear"></a>

```typescript
public readonly flake8Bugbear: Flake8BugbearOptions;
```

- *Type:* <a href="#projen.python.Flake8BugbearOptions">Flake8BugbearOptions</a>

Options for the `flake8-bugbear` plugin.

---

##### `flake8Builtins`<sup>Optional</sup> <a name="flake8Builtins" id="projen.python.RuffConfiguration.property.flake8Builtins"></a>

```typescript
public readonly flake8Builtins: Flake8BuiltinsOptions;
```

- *Type:* <a href="#projen.python.Flake8BuiltinsOptions">Flake8BuiltinsOptions</a>

Options for the `flake8-builtins` plugin.

---

##### `flake8Comprehensions`<sup>Optional</sup> <a name="flake8Comprehensions" id="projen.python.RuffConfiguration.property.flake8Comprehensions"></a>

```typescript
public readonly flake8Comprehensions: Flake8ComprehensionsOptions;
```

- *Type:* <a href="#projen.python.Flake8ComprehensionsOptions">Flake8ComprehensionsOptions</a>

Options for the `flake8-comprehensions` plugin.

---

##### `flake8Copyright`<sup>Optional</sup> <a name="flake8Copyright" id="projen.python.RuffConfiguration.property.flake8Copyright"></a>

```typescript
public readonly flake8Copyright: Flake8CopyrightOptions;
```

- *Type:* <a href="#projen.python.Flake8CopyrightOptions">Flake8CopyrightOptions</a>

Options for the `flake8-copyright` plugin.

---

##### `flake8Errmsg`<sup>Optional</sup> <a name="flake8Errmsg" id="projen.python.RuffConfiguration.property.flake8Errmsg"></a>

```typescript
public readonly flake8Errmsg: Flake8ErrMsgOptions;
```

- *Type:* <a href="#projen.python.Flake8ErrMsgOptions">Flake8ErrMsgOptions</a>

Options for the `flake8-errmsg` plugin.

---

##### `flake8Gettext`<sup>Optional</sup> <a name="flake8Gettext" id="projen.python.RuffConfiguration.property.flake8Gettext"></a>

```typescript
public readonly flake8Gettext: Flake8GetTextOptions;
```

- *Type:* <a href="#projen.python.Flake8GetTextOptions">Flake8GetTextOptions</a>

Options for the `flake8-gettext` plugin.

---

##### `flake8ImplicitStrConcat`<sup>Optional</sup> <a name="flake8ImplicitStrConcat" id="projen.python.RuffConfiguration.property.flake8ImplicitStrConcat"></a>

```typescript
public readonly flake8ImplicitStrConcat: Flake8ImplicitStrConcatOptions;
```

- *Type:* <a href="#projen.python.Flake8ImplicitStrConcatOptions">Flake8ImplicitStrConcatOptions</a>

Options for the `flake8-implicit-str-concat` plugin.

---

##### `flake8ImportConventions`<sup>Optional</sup> <a name="flake8ImportConventions" id="projen.python.RuffConfiguration.property.flake8ImportConventions"></a>

```typescript
public readonly flake8ImportConventions: Flake8ImportConventionsOptions;
```

- *Type:* <a href="#projen.python.Flake8ImportConventionsOptions">Flake8ImportConventionsOptions</a>

Options for the `flake8-import-conventions` plugin.

---

##### `flake8PytestStyle`<sup>Optional</sup> <a name="flake8PytestStyle" id="projen.python.RuffConfiguration.property.flake8PytestStyle"></a>

```typescript
public readonly flake8PytestStyle: Flake8PytestStyleOptions;
```

- *Type:* <a href="#projen.python.Flake8PytestStyleOptions">Flake8PytestStyleOptions</a>

Options for the `flake8-pytest-style` plugin.

---

##### `flake8Quotes`<sup>Optional</sup> <a name="flake8Quotes" id="projen.python.RuffConfiguration.property.flake8Quotes"></a>

```typescript
public readonly flake8Quotes: Flake8QuotesOptions;
```

- *Type:* <a href="#projen.python.Flake8QuotesOptions">Flake8QuotesOptions</a>

Options for the `flake8-quotes` plugin.

---

##### `flake8Self`<sup>Optional</sup> <a name="flake8Self" id="projen.python.RuffConfiguration.property.flake8Self"></a>

```typescript
public readonly flake8Self: Flake8SelfOptions;
```

- *Type:* <a href="#projen.python.Flake8SelfOptions">Flake8SelfOptions</a>

Options for the `flake8_self` plugin.

---

##### `flake8TidyImports`<sup>Optional</sup> <a name="flake8TidyImports" id="projen.python.RuffConfiguration.property.flake8TidyImports"></a>

```typescript
public readonly flake8TidyImports: Flake8TidyImportsOptions;
```

- *Type:* <a href="#projen.python.Flake8TidyImportsOptions">Flake8TidyImportsOptions</a>

Options for the `flake8-tidy-imports` plugin.

---

##### `flake8TypeChecking`<sup>Optional</sup> <a name="flake8TypeChecking" id="projen.python.RuffConfiguration.property.flake8TypeChecking"></a>

```typescript
public readonly flake8TypeChecking: Flake8TypeCheckingOptions;
```

- *Type:* <a href="#projen.python.Flake8TypeCheckingOptions">Flake8TypeCheckingOptions</a>

Options for the `flake8-type-checking` plugin.

---

##### `flake8UnusedArguments`<sup>Optional</sup> <a name="flake8UnusedArguments" id="projen.python.RuffConfiguration.property.flake8UnusedArguments"></a>

```typescript
public readonly flake8UnusedArguments: Flake8UnusedArgumentsOptions;
```

- *Type:* <a href="#projen.python.Flake8UnusedArgumentsOptions">Flake8UnusedArgumentsOptions</a>

Options for the `flake8-unused-arguments` plugin.

---

##### `forceExclude`<sup>Optional</sup> <a name="forceExclude" id="projen.python.RuffConfiguration.property.forceExclude"></a>

```typescript
public readonly forceExclude: boolean;
```

- *Type:* boolean

Whether to enforce [`exclude`](#exclude) and [`extend-exclude`](#extend-exclude) patterns, even for paths that are passed to Ruff explicitly.

Typically, Ruff will lint
any paths passed in directly, even if they would typically be
excluded. Setting `force-exclude = true` will cause Ruff to
respect these exclusions unequivocally.

This is useful for [`pre-commit`](https://pre-commit.com/), which explicitly passes all
changed files to the [`ruff-pre-commit`](https://github.com/astral-sh/ruff-pre-commit)
plugin, regardless of whether they're marked as excluded by Ruff's own
settings.

---

##### `format`<sup>Optional</sup> <a name="format" id="projen.python.RuffConfiguration.property.format"></a>

```typescript
public readonly format: FormatOptions;
```

- *Type:* <a href="#projen.python.FormatOptions">FormatOptions</a>

Options to configure code formatting.

---

##### `ignore`<sup>Optional</sup> <a name="ignore" id="projen.python.RuffConfiguration.property.ignore"></a>

```typescript
public readonly ignore: RuleSelector[];
```

- *Type:* <a href="#projen.python.RuleSelector">RuleSelector</a>[]

A list of rule codes or prefixes to ignore.

Prefixes can specify exact
rules (like `F841`), entire categories (like `F`), or anything in
between.

When breaking ties between enabled and disabled rules (via `select` and
`ignore`, respectively), more specific prefixes override less
specific prefixes. `ignore` takes precedence over `select` if the same
prefix appears in both.

---

##### `ignoreInitModuleImports`<sup>Optional</sup> <a name="ignoreInitModuleImports" id="projen.python.RuffConfiguration.property.ignoreInitModuleImports"></a>

```typescript
public readonly ignoreInitModuleImports: boolean;
```

- *Type:* boolean

Avoid automatically removing unused imports in `__init__.py` files. Such imports will still be flagged, but with a dedicated message suggesting that the import is either added to the module's `__all__` symbol, or re-exported with a redundant alias (e.g., `import os as os`).

This option is enabled by default, but you can opt-in to removal of imports
via an unsafe fix.

---

##### `include`<sup>Optional</sup> <a name="include" id="projen.python.RuffConfiguration.property.include"></a>

```typescript
public readonly include: string[];
```

- *Type:* string[]

A list of file patterns to include when linting.

Inclusion are based on globs, and should be single-path patterns, like
`*.pyw`, to include any file with the `.pyw` extension. `pyproject.toml` is
included here not for configuration but because we lint whether e.g. the
`[project]` matches the schema.

Notebook files (`.ipynb` extension) are included by default on Ruff 0.6.0+.

For more information on the glob syntax, refer to the [`globset` documentation](https://docs.rs/globset/latest/globset/#syntax).

---

##### `indentWidth`<sup>Optional</sup> <a name="indentWidth" id="projen.python.RuffConfiguration.property.indentWidth"></a>

```typescript
public readonly indentWidth: number;
```

- *Type:* number

The number of spaces per indentation level (tab).

Used by the formatter and when enforcing long-line violations (like `E501`) to determine the visual
width of a tab.

This option changes the number of spaces the formatter inserts when
using soft-tabs (`indent-style = space`).

PEP 8 recommends using 4 spaces per [indentation level](https://peps.python.org/pep-0008/#indentation).

---

##### `isort`<sup>Optional</sup> <a name="isort" id="projen.python.RuffConfiguration.property.isort"></a>

```typescript
public readonly isort: IsortOptions;
```

- *Type:* <a href="#projen.python.IsortOptions">IsortOptions</a>

Options for the `isort` plugin.

---

##### `lineLength`<sup>Optional</sup> <a name="lineLength" id="projen.python.RuffConfiguration.property.lineLength"></a>

```typescript
public readonly lineLength: number;
```

- *Type:* number

The line length to use when enforcing long-lines violations (like `E501`) and at which `isort` and the formatter prefers to wrap lines.

The length is determined by the number of characters per line, except for lines containing East Asian characters or emojis.
For these lines, the [unicode width](https://unicode.org/reports/tr11/) of each character is added up to determine the length.

The value must be greater than `0` and less than or equal to `320`.

Note: While the formatter will attempt to format lines such that they remain
within the `line-length`, it isn't a hard upper bound, and formatted lines may
exceed the `line-length`.

See [`pycodestyle.max-line-length`](#lint_pycodestyle_max-line-length) to configure different lengths for `E501` and the formatter.

---

##### `lint`<sup>Optional</sup> <a name="lint" id="projen.python.RuffConfiguration.property.lint"></a>

```typescript
public readonly lint: LintOptions;
```

- *Type:* <a href="#projen.python.LintOptions">LintOptions</a>

---

##### `loggerObjects`<sup>Optional</sup> <a name="loggerObjects" id="projen.python.RuffConfiguration.property.loggerObjects"></a>

```typescript
public readonly loggerObjects: string[];
```

- *Type:* string[]

A list of objects that should be treated equivalently to a `logging.Logger` object.

This is useful for ensuring proper diagnostics (e.g., to identify
`logging` deprecations and other best-practices) for projects that
re-export a `logging.Logger` object from a common module.

For example, if you have a module `logging_setup.py` with the following
contents:
```python
import logging

logger = logging.getLogger(__name__)
```

Adding `"logging_setup.logger"` to `logger-objects` will ensure that
`logging_setup.logger` is treated as a `logging.Logger` object when
imported from other modules (e.g., `from logging_setup import logger`).

---

##### `mccabe`<sup>Optional</sup> <a name="mccabe" id="projen.python.RuffConfiguration.property.mccabe"></a>

```typescript
public readonly mccabe: McCabeOptions;
```

- *Type:* <a href="#projen.python.McCabeOptions">McCabeOptions</a>

Options for the `mccabe` plugin.

---

##### `namespacePackages`<sup>Optional</sup> <a name="namespacePackages" id="projen.python.RuffConfiguration.property.namespacePackages"></a>

```typescript
public readonly namespacePackages: string[];
```

- *Type:* string[]

Mark the specified directories as namespace packages.

For the purpose of
module resolution, Ruff will treat those directories and all their subdirectories
as if they contained an `__init__.py` file.

---

##### `outputFormat`<sup>Optional</sup> <a name="outputFormat" id="projen.python.RuffConfiguration.property.outputFormat"></a>

```typescript
public readonly outputFormat: OutputFormat;
```

- *Type:* <a href="#projen.python.OutputFormat">OutputFormat</a>

The style in which violation messages should be formatted: `"full"` (default) (shows source), `"concise"`, `"grouped"` (group messages by file), `"json"` (machine-readable), `"junit"` (machine-readable XML), `"github"` (GitHub Actions annotations), `"gitlab"` (GitLab CI code quality report), `"pylint"` (Pylint text format) or `"azure"` (Azure Pipeline logging commands).

---

##### `pep8Naming`<sup>Optional</sup> <a name="pep8Naming" id="projen.python.RuffConfiguration.property.pep8Naming"></a>

```typescript
public readonly pep8Naming: Pep8NamingOptions;
```

- *Type:* <a href="#projen.python.Pep8NamingOptions">Pep8NamingOptions</a>

Options for the `pep8-naming` plugin.

---

##### `perFileIgnores`<sup>Optional</sup> <a name="perFileIgnores" id="projen.python.RuffConfiguration.property.perFileIgnores"></a>

```typescript
public readonly perFileIgnores: {[ key: string ]: RuleSelector[]};
```

- *Type:* {[ key: string ]: <a href="#projen.python.RuleSelector">RuleSelector</a>[]}

A list of mappings from file pattern to rule codes or prefixes to exclude, when considering any matching files.

An initial '!' negates
the file pattern.

---

##### `perFileTargetVersion`<sup>Optional</sup> <a name="perFileTargetVersion" id="projen.python.RuffConfiguration.property.perFileTargetVersion"></a>

```typescript
public readonly perFileTargetVersion: {[ key: string ]: PythonVersion};
```

- *Type:* {[ key: string ]: <a href="#projen.python.PythonVersion">PythonVersion</a>}

A list of mappings from glob-style file pattern to Python version to use when checking the corresponding file(s).

This may be useful for overriding the global Python version settings in `target-version` or
`requires-python` for a subset of files. For example, if you have a project with a minimum
supported Python version of 3.9 but a subdirectory of developer scripts that want to use a
newer feature like the `match` statement from Python 3.10, you can use
`per-file-target-version` to specify `"developer_scripts/*.py" = "py310"`.

This setting is used by the linter to enforce any enabled version-specific lint rules, as
well as by the formatter for any version-specific formatting options, such as parenthesizing
context managers on Python 3.10+.

---

##### `preview`<sup>Optional</sup> <a name="preview" id="projen.python.RuffConfiguration.property.preview"></a>

```typescript
public readonly preview: boolean;
```

- *Type:* boolean

Whether to enable preview mode.

When preview mode is enabled, Ruff will
use unstable rules, fixes, and formatting.

---

##### `pycodestyle`<sup>Optional</sup> <a name="pycodestyle" id="projen.python.RuffConfiguration.property.pycodestyle"></a>

```typescript
public readonly pycodestyle: PycodestyleOptions;
```

- *Type:* <a href="#projen.python.PycodestyleOptions">PycodestyleOptions</a>

Options for the `pycodestyle` plugin.

---

##### `pydocstyle`<sup>Optional</sup> <a name="pydocstyle" id="projen.python.RuffConfiguration.property.pydocstyle"></a>

```typescript
public readonly pydocstyle: PydocstyleOptions;
```

- *Type:* <a href="#projen.python.PydocstyleOptions">PydocstyleOptions</a>

Options for the `pydocstyle` plugin.

---

##### `pyflakes`<sup>Optional</sup> <a name="pyflakes" id="projen.python.RuffConfiguration.property.pyflakes"></a>

```typescript
public readonly pyflakes: PyflakesOptions;
```

- *Type:* <a href="#projen.python.PyflakesOptions">PyflakesOptions</a>

Options for the `pyflakes` plugin.

---

##### `pylint`<sup>Optional</sup> <a name="pylint" id="projen.python.RuffConfiguration.property.pylint"></a>

```typescript
public readonly pylint: PylintOptions;
```

- *Type:* <a href="#projen.python.PylintOptions">PylintOptions</a>

Options for the `pylint` plugin.

---

##### `pyupgrade`<sup>Optional</sup> <a name="pyupgrade" id="projen.python.RuffConfiguration.property.pyupgrade"></a>

```typescript
public readonly pyupgrade: PyUpgradeOptions;
```

- *Type:* <a href="#projen.python.PyUpgradeOptions">PyUpgradeOptions</a>

Options for the `pyupgrade` plugin.

---

##### `requiredVersion`<sup>Optional</sup> <a name="requiredVersion" id="projen.python.RuffConfiguration.property.requiredVersion"></a>

```typescript
public readonly requiredVersion: string;
```

- *Type:* string

Enforce a requirement on the version of Ruff, to enforce at runtime.

If the version of Ruff does not meet the requirement, Ruff will exit
with an error.

Useful for unifying results across many environments, e.g., with a
`pyproject.toml` file.

Accepts a [PEP 440](https://peps.python.org/pep-0440/) specifier, like `==0.3.1` or `>=0.3.1`.

---

##### `respectGitignore`<sup>Optional</sup> <a name="respectGitignore" id="projen.python.RuffConfiguration.property.respectGitignore"></a>

```typescript
public readonly respectGitignore: boolean;
```

- *Type:* boolean

Whether to automatically exclude files that are ignored by `.ignore`, `.gitignore`, `.git/info/exclude`, and global `gitignore` files. Enabled by default.

---

##### `select`<sup>Optional</sup> <a name="select" id="projen.python.RuffConfiguration.property.select"></a>

```typescript
public readonly select: RuleSelector[];
```

- *Type:* <a href="#projen.python.RuleSelector">RuleSelector</a>[]

A list of rule codes or prefixes to enable.

Prefixes can specify exact
rules (like `F841`), entire categories (like `F`), or anything in
between.

When breaking ties between enabled and disabled rules (via `select` and
`ignore`, respectively), more specific prefixes override less
specific prefixes. `ignore` takes precedence over `select` if the
same prefix appears in both.

---

##### `showFixes`<sup>Optional</sup> <a name="showFixes" id="projen.python.RuffConfiguration.property.showFixes"></a>

```typescript
public readonly showFixes: boolean;
```

- *Type:* boolean

Whether to show an enumeration of all fixed lint violations (overridden by the `--show-fixes` command-line flag).

---

##### `src`<sup>Optional</sup> <a name="src" id="projen.python.RuffConfiguration.property.src"></a>

```typescript
public readonly src: string[];
```

- *Type:* string[]

The directories to consider when resolving first- vs. third-party imports.

When omitted, the `src` directory will typically default to including both:

1. The directory containing the nearest `pyproject.toml`, `ruff.toml`, or `.ruff.toml` file (the "project root").
2. The `"src"` subdirectory of the project root.

These defaults ensure that Ruff supports both flat layouts and `src` layouts out-of-the-box.
(If a configuration file is explicitly provided (e.g., via the `--config` command-line
flag), the current working directory will be considered the project root.)

As an example, consider an alternative project structure, like:

```text
my_project
├── pyproject.toml
└── lib
└── my_package
├── __init__.py
├── foo.py
└── bar.py
```

In this case, the `./lib` directory should be included in the `src` option
(e.g., `src = ["lib"]`), such that when resolving imports, `my_package.foo`
is considered first-party.

This field supports globs. For example, if you have a series of Python
packages in a `python_modules` directory, `src = ["python_modules/*"]`
would expand to incorporate all packages in that directory. User home
directory and environment variables will also be expanded.

---

##### `targetVersion`<sup>Optional</sup> <a name="targetVersion" id="projen.python.RuffConfiguration.property.targetVersion"></a>

```typescript
public readonly targetVersion: PythonVersion;
```

- *Type:* <a href="#projen.python.PythonVersion">PythonVersion</a>

The minimum Python version to target, e.g., when considering automatic code upgrades, like rewriting type annotations. Ruff will not propose changes using features that are not available in the given version.

For example, to represent supporting Python >=3.11 or ==3.11
specify `target-version = "py311"`.

If you're already using a `pyproject.toml` file, we recommend
`project.requires-python` instead, as it's based on Python packaging
standards, and will be respected by other tools. For example, Ruff
treats the following as identical to `target-version = "py38"`:

```toml
[project]
requires-python = ">=3.8"
```

If both are specified, `target-version` takes precedence over
`requires-python`. See [_Inferring the Python version_](https://docs.astral.sh/ruff/configuration/#inferring-the-python-version)
for a complete description of how the `target-version` is determined
when left unspecified.

Note that a stub file can [sometimes make use of a typing feature](https://typing.python.org/en/latest/spec/distributing.html#syntax)
before it is available at runtime, as long as the stub does not make
use of new *syntax*. For example, a type checker will understand
`int | str` in a stub as being a `Union` type annotation, even if the
type checker is run using Python 3.9, despite the fact that the `|`
operator can only be used to create union types at runtime on Python
3.10+. As such, Ruff will often recommend newer features in a stub
file than it would for an equivalent runtime file with the same target
version.

---

##### `taskTags`<sup>Optional</sup> <a name="taskTags" id="projen.python.RuffConfiguration.property.taskTags"></a>

```typescript
public readonly taskTags: string[];
```

- *Type:* string[]

A list of task tags to recognize (e.g., "TODO", "FIXME", "XXX").

Comments starting with these tags will be ignored by commented-out code
detection (`ERA`), and skipped by line-length rules (`E501`) if
[`ignore-overlong-task-comments`](#lint_pycodestyle_ignore-overlong-task-comments) is set to `true`.

---

##### `typingModules`<sup>Optional</sup> <a name="typingModules" id="projen.python.RuffConfiguration.property.typingModules"></a>

```typescript
public readonly typingModules: string[];
```

- *Type:* string[]

A list of modules whose exports should be treated equivalently to members of the `typing` module.

This is useful for ensuring proper type annotation inference for
projects that re-export `typing` and `typing_extensions` members
from a compatibility module. If omitted, any members imported from
modules apart from `typing` and `typing_extensions` will be treated
as ordinary Python objects.

---

##### `unfixable`<sup>Optional</sup> <a name="unfixable" id="projen.python.RuffConfiguration.property.unfixable"></a>

```typescript
public readonly unfixable: RuleSelector[];
```

- *Type:* <a href="#projen.python.RuleSelector">RuleSelector</a>[]

A list of rule codes or prefixes to consider non-fixable.

---

##### `unsafeFixes`<sup>Optional</sup> <a name="unsafeFixes" id="projen.python.RuffConfiguration.property.unsafeFixes"></a>

```typescript
public readonly unsafeFixes: boolean;
```

- *Type:* boolean

Enable application of unsafe fixes.

If excluded, a hint will be displayed when unsafe fixes are available.
If set to false, the hint will be hidden.

---

### RuffOptions <a name="RuffOptions" id="projen.python.RuffOptions"></a>

Options for the `ruff` plugin.

#### Initializer <a name="Initializer" id="projen.python.RuffOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const ruffOptions: python.RuffOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.RuffOptions.property.allowedMarkupCalls">allowedMarkupCalls</a></code> | <code>string[]</code> | A list of callable names, whose result may be safely passed into [`markupsafe.Markup`](https://markupsafe.palletsprojects.com/en/stable/escaping/#markupsafe.Markup). |
| <code><a href="#projen.python.RuffOptions.property.extendMarkupNames">extendMarkupNames</a></code> | <code>string[]</code> | A list of additional callable names that behave like [`markupsafe.Markup`](https://markupsafe.palletsprojects.com/en/stable/escaping/#markupsafe.Markup). |
| <code><a href="#projen.python.RuffOptions.property.parenthesizeTupleInSubscript">parenthesizeTupleInSubscript</a></code> | <code>boolean</code> | Whether to prefer accessing items keyed by tuples with parentheses around the tuple (see `RUF031`). |

---

##### `allowedMarkupCalls`<sup>Optional</sup> <a name="allowedMarkupCalls" id="projen.python.RuffOptions.property.allowedMarkupCalls"></a>

```typescript
public readonly allowedMarkupCalls: string[];
```

- *Type:* string[]

A list of callable names, whose result may be safely passed into [`markupsafe.Markup`](https://markupsafe.palletsprojects.com/en/stable/escaping/#markupsafe.Markup).

Expects to receive a list of fully-qualified names (e.g., `bleach.clean`, rather than `clean`).

This setting helps you avoid false positives in code like:

```python
from bleach import clean
from markupsafe import Markup

cleaned_markup = Markup(clean(some_user_input))
```

Where the use of [`bleach.clean`](https://bleach.readthedocs.io/en/latest/clean.html)
usually ensures that there's no XSS vulnerability.

Although it is not recommended, you may also use this setting to whitelist other
kinds of calls, e.g. calls to i18n translation functions, where how safe that is
will depend on the implementation and how well the translations are audited.

Another common use-case is to wrap the output of functions that generate markup
like [`xml.etree.ElementTree.tostring`](https://docs.python.org/3/library/xml.etree.elementtree.html#xml.etree.ElementTree.tostring)
or template rendering engines where sanitization of potential user input is either
already baked in or has to happen before rendering.

---

##### `extendMarkupNames`<sup>Optional</sup> <a name="extendMarkupNames" id="projen.python.RuffOptions.property.extendMarkupNames"></a>

```typescript
public readonly extendMarkupNames: string[];
```

- *Type:* string[]

A list of additional callable names that behave like [`markupsafe.Markup`](https://markupsafe.palletsprojects.com/en/stable/escaping/#markupsafe.Markup).

Expects to receive a list of fully-qualified names (e.g., `webhelpers.html.literal`, rather than
`literal`).

---

##### `parenthesizeTupleInSubscript`<sup>Optional</sup> <a name="parenthesizeTupleInSubscript" id="projen.python.RuffOptions.property.parenthesizeTupleInSubscript"></a>

```typescript
public readonly parenthesizeTupleInSubscript: boolean;
```

- *Type:* boolean

Whether to prefer accessing items keyed by tuples with parentheses around the tuple (see `RUF031`).

---

### Rules <a name="Rules" id="projen.python.Rules"></a>

#### Initializer <a name="Initializer" id="projen.python.Rules.Initializer"></a>

```typescript
import { python } from 'projen'

const rules: python.Rules = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Rules.property.ambiguousProtocolMember">ambiguousProtocolMember</a></code> | <code>string</code> | ## What it does Checks for protocol classes with members that will lead to ambiguous interfaces. |
| <code><a href="#projen.python.Rules.property.byteStringTypeAnnotation">byteStringTypeAnnotation</a></code> | <code>string</code> | ## What it does Checks for byte-strings in type annotation positions. |
| <code><a href="#projen.python.Rules.property.callNonCallable">callNonCallable</a></code> | <code>string</code> | ## What it does Checks for calls to non-callable objects. |
| <code><a href="#projen.python.Rules.property.conflictingArgumentForms">conflictingArgumentForms</a></code> | <code>string</code> | ## What it does Checks whether an argument is used as both a value and a type form in a call. |
| <code><a href="#projen.python.Rules.property.conflictingDeclarations">conflictingDeclarations</a></code> | <code>string</code> | ## What it does Checks whether a variable has been declared as two conflicting types. |
| <code><a href="#projen.python.Rules.property.conflictingMetaclass">conflictingMetaclass</a></code> | <code>string</code> | ## What it does Checks for class definitions where the metaclass of the class being created would not be a subclass of the metaclasses of all the class's bases. |
| <code><a href="#projen.python.Rules.property.cyclicClassDefinition">cyclicClassDefinition</a></code> | <code>string</code> | ## What it does Checks for class definitions in stub files that inherit (directly or indirectly) from themselves. |
| <code><a href="#projen.python.Rules.property.deprecated">deprecated</a></code> | <code>string</code> | ## What it does Checks for uses of deprecated items. |
| <code><a href="#projen.python.Rules.property.divisionByZero">divisionByZero</a></code> | <code>string</code> | ## What it does It detects division by zero. |
| <code><a href="#projen.python.Rules.property.duplicateBase">duplicateBase</a></code> | <code>string</code> | ## What it does Checks for class definitions with duplicate bases. |
| <code><a href="#projen.python.Rules.property.duplicateKwOnly">duplicateKwOnly</a></code> | <code>string</code> | ## What it does Checks for dataclass definitions with more than one field annotated with `KW_ONLY`. |
| <code><a href="#projen.python.Rules.property.escapeCharacterInForwardAnnotation">escapeCharacterInForwardAnnotation</a></code> | <code>string</code> | TODO #14889. |
| <code><a href="#projen.python.Rules.property.fstringTypeAnnotation">fstringTypeAnnotation</a></code> | <code>string</code> | ## What it does Checks for f-strings in type annotation positions. |
| <code><a href="#projen.python.Rules.property.ignoreCommentUnknownRule">ignoreCommentUnknownRule</a></code> | <code>string</code> | ## What it does Checks for `ty: ignore[code]` where `code` isn't a known lint rule. |
| <code><a href="#projen.python.Rules.property.implicitConcatenatedStringTypeAnnotation">implicitConcatenatedStringTypeAnnotation</a></code> | <code>string</code> | ## What it does Checks for implicit concatenated strings in type annotation positions. |
| <code><a href="#projen.python.Rules.property.inconsistentMro">inconsistentMro</a></code> | <code>string</code> | ## What it does Checks for classes with an inconsistent [method resolution order] (MRO). |
| <code><a href="#projen.python.Rules.property.indexOutOfBounds">indexOutOfBounds</a></code> | <code>string</code> | ## What it does Checks for attempts to use an out of bounds index to get an item from a container. |
| <code><a href="#projen.python.Rules.property.instanceLayoutConflict">instanceLayoutConflict</a></code> | <code>string</code> | ## What it does Checks for classes definitions which will fail at runtime due to "instance memory layout conflicts". |
| <code><a href="#projen.python.Rules.property.invalidArgumentType">invalidArgumentType</a></code> | <code>string</code> | ## What it does Detects call arguments whose type is not assignable to the corresponding typed parameter. |
| <code><a href="#projen.python.Rules.property.invalidAssignment">invalidAssignment</a></code> | <code>string</code> | ## What it does Checks for assignments where the type of the value is not [assignable to] the type of the assignee. |
| <code><a href="#projen.python.Rules.property.invalidAttributeAccess">invalidAttributeAccess</a></code> | <code>string</code> | ## What it does Checks for assignments to class variables from instances and assignments to instance variables from its class. |
| <code><a href="#projen.python.Rules.property.invalidAwait">invalidAwait</a></code> | <code>string</code> | ## What it does Checks for `await` being used with types that are not [Awaitable]. |
| <code><a href="#projen.python.Rules.property.invalidBase">invalidBase</a></code> | <code>string</code> | ## What it does Checks for class definitions that have bases which are not instances of `type`. |
| <code><a href="#projen.python.Rules.property.invalidContextManager">invalidContextManager</a></code> | <code>string</code> | ## What it does Checks for expressions used in `with` statements that do not implement the context manager protocol. |
| <code><a href="#projen.python.Rules.property.invalidDeclaration">invalidDeclaration</a></code> | <code>string</code> | ## What it does Checks for declarations where the inferred type of an existing symbol is not [assignable to] its post-hoc declared type. |
| <code><a href="#projen.python.Rules.property.invalidExceptionCaught">invalidExceptionCaught</a></code> | <code>string</code> | ## What it does Checks for exception handlers that catch non-exception classes. |
| <code><a href="#projen.python.Rules.property.invalidGenericClass">invalidGenericClass</a></code> | <code>string</code> | ## What it does Checks for the creation of invalid generic classes. |
| <code><a href="#projen.python.Rules.property.invalidIgnoreComment">invalidIgnoreComment</a></code> | <code>string</code> | ## What it does Checks for `type: ignore` and `ty: ignore` comments that are syntactically incorrect. |
| <code><a href="#projen.python.Rules.property.invalidKey">invalidKey</a></code> | <code>string</code> | ## What it does Checks for subscript accesses with invalid keys and `TypedDict` construction with an unknown key. |
| <code><a href="#projen.python.Rules.property.invalidLegacyTypeVariable">invalidLegacyTypeVariable</a></code> | <code>string</code> | ## What it does Checks for the creation of invalid legacy `TypeVar`s. |
| <code><a href="#projen.python.Rules.property.invalidMetaclass">invalidMetaclass</a></code> | <code>string</code> | ## What it does Checks for arguments to `metaclass=` that are invalid. |
| <code><a href="#projen.python.Rules.property.invalidNamedTuple">invalidNamedTuple</a></code> | <code>string</code> | ## What it does Checks for invalidly defined `NamedTuple` classes. |
| <code><a href="#projen.python.Rules.property.invalidNewtype">invalidNewtype</a></code> | <code>string</code> | ## What it does Checks for the creation of invalid `NewType`s. |
| <code><a href="#projen.python.Rules.property.invalidOverload">invalidOverload</a></code> | <code>string</code> | ## What it does Checks for various invalid `@overload` usages. |
| <code><a href="#projen.python.Rules.property.invalidParameterDefault">invalidParameterDefault</a></code> | <code>string</code> | ## What it does Checks for default values that can't be assigned to the parameter's annotated type. |
| <code><a href="#projen.python.Rules.property.invalidParamspec">invalidParamspec</a></code> | <code>string</code> | ## What it does Checks for the creation of invalid `ParamSpec`s. |
| <code><a href="#projen.python.Rules.property.invalidProtocol">invalidProtocol</a></code> | <code>string</code> | ## What it does Checks for protocol classes that will raise `TypeError` at runtime. |
| <code><a href="#projen.python.Rules.property.invalidRaise">invalidRaise</a></code> | <code>string</code> | Checks for `raise` statements that raise non-exceptions or use invalid causes for their raised exceptions. |
| <code><a href="#projen.python.Rules.property.invalidReturnType">invalidReturnType</a></code> | <code>string</code> | ## What it does Detects returned values that can't be assigned to the function's annotated return type. |
| <code><a href="#projen.python.Rules.property.invalidSuperArgument">invalidSuperArgument</a></code> | <code>string</code> | ## What it does Detects `super()` calls where: - the first argument is not a valid class literal, or - the second argument is not an instance or subclass of the first argument. |
| <code><a href="#projen.python.Rules.property.invalidSyntaxInForwardAnnotation">invalidSyntaxInForwardAnnotation</a></code> | <code>string</code> | TODO #14889. |
| <code><a href="#projen.python.Rules.property.invalidTypeAliasType">invalidTypeAliasType</a></code> | <code>string</code> | ## What it does Checks for the creation of invalid `TypeAliasType`s. |
| <code><a href="#projen.python.Rules.property.invalidTypeCheckingConstant">invalidTypeCheckingConstant</a></code> | <code>string</code> | ## What it does Checks for a value other than `False` assigned to the `TYPE_CHECKING` variable, or an annotation not assignable from `bool`. |
| <code><a href="#projen.python.Rules.property.invalidTypeForm">invalidTypeForm</a></code> | <code>string</code> | ## What it does Checks for expressions that are used as [type expressions] but cannot validly be interpreted as such. |
| <code><a href="#projen.python.Rules.property.invalidTypeGuardCall">invalidTypeGuardCall</a></code> | <code>string</code> | ## What it does Checks for type guard function calls without a valid target. |
| <code><a href="#projen.python.Rules.property.invalidTypeGuardDefinition">invalidTypeGuardDefinition</a></code> | <code>string</code> | ## What it does Checks for type guard functions without a first non-self-like non-keyword-only non-variadic parameter. |
| <code><a href="#projen.python.Rules.property.invalidTypeVariableConstraints">invalidTypeVariableConstraints</a></code> | <code>string</code> | ## What it does Checks for constrained [type variables] with only one constraint. |
| <code><a href="#projen.python.Rules.property.missingArgument">missingArgument</a></code> | <code>string</code> | ## What it does Checks for missing required arguments in a call. |
| <code><a href="#projen.python.Rules.property.missingTypedDictKey">missingTypedDictKey</a></code> | <code>string</code> | ## What it does Detects missing required keys in `TypedDict` constructor calls. |
| <code><a href="#projen.python.Rules.property.noMatchingOverload">noMatchingOverload</a></code> | <code>string</code> | ## What it does Checks for calls to an overloaded function that do not match any of the overloads. |
| <code><a href="#projen.python.Rules.property.nonSubscriptable">nonSubscriptable</a></code> | <code>string</code> | ## What it does Checks for subscripting objects that do not support subscripting. |
| <code><a href="#projen.python.Rules.property.notIterable">notIterable</a></code> | <code>string</code> | ## What it does Checks for objects that are not iterable but are used in a context that requires them to be. |
| <code><a href="#projen.python.Rules.property.parameterAlreadyAssigned">parameterAlreadyAssigned</a></code> | <code>string</code> | ## What it does Checks for calls which provide more than one argument for a single parameter. |
| <code><a href="#projen.python.Rules.property.positionalOnlyParameterAsKwarg">positionalOnlyParameterAsKwarg</a></code> | <code>string</code> | ## What it does Checks for keyword arguments in calls that match positional-only parameters of the callable. |
| <code><a href="#projen.python.Rules.property.possiblyMissingAttribute">possiblyMissingAttribute</a></code> | <code>string</code> | ## What it does Checks for possibly missing attributes. |
| <code><a href="#projen.python.Rules.property.possiblyMissingImplicitCall">possiblyMissingImplicitCall</a></code> | <code>string</code> | ## What it does Checks for implicit calls to possibly missing methods. |
| <code><a href="#projen.python.Rules.property.possiblyMissingImport">possiblyMissingImport</a></code> | <code>string</code> | ## What it does Checks for imports of symbols that may be missing. |
| <code><a href="#projen.python.Rules.property.possiblyUnresolvedReference">possiblyUnresolvedReference</a></code> | <code>string</code> | ## What it does Checks for references to names that are possibly not defined. |
| <code><a href="#projen.python.Rules.property.rawStringTypeAnnotation">rawStringTypeAnnotation</a></code> | <code>string</code> | ## What it does Checks for raw-strings in type annotation positions. |
| <code><a href="#projen.python.Rules.property.redundantCast">redundantCast</a></code> | <code>string</code> | ## What it does Detects redundant `cast` calls where the value already has the target type. |
| <code><a href="#projen.python.Rules.property.staticAssertError">staticAssertError</a></code> | <code>string</code> | ## What it does Makes sure that the argument of `static_assert` is statically known to be true. |
| <code><a href="#projen.python.Rules.property.subclassOfFinalClass">subclassOfFinalClass</a></code> | <code>string</code> | ## What it does Checks for classes that subclass final classes. |
| <code><a href="#projen.python.Rules.property.tooManyPositionalArguments">tooManyPositionalArguments</a></code> | <code>string</code> | ## What it does Checks for calls that pass more positional arguments than the callable can accept. |
| <code><a href="#projen.python.Rules.property.typeAssertionFailure">typeAssertionFailure</a></code> | <code>string</code> | ## What it does Checks for `assert_type()` and `assert_never()` calls where the actual type is not the same as the asserted type. |
| <code><a href="#projen.python.Rules.property.unavailableImplicitSuperArguments">unavailableImplicitSuperArguments</a></code> | <code>string</code> | ## What it does Detects invalid `super()` calls where implicit arguments like the enclosing class or first method argument are unavailable. |
| <code><a href="#projen.python.Rules.property.undefinedReveal">undefinedReveal</a></code> | <code>string</code> | ## What it does Checks for calls to `reveal_type` without importing it. |
| <code><a href="#projen.python.Rules.property.unknownArgument">unknownArgument</a></code> | <code>string</code> | ## What it does Checks for keyword arguments in calls that don't match any parameter of the callable. |
| <code><a href="#projen.python.Rules.property.unresolvedAttribute">unresolvedAttribute</a></code> | <code>string</code> | ## What it does Checks for unresolved attributes. |
| <code><a href="#projen.python.Rules.property.unresolvedGlobal">unresolvedGlobal</a></code> | <code>string</code> | ## What it does Detects variables declared as `global` in an inner scope that have no explicit bindings or declarations in the global scope. |
| <code><a href="#projen.python.Rules.property.unresolvedImport">unresolvedImport</a></code> | <code>string</code> | ## What it does Checks for import statements for which the module cannot be resolved. |
| <code><a href="#projen.python.Rules.property.unresolvedReference">unresolvedReference</a></code> | <code>string</code> | ## What it does Checks for references to names that are not defined. |
| <code><a href="#projen.python.Rules.property.unsupportedBase">unsupportedBase</a></code> | <code>string</code> | ## What it does Checks for class definitions that have bases which are unsupported by ty. |
| <code><a href="#projen.python.Rules.property.unsupportedBoolConversion">unsupportedBoolConversion</a></code> | <code>string</code> | ## What it does Checks for bool conversions where the object doesn't correctly implement `__bool__`. |
| <code><a href="#projen.python.Rules.property.unsupportedOperator">unsupportedOperator</a></code> | <code>string</code> | ## What it does Checks for binary expressions, comparisons, and unary expressions where the operands don't support the operator. |
| <code><a href="#projen.python.Rules.property.unusedIgnoreComment">unusedIgnoreComment</a></code> | <code>string</code> | ## What it does Checks for `type: ignore` or `ty: ignore` directives that are no longer applicable. |
| <code><a href="#projen.python.Rules.property.uselessOverloadBody">uselessOverloadBody</a></code> | <code>string</code> | ## What it does Checks for various `@overload`-decorated functions that have non-stub bodies. |
| <code><a href="#projen.python.Rules.property.zeroStepsizeInSlice">zeroStepsizeInSlice</a></code> | <code>string</code> | ## What it does Checks for step size 0 in slices. |

---

##### `ambiguousProtocolMember`<sup>Optional</sup> <a name="ambiguousProtocolMember" id="projen.python.Rules.property.ambiguousProtocolMember"></a>

```typescript
public readonly ambiguousProtocolMember: string;
```

- *Type:* string

## What it does Checks for protocol classes with members that will lead to ambiguous interfaces.

## Why is this bad?
Assigning to an undeclared variable in a protocol class leads to an ambiguous
interface which may lead to the type checker inferring unexpected things. It's
recommended to ensure that all members of a protocol class are explicitly declared.

## Examples

```py
from typing import Protocol

class BaseProto(Protocol):
a: int                               # fine (explicitly declared as `int`)
def method_member(self) -> int: ...  # fine: a method definition using `def` is considered a declaration
c = "some variable"                  # error: no explicit declaration, leading to ambiguity
b = method_member                    # error: no explicit declaration, leading to ambiguity

# error: this creates implicit assignments of `d` and `e` in the protocol class body.
# Were they really meant to be considered protocol members?
for d, e in enumerate(range(42)):
pass

class SubProto(BaseProto, Protocol):
a = 42  # fine (declared in superclass)
```

---

##### `byteStringTypeAnnotation`<sup>Optional</sup> <a name="byteStringTypeAnnotation" id="projen.python.Rules.property.byteStringTypeAnnotation"></a>

```typescript
public readonly byteStringTypeAnnotation: string;
```

- *Type:* string

## What it does Checks for byte-strings in type annotation positions.

## Why is this bad?
Static analysis tools like ty can't analyze type annotations that use byte-string notation.

## Examples
```python
def test(): -> b"int":
...
```

Use instead:
```python
def test(): -> "int":
...
```

---

##### `callNonCallable`<sup>Optional</sup> <a name="callNonCallable" id="projen.python.Rules.property.callNonCallable"></a>

```typescript
public readonly callNonCallable: string;
```

- *Type:* string

## What it does Checks for calls to non-callable objects.

## Why is this bad?
Calling a non-callable object will raise a `TypeError` at runtime.

## Examples
```python
4()  # TypeError: 'int' object is not callable
```

---

##### `conflictingArgumentForms`<sup>Optional</sup> <a name="conflictingArgumentForms" id="projen.python.Rules.property.conflictingArgumentForms"></a>

```typescript
public readonly conflictingArgumentForms: string;
```

- *Type:* string

## What it does Checks whether an argument is used as both a value and a type form in a call.

## Why is this bad?
Such calls have confusing semantics and often indicate a logic error.

## Examples
```python
from typing import reveal_type
from ty_extensions import is_singleton

if flag:
f = repr  # Expects a value
else:
f = is_singleton  # Expects a type form

f(int)  # error
```

---

##### `conflictingDeclarations`<sup>Optional</sup> <a name="conflictingDeclarations" id="projen.python.Rules.property.conflictingDeclarations"></a>

```typescript
public readonly conflictingDeclarations: string;
```

- *Type:* string

## What it does Checks whether a variable has been declared as two conflicting types.

## Why is this bad
A variable with two conflicting declarations likely indicates a mistake.
Moreover, it could lead to incorrect or ill-defined type inference for
other code that relies on these variables.

## Examples
```python
if b:
a: int
else:
a: str

a = 1
```

---

##### `conflictingMetaclass`<sup>Optional</sup> <a name="conflictingMetaclass" id="projen.python.Rules.property.conflictingMetaclass"></a>

```typescript
public readonly conflictingMetaclass: string;
```

- *Type:* string

## What it does Checks for class definitions where the metaclass of the class being created would not be a subclass of the metaclasses of all the class's bases.

## Why is it bad?
Such a class definition raises a `TypeError` at runtime.

## Examples
```python
class M1(type): ...
class M2(type): ...
class A(metaclass=M1): ...
class B(metaclass=M2): ...

# TypeError: metaclass conflict
class C(A, B): ...
```

---

##### `cyclicClassDefinition`<sup>Optional</sup> <a name="cyclicClassDefinition" id="projen.python.Rules.property.cyclicClassDefinition"></a>

```typescript
public readonly cyclicClassDefinition: string;
```

- *Type:* string

## What it does Checks for class definitions in stub files that inherit (directly or indirectly) from themselves.

## Why is it bad?
Although forward references are natively supported in stub files,
inheritance cycles are still disallowed, as it is impossible to
resolve a consistent [method resolution order] for a class that
inherits from itself.

## Examples
```python
# foo.pyi
class A(B): ...
class B(A): ...
```

[method resolution order]: https://docs.python.org/3/glossary.html#term-method-resolution-order

---

##### `deprecated`<sup>Optional</sup> <a name="deprecated" id="projen.python.Rules.property.deprecated"></a>

```typescript
public readonly deprecated: string;
```

- *Type:* string

## What it does Checks for uses of deprecated items.

## Why is this bad?
Deprecated items should no longer be used.

## Examples
```python

---

##### `divisionByZero`<sup>Optional</sup> <a name="divisionByZero" id="projen.python.Rules.property.divisionByZero"></a>

```typescript
public readonly divisionByZero: string;
```

- *Type:* string

## What it does It detects division by zero.

## Why is this bad?
Dividing by zero raises a `ZeroDivisionError` at runtime.

## Examples
```python
5 / 0
```

---

##### `duplicateBase`<sup>Optional</sup> <a name="duplicateBase" id="projen.python.Rules.property.duplicateBase"></a>

```typescript
public readonly duplicateBase: string;
```

- *Type:* string

## What it does Checks for class definitions with duplicate bases.

## Why is this bad?
Class definitions with duplicate bases raise `TypeError` at runtime.

## Examples
```python
class A: ...

# TypeError: duplicate base class
class B(A, A): ...
```

---

##### `duplicateKwOnly`<sup>Optional</sup> <a name="duplicateKwOnly" id="projen.python.Rules.property.duplicateKwOnly"></a>

```typescript
public readonly duplicateKwOnly: string;
```

- *Type:* string

## What it does Checks for dataclass definitions with more than one field annotated with `KW_ONLY`.

## Why is this bad?
`dataclasses.KW_ONLY` is a special marker used to
emulate the `*` syntax in normal signatures.
It can only be used once per dataclass.

Attempting to annotate two different fields with
it will lead to a runtime error.

## Examples
```python
from dataclasses import dataclass, KW_ONLY

---

##### `escapeCharacterInForwardAnnotation`<sup>Optional</sup> <a name="escapeCharacterInForwardAnnotation" id="projen.python.Rules.property.escapeCharacterInForwardAnnotation"></a>

```typescript
public readonly escapeCharacterInForwardAnnotation: string;
```

- *Type:* string

TODO #14889.

---

##### `fstringTypeAnnotation`<sup>Optional</sup> <a name="fstringTypeAnnotation" id="projen.python.Rules.property.fstringTypeAnnotation"></a>

```typescript
public readonly fstringTypeAnnotation: string;
```

- *Type:* string

## What it does Checks for f-strings in type annotation positions.

## Why is this bad?
Static analysis tools like ty can't analyze type annotations that use f-string notation.

## Examples
```python
def test(): -> f"int":
...
```

Use instead:
```python
def test(): -> "int":
...
```

---

##### `ignoreCommentUnknownRule`<sup>Optional</sup> <a name="ignoreCommentUnknownRule" id="projen.python.Rules.property.ignoreCommentUnknownRule"></a>

```typescript
public readonly ignoreCommentUnknownRule: string;
```

- *Type:* string

## What it does Checks for `ty: ignore[code]` where `code` isn't a known lint rule.

## Why is this bad?
A `ty: ignore[code]` directive with a `code` that doesn't match
any known rule will not suppress any type errors, and is probably a mistake.

## Examples
```py
a = 20 / 0  # ty: ignore[division-by-zer]
```

Use instead:

```py
a = 20 / 0  # ty: ignore[division-by-zero]
```

---

##### `implicitConcatenatedStringTypeAnnotation`<sup>Optional</sup> <a name="implicitConcatenatedStringTypeAnnotation" id="projen.python.Rules.property.implicitConcatenatedStringTypeAnnotation"></a>

```typescript
public readonly implicitConcatenatedStringTypeAnnotation: string;
```

- *Type:* string

## What it does Checks for implicit concatenated strings in type annotation positions.

## Why is this bad?
Static analysis tools like ty can't analyze type annotations that use implicit concatenated strings.

## Examples
```python
def test(): -> "Literal[" "5" "]":
...
```

Use instead:
```python
def test(): -> "Literal[5]":
...
```

---

##### `inconsistentMro`<sup>Optional</sup> <a name="inconsistentMro" id="projen.python.Rules.property.inconsistentMro"></a>

```typescript
public readonly inconsistentMro: string;
```

- *Type:* string

## What it does Checks for classes with an inconsistent [method resolution order] (MRO).

## Why is this bad?
Classes with an inconsistent MRO will raise a `TypeError` at runtime.

## Examples
```python
class A: ...
class B(A): ...

# TypeError: Cannot create a consistent method resolution order
class C(A, B): ...
```

[method resolution order]: https://docs.python.org/3/glossary.html#term-method-resolution-order

---

##### `indexOutOfBounds`<sup>Optional</sup> <a name="indexOutOfBounds" id="projen.python.Rules.property.indexOutOfBounds"></a>

```typescript
public readonly indexOutOfBounds: string;
```

- *Type:* string

## What it does Checks for attempts to use an out of bounds index to get an item from a container.

## Why is this bad?
Using an out of bounds index will raise an `IndexError` at runtime.

## Examples
```python
t = (0, 1, 2)
t[3]  # IndexError: tuple index out of range
```

---

##### `instanceLayoutConflict`<sup>Optional</sup> <a name="instanceLayoutConflict" id="projen.python.Rules.property.instanceLayoutConflict"></a>

```typescript
public readonly instanceLayoutConflict: string;
```

- *Type:* string

## What it does Checks for classes definitions which will fail at runtime due to "instance memory layout conflicts".

This error is usually caused by attempting to combine multiple classes
that define non-empty `__slots__` in a class's [Method Resolution Order]
(MRO), or by attempting to combine multiple builtin classes in a class's
MRO.

## Why is this bad?
Inheriting from bases with conflicting instance memory layouts
will lead to a `TypeError` at runtime.

An instance memory layout conflict occurs when CPython cannot determine
the memory layout instances of a class should have, because the instance
memory layout of one of its bases conflicts with the instance memory layout
of one or more of its other bases.

For example, if a Python class defines non-empty `__slots__`, this will
impact the memory layout of instances of that class. Multiple inheritance
from more than one different class defining non-empty `__slots__` is not
allowed:

```python
class A:
__slots__ = ("a", "b")

class B:
__slots__ = ("a", "b")  # Even if the values are the same

# TypeError: multiple bases have instance lay-out conflict
class C(A, B): ...
```

An instance layout conflict can also be caused by attempting to use
multiple inheritance with two builtin classes, due to the way that these
classes are implemented in a CPython C extension:

```python
class A(int, float): ...  # TypeError: multiple bases have instance lay-out conflict
```

Note that pure-Python classes with no `__slots__`, or pure-Python classes
with empty `__slots__`, are always compatible:

```python
class A: ...
class B:
__slots__ = ()
class C:
__slots__ = ("a", "b")

# fine
class D(A, B, C): ...
```

## Known problems
Classes that have "dynamic" definitions of `__slots__` (definitions do not consist
of string literals, or tuples of string literals) are not currently considered disjoint
bases by ty.

Additionally, this check is not exhaustive: many C extensions (including several in
the standard library) define classes that use extended memory layouts and thus cannot
coexist in a single MRO. Since it is currently not possible to represent this fact in
stub files, having a full knowledge of these classes is also impossible. When it comes
to classes that do not define `__slots__` at the Python level, therefore, ty, currently
only hard-codes a number of cases where it knows that a class will produce instances with
an atypical memory layout.

## Further reading
- [CPython documentation: `__slots__`](https://docs.python.org/3/reference/datamodel.html#slots)
- [CPython documentation: Method Resolution Order](https://docs.python.org/3/glossary.html#term-method-resolution-order)

[Method Resolution Order]: https://docs.python.org/3/glossary.html#term-method-resolution-order

---

##### `invalidArgumentType`<sup>Optional</sup> <a name="invalidArgumentType" id="projen.python.Rules.property.invalidArgumentType"></a>

```typescript
public readonly invalidArgumentType: string;
```

- *Type:* string

## What it does Detects call arguments whose type is not assignable to the corresponding typed parameter.

## Why is this bad?
Passing an argument of a type the function (or callable object) does not accept violates
the expectations of the function author and may cause unexpected runtime errors within the
body of the function.

## Examples
```python
def func(x: int): ...
func("foo")  # error: [invalid-argument-type]
```

---

##### `invalidAssignment`<sup>Optional</sup> <a name="invalidAssignment" id="projen.python.Rules.property.invalidAssignment"></a>

```typescript
public readonly invalidAssignment: string;
```

- *Type:* string

## What it does Checks for assignments where the type of the value is not [assignable to] the type of the assignee.

## Why is this bad?
Such assignments break the rules of the type system and
weaken a type checker's ability to accurately reason about your code.

## Examples
```python
a: int = ''
```

[assignable to]: https://typing.python.org/en/latest/spec/glossary.html#term-assignable

---

##### `invalidAttributeAccess`<sup>Optional</sup> <a name="invalidAttributeAccess" id="projen.python.Rules.property.invalidAttributeAccess"></a>

```typescript
public readonly invalidAttributeAccess: string;
```

- *Type:* string

## What it does Checks for assignments to class variables from instances and assignments to instance variables from its class.

## Why is this bad?
Incorrect assignments break the rules of the type system and
weaken a type checker's ability to accurately reason about your code.

## Examples
```python
class C:
class_var: ClassVar[int] = 1
instance_var: int

C.class_var = 3  # okay
C().class_var = 3  # error: Cannot assign to class variable

C().instance_var = 3  # okay
C.instance_var = 3  # error: Cannot assign to instance variable
```

---

##### `invalidAwait`<sup>Optional</sup> <a name="invalidAwait" id="projen.python.Rules.property.invalidAwait"></a>

```typescript
public readonly invalidAwait: string;
```

- *Type:* string

## What it does Checks for `await` being used with types that are not [Awaitable].

## Why is this bad?
Such expressions will lead to `TypeError` being raised at runtime.

## Examples
```python
import asyncio

class InvalidAwait:
def __await__(self) -> int:
return 5

async def main() -> None:
await InvalidAwait()  # error: [invalid-await]
await 42  # error: [invalid-await]

asyncio.run(main())
```

[Awaitable]: https://docs.python.org/3/library/collections.abc.html#collections.abc.Awaitable

---

##### `invalidBase`<sup>Optional</sup> <a name="invalidBase" id="projen.python.Rules.property.invalidBase"></a>

```typescript
public readonly invalidBase: string;
```

- *Type:* string

## What it does Checks for class definitions that have bases which are not instances of `type`.

## Why is this bad?
Class definitions with bases like this will lead to `TypeError` being raised at runtime.

## Examples
```python
class A(42): ...  # error: [invalid-base]
```

---

##### `invalidContextManager`<sup>Optional</sup> <a name="invalidContextManager" id="projen.python.Rules.property.invalidContextManager"></a>

```typescript
public readonly invalidContextManager: string;
```

- *Type:* string

## What it does Checks for expressions used in `with` statements that do not implement the context manager protocol.

## Why is this bad?
Such a statement will raise `TypeError` at runtime.

## Examples
```python
# TypeError: 'int' object does not support the context manager protocol
with 1:
print(2)
```

---

##### `invalidDeclaration`<sup>Optional</sup> <a name="invalidDeclaration" id="projen.python.Rules.property.invalidDeclaration"></a>

```typescript
public readonly invalidDeclaration: string;
```

- *Type:* string

## What it does Checks for declarations where the inferred type of an existing symbol is not [assignable to] its post-hoc declared type.

## Why is this bad?
Such declarations break the rules of the type system and
weaken a type checker's ability to accurately reason about your code.

## Examples
```python
a = 1
a: str
```

[assignable to]: https://typing.python.org/en/latest/spec/glossary.html#term-assignable

---

##### `invalidExceptionCaught`<sup>Optional</sup> <a name="invalidExceptionCaught" id="projen.python.Rules.property.invalidExceptionCaught"></a>

```typescript
public readonly invalidExceptionCaught: string;
```

- *Type:* string

## What it does Checks for exception handlers that catch non-exception classes.

## Why is this bad?
Catching classes that do not inherit from `BaseException` will raise a `TypeError` at runtime.

## Example
```python
try:
1 / 0
except 1:
...
```

Use instead:
```python
try:
1 / 0
except ZeroDivisionError:
...
```

## References
- [Python documentation: except clause](https://docs.python.org/3/reference/compound_stmts.html#except-clause)
- [Python documentation: Built-in Exceptions](https://docs.python.org/3/library/exceptions.html#built-in-exceptions)

## Ruff rule
This rule corresponds to Ruff's [`except-with-non-exception-classes` (`B030`)](https://docs.astral.sh/ruff/rules/except-with-non-exception-classes)

---

##### `invalidGenericClass`<sup>Optional</sup> <a name="invalidGenericClass" id="projen.python.Rules.property.invalidGenericClass"></a>

```typescript
public readonly invalidGenericClass: string;
```

- *Type:* string

## What it does Checks for the creation of invalid generic classes.

## Why is this bad?
There are several requirements that you must follow when defining a generic class.

## Examples
```python
from typing import Generic, TypeVar

T = TypeVar("T")  # okay

# error: class uses both PEP-695 syntax and legacy syntax
class C[U](Generic[T]): ...
```

## References
- [Typing spec: Generics](https://typing.python.org/en/latest/spec/generics.html#introduction)

---

##### `invalidIgnoreComment`<sup>Optional</sup> <a name="invalidIgnoreComment" id="projen.python.Rules.property.invalidIgnoreComment"></a>

```typescript
public readonly invalidIgnoreComment: string;
```

- *Type:* string

## What it does Checks for `type: ignore` and `ty: ignore` comments that are syntactically incorrect.

## Why is this bad?
A syntactically incorrect ignore comment is probably a mistake and is useless.

## Examples
```py
a = 20 / 0  # type: ignoree
```

Use instead:

```py
a = 20 / 0  # type: ignore
```

---

##### `invalidKey`<sup>Optional</sup> <a name="invalidKey" id="projen.python.Rules.property.invalidKey"></a>

```typescript
public readonly invalidKey: string;
```

- *Type:* string

## What it does Checks for subscript accesses with invalid keys and `TypedDict` construction with an unknown key.

## Why is this bad?
Subscripting with an invalid key will raise a `KeyError` at runtime.

Creating a `TypedDict` with an unknown key is likely a mistake; if the `TypedDict` is
`closed=true` it also violates the expectations of the type.

## Examples
```python
from typing import TypedDict

class Person(TypedDict):
name: str
age: int

alice = Person(name="Alice", age=30)
alice["height"]  # KeyError: 'height'

bob: Person = { "name": "Bob", "age": 30 }  # typo!

carol = Person(name="Carol", age=25)  # typo!
```

---

##### `invalidLegacyTypeVariable`<sup>Optional</sup> <a name="invalidLegacyTypeVariable" id="projen.python.Rules.property.invalidLegacyTypeVariable"></a>

```typescript
public readonly invalidLegacyTypeVariable: string;
```

- *Type:* string

## What it does Checks for the creation of invalid legacy `TypeVar`s.

## Why is this bad?
There are several requirements that you must follow when creating a legacy `TypeVar`.

## Examples
```python
from typing import TypeVar

T = TypeVar("T")  # okay
Q = TypeVar("S")  # error: TypeVar name must match the variable it's assigned to
T = TypeVar("T")  # error: TypeVars should not be redefined

# error: TypeVar must be immediately assigned to a variable
def f(t: TypeVar("U")): ...
```

## References
- [Typing spec: Generics](https://typing.python.org/en/latest/spec/generics.html#introduction)

---

##### `invalidMetaclass`<sup>Optional</sup> <a name="invalidMetaclass" id="projen.python.Rules.property.invalidMetaclass"></a>

```typescript
public readonly invalidMetaclass: string;
```

- *Type:* string

## What it does Checks for arguments to `metaclass=` that are invalid.

## Why is this bad?
Python allows arbitrary expressions to be used as the argument to `metaclass=`.
These expressions, however, need to be callable and accept the same arguments
as `type.__new__`.

## Example

```python
def f(): ...

# TypeError: f() takes 0 positional arguments but 3 were given
class B(metaclass=f): ...
```

## References
- [Python documentation: Metaclasses](https://docs.python.org/3/reference/datamodel.html#metaclasses)

---

##### `invalidNamedTuple`<sup>Optional</sup> <a name="invalidNamedTuple" id="projen.python.Rules.property.invalidNamedTuple"></a>

```typescript
public readonly invalidNamedTuple: string;
```

- *Type:* string

## What it does Checks for invalidly defined `NamedTuple` classes.

## Why is this bad?
An invalidly defined `NamedTuple` class may lead to the type checker
drawing incorrect conclusions. It may also lead to `TypeError`s at runtime.

## Examples
A class definition cannot combine `NamedTuple` with other base classes
in multiple inheritance; doing so raises a `TypeError` at runtime. The sole
exception to this rule is `Generic[]`, which can be used alongside `NamedTuple`
in a class's bases list.

```pycon
>>> from typing import NamedTuple
>>> class Foo(NamedTuple, object): ...
TypeError: can only inherit from a NamedTuple type and Generic
```

---

##### `invalidNewtype`<sup>Optional</sup> <a name="invalidNewtype" id="projen.python.Rules.property.invalidNewtype"></a>

```typescript
public readonly invalidNewtype: string;
```

- *Type:* string

## What it does Checks for the creation of invalid `NewType`s.

## Why is this bad?
There are several requirements that you must follow when creating a `NewType`.

## Examples
```python
from typing import NewType

def get_name() -> str: ...

Foo = NewType("Foo", int)        # okay
Bar = NewType(get_name(), int)   # error: The first argument to `NewType` must be a string literal
Baz = NewType("Baz", int | str)  # error: invalid base for `typing.NewType`
```

---

##### `invalidOverload`<sup>Optional</sup> <a name="invalidOverload" id="projen.python.Rules.property.invalidOverload"></a>

```typescript
public readonly invalidOverload: string;
```

- *Type:* string

## What it does Checks for various invalid `@overload` usages.

## Why is this bad?
The `@overload` decorator is used to define functions and methods that accepts different
combinations of arguments and return different types based on the arguments passed. This is
mainly beneficial for type checkers. But, if the `@overload` usage is invalid, the type
checker may not be able to provide correct type information.

## Example

Defining only one overload:

```py
from typing import overload

---

##### `invalidParameterDefault`<sup>Optional</sup> <a name="invalidParameterDefault" id="projen.python.Rules.property.invalidParameterDefault"></a>

```typescript
public readonly invalidParameterDefault: string;
```

- *Type:* string

## What it does Checks for default values that can't be assigned to the parameter's annotated type.

## Why is this bad?
This breaks the rules of the type system and
weakens a type checker's ability to accurately reason about your code.

## Examples
```python
def f(a: int = ''): ...
```

---

##### `invalidParamspec`<sup>Optional</sup> <a name="invalidParamspec" id="projen.python.Rules.property.invalidParamspec"></a>

```typescript
public readonly invalidParamspec: string;
```

- *Type:* string

## What it does Checks for the creation of invalid `ParamSpec`s.

## Why is this bad?
There are several requirements that you must follow when creating a `ParamSpec`.

## Examples
```python
from typing import ParamSpec

P1 = ParamSpec("P1")  # okay
P2 = ParamSpec("S2")  # error: ParamSpec name must match the variable it's assigned to
```

## References
- [Typing spec: ParamSpec](https://typing.python.org/en/latest/spec/generics.html#paramspec)

---

##### `invalidProtocol`<sup>Optional</sup> <a name="invalidProtocol" id="projen.python.Rules.property.invalidProtocol"></a>

```typescript
public readonly invalidProtocol: string;
```

- *Type:* string

## What it does Checks for protocol classes that will raise `TypeError` at runtime.

## Why is this bad?
An invalidly defined protocol class may lead to the type checker inferring
unexpected things. It may also lead to `TypeError`s at runtime.

## Examples
A `Protocol` class cannot inherit from a non-`Protocol` class;
this raises a `TypeError` at runtime:

```pycon
>>> from typing import Protocol
>>> class Foo(int, Protocol): ...
...
Traceback (most recent call last):
File "<python-input-1>", line 1, in <module>
class Foo(int, Protocol): ...
TypeError: Protocols can only inherit from other protocols, got <class 'int'>
```

---

##### `invalidRaise`<sup>Optional</sup> <a name="invalidRaise" id="projen.python.Rules.property.invalidRaise"></a>

```typescript
public readonly invalidRaise: string;
```

- *Type:* string

Checks for `raise` statements that raise non-exceptions or use invalid causes for their raised exceptions.

## Why is this bad?
Only subclasses or instances of `BaseException` can be raised.
For an exception's cause, the same rules apply, except that `None` is also
permitted. Violating these rules results in a `TypeError` at runtime.

## Examples
```python
def f():
try:
something()
except NameError:
raise "oops!" from f

def g():
raise NotImplemented from 42
```

Use instead:
```python
def f():
try:
something()
except NameError as e:
raise RuntimeError("oops!") from e

def g():
raise NotImplementedError from None
```

## References
- [Python documentation: The `raise` statement](https://docs.python.org/3/reference/simple_stmts.html#raise)
- [Python documentation: Built-in Exceptions](https://docs.python.org/3/library/exceptions.html#built-in-exceptions)

---

##### `invalidReturnType`<sup>Optional</sup> <a name="invalidReturnType" id="projen.python.Rules.property.invalidReturnType"></a>

```typescript
public readonly invalidReturnType: string;
```

- *Type:* string

## What it does Detects returned values that can't be assigned to the function's annotated return type.

## Why is this bad?
Returning an object of a type incompatible with the annotated return type may cause confusion to the user calling the function.

## Examples
```python
def func() -> int:
return "a"  # error: [invalid-return-type]
```

---

##### `invalidSuperArgument`<sup>Optional</sup> <a name="invalidSuperArgument" id="projen.python.Rules.property.invalidSuperArgument"></a>

```typescript
public readonly invalidSuperArgument: string;
```

- *Type:* string

## What it does Detects `super()` calls where: - the first argument is not a valid class literal, or - the second argument is not an instance or subclass of the first argument.

## Why is this bad?
`super(type, obj)` expects:
- the first argument to be a class,
- and the second argument to satisfy one of the following:
- `isinstance(obj, type)` is `True`
- `issubclass(obj, type)` is `True`

Violating this relationship will raise a `TypeError` at runtime.

## Examples
```python
class A:
...
class B(A):
...

super(A, B())  # it's okay! `A` satisfies `isinstance(B(), A)`

super(A(), B()) # error: `A()` is not a class

super(B, A())  # error: `A()` does not satisfy `isinstance(A(), B)`
super(B, A)  # error: `A` does not satisfy `issubclass(A, B)`
```

## References
- [Python documentation: super()](https://docs.python.org/3/library/functions.html#super)

---

##### `invalidSyntaxInForwardAnnotation`<sup>Optional</sup> <a name="invalidSyntaxInForwardAnnotation" id="projen.python.Rules.property.invalidSyntaxInForwardAnnotation"></a>

```typescript
public readonly invalidSyntaxInForwardAnnotation: string;
```

- *Type:* string

TODO #14889.

---

##### `invalidTypeAliasType`<sup>Optional</sup> <a name="invalidTypeAliasType" id="projen.python.Rules.property.invalidTypeAliasType"></a>

```typescript
public readonly invalidTypeAliasType: string;
```

- *Type:* string

## What it does Checks for the creation of invalid `TypeAliasType`s.

## Why is this bad?
There are several requirements that you must follow when creating a `TypeAliasType`.

## Examples
```python
from typing import TypeAliasType

IntOrStr = TypeAliasType("IntOrStr", int | str)  # okay
NewAlias = TypeAliasType(get_name(), int)        # error: TypeAliasType name must be a string literal
```

---

##### `invalidTypeCheckingConstant`<sup>Optional</sup> <a name="invalidTypeCheckingConstant" id="projen.python.Rules.property.invalidTypeCheckingConstant"></a>

```typescript
public readonly invalidTypeCheckingConstant: string;
```

- *Type:* string

## What it does Checks for a value other than `False` assigned to the `TYPE_CHECKING` variable, or an annotation not assignable from `bool`.

## Why is this bad?
The name `TYPE_CHECKING` is reserved for a flag that can be used to provide conditional
code seen only by the type checker, and not at runtime. Normally this flag is imported from
`typing` or `typing_extensions`, but it can also be defined locally. If defined locally, it
must be assigned the value `False` at runtime; the type checker will consider its value to
be `True`. If annotated, it must be annotated as a type that can accept `bool` values.

## Examples
```python
TYPE_CHECKING: str
TYPE_CHECKING = ''
```

---

##### `invalidTypeForm`<sup>Optional</sup> <a name="invalidTypeForm" id="projen.python.Rules.property.invalidTypeForm"></a>

```typescript
public readonly invalidTypeForm: string;
```

- *Type:* string

## What it does Checks for expressions that are used as [type expressions] but cannot validly be interpreted as such.

## Why is this bad?
Such expressions cannot be understood by ty.
In some cases, they might raise errors at runtime.

## Examples
```python
from typing import Annotated

a: type[1]  # `1` is not a type
b: Annotated[int]  # `Annotated` expects at least two arguments
```
[type expressions]: https://typing.python.org/en/latest/spec/annotations.html#type-and-annotation-expressions

---

##### `invalidTypeGuardCall`<sup>Optional</sup> <a name="invalidTypeGuardCall" id="projen.python.Rules.property.invalidTypeGuardCall"></a>

```typescript
public readonly invalidTypeGuardCall: string;
```

- *Type:* string

## What it does Checks for type guard function calls without a valid target.

## Why is this bad?
The first non-keyword non-variadic argument to a type guard function
is its target and must map to a symbol.

Starred (`is_str(*a)`), literal (`is_str(42)`) and other non-symbol-like
expressions are invalid as narrowing targets.

## Examples
```python
from typing import TypeIs

def f(v: object) -> TypeIs[int]: ...

f()  # Error
f(*a)  # Error
f(10)  # Error
```

---

##### `invalidTypeGuardDefinition`<sup>Optional</sup> <a name="invalidTypeGuardDefinition" id="projen.python.Rules.property.invalidTypeGuardDefinition"></a>

```typescript
public readonly invalidTypeGuardDefinition: string;
```

- *Type:* string

## What it does Checks for type guard functions without a first non-self-like non-keyword-only non-variadic parameter.

## Why is this bad?
Type narrowing functions must accept at least one positional argument
(non-static methods must accept another in addition to `self`/`cls`).

Extra parameters/arguments are allowed but do not affect narrowing.

## Examples
```python
from typing import TypeIs

def f() -> TypeIs[int]: ...  # Error, no parameter
def f(*, v: object) -> TypeIs[int]: ...  # Error, no positional arguments allowed
def f(*args: object) -> TypeIs[int]: ... # Error, expect variadic arguments
class C:
def f(self) -> TypeIs[int]: ...  # Error, only positional argument expected is `self`
```

---

##### `invalidTypeVariableConstraints`<sup>Optional</sup> <a name="invalidTypeVariableConstraints" id="projen.python.Rules.property.invalidTypeVariableConstraints"></a>

```typescript
public readonly invalidTypeVariableConstraints: string;
```

- *Type:* string

## What it does Checks for constrained [type variables] with only one constraint.

## Why is this bad?
A constrained type variable must have at least two constraints.

## Examples
```python
from typing import TypeVar

T = TypeVar('T', str)  # invalid constrained TypeVar
```

Use instead:
```python
T = TypeVar('T', str, int)  # valid constrained TypeVar
# or
T = TypeVar('T', bound=str)  # valid bound TypeVar
```

[type variables]: https://docs.python.org/3/library/typing.html#typing.TypeVar

---

##### `missingArgument`<sup>Optional</sup> <a name="missingArgument" id="projen.python.Rules.property.missingArgument"></a>

```typescript
public readonly missingArgument: string;
```

- *Type:* string

## What it does Checks for missing required arguments in a call.

## Why is this bad?
Failing to provide a required argument will raise a `TypeError` at runtime.

## Examples
```python
def func(x: int): ...
func()  # TypeError: func() missing 1 required positional argument: 'x'
```

---

##### `missingTypedDictKey`<sup>Optional</sup> <a name="missingTypedDictKey" id="projen.python.Rules.property.missingTypedDictKey"></a>

```typescript
public readonly missingTypedDictKey: string;
```

- *Type:* string

## What it does Detects missing required keys in `TypedDict` constructor calls.

## Why is this bad?
`TypedDict` requires all non-optional keys to be provided during construction.
Missing items can lead to a `KeyError` at runtime.

## Example
```python
from typing import TypedDict

class Person(TypedDict):
name: str
age: int

alice: Person = {"name": "Alice"}  # missing required key 'age'

alice["age"]  # KeyError
```

---

##### `noMatchingOverload`<sup>Optional</sup> <a name="noMatchingOverload" id="projen.python.Rules.property.noMatchingOverload"></a>

```typescript
public readonly noMatchingOverload: string;
```

- *Type:* string

## What it does Checks for calls to an overloaded function that do not match any of the overloads.

## Why is this bad?
Failing to provide the correct arguments to one of the overloads will raise a `TypeError`
at runtime.

## Examples
```python

---

##### `nonSubscriptable`<sup>Optional</sup> <a name="nonSubscriptable" id="projen.python.Rules.property.nonSubscriptable"></a>

```typescript
public readonly nonSubscriptable: string;
```

- *Type:* string

## What it does Checks for subscripting objects that do not support subscripting.

## Why is this bad?
Subscripting an object that does not support it will raise a `TypeError` at runtime.

## Examples
```python
4[1]  # TypeError: 'int' object is not subscriptable
```

---

##### `notIterable`<sup>Optional</sup> <a name="notIterable" id="projen.python.Rules.property.notIterable"></a>

```typescript
public readonly notIterable: string;
```

- *Type:* string

## What it does Checks for objects that are not iterable but are used in a context that requires them to be.

## Why is this bad?
Iterating over an object that is not iterable will raise a `TypeError` at runtime.

## Examples

```python
for i in 34:  # TypeError: 'int' object is not iterable
pass
```

---

##### `parameterAlreadyAssigned`<sup>Optional</sup> <a name="parameterAlreadyAssigned" id="projen.python.Rules.property.parameterAlreadyAssigned"></a>

```typescript
public readonly parameterAlreadyAssigned: string;
```

- *Type:* string

## What it does Checks for calls which provide more than one argument for a single parameter.

## Why is this bad?
Providing multiple values for a single parameter will raise a `TypeError` at runtime.

## Examples

```python
def f(x: int) -> int: ...

f(1, x=2)  # Error raised here
```

---

##### `positionalOnlyParameterAsKwarg`<sup>Optional</sup> <a name="positionalOnlyParameterAsKwarg" id="projen.python.Rules.property.positionalOnlyParameterAsKwarg"></a>

```typescript
public readonly positionalOnlyParameterAsKwarg: string;
```

- *Type:* string

## What it does Checks for keyword arguments in calls that match positional-only parameters of the callable.

## Why is this bad?
Providing a positional-only parameter as a keyword argument will raise `TypeError` at runtime.

## Example

```python
def f(x: int, /) -> int: ...

f(x=1)  # Error raised here
```

---

##### `possiblyMissingAttribute`<sup>Optional</sup> <a name="possiblyMissingAttribute" id="projen.python.Rules.property.possiblyMissingAttribute"></a>

```typescript
public readonly possiblyMissingAttribute: string;
```

- *Type:* string

## What it does Checks for possibly missing attributes.

## Why is this bad?
Attempting to access a missing attribute will raise an `AttributeError` at runtime.

## Examples
```python
class A:
if b:
c = 0

A.c  # AttributeError: type object 'A' has no attribute 'c'
```

---

##### `possiblyMissingImplicitCall`<sup>Optional</sup> <a name="possiblyMissingImplicitCall" id="projen.python.Rules.property.possiblyMissingImplicitCall"></a>

```typescript
public readonly possiblyMissingImplicitCall: string;
```

- *Type:* string

## What it does Checks for implicit calls to possibly missing methods.

## Why is this bad?
Expressions such as `x[y]` and `x * y` call methods
under the hood (`__getitem__` and `__mul__` respectively).
Calling a missing method will raise an `AttributeError` at runtime.

## Examples
```python
import datetime

class A:
if datetime.date.today().weekday() != 6:
def __getitem__(self, v): ...

A()[0]  # TypeError: 'A' object is not subscriptable
```

---

##### `possiblyMissingImport`<sup>Optional</sup> <a name="possiblyMissingImport" id="projen.python.Rules.property.possiblyMissingImport"></a>

```typescript
public readonly possiblyMissingImport: string;
```

- *Type:* string

## What it does Checks for imports of symbols that may be missing.

## Why is this bad?
Importing a missing module or name will raise a `ModuleNotFoundError`
or `ImportError` at runtime.

## Examples
```python
# module.py
import datetime

if datetime.date.today().weekday() != 6:
a = 1

# main.py
from module import a  # ImportError: cannot import name 'a' from 'module'
```

---

##### `possiblyUnresolvedReference`<sup>Optional</sup> <a name="possiblyUnresolvedReference" id="projen.python.Rules.property.possiblyUnresolvedReference"></a>

```typescript
public readonly possiblyUnresolvedReference: string;
```

- *Type:* string

## What it does Checks for references to names that are possibly not defined.

## Why is this bad?
Using an undefined variable will raise a `NameError` at runtime.

## Example

```python
for i in range(0):
x = i

print(x)  # NameError: name 'x' is not defined
```

---

##### `rawStringTypeAnnotation`<sup>Optional</sup> <a name="rawStringTypeAnnotation" id="projen.python.Rules.property.rawStringTypeAnnotation"></a>

```typescript
public readonly rawStringTypeAnnotation: string;
```

- *Type:* string

## What it does Checks for raw-strings in type annotation positions.

## Why is this bad?
Static analysis tools like ty can't analyze type annotations that use raw-string notation.

## Examples
```python
def test(): -> r"int":
...
```

Use instead:
```python
def test(): -> "int":
...
```

---

##### `redundantCast`<sup>Optional</sup> <a name="redundantCast" id="projen.python.Rules.property.redundantCast"></a>

```typescript
public readonly redundantCast: string;
```

- *Type:* string

## What it does Detects redundant `cast` calls where the value already has the target type.

## Why is this bad?
These casts have no effect and can be removed.

## Example
```python
def f() -> int:
return 10

cast(int, f())  # Redundant
```

---

##### `staticAssertError`<sup>Optional</sup> <a name="staticAssertError" id="projen.python.Rules.property.staticAssertError"></a>

```typescript
public readonly staticAssertError: string;
```

- *Type:* string

## What it does Makes sure that the argument of `static_assert` is statically known to be true.

## Why is this bad?
A `static_assert` call represents an explicit request from the user
for the type checker to emit an error if the argument cannot be verified
to evaluate to `True` in a boolean context.

## Examples
```python
from ty_extensions import static_assert

static_assert(1 + 1 == 3)  # error: evaluates to `False`

static_assert(int(2.0 * 3.0) == 6)  # error: does not have a statically known truthiness
```

---

##### `subclassOfFinalClass`<sup>Optional</sup> <a name="subclassOfFinalClass" id="projen.python.Rules.property.subclassOfFinalClass"></a>

```typescript
public readonly subclassOfFinalClass: string;
```

- *Type:* string

## What it does Checks for classes that subclass final classes.

## Why is this bad?
Decorating a class with `@final` declares to the type checker that it should not be subclassed.

## Example

```python
from typing import final

---

##### `tooManyPositionalArguments`<sup>Optional</sup> <a name="tooManyPositionalArguments" id="projen.python.Rules.property.tooManyPositionalArguments"></a>

```typescript
public readonly tooManyPositionalArguments: string;
```

- *Type:* string

## What it does Checks for calls that pass more positional arguments than the callable can accept.

## Why is this bad?
Passing too many positional arguments will raise `TypeError` at runtime.

## Example

```python
def f(): ...

f("foo")  # Error raised here
```

---

##### `typeAssertionFailure`<sup>Optional</sup> <a name="typeAssertionFailure" id="projen.python.Rules.property.typeAssertionFailure"></a>

```typescript
public readonly typeAssertionFailure: string;
```

- *Type:* string

## What it does Checks for `assert_type()` and `assert_never()` calls where the actual type is not the same as the asserted type.

## Why is this bad?
`assert_type()` allows confirming the inferred type of a certain value.

## Example

```python
def _(x: int):
assert_type(x, int)  # fine
assert_type(x, str)  # error: Actual type does not match asserted type
```

---

##### `unavailableImplicitSuperArguments`<sup>Optional</sup> <a name="unavailableImplicitSuperArguments" id="projen.python.Rules.property.unavailableImplicitSuperArguments"></a>

```typescript
public readonly unavailableImplicitSuperArguments: string;
```

- *Type:* string

## What it does Detects invalid `super()` calls where implicit arguments like the enclosing class or first method argument are unavailable.

## Why is this bad?
When `super()` is used without arguments, Python tries to find two things:
the nearest enclosing class and the first argument of the immediately enclosing function (typically self or cls).
If either of these is missing, the call will fail at runtime with a `RuntimeError`.

## Examples
```python
super()  # error: no enclosing class or function found

def func():
super()  # error: no enclosing class or first argument exists

class A:
f = super()  # error: no enclosing function to provide the first argument

def method(self):
def nested():
super()  # error: first argument does not exist in this nested function

lambda: super()  # error: first argument does not exist in this lambda

(super() for _ in range(10))  # error: argument is not available in generator expression

super()  # okay! both enclosing class and first argument are available
```

## References
- [Python documentation: super()](https://docs.python.org/3/library/functions.html#super)

---

##### `undefinedReveal`<sup>Optional</sup> <a name="undefinedReveal" id="projen.python.Rules.property.undefinedReveal"></a>

```typescript
public readonly undefinedReveal: string;
```

- *Type:* string

## What it does Checks for calls to `reveal_type` without importing it.

## Why is this bad?
Using `reveal_type` without importing it will raise a `NameError` at runtime.

## Examples
```python
reveal_type(1)  # NameError: name 'reveal_type' is not defined
```

---

##### `unknownArgument`<sup>Optional</sup> <a name="unknownArgument" id="projen.python.Rules.property.unknownArgument"></a>

```typescript
public readonly unknownArgument: string;
```

- *Type:* string

## What it does Checks for keyword arguments in calls that don't match any parameter of the callable.

## Why is this bad?
Providing an unknown keyword argument will raise `TypeError` at runtime.

## Example

```python
def f(x: int) -> int: ...

f(x=1, y=2)  # Error raised here
```

---

##### `unresolvedAttribute`<sup>Optional</sup> <a name="unresolvedAttribute" id="projen.python.Rules.property.unresolvedAttribute"></a>

```typescript
public readonly unresolvedAttribute: string;
```

- *Type:* string

## What it does Checks for unresolved attributes.

## Why is this bad?
Accessing an unbound attribute will raise an `AttributeError` at runtime.
An unresolved attribute is not guaranteed to exist from the type alone,
so this could also indicate that the object is not of the type that the user expects.

## Examples
```python
class A: ...

A().foo  # AttributeError: 'A' object has no attribute 'foo'
```

---

##### `unresolvedGlobal`<sup>Optional</sup> <a name="unresolvedGlobal" id="projen.python.Rules.property.unresolvedGlobal"></a>

```typescript
public readonly unresolvedGlobal: string;
```

- *Type:* string

## What it does Detects variables declared as `global` in an inner scope that have no explicit bindings or declarations in the global scope.

## Why is this bad?
Function bodies with `global` statements can run in any order (or not at all), which makes
it hard for static analysis tools to infer the types of globals without
explicit definitions or declarations.

## Example
```python
def f():
global x  # unresolved global
x = 42

def g():
print(x)  # unresolved reference
```

Use instead:

```python
x: int

def f():
global x
x = 42

def g():
print(x)
```

Or:

```python
x: int | None = None

def f():
global x
x = 42

def g():
print(x)
```

---

##### `unresolvedImport`<sup>Optional</sup> <a name="unresolvedImport" id="projen.python.Rules.property.unresolvedImport"></a>

```typescript
public readonly unresolvedImport: string;
```

- *Type:* string

## What it does Checks for import statements for which the module cannot be resolved.

## Why is this bad?
Importing a module that cannot be resolved will raise a `ModuleNotFoundError`
at runtime.

## Examples
```python
import foo  # ModuleNotFoundError: No module named 'foo'
```

---

##### `unresolvedReference`<sup>Optional</sup> <a name="unresolvedReference" id="projen.python.Rules.property.unresolvedReference"></a>

```typescript
public readonly unresolvedReference: string;
```

- *Type:* string

## What it does Checks for references to names that are not defined.

## Why is this bad?
Using an undefined variable will raise a `NameError` at runtime.

## Example

```python
print(x)  # NameError: name 'x' is not defined
```

---

##### `unsupportedBase`<sup>Optional</sup> <a name="unsupportedBase" id="projen.python.Rules.property.unsupportedBase"></a>

```typescript
public readonly unsupportedBase: string;
```

- *Type:* string

## What it does Checks for class definitions that have bases which are unsupported by ty.

## Why is this bad?
If a class has a base that is an instance of a complex type such as a union type,
ty will not be able to resolve the [method resolution order] (MRO) for the class.
This will lead to an inferior understanding of your codebase and unpredictable
type-checking behavior.

## Examples
```python
import datetime

class A: ...
class B: ...

if datetime.date.today().weekday() != 6:
C = A
else:
C = B

class D(C): ...  # error: [unsupported-base]
```

[method resolution order]: https://docs.python.org/3/glossary.html#term-method-resolution-order

---

##### `unsupportedBoolConversion`<sup>Optional</sup> <a name="unsupportedBoolConversion" id="projen.python.Rules.property.unsupportedBoolConversion"></a>

```typescript
public readonly unsupportedBoolConversion: string;
```

- *Type:* string

## What it does Checks for bool conversions where the object doesn't correctly implement `__bool__`.

## Why is this bad?
If an exception is raised when you attempt to evaluate the truthiness of an object,
using the object in a boolean context will fail at runtime.

## Examples

```python
class NotBoolable:
__bool__ = None

b1 = NotBoolable()
b2 = NotBoolable()

if b1:  # exception raised here
pass

b1 and b2  # exception raised here
not b1  # exception raised here
b1 < b2 < b1  # exception raised here
```

---

##### `unsupportedOperator`<sup>Optional</sup> <a name="unsupportedOperator" id="projen.python.Rules.property.unsupportedOperator"></a>

```typescript
public readonly unsupportedOperator: string;
```

- *Type:* string

## What it does Checks for binary expressions, comparisons, and unary expressions where the operands don't support the operator.

## Why is this bad?
Attempting to use an unsupported operator will raise a `TypeError` at
runtime.

## Examples
```python
class A: ...

A() + A()  # TypeError: unsupported operand type(s) for +: 'A' and 'A'
```

---

##### `unusedIgnoreComment`<sup>Optional</sup> <a name="unusedIgnoreComment" id="projen.python.Rules.property.unusedIgnoreComment"></a>

```typescript
public readonly unusedIgnoreComment: string;
```

- *Type:* string

## What it does Checks for `type: ignore` or `ty: ignore` directives that are no longer applicable.

## Why is this bad?
A `type: ignore` directive that no longer matches any diagnostic violations is likely
included by mistake, and should be removed to avoid confusion.

## Examples
```py
a = 20 / 2  # ty: ignore[division-by-zero]
```

Use instead:

```py
a = 20 / 2
```

---

##### `uselessOverloadBody`<sup>Optional</sup> <a name="uselessOverloadBody" id="projen.python.Rules.property.uselessOverloadBody"></a>

```typescript
public readonly uselessOverloadBody: string;
```

- *Type:* string

## What it does Checks for various `@overload`-decorated functions that have non-stub bodies.

## Why is this bad?
Functions decorated with `@overload` are ignored at runtime; they are overridden
by the implementation function that follows the series of overloads. While it is
not illegal to provide a body for an `@overload`-decorated function, it may indicate
a misunderstanding of how the `@overload` decorator works.

## Example

```py
from typing import overload

---

##### `zeroStepsizeInSlice`<sup>Optional</sup> <a name="zeroStepsizeInSlice" id="projen.python.Rules.property.zeroStepsizeInSlice"></a>

```typescript
public readonly zeroStepsizeInSlice: string;
```

- *Type:* string

## What it does Checks for step size 0 in slices.

## Why is this bad?
A slice with a step size of zero will raise a `ValueError` at runtime.

## Examples
```python
l = list(range(10))
l[1:10:0]  # ValueError: slice step cannot be zero
```

---

### SchemaConflictItem <a name="SchemaConflictItem" id="projen.python.SchemaConflictItem"></a>

A single item in a conflicting set.

Each item is a pair of an (optional) package and a corresponding extra or group name for that
package.

#### Initializer <a name="Initializer" id="projen.python.SchemaConflictItem.Initializer"></a>

```typescript
import { python } from 'projen'

const schemaConflictItem: python.SchemaConflictItem = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.SchemaConflictItem.property.extra">extra</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.SchemaConflictItem.property.group">group</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.SchemaConflictItem.property.package">package</a></code> | <code>string</code> | *No description.* |

---

##### `extra`<sup>Optional</sup> <a name="extra" id="projen.python.SchemaConflictItem.property.extra"></a>

```typescript
public readonly extra: string;
```

- *Type:* string

---

##### `group`<sup>Optional</sup> <a name="group" id="projen.python.SchemaConflictItem.property.group"></a>

```typescript
public readonly group: string;
```

- *Type:* string

---

##### `package`<sup>Optional</sup> <a name="package" id="projen.python.SchemaConflictItem.property.package"></a>

```typescript
public readonly package: string;
```

- *Type:* string

---

### SetuptoolsConfiguration <a name="SetuptoolsConfiguration" id="projen.python.SetuptoolsConfiguration"></a>

``setuptools``-specific configurations that can be set by users that require customization.

These configurations are completely optional and probably can be skipped when creating simple packages. They are equivalent to some of the `Keywords <https://setuptools.pypa.io/en/latest/references/keywords.html>`_ used by the ``setup.py`` file, and can be set via the ``tool.setuptools`` table. It considers only ``setuptools`` `parameters <https://setuptools.pypa.io/en/latest/userguide/pyproject_config.html#setuptools-specific-configuration>`_ that are not covered by :pep:`621`; and intentionally excludes ``dependency_links`` and ``setup_requires`` (incompatible with modern workflows/standards).

#### Initializer <a name="Initializer" id="projen.python.SetuptoolsConfiguration.Initializer"></a>

```typescript
import { python } from 'projen'

const setuptoolsConfiguration: python.SetuptoolsConfiguration = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.SetuptoolsConfiguration.property.cmdclass">cmdclass</a></code> | <code>any</code> | Mapping of distutils-style command names to ``setuptools.Command`` subclasses which in turn should be represented by strings with a qualified class name (i.e., "dotted" form with module), e.g.::. |
| <code><a href="#projen.python.SetuptoolsConfiguration.property.dataFiles">dataFiles</a></code> | <code>any</code> | ``dict``-like structure where each key represents a directory and the value is a list of glob patterns that should be installed in them. |
| <code><a href="#projen.python.SetuptoolsConfiguration.property.dynamic">dynamic</a></code> | <code><a href="#projen.python.SetuptoolsConfigurationDynamic">SetuptoolsConfigurationDynamic</a></code> | Instructions for loading :pep:`621`-related metadata dynamically. |
| <code><a href="#projen.python.SetuptoolsConfiguration.property.eagerResources">eagerResources</a></code> | <code>string[]</code> | Resources that should be extracted together, if any of them is needed, or if any C extensions included in the project are imported. |
| <code><a href="#projen.python.SetuptoolsConfiguration.property.excludePackageData">excludePackageData</a></code> | <code>any</code> | Mapping from package names to lists of glob patterns that should be excluded For more information on how to include data files, check ``setuptools`` `docs <https://setuptools.pypa.io/en/latest/userguide/datafiles.html>`_. |
| <code><a href="#projen.python.SetuptoolsConfiguration.property.includePackageData">includePackageData</a></code> | <code>boolean</code> | Automatically include any data files inside the package directories that are specified by ``MANIFEST.in`` For more information on how to include data files, check ``setuptools`` `docs <https://setuptools.pypa.io/en/latest/userguide/datafiles.html>`_. |
| <code><a href="#projen.python.SetuptoolsConfiguration.property.licenseFiles">licenseFiles</a></code> | <code>string[]</code> | **PROVISIONAL**: list of glob patterns for all license files being distributed. |
| <code><a href="#projen.python.SetuptoolsConfiguration.property.namespacePackages">namespacePackages</a></code> | <code>string[]</code> | **DEPRECATED**: use implicit namespaces instead (:pep:`420`). |
| <code><a href="#projen.python.SetuptoolsConfiguration.property.obsoletes">obsoletes</a></code> | <code>string[]</code> | Packages which this package renders obsolete **(not supported by pip)**. |
| <code><a href="#projen.python.SetuptoolsConfiguration.property.packageData">packageData</a></code> | <code>any</code> | Mapping from package names to lists of glob patterns. |
| <code><a href="#projen.python.SetuptoolsConfiguration.property.packageDir">packageDir</a></code> | <code>any</code> | :class:`dict`-like structure mapping from package names to directories where their code can be found. |
| <code><a href="#projen.python.SetuptoolsConfiguration.property.packages">packages</a></code> | <code>any</code> | Packages that should be included in the distribution. |
| <code><a href="#projen.python.SetuptoolsConfiguration.property.platforms">platforms</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.SetuptoolsConfiguration.property.provides">provides</a></code> | <code>string[]</code> | Package and virtual package names contained within this package **(not supported by pip)**. |
| <code><a href="#projen.python.SetuptoolsConfiguration.property.pyModules">pyModules</a></code> | <code>string[]</code> | Modules that setuptools will manipulate. |
| <code><a href="#projen.python.SetuptoolsConfiguration.property.scriptFiles">scriptFiles</a></code> | <code>string[]</code> | Legacy way of defining scripts (entry-points are preferred). |
| <code><a href="#projen.python.SetuptoolsConfiguration.property.zipSafe">zipSafe</a></code> | <code>boolean</code> | Whether the project can be safely installed and run from a zip file. |

---

##### `cmdclass`<sup>Optional</sup> <a name="cmdclass" id="projen.python.SetuptoolsConfiguration.property.cmdclass"></a>

```typescript
public readonly cmdclass: any;
```

- *Type:* any

Mapping of distutils-style command names to ``setuptools.Command`` subclasses which in turn should be represented by strings with a qualified class name (i.e., "dotted" form with module), e.g.::.

cmdclass = {mycmd = "pkg.subpkg.module.CommandClass"}

The command class should be a directly defined at the top-level of the containing module (no class nesting).

---

##### `dataFiles`<sup>Optional</sup> <a name="dataFiles" id="projen.python.SetuptoolsConfiguration.property.dataFiles"></a>

```typescript
public readonly dataFiles: any;
```

- *Type:* any

``dict``-like structure where each key represents a directory and the value is a list of glob patterns that should be installed in them.

**DISCOURAGED**: please notice this might not work as expected with wheels. Whenever possible, consider using data files inside the package directories (or create a new namespace package that only contains data files). See `data files support <https://setuptools.pypa.io/en/latest/userguide/datafiles.html>`_.

---

##### `dynamic`<sup>Optional</sup> <a name="dynamic" id="projen.python.SetuptoolsConfiguration.property.dynamic"></a>

```typescript
public readonly dynamic: SetuptoolsConfigurationDynamic;
```

- *Type:* <a href="#projen.python.SetuptoolsConfigurationDynamic">SetuptoolsConfigurationDynamic</a>

Instructions for loading :pep:`621`-related metadata dynamically.

---

##### `eagerResources`<sup>Optional</sup> <a name="eagerResources" id="projen.python.SetuptoolsConfiguration.property.eagerResources"></a>

```typescript
public readonly eagerResources: string[];
```

- *Type:* string[]

Resources that should be extracted together, if any of them is needed, or if any C extensions included in the project are imported.

**OBSOLETE**: only relevant for ``pkg_resources``, ``easy_install`` and ``setup.py install`` in the context of ``eggs`` (**DEPRECATED**).

---

##### `excludePackageData`<sup>Optional</sup> <a name="excludePackageData" id="projen.python.SetuptoolsConfiguration.property.excludePackageData"></a>

```typescript
public readonly excludePackageData: any;
```

- *Type:* any

Mapping from package names to lists of glob patterns that should be excluded For more information on how to include data files, check ``setuptools`` `docs <https://setuptools.pypa.io/en/latest/userguide/datafiles.html>`_.

---

##### `includePackageData`<sup>Optional</sup> <a name="includePackageData" id="projen.python.SetuptoolsConfiguration.property.includePackageData"></a>

```typescript
public readonly includePackageData: boolean;
```

- *Type:* boolean

Automatically include any data files inside the package directories that are specified by ``MANIFEST.in`` For more information on how to include data files, check ``setuptools`` `docs <https://setuptools.pypa.io/en/latest/userguide/datafiles.html>`_.

---

##### `licenseFiles`<sup>Optional</sup> <a name="licenseFiles" id="projen.python.SetuptoolsConfiguration.property.licenseFiles"></a>

```typescript
public readonly licenseFiles: string[];
```

- *Type:* string[]

**PROVISIONAL**: list of glob patterns for all license files being distributed.

(likely to become standard with :pep:`639`). By default: ``['LICEN[CS]E*', 'COPYING*', 'NOTICE*', 'AUTHORS*']``

---

##### `namespacePackages`<sup>Optional</sup> <a name="namespacePackages" id="projen.python.SetuptoolsConfiguration.property.namespacePackages"></a>

```typescript
public readonly namespacePackages: string[];
```

- *Type:* string[]

**DEPRECATED**: use implicit namespaces instead (:pep:`420`).

---

##### `obsoletes`<sup>Optional</sup> <a name="obsoletes" id="projen.python.SetuptoolsConfiguration.property.obsoletes"></a>

```typescript
public readonly obsoletes: string[];
```

- *Type:* string[]

Packages which this package renders obsolete **(not supported by pip)**.

---

##### `packageData`<sup>Optional</sup> <a name="packageData" id="projen.python.SetuptoolsConfiguration.property.packageData"></a>

```typescript
public readonly packageData: any;
```

- *Type:* any

Mapping from package names to lists of glob patterns.

Usually this option is not needed when using ``include-package-data = true`` For more information on how to include data files, check ``setuptools`` `docs <https://setuptools.pypa.io/en/latest/userguide/datafiles.html>`_.

---

##### `packageDir`<sup>Optional</sup> <a name="packageDir" id="projen.python.SetuptoolsConfiguration.property.packageDir"></a>

```typescript
public readonly packageDir: any;
```

- *Type:* any

:class:`dict`-like structure mapping from package names to directories where their code can be found.

The empty string (as key) means that all packages are contained inside the given directory will be included in the distribution.

---

##### `packages`<sup>Optional</sup> <a name="packages" id="projen.python.SetuptoolsConfiguration.property.packages"></a>

```typescript
public readonly packages: any;
```

- *Type:* any

Packages that should be included in the distribution.

It can be given either as a list of package identifiers or as a ``dict``-like structure with a single key ``find`` which corresponds to a dynamic call to ``setuptools.config.expand.find_packages`` function. The ``find`` key is associated with a nested ``dict``-like structure that can contain ``where``, ``include``, ``exclude`` and ``namespaces`` keys, mimicking the keyword arguments of the associated function.

---

##### `platforms`<sup>Optional</sup> <a name="platforms" id="projen.python.SetuptoolsConfiguration.property.platforms"></a>

```typescript
public readonly platforms: string[];
```

- *Type:* string[]

---

##### `provides`<sup>Optional</sup> <a name="provides" id="projen.python.SetuptoolsConfiguration.property.provides"></a>

```typescript
public readonly provides: string[];
```

- *Type:* string[]

Package and virtual package names contained within this package **(not supported by pip)**.

---

##### `pyModules`<sup>Optional</sup> <a name="pyModules" id="projen.python.SetuptoolsConfiguration.property.pyModules"></a>

```typescript
public readonly pyModules: string[];
```

- *Type:* string[]

Modules that setuptools will manipulate.

---

##### `scriptFiles`<sup>Optional</sup> <a name="scriptFiles" id="projen.python.SetuptoolsConfiguration.property.scriptFiles"></a>

```typescript
public readonly scriptFiles: string[];
```

- *Type:* string[]

Legacy way of defining scripts (entry-points are preferred).

Equivalent to the ``script`` keyword in ``setup.py`` (it was renamed to avoid confusion with entry-point based ``project.scripts`` defined in :pep:`621`). **DISCOURAGED**: generic script wrappers are tricky and may not work properly. Whenever possible, please use ``project.scripts`` instead.

---

##### `zipSafe`<sup>Optional</sup> <a name="zipSafe" id="projen.python.SetuptoolsConfiguration.property.zipSafe"></a>

```typescript
public readonly zipSafe: boolean;
```

- *Type:* boolean

Whether the project can be safely installed and run from a zip file.

**OBSOLETE**: only relevant for ``pkg_resources``, ``easy_install`` and ``setup.py install`` in the context of ``eggs`` (**DEPRECATED**).

---

### SetuptoolsConfigurationDynamic <a name="SetuptoolsConfigurationDynamic" id="projen.python.SetuptoolsConfigurationDynamic"></a>

Instructions for loading :pep:`621`-related metadata dynamically.

#### Initializer <a name="Initializer" id="projen.python.SetuptoolsConfigurationDynamic.Initializer"></a>

```typescript
import { python } from 'projen'

const setuptoolsConfigurationDynamic: python.SetuptoolsConfigurationDynamic = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.SetuptoolsConfigurationDynamic.property.classifiers">classifiers</a></code> | <code><a href="#projen.python.FileDirective">FileDirective</a></code> | *No description.* |
| <code><a href="#projen.python.SetuptoolsConfigurationDynamic.property.dependencies">dependencies</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.SetuptoolsConfigurationDynamic.property.description">description</a></code> | <code><a href="#projen.python.FileDirective">FileDirective</a></code> | *No description.* |
| <code><a href="#projen.python.SetuptoolsConfigurationDynamic.property.entryPoints">entryPoints</a></code> | <code><a href="#projen.python.FileDirective">FileDirective</a></code> | *No description.* |
| <code><a href="#projen.python.SetuptoolsConfigurationDynamic.property.optionalDependencies">optionalDependencies</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.SetuptoolsConfigurationDynamic.property.readme">readme</a></code> | <code>any</code> | *No description.* |
| <code><a href="#projen.python.SetuptoolsConfigurationDynamic.property.version">version</a></code> | <code>any</code> | A version dynamically loaded via either the ``attr:`` or ``file:`` directives. |

---

##### `classifiers`<sup>Optional</sup> <a name="classifiers" id="projen.python.SetuptoolsConfigurationDynamic.property.classifiers"></a>

```typescript
public readonly classifiers: FileDirective;
```

- *Type:* <a href="#projen.python.FileDirective">FileDirective</a>

---

##### `dependencies`<sup>Optional</sup> <a name="dependencies" id="projen.python.SetuptoolsConfigurationDynamic.property.dependencies"></a>

```typescript
public readonly dependencies: any;
```

- *Type:* any

---

##### `description`<sup>Optional</sup> <a name="description" id="projen.python.SetuptoolsConfigurationDynamic.property.description"></a>

```typescript
public readonly description: FileDirective;
```

- *Type:* <a href="#projen.python.FileDirective">FileDirective</a>

---

##### `entryPoints`<sup>Optional</sup> <a name="entryPoints" id="projen.python.SetuptoolsConfigurationDynamic.property.entryPoints"></a>

```typescript
public readonly entryPoints: FileDirective;
```

- *Type:* <a href="#projen.python.FileDirective">FileDirective</a>

---

##### `optionalDependencies`<sup>Optional</sup> <a name="optionalDependencies" id="projen.python.SetuptoolsConfigurationDynamic.property.optionalDependencies"></a>

```typescript
public readonly optionalDependencies: any;
```

- *Type:* any

---

##### `readme`<sup>Optional</sup> <a name="readme" id="projen.python.SetuptoolsConfigurationDynamic.property.readme"></a>

```typescript
public readonly readme: any;
```

- *Type:* any

---

##### `version`<sup>Optional</sup> <a name="version" id="projen.python.SetuptoolsConfigurationDynamic.property.version"></a>

```typescript
public readonly version: any;
```

- *Type:* any

A version dynamically loaded via either the ``attr:`` or ``file:`` directives.

Please make sure the given file or attribute respects :pep:`440`. Also ensure to set ``project.dynamic`` accordingly.

---

### SrcOptions <a name="SrcOptions" id="projen.python.SrcOptions"></a>

#### Initializer <a name="Initializer" id="projen.python.SrcOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const srcOptions: python.SrcOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.SrcOptions.property.exclude">exclude</a></code> | <code>string[]</code> | A list of file and directory patterns to exclude from type checking. |
| <code><a href="#projen.python.SrcOptions.property.include">include</a></code> | <code>string[]</code> | A list of files and directories to check. |
| <code><a href="#projen.python.SrcOptions.property.respectIgnoreFiles">respectIgnoreFiles</a></code> | <code>boolean</code> | Whether to automatically exclude files that are ignored by `.ignore`, `.gitignore`, `.git/info/exclude`, and global `gitignore` files. Enabled by default. |
| <code><a href="#projen.python.SrcOptions.property.root">root</a></code> | <code>string</code> | The root of the project, used for finding first-party modules. |

---

##### `exclude`<sup>Optional</sup> <a name="exclude" id="projen.python.SrcOptions.property.exclude"></a>

```typescript
public readonly exclude: string[];
```

- *Type:* string[]

A list of file and directory patterns to exclude from type checking.

Patterns follow a syntax similar to `.gitignore`:

- `./src/` matches only a directory
- `./src` matches both files and directories
- `src` matches files or directories named `src`
- `*` matches any (possibly empty) sequence of characters (except `/`).
- `**` matches zero or more path components.
This sequence **must** form a single path component, so both `**a` and `b**` are invalid and will result in an error.
A sequence of more than two consecutive `*` characters is also invalid.
- `?` matches any single character except `/`
- `[abc]` matches any character inside the brackets. Character sequences can also specify ranges of characters, as ordered by Unicode,
so e.g. `[0-9]` specifies any character between `0` and `9` inclusive. An unclosed bracket is invalid.
- `!pattern` negates a pattern (undoes the exclusion of files that would otherwise be excluded)

All paths are anchored relative to the project root (`src` only
matches `<project_root>/src` and not `<project_root>/test/src`).
To exclude any directory or file named `src`, use `*_/src` instead.

By default, ty excludes commonly ignored directories:

- `*_/.bzr/`
- `*_/.direnv/`
- `*_/.eggs/`
- `*_/.git/`
- `*_/.git-rewrite/`
- `*_/.hg/`
- `*_/.mypy_cache/`
- `*_/.nox/`
- `*_/.pants.d/`
- `*_/.pytype/`
- `*_/.ruff_cache/`
- `*_/.svn/`
- `*_/.tox/`
- `*_/.venv/`
- `*_/__pypackages__/`
- `*_/_build/`
- `*_/buck-out/`
- `*_/dist/`
- `*_/node_modules/`
- `*_/venv/`

You can override any default exclude by using a negated pattern. For example,
to re-include `dist` use `exclude = ["!dist"]`

---

##### `include`<sup>Optional</sup> <a name="include" id="projen.python.SrcOptions.property.include"></a>

```typescript
public readonly include: string[];
```

- *Type:* string[]

A list of files and directories to check.

The `include` option
follows a similar syntax to `.gitignore` but reversed:
Including a file or directory will make it so that it (and its contents)
are type checked.

- `./src/` matches only a directory
- `./src` matches both files and directories
- `src` matches a file or directory named `src`
- `*` matches any (possibly empty) sequence of characters (except `/`).
- `**` matches zero or more path components.
This sequence **must** form a single path component, so both `**a` and `b**` are invalid and will result in an error.
A sequence of more than two consecutive `*` characters is also invalid.
- `?` matches any single character except `/`
- `[abc]` matches any character inside the brackets. Character sequences can also specify ranges of characters, as ordered by Unicode,
so e.g. `[0-9]` specifies any character between `0` and `9` inclusive. An unclosed bracket is invalid.

All paths are anchored relative to the project root (`src` only
matches `<project_root>/src` and not `<project_root>/test/src`).

`exclude` takes precedence over `include`.

---

##### `respectIgnoreFiles`<sup>Optional</sup> <a name="respectIgnoreFiles" id="projen.python.SrcOptions.property.respectIgnoreFiles"></a>

```typescript
public readonly respectIgnoreFiles: boolean;
```

- *Type:* boolean

Whether to automatically exclude files that are ignored by `.ignore`, `.gitignore`, `.git/info/exclude`, and global `gitignore` files. Enabled by default.

---

##### `root`<sup>Optional</sup> <a name="root" id="projen.python.SrcOptions.property.root"></a>

```typescript
public readonly root: string;
```

- *Type:* string

The root of the project, used for finding first-party modules.

If left unspecified, ty will try to detect common project layouts and initialize `src.root` accordingly:

* if a `./src` directory exists, include `.` and `./src` in the first party search path (src layout or flat)
* if a `./<project-name>/<project-name>` directory exists, include `.` and `./<project-name>` in the first party search path
* otherwise, default to `.` (flat layout)

Besides, if a `./tests` directory exists and is not a package (i.e. it does not contain an `__init__.py` file),
it will also be included in the first party search path.

---

### StaticMetadata <a name="StaticMetadata" id="projen.python.StaticMetadata"></a>

A subset of the Python Package Metadata 2.3 standard as specified in <https://packaging.python.org/specifications/core-metadata/>.

#### Initializer <a name="Initializer" id="projen.python.StaticMetadata.Initializer"></a>

```typescript
import { python } from 'projen'

const staticMetadata: python.StaticMetadata = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.StaticMetadata.property.name">name</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.StaticMetadata.property.providesExtra">providesExtra</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.StaticMetadata.property.requiresDist">requiresDist</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.StaticMetadata.property.requiresPython">requiresPython</a></code> | <code>string</code> | PEP 508-style Python requirement, e.g., `>=3.10`. |
| <code><a href="#projen.python.StaticMetadata.property.version">version</a></code> | <code>string</code> | PEP 440-style package version, e.g., `1.2.3`. |

---

##### `name`<sup>Required</sup> <a name="name" id="projen.python.StaticMetadata.property.name"></a>

```typescript
public readonly name: string;
```

- *Type:* string

---

##### `providesExtra`<sup>Optional</sup> <a name="providesExtra" id="projen.python.StaticMetadata.property.providesExtra"></a>

```typescript
public readonly providesExtra: string[];
```

- *Type:* string[]

---

##### `requiresDist`<sup>Optional</sup> <a name="requiresDist" id="projen.python.StaticMetadata.property.requiresDist"></a>

```typescript
public readonly requiresDist: string[];
```

- *Type:* string[]

---

##### `requiresPython`<sup>Optional</sup> <a name="requiresPython" id="projen.python.StaticMetadata.property.requiresPython"></a>

```typescript
public readonly requiresPython: string;
```

- *Type:* string

PEP 508-style Python requirement, e.g., `>=3.10`.

---

##### `version`<sup>Optional</sup> <a name="version" id="projen.python.StaticMetadata.property.version"></a>

```typescript
public readonly version: string;
```

- *Type:* string

PEP 440-style package version, e.g., `1.2.3`.

---

### Target <a name="Target" id="projen.python.Target"></a>

#### Initializer <a name="Initializer" id="projen.python.Target.Initializer"></a>

```typescript
import { python } from 'projen'

const target: python.Target = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Target.property.artifacts">artifacts</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.Target.property.bypassSelection">bypassSelection</a></code> | <code>boolean</code> | Whether or not to suppress the error when one has not defined any file selection options and all heuristics have failed to determine what to ship. |
| <code><a href="#projen.python.Target.property.dependencies">dependencies</a></code> | <code>string[]</code> | Additional dependencies to install in the environment. |
| <code><a href="#projen.python.Target.property.exclude">exclude</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.Target.property.forceInclude">forceInclude</a></code> | <code>{[ key: string ]: string}</code> | Whether to force include files. |
| <code><a href="#projen.python.Target.property.ignoreVcs">ignoreVcs</a></code> | <code>boolean</code> | *No description.* |
| <code><a href="#projen.python.Target.property.include">include</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.Target.property.onlyInclude">onlyInclude</a></code> | <code>string[]</code> | *No description.* |
| <code><a href="#projen.python.Target.property.onlyPackages">onlyPackages</a></code> | <code>boolean</code> | Whether to only include non-artifact files in packages. |
| <code><a href="#projen.python.Target.property.packages">packages</a></code> | <code>string[]</code> | List of packages to build. |
| <code><a href="#projen.python.Target.property.requireRuntimeDependencies">requireRuntimeDependencies</a></code> | <code>boolean</code> | Whether to install the project’s runtime dependencies. |
| <code><a href="#projen.python.Target.property.requireRuntimeFeatures">requireRuntimeFeatures</a></code> | <code>string[]</code> | A list of the project’s runtime features to install. |
| <code><a href="#projen.python.Target.property.sources">sources</a></code> | <code>{[ key: string ]: string}</code> | Rewrite relative paths. |
| <code><a href="#projen.python.Target.property.versions">versions</a></code> | <code>string[]</code> | List of versions to build. |

---

##### `artifacts`<sup>Optional</sup> <a name="artifacts" id="projen.python.Target.property.artifacts"></a>

```typescript
public readonly artifacts: string[];
```

- *Type:* string[]

---

##### `bypassSelection`<sup>Optional</sup> <a name="bypassSelection" id="projen.python.Target.property.bypassSelection"></a>

```typescript
public readonly bypassSelection: boolean;
```

- *Type:* boolean

Whether or not to suppress the error when one has not defined any file selection options and all heuristics have failed to determine what to ship.

---

##### `dependencies`<sup>Optional</sup> <a name="dependencies" id="projen.python.Target.property.dependencies"></a>

```typescript
public readonly dependencies: string[];
```

- *Type:* string[]

Additional dependencies to install in the environment.

---

##### `exclude`<sup>Optional</sup> <a name="exclude" id="projen.python.Target.property.exclude"></a>

```typescript
public readonly exclude: string[];
```

- *Type:* string[]

---

##### `forceInclude`<sup>Optional</sup> <a name="forceInclude" id="projen.python.Target.property.forceInclude"></a>

```typescript
public readonly forceInclude: {[ key: string ]: string};
```

- *Type:* {[ key: string ]: string}

Whether to force include files.

---

##### `ignoreVcs`<sup>Optional</sup> <a name="ignoreVcs" id="projen.python.Target.property.ignoreVcs"></a>

```typescript
public readonly ignoreVcs: boolean;
```

- *Type:* boolean

---

##### `include`<sup>Optional</sup> <a name="include" id="projen.python.Target.property.include"></a>

```typescript
public readonly include: string[];
```

- *Type:* string[]

---

##### `onlyInclude`<sup>Optional</sup> <a name="onlyInclude" id="projen.python.Target.property.onlyInclude"></a>

```typescript
public readonly onlyInclude: string[];
```

- *Type:* string[]

---

##### `onlyPackages`<sup>Optional</sup> <a name="onlyPackages" id="projen.python.Target.property.onlyPackages"></a>

```typescript
public readonly onlyPackages: boolean;
```

- *Type:* boolean

Whether to only include non-artifact files in packages.

---

##### `packages`<sup>Optional</sup> <a name="packages" id="projen.python.Target.property.packages"></a>

```typescript
public readonly packages: string[];
```

- *Type:* string[]

List of packages to build.

---

##### `requireRuntimeDependencies`<sup>Optional</sup> <a name="requireRuntimeDependencies" id="projen.python.Target.property.requireRuntimeDependencies"></a>

```typescript
public readonly requireRuntimeDependencies: boolean;
```

- *Type:* boolean

Whether to install the project’s runtime dependencies.

---

##### `requireRuntimeFeatures`<sup>Optional</sup> <a name="requireRuntimeFeatures" id="projen.python.Target.property.requireRuntimeFeatures"></a>

```typescript
public readonly requireRuntimeFeatures: string[];
```

- *Type:* string[]

A list of the project’s runtime features to install.

---

##### `sources`<sup>Optional</sup> <a name="sources" id="projen.python.Target.property.sources"></a>

```typescript
public readonly sources: {[ key: string ]: string};
```

- *Type:* {[ key: string ]: string}

Rewrite relative paths.

---

##### `versions`<sup>Optional</sup> <a name="versions" id="projen.python.Target.property.versions"></a>

```typescript
public readonly versions: string[];
```

- *Type:* string[]

List of versions to build.

---

### TerminalOptions <a name="TerminalOptions" id="projen.python.TerminalOptions"></a>

#### Initializer <a name="Initializer" id="projen.python.TerminalOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const terminalOptions: python.TerminalOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.TerminalOptions.property.errorOnWarning">errorOnWarning</a></code> | <code>boolean</code> | Use exit code 1 if there are any warning-level diagnostics. |
| <code><a href="#projen.python.TerminalOptions.property.outputFormat">outputFormat</a></code> | <code>string</code> | The format to use for printing diagnostic messages. |

---

##### `errorOnWarning`<sup>Optional</sup> <a name="errorOnWarning" id="projen.python.TerminalOptions.property.errorOnWarning"></a>

```typescript
public readonly errorOnWarning: boolean;
```

- *Type:* boolean
- *Default:* false`.

Use exit code 1 if there are any warning-level diagnostics.

Defaults to `false`.

---

##### `outputFormat`<sup>Optional</sup> <a name="outputFormat" id="projen.python.TerminalOptions.property.outputFormat"></a>

```typescript
public readonly outputFormat: string;
```

- *Type:* string
- *Default:* full`.

The format to use for printing diagnostic messages.

Defaults to `full`.

---

### ToolUvWorkspace <a name="ToolUvWorkspace" id="projen.python.ToolUvWorkspace"></a>

#### Initializer <a name="Initializer" id="projen.python.ToolUvWorkspace.Initializer"></a>

```typescript
import { python } from 'projen'

const toolUvWorkspace: python.ToolUvWorkspace = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.ToolUvWorkspace.property.exclude">exclude</a></code> | <code>string[]</code> | Packages to exclude as workspace members. If a package matches both `members` and `exclude`, it will be excluded. |
| <code><a href="#projen.python.ToolUvWorkspace.property.members">members</a></code> | <code>string[]</code> | Packages to include as workspace members. |

---

##### `exclude`<sup>Optional</sup> <a name="exclude" id="projen.python.ToolUvWorkspace.property.exclude"></a>

```typescript
public readonly exclude: string[];
```

- *Type:* string[]

Packages to exclude as workspace members. If a package matches both `members` and `exclude`, it will be excluded.

Supports both globs and explicit paths.

For more information on the glob syntax, refer to the [`glob` documentation](https://docs.rs/glob/latest/glob/struct.Pattern.html).

---

##### `members`<sup>Optional</sup> <a name="members" id="projen.python.ToolUvWorkspace.property.members"></a>

```typescript
public readonly members: string[];
```

- *Type:* string[]

Packages to include as workspace members.

Supports both globs and explicit paths.

For more information on the glob syntax, refer to the [`glob` documentation](https://docs.rs/glob/latest/glob/struct.Pattern.html).

---

### TyConfiguration <a name="TyConfiguration" id="projen.python.TyConfiguration"></a>

#### Initializer <a name="Initializer" id="projen.python.TyConfiguration.Initializer"></a>

```typescript
import { python } from 'projen'

const tyConfiguration: python.TyConfiguration = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.TyConfiguration.property.environment">environment</a></code> | <code><a href="#projen.python.EnvironmentOptions">EnvironmentOptions</a></code> | Configures the type checking environment. |
| <code><a href="#projen.python.TyConfiguration.property.overrides">overrides</a></code> | <code><a href="#projen.python.OverrideOptions">OverrideOptions</a>[]</code> | Override configurations for specific file patterns. |
| <code><a href="#projen.python.TyConfiguration.property.rules">rules</a></code> | <code><a href="#projen.python.Rules">Rules</a></code> | Configures the enabled rules and their severity. |
| <code><a href="#projen.python.TyConfiguration.property.src">src</a></code> | <code><a href="#projen.python.SrcOptions">SrcOptions</a></code> | *No description.* |
| <code><a href="#projen.python.TyConfiguration.property.terminal">terminal</a></code> | <code><a href="#projen.python.TerminalOptions">TerminalOptions</a></code> | *No description.* |

---

##### `environment`<sup>Optional</sup> <a name="environment" id="projen.python.TyConfiguration.property.environment"></a>

```typescript
public readonly environment: EnvironmentOptions;
```

- *Type:* <a href="#projen.python.EnvironmentOptions">EnvironmentOptions</a>

Configures the type checking environment.

---

##### `overrides`<sup>Optional</sup> <a name="overrides" id="projen.python.TyConfiguration.property.overrides"></a>

```typescript
public readonly overrides: OverrideOptions[];
```

- *Type:* <a href="#projen.python.OverrideOptions">OverrideOptions</a>[]

Override configurations for specific file patterns.

Each override specifies include/exclude patterns and rule configurations
that apply to matching files. Multiple overrides can match the same file,
with later overrides taking precedence.

---

##### `rules`<sup>Optional</sup> <a name="rules" id="projen.python.TyConfiguration.property.rules"></a>

```typescript
public readonly rules: Rules;
```

- *Type:* <a href="#projen.python.Rules">Rules</a>

Configures the enabled rules and their severity.

See [the rules documentation](https://ty.dev/rules) for a list of all available rules.

Valid severities are:

* `ignore`: Disable the rule.
* `warn`: Enable the rule and create a warning diagnostic.
* `error`: Enable the rule and create an error diagnostic.
ty will exit with a non-zero code if any error diagnostics are emitted.

---

##### `src`<sup>Optional</sup> <a name="src" id="projen.python.TyConfiguration.property.src"></a>

```typescript
public readonly src: SrcOptions;
```

- *Type:* <a href="#projen.python.SrcOptions">SrcOptions</a>

---

##### `terminal`<sup>Optional</sup> <a name="terminal" id="projen.python.TyConfiguration.property.terminal"></a>

```typescript
public readonly terminal: TerminalOptions;
```

- *Type:* <a href="#projen.python.TerminalOptions">TerminalOptions</a>

---

### UvConfiguration <a name="UvConfiguration" id="projen.python.UvConfiguration"></a>

Metadata and configuration for uv.

#### Initializer <a name="Initializer" id="projen.python.UvConfiguration.Initializer"></a>

```typescript
import { python } from 'projen'

const uvConfiguration: python.UvConfiguration = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.UvConfiguration.property.addBounds">addBounds</a></code> | <code>string</code> | The default version specifier when adding a dependency. |
| <code><a href="#projen.python.UvConfiguration.property.allowInsecureHost">allowInsecureHost</a></code> | <code>string[]</code> | Allow insecure connections to host. |
| <code><a href="#projen.python.UvConfiguration.property.buildBackend">buildBackend</a></code> | <code><a href="#projen.python.BuildBackendSettings">BuildBackendSettings</a></code> | Configuration for the uv build backend. |
| <code><a href="#projen.python.UvConfiguration.property.buildConstraintDependencies">buildConstraintDependencies</a></code> | <code>string[]</code> | PEP 508-style requirements, e.g., `ruff==0.5.0`, or `ruff @ https://...`. |
| <code><a href="#projen.python.UvConfiguration.property.cacheDir">cacheDir</a></code> | <code>string</code> | Path to the cache directory. |
| <code><a href="#projen.python.UvConfiguration.property.cacheKeys">cacheKeys</a></code> | <code>any[]</code> | The keys to consider when caching builds for the project. |
| <code><a href="#projen.python.UvConfiguration.property.checkUrl">checkUrl</a></code> | <code>string</code> | Check an index URL for existing files to skip duplicate uploads. |
| <code><a href="#projen.python.UvConfiguration.property.compileBytecode">compileBytecode</a></code> | <code>boolean</code> | Compile Python files to bytecode after installation. |
| <code><a href="#projen.python.UvConfiguration.property.concurrentBuilds">concurrentBuilds</a></code> | <code>number</code> | The maximum number of source distributions that uv will build concurrently at any given time. |
| <code><a href="#projen.python.UvConfiguration.property.concurrentDownloads">concurrentDownloads</a></code> | <code>number</code> | The maximum number of in-flight concurrent downloads that uv will perform at any given time. |
| <code><a href="#projen.python.UvConfiguration.property.concurrentInstalls">concurrentInstalls</a></code> | <code>number</code> | The number of threads used when installing and unzipping packages. |
| <code><a href="#projen.python.UvConfiguration.property.configSettings">configSettings</a></code> | <code>{[ key: string ]: any}</code> | Settings to pass to the [PEP 517](https://peps.python.org/pep-0517/) build backend, specified as `KEY=VALUE` pairs. |
| <code><a href="#projen.python.UvConfiguration.property.configSettingsPackage">configSettingsPackage</a></code> | <code>{[ key: string ]: {[ key: string ]: any}}</code> | Settings to pass to the [PEP 517](https://peps.python.org/pep-0517/) build backend for specific packages, specified as `KEY=VALUE` pairs. |
| <code><a href="#projen.python.UvConfiguration.property.conflicts">conflicts</a></code> | <code><a href="#projen.python.SchemaConflictItem">SchemaConflictItem</a>[][]</code> | A list of sets of conflicting groups or extras. |
| <code><a href="#projen.python.UvConfiguration.property.constraintDependencies">constraintDependencies</a></code> | <code>string[]</code> | PEP 508-style requirements, e.g., `ruff==0.5.0`, or `ruff @ https://...`. |
| <code><a href="#projen.python.UvConfiguration.property.defaultGroups">defaultGroups</a></code> | <code>any</code> | The list of `dependency-groups` to install by default. |
| <code><a href="#projen.python.UvConfiguration.property.dependencyGroups">dependencyGroups</a></code> | <code>{[ key: string ]: <a href="#projen.python.DependencyGroupSettings">DependencyGroupSettings</a>}</code> | Additional settings for `dependency-groups`. |
| <code><a href="#projen.python.UvConfiguration.property.dependencyMetadata">dependencyMetadata</a></code> | <code><a href="#projen.python.StaticMetadata">StaticMetadata</a>[]</code> | Pre-defined static metadata for dependencies of the project (direct or transitive). |
| <code><a href="#projen.python.UvConfiguration.property.devDependencies">devDependencies</a></code> | <code>string[]</code> | PEP 508-style requirements, e.g., `ruff==0.5.0`, or `ruff @ https://...`. |
| <code><a href="#projen.python.UvConfiguration.property.environments">environments</a></code> | <code>string[]</code> | A list of environment markers, e.g., `python_version >= '3.6'`. |
| <code><a href="#projen.python.UvConfiguration.property.excludeDependencies">excludeDependencies</a></code> | <code>string[]</code> | Package names to exclude, e.g., `werkzeug`, `numpy`. |
| <code><a href="#projen.python.UvConfiguration.property.excludeNewer">excludeNewer</a></code> | <code>string</code> | Limit candidate packages to those that were uploaded prior to a given point in time. |
| <code><a href="#projen.python.UvConfiguration.property.excludeNewerPackage">excludeNewerPackage</a></code> | <code>{[ key: string ]: string}</code> | Limit candidate packages for specific packages to those that were uploaded prior to the given date. |
| <code><a href="#projen.python.UvConfiguration.property.extraBuildDependencies">extraBuildDependencies</a></code> | <code>{[ key: string ]: any[]}</code> | Additional build dependencies for packages. |
| <code><a href="#projen.python.UvConfiguration.property.extraBuildVariables">extraBuildVariables</a></code> | <code>{[ key: string ]: {[ key: string ]: string}}</code> | Extra environment variables to set when building certain packages. |
| <code><a href="#projen.python.UvConfiguration.property.extraIndexUrl">extraIndexUrl</a></code> | <code>string[]</code> | Extra URLs of package indexes to use, in addition to `--index-url`. |
| <code><a href="#projen.python.UvConfiguration.property.findLinks">findLinks</a></code> | <code>string[]</code> | Locations to search for candidate distributions, in addition to those found in the registry indexes. |
| <code><a href="#projen.python.UvConfiguration.property.forkStrategy">forkStrategy</a></code> | <code>string</code> | The strategy to use when selecting multiple versions of a given package across Python versions and platforms. |
| <code><a href="#projen.python.UvConfiguration.property.index">index</a></code> | <code><a href="#projen.python.Index">Index</a>[]</code> | The indexes to use when resolving dependencies. |
| <code><a href="#projen.python.UvConfiguration.property.indexStrategy">indexStrategy</a></code> | <code>string</code> | The strategy to use when resolving against multiple index URLs. |
| <code><a href="#projen.python.UvConfiguration.property.indexUrl">indexUrl</a></code> | <code>string</code> | The URL of the Python package index (by default: <https://pypi.org/simple>). |
| <code><a href="#projen.python.UvConfiguration.property.keyringProvider">keyringProvider</a></code> | <code>string</code> | Attempt to use `keyring` for authentication for index URLs. |
| <code><a href="#projen.python.UvConfiguration.property.linkMode">linkMode</a></code> | <code>string</code> | The method to use when installing packages from the global cache. |
| <code><a href="#projen.python.UvConfiguration.property.managed">managed</a></code> | <code>boolean</code> | Whether the project is managed by uv. |
| <code><a href="#projen.python.UvConfiguration.property.nativeTls">nativeTls</a></code> | <code>boolean</code> | Whether to load TLS certificates from the platform's native certificate store. |
| <code><a href="#projen.python.UvConfiguration.property.noBinary">noBinary</a></code> | <code>boolean</code> | Don't install pre-built wheels. |
| <code><a href="#projen.python.UvConfiguration.property.noBinaryPackage">noBinaryPackage</a></code> | <code>string[]</code> | Don't install pre-built wheels for a specific package. |
| <code><a href="#projen.python.UvConfiguration.property.noBuild">noBuild</a></code> | <code>boolean</code> | Don't build source distributions. |
| <code><a href="#projen.python.UvConfiguration.property.noBuildIsolation">noBuildIsolation</a></code> | <code>boolean</code> | Disable isolation when building source distributions. |
| <code><a href="#projen.python.UvConfiguration.property.noBuildIsolationPackage">noBuildIsolationPackage</a></code> | <code>string[]</code> | Disable isolation when building source distributions for a specific package. |
| <code><a href="#projen.python.UvConfiguration.property.noBuildPackage">noBuildPackage</a></code> | <code>string[]</code> | Don't build source distributions for a specific package. |
| <code><a href="#projen.python.UvConfiguration.property.noCache">noCache</a></code> | <code>boolean</code> | Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation. |
| <code><a href="#projen.python.UvConfiguration.property.noIndex">noIndex</a></code> | <code>boolean</code> | Ignore all registry indexes (e.g., PyPI), instead relying on direct URL dependencies and those provided via `--find-links`. |
| <code><a href="#projen.python.UvConfiguration.property.noSources">noSources</a></code> | <code>boolean</code> | Ignore the `tool.uv.sources` table when resolving dependencies. Used to lock against the standards-compliant, publishable package metadata, as opposed to using any local or Git sources. |
| <code><a href="#projen.python.UvConfiguration.property.offline">offline</a></code> | <code>boolean</code> | Disable network access, relying only on locally cached data and locally available files. |
| <code><a href="#projen.python.UvConfiguration.property.overrideDependencies">overrideDependencies</a></code> | <code>string[]</code> | PEP 508-style requirements, e.g., `ruff==0.5.0`, or `ruff @ https://...`. |
| <code><a href="#projen.python.UvConfiguration.property.package">package</a></code> | <code>boolean</code> | Whether the project should be considered a Python package, or a non-package ("virtual") project. |
| <code><a href="#projen.python.UvConfiguration.property.pip">pip</a></code> | <code><a href="#projen.python.PipOptions">PipOptions</a></code> | *No description.* |
| <code><a href="#projen.python.UvConfiguration.property.prerelease">prerelease</a></code> | <code>string</code> | The strategy to use when considering pre-release versions. |
| <code><a href="#projen.python.UvConfiguration.property.preview">preview</a></code> | <code>boolean</code> | Whether to enable experimental, preview features. |
| <code><a href="#projen.python.UvConfiguration.property.publishUrl">publishUrl</a></code> | <code>string</code> | The URL for publishing packages to the Python package index (by default: <https://upload.pypi.org/legacy/>). |
| <code><a href="#projen.python.UvConfiguration.property.pypyInstallMirror">pypyInstallMirror</a></code> | <code>string</code> | Mirror URL to use for downloading managed PyPy installations. |
| <code><a href="#projen.python.UvConfiguration.property.pythonDownloads">pythonDownloads</a></code> | <code>string</code> | Whether to allow Python downloads. |
| <code><a href="#projen.python.UvConfiguration.property.pythonDownloadsJsonUrl">pythonDownloadsJsonUrl</a></code> | <code>string</code> | URL pointing to JSON of custom Python installations. |
| <code><a href="#projen.python.UvConfiguration.property.pythonInstallMirror">pythonInstallMirror</a></code> | <code>string</code> | Mirror URL for downloading managed Python installations. |
| <code><a href="#projen.python.UvConfiguration.property.pythonPreference">pythonPreference</a></code> | <code>string</code> | Whether to prefer using Python installations that are already present on the system, or those that are downloaded and installed by uv. |
| <code><a href="#projen.python.UvConfiguration.property.reinstall">reinstall</a></code> | <code>boolean</code> | Reinstall all packages, regardless of whether they're already installed. |
| <code><a href="#projen.python.UvConfiguration.property.reinstallPackage">reinstallPackage</a></code> | <code>string[]</code> | Reinstall a specific package, regardless of whether it's already installed. |
| <code><a href="#projen.python.UvConfiguration.property.requiredEnvironments">requiredEnvironments</a></code> | <code>string[]</code> | A list of environment markers, e.g., `sys_platform == 'darwin'. |
| <code><a href="#projen.python.UvConfiguration.property.requiredVersion">requiredVersion</a></code> | <code>string</code> | Enforce a requirement on the version of uv. |
| <code><a href="#projen.python.UvConfiguration.property.resolution">resolution</a></code> | <code>string</code> | The strategy to use when selecting between the different compatible versions for a given package requirement. |
| <code><a href="#projen.python.UvConfiguration.property.sources">sources</a></code> | <code>{[ key: string ]: any[]}</code> | The sources to use when resolving dependencies. |
| <code><a href="#projen.python.UvConfiguration.property.trustedPublishing">trustedPublishing</a></code> | <code><a href="#projen.python.TrustedPublishing">TrustedPublishing</a></code> | Configure trusted publishing. |
| <code><a href="#projen.python.UvConfiguration.property.upgrade">upgrade</a></code> | <code>boolean</code> | Allow package upgrades, ignoring pinned versions in any existing output file. |
| <code><a href="#projen.python.UvConfiguration.property.upgradePackage">upgradePackage</a></code> | <code>string[]</code> | Allow upgrades for a specific package, ignoring pinned versions in any existing output file. |
| <code><a href="#projen.python.UvConfiguration.property.workspace">workspace</a></code> | <code><a href="#projen.python.ToolUvWorkspace">ToolUvWorkspace</a></code> | The workspace definition for the project, if any. |

---

##### `addBounds`<sup>Optional</sup> <a name="addBounds" id="projen.python.UvConfiguration.property.addBounds"></a>

```typescript
public readonly addBounds: string;
```

- *Type:* string

The default version specifier when adding a dependency.

When adding a dependency to the project, if no constraint or URL is provided, a constraint
is added based on the latest compatible version of the package. By default, a lower bound
constraint is used, e.g., `>=1.2.3`.

When `--frozen` is provided, no resolution is performed, and dependencies are always added
without constraints.

This option is in preview and may change in any future release.

---

##### `allowInsecureHost`<sup>Optional</sup> <a name="allowInsecureHost" id="projen.python.UvConfiguration.property.allowInsecureHost"></a>

```typescript
public readonly allowInsecureHost: string[];
```

- *Type:* string[]

Allow insecure connections to host.

Expects to receive either a hostname (e.g., `localhost`), a host-port pair (e.g.,
`localhost:8080`), or a URL (e.g., `https://localhost`).

WARNING: Hosts included in this list will not be verified against the system's certificate
store. Only use `--allow-insecure-host` in a secure network with verified sources, as it
bypasses SSL verification and could expose you to MITM attacks.

---

##### `buildBackend`<sup>Optional</sup> <a name="buildBackend" id="projen.python.UvConfiguration.property.buildBackend"></a>

```typescript
public readonly buildBackend: BuildBackendSettings;
```

- *Type:* <a href="#projen.python.BuildBackendSettings">BuildBackendSettings</a>

Configuration for the uv build backend.

Note that those settings only apply when using the `uv_build` backend, other build backends
(such as hatchling) have their own configuration.

---

##### `buildConstraintDependencies`<sup>Optional</sup> <a name="buildConstraintDependencies" id="projen.python.UvConfiguration.property.buildConstraintDependencies"></a>

```typescript
public readonly buildConstraintDependencies: string[];
```

- *Type:* string[]

PEP 508-style requirements, e.g., `ruff==0.5.0`, or `ruff @ https://...`.

---

##### `cacheDir`<sup>Optional</sup> <a name="cacheDir" id="projen.python.UvConfiguration.property.cacheDir"></a>

```typescript
public readonly cacheDir: string;
```

- *Type:* string

Path to the cache directory.

---

##### `cacheKeys`<sup>Optional</sup> <a name="cacheKeys" id="projen.python.UvConfiguration.property.cacheKeys"></a>

```typescript
public readonly cacheKeys: any[];
```

- *Type:* any[]

The keys to consider when caching builds for the project.

Cache keys enable you to specify the files or directories that should trigger a rebuild when
modified. By default, uv will rebuild a project whenever the `pyproject.toml`, `setup.py`,
or `setup.cfg` files in the project directory are modified, or if a `src` directory is
added or removed, i.e.:

```toml
cache-keys = [{ file = "pyproject.toml" }, { file = "setup.py" }, { file = "setup.cfg" }, { dir = "src" }]
```

As an example: if a project uses dynamic metadata to read its dependencies from a
`requirements.txt` file, you can specify `cache-keys = [{ file = "requirements.txt" }, { file = "pyproject.toml" }]`
to ensure that the project is rebuilt whenever the `requirements.txt` file is modified (in
addition to watching the `pyproject.toml`).

Globs are supported, following the syntax of the [`glob`](https://docs.rs/glob/0.3.1/glob/struct.Pattern.html)
crate. For example, to invalidate the cache whenever a `.toml` file in the project directory
or any of its subdirectories is modified, you can specify `cache-keys = [{ file = "*_/*.toml" }]`.
Note that the use of globs can be expensive, as uv may need to walk the filesystem to
determine whether any files have changed.

Cache keys can also include version control information. For example, if a project uses
`setuptools_scm` to read its version from a Git commit, you can specify `cache-keys = [{ git = { commit = true }, { file = "pyproject.toml" }]`
to include the current Git commit hash in the cache key (in addition to the
`pyproject.toml`). Git tags are also supported via `cache-keys = [{ git = { commit = true, tags = true } }]`.

Cache keys can also include environment variables. For example, if a project relies on
`MACOSX_DEPLOYMENT_TARGET` or other environment variables to determine its behavior, you can
specify `cache-keys = [{ env = "MACOSX_DEPLOYMENT_TARGET" }]` to invalidate the cache
whenever the environment variable changes.

Cache keys only affect the project defined by the `pyproject.toml` in which they're
specified (as opposed to, e.g., affecting all members in a workspace), and all paths and
globs are interpreted as relative to the project directory.

---

##### `checkUrl`<sup>Optional</sup> <a name="checkUrl" id="projen.python.UvConfiguration.property.checkUrl"></a>

```typescript
public readonly checkUrl: string;
```

- *Type:* string

Check an index URL for existing files to skip duplicate uploads.

This option allows retrying publishing that failed after only some, but not all files have
been uploaded, and handles error due to parallel uploads of the same file.

Before uploading, the index is checked. If the exact same file already exists in the index,
the file will not be uploaded. If an error occurred during the upload, the index is checked
again, to handle cases where the identical file was uploaded twice in parallel.

The exact behavior will vary based on the index. When uploading to PyPI, uploading the same
file succeeds even without `--check-url`, while most other indexes error.

The index must provide one of the supported hashes (SHA-256, SHA-384, or SHA-512).

---

##### `compileBytecode`<sup>Optional</sup> <a name="compileBytecode" id="projen.python.UvConfiguration.property.compileBytecode"></a>

```typescript
public readonly compileBytecode: boolean;
```

- *Type:* boolean

Compile Python files to bytecode after installation.

By default, uv does not compile Python (`.py`) files to bytecode (`__pycache__/*.pyc`);
instead, compilation is performed lazily the first time a module is imported. For use-cases
in which start time is critical, such as CLI applications and Docker containers, this option
can be enabled to trade longer installation times for faster start times.

When enabled, uv will process the entire site-packages directory (including packages that
are not being modified by the current operation) for consistency. Like pip, it will also
ignore errors.

---

##### `concurrentBuilds`<sup>Optional</sup> <a name="concurrentBuilds" id="projen.python.UvConfiguration.property.concurrentBuilds"></a>

```typescript
public readonly concurrentBuilds: number;
```

- *Type:* number
- *Default:* the number of available CPU cores.

The maximum number of source distributions that uv will build concurrently at any given time.

Defaults to the number of available CPU cores.

---

##### `concurrentDownloads`<sup>Optional</sup> <a name="concurrentDownloads" id="projen.python.UvConfiguration.property.concurrentDownloads"></a>

```typescript
public readonly concurrentDownloads: number;
```

- *Type:* number

The maximum number of in-flight concurrent downloads that uv will perform at any given time.

---

##### `concurrentInstalls`<sup>Optional</sup> <a name="concurrentInstalls" id="projen.python.UvConfiguration.property.concurrentInstalls"></a>

```typescript
public readonly concurrentInstalls: number;
```

- *Type:* number
- *Default:* the number of available CPU cores.

The number of threads used when installing and unzipping packages.

Defaults to the number of available CPU cores.

---

##### `configSettings`<sup>Optional</sup> <a name="configSettings" id="projen.python.UvConfiguration.property.configSettings"></a>

```typescript
public readonly configSettings: {[ key: string ]: any};
```

- *Type:* {[ key: string ]: any}

Settings to pass to the [PEP 517](https://peps.python.org/pep-0517/) build backend, specified as `KEY=VALUE` pairs.

---

##### `configSettingsPackage`<sup>Optional</sup> <a name="configSettingsPackage" id="projen.python.UvConfiguration.property.configSettingsPackage"></a>

```typescript
public readonly configSettingsPackage: {[ key: string ]: {[ key: string ]: any}};
```

- *Type:* {[ key: string ]: {[ key: string ]: any}}

Settings to pass to the [PEP 517](https://peps.python.org/pep-0517/) build backend for specific packages, specified as `KEY=VALUE` pairs.

Accepts a map from package names to string key-value pairs.

---

##### `conflicts`<sup>Optional</sup> <a name="conflicts" id="projen.python.UvConfiguration.property.conflicts"></a>

```typescript
public readonly conflicts: SchemaConflictItem[][];
```

- *Type:* <a href="#projen.python.SchemaConflictItem">SchemaConflictItem</a>[][]

A list of sets of conflicting groups or extras.

---

##### `constraintDependencies`<sup>Optional</sup> <a name="constraintDependencies" id="projen.python.UvConfiguration.property.constraintDependencies"></a>

```typescript
public readonly constraintDependencies: string[];
```

- *Type:* string[]

PEP 508-style requirements, e.g., `ruff==0.5.0`, or `ruff @ https://...`.

---

##### `defaultGroups`<sup>Optional</sup> <a name="defaultGroups" id="projen.python.UvConfiguration.property.defaultGroups"></a>

```typescript
public readonly defaultGroups: any;
```

- *Type:* any

The list of `dependency-groups` to install by default.

Can also be the literal `"all"` to default enable all groups.

---

##### `dependencyGroups`<sup>Optional</sup> <a name="dependencyGroups" id="projen.python.UvConfiguration.property.dependencyGroups"></a>

```typescript
public readonly dependencyGroups: {[ key: string ]: DependencyGroupSettings};
```

- *Type:* {[ key: string ]: <a href="#projen.python.DependencyGroupSettings">DependencyGroupSettings</a>}

Additional settings for `dependency-groups`.

Currently this can only be used to add `requires-python` constraints
to dependency groups (typically to inform uv that your dev tooling
has a higher python requirement than your actual project).

This cannot be used to define dependency groups, use the top-level
`[dependency-groups]` table for that.

---

##### `dependencyMetadata`<sup>Optional</sup> <a name="dependencyMetadata" id="projen.python.UvConfiguration.property.dependencyMetadata"></a>

```typescript
public readonly dependencyMetadata: StaticMetadata[];
```

- *Type:* <a href="#projen.python.StaticMetadata">StaticMetadata</a>[]

Pre-defined static metadata for dependencies of the project (direct or transitive).

When
provided, enables the resolver to use the specified metadata instead of querying the
registry or building the relevant package from source.

Metadata should be provided in adherence with the [Metadata 2.3](https://packaging.python.org/en/latest/specifications/core-metadata/)
standard, though only the following fields are respected:

- `name`: The name of the package.
- (Optional) `version`: The version of the package. If omitted, the metadata will be applied
to all versions of the package.
- (Optional) `requires-dist`: The dependencies of the package (e.g., `werkzeug>=0.14`).
- (Optional) `requires-python`: The Python version required by the package (e.g., `>=3.10`).
- (Optional) `provides-extra`: The extras provided by the package.

---

##### `devDependencies`<sup>Optional</sup> <a name="devDependencies" id="projen.python.UvConfiguration.property.devDependencies"></a>

```typescript
public readonly devDependencies: string[];
```

- *Type:* string[]

PEP 508-style requirements, e.g., `ruff==0.5.0`, or `ruff @ https://...`.

---

##### `environments`<sup>Optional</sup> <a name="environments" id="projen.python.UvConfiguration.property.environments"></a>

```typescript
public readonly environments: string[];
```

- *Type:* string[]

A list of environment markers, e.g., `python_version >= '3.6'`.

---

##### `excludeDependencies`<sup>Optional</sup> <a name="excludeDependencies" id="projen.python.UvConfiguration.property.excludeDependencies"></a>

```typescript
public readonly excludeDependencies: string[];
```

- *Type:* string[]

Package names to exclude, e.g., `werkzeug`, `numpy`.

---

##### `excludeNewer`<sup>Optional</sup> <a name="excludeNewer" id="projen.python.UvConfiguration.property.excludeNewer"></a>

```typescript
public readonly excludeNewer: string;
```

- *Type:* string

Limit candidate packages to those that were uploaded prior to a given point in time.

Accepts a superset of [RFC 3339](https://www.rfc-editor.org/rfc/rfc3339.html) (e.g.,
`2006-12-02T02:07:43Z`). A full timestamp is required to ensure that the resolver will
behave consistently across timezones.

---

##### `excludeNewerPackage`<sup>Optional</sup> <a name="excludeNewerPackage" id="projen.python.UvConfiguration.property.excludeNewerPackage"></a>

```typescript
public readonly excludeNewerPackage: {[ key: string ]: string};
```

- *Type:* {[ key: string ]: string}

Limit candidate packages for specific packages to those that were uploaded prior to the given date.

Accepts package-date pairs in a dictionary format.

---

##### `extraBuildDependencies`<sup>Optional</sup> <a name="extraBuildDependencies" id="projen.python.UvConfiguration.property.extraBuildDependencies"></a>

```typescript
public readonly extraBuildDependencies: {[ key: string ]: any[]};
```

- *Type:* {[ key: string ]: any[]}

Additional build dependencies for packages.

This allows extending the PEP 517 build environment for the project's dependencies with
additional packages. This is useful for packages that assume the presence of packages like
`pip`, and do not declare them as build dependencies.

---

##### `extraBuildVariables`<sup>Optional</sup> <a name="extraBuildVariables" id="projen.python.UvConfiguration.property.extraBuildVariables"></a>

```typescript
public readonly extraBuildVariables: {[ key: string ]: {[ key: string ]: string}};
```

- *Type:* {[ key: string ]: {[ key: string ]: string}}

Extra environment variables to set when building certain packages.

Environment variables will be added to the environment when building the
specified packages.

---

##### `extraIndexUrl`<sup>Optional</sup> <a name="extraIndexUrl" id="projen.python.UvConfiguration.property.extraIndexUrl"></a>

```typescript
public readonly extraIndexUrl: string[];
```

- *Type:* string[]

Extra URLs of package indexes to use, in addition to `--index-url`.

Accepts either a repository compliant with [PEP 503](https://peps.python.org/pep-0503/)
(the simple repository API), or a local directory laid out in the same format.

All indexes provided via this flag take priority over the index specified by
[`index_url`](#index-url) or [`index`](#index) with `default = true`. When multiple indexes
are provided, earlier values take priority.

To control uv's resolution strategy when multiple indexes are present, see
[`index_strategy`](#index-strategy).

(Deprecated: use `index` instead.)

---

##### `findLinks`<sup>Optional</sup> <a name="findLinks" id="projen.python.UvConfiguration.property.findLinks"></a>

```typescript
public readonly findLinks: string[];
```

- *Type:* string[]

Locations to search for candidate distributions, in addition to those found in the registry indexes.

If a path, the target must be a directory that contains packages as wheel files (`.whl`) or
source distributions (e.g., `.tar.gz` or `.zip`) at the top level.

If a URL, the page must contain a flat list of links to package files adhering to the
formats described above.

---

##### `forkStrategy`<sup>Optional</sup> <a name="forkStrategy" id="projen.python.UvConfiguration.property.forkStrategy"></a>

```typescript
public readonly forkStrategy: string;
```

- *Type:* string

The strategy to use when selecting multiple versions of a given package across Python versions and platforms.

By default, uv will optimize for selecting the latest version of each package for each
supported Python version (`requires-python`), while minimizing the number of selected
versions across platforms.

Under `fewest`, uv will minimize the number of selected versions for each package,
preferring older versions that are compatible with a wider range of supported Python
versions or platforms.

---

##### `index`<sup>Optional</sup> <a name="index" id="projen.python.UvConfiguration.property.index"></a>

```typescript
public readonly index: Index[];
```

- *Type:* <a href="#projen.python.Index">Index</a>[]

The indexes to use when resolving dependencies.

Accepts either a repository compliant with [PEP 503](https://peps.python.org/pep-0503/)
(the simple repository API), or a local directory laid out in the same format.

Indexes are considered in the order in which they're defined, such that the first-defined
index has the highest priority. Further, the indexes provided by this setting are given
higher priority than any indexes specified via [`index_url`](#index-url) or
[`extra_index_url`](#extra-index-url). uv will only consider the first index that contains
a given package, unless an alternative [index strategy](#index-strategy) is specified.

If an index is marked as `explicit = true`, it will be used exclusively for the
dependencies that select it explicitly via `[tool.uv.sources]`, as in:

```toml
[[tool.uv.index]]
name = "pytorch"
url = "https://download.pytorch.org/whl/cu121"
explicit = true

[tool.uv.sources]
torch = { index = "pytorch" }
```

If an index is marked as `default = true`, it will be moved to the end of the prioritized list, such that it is
given the lowest priority when resolving packages. Additionally, marking an index as default will disable the
PyPI default index.

---

##### `indexStrategy`<sup>Optional</sup> <a name="indexStrategy" id="projen.python.UvConfiguration.property.indexStrategy"></a>

```typescript
public readonly indexStrategy: string;
```

- *Type:* string

The strategy to use when resolving against multiple index URLs.

By default, uv will stop at the first index on which a given package is available, and
limit resolutions to those present on that first index (`first-index`). This prevents
"dependency confusion" attacks, whereby an attacker can upload a malicious package under the
same name to an alternate index.

---

##### `indexUrl`<sup>Optional</sup> <a name="indexUrl" id="projen.python.UvConfiguration.property.indexUrl"></a>

```typescript
public readonly indexUrl: string;
```

- *Type:* string

The URL of the Python package index (by default: <https://pypi.org/simple>).

Accepts either a repository compliant with [PEP 503](https://peps.python.org/pep-0503/)
(the simple repository API), or a local directory laid out in the same format.

The index provided by this setting is given lower priority than any indexes specified via
[`extra_index_url`](#extra-index-url) or [`index`](#index).

(Deprecated: use `index` instead.)

---

##### `keyringProvider`<sup>Optional</sup> <a name="keyringProvider" id="projen.python.UvConfiguration.property.keyringProvider"></a>

```typescript
public readonly keyringProvider: string;
```

- *Type:* string

Attempt to use `keyring` for authentication for index URLs.

At present, only `--keyring-provider subprocess` is supported, which configures uv to
use the `keyring` CLI to handle authentication.

---

##### `linkMode`<sup>Optional</sup> <a name="linkMode" id="projen.python.UvConfiguration.property.linkMode"></a>

```typescript
public readonly linkMode: string;
```

- *Type:* string
- *Default:* clone` (also known as Copy-on-Write) on macOS, and `hardlink` on Linux and

The method to use when installing packages from the global cache.

Defaults to `clone` (also known as Copy-on-Write) on macOS, and `hardlink` on Linux and
Windows.

WARNING: The use of symlink link mode is discouraged, as they create tight coupling between
the cache and the target environment. For example, clearing the cache (`uv cache clean`)
will break all installed packages by way of removing the underlying source files. Use
symlinks with caution.

---

##### `managed`<sup>Optional</sup> <a name="managed" id="projen.python.UvConfiguration.property.managed"></a>

```typescript
public readonly managed: boolean;
```

- *Type:* boolean

Whether the project is managed by uv.

If `false`, uv will ignore the project when
`uv run` is invoked.

---

##### `nativeTls`<sup>Optional</sup> <a name="nativeTls" id="projen.python.UvConfiguration.property.nativeTls"></a>

```typescript
public readonly nativeTls: boolean;
```

- *Type:* boolean

Whether to load TLS certificates from the platform's native certificate store.

By default, uv loads certificates from the bundled `webpki-roots` crate. The
`webpki-roots` are a reliable set of trust roots from Mozilla, and including them in uv
improves portability and performance (especially on macOS).

However, in some cases, you may want to use the platform's native certificate store,
especially if you're relying on a corporate trust root (e.g., for a mandatory proxy) that's
included in your system's certificate store.

---

##### `noBinary`<sup>Optional</sup> <a name="noBinary" id="projen.python.UvConfiguration.property.noBinary"></a>

```typescript
public readonly noBinary: boolean;
```

- *Type:* boolean

Don't install pre-built wheels.

The given packages will be built and installed from source. The resolver will still use
pre-built wheels to extract package metadata, if available.

---

##### `noBinaryPackage`<sup>Optional</sup> <a name="noBinaryPackage" id="projen.python.UvConfiguration.property.noBinaryPackage"></a>

```typescript
public readonly noBinaryPackage: string[];
```

- *Type:* string[]

Don't install pre-built wheels for a specific package.

---

##### `noBuild`<sup>Optional</sup> <a name="noBuild" id="projen.python.UvConfiguration.property.noBuild"></a>

```typescript
public readonly noBuild: boolean;
```

- *Type:* boolean

Don't build source distributions.

When enabled, resolving will not run arbitrary Python code. The cached wheels of
already-built source distributions will be reused, but operations that require building
distributions will exit with an error.

---

##### `noBuildIsolation`<sup>Optional</sup> <a name="noBuildIsolation" id="projen.python.UvConfiguration.property.noBuildIsolation"></a>

```typescript
public readonly noBuildIsolation: boolean;
```

- *Type:* boolean

Disable isolation when building source distributions.

Assumes that build dependencies specified by [PEP 518](https://peps.python.org/pep-0518/)
are already installed.

---

##### `noBuildIsolationPackage`<sup>Optional</sup> <a name="noBuildIsolationPackage" id="projen.python.UvConfiguration.property.noBuildIsolationPackage"></a>

```typescript
public readonly noBuildIsolationPackage: string[];
```

- *Type:* string[]

Disable isolation when building source distributions for a specific package.

Assumes that the packages' build dependencies specified by [PEP 518](https://peps.python.org/pep-0518/)
are already installed.

---

##### `noBuildPackage`<sup>Optional</sup> <a name="noBuildPackage" id="projen.python.UvConfiguration.property.noBuildPackage"></a>

```typescript
public readonly noBuildPackage: string[];
```

- *Type:* string[]

Don't build source distributions for a specific package.

---

##### `noCache`<sup>Optional</sup> <a name="noCache" id="projen.python.UvConfiguration.property.noCache"></a>

```typescript
public readonly noCache: boolean;
```

- *Type:* boolean

Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation.

---

##### `noIndex`<sup>Optional</sup> <a name="noIndex" id="projen.python.UvConfiguration.property.noIndex"></a>

```typescript
public readonly noIndex: boolean;
```

- *Type:* boolean

Ignore all registry indexes (e.g., PyPI), instead relying on direct URL dependencies and those provided via `--find-links`.

---

##### `noSources`<sup>Optional</sup> <a name="noSources" id="projen.python.UvConfiguration.property.noSources"></a>

```typescript
public readonly noSources: boolean;
```

- *Type:* boolean

Ignore the `tool.uv.sources` table when resolving dependencies. Used to lock against the standards-compliant, publishable package metadata, as opposed to using any local or Git sources.

---

##### `offline`<sup>Optional</sup> <a name="offline" id="projen.python.UvConfiguration.property.offline"></a>

```typescript
public readonly offline: boolean;
```

- *Type:* boolean

Disable network access, relying only on locally cached data and locally available files.

---

##### `overrideDependencies`<sup>Optional</sup> <a name="overrideDependencies" id="projen.python.UvConfiguration.property.overrideDependencies"></a>

```typescript
public readonly overrideDependencies: string[];
```

- *Type:* string[]

PEP 508-style requirements, e.g., `ruff==0.5.0`, or `ruff @ https://...`.

---

##### `package`<sup>Optional</sup> <a name="package" id="projen.python.UvConfiguration.property.package"></a>

```typescript
public readonly package: boolean;
```

- *Type:* boolean

Whether the project should be considered a Python package, or a non-package ("virtual") project.

Packages are built and installed into the virtual environment in editable mode and thus
require a build backend, while virtual projects are _not_ built or installed; instead, only
their dependencies are included in the virtual environment.

Creating a package requires that a `build-system` is present in the `pyproject.toml`, and
that the project adheres to a structure that adheres to the build backend's expectations
(e.g., a `src` layout).

---

##### `pip`<sup>Optional</sup> <a name="pip" id="projen.python.UvConfiguration.property.pip"></a>

```typescript
public readonly pip: PipOptions;
```

- *Type:* <a href="#projen.python.PipOptions">PipOptions</a>

---

##### `prerelease`<sup>Optional</sup> <a name="prerelease" id="projen.python.UvConfiguration.property.prerelease"></a>

```typescript
public readonly prerelease: string;
```

- *Type:* string

The strategy to use when considering pre-release versions.

By default, uv will accept pre-releases for packages that _only_ publish pre-releases,
along with first-party requirements that contain an explicit pre-release marker in the
declared specifiers (`if-necessary-or-explicit`).

---

##### `preview`<sup>Optional</sup> <a name="preview" id="projen.python.UvConfiguration.property.preview"></a>

```typescript
public readonly preview: boolean;
```

- *Type:* boolean

Whether to enable experimental, preview features.

---

##### `publishUrl`<sup>Optional</sup> <a name="publishUrl" id="projen.python.UvConfiguration.property.publishUrl"></a>

```typescript
public readonly publishUrl: string;
```

- *Type:* string

The URL for publishing packages to the Python package index (by default: <https://upload.pypi.org/legacy/>).

---

##### `pypyInstallMirror`<sup>Optional</sup> <a name="pypyInstallMirror" id="projen.python.UvConfiguration.property.pypyInstallMirror"></a>

```typescript
public readonly pypyInstallMirror: string;
```

- *Type:* string

Mirror URL to use for downloading managed PyPy installations.

By default, managed PyPy installations are downloaded from [downloads.python.org](https://downloads.python.org/).
This variable can be set to a mirror URL to use a different source for PyPy installations.
The provided URL will replace `https://downloads.python.org/pypy` in, e.g., `https://downloads.python.org/pypy/pypy3.8-v7.3.7-osx64.tar.bz2`.

Distributions can be read from a
local directory by using the `file://` URL scheme.

---

##### `pythonDownloads`<sup>Optional</sup> <a name="pythonDownloads" id="projen.python.UvConfiguration.property.pythonDownloads"></a>

```typescript
public readonly pythonDownloads: string;
```

- *Type:* string

Whether to allow Python downloads.

---

##### `pythonDownloadsJsonUrl`<sup>Optional</sup> <a name="pythonDownloadsJsonUrl" id="projen.python.UvConfiguration.property.pythonDownloadsJsonUrl"></a>

```typescript
public readonly pythonDownloadsJsonUrl: string;
```

- *Type:* string

URL pointing to JSON of custom Python installations.

Note that currently, only local paths are supported.

---

##### `pythonInstallMirror`<sup>Optional</sup> <a name="pythonInstallMirror" id="projen.python.UvConfiguration.property.pythonInstallMirror"></a>

```typescript
public readonly pythonInstallMirror: string;
```

- *Type:* string

Mirror URL for downloading managed Python installations.

By default, managed Python installations are downloaded from [`python-build-standalone`](https://github.com/astral-sh/python-build-standalone).
This variable can be set to a mirror URL to use a different source for Python installations.
The provided URL will replace `https://github.com/astral-sh/python-build-standalone/releases/download` in, e.g., `https://github.com/astral-sh/python-build-standalone/releases/download/20240713/cpython-3.12.4%2B20240713-aarch64-apple-darwin-install_only.tar.gz`.

Distributions can be read from a local directory by using the `file://` URL scheme.

---

##### `pythonPreference`<sup>Optional</sup> <a name="pythonPreference" id="projen.python.UvConfiguration.property.pythonPreference"></a>

```typescript
public readonly pythonPreference: string;
```

- *Type:* string

Whether to prefer using Python installations that are already present on the system, or those that are downloaded and installed by uv.

---

##### `reinstall`<sup>Optional</sup> <a name="reinstall" id="projen.python.UvConfiguration.property.reinstall"></a>

```typescript
public readonly reinstall: boolean;
```

- *Type:* boolean

Reinstall all packages, regardless of whether they're already installed.

Implies `refresh`.

---

##### `reinstallPackage`<sup>Optional</sup> <a name="reinstallPackage" id="projen.python.UvConfiguration.property.reinstallPackage"></a>

```typescript
public readonly reinstallPackage: string[];
```

- *Type:* string[]

Reinstall a specific package, regardless of whether it's already installed.

Implies
`refresh-package`.

---

##### `requiredEnvironments`<sup>Optional</sup> <a name="requiredEnvironments" id="projen.python.UvConfiguration.property.requiredEnvironments"></a>

```typescript
public readonly requiredEnvironments: string[];
```

- *Type:* string[]

A list of environment markers, e.g., `sys_platform == 'darwin'.

---

##### `requiredVersion`<sup>Optional</sup> <a name="requiredVersion" id="projen.python.UvConfiguration.property.requiredVersion"></a>

```typescript
public readonly requiredVersion: string;
```

- *Type:* string

Enforce a requirement on the version of uv.

If the version of uv does not meet the requirement at runtime, uv will exit
with an error.

Accepts a [PEP 440](https://peps.python.org/pep-0440/) specifier, like `==0.5.0` or `>=0.5.0`.

---

##### `resolution`<sup>Optional</sup> <a name="resolution" id="projen.python.UvConfiguration.property.resolution"></a>

```typescript
public readonly resolution: string;
```

- *Type:* string

The strategy to use when selecting between the different compatible versions for a given package requirement.

By default, uv will use the latest compatible version of each package (`highest`).

---

##### `sources`<sup>Optional</sup> <a name="sources" id="projen.python.UvConfiguration.property.sources"></a>

```typescript
public readonly sources: {[ key: string ]: any[]};
```

- *Type:* {[ key: string ]: any[]}

The sources to use when resolving dependencies.

`tool.uv.sources` enriches the dependency metadata with additional sources, incorporated
during development. A dependency source can be a Git repository, a URL, a local path, or an
alternative registry.

See [Dependencies](https://docs.astral.sh/uv/concepts/projects/dependencies/) for more.

---

##### `trustedPublishing`<sup>Optional</sup> <a name="trustedPublishing" id="projen.python.UvConfiguration.property.trustedPublishing"></a>

```typescript
public readonly trustedPublishing: TrustedPublishing;
```

- *Type:* <a href="#projen.python.TrustedPublishing">TrustedPublishing</a>

Configure trusted publishing.

By default, uv checks for trusted publishing when running in a supported environment, but
ignores it if it isn't configured.

uv's supported environments for trusted publishing include GitHub Actions and GitLab CI/CD.

---

##### `upgrade`<sup>Optional</sup> <a name="upgrade" id="projen.python.UvConfiguration.property.upgrade"></a>

```typescript
public readonly upgrade: boolean;
```

- *Type:* boolean

Allow package upgrades, ignoring pinned versions in any existing output file.

---

##### `upgradePackage`<sup>Optional</sup> <a name="upgradePackage" id="projen.python.UvConfiguration.property.upgradePackage"></a>

```typescript
public readonly upgradePackage: string[];
```

- *Type:* string[]

Allow upgrades for a specific package, ignoring pinned versions in any existing output file.

Accepts both standalone package names (`ruff`) and version specifiers (`ruff<0.5.0`).

---

##### `workspace`<sup>Optional</sup> <a name="workspace" id="projen.python.UvConfiguration.property.workspace"></a>

```typescript
public readonly workspace: ToolUvWorkspace;
```

- *Type:* <a href="#projen.python.ToolUvWorkspace">ToolUvWorkspace</a>

The workspace definition for the project, if any.

---

### VenvOptions <a name="VenvOptions" id="projen.python.VenvOptions"></a>

Options for venv.

#### Initializer <a name="Initializer" id="projen.python.VenvOptions.Initializer"></a>

```typescript
import { python } from 'projen'

const venvOptions: python.VenvOptions = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.VenvOptions.property.envdir">envdir</a></code> | <code>string</code> | Name of directory to store the environment in. |
| <code><a href="#projen.python.VenvOptions.property.pythonExec">pythonExec</a></code> | <code>string</code> | Path to the python executable to use. |

---

##### `envdir`<sup>Optional</sup> <a name="envdir" id="projen.python.VenvOptions.property.envdir"></a>

```typescript
public readonly envdir: string;
```

- *Type:* string
- *Default:* ".env"

Name of directory to store the environment in.

---

##### `pythonExec`<sup>Optional</sup> <a name="pythonExec" id="projen.python.VenvOptions.property.pythonExec"></a>

```typescript
public readonly pythonExec: string;
```

- *Type:* string
- *Default:* "python"

Path to the python executable to use.

---

### Version <a name="Version" id="projen.python.Version"></a>

Version configuration.

#### Initializer <a name="Initializer" id="projen.python.Version.Initializer"></a>

```typescript
import { python } from 'projen'

const version: python.Version = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.Version.property.path">path</a></code> | <code>string</code> | A relative path to a file containing the project version. |
| <code><a href="#projen.python.Version.property.pattern">pattern</a></code> | <code>string</code> | A regex pattern to extract the version. |
| <code><a href="#projen.python.Version.property.source">source</a></code> | <code>string</code> | A source to use for retrieving and updating the version. |

---

##### `path`<sup>Optional</sup> <a name="path" id="projen.python.Version.property.path"></a>

```typescript
public readonly path: string;
```

- *Type:* string

A relative path to a file containing the project version.

---

##### `pattern`<sup>Optional</sup> <a name="pattern" id="projen.python.Version.property.pattern"></a>

```typescript
public readonly pattern: string;
```

- *Type:* string

A regex pattern to extract the version.

---

##### `source`<sup>Optional</sup> <a name="source" id="projen.python.Version.property.source"></a>

```typescript
public readonly source: string;
```

- *Type:* string

A source to use for retrieving and updating the version.

---

### VersionSpec <a name="VersionSpec" id="projen.python.VersionSpec"></a>

#### Initializer <a name="Initializer" id="projen.python.VersionSpec.Initializer"></a>

```typescript
import { python } from 'projen'

const versionSpec: python.VersionSpec = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.VersionSpec.property.comparator">comparator</a></code> | <code><a href="#projen.python.VersionComparator">VersionComparator</a></code> | The comparison operator. |
| <code><a href="#projen.python.VersionSpec.property.version">version</a></code> | <code>string</code> | The version number. |

---

##### `comparator`<sup>Required</sup> <a name="comparator" id="projen.python.VersionSpec.property.comparator"></a>

```typescript
public readonly comparator: VersionComparator;
```

- *Type:* <a href="#projen.python.VersionComparator">VersionComparator</a>

The comparison operator.

---

##### `version`<sup>Required</sup> <a name="version" id="projen.python.VersionSpec.property.version"></a>

```typescript
public readonly version: string;
```

- *Type:* string

The version number.

---

### WheelDataIncludes <a name="WheelDataIncludes" id="projen.python.WheelDataIncludes"></a>

Data includes for wheels.

See `BuildBackendSettings::data`.

#### Initializer <a name="Initializer" id="projen.python.WheelDataIncludes.Initializer"></a>

```typescript
import { python } from 'projen'

const wheelDataIncludes: python.WheelDataIncludes = { ... }
```

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.WheelDataIncludes.property.data">data</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.WheelDataIncludes.property.headers">headers</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.WheelDataIncludes.property.platlib">platlib</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.WheelDataIncludes.property.purelib">purelib</a></code> | <code>string</code> | *No description.* |
| <code><a href="#projen.python.WheelDataIncludes.property.scripts">scripts</a></code> | <code>string</code> | *No description.* |

---

##### `data`<sup>Optional</sup> <a name="data" id="projen.python.WheelDataIncludes.property.data"></a>

```typescript
public readonly data: string;
```

- *Type:* string

---

##### `headers`<sup>Optional</sup> <a name="headers" id="projen.python.WheelDataIncludes.property.headers"></a>

```typescript
public readonly headers: string;
```

- *Type:* string

---

##### `platlib`<sup>Optional</sup> <a name="platlib" id="projen.python.WheelDataIncludes.property.platlib"></a>

```typescript
public readonly platlib: string;
```

- *Type:* string

---

##### `purelib`<sup>Optional</sup> <a name="purelib" id="projen.python.WheelDataIncludes.property.purelib"></a>

```typescript
public readonly purelib: string;
```

- *Type:* string

---

##### `scripts`<sup>Optional</sup> <a name="scripts" id="projen.python.WheelDataIncludes.property.scripts"></a>

```typescript
public readonly scripts: string;
```

- *Type:* string

---

## Classes <a name="Classes" id="Classes"></a>

### ImportSection <a name="ImportSection" id="projen.python.ImportSection"></a>


#### Static Functions <a name="Static Functions" id="Static Functions"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.ImportSection.fromString">fromString</a></code> | *No description.* |

---

##### `fromString` <a name="fromString" id="projen.python.ImportSection.fromString"></a>

```typescript
import { python } from 'projen'

python.ImportSection.fromString(value: string)
```

###### `value`<sup>Required</sup> <a name="value" id="projen.python.ImportSection.fromString.parameter.value"></a>

- *Type:* string

---

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.ImportSection.property.value">value</a></code> | <code>string</code> | *No description.* |

---

##### `value`<sup>Required</sup> <a name="value" id="projen.python.ImportSection.property.value"></a>

```typescript
public readonly value: string;
```

- *Type:* string

---


### PytestConfigurationLogAutoIndent <a name="PytestConfigurationLogAutoIndent" id="projen.python.PytestConfigurationLogAutoIndent"></a>

Allow selective auto-indentation of multiline log messages.


#### Static Functions <a name="Static Functions" id="Static Functions"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PytestConfigurationLogAutoIndent.fromBoolean">fromBoolean</a></code> | *No description.* |
| <code><a href="#projen.python.PytestConfigurationLogAutoIndent.fromNumber">fromNumber</a></code> | *No description.* |
| <code><a href="#projen.python.PytestConfigurationLogAutoIndent.fromString">fromString</a></code> | *No description.* |

---

##### `fromBoolean` <a name="fromBoolean" id="projen.python.PytestConfigurationLogAutoIndent.fromBoolean"></a>

```typescript
import { python } from 'projen'

python.PytestConfigurationLogAutoIndent.fromBoolean(value: boolean)
```

###### `value`<sup>Required</sup> <a name="value" id="projen.python.PytestConfigurationLogAutoIndent.fromBoolean.parameter.value"></a>

- *Type:* boolean

---

##### `fromNumber` <a name="fromNumber" id="projen.python.PytestConfigurationLogAutoIndent.fromNumber"></a>

```typescript
import { python } from 'projen'

python.PytestConfigurationLogAutoIndent.fromNumber(value: number)
```

###### `value`<sup>Required</sup> <a name="value" id="projen.python.PytestConfigurationLogAutoIndent.fromNumber.parameter.value"></a>

- *Type:* number

---

##### `fromString` <a name="fromString" id="projen.python.PytestConfigurationLogAutoIndent.fromString"></a>

```typescript
import { python } from 'projen'

python.PytestConfigurationLogAutoIndent.fromString(value: string)
```

###### `value`<sup>Required</sup> <a name="value" id="projen.python.PytestConfigurationLogAutoIndent.fromString.parameter.value"></a>

- *Type:* string

---

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PytestConfigurationLogAutoIndent.property.value">value</a></code> | <code>string \| number \| boolean</code> | *No description.* |

---

##### `value`<sup>Required</sup> <a name="value" id="projen.python.PytestConfigurationLogAutoIndent.property.value"></a>

```typescript
public readonly value: string | number | boolean;
```

- *Type:* string | number | boolean

---


### PytestConfigurationVerbosityAssertions <a name="PytestConfigurationVerbosityAssertions" id="projen.python.PytestConfigurationVerbosityAssertions"></a>

Set a verbosity level specifically for assertion related output, overriding the application wide level.


#### Static Functions <a name="Static Functions" id="Static Functions"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PytestConfigurationVerbosityAssertions.fromNumber">fromNumber</a></code> | *No description.* |
| <code><a href="#projen.python.PytestConfigurationVerbosityAssertions.fromString">fromString</a></code> | *No description.* |

---

##### `fromNumber` <a name="fromNumber" id="projen.python.PytestConfigurationVerbosityAssertions.fromNumber"></a>

```typescript
import { python } from 'projen'

python.PytestConfigurationVerbosityAssertions.fromNumber(value: number)
```

###### `value`<sup>Required</sup> <a name="value" id="projen.python.PytestConfigurationVerbosityAssertions.fromNumber.parameter.value"></a>

- *Type:* number

---

##### `fromString` <a name="fromString" id="projen.python.PytestConfigurationVerbosityAssertions.fromString"></a>

```typescript
import { python } from 'projen'

python.PytestConfigurationVerbosityAssertions.fromString(value: string)
```

###### `value`<sup>Required</sup> <a name="value" id="projen.python.PytestConfigurationVerbosityAssertions.fromString.parameter.value"></a>

- *Type:* string

---

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PytestConfigurationVerbosityAssertions.property.value">value</a></code> | <code>string \| number</code> | *No description.* |

---

##### `value`<sup>Required</sup> <a name="value" id="projen.python.PytestConfigurationVerbosityAssertions.property.value"></a>

```typescript
public readonly value: string | number;
```

- *Type:* string | number

---


### PytestConfigurationVerbositySubtests <a name="PytestConfigurationVerbositySubtests" id="projen.python.PytestConfigurationVerbositySubtests"></a>

Set the verbosity level specifically for passed subtests.

A value of 1 or higher will show output for passed subtests (failed subtests are always reported). Passed subtests output can be suppressed with the value 0, which overwrites the -v command-line option.


#### Static Functions <a name="Static Functions" id="Static Functions"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PytestConfigurationVerbositySubtests.fromNumber">fromNumber</a></code> | *No description.* |
| <code><a href="#projen.python.PytestConfigurationVerbositySubtests.fromString">fromString</a></code> | *No description.* |

---

##### `fromNumber` <a name="fromNumber" id="projen.python.PytestConfigurationVerbositySubtests.fromNumber"></a>

```typescript
import { python } from 'projen'

python.PytestConfigurationVerbositySubtests.fromNumber(value: number)
```

###### `value`<sup>Required</sup> <a name="value" id="projen.python.PytestConfigurationVerbositySubtests.fromNumber.parameter.value"></a>

- *Type:* number

---

##### `fromString` <a name="fromString" id="projen.python.PytestConfigurationVerbositySubtests.fromString"></a>

```typescript
import { python } from 'projen'

python.PytestConfigurationVerbositySubtests.fromString(value: string)
```

###### `value`<sup>Required</sup> <a name="value" id="projen.python.PytestConfigurationVerbositySubtests.fromString.parameter.value"></a>

- *Type:* string

---

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PytestConfigurationVerbositySubtests.property.value">value</a></code> | <code>string \| number</code> | *No description.* |

---

##### `value`<sup>Required</sup> <a name="value" id="projen.python.PytestConfigurationVerbositySubtests.property.value"></a>

```typescript
public readonly value: string | number;
```

- *Type:* string | number

---


### PytestConfigurationVerbosityTestCases <a name="PytestConfigurationVerbosityTestCases" id="projen.python.PytestConfigurationVerbosityTestCases"></a>

Set a verbosity level specifically for test case execution related output, overriding the application wide level.


#### Static Functions <a name="Static Functions" id="Static Functions"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PytestConfigurationVerbosityTestCases.fromNumber">fromNumber</a></code> | *No description.* |
| <code><a href="#projen.python.PytestConfigurationVerbosityTestCases.fromString">fromString</a></code> | *No description.* |

---

##### `fromNumber` <a name="fromNumber" id="projen.python.PytestConfigurationVerbosityTestCases.fromNumber"></a>

```typescript
import { python } from 'projen'

python.PytestConfigurationVerbosityTestCases.fromNumber(value: number)
```

###### `value`<sup>Required</sup> <a name="value" id="projen.python.PytestConfigurationVerbosityTestCases.fromNumber.parameter.value"></a>

- *Type:* number

---

##### `fromString` <a name="fromString" id="projen.python.PytestConfigurationVerbosityTestCases.fromString"></a>

```typescript
import { python } from 'projen'

python.PytestConfigurationVerbosityTestCases.fromString(value: string)
```

###### `value`<sup>Required</sup> <a name="value" id="projen.python.PytestConfigurationVerbosityTestCases.fromString.parameter.value"></a>

- *Type:* string

---

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.PytestConfigurationVerbosityTestCases.property.value">value</a></code> | <code>string \| number</code> | *No description.* |

---

##### `value`<sup>Required</sup> <a name="value" id="projen.python.PytestConfigurationVerbosityTestCases.property.value"></a>

```typescript
public readonly value: string | number;
```

- *Type:* string | number

---


## Protocols <a name="Protocols" id="Protocols"></a>

### IPackageProvider <a name="IPackageProvider" id="projen.python.IPackageProvider"></a>

- *Implemented By:* <a href="#projen.python.IPackageProvider">IPackageProvider</a>


#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.IPackageProvider.property.packages">packages</a></code> | <code>projen.Dependency[]</code> | An array of packages (may be dynamically generated). |

---

##### `packages`<sup>Required</sup> <a name="packages" id="projen.python.IPackageProvider.property.packages"></a>

```typescript
public readonly packages: Dependency[];
```

- *Type:* projen.Dependency[]

An array of packages (may be dynamically generated).

---

### IPythonDeps <a name="IPythonDeps" id="projen.python.IPythonDeps"></a>

- *Implemented By:* <a href="#projen.python.Hatch">Hatch</a>, <a href="#projen.python.PackageManagerBase">PackageManagerBase</a>, <a href="#projen.python.Pdm">Pdm</a>, <a href="#projen.python.Poetry">Poetry</a>, <a href="#projen.python.Setuptools">Setuptools</a>, <a href="#projen.python.Uv">Uv</a>, <a href="#projen.python.IPythonDeps">IPythonDeps</a>

#### Methods <a name="Methods" id="Methods"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.IPythonDeps.addDependency">addDependency</a></code> | Adds a runtime dependency. |
| <code><a href="#projen.python.IPythonDeps.addDevDependency">addDevDependency</a></code> | Adds a dev dependency. |
| <code><a href="#projen.python.IPythonDeps.installDependencies">installDependencies</a></code> | Installs dependencies (called during post-synthesis). |

---

##### `addDependency` <a name="addDependency" id="projen.python.IPythonDeps.addDependency"></a>

```typescript
public addDependency(spec: string): void
```

Adds a runtime dependency.

###### `spec`<sup>Required</sup> <a name="spec" id="projen.python.IPythonDeps.addDependency.parameter.spec"></a>

- *Type:* string

Format `<module>@<semver>`.

---

##### `addDevDependency` <a name="addDevDependency" id="projen.python.IPythonDeps.addDevDependency"></a>

```typescript
public addDevDependency(spec: string): void
```

Adds a dev dependency.

###### `spec`<sup>Required</sup> <a name="spec" id="projen.python.IPythonDeps.addDevDependency.parameter.spec"></a>

- *Type:* string

Format `<module>@<semver>`.

---

##### `installDependencies` <a name="installDependencies" id="projen.python.IPythonDeps.installDependencies"></a>

```typescript
public installDependencies(): void
```

Installs dependencies (called during post-synthesis).

#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.IPythonDeps.property.installCiTask">installCiTask</a></code> | <code>projen.Task</code> | A task that installs and updates dependencies. |

---

##### `installCiTask`<sup>Required</sup> <a name="installCiTask" id="projen.python.IPythonDeps.property.installCiTask"></a>

```typescript
public readonly installCiTask: Task;
```

- *Type:* projen.Task

A task that installs and updates dependencies.

---

### IPythonEnv <a name="IPythonEnv" id="projen.python.IPythonEnv"></a>

- *Implemented By:* <a href="#projen.python.Hatch">Hatch</a>, <a href="#projen.python.PackageManagerBase">PackageManagerBase</a>, <a href="#projen.python.Pdm">Pdm</a>, <a href="#projen.python.Poetry">Poetry</a>, <a href="#projen.python.Setuptools">Setuptools</a>, <a href="#projen.python.Uv">Uv</a>, <a href="#projen.python.Venv">Venv</a>, <a href="#projen.python.IPythonEnv">IPythonEnv</a>

#### Methods <a name="Methods" id="Methods"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.IPythonEnv.setupEnvironment">setupEnvironment</a></code> | Initializes the virtual environment if it doesn't exist (called during post-synthesis). |

---

##### `setupEnvironment` <a name="setupEnvironment" id="projen.python.IPythonEnv.setupEnvironment"></a>

```typescript
public setupEnvironment(): void
```

Initializes the virtual environment if it doesn't exist (called during post-synthesis).


### IPythonLinting <a name="IPythonLinting" id="projen.python.IPythonLinting"></a>

- *Implemented By:* <a href="#projen.python.Hatch">Hatch</a>, <a href="#projen.python.PackageManagerBase">PackageManagerBase</a>, <a href="#projen.python.Pdm">Pdm</a>, <a href="#projen.python.Poetry">Poetry</a>, <a href="#projen.python.Setuptools">Setuptools</a>, <a href="#projen.python.Uv">Uv</a>, <a href="#projen.python.IPythonLinting">IPythonLinting</a>


#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.IPythonLinting.property.formatTask">formatTask</a></code> | <code>projen.Task</code> | A task that runs formatting. |
| <code><a href="#projen.python.IPythonLinting.property.lintTask">lintTask</a></code> | <code>projen.Task</code> | A task that runs linting. |
| <code><a href="#projen.python.IPythonLinting.property.typeCheckTask">typeCheckTask</a></code> | <code>projen.Task</code> | A task that runs static type checking. |

---

##### `formatTask`<sup>Optional</sup> <a name="formatTask" id="projen.python.IPythonLinting.property.formatTask"></a>

```typescript
public readonly formatTask: Task;
```

- *Type:* projen.Task

A task that runs formatting.

---

##### `lintTask`<sup>Optional</sup> <a name="lintTask" id="projen.python.IPythonLinting.property.lintTask"></a>

```typescript
public readonly lintTask: Task;
```

- *Type:* projen.Task

A task that runs linting.

---

##### `typeCheckTask`<sup>Optional</sup> <a name="typeCheckTask" id="projen.python.IPythonLinting.property.typeCheckTask"></a>

```typescript
public readonly typeCheckTask: Task;
```

- *Type:* projen.Task

A task that runs static type checking.

---

### IPythonPackaging <a name="IPythonPackaging" id="projen.python.IPythonPackaging"></a>

- *Implemented By:* <a href="#projen.python.Hatch">Hatch</a>, <a href="#projen.python.PackageManagerBase">PackageManagerBase</a>, <a href="#projen.python.Pdm">Pdm</a>, <a href="#projen.python.Poetry">Poetry</a>, <a href="#projen.python.Setuptools">Setuptools</a>, <a href="#projen.python.Uv">Uv</a>, <a href="#projen.python.IPythonPackaging">IPythonPackaging</a>


#### Properties <a name="Properties" id="Properties"></a>

| **Name** | **Type** | **Description** |
| --- | --- | --- |
| <code><a href="#projen.python.IPythonPackaging.property.publishTask">publishTask</a></code> | <code>projen.Task</code> | A task that uploads the package to a package repository. |

---

##### `publishTask`<sup>Required</sup> <a name="publishTask" id="projen.python.IPythonPackaging.property.publishTask"></a>

```typescript
public readonly publishTask: Task;
```

- *Type:* projen.Task

A task that uploads the package to a package repository.

---

## Enums <a name="Enums" id="Enums"></a>

### BlackConfigurationTargetVersion <a name="BlackConfigurationTargetVersion" id="projen.python.BlackConfigurationTargetVersion"></a>

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.BlackConfigurationTargetVersion.PY33">PY33</a></code> | py33. |
| <code><a href="#projen.python.BlackConfigurationTargetVersion.PY34">PY34</a></code> | py34. |
| <code><a href="#projen.python.BlackConfigurationTargetVersion.PY35">PY35</a></code> | py35. |
| <code><a href="#projen.python.BlackConfigurationTargetVersion.PY36">PY36</a></code> | py36. |
| <code><a href="#projen.python.BlackConfigurationTargetVersion.PY37">PY37</a></code> | py37. |
| <code><a href="#projen.python.BlackConfigurationTargetVersion.PY38">PY38</a></code> | py38. |
| <code><a href="#projen.python.BlackConfigurationTargetVersion.PY39">PY39</a></code> | py39. |
| <code><a href="#projen.python.BlackConfigurationTargetVersion.PY310">PY310</a></code> | py310. |
| <code><a href="#projen.python.BlackConfigurationTargetVersion.PY311">PY311</a></code> | py311. |
| <code><a href="#projen.python.BlackConfigurationTargetVersion.PY312">PY312</a></code> | py312. |
| <code><a href="#projen.python.BlackConfigurationTargetVersion.PY313">PY313</a></code> | py313. |

---

##### `PY33` <a name="PY33" id="projen.python.BlackConfigurationTargetVersion.PY33"></a>

py33.

---


##### `PY34` <a name="PY34" id="projen.python.BlackConfigurationTargetVersion.PY34"></a>

py34.

---


##### `PY35` <a name="PY35" id="projen.python.BlackConfigurationTargetVersion.PY35"></a>

py35.

---


##### `PY36` <a name="PY36" id="projen.python.BlackConfigurationTargetVersion.PY36"></a>

py36.

---


##### `PY37` <a name="PY37" id="projen.python.BlackConfigurationTargetVersion.PY37"></a>

py37.

---


##### `PY38` <a name="PY38" id="projen.python.BlackConfigurationTargetVersion.PY38"></a>

py38.

---


##### `PY39` <a name="PY39" id="projen.python.BlackConfigurationTargetVersion.PY39"></a>

py39.

---


##### `PY310` <a name="PY310" id="projen.python.BlackConfigurationTargetVersion.PY310"></a>

py310.

---


##### `PY311` <a name="PY311" id="projen.python.BlackConfigurationTargetVersion.PY311"></a>

py311.

---


##### `PY312` <a name="PY312" id="projen.python.BlackConfigurationTargetVersion.PY312"></a>

py312.

---


##### `PY313` <a name="PY313" id="projen.python.BlackConfigurationTargetVersion.PY313"></a>

py313.

---


### ConstantType <a name="ConstantType" id="projen.python.ConstantType"></a>

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.ConstantType.BYTES">BYTES</a></code> | bytes. |
| <code><a href="#projen.python.ConstantType.COMPLEX">COMPLEX</a></code> | complex. |
| <code><a href="#projen.python.ConstantType.FLOAT">FLOAT</a></code> | float. |
| <code><a href="#projen.python.ConstantType.INT">INT</a></code> | int. |
| <code><a href="#projen.python.ConstantType.STR">STR</a></code> | str. |

---

##### `BYTES` <a name="BYTES" id="projen.python.ConstantType.BYTES"></a>

bytes.

---


##### `COMPLEX` <a name="COMPLEX" id="projen.python.ConstantType.COMPLEX"></a>

complex.

---


##### `FLOAT` <a name="FLOAT" id="projen.python.ConstantType.FLOAT"></a>

float.

---


##### `INT` <a name="INT" id="projen.python.ConstantType.INT"></a>

int.

---


##### `STR` <a name="STR" id="projen.python.ConstantType.STR"></a>

str.

---


### FollowImports <a name="FollowImports" id="projen.python.FollowImports"></a>

Directs what to do with imports when the imported module is found as a `.py` file and not part of the files, modules and packages provided on the command line.

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.FollowImports.NORMAL">NORMAL</a></code> | normal. |
| <code><a href="#projen.python.FollowImports.SILENT">SILENT</a></code> | silent. |
| <code><a href="#projen.python.FollowImports.SKIP">SKIP</a></code> | skip. |
| <code><a href="#projen.python.FollowImports.ERROR">ERROR</a></code> | error. |

---

##### `NORMAL` <a name="NORMAL" id="projen.python.FollowImports.NORMAL"></a>

normal.

---


##### `SILENT` <a name="SILENT" id="projen.python.FollowImports.SILENT"></a>

silent.

---


##### `SKIP` <a name="SKIP" id="projen.python.FollowImports.SKIP"></a>

skip.

---


##### `ERROR` <a name="ERROR" id="projen.python.FollowImports.ERROR"></a>

error.

---


### LogLevel <a name="LogLevel" id="projen.python.LogLevel"></a>

A minimum log level.

Can be level name or integer value.

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.LogLevel.CRITICAL">CRITICAL</a></code> | CRITICAL. |
| <code><a href="#projen.python.LogLevel.ERROR">ERROR</a></code> | ERROR. |
| <code><a href="#projen.python.LogLevel.WARNING">WARNING</a></code> | WARNING. |
| <code><a href="#projen.python.LogLevel.INFO">INFO</a></code> | INFO. |
| <code><a href="#projen.python.LogLevel.DEBUG">DEBUG</a></code> | DEBUG. |
| <code><a href="#projen.python.LogLevel.NOTSET">NOTSET</a></code> | NOTSET. |

---

##### `CRITICAL` <a name="CRITICAL" id="projen.python.LogLevel.CRITICAL"></a>

CRITICAL.

---


##### `ERROR` <a name="ERROR" id="projen.python.LogLevel.ERROR"></a>

ERROR.

---


##### `WARNING` <a name="WARNING" id="projen.python.LogLevel.WARNING"></a>

WARNING.

---


##### `INFO` <a name="INFO" id="projen.python.LogLevel.INFO"></a>

INFO.

---


##### `DEBUG` <a name="DEBUG" id="projen.python.LogLevel.DEBUG"></a>

DEBUG.

---


##### `NOTSET` <a name="NOTSET" id="projen.python.LogLevel.NOTSET"></a>

NOTSET.

---


### OutputFormat <a name="OutputFormat" id="projen.python.OutputFormat"></a>

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.OutputFormat.CONCISE">CONCISE</a></code> | concise. |
| <code><a href="#projen.python.OutputFormat.FULL">FULL</a></code> | full. |
| <code><a href="#projen.python.OutputFormat.JSON">JSON</a></code> | json. |
| <code><a href="#projen.python.OutputFormat.JSON_HYPHEN_LINES">JSON_HYPHEN_LINES</a></code> | json-lines. |
| <code><a href="#projen.python.OutputFormat.JUNIT">JUNIT</a></code> | junit. |
| <code><a href="#projen.python.OutputFormat.GROUPED">GROUPED</a></code> | grouped. |
| <code><a href="#projen.python.OutputFormat.GITHUB">GITHUB</a></code> | github. |
| <code><a href="#projen.python.OutputFormat.GITLAB">GITLAB</a></code> | gitlab. |
| <code><a href="#projen.python.OutputFormat.PYLINT">PYLINT</a></code> | pylint. |
| <code><a href="#projen.python.OutputFormat.RDJSON">RDJSON</a></code> | rdjson. |
| <code><a href="#projen.python.OutputFormat.AZURE">AZURE</a></code> | azure. |
| <code><a href="#projen.python.OutputFormat.SARIF">SARIF</a></code> | sarif. |

---

##### `CONCISE` <a name="CONCISE" id="projen.python.OutputFormat.CONCISE"></a>

concise.

---


##### `FULL` <a name="FULL" id="projen.python.OutputFormat.FULL"></a>

full.

---


##### `JSON` <a name="JSON" id="projen.python.OutputFormat.JSON"></a>

json.

---


##### `JSON_HYPHEN_LINES` <a name="JSON_HYPHEN_LINES" id="projen.python.OutputFormat.JSON_HYPHEN_LINES"></a>

json-lines.

---


##### `JUNIT` <a name="JUNIT" id="projen.python.OutputFormat.JUNIT"></a>

junit.

---


##### `GROUPED` <a name="GROUPED" id="projen.python.OutputFormat.GROUPED"></a>

grouped.

---


##### `GITHUB` <a name="GITHUB" id="projen.python.OutputFormat.GITHUB"></a>

github.

---


##### `GITLAB` <a name="GITLAB" id="projen.python.OutputFormat.GITLAB"></a>

gitlab.

---


##### `PYLINT` <a name="PYLINT" id="projen.python.OutputFormat.PYLINT"></a>

pylint.

---


##### `RDJSON` <a name="RDJSON" id="projen.python.OutputFormat.RDJSON"></a>

rdjson.

---


##### `AZURE` <a name="AZURE" id="projen.python.OutputFormat.AZURE"></a>

azure.

---


##### `SARIF` <a name="SARIF" id="projen.python.OutputFormat.SARIF"></a>

sarif.

---


### PackageSourceType <a name="PackageSourceType" id="projen.python.PackageSourceType"></a>

index or find_links.

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PackageSourceType.INDEX">INDEX</a></code> | index. |
| <code><a href="#projen.python.PackageSourceType.FIND_UNDERSCORE_LINKS">FIND_UNDERSCORE_LINKS</a></code> | find_links. |

---

##### `INDEX` <a name="INDEX" id="projen.python.PackageSourceType.INDEX"></a>

index.

---


##### `FIND_UNDERSCORE_LINKS` <a name="FIND_UNDERSCORE_LINKS" id="projen.python.PackageSourceType.FIND_UNDERSCORE_LINKS"></a>

find_links.

---


### ParametrizeNameType <a name="ParametrizeNameType" id="projen.python.ParametrizeNameType"></a>

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.ParametrizeNameType.CSV">CSV</a></code> | csv. |
| <code><a href="#projen.python.ParametrizeNameType.TUPLE">TUPLE</a></code> | tuple. |
| <code><a href="#projen.python.ParametrizeNameType.LIST">LIST</a></code> | list. |

---

##### `CSV` <a name="CSV" id="projen.python.ParametrizeNameType.CSV"></a>

csv.

---


##### `TUPLE` <a name="TUPLE" id="projen.python.ParametrizeNameType.TUPLE"></a>

tuple.

---


##### `LIST` <a name="LIST" id="projen.python.ParametrizeNameType.LIST"></a>

list.

---


### ParametrizeValuesRowType <a name="ParametrizeValuesRowType" id="projen.python.ParametrizeValuesRowType"></a>

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.ParametrizeValuesRowType.TUPLE">TUPLE</a></code> | tuple. |
| <code><a href="#projen.python.ParametrizeValuesRowType.LIST">LIST</a></code> | list. |

---

##### `TUPLE` <a name="TUPLE" id="projen.python.ParametrizeValuesRowType.TUPLE"></a>

tuple.

---


##### `LIST` <a name="LIST" id="projen.python.ParametrizeValuesRowType.LIST"></a>

list.

---


### ParametrizeValuesType <a name="ParametrizeValuesType" id="projen.python.ParametrizeValuesType"></a>

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.ParametrizeValuesType.TUPLE">TUPLE</a></code> | tuple. |
| <code><a href="#projen.python.ParametrizeValuesType.LIST">LIST</a></code> | list. |

---

##### `TUPLE` <a name="TUPLE" id="projen.python.ParametrizeValuesType.TUPLE"></a>

tuple.

---


##### `LIST` <a name="LIST" id="projen.python.ParametrizeValuesType.LIST"></a>

list.

---


### PdmConfigurationBuildEditableBackend <a name="PdmConfigurationBuildEditableBackend" id="projen.python.PdmConfigurationBuildEditableBackend"></a>

Format of editable wheels.

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PdmConfigurationBuildEditableBackend.PATH">PATH</a></code> | path. |
| <code><a href="#projen.python.PdmConfigurationBuildEditableBackend.EDITABLES">EDITABLES</a></code> | editables. |

---

##### `PATH` <a name="PATH" id="projen.python.PdmConfigurationBuildEditableBackend.PATH"></a>

path.

---


##### `EDITABLES` <a name="EDITABLES" id="projen.python.PdmConfigurationBuildEditableBackend.EDITABLES"></a>

editables.

---


### PdmConfigurationVersionSource <a name="PdmConfigurationVersionSource" id="projen.python.PdmConfigurationVersionSource"></a>

Dynamic version source.

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PdmConfigurationVersionSource.FILE">FILE</a></code> | file. |
| <code><a href="#projen.python.PdmConfigurationVersionSource.SCM">SCM</a></code> | scm. |
| <code><a href="#projen.python.PdmConfigurationVersionSource.CALL">CALL</a></code> | call. |

---

##### `FILE` <a name="FILE" id="projen.python.PdmConfigurationVersionSource.FILE"></a>

file.

---


##### `SCM` <a name="SCM" id="projen.python.PdmConfigurationVersionSource.SCM"></a>

scm.

---


##### `CALL` <a name="CALL" id="projen.python.PdmConfigurationVersionSource.CALL"></a>

call.

---


### Platform <a name="Platform" id="projen.python.Platform"></a>

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.Platform.LINUX">LINUX</a></code> | linux. |
| <code><a href="#projen.python.Platform.WINDOWS">WINDOWS</a></code> | windows. |
| <code><a href="#projen.python.Platform.MACOS">MACOS</a></code> | macos. |

---

##### `LINUX` <a name="LINUX" id="projen.python.Platform.LINUX"></a>

linux.

---


##### `WINDOWS` <a name="WINDOWS" id="projen.python.Platform.WINDOWS"></a>

windows.

---


##### `MACOS` <a name="MACOS" id="projen.python.Platform.MACOS"></a>

macos.

---


### PoetryPackageFormat <a name="PoetryPackageFormat" id="projen.python.PoetryPackageFormat"></a>

A Python packaging format.

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PoetryPackageFormat.SDIST">SDIST</a></code> | sdist. |
| <code><a href="#projen.python.PoetryPackageFormat.WHEEL">WHEEL</a></code> | wheel. |

---

##### `SDIST` <a name="SDIST" id="projen.python.PoetryPackageFormat.SDIST"></a>

sdist.

---


##### `WHEEL` <a name="WHEEL" id="projen.python.PoetryPackageFormat.WHEEL"></a>

wheel.

---


### PyprojectTomlProjectDynamic <a name="PyprojectTomlProjectDynamic" id="projen.python.PyprojectTomlProjectDynamic"></a>

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PyprojectTomlProjectDynamic.VERSION">VERSION</a></code> | version. |
| <code><a href="#projen.python.PyprojectTomlProjectDynamic.DESCRIPTION">DESCRIPTION</a></code> | description. |
| <code><a href="#projen.python.PyprojectTomlProjectDynamic.README">README</a></code> | readme. |
| <code><a href="#projen.python.PyprojectTomlProjectDynamic.REQUIRES_HYPHEN_PYTHON">REQUIRES_HYPHEN_PYTHON</a></code> | requires-python. |
| <code><a href="#projen.python.PyprojectTomlProjectDynamic.LICENSE">LICENSE</a></code> | license. |
| <code><a href="#projen.python.PyprojectTomlProjectDynamic.LICENSE_HYPHEN_FILES">LICENSE_HYPHEN_FILES</a></code> | license-files. |
| <code><a href="#projen.python.PyprojectTomlProjectDynamic.AUTHORS">AUTHORS</a></code> | authors. |
| <code><a href="#projen.python.PyprojectTomlProjectDynamic.MAINTAINERS">MAINTAINERS</a></code> | maintainers. |
| <code><a href="#projen.python.PyprojectTomlProjectDynamic.KEYWORDS">KEYWORDS</a></code> | keywords. |
| <code><a href="#projen.python.PyprojectTomlProjectDynamic.CLASSIFIERS">CLASSIFIERS</a></code> | classifiers. |
| <code><a href="#projen.python.PyprojectTomlProjectDynamic.URLS">URLS</a></code> | urls. |
| <code><a href="#projen.python.PyprojectTomlProjectDynamic.SCRIPTS">SCRIPTS</a></code> | scripts. |
| <code><a href="#projen.python.PyprojectTomlProjectDynamic.GUI_HYPHEN_SCRIPTS">GUI_HYPHEN_SCRIPTS</a></code> | gui-scripts. |
| <code><a href="#projen.python.PyprojectTomlProjectDynamic.ENTRY_HYPHEN_POINTS">ENTRY_HYPHEN_POINTS</a></code> | entry-points. |
| <code><a href="#projen.python.PyprojectTomlProjectDynamic.DEPENDENCIES">DEPENDENCIES</a></code> | dependencies. |
| <code><a href="#projen.python.PyprojectTomlProjectDynamic.OPTIONAL_HYPHEN_DEPENDENCIES">OPTIONAL_HYPHEN_DEPENDENCIES</a></code> | optional-dependencies. |
| <code><a href="#projen.python.PyprojectTomlProjectDynamic.IMPORT_HYPHEN_NAMES">IMPORT_HYPHEN_NAMES</a></code> | import-names. |
| <code><a href="#projen.python.PyprojectTomlProjectDynamic.IMPORT_HYPHEN_NAMESPACES">IMPORT_HYPHEN_NAMESPACES</a></code> | import-namespaces. |

---

##### `VERSION` <a name="VERSION" id="projen.python.PyprojectTomlProjectDynamic.VERSION"></a>

version.

---


##### `DESCRIPTION` <a name="DESCRIPTION" id="projen.python.PyprojectTomlProjectDynamic.DESCRIPTION"></a>

description.

---


##### `README` <a name="README" id="projen.python.PyprojectTomlProjectDynamic.README"></a>

readme.

---


##### `REQUIRES_HYPHEN_PYTHON` <a name="REQUIRES_HYPHEN_PYTHON" id="projen.python.PyprojectTomlProjectDynamic.REQUIRES_HYPHEN_PYTHON"></a>

requires-python.

---


##### `LICENSE` <a name="LICENSE" id="projen.python.PyprojectTomlProjectDynamic.LICENSE"></a>

license.

---


##### `LICENSE_HYPHEN_FILES` <a name="LICENSE_HYPHEN_FILES" id="projen.python.PyprojectTomlProjectDynamic.LICENSE_HYPHEN_FILES"></a>

license-files.

---


##### `AUTHORS` <a name="AUTHORS" id="projen.python.PyprojectTomlProjectDynamic.AUTHORS"></a>

authors.

---


##### `MAINTAINERS` <a name="MAINTAINERS" id="projen.python.PyprojectTomlProjectDynamic.MAINTAINERS"></a>

maintainers.

---


##### `KEYWORDS` <a name="KEYWORDS" id="projen.python.PyprojectTomlProjectDynamic.KEYWORDS"></a>

keywords.

---


##### `CLASSIFIERS` <a name="CLASSIFIERS" id="projen.python.PyprojectTomlProjectDynamic.CLASSIFIERS"></a>

classifiers.

---


##### `URLS` <a name="URLS" id="projen.python.PyprojectTomlProjectDynamic.URLS"></a>

urls.

---


##### `SCRIPTS` <a name="SCRIPTS" id="projen.python.PyprojectTomlProjectDynamic.SCRIPTS"></a>

scripts.

---


##### `GUI_HYPHEN_SCRIPTS` <a name="GUI_HYPHEN_SCRIPTS" id="projen.python.PyprojectTomlProjectDynamic.GUI_HYPHEN_SCRIPTS"></a>

gui-scripts.

---


##### `ENTRY_HYPHEN_POINTS` <a name="ENTRY_HYPHEN_POINTS" id="projen.python.PyprojectTomlProjectDynamic.ENTRY_HYPHEN_POINTS"></a>

entry-points.

---


##### `DEPENDENCIES` <a name="DEPENDENCIES" id="projen.python.PyprojectTomlProjectDynamic.DEPENDENCIES"></a>

dependencies.

---


##### `OPTIONAL_HYPHEN_DEPENDENCIES` <a name="OPTIONAL_HYPHEN_DEPENDENCIES" id="projen.python.PyprojectTomlProjectDynamic.OPTIONAL_HYPHEN_DEPENDENCIES"></a>

optional-dependencies.

---


##### `IMPORT_HYPHEN_NAMES` <a name="IMPORT_HYPHEN_NAMES" id="projen.python.PyprojectTomlProjectDynamic.IMPORT_HYPHEN_NAMES"></a>

import-names.

---


##### `IMPORT_HYPHEN_NAMESPACES` <a name="IMPORT_HYPHEN_NAMESPACES" id="projen.python.PyprojectTomlProjectDynamic.IMPORT_HYPHEN_NAMESPACES"></a>

import-namespaces.

---


### PyrightConfigurationTypeCheckingMode <a name="PyrightConfigurationTypeCheckingMode" id="projen.python.PyrightConfigurationTypeCheckingMode"></a>

Specifies the default rule set to use.

Some rules can be overridden using additional configuration flags documented below. If set to `off`, all type-checking rules are disabled, but Python syntax and semantic errors are still reported.

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PyrightConfigurationTypeCheckingMode.OFF">OFF</a></code> | off. |
| <code><a href="#projen.python.PyrightConfigurationTypeCheckingMode.BASIC">BASIC</a></code> | basic. |
| <code><a href="#projen.python.PyrightConfigurationTypeCheckingMode.STANDARD">STANDARD</a></code> | standard. |
| <code><a href="#projen.python.PyrightConfigurationTypeCheckingMode.STRICT">STRICT</a></code> | strict. |

---

##### `OFF` <a name="OFF" id="projen.python.PyrightConfigurationTypeCheckingMode.OFF"></a>

off.

---


##### `BASIC` <a name="BASIC" id="projen.python.PyrightConfigurationTypeCheckingMode.BASIC"></a>

basic.

---


##### `STANDARD` <a name="STANDARD" id="projen.python.PyrightConfigurationTypeCheckingMode.STANDARD"></a>

standard.

---


##### `STRICT` <a name="STRICT" id="projen.python.PyrightConfigurationTypeCheckingMode.STRICT"></a>

strict.

---


### PytestConfigurationConsoleOutputStyle <a name="PytestConfigurationConsoleOutputStyle" id="projen.python.PytestConfigurationConsoleOutputStyle"></a>

Sets console output style during test execution.

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PytestConfigurationConsoleOutputStyle.CLASSIC">CLASSIC</a></code> | classic. |
| <code><a href="#projen.python.PytestConfigurationConsoleOutputStyle.PROGRESS">PROGRESS</a></code> | progress. |
| <code><a href="#projen.python.PytestConfigurationConsoleOutputStyle.PROGRESS_HYPHEN_EVEN_HYPHEN_WHEN_HYPHEN_CAPTURE_HYPHEN_NO">PROGRESS_HYPHEN_EVEN_HYPHEN_WHEN_HYPHEN_CAPTURE_HYPHEN_NO</a></code> | progress-even-when-capture-no. |
| <code><a href="#projen.python.PytestConfigurationConsoleOutputStyle.COUNT">COUNT</a></code> | count. |
| <code><a href="#projen.python.PytestConfigurationConsoleOutputStyle.TIMES">TIMES</a></code> | times. |

---

##### `CLASSIC` <a name="CLASSIC" id="projen.python.PytestConfigurationConsoleOutputStyle.CLASSIC"></a>

classic.

---


##### `PROGRESS` <a name="PROGRESS" id="projen.python.PytestConfigurationConsoleOutputStyle.PROGRESS"></a>

progress.

---


##### `PROGRESS_HYPHEN_EVEN_HYPHEN_WHEN_HYPHEN_CAPTURE_HYPHEN_NO` <a name="PROGRESS_HYPHEN_EVEN_HYPHEN_WHEN_HYPHEN_CAPTURE_HYPHEN_NO" id="projen.python.PytestConfigurationConsoleOutputStyle.PROGRESS_HYPHEN_EVEN_HYPHEN_WHEN_HYPHEN_CAPTURE_HYPHEN_NO"></a>

progress-even-when-capture-no.

---


##### `COUNT` <a name="COUNT" id="projen.python.PytestConfigurationConsoleOutputStyle.COUNT"></a>

count.

---


##### `TIMES` <a name="TIMES" id="projen.python.PytestConfigurationConsoleOutputStyle.TIMES"></a>

times.

---


### PytestConfigurationEmptyParameterSetMark <a name="PytestConfigurationEmptyParameterSetMark" id="projen.python.PytestConfigurationEmptyParameterSetMark"></a>

Allows to pick the action for empty parametersets in parameterization.

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PytestConfigurationEmptyParameterSetMark.SKIP">SKIP</a></code> | skip. |
| <code><a href="#projen.python.PytestConfigurationEmptyParameterSetMark.XFAIL">XFAIL</a></code> | xfail. |
| <code><a href="#projen.python.PytestConfigurationEmptyParameterSetMark.FAIL_UNDERSCORE_AT_UNDERSCORE_COLLECT">FAIL_UNDERSCORE_AT_UNDERSCORE_COLLECT</a></code> | fail_at_collect. |

---

##### `SKIP` <a name="SKIP" id="projen.python.PytestConfigurationEmptyParameterSetMark.SKIP"></a>

skip.

---


##### `XFAIL` <a name="XFAIL" id="projen.python.PytestConfigurationEmptyParameterSetMark.XFAIL"></a>

xfail.

---


##### `FAIL_UNDERSCORE_AT_UNDERSCORE_COLLECT` <a name="FAIL_UNDERSCORE_AT_UNDERSCORE_COLLECT" id="projen.python.PytestConfigurationEmptyParameterSetMark.FAIL_UNDERSCORE_AT_UNDERSCORE_COLLECT"></a>

fail_at_collect.

---


### PytestConfigurationJunitDurationReport <a name="PytestConfigurationJunitDurationReport" id="projen.python.PytestConfigurationJunitDurationReport"></a>

Configures how durations are recorded into the JUnit XML report:.

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PytestConfigurationJunitDurationReport.TOTAL">TOTAL</a></code> | total. |
| <code><a href="#projen.python.PytestConfigurationJunitDurationReport.CALL">CALL</a></code> | call. |

---

##### `TOTAL` <a name="TOTAL" id="projen.python.PytestConfigurationJunitDurationReport.TOTAL"></a>

total.

---


##### `CALL` <a name="CALL" id="projen.python.PytestConfigurationJunitDurationReport.CALL"></a>

call.

---


### PytestConfigurationJunitFamily <a name="PytestConfigurationJunitFamily" id="projen.python.PytestConfigurationJunitFamily"></a>

Configures the format of the generated JUnit XML file.

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PytestConfigurationJunitFamily.LEGACY">LEGACY</a></code> | legacy. |
| <code><a href="#projen.python.PytestConfigurationJunitFamily.XUNIT1">XUNIT1</a></code> | xunit1. |
| <code><a href="#projen.python.PytestConfigurationJunitFamily.XUNIT2">XUNIT2</a></code> | xunit2. |

---

##### `LEGACY` <a name="LEGACY" id="projen.python.PytestConfigurationJunitFamily.LEGACY"></a>

legacy.

---


##### `XUNIT1` <a name="XUNIT1" id="projen.python.PytestConfigurationJunitFamily.XUNIT1"></a>

xunit1.

---


##### `XUNIT2` <a name="XUNIT2" id="projen.python.PytestConfigurationJunitFamily.XUNIT2"></a>

xunit2.

---


### PytestConfigurationJunitLogging <a name="PytestConfigurationJunitLogging" id="projen.python.PytestConfigurationJunitLogging"></a>

Configures if captured output should be written to the JUnit XML file.

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PytestConfigurationJunitLogging.NO">NO</a></code> | no. |
| <code><a href="#projen.python.PytestConfigurationJunitLogging.LOG">LOG</a></code> | log. |
| <code><a href="#projen.python.PytestConfigurationJunitLogging.SYSTEM_HYPHEN_OUT">SYSTEM_HYPHEN_OUT</a></code> | system-out. |
| <code><a href="#projen.python.PytestConfigurationJunitLogging.SYSTEM_HYPHEN_ERR">SYSTEM_HYPHEN_ERR</a></code> | system-err. |
| <code><a href="#projen.python.PytestConfigurationJunitLogging.OUT_HYPHEN_ERR">OUT_HYPHEN_ERR</a></code> | out-err. |
| <code><a href="#projen.python.PytestConfigurationJunitLogging.ALL">ALL</a></code> | all. |

---

##### `NO` <a name="NO" id="projen.python.PytestConfigurationJunitLogging.NO"></a>

no.

---


##### `LOG` <a name="LOG" id="projen.python.PytestConfigurationJunitLogging.LOG"></a>

log.

---


##### `SYSTEM_HYPHEN_OUT` <a name="SYSTEM_HYPHEN_OUT" id="projen.python.PytestConfigurationJunitLogging.SYSTEM_HYPHEN_OUT"></a>

system-out.

---


##### `SYSTEM_HYPHEN_ERR` <a name="SYSTEM_HYPHEN_ERR" id="projen.python.PytestConfigurationJunitLogging.SYSTEM_HYPHEN_ERR"></a>

system-err.

---


##### `OUT_HYPHEN_ERR` <a name="OUT_HYPHEN_ERR" id="projen.python.PytestConfigurationJunitLogging.OUT_HYPHEN_ERR"></a>

out-err.

---


##### `ALL` <a name="ALL" id="projen.python.PytestConfigurationJunitLogging.ALL"></a>

all.

---


### PytestConfigurationLogFileMode <a name="PytestConfigurationLogFileMode" id="projen.python.PytestConfigurationLogFileMode"></a>

Sets the mode that the logging file is opened with.

The options are 'w' to recreate the file (the default) or 'a' to append to the file.

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PytestConfigurationLogFileMode.A">A</a></code> | a. |
| <code><a href="#projen.python.PytestConfigurationLogFileMode.W">W</a></code> | w. |

---

##### `A` <a name="A" id="projen.python.PytestConfigurationLogFileMode.A"></a>

a.

---


##### `W` <a name="W" id="projen.python.PytestConfigurationLogFileMode.W"></a>

w.

---


### PytestConfigurationTmpPathRetentionPolicy <a name="PytestConfigurationTmpPathRetentionPolicy" id="projen.python.PytestConfigurationTmpPathRetentionPolicy"></a>

Controls which directories created by the tmp_path fixture are kept around, based on test outcome.

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PytestConfigurationTmpPathRetentionPolicy.ALL">ALL</a></code> | all. |
| <code><a href="#projen.python.PytestConfigurationTmpPathRetentionPolicy.FAILED">FAILED</a></code> | failed. |
| <code><a href="#projen.python.PytestConfigurationTmpPathRetentionPolicy.NONE">NONE</a></code> | none. |

---

##### `ALL` <a name="ALL" id="projen.python.PytestConfigurationTmpPathRetentionPolicy.ALL"></a>

all.

---


##### `FAILED` <a name="FAILED" id="projen.python.PytestConfigurationTmpPathRetentionPolicy.FAILED"></a>

failed.

---


##### `NONE` <a name="NONE" id="projen.python.PytestConfigurationTmpPathRetentionPolicy.NONE"></a>

none.

---


### PythonFormatter <a name="PythonFormatter" id="projen.python.PythonFormatter"></a>

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PythonFormatter.BLACK">BLACK</a></code> | The uncompromising Python code formatter. |
| <code><a href="#projen.python.PythonFormatter.RUFF">RUFF</a></code> | An extremely fast Python formatter, written in Rust. |

---

##### `BLACK` <a name="BLACK" id="projen.python.PythonFormatter.BLACK"></a>

The uncompromising Python code formatter.

---


##### `RUFF` <a name="RUFF" id="projen.python.PythonFormatter.RUFF"></a>

An extremely fast Python formatter, written in Rust.

---


### PythonLinter <a name="PythonLinter" id="projen.python.PythonLinter"></a>

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PythonLinter.RUFF">RUFF</a></code> | An extremely fast Python linter, written in Rust. |

---

##### `RUFF` <a name="RUFF" id="projen.python.PythonLinter.RUFF"></a>

An extremely fast Python linter, written in Rust.

---


### PythonPackageManager <a name="PythonPackageManager" id="projen.python.PythonPackageManager"></a>

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PythonPackageManager.HATCH">HATCH</a></code> | Use hatch to manage your project dependencies, virtual environment, and (optional) packaging/publishing. |
| <code><a href="#projen.python.PythonPackageManager.PDM">PDM</a></code> | Use pdm to manage your project dependencies, virtual environment, and (optional) packaging/publishing. |
| <code><a href="#projen.python.PythonPackageManager.SETUPTOOLS">SETUPTOOLS</a></code> | Use setuptools to manage your project dependencies, virtual environment, and (optional) packaging/publishing. |
| <code><a href="#projen.python.PythonPackageManager.POETRY">POETRY</a></code> | Use poetry to manage your project dependencies, virtual environment, and (optional) packaging/publishing. |
| <code><a href="#projen.python.PythonPackageManager.UV">UV</a></code> | Use uv to manage your project dependencies, virtual environment, and (optional) packaging/publishing. |

---

##### `HATCH` <a name="HATCH" id="projen.python.PythonPackageManager.HATCH"></a>

Use hatch to manage your project dependencies, virtual environment, and (optional) packaging/publishing.

This feature is incompatible with venv.

---


##### `PDM` <a name="PDM" id="projen.python.PythonPackageManager.PDM"></a>

Use pdm to manage your project dependencies, virtual environment, and (optional) packaging/publishing.

This feature is incompatible with venv.

---


##### `SETUPTOOLS` <a name="SETUPTOOLS" id="projen.python.PythonPackageManager.SETUPTOOLS"></a>

Use setuptools to manage your project dependencies, virtual environment, and (optional) packaging/publishing.

---


##### `POETRY` <a name="POETRY" id="projen.python.PythonPackageManager.POETRY"></a>

Use poetry to manage your project dependencies, virtual environment, and (optional) packaging/publishing.

This feature is incompatible with venv.

---


##### `UV` <a name="UV" id="projen.python.PythonPackageManager.UV"></a>

Use uv to manage your project dependencies, virtual environment, and (optional) packaging/publishing.

This feature is incompatible with venv.

---


### PythonPlatform <a name="PythonPlatform" id="projen.python.PythonPlatform"></a>

Specifies the target platform that will be used to execute the source code.

Should be one of `Windows`, `Darwin`, `Linux`, or `All`. If specified, pyright will tailor its use of type stub files, which conditionalize type definitions based on the platform. If no platform is specified, pyright will use the current platform.

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PythonPlatform.WINDOWS">WINDOWS</a></code> | Windows. |
| <code><a href="#projen.python.PythonPlatform.DARWIN">DARWIN</a></code> | Darwin. |
| <code><a href="#projen.python.PythonPlatform.LINUX">LINUX</a></code> | Linux. |
| <code><a href="#projen.python.PythonPlatform.ALL">ALL</a></code> | All. |

---

##### `WINDOWS` <a name="WINDOWS" id="projen.python.PythonPlatform.WINDOWS"></a>

Windows.

---


##### `DARWIN` <a name="DARWIN" id="projen.python.PythonPlatform.DARWIN"></a>

Darwin.

---


##### `LINUX` <a name="LINUX" id="projen.python.PythonPlatform.LINUX"></a>

Linux.

---


##### `ALL` <a name="ALL" id="projen.python.PythonPlatform.ALL"></a>

All.

---


### PythonTypeChecker <a name="PythonTypeChecker" id="projen.python.PythonTypeChecker"></a>

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PythonTypeChecker.MYPY">MYPY</a></code> | Optional static typing for Python. |
| <code><a href="#projen.python.PythonTypeChecker.TY">TY</a></code> | An extremely fast Python type checker, written in Rust. |
| <code><a href="#projen.python.PythonTypeChecker.PYRIGHT">PYRIGHT</a></code> | Static type checker for Python. |

---

##### `MYPY` <a name="MYPY" id="projen.python.PythonTypeChecker.MYPY"></a>

Optional static typing for Python.

---


##### `TY` <a name="TY" id="projen.python.PythonTypeChecker.TY"></a>

An extremely fast Python type checker, written in Rust.

---


##### `PYRIGHT` <a name="PYRIGHT" id="projen.python.PythonTypeChecker.PYRIGHT"></a>

Static type checker for Python.

---


### PythonVersion <a name="PythonVersion" id="projen.python.PythonVersion"></a>

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.PythonVersion.PY37">PY37</a></code> | py37. |
| <code><a href="#projen.python.PythonVersion.PY38">PY38</a></code> | py38. |
| <code><a href="#projen.python.PythonVersion.PY39">PY39</a></code> | py39. |
| <code><a href="#projen.python.PythonVersion.PY310">PY310</a></code> | py310. |
| <code><a href="#projen.python.PythonVersion.PY311">PY311</a></code> | py311. |
| <code><a href="#projen.python.PythonVersion.PY312">PY312</a></code> | py312. |
| <code><a href="#projen.python.PythonVersion.PY313">PY313</a></code> | py313. |
| <code><a href="#projen.python.PythonVersion.PY314">PY314</a></code> | py314. |

---

##### `PY37` <a name="PY37" id="projen.python.PythonVersion.PY37"></a>

py37.

---


##### `PY38` <a name="PY38" id="projen.python.PythonVersion.PY38"></a>

py38.

---


##### `PY39` <a name="PY39" id="projen.python.PythonVersion.PY39"></a>

py39.

---


##### `PY310` <a name="PY310" id="projen.python.PythonVersion.PY310"></a>

py310.

---


##### `PY311` <a name="PY311" id="projen.python.PythonVersion.PY311"></a>

py311.

---


##### `PY312` <a name="PY312" id="projen.python.PythonVersion.PY312"></a>

py312.

---


##### `PY313` <a name="PY313" id="projen.python.PythonVersion.PY313"></a>

py313.

---


##### `PY314` <a name="PY314" id="projen.python.PythonVersion.PY314"></a>

py314.

---


### QuoteStyle <a name="QuoteStyle" id="projen.python.QuoteStyle"></a>

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.QuoteStyle.SINGLE">SINGLE</a></code> | single. |
| <code><a href="#projen.python.QuoteStyle.DOUBLE">DOUBLE</a></code> | double. |
| <code><a href="#projen.python.QuoteStyle.PRESERVE">PRESERVE</a></code> | preserve. |

---

##### `SINGLE` <a name="SINGLE" id="projen.python.QuoteStyle.SINGLE"></a>

single.

---


##### `DOUBLE` <a name="DOUBLE" id="projen.python.QuoteStyle.DOUBLE"></a>

double.

---


##### `PRESERVE` <a name="PRESERVE" id="projen.python.QuoteStyle.PRESERVE"></a>

preserve.

---


### RuleSelector <a name="RuleSelector" id="projen.python.RuleSelector"></a>

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.RuleSelector.A">A</a></code> | A. |
| <code><a href="#projen.python.RuleSelector.A0">A0</a></code> | A0. |
| <code><a href="#projen.python.RuleSelector.A00">A00</a></code> | A00. |
| <code><a href="#projen.python.RuleSelector.A001">A001</a></code> | A001. |
| <code><a href="#projen.python.RuleSelector.A002">A002</a></code> | A002. |
| <code><a href="#projen.python.RuleSelector.A003">A003</a></code> | A003. |
| <code><a href="#projen.python.RuleSelector.A004">A004</a></code> | A004. |
| <code><a href="#projen.python.RuleSelector.A005">A005</a></code> | A005. |
| <code><a href="#projen.python.RuleSelector.A006">A006</a></code> | A006. |
| <code><a href="#projen.python.RuleSelector.AIR">AIR</a></code> | AIR. |
| <code><a href="#projen.python.RuleSelector.AIR0">AIR0</a></code> | AIR0. |
| <code><a href="#projen.python.RuleSelector.AIR00">AIR00</a></code> | AIR00. |
| <code><a href="#projen.python.RuleSelector.AIR001">AIR001</a></code> | AIR001. |
| <code><a href="#projen.python.RuleSelector.AIR002">AIR002</a></code> | AIR002. |
| <code><a href="#projen.python.RuleSelector.AIR3">AIR3</a></code> | AIR3. |
| <code><a href="#projen.python.RuleSelector.AIR30">AIR30</a></code> | AIR30. |
| <code><a href="#projen.python.RuleSelector.AIR301">AIR301</a></code> | AIR301. |
| <code><a href="#projen.python.RuleSelector.AIR302">AIR302</a></code> | AIR302. |
| <code><a href="#projen.python.RuleSelector.AIR31">AIR31</a></code> | AIR31. |
| <code><a href="#projen.python.RuleSelector.AIR311">AIR311</a></code> | AIR311. |
| <code><a href="#projen.python.RuleSelector.AIR312">AIR312</a></code> | AIR312. |
| <code><a href="#projen.python.RuleSelector.ALL">ALL</a></code> | ALL. |
| <code><a href="#projen.python.RuleSelector.ANN">ANN</a></code> | ANN. |
| <code><a href="#projen.python.RuleSelector.ANN0">ANN0</a></code> | ANN0. |
| <code><a href="#projen.python.RuleSelector.ANN00">ANN00</a></code> | ANN00. |
| <code><a href="#projen.python.RuleSelector.ANN001">ANN001</a></code> | ANN001. |
| <code><a href="#projen.python.RuleSelector.ANN002">ANN002</a></code> | ANN002. |
| <code><a href="#projen.python.RuleSelector.ANN003">ANN003</a></code> | ANN003. |
| <code><a href="#projen.python.RuleSelector.ANN2">ANN2</a></code> | ANN2. |
| <code><a href="#projen.python.RuleSelector.ANN20">ANN20</a></code> | ANN20. |
| <code><a href="#projen.python.RuleSelector.ANN201">ANN201</a></code> | ANN201. |
| <code><a href="#projen.python.RuleSelector.ANN202">ANN202</a></code> | ANN202. |
| <code><a href="#projen.python.RuleSelector.ANN204">ANN204</a></code> | ANN204. |
| <code><a href="#projen.python.RuleSelector.ANN205">ANN205</a></code> | ANN205. |
| <code><a href="#projen.python.RuleSelector.ANN206">ANN206</a></code> | ANN206. |
| <code><a href="#projen.python.RuleSelector.ANN4">ANN4</a></code> | ANN4. |
| <code><a href="#projen.python.RuleSelector.ANN40">ANN40</a></code> | ANN40. |
| <code><a href="#projen.python.RuleSelector.ANN401">ANN401</a></code> | ANN401. |
| <code><a href="#projen.python.RuleSelector.ARG">ARG</a></code> | ARG. |
| <code><a href="#projen.python.RuleSelector.ARG0">ARG0</a></code> | ARG0. |
| <code><a href="#projen.python.RuleSelector.ARG00">ARG00</a></code> | ARG00. |
| <code><a href="#projen.python.RuleSelector.ARG001">ARG001</a></code> | ARG001. |
| <code><a href="#projen.python.RuleSelector.ARG002">ARG002</a></code> | ARG002. |
| <code><a href="#projen.python.RuleSelector.ARG003">ARG003</a></code> | ARG003. |
| <code><a href="#projen.python.RuleSelector.ARG004">ARG004</a></code> | ARG004. |
| <code><a href="#projen.python.RuleSelector.ARG005">ARG005</a></code> | ARG005. |
| <code><a href="#projen.python.RuleSelector.ASYNC">ASYNC</a></code> | ASYNC. |
| <code><a href="#projen.python.RuleSelector.ASYNC1">ASYNC1</a></code> | ASYNC1. |
| <code><a href="#projen.python.RuleSelector.ASYNC10">ASYNC10</a></code> | ASYNC10. |
| <code><a href="#projen.python.RuleSelector.ASYNC100">ASYNC100</a></code> | ASYNC100. |
| <code><a href="#projen.python.RuleSelector.ASYNC105">ASYNC105</a></code> | ASYNC105. |
| <code><a href="#projen.python.RuleSelector.ASYNC109">ASYNC109</a></code> | ASYNC109. |
| <code><a href="#projen.python.RuleSelector.ASYNC11">ASYNC11</a></code> | ASYNC11. |
| <code><a href="#projen.python.RuleSelector.ASYNC110">ASYNC110</a></code> | ASYNC110. |
| <code><a href="#projen.python.RuleSelector.ASYNC115">ASYNC115</a></code> | ASYNC115. |
| <code><a href="#projen.python.RuleSelector.ASYNC116">ASYNC116</a></code> | ASYNC116. |
| <code><a href="#projen.python.RuleSelector.ASYNC2">ASYNC2</a></code> | ASYNC2. |
| <code><a href="#projen.python.RuleSelector.ASYNC21">ASYNC21</a></code> | ASYNC21. |
| <code><a href="#projen.python.RuleSelector.ASYNC210">ASYNC210</a></code> | ASYNC210. |
| <code><a href="#projen.python.RuleSelector.ASYNC212">ASYNC212</a></code> | ASYNC212. |
| <code><a href="#projen.python.RuleSelector.ASYNC22">ASYNC22</a></code> | ASYNC22. |
| <code><a href="#projen.python.RuleSelector.ASYNC220">ASYNC220</a></code> | ASYNC220. |
| <code><a href="#projen.python.RuleSelector.ASYNC221">ASYNC221</a></code> | ASYNC221. |
| <code><a href="#projen.python.RuleSelector.ASYNC222">ASYNC222</a></code> | ASYNC222. |
| <code><a href="#projen.python.RuleSelector.ASYNC23">ASYNC23</a></code> | ASYNC23. |
| <code><a href="#projen.python.RuleSelector.ASYNC230">ASYNC230</a></code> | ASYNC230. |
| <code><a href="#projen.python.RuleSelector.ASYNC24">ASYNC24</a></code> | ASYNC24. |
| <code><a href="#projen.python.RuleSelector.ASYNC240">ASYNC240</a></code> | ASYNC240. |
| <code><a href="#projen.python.RuleSelector.ASYNC25">ASYNC25</a></code> | ASYNC25. |
| <code><a href="#projen.python.RuleSelector.ASYNC250">ASYNC250</a></code> | ASYNC250. |
| <code><a href="#projen.python.RuleSelector.ASYNC251">ASYNC251</a></code> | ASYNC251. |
| <code><a href="#projen.python.RuleSelector.B">B</a></code> | B. |
| <code><a href="#projen.python.RuleSelector.B0">B0</a></code> | B0. |
| <code><a href="#projen.python.RuleSelector.B00">B00</a></code> | B00. |
| <code><a href="#projen.python.RuleSelector.B002">B002</a></code> | B002. |
| <code><a href="#projen.python.RuleSelector.B003">B003</a></code> | B003. |
| <code><a href="#projen.python.RuleSelector.B004">B004</a></code> | B004. |
| <code><a href="#projen.python.RuleSelector.B005">B005</a></code> | B005. |
| <code><a href="#projen.python.RuleSelector.B006">B006</a></code> | B006. |
| <code><a href="#projen.python.RuleSelector.B007">B007</a></code> | B007. |
| <code><a href="#projen.python.RuleSelector.B008">B008</a></code> | B008. |
| <code><a href="#projen.python.RuleSelector.B009">B009</a></code> | B009. |
| <code><a href="#projen.python.RuleSelector.B01">B01</a></code> | B01. |
| <code><a href="#projen.python.RuleSelector.B010">B010</a></code> | B010. |
| <code><a href="#projen.python.RuleSelector.B011">B011</a></code> | B011. |
| <code><a href="#projen.python.RuleSelector.B012">B012</a></code> | B012. |
| <code><a href="#projen.python.RuleSelector.B013">B013</a></code> | B013. |
| <code><a href="#projen.python.RuleSelector.B014">B014</a></code> | B014. |
| <code><a href="#projen.python.RuleSelector.B015">B015</a></code> | B015. |
| <code><a href="#projen.python.RuleSelector.B016">B016</a></code> | B016. |
| <code><a href="#projen.python.RuleSelector.B017">B017</a></code> | B017. |
| <code><a href="#projen.python.RuleSelector.B018">B018</a></code> | B018. |
| <code><a href="#projen.python.RuleSelector.B019">B019</a></code> | B019. |
| <code><a href="#projen.python.RuleSelector.B02">B02</a></code> | B02. |
| <code><a href="#projen.python.RuleSelector.B020">B020</a></code> | B020. |
| <code><a href="#projen.python.RuleSelector.B021">B021</a></code> | B021. |
| <code><a href="#projen.python.RuleSelector.B022">B022</a></code> | B022. |
| <code><a href="#projen.python.RuleSelector.B023">B023</a></code> | B023. |
| <code><a href="#projen.python.RuleSelector.B024">B024</a></code> | B024. |
| <code><a href="#projen.python.RuleSelector.B025">B025</a></code> | B025. |
| <code><a href="#projen.python.RuleSelector.B026">B026</a></code> | B026. |
| <code><a href="#projen.python.RuleSelector.B027">B027</a></code> | B027. |
| <code><a href="#projen.python.RuleSelector.B028">B028</a></code> | B028. |
| <code><a href="#projen.python.RuleSelector.B029">B029</a></code> | B029. |
| <code><a href="#projen.python.RuleSelector.B03">B03</a></code> | B03. |
| <code><a href="#projen.python.RuleSelector.B030">B030</a></code> | B030. |
| <code><a href="#projen.python.RuleSelector.B031">B031</a></code> | B031. |
| <code><a href="#projen.python.RuleSelector.B032">B032</a></code> | B032. |
| <code><a href="#projen.python.RuleSelector.B033">B033</a></code> | B033. |
| <code><a href="#projen.python.RuleSelector.B034">B034</a></code> | B034. |
| <code><a href="#projen.python.RuleSelector.B035">B035</a></code> | B035. |
| <code><a href="#projen.python.RuleSelector.B039">B039</a></code> | B039. |
| <code><a href="#projen.python.RuleSelector.B9">B9</a></code> | B9. |
| <code><a href="#projen.python.RuleSelector.B90">B90</a></code> | B90. |
| <code><a href="#projen.python.RuleSelector.B901">B901</a></code> | B901. |
| <code><a href="#projen.python.RuleSelector.B903">B903</a></code> | B903. |
| <code><a href="#projen.python.RuleSelector.B904">B904</a></code> | B904. |
| <code><a href="#projen.python.RuleSelector.B905">B905</a></code> | B905. |
| <code><a href="#projen.python.RuleSelector.B909">B909</a></code> | B909. |
| <code><a href="#projen.python.RuleSelector.B91">B91</a></code> | B91. |
| <code><a href="#projen.python.RuleSelector.B911">B911</a></code> | B911. |
| <code><a href="#projen.python.RuleSelector.B912">B912</a></code> | B912. |
| <code><a href="#projen.python.RuleSelector.BLE">BLE</a></code> | BLE. |
| <code><a href="#projen.python.RuleSelector.BLE0">BLE0</a></code> | BLE0. |
| <code><a href="#projen.python.RuleSelector.BLE00">BLE00</a></code> | BLE00. |
| <code><a href="#projen.python.RuleSelector.BLE001">BLE001</a></code> | BLE001. |
| <code><a href="#projen.python.RuleSelector.C">C</a></code> | C. |
| <code><a href="#projen.python.RuleSelector.C4">C4</a></code> | C4. |
| <code><a href="#projen.python.RuleSelector.C40">C40</a></code> | C40. |
| <code><a href="#projen.python.RuleSelector.C400">C400</a></code> | C400. |
| <code><a href="#projen.python.RuleSelector.C401">C401</a></code> | C401. |
| <code><a href="#projen.python.RuleSelector.C402">C402</a></code> | C402. |
| <code><a href="#projen.python.RuleSelector.C403">C403</a></code> | C403. |
| <code><a href="#projen.python.RuleSelector.C404">C404</a></code> | C404. |
| <code><a href="#projen.python.RuleSelector.C405">C405</a></code> | C405. |
| <code><a href="#projen.python.RuleSelector.C406">C406</a></code> | C406. |
| <code><a href="#projen.python.RuleSelector.C408">C408</a></code> | C408. |
| <code><a href="#projen.python.RuleSelector.C409">C409</a></code> | C409. |
| <code><a href="#projen.python.RuleSelector.C41">C41</a></code> | C41. |
| <code><a href="#projen.python.RuleSelector.C410">C410</a></code> | C410. |
| <code><a href="#projen.python.RuleSelector.C411">C411</a></code> | C411. |
| <code><a href="#projen.python.RuleSelector.C413">C413</a></code> | C413. |
| <code><a href="#projen.python.RuleSelector.C414">C414</a></code> | C414. |
| <code><a href="#projen.python.RuleSelector.C415">C415</a></code> | C415. |
| <code><a href="#projen.python.RuleSelector.C416">C416</a></code> | C416. |
| <code><a href="#projen.python.RuleSelector.C417">C417</a></code> | C417. |
| <code><a href="#projen.python.RuleSelector.C418">C418</a></code> | C418. |
| <code><a href="#projen.python.RuleSelector.C419">C419</a></code> | C419. |
| <code><a href="#projen.python.RuleSelector.C42">C42</a></code> | C42. |
| <code><a href="#projen.python.RuleSelector.C420">C420</a></code> | C420. |
| <code><a href="#projen.python.RuleSelector.C9">C9</a></code> | C9. |
| <code><a href="#projen.python.RuleSelector.C90">C90</a></code> | C90. |
| <code><a href="#projen.python.RuleSelector.C901">C901</a></code> | C901. |
| <code><a href="#projen.python.RuleSelector.COM">COM</a></code> | COM. |
| <code><a href="#projen.python.RuleSelector.COM8">COM8</a></code> | COM8. |
| <code><a href="#projen.python.RuleSelector.COM81">COM81</a></code> | COM81. |
| <code><a href="#projen.python.RuleSelector.COM812">COM812</a></code> | COM812. |
| <code><a href="#projen.python.RuleSelector.COM818">COM818</a></code> | COM818. |
| <code><a href="#projen.python.RuleSelector.COM819">COM819</a></code> | COM819. |
| <code><a href="#projen.python.RuleSelector.CPY">CPY</a></code> | CPY. |
| <code><a href="#projen.python.RuleSelector.CPY0">CPY0</a></code> | CPY0. |
| <code><a href="#projen.python.RuleSelector.CPY00">CPY00</a></code> | CPY00. |
| <code><a href="#projen.python.RuleSelector.CPY001">CPY001</a></code> | CPY001. |
| <code><a href="#projen.python.RuleSelector.D">D</a></code> | D. |
| <code><a href="#projen.python.RuleSelector.D1">D1</a></code> | D1. |
| <code><a href="#projen.python.RuleSelector.D10">D10</a></code> | D10. |
| <code><a href="#projen.python.RuleSelector.D100">D100</a></code> | D100. |
| <code><a href="#projen.python.RuleSelector.D101">D101</a></code> | D101. |
| <code><a href="#projen.python.RuleSelector.D102">D102</a></code> | D102. |
| <code><a href="#projen.python.RuleSelector.D103">D103</a></code> | D103. |
| <code><a href="#projen.python.RuleSelector.D104">D104</a></code> | D104. |
| <code><a href="#projen.python.RuleSelector.D105">D105</a></code> | D105. |
| <code><a href="#projen.python.RuleSelector.D106">D106</a></code> | D106. |
| <code><a href="#projen.python.RuleSelector.D107">D107</a></code> | D107. |
| <code><a href="#projen.python.RuleSelector.D2">D2</a></code> | D2. |
| <code><a href="#projen.python.RuleSelector.D20">D20</a></code> | D20. |
| <code><a href="#projen.python.RuleSelector.D200">D200</a></code> | D200. |
| <code><a href="#projen.python.RuleSelector.D201">D201</a></code> | D201. |
| <code><a href="#projen.python.RuleSelector.D202">D202</a></code> | D202. |
| <code><a href="#projen.python.RuleSelector.D203">D203</a></code> | D203. |
| <code><a href="#projen.python.RuleSelector.D204">D204</a></code> | D204. |
| <code><a href="#projen.python.RuleSelector.D205">D205</a></code> | D205. |
| <code><a href="#projen.python.RuleSelector.D206">D206</a></code> | D206. |
| <code><a href="#projen.python.RuleSelector.D207">D207</a></code> | D207. |
| <code><a href="#projen.python.RuleSelector.D208">D208</a></code> | D208. |
| <code><a href="#projen.python.RuleSelector.D209">D209</a></code> | D209. |
| <code><a href="#projen.python.RuleSelector.D21">D21</a></code> | D21. |
| <code><a href="#projen.python.RuleSelector.D210">D210</a></code> | D210. |
| <code><a href="#projen.python.RuleSelector.D211">D211</a></code> | D211. |
| <code><a href="#projen.python.RuleSelector.D212">D212</a></code> | D212. |
| <code><a href="#projen.python.RuleSelector.D213">D213</a></code> | D213. |
| <code><a href="#projen.python.RuleSelector.D214">D214</a></code> | D214. |
| <code><a href="#projen.python.RuleSelector.D215">D215</a></code> | D215. |
| <code><a href="#projen.python.RuleSelector.D3">D3</a></code> | D3. |
| <code><a href="#projen.python.RuleSelector.D30">D30</a></code> | D30. |
| <code><a href="#projen.python.RuleSelector.D300">D300</a></code> | D300. |
| <code><a href="#projen.python.RuleSelector.D301">D301</a></code> | D301. |
| <code><a href="#projen.python.RuleSelector.D4">D4</a></code> | D4. |
| <code><a href="#projen.python.RuleSelector.D40">D40</a></code> | D40. |
| <code><a href="#projen.python.RuleSelector.D400">D400</a></code> | D400. |
| <code><a href="#projen.python.RuleSelector.D401">D401</a></code> | D401. |
| <code><a href="#projen.python.RuleSelector.D402">D402</a></code> | D402. |
| <code><a href="#projen.python.RuleSelector.D403">D403</a></code> | D403. |
| <code><a href="#projen.python.RuleSelector.D404">D404</a></code> | D404. |
| <code><a href="#projen.python.RuleSelector.D405">D405</a></code> | D405. |
| <code><a href="#projen.python.RuleSelector.D406">D406</a></code> | D406. |
| <code><a href="#projen.python.RuleSelector.D407">D407</a></code> | D407. |
| <code><a href="#projen.python.RuleSelector.D408">D408</a></code> | D408. |
| <code><a href="#projen.python.RuleSelector.D409">D409</a></code> | D409. |
| <code><a href="#projen.python.RuleSelector.D41">D41</a></code> | D41. |
| <code><a href="#projen.python.RuleSelector.D410">D410</a></code> | D410. |
| <code><a href="#projen.python.RuleSelector.D411">D411</a></code> | D411. |
| <code><a href="#projen.python.RuleSelector.D412">D412</a></code> | D412. |
| <code><a href="#projen.python.RuleSelector.D413">D413</a></code> | D413. |
| <code><a href="#projen.python.RuleSelector.D414">D414</a></code> | D414. |
| <code><a href="#projen.python.RuleSelector.D415">D415</a></code> | D415. |
| <code><a href="#projen.python.RuleSelector.D416">D416</a></code> | D416. |
| <code><a href="#projen.python.RuleSelector.D417">D417</a></code> | D417. |
| <code><a href="#projen.python.RuleSelector.D418">D418</a></code> | D418. |
| <code><a href="#projen.python.RuleSelector.D419">D419</a></code> | D419. |
| <code><a href="#projen.python.RuleSelector.DJ">DJ</a></code> | DJ. |
| <code><a href="#projen.python.RuleSelector.DJ0">DJ0</a></code> | DJ0. |
| <code><a href="#projen.python.RuleSelector.DJ00">DJ00</a></code> | DJ00. |
| <code><a href="#projen.python.RuleSelector.DJ001">DJ001</a></code> | DJ001. |
| <code><a href="#projen.python.RuleSelector.DJ003">DJ003</a></code> | DJ003. |
| <code><a href="#projen.python.RuleSelector.DJ006">DJ006</a></code> | DJ006. |
| <code><a href="#projen.python.RuleSelector.DJ007">DJ007</a></code> | DJ007. |
| <code><a href="#projen.python.RuleSelector.DJ008">DJ008</a></code> | DJ008. |
| <code><a href="#projen.python.RuleSelector.DJ01">DJ01</a></code> | DJ01. |
| <code><a href="#projen.python.RuleSelector.DJ012">DJ012</a></code> | DJ012. |
| <code><a href="#projen.python.RuleSelector.DJ013">DJ013</a></code> | DJ013. |
| <code><a href="#projen.python.RuleSelector.DOC">DOC</a></code> | DOC. |
| <code><a href="#projen.python.RuleSelector.DOC1">DOC1</a></code> | DOC1. |
| <code><a href="#projen.python.RuleSelector.DOC10">DOC10</a></code> | DOC10. |
| <code><a href="#projen.python.RuleSelector.DOC102">DOC102</a></code> | DOC102. |
| <code><a href="#projen.python.RuleSelector.DOC2">DOC2</a></code> | DOC2. |
| <code><a href="#projen.python.RuleSelector.DOC20">DOC20</a></code> | DOC20. |
| <code><a href="#projen.python.RuleSelector.DOC201">DOC201</a></code> | DOC201. |
| <code><a href="#projen.python.RuleSelector.DOC202">DOC202</a></code> | DOC202. |
| <code><a href="#projen.python.RuleSelector.DOC4">DOC4</a></code> | DOC4. |
| <code><a href="#projen.python.RuleSelector.DOC40">DOC40</a></code> | DOC40. |
| <code><a href="#projen.python.RuleSelector.DOC402">DOC402</a></code> | DOC402. |
| <code><a href="#projen.python.RuleSelector.DOC403">DOC403</a></code> | DOC403. |
| <code><a href="#projen.python.RuleSelector.DOC5">DOC5</a></code> | DOC5. |
| <code><a href="#projen.python.RuleSelector.DOC50">DOC50</a></code> | DOC50. |
| <code><a href="#projen.python.RuleSelector.DOC501">DOC501</a></code> | DOC501. |
| <code><a href="#projen.python.RuleSelector.DOC502">DOC502</a></code> | DOC502. |
| <code><a href="#projen.python.RuleSelector.DTZ">DTZ</a></code> | DTZ. |
| <code><a href="#projen.python.RuleSelector.DTZ0">DTZ0</a></code> | DTZ0. |
| <code><a href="#projen.python.RuleSelector.DTZ00">DTZ00</a></code> | DTZ00. |
| <code><a href="#projen.python.RuleSelector.DTZ001">DTZ001</a></code> | DTZ001. |
| <code><a href="#projen.python.RuleSelector.DTZ002">DTZ002</a></code> | DTZ002. |
| <code><a href="#projen.python.RuleSelector.DTZ003">DTZ003</a></code> | DTZ003. |
| <code><a href="#projen.python.RuleSelector.DTZ004">DTZ004</a></code> | DTZ004. |
| <code><a href="#projen.python.RuleSelector.DTZ005">DTZ005</a></code> | DTZ005. |
| <code><a href="#projen.python.RuleSelector.DTZ006">DTZ006</a></code> | DTZ006. |
| <code><a href="#projen.python.RuleSelector.DTZ007">DTZ007</a></code> | DTZ007. |
| <code><a href="#projen.python.RuleSelector.DTZ01">DTZ01</a></code> | DTZ01. |
| <code><a href="#projen.python.RuleSelector.DTZ011">DTZ011</a></code> | DTZ011. |
| <code><a href="#projen.python.RuleSelector.DTZ012">DTZ012</a></code> | DTZ012. |
| <code><a href="#projen.python.RuleSelector.DTZ9">DTZ9</a></code> | DTZ9. |
| <code><a href="#projen.python.RuleSelector.DTZ90">DTZ90</a></code> | DTZ90. |
| <code><a href="#projen.python.RuleSelector.DTZ901">DTZ901</a></code> | DTZ901. |
| <code><a href="#projen.python.RuleSelector.E">E</a></code> | E. |
| <code><a href="#projen.python.RuleSelector.E1">E1</a></code> | E1. |
| <code><a href="#projen.python.RuleSelector.E10">E10</a></code> | E10. |
| <code><a href="#projen.python.RuleSelector.E101">E101</a></code> | E101. |
| <code><a href="#projen.python.RuleSelector.E11">E11</a></code> | E11. |
| <code><a href="#projen.python.RuleSelector.E111">E111</a></code> | E111. |
| <code><a href="#projen.python.RuleSelector.E112">E112</a></code> | E112. |
| <code><a href="#projen.python.RuleSelector.E113">E113</a></code> | E113. |
| <code><a href="#projen.python.RuleSelector.E114">E114</a></code> | E114. |
| <code><a href="#projen.python.RuleSelector.E115">E115</a></code> | E115. |
| <code><a href="#projen.python.RuleSelector.E116">E116</a></code> | E116. |
| <code><a href="#projen.python.RuleSelector.E117">E117</a></code> | E117. |
| <code><a href="#projen.python.RuleSelector.E2">E2</a></code> | E2. |
| <code><a href="#projen.python.RuleSelector.E20">E20</a></code> | E20. |
| <code><a href="#projen.python.RuleSelector.E201">E201</a></code> | E201. |
| <code><a href="#projen.python.RuleSelector.E202">E202</a></code> | E202. |
| <code><a href="#projen.python.RuleSelector.E203">E203</a></code> | E203. |
| <code><a href="#projen.python.RuleSelector.E204">E204</a></code> | E204. |
| <code><a href="#projen.python.RuleSelector.E21">E21</a></code> | E21. |
| <code><a href="#projen.python.RuleSelector.E211">E211</a></code> | E211. |
| <code><a href="#projen.python.RuleSelector.E22">E22</a></code> | E22. |
| <code><a href="#projen.python.RuleSelector.E221">E221</a></code> | E221. |
| <code><a href="#projen.python.RuleSelector.E222">E222</a></code> | E222. |
| <code><a href="#projen.python.RuleSelector.E223">E223</a></code> | E223. |
| <code><a href="#projen.python.RuleSelector.E224">E224</a></code> | E224. |
| <code><a href="#projen.python.RuleSelector.E225">E225</a></code> | E225. |
| <code><a href="#projen.python.RuleSelector.E226">E226</a></code> | E226. |
| <code><a href="#projen.python.RuleSelector.E227">E227</a></code> | E227. |
| <code><a href="#projen.python.RuleSelector.E228">E228</a></code> | E228. |
| <code><a href="#projen.python.RuleSelector.E23">E23</a></code> | E23. |
| <code><a href="#projen.python.RuleSelector.E231">E231</a></code> | E231. |
| <code><a href="#projen.python.RuleSelector.E24">E24</a></code> | E24. |
| <code><a href="#projen.python.RuleSelector.E241">E241</a></code> | E241. |
| <code><a href="#projen.python.RuleSelector.E242">E242</a></code> | E242. |
| <code><a href="#projen.python.RuleSelector.E25">E25</a></code> | E25. |
| <code><a href="#projen.python.RuleSelector.E251">E251</a></code> | E251. |
| <code><a href="#projen.python.RuleSelector.E252">E252</a></code> | E252. |
| <code><a href="#projen.python.RuleSelector.E26">E26</a></code> | E26. |
| <code><a href="#projen.python.RuleSelector.E261">E261</a></code> | E261. |
| <code><a href="#projen.python.RuleSelector.E262">E262</a></code> | E262. |
| <code><a href="#projen.python.RuleSelector.E265">E265</a></code> | E265. |
| <code><a href="#projen.python.RuleSelector.E266">E266</a></code> | E266. |
| <code><a href="#projen.python.RuleSelector.E27">E27</a></code> | E27. |
| <code><a href="#projen.python.RuleSelector.E271">E271</a></code> | E271. |
| <code><a href="#projen.python.RuleSelector.E272">E272</a></code> | E272. |
| <code><a href="#projen.python.RuleSelector.E273">E273</a></code> | E273. |
| <code><a href="#projen.python.RuleSelector.E274">E274</a></code> | E274. |
| <code><a href="#projen.python.RuleSelector.E275">E275</a></code> | E275. |
| <code><a href="#projen.python.RuleSelector.E3">E3</a></code> | E3. |
| <code><a href="#projen.python.RuleSelector.E30">E30</a></code> | E30. |
| <code><a href="#projen.python.RuleSelector.E301">E301</a></code> | E301. |
| <code><a href="#projen.python.RuleSelector.E302">E302</a></code> | E302. |
| <code><a href="#projen.python.RuleSelector.E303">E303</a></code> | E303. |
| <code><a href="#projen.python.RuleSelector.E304">E304</a></code> | E304. |
| <code><a href="#projen.python.RuleSelector.E305">E305</a></code> | E305. |
| <code><a href="#projen.python.RuleSelector.E306">E306</a></code> | E306. |
| <code><a href="#projen.python.RuleSelector.E4">E4</a></code> | E4. |
| <code><a href="#projen.python.RuleSelector.E40">E40</a></code> | E40. |
| <code><a href="#projen.python.RuleSelector.E401">E401</a></code> | E401. |
| <code><a href="#projen.python.RuleSelector.E402">E402</a></code> | E402. |
| <code><a href="#projen.python.RuleSelector.E5">E5</a></code> | E5. |
| <code><a href="#projen.python.RuleSelector.E50">E50</a></code> | E50. |
| <code><a href="#projen.python.RuleSelector.E501">E501</a></code> | E501. |
| <code><a href="#projen.python.RuleSelector.E502">E502</a></code> | E502. |
| <code><a href="#projen.python.RuleSelector.E7">E7</a></code> | E7. |
| <code><a href="#projen.python.RuleSelector.E70">E70</a></code> | E70. |
| <code><a href="#projen.python.RuleSelector.E701">E701</a></code> | E701. |
| <code><a href="#projen.python.RuleSelector.E702">E702</a></code> | E702. |
| <code><a href="#projen.python.RuleSelector.E703">E703</a></code> | E703. |
| <code><a href="#projen.python.RuleSelector.E71">E71</a></code> | E71. |
| <code><a href="#projen.python.RuleSelector.E711">E711</a></code> | E711. |
| <code><a href="#projen.python.RuleSelector.E712">E712</a></code> | E712. |
| <code><a href="#projen.python.RuleSelector.E713">E713</a></code> | E713. |
| <code><a href="#projen.python.RuleSelector.E714">E714</a></code> | E714. |
| <code><a href="#projen.python.RuleSelector.E72">E72</a></code> | E72. |
| <code><a href="#projen.python.RuleSelector.E721">E721</a></code> | E721. |
| <code><a href="#projen.python.RuleSelector.E722">E722</a></code> | E722. |
| <code><a href="#projen.python.RuleSelector.E73">E73</a></code> | E73. |
| <code><a href="#projen.python.RuleSelector.E731">E731</a></code> | E731. |
| <code><a href="#projen.python.RuleSelector.E74">E74</a></code> | E74. |
| <code><a href="#projen.python.RuleSelector.E741">E741</a></code> | E741. |
| <code><a href="#projen.python.RuleSelector.E742">E742</a></code> | E742. |
| <code><a href="#projen.python.RuleSelector.E743">E743</a></code> | E743. |
| <code><a href="#projen.python.RuleSelector.E9">E9</a></code> | E9. |
| <code><a href="#projen.python.RuleSelector.E90">E90</a></code> | E90. |
| <code><a href="#projen.python.RuleSelector.E902">E902</a></code> | E902. |
| <code><a href="#projen.python.RuleSelector.EM">EM</a></code> | EM. |
| <code><a href="#projen.python.RuleSelector.EM1">EM1</a></code> | EM1. |
| <code><a href="#projen.python.RuleSelector.EM10">EM10</a></code> | EM10. |
| <code><a href="#projen.python.RuleSelector.EM101">EM101</a></code> | EM101. |
| <code><a href="#projen.python.RuleSelector.EM102">EM102</a></code> | EM102. |
| <code><a href="#projen.python.RuleSelector.EM103">EM103</a></code> | EM103. |
| <code><a href="#projen.python.RuleSelector.ERA">ERA</a></code> | ERA. |
| <code><a href="#projen.python.RuleSelector.ERA0">ERA0</a></code> | ERA0. |
| <code><a href="#projen.python.RuleSelector.ERA00">ERA00</a></code> | ERA00. |
| <code><a href="#projen.python.RuleSelector.ERA001">ERA001</a></code> | ERA001. |
| <code><a href="#projen.python.RuleSelector.EXE">EXE</a></code> | EXE. |
| <code><a href="#projen.python.RuleSelector.EXE0">EXE0</a></code> | EXE0. |
| <code><a href="#projen.python.RuleSelector.EXE00">EXE00</a></code> | EXE00. |
| <code><a href="#projen.python.RuleSelector.EXE001">EXE001</a></code> | EXE001. |
| <code><a href="#projen.python.RuleSelector.EXE002">EXE002</a></code> | EXE002. |
| <code><a href="#projen.python.RuleSelector.EXE003">EXE003</a></code> | EXE003. |
| <code><a href="#projen.python.RuleSelector.EXE004">EXE004</a></code> | EXE004. |
| <code><a href="#projen.python.RuleSelector.EXE005">EXE005</a></code> | EXE005. |
| <code><a href="#projen.python.RuleSelector.F">F</a></code> | F. |
| <code><a href="#projen.python.RuleSelector.F4">F4</a></code> | F4. |
| <code><a href="#projen.python.RuleSelector.F40">F40</a></code> | F40. |
| <code><a href="#projen.python.RuleSelector.F401">F401</a></code> | F401. |
| <code><a href="#projen.python.RuleSelector.F402">F402</a></code> | F402. |
| <code><a href="#projen.python.RuleSelector.F403">F403</a></code> | F403. |
| <code><a href="#projen.python.RuleSelector.F404">F404</a></code> | F404. |
| <code><a href="#projen.python.RuleSelector.F405">F405</a></code> | F405. |
| <code><a href="#projen.python.RuleSelector.F406">F406</a></code> | F406. |
| <code><a href="#projen.python.RuleSelector.F407">F407</a></code> | F407. |
| <code><a href="#projen.python.RuleSelector.F5">F5</a></code> | F5. |
| <code><a href="#projen.python.RuleSelector.F50">F50</a></code> | F50. |
| <code><a href="#projen.python.RuleSelector.F501">F501</a></code> | F501. |
| <code><a href="#projen.python.RuleSelector.F502">F502</a></code> | F502. |
| <code><a href="#projen.python.RuleSelector.F503">F503</a></code> | F503. |
| <code><a href="#projen.python.RuleSelector.F504">F504</a></code> | F504. |
| <code><a href="#projen.python.RuleSelector.F505">F505</a></code> | F505. |
| <code><a href="#projen.python.RuleSelector.F506">F506</a></code> | F506. |
| <code><a href="#projen.python.RuleSelector.F507">F507</a></code> | F507. |
| <code><a href="#projen.python.RuleSelector.F508">F508</a></code> | F508. |
| <code><a href="#projen.python.RuleSelector.F509">F509</a></code> | F509. |
| <code><a href="#projen.python.RuleSelector.F52">F52</a></code> | F52. |
| <code><a href="#projen.python.RuleSelector.F521">F521</a></code> | F521. |
| <code><a href="#projen.python.RuleSelector.F522">F522</a></code> | F522. |
| <code><a href="#projen.python.RuleSelector.F523">F523</a></code> | F523. |
| <code><a href="#projen.python.RuleSelector.F524">F524</a></code> | F524. |
| <code><a href="#projen.python.RuleSelector.F525">F525</a></code> | F525. |
| <code><a href="#projen.python.RuleSelector.F54">F54</a></code> | F54. |
| <code><a href="#projen.python.RuleSelector.F541">F541</a></code> | F541. |
| <code><a href="#projen.python.RuleSelector.F6">F6</a></code> | F6. |
| <code><a href="#projen.python.RuleSelector.F60">F60</a></code> | F60. |
| <code><a href="#projen.python.RuleSelector.F601">F601</a></code> | F601. |
| <code><a href="#projen.python.RuleSelector.F602">F602</a></code> | F602. |
| <code><a href="#projen.python.RuleSelector.F62">F62</a></code> | F62. |
| <code><a href="#projen.python.RuleSelector.F621">F621</a></code> | F621. |
| <code><a href="#projen.python.RuleSelector.F622">F622</a></code> | F622. |
| <code><a href="#projen.python.RuleSelector.F63">F63</a></code> | F63. |
| <code><a href="#projen.python.RuleSelector.F631">F631</a></code> | F631. |
| <code><a href="#projen.python.RuleSelector.F632">F632</a></code> | F632. |
| <code><a href="#projen.python.RuleSelector.F633">F633</a></code> | F633. |
| <code><a href="#projen.python.RuleSelector.F634">F634</a></code> | F634. |
| <code><a href="#projen.python.RuleSelector.F7">F7</a></code> | F7. |
| <code><a href="#projen.python.RuleSelector.F70">F70</a></code> | F70. |
| <code><a href="#projen.python.RuleSelector.F701">F701</a></code> | F701. |
| <code><a href="#projen.python.RuleSelector.F702">F702</a></code> | F702. |
| <code><a href="#projen.python.RuleSelector.F704">F704</a></code> | F704. |
| <code><a href="#projen.python.RuleSelector.F706">F706</a></code> | F706. |
| <code><a href="#projen.python.RuleSelector.F707">F707</a></code> | F707. |
| <code><a href="#projen.python.RuleSelector.F72">F72</a></code> | F72. |
| <code><a href="#projen.python.RuleSelector.F722">F722</a></code> | F722. |
| <code><a href="#projen.python.RuleSelector.F8">F8</a></code> | F8. |
| <code><a href="#projen.python.RuleSelector.F81">F81</a></code> | F81. |
| <code><a href="#projen.python.RuleSelector.F811">F811</a></code> | F811. |
| <code><a href="#projen.python.RuleSelector.F82">F82</a></code> | F82. |
| <code><a href="#projen.python.RuleSelector.F821">F821</a></code> | F821. |
| <code><a href="#projen.python.RuleSelector.F822">F822</a></code> | F822. |
| <code><a href="#projen.python.RuleSelector.F823">F823</a></code> | F823. |
| <code><a href="#projen.python.RuleSelector.F84">F84</a></code> | F84. |
| <code><a href="#projen.python.RuleSelector.F841">F841</a></code> | F841. |
| <code><a href="#projen.python.RuleSelector.F842">F842</a></code> | F842. |
| <code><a href="#projen.python.RuleSelector.F9">F9</a></code> | F9. |
| <code><a href="#projen.python.RuleSelector.F90">F90</a></code> | F90. |
| <code><a href="#projen.python.RuleSelector.F901">F901</a></code> | F901. |
| <code><a href="#projen.python.RuleSelector.FA">FA</a></code> | FA. |
| <code><a href="#projen.python.RuleSelector.FA1">FA1</a></code> | FA1. |
| <code><a href="#projen.python.RuleSelector.FA10">FA10</a></code> | FA10. |
| <code><a href="#projen.python.RuleSelector.FA100">FA100</a></code> | FA100. |
| <code><a href="#projen.python.RuleSelector.FA102">FA102</a></code> | FA102. |
| <code><a href="#projen.python.RuleSelector.FAST">FAST</a></code> | FAST. |
| <code><a href="#projen.python.RuleSelector.FAST0">FAST0</a></code> | FAST0. |
| <code><a href="#projen.python.RuleSelector.FAST00">FAST00</a></code> | FAST00. |
| <code><a href="#projen.python.RuleSelector.FAST001">FAST001</a></code> | FAST001. |
| <code><a href="#projen.python.RuleSelector.FAST002">FAST002</a></code> | FAST002. |
| <code><a href="#projen.python.RuleSelector.FAST003">FAST003</a></code> | FAST003. |
| <code><a href="#projen.python.RuleSelector.FBT">FBT</a></code> | FBT. |
| <code><a href="#projen.python.RuleSelector.FBT0">FBT0</a></code> | FBT0. |
| <code><a href="#projen.python.RuleSelector.FBT00">FBT00</a></code> | FBT00. |
| <code><a href="#projen.python.RuleSelector.FBT001">FBT001</a></code> | FBT001. |
| <code><a href="#projen.python.RuleSelector.FBT002">FBT002</a></code> | FBT002. |
| <code><a href="#projen.python.RuleSelector.FBT003">FBT003</a></code> | FBT003. |
| <code><a href="#projen.python.RuleSelector.FIX">FIX</a></code> | FIX. |
| <code><a href="#projen.python.RuleSelector.FIX0">FIX0</a></code> | FIX0. |
| <code><a href="#projen.python.RuleSelector.FIX00">FIX00</a></code> | FIX00. |
| <code><a href="#projen.python.RuleSelector.FIX001">FIX001</a></code> | FIX001. |
| <code><a href="#projen.python.RuleSelector.FIX002">FIX002</a></code> | FIX002. |
| <code><a href="#projen.python.RuleSelector.FIX003">FIX003</a></code> | FIX003. |
| <code><a href="#projen.python.RuleSelector.FIX004">FIX004</a></code> | FIX004. |
| <code><a href="#projen.python.RuleSelector.FLY">FLY</a></code> | FLY. |
| <code><a href="#projen.python.RuleSelector.FLY0">FLY0</a></code> | FLY0. |
| <code><a href="#projen.python.RuleSelector.FLY00">FLY00</a></code> | FLY00. |
| <code><a href="#projen.python.RuleSelector.FLY002">FLY002</a></code> | FLY002. |
| <code><a href="#projen.python.RuleSelector.FURB">FURB</a></code> | FURB. |
| <code><a href="#projen.python.RuleSelector.FURB1">FURB1</a></code> | FURB1. |
| <code><a href="#projen.python.RuleSelector.FURB10">FURB10</a></code> | FURB10. |
| <code><a href="#projen.python.RuleSelector.FURB101">FURB101</a></code> | FURB101. |
| <code><a href="#projen.python.RuleSelector.FURB103">FURB103</a></code> | FURB103. |
| <code><a href="#projen.python.RuleSelector.FURB105">FURB105</a></code> | FURB105. |
| <code><a href="#projen.python.RuleSelector.FURB11">FURB11</a></code> | FURB11. |
| <code><a href="#projen.python.RuleSelector.FURB110">FURB110</a></code> | FURB110. |
| <code><a href="#projen.python.RuleSelector.FURB113">FURB113</a></code> | FURB113. |
| <code><a href="#projen.python.RuleSelector.FURB116">FURB116</a></code> | FURB116. |
| <code><a href="#projen.python.RuleSelector.FURB118">FURB118</a></code> | FURB118. |
| <code><a href="#projen.python.RuleSelector.FURB12">FURB12</a></code> | FURB12. |
| <code><a href="#projen.python.RuleSelector.FURB122">FURB122</a></code> | FURB122. |
| <code><a href="#projen.python.RuleSelector.FURB129">FURB129</a></code> | FURB129. |
| <code><a href="#projen.python.RuleSelector.FURB13">FURB13</a></code> | FURB13. |
| <code><a href="#projen.python.RuleSelector.FURB131">FURB131</a></code> | FURB131. |
| <code><a href="#projen.python.RuleSelector.FURB132">FURB132</a></code> | FURB132. |
| <code><a href="#projen.python.RuleSelector.FURB136">FURB136</a></code> | FURB136. |
| <code><a href="#projen.python.RuleSelector.FURB14">FURB14</a></code> | FURB14. |
| <code><a href="#projen.python.RuleSelector.FURB140">FURB140</a></code> | FURB140. |
| <code><a href="#projen.python.RuleSelector.FURB142">FURB142</a></code> | FURB142. |
| <code><a href="#projen.python.RuleSelector.FURB145">FURB145</a></code> | FURB145. |
| <code><a href="#projen.python.RuleSelector.FURB148">FURB148</a></code> | FURB148. |
| <code><a href="#projen.python.RuleSelector.FURB15">FURB15</a></code> | FURB15. |
| <code><a href="#projen.python.RuleSelector.FURB152">FURB152</a></code> | FURB152. |
| <code><a href="#projen.python.RuleSelector.FURB154">FURB154</a></code> | FURB154. |
| <code><a href="#projen.python.RuleSelector.FURB156">FURB156</a></code> | FURB156. |
| <code><a href="#projen.python.RuleSelector.FURB157">FURB157</a></code> | FURB157. |
| <code><a href="#projen.python.RuleSelector.FURB16">FURB16</a></code> | FURB16. |
| <code><a href="#projen.python.RuleSelector.FURB161">FURB161</a></code> | FURB161. |
| <code><a href="#projen.python.RuleSelector.FURB162">FURB162</a></code> | FURB162. |
| <code><a href="#projen.python.RuleSelector.FURB163">FURB163</a></code> | FURB163. |
| <code><a href="#projen.python.RuleSelector.FURB164">FURB164</a></code> | FURB164. |
| <code><a href="#projen.python.RuleSelector.FURB166">FURB166</a></code> | FURB166. |
| <code><a href="#projen.python.RuleSelector.FURB167">FURB167</a></code> | FURB167. |
| <code><a href="#projen.python.RuleSelector.FURB168">FURB168</a></code> | FURB168. |
| <code><a href="#projen.python.RuleSelector.FURB169">FURB169</a></code> | FURB169. |
| <code><a href="#projen.python.RuleSelector.FURB17">FURB17</a></code> | FURB17. |
| <code><a href="#projen.python.RuleSelector.FURB171">FURB171</a></code> | FURB171. |
| <code><a href="#projen.python.RuleSelector.FURB177">FURB177</a></code> | FURB177. |
| <code><a href="#projen.python.RuleSelector.FURB18">FURB18</a></code> | FURB18. |
| <code><a href="#projen.python.RuleSelector.FURB180">FURB180</a></code> | FURB180. |
| <code><a href="#projen.python.RuleSelector.FURB181">FURB181</a></code> | FURB181. |
| <code><a href="#projen.python.RuleSelector.FURB187">FURB187</a></code> | FURB187. |
| <code><a href="#projen.python.RuleSelector.FURB188">FURB188</a></code> | FURB188. |
| <code><a href="#projen.python.RuleSelector.FURB189">FURB189</a></code> | FURB189. |
| <code><a href="#projen.python.RuleSelector.FURB19">FURB19</a></code> | FURB19. |
| <code><a href="#projen.python.RuleSelector.FURB192">FURB192</a></code> | FURB192. |
| <code><a href="#projen.python.RuleSelector.G">G</a></code> | G. |
| <code><a href="#projen.python.RuleSelector.G0">G0</a></code> | G0. |
| <code><a href="#projen.python.RuleSelector.G00">G00</a></code> | G00. |
| <code><a href="#projen.python.RuleSelector.G001">G001</a></code> | G001. |
| <code><a href="#projen.python.RuleSelector.G002">G002</a></code> | G002. |
| <code><a href="#projen.python.RuleSelector.G003">G003</a></code> | G003. |
| <code><a href="#projen.python.RuleSelector.G004">G004</a></code> | G004. |
| <code><a href="#projen.python.RuleSelector.G01">G01</a></code> | G01. |
| <code><a href="#projen.python.RuleSelector.G010">G010</a></code> | G010. |
| <code><a href="#projen.python.RuleSelector.G1">G1</a></code> | G1. |
| <code><a href="#projen.python.RuleSelector.G10">G10</a></code> | G10. |
| <code><a href="#projen.python.RuleSelector.G101">G101</a></code> | G101. |
| <code><a href="#projen.python.RuleSelector.G2">G2</a></code> | G2. |
| <code><a href="#projen.python.RuleSelector.G20">G20</a></code> | G20. |
| <code><a href="#projen.python.RuleSelector.G201">G201</a></code> | G201. |
| <code><a href="#projen.python.RuleSelector.G202">G202</a></code> | G202. |
| <code><a href="#projen.python.RuleSelector.I">I</a></code> | I. |
| <code><a href="#projen.python.RuleSelector.I0">I0</a></code> | I0. |
| <code><a href="#projen.python.RuleSelector.I00">I00</a></code> | I00. |
| <code><a href="#projen.python.RuleSelector.I001">I001</a></code> | I001. |
| <code><a href="#projen.python.RuleSelector.I002">I002</a></code> | I002. |
| <code><a href="#projen.python.RuleSelector.ICN">ICN</a></code> | ICN. |
| <code><a href="#projen.python.RuleSelector.ICN0">ICN0</a></code> | ICN0. |
| <code><a href="#projen.python.RuleSelector.ICN00">ICN00</a></code> | ICN00. |
| <code><a href="#projen.python.RuleSelector.ICN001">ICN001</a></code> | ICN001. |
| <code><a href="#projen.python.RuleSelector.ICN002">ICN002</a></code> | ICN002. |
| <code><a href="#projen.python.RuleSelector.ICN003">ICN003</a></code> | ICN003. |
| <code><a href="#projen.python.RuleSelector.INP">INP</a></code> | INP. |
| <code><a href="#projen.python.RuleSelector.INP0">INP0</a></code> | INP0. |
| <code><a href="#projen.python.RuleSelector.INP00">INP00</a></code> | INP00. |
| <code><a href="#projen.python.RuleSelector.INP001">INP001</a></code> | INP001. |
| <code><a href="#projen.python.RuleSelector.INT">INT</a></code> | INT. |
| <code><a href="#projen.python.RuleSelector.INT0">INT0</a></code> | INT0. |
| <code><a href="#projen.python.RuleSelector.INT00">INT00</a></code> | INT00. |
| <code><a href="#projen.python.RuleSelector.INT001">INT001</a></code> | INT001. |
| <code><a href="#projen.python.RuleSelector.INT002">INT002</a></code> | INT002. |
| <code><a href="#projen.python.RuleSelector.INT003">INT003</a></code> | INT003. |
| <code><a href="#projen.python.RuleSelector.ISC">ISC</a></code> | ISC. |
| <code><a href="#projen.python.RuleSelector.ISC0">ISC0</a></code> | ISC0. |
| <code><a href="#projen.python.RuleSelector.ISC00">ISC00</a></code> | ISC00. |
| <code><a href="#projen.python.RuleSelector.ISC001">ISC001</a></code> | ISC001. |
| <code><a href="#projen.python.RuleSelector.ISC002">ISC002</a></code> | ISC002. |
| <code><a href="#projen.python.RuleSelector.ISC003">ISC003</a></code> | ISC003. |
| <code><a href="#projen.python.RuleSelector.LOG">LOG</a></code> | LOG. |
| <code><a href="#projen.python.RuleSelector.LOG0">LOG0</a></code> | LOG0. |
| <code><a href="#projen.python.RuleSelector.LOG00">LOG00</a></code> | LOG00. |
| <code><a href="#projen.python.RuleSelector.LOG001">LOG001</a></code> | LOG001. |
| <code><a href="#projen.python.RuleSelector.LOG002">LOG002</a></code> | LOG002. |
| <code><a href="#projen.python.RuleSelector.LOG004">LOG004</a></code> | LOG004. |
| <code><a href="#projen.python.RuleSelector.LOG007">LOG007</a></code> | LOG007. |
| <code><a href="#projen.python.RuleSelector.LOG009">LOG009</a></code> | LOG009. |
| <code><a href="#projen.python.RuleSelector.LOG01">LOG01</a></code> | LOG01. |
| <code><a href="#projen.python.RuleSelector.LOG014">LOG014</a></code> | LOG014. |
| <code><a href="#projen.python.RuleSelector.LOG015">LOG015</a></code> | LOG015. |
| <code><a href="#projen.python.RuleSelector.N">N</a></code> | N. |
| <code><a href="#projen.python.RuleSelector.N8">N8</a></code> | N8. |
| <code><a href="#projen.python.RuleSelector.N80">N80</a></code> | N80. |
| <code><a href="#projen.python.RuleSelector.N801">N801</a></code> | N801. |
| <code><a href="#projen.python.RuleSelector.N802">N802</a></code> | N802. |
| <code><a href="#projen.python.RuleSelector.N803">N803</a></code> | N803. |
| <code><a href="#projen.python.RuleSelector.N804">N804</a></code> | N804. |
| <code><a href="#projen.python.RuleSelector.N805">N805</a></code> | N805. |
| <code><a href="#projen.python.RuleSelector.N806">N806</a></code> | N806. |
| <code><a href="#projen.python.RuleSelector.N807">N807</a></code> | N807. |
| <code><a href="#projen.python.RuleSelector.N81">N81</a></code> | N81. |
| <code><a href="#projen.python.RuleSelector.N811">N811</a></code> | N811. |
| <code><a href="#projen.python.RuleSelector.N812">N812</a></code> | N812. |
| <code><a href="#projen.python.RuleSelector.N813">N813</a></code> | N813. |
| <code><a href="#projen.python.RuleSelector.N814">N814</a></code> | N814. |
| <code><a href="#projen.python.RuleSelector.N815">N815</a></code> | N815. |
| <code><a href="#projen.python.RuleSelector.N816">N816</a></code> | N816. |
| <code><a href="#projen.python.RuleSelector.N817">N817</a></code> | N817. |
| <code><a href="#projen.python.RuleSelector.N818">N818</a></code> | N818. |
| <code><a href="#projen.python.RuleSelector.N9">N9</a></code> | N9. |
| <code><a href="#projen.python.RuleSelector.N99">N99</a></code> | N99. |
| <code><a href="#projen.python.RuleSelector.N999">N999</a></code> | N999. |
| <code><a href="#projen.python.RuleSelector.NPY">NPY</a></code> | NPY. |
| <code><a href="#projen.python.RuleSelector.NPY0">NPY0</a></code> | NPY0. |
| <code><a href="#projen.python.RuleSelector.NPY00">NPY00</a></code> | NPY00. |
| <code><a href="#projen.python.RuleSelector.NPY001">NPY001</a></code> | NPY001. |
| <code><a href="#projen.python.RuleSelector.NPY002">NPY002</a></code> | NPY002. |
| <code><a href="#projen.python.RuleSelector.NPY003">NPY003</a></code> | NPY003. |
| <code><a href="#projen.python.RuleSelector.NPY2">NPY2</a></code> | NPY2. |
| <code><a href="#projen.python.RuleSelector.NPY20">NPY20</a></code> | NPY20. |
| <code><a href="#projen.python.RuleSelector.NPY201">NPY201</a></code> | NPY201. |
| <code><a href="#projen.python.RuleSelector.PD">PD</a></code> | PD. |
| <code><a href="#projen.python.RuleSelector.PD0">PD0</a></code> | PD0. |
| <code><a href="#projen.python.RuleSelector.PD00">PD00</a></code> | PD00. |
| <code><a href="#projen.python.RuleSelector.PD002">PD002</a></code> | PD002. |
| <code><a href="#projen.python.RuleSelector.PD003">PD003</a></code> | PD003. |
| <code><a href="#projen.python.RuleSelector.PD004">PD004</a></code> | PD004. |
| <code><a href="#projen.python.RuleSelector.PD007">PD007</a></code> | PD007. |
| <code><a href="#projen.python.RuleSelector.PD008">PD008</a></code> | PD008. |
| <code><a href="#projen.python.RuleSelector.PD009">PD009</a></code> | PD009. |
| <code><a href="#projen.python.RuleSelector.PD01">PD01</a></code> | PD01. |
| <code><a href="#projen.python.RuleSelector.PD010">PD010</a></code> | PD010. |
| <code><a href="#projen.python.RuleSelector.PD011">PD011</a></code> | PD011. |
| <code><a href="#projen.python.RuleSelector.PD012">PD012</a></code> | PD012. |
| <code><a href="#projen.python.RuleSelector.PD013">PD013</a></code> | PD013. |
| <code><a href="#projen.python.RuleSelector.PD015">PD015</a></code> | PD015. |
| <code><a href="#projen.python.RuleSelector.PD1">PD1</a></code> | PD1. |
| <code><a href="#projen.python.RuleSelector.PD10">PD10</a></code> | PD10. |
| <code><a href="#projen.python.RuleSelector.PD101">PD101</a></code> | PD101. |
| <code><a href="#projen.python.RuleSelector.PERF">PERF</a></code> | PERF. |
| <code><a href="#projen.python.RuleSelector.PERF1">PERF1</a></code> | PERF1. |
| <code><a href="#projen.python.RuleSelector.PERF10">PERF10</a></code> | PERF10. |
| <code><a href="#projen.python.RuleSelector.PERF101">PERF101</a></code> | PERF101. |
| <code><a href="#projen.python.RuleSelector.PERF102">PERF102</a></code> | PERF102. |
| <code><a href="#projen.python.RuleSelector.PERF2">PERF2</a></code> | PERF2. |
| <code><a href="#projen.python.RuleSelector.PERF20">PERF20</a></code> | PERF20. |
| <code><a href="#projen.python.RuleSelector.PERF203">PERF203</a></code> | PERF203. |
| <code><a href="#projen.python.RuleSelector.PERF4">PERF4</a></code> | PERF4. |
| <code><a href="#projen.python.RuleSelector.PERF40">PERF40</a></code> | PERF40. |
| <code><a href="#projen.python.RuleSelector.PERF401">PERF401</a></code> | PERF401. |
| <code><a href="#projen.python.RuleSelector.PERF402">PERF402</a></code> | PERF402. |
| <code><a href="#projen.python.RuleSelector.PERF403">PERF403</a></code> | PERF403. |
| <code><a href="#projen.python.RuleSelector.PGH">PGH</a></code> | PGH. |
| <code><a href="#projen.python.RuleSelector.PGH0">PGH0</a></code> | PGH0. |
| <code><a href="#projen.python.RuleSelector.PGH00">PGH00</a></code> | PGH00. |
| <code><a href="#projen.python.RuleSelector.PGH003">PGH003</a></code> | PGH003. |
| <code><a href="#projen.python.RuleSelector.PGH004">PGH004</a></code> | PGH004. |
| <code><a href="#projen.python.RuleSelector.PGH005">PGH005</a></code> | PGH005. |
| <code><a href="#projen.python.RuleSelector.PIE">PIE</a></code> | PIE. |
| <code><a href="#projen.python.RuleSelector.PIE7">PIE7</a></code> | PIE7. |
| <code><a href="#projen.python.RuleSelector.PIE79">PIE79</a></code> | PIE79. |
| <code><a href="#projen.python.RuleSelector.PIE790">PIE790</a></code> | PIE790. |
| <code><a href="#projen.python.RuleSelector.PIE794">PIE794</a></code> | PIE794. |
| <code><a href="#projen.python.RuleSelector.PIE796">PIE796</a></code> | PIE796. |
| <code><a href="#projen.python.RuleSelector.PIE8">PIE8</a></code> | PIE8. |
| <code><a href="#projen.python.RuleSelector.PIE80">PIE80</a></code> | PIE80. |
| <code><a href="#projen.python.RuleSelector.PIE800">PIE800</a></code> | PIE800. |
| <code><a href="#projen.python.RuleSelector.PIE804">PIE804</a></code> | PIE804. |
| <code><a href="#projen.python.RuleSelector.PIE807">PIE807</a></code> | PIE807. |
| <code><a href="#projen.python.RuleSelector.PIE808">PIE808</a></code> | PIE808. |
| <code><a href="#projen.python.RuleSelector.PIE81">PIE81</a></code> | PIE81. |
| <code><a href="#projen.python.RuleSelector.PIE810">PIE810</a></code> | PIE810. |
| <code><a href="#projen.python.RuleSelector.PL">PL</a></code> | PL. |
| <code><a href="#projen.python.RuleSelector.PLC">PLC</a></code> | PLC. |
| <code><a href="#projen.python.RuleSelector.PLC0">PLC0</a></code> | PLC0. |
| <code><a href="#projen.python.RuleSelector.PLC01">PLC01</a></code> | PLC01. |
| <code><a href="#projen.python.RuleSelector.PLC010">PLC010</a></code> | PLC010. |
| <code><a href="#projen.python.RuleSelector.PLC0105">PLC0105</a></code> | PLC0105. |
| <code><a href="#projen.python.RuleSelector.PLC013">PLC013</a></code> | PLC013. |
| <code><a href="#projen.python.RuleSelector.PLC0131">PLC0131</a></code> | PLC0131. |
| <code><a href="#projen.python.RuleSelector.PLC0132">PLC0132</a></code> | PLC0132. |
| <code><a href="#projen.python.RuleSelector.PLC02">PLC02</a></code> | PLC02. |
| <code><a href="#projen.python.RuleSelector.PLC020">PLC020</a></code> | PLC020. |
| <code><a href="#projen.python.RuleSelector.PLC0205">PLC0205</a></code> | PLC0205. |
| <code><a href="#projen.python.RuleSelector.PLC0206">PLC0206</a></code> | PLC0206. |
| <code><a href="#projen.python.RuleSelector.PLC0207">PLC0207</a></code> | PLC0207. |
| <code><a href="#projen.python.RuleSelector.PLC0208">PLC0208</a></code> | PLC0208. |
| <code><a href="#projen.python.RuleSelector.PLC04">PLC04</a></code> | PLC04. |
| <code><a href="#projen.python.RuleSelector.PLC041">PLC041</a></code> | PLC041. |
| <code><a href="#projen.python.RuleSelector.PLC0414">PLC0414</a></code> | PLC0414. |
| <code><a href="#projen.python.RuleSelector.PLC0415">PLC0415</a></code> | PLC0415. |
| <code><a href="#projen.python.RuleSelector.PLC1">PLC1</a></code> | PLC1. |
| <code><a href="#projen.python.RuleSelector.PLC18">PLC18</a></code> | PLC18. |
| <code><a href="#projen.python.RuleSelector.PLC180">PLC180</a></code> | PLC180. |
| <code><a href="#projen.python.RuleSelector.PLC1802">PLC1802</a></code> | PLC1802. |
| <code><a href="#projen.python.RuleSelector.PLC19">PLC19</a></code> | PLC19. |
| <code><a href="#projen.python.RuleSelector.PLC190">PLC190</a></code> | PLC190. |
| <code><a href="#projen.python.RuleSelector.PLC1901">PLC1901</a></code> | PLC1901. |
| <code><a href="#projen.python.RuleSelector.PLC2">PLC2</a></code> | PLC2. |
| <code><a href="#projen.python.RuleSelector.PLC24">PLC24</a></code> | PLC24. |
| <code><a href="#projen.python.RuleSelector.PLC240">PLC240</a></code> | PLC240. |
| <code><a href="#projen.python.RuleSelector.PLC2401">PLC2401</a></code> | PLC2401. |
| <code><a href="#projen.python.RuleSelector.PLC2403">PLC2403</a></code> | PLC2403. |
| <code><a href="#projen.python.RuleSelector.PLC27">PLC27</a></code> | PLC27. |
| <code><a href="#projen.python.RuleSelector.PLC270">PLC270</a></code> | PLC270. |
| <code><a href="#projen.python.RuleSelector.PLC2701">PLC2701</a></code> | PLC2701. |
| <code><a href="#projen.python.RuleSelector.PLC28">PLC28</a></code> | PLC28. |
| <code><a href="#projen.python.RuleSelector.PLC280">PLC280</a></code> | PLC280. |
| <code><a href="#projen.python.RuleSelector.PLC2801">PLC2801</a></code> | PLC2801. |
| <code><a href="#projen.python.RuleSelector.PLC3">PLC3</a></code> | PLC3. |
| <code><a href="#projen.python.RuleSelector.PLC30">PLC30</a></code> | PLC30. |
| <code><a href="#projen.python.RuleSelector.PLC300">PLC300</a></code> | PLC300. |
| <code><a href="#projen.python.RuleSelector.PLC3002">PLC3002</a></code> | PLC3002. |
| <code><a href="#projen.python.RuleSelector.PLE">PLE</a></code> | PLE. |
| <code><a href="#projen.python.RuleSelector.PLE0">PLE0</a></code> | PLE0. |
| <code><a href="#projen.python.RuleSelector.PLE01">PLE01</a></code> | PLE01. |
| <code><a href="#projen.python.RuleSelector.PLE010">PLE010</a></code> | PLE010. |
| <code><a href="#projen.python.RuleSelector.PLE0100">PLE0100</a></code> | PLE0100. |
| <code><a href="#projen.python.RuleSelector.PLE0101">PLE0101</a></code> | PLE0101. |
| <code><a href="#projen.python.RuleSelector.PLE011">PLE011</a></code> | PLE011. |
| <code><a href="#projen.python.RuleSelector.PLE0115">PLE0115</a></code> | PLE0115. |
| <code><a href="#projen.python.RuleSelector.PLE0116">PLE0116</a></code> | PLE0116. |
| <code><a href="#projen.python.RuleSelector.PLE0117">PLE0117</a></code> | PLE0117. |
| <code><a href="#projen.python.RuleSelector.PLE0118">PLE0118</a></code> | PLE0118. |
| <code><a href="#projen.python.RuleSelector.PLE02">PLE02</a></code> | PLE02. |
| <code><a href="#projen.python.RuleSelector.PLE023">PLE023</a></code> | PLE023. |
| <code><a href="#projen.python.RuleSelector.PLE0237">PLE0237</a></code> | PLE0237. |
| <code><a href="#projen.python.RuleSelector.PLE024">PLE024</a></code> | PLE024. |
| <code><a href="#projen.python.RuleSelector.PLE0241">PLE0241</a></code> | PLE0241. |
| <code><a href="#projen.python.RuleSelector.PLE03">PLE03</a></code> | PLE03. |
| <code><a href="#projen.python.RuleSelector.PLE030">PLE030</a></code> | PLE030. |
| <code><a href="#projen.python.RuleSelector.PLE0302">PLE0302</a></code> | PLE0302. |
| <code><a href="#projen.python.RuleSelector.PLE0303">PLE0303</a></code> | PLE0303. |
| <code><a href="#projen.python.RuleSelector.PLE0304">PLE0304</a></code> | PLE0304. |
| <code><a href="#projen.python.RuleSelector.PLE0305">PLE0305</a></code> | PLE0305. |
| <code><a href="#projen.python.RuleSelector.PLE0307">PLE0307</a></code> | PLE0307. |
| <code><a href="#projen.python.RuleSelector.PLE0308">PLE0308</a></code> | PLE0308. |
| <code><a href="#projen.python.RuleSelector.PLE0309">PLE0309</a></code> | PLE0309. |
| <code><a href="#projen.python.RuleSelector.PLE06">PLE06</a></code> | PLE06. |
| <code><a href="#projen.python.RuleSelector.PLE060">PLE060</a></code> | PLE060. |
| <code><a href="#projen.python.RuleSelector.PLE0604">PLE0604</a></code> | PLE0604. |
| <code><a href="#projen.python.RuleSelector.PLE0605">PLE0605</a></code> | PLE0605. |
| <code><a href="#projen.python.RuleSelector.PLE064">PLE064</a></code> | PLE064. |
| <code><a href="#projen.python.RuleSelector.PLE0643">PLE0643</a></code> | PLE0643. |
| <code><a href="#projen.python.RuleSelector.PLE07">PLE07</a></code> | PLE07. |
| <code><a href="#projen.python.RuleSelector.PLE070">PLE070</a></code> | PLE070. |
| <code><a href="#projen.python.RuleSelector.PLE0704">PLE0704</a></code> | PLE0704. |
| <code><a href="#projen.python.RuleSelector.PLE1">PLE1</a></code> | PLE1. |
| <code><a href="#projen.python.RuleSelector.PLE11">PLE11</a></code> | PLE11. |
| <code><a href="#projen.python.RuleSelector.PLE113">PLE113</a></code> | PLE113. |
| <code><a href="#projen.python.RuleSelector.PLE1132">PLE1132</a></code> | PLE1132. |
| <code><a href="#projen.python.RuleSelector.PLE114">PLE114</a></code> | PLE114. |
| <code><a href="#projen.python.RuleSelector.PLE1141">PLE1141</a></code> | PLE1141. |
| <code><a href="#projen.python.RuleSelector.PLE1142">PLE1142</a></code> | PLE1142. |
| <code><a href="#projen.python.RuleSelector.PLE12">PLE12</a></code> | PLE12. |
| <code><a href="#projen.python.RuleSelector.PLE120">PLE120</a></code> | PLE120. |
| <code><a href="#projen.python.RuleSelector.PLE1205">PLE1205</a></code> | PLE1205. |
| <code><a href="#projen.python.RuleSelector.PLE1206">PLE1206</a></code> | PLE1206. |
| <code><a href="#projen.python.RuleSelector.PLE13">PLE13</a></code> | PLE13. |
| <code><a href="#projen.python.RuleSelector.PLE130">PLE130</a></code> | PLE130. |
| <code><a href="#projen.python.RuleSelector.PLE1300">PLE1300</a></code> | PLE1300. |
| <code><a href="#projen.python.RuleSelector.PLE1307">PLE1307</a></code> | PLE1307. |
| <code><a href="#projen.python.RuleSelector.PLE131">PLE131</a></code> | PLE131. |
| <code><a href="#projen.python.RuleSelector.PLE1310">PLE1310</a></code> | PLE1310. |
| <code><a href="#projen.python.RuleSelector.PLE15">PLE15</a></code> | PLE15. |
| <code><a href="#projen.python.RuleSelector.PLE150">PLE150</a></code> | PLE150. |
| <code><a href="#projen.python.RuleSelector.PLE1507">PLE1507</a></code> | PLE1507. |
| <code><a href="#projen.python.RuleSelector.PLE151">PLE151</a></code> | PLE151. |
| <code><a href="#projen.python.RuleSelector.PLE1519">PLE1519</a></code> | PLE1519. |
| <code><a href="#projen.python.RuleSelector.PLE152">PLE152</a></code> | PLE152. |
| <code><a href="#projen.python.RuleSelector.PLE1520">PLE1520</a></code> | PLE1520. |
| <code><a href="#projen.python.RuleSelector.PLE17">PLE17</a></code> | PLE17. |
| <code><a href="#projen.python.RuleSelector.PLE170">PLE170</a></code> | PLE170. |
| <code><a href="#projen.python.RuleSelector.PLE1700">PLE1700</a></code> | PLE1700. |
| <code><a href="#projen.python.RuleSelector.PLE2">PLE2</a></code> | PLE2. |
| <code><a href="#projen.python.RuleSelector.PLE25">PLE25</a></code> | PLE25. |
| <code><a href="#projen.python.RuleSelector.PLE250">PLE250</a></code> | PLE250. |
| <code><a href="#projen.python.RuleSelector.PLE2502">PLE2502</a></code> | PLE2502. |
| <code><a href="#projen.python.RuleSelector.PLE251">PLE251</a></code> | PLE251. |
| <code><a href="#projen.python.RuleSelector.PLE2510">PLE2510</a></code> | PLE2510. |
| <code><a href="#projen.python.RuleSelector.PLE2512">PLE2512</a></code> | PLE2512. |
| <code><a href="#projen.python.RuleSelector.PLE2513">PLE2513</a></code> | PLE2513. |
| <code><a href="#projen.python.RuleSelector.PLE2514">PLE2514</a></code> | PLE2514. |
| <code><a href="#projen.python.RuleSelector.PLE2515">PLE2515</a></code> | PLE2515. |
| <code><a href="#projen.python.RuleSelector.PLE4">PLE4</a></code> | PLE4. |
| <code><a href="#projen.python.RuleSelector.PLE47">PLE47</a></code> | PLE47. |
| <code><a href="#projen.python.RuleSelector.PLE470">PLE470</a></code> | PLE470. |
| <code><a href="#projen.python.RuleSelector.PLE4703">PLE4703</a></code> | PLE4703. |
| <code><a href="#projen.python.RuleSelector.PLR">PLR</a></code> | PLR. |
| <code><a href="#projen.python.RuleSelector.PLR0">PLR0</a></code> | PLR0. |
| <code><a href="#projen.python.RuleSelector.PLR01">PLR01</a></code> | PLR01. |
| <code><a href="#projen.python.RuleSelector.PLR012">PLR012</a></code> | PLR012. |
| <code><a href="#projen.python.RuleSelector.PLR0124">PLR0124</a></code> | PLR0124. |
| <code><a href="#projen.python.RuleSelector.PLR013">PLR013</a></code> | PLR013. |
| <code><a href="#projen.python.RuleSelector.PLR0133">PLR0133</a></code> | PLR0133. |
| <code><a href="#projen.python.RuleSelector.PLR02">PLR02</a></code> | PLR02. |
| <code><a href="#projen.python.RuleSelector.PLR020">PLR020</a></code> | PLR020. |
| <code><a href="#projen.python.RuleSelector.PLR0202">PLR0202</a></code> | PLR0202. |
| <code><a href="#projen.python.RuleSelector.PLR0203">PLR0203</a></code> | PLR0203. |
| <code><a href="#projen.python.RuleSelector.PLR0206">PLR0206</a></code> | PLR0206. |
| <code><a href="#projen.python.RuleSelector.PLR04">PLR04</a></code> | PLR04. |
| <code><a href="#projen.python.RuleSelector.PLR040">PLR040</a></code> | PLR040. |
| <code><a href="#projen.python.RuleSelector.PLR0402">PLR0402</a></code> | PLR0402. |
| <code><a href="#projen.python.RuleSelector.PLR09">PLR09</a></code> | PLR09. |
| <code><a href="#projen.python.RuleSelector.PLR090">PLR090</a></code> | PLR090. |
| <code><a href="#projen.python.RuleSelector.PLR0904">PLR0904</a></code> | PLR0904. |
| <code><a href="#projen.python.RuleSelector.PLR091">PLR091</a></code> | PLR091. |
| <code><a href="#projen.python.RuleSelector.PLR0911">PLR0911</a></code> | PLR0911. |
| <code><a href="#projen.python.RuleSelector.PLR0912">PLR0912</a></code> | PLR0912. |
| <code><a href="#projen.python.RuleSelector.PLR0913">PLR0913</a></code> | PLR0913. |
| <code><a href="#projen.python.RuleSelector.PLR0914">PLR0914</a></code> | PLR0914. |
| <code><a href="#projen.python.RuleSelector.PLR0915">PLR0915</a></code> | PLR0915. |
| <code><a href="#projen.python.RuleSelector.PLR0916">PLR0916</a></code> | PLR0916. |
| <code><a href="#projen.python.RuleSelector.PLR0917">PLR0917</a></code> | PLR0917. |
| <code><a href="#projen.python.RuleSelector.PLR1">PLR1</a></code> | PLR1. |
| <code><a href="#projen.python.RuleSelector.PLR17">PLR17</a></code> | PLR17. |
| <code><a href="#projen.python.RuleSelector.PLR170">PLR170</a></code> | PLR170. |
| <code><a href="#projen.python.RuleSelector.PLR1702">PLR1702</a></code> | PLR1702. |
| <code><a href="#projen.python.RuleSelector.PLR1704">PLR1704</a></code> | PLR1704. |
| <code><a href="#projen.python.RuleSelector.PLR1708">PLR1708</a></code> | PLR1708. |
| <code><a href="#projen.python.RuleSelector.PLR171">PLR171</a></code> | PLR171. |
| <code><a href="#projen.python.RuleSelector.PLR1711">PLR1711</a></code> | PLR1711. |
| <code><a href="#projen.python.RuleSelector.PLR1714">PLR1714</a></code> | PLR1714. |
| <code><a href="#projen.python.RuleSelector.PLR1716">PLR1716</a></code> | PLR1716. |
| <code><a href="#projen.python.RuleSelector.PLR172">PLR172</a></code> | PLR172. |
| <code><a href="#projen.python.RuleSelector.PLR1722">PLR1722</a></code> | PLR1722. |
| <code><a href="#projen.python.RuleSelector.PLR173">PLR173</a></code> | PLR173. |
| <code><a href="#projen.python.RuleSelector.PLR1730">PLR1730</a></code> | PLR1730. |
| <code><a href="#projen.python.RuleSelector.PLR1733">PLR1733</a></code> | PLR1733. |
| <code><a href="#projen.python.RuleSelector.PLR1736">PLR1736</a></code> | PLR1736. |
| <code><a href="#projen.python.RuleSelector.PLR2">PLR2</a></code> | PLR2. |
| <code><a href="#projen.python.RuleSelector.PLR20">PLR20</a></code> | PLR20. |
| <code><a href="#projen.python.RuleSelector.PLR200">PLR200</a></code> | PLR200. |
| <code><a href="#projen.python.RuleSelector.PLR2004">PLR2004</a></code> | PLR2004. |
| <code><a href="#projen.python.RuleSelector.PLR204">PLR204</a></code> | PLR204. |
| <code><a href="#projen.python.RuleSelector.PLR2044">PLR2044</a></code> | PLR2044. |
| <code><a href="#projen.python.RuleSelector.PLR5">PLR5</a></code> | PLR5. |
| <code><a href="#projen.python.RuleSelector.PLR55">PLR55</a></code> | PLR55. |
| <code><a href="#projen.python.RuleSelector.PLR550">PLR550</a></code> | PLR550. |
| <code><a href="#projen.python.RuleSelector.PLR5501">PLR5501</a></code> | PLR5501. |
| <code><a href="#projen.python.RuleSelector.PLR6">PLR6</a></code> | PLR6. |
| <code><a href="#projen.python.RuleSelector.PLR61">PLR61</a></code> | PLR61. |
| <code><a href="#projen.python.RuleSelector.PLR610">PLR610</a></code> | PLR610. |
| <code><a href="#projen.python.RuleSelector.PLR6104">PLR6104</a></code> | PLR6104. |
| <code><a href="#projen.python.RuleSelector.PLR62">PLR62</a></code> | PLR62. |
| <code><a href="#projen.python.RuleSelector.PLR620">PLR620</a></code> | PLR620. |
| <code><a href="#projen.python.RuleSelector.PLR6201">PLR6201</a></code> | PLR6201. |
| <code><a href="#projen.python.RuleSelector.PLR63">PLR63</a></code> | PLR63. |
| <code><a href="#projen.python.RuleSelector.PLR630">PLR630</a></code> | PLR630. |
| <code><a href="#projen.python.RuleSelector.PLR6301">PLR6301</a></code> | PLR6301. |
| <code><a href="#projen.python.RuleSelector.PLW">PLW</a></code> | PLW. |
| <code><a href="#projen.python.RuleSelector.PLW0">PLW0</a></code> | PLW0. |
| <code><a href="#projen.python.RuleSelector.PLW01">PLW01</a></code> | PLW01. |
| <code><a href="#projen.python.RuleSelector.PLW010">PLW010</a></code> | PLW010. |
| <code><a href="#projen.python.RuleSelector.PLW0108">PLW0108</a></code> | PLW0108. |
| <code><a href="#projen.python.RuleSelector.PLW012">PLW012</a></code> | PLW012. |
| <code><a href="#projen.python.RuleSelector.PLW0120">PLW0120</a></code> | PLW0120. |
| <code><a href="#projen.python.RuleSelector.PLW0127">PLW0127</a></code> | PLW0127. |
| <code><a href="#projen.python.RuleSelector.PLW0128">PLW0128</a></code> | PLW0128. |
| <code><a href="#projen.python.RuleSelector.PLW0129">PLW0129</a></code> | PLW0129. |
| <code><a href="#projen.python.RuleSelector.PLW013">PLW013</a></code> | PLW013. |
| <code><a href="#projen.python.RuleSelector.PLW0131">PLW0131</a></code> | PLW0131. |
| <code><a href="#projen.python.RuleSelector.PLW0133">PLW0133</a></code> | PLW0133. |
| <code><a href="#projen.python.RuleSelector.PLW017">PLW017</a></code> | PLW017. |
| <code><a href="#projen.python.RuleSelector.PLW0177">PLW0177</a></code> | PLW0177. |
| <code><a href="#projen.python.RuleSelector.PLW02">PLW02</a></code> | PLW02. |
| <code><a href="#projen.python.RuleSelector.PLW021">PLW021</a></code> | PLW021. |
| <code><a href="#projen.python.RuleSelector.PLW0211">PLW0211</a></code> | PLW0211. |
| <code><a href="#projen.python.RuleSelector.PLW024">PLW024</a></code> | PLW024. |
| <code><a href="#projen.python.RuleSelector.PLW0244">PLW0244</a></code> | PLW0244. |
| <code><a href="#projen.python.RuleSelector.PLW0245">PLW0245</a></code> | PLW0245. |
| <code><a href="#projen.python.RuleSelector.PLW04">PLW04</a></code> | PLW04. |
| <code><a href="#projen.python.RuleSelector.PLW040">PLW040</a></code> | PLW040. |
| <code><a href="#projen.python.RuleSelector.PLW0406">PLW0406</a></code> | PLW0406. |
| <code><a href="#projen.python.RuleSelector.PLW06">PLW06</a></code> | PLW06. |
| <code><a href="#projen.python.RuleSelector.PLW060">PLW060</a></code> | PLW060. |
| <code><a href="#projen.python.RuleSelector.PLW0602">PLW0602</a></code> | PLW0602. |
| <code><a href="#projen.python.RuleSelector.PLW0603">PLW0603</a></code> | PLW0603. |
| <code><a href="#projen.python.RuleSelector.PLW0604">PLW0604</a></code> | PLW0604. |
| <code><a href="#projen.python.RuleSelector.PLW064">PLW064</a></code> | PLW064. |
| <code><a href="#projen.python.RuleSelector.PLW0642">PLW0642</a></code> | PLW0642. |
| <code><a href="#projen.python.RuleSelector.PLW07">PLW07</a></code> | PLW07. |
| <code><a href="#projen.python.RuleSelector.PLW071">PLW071</a></code> | PLW071. |
| <code><a href="#projen.python.RuleSelector.PLW0711">PLW0711</a></code> | PLW0711. |
| <code><a href="#projen.python.RuleSelector.PLW1">PLW1</a></code> | PLW1. |
| <code><a href="#projen.python.RuleSelector.PLW15">PLW15</a></code> | PLW15. |
| <code><a href="#projen.python.RuleSelector.PLW150">PLW150</a></code> | PLW150. |
| <code><a href="#projen.python.RuleSelector.PLW1501">PLW1501</a></code> | PLW1501. |
| <code><a href="#projen.python.RuleSelector.PLW1507">PLW1507</a></code> | PLW1507. |
| <code><a href="#projen.python.RuleSelector.PLW1508">PLW1508</a></code> | PLW1508. |
| <code><a href="#projen.python.RuleSelector.PLW1509">PLW1509</a></code> | PLW1509. |
| <code><a href="#projen.python.RuleSelector.PLW151">PLW151</a></code> | PLW151. |
| <code><a href="#projen.python.RuleSelector.PLW1510">PLW1510</a></code> | PLW1510. |
| <code><a href="#projen.python.RuleSelector.PLW1514">PLW1514</a></code> | PLW1514. |
| <code><a href="#projen.python.RuleSelector.PLW16">PLW16</a></code> | PLW16. |
| <code><a href="#projen.python.RuleSelector.PLW164">PLW164</a></code> | PLW164. |
| <code><a href="#projen.python.RuleSelector.PLW1641">PLW1641</a></code> | PLW1641. |
| <code><a href="#projen.python.RuleSelector.PLW2">PLW2</a></code> | PLW2. |
| <code><a href="#projen.python.RuleSelector.PLW21">PLW21</a></code> | PLW21. |
| <code><a href="#projen.python.RuleSelector.PLW210">PLW210</a></code> | PLW210. |
| <code><a href="#projen.python.RuleSelector.PLW2101">PLW2101</a></code> | PLW2101. |
| <code><a href="#projen.python.RuleSelector.PLW29">PLW29</a></code> | PLW29. |
| <code><a href="#projen.python.RuleSelector.PLW290">PLW290</a></code> | PLW290. |
| <code><a href="#projen.python.RuleSelector.PLW2901">PLW2901</a></code> | PLW2901. |
| <code><a href="#projen.python.RuleSelector.PLW3">PLW3</a></code> | PLW3. |
| <code><a href="#projen.python.RuleSelector.PLW32">PLW32</a></code> | PLW32. |
| <code><a href="#projen.python.RuleSelector.PLW320">PLW320</a></code> | PLW320. |
| <code><a href="#projen.python.RuleSelector.PLW3201">PLW3201</a></code> | PLW3201. |
| <code><a href="#projen.python.RuleSelector.PLW33">PLW33</a></code> | PLW33. |
| <code><a href="#projen.python.RuleSelector.PLW330">PLW330</a></code> | PLW330. |
| <code><a href="#projen.python.RuleSelector.PLW3301">PLW3301</a></code> | PLW3301. |
| <code><a href="#projen.python.RuleSelector.PT">PT</a></code> | PT. |
| <code><a href="#projen.python.RuleSelector.PT0">PT0</a></code> | PT0. |
| <code><a href="#projen.python.RuleSelector.PT00">PT00</a></code> | PT00. |
| <code><a href="#projen.python.RuleSelector.PT001">PT001</a></code> | PT001. |
| <code><a href="#projen.python.RuleSelector.PT002">PT002</a></code> | PT002. |
| <code><a href="#projen.python.RuleSelector.PT003">PT003</a></code> | PT003. |
| <code><a href="#projen.python.RuleSelector.PT006">PT006</a></code> | PT006. |
| <code><a href="#projen.python.RuleSelector.PT007">PT007</a></code> | PT007. |
| <code><a href="#projen.python.RuleSelector.PT008">PT008</a></code> | PT008. |
| <code><a href="#projen.python.RuleSelector.PT009">PT009</a></code> | PT009. |
| <code><a href="#projen.python.RuleSelector.PT01">PT01</a></code> | PT01. |
| <code><a href="#projen.python.RuleSelector.PT010">PT010</a></code> | PT010. |
| <code><a href="#projen.python.RuleSelector.PT011">PT011</a></code> | PT011. |
| <code><a href="#projen.python.RuleSelector.PT012">PT012</a></code> | PT012. |
| <code><a href="#projen.python.RuleSelector.PT013">PT013</a></code> | PT013. |
| <code><a href="#projen.python.RuleSelector.PT014">PT014</a></code> | PT014. |
| <code><a href="#projen.python.RuleSelector.PT015">PT015</a></code> | PT015. |
| <code><a href="#projen.python.RuleSelector.PT016">PT016</a></code> | PT016. |
| <code><a href="#projen.python.RuleSelector.PT017">PT017</a></code> | PT017. |
| <code><a href="#projen.python.RuleSelector.PT018">PT018</a></code> | PT018. |
| <code><a href="#projen.python.RuleSelector.PT019">PT019</a></code> | PT019. |
| <code><a href="#projen.python.RuleSelector.PT02">PT02</a></code> | PT02. |
| <code><a href="#projen.python.RuleSelector.PT020">PT020</a></code> | PT020. |
| <code><a href="#projen.python.RuleSelector.PT021">PT021</a></code> | PT021. |
| <code><a href="#projen.python.RuleSelector.PT022">PT022</a></code> | PT022. |
| <code><a href="#projen.python.RuleSelector.PT023">PT023</a></code> | PT023. |
| <code><a href="#projen.python.RuleSelector.PT024">PT024</a></code> | PT024. |
| <code><a href="#projen.python.RuleSelector.PT025">PT025</a></code> | PT025. |
| <code><a href="#projen.python.RuleSelector.PT026">PT026</a></code> | PT026. |
| <code><a href="#projen.python.RuleSelector.PT027">PT027</a></code> | PT027. |
| <code><a href="#projen.python.RuleSelector.PT028">PT028</a></code> | PT028. |
| <code><a href="#projen.python.RuleSelector.PT029">PT029</a></code> | PT029. |
| <code><a href="#projen.python.RuleSelector.PT03">PT03</a></code> | PT03. |
| <code><a href="#projen.python.RuleSelector.PT030">PT030</a></code> | PT030. |
| <code><a href="#projen.python.RuleSelector.PT031">PT031</a></code> | PT031. |
| <code><a href="#projen.python.RuleSelector.PTH">PTH</a></code> | PTH. |
| <code><a href="#projen.python.RuleSelector.PTH1">PTH1</a></code> | PTH1. |
| <code><a href="#projen.python.RuleSelector.PTH10">PTH10</a></code> | PTH10. |
| <code><a href="#projen.python.RuleSelector.PTH100">PTH100</a></code> | PTH100. |
| <code><a href="#projen.python.RuleSelector.PTH101">PTH101</a></code> | PTH101. |
| <code><a href="#projen.python.RuleSelector.PTH102">PTH102</a></code> | PTH102. |
| <code><a href="#projen.python.RuleSelector.PTH103">PTH103</a></code> | PTH103. |
| <code><a href="#projen.python.RuleSelector.PTH104">PTH104</a></code> | PTH104. |
| <code><a href="#projen.python.RuleSelector.PTH105">PTH105</a></code> | PTH105. |
| <code><a href="#projen.python.RuleSelector.PTH106">PTH106</a></code> | PTH106. |
| <code><a href="#projen.python.RuleSelector.PTH107">PTH107</a></code> | PTH107. |
| <code><a href="#projen.python.RuleSelector.PTH108">PTH108</a></code> | PTH108. |
| <code><a href="#projen.python.RuleSelector.PTH109">PTH109</a></code> | PTH109. |
| <code><a href="#projen.python.RuleSelector.PTH11">PTH11</a></code> | PTH11. |
| <code><a href="#projen.python.RuleSelector.PTH110">PTH110</a></code> | PTH110. |
| <code><a href="#projen.python.RuleSelector.PTH111">PTH111</a></code> | PTH111. |
| <code><a href="#projen.python.RuleSelector.PTH112">PTH112</a></code> | PTH112. |
| <code><a href="#projen.python.RuleSelector.PTH113">PTH113</a></code> | PTH113. |
| <code><a href="#projen.python.RuleSelector.PTH114">PTH114</a></code> | PTH114. |
| <code><a href="#projen.python.RuleSelector.PTH115">PTH115</a></code> | PTH115. |
| <code><a href="#projen.python.RuleSelector.PTH116">PTH116</a></code> | PTH116. |
| <code><a href="#projen.python.RuleSelector.PTH117">PTH117</a></code> | PTH117. |
| <code><a href="#projen.python.RuleSelector.PTH118">PTH118</a></code> | PTH118. |
| <code><a href="#projen.python.RuleSelector.PTH119">PTH119</a></code> | PTH119. |
| <code><a href="#projen.python.RuleSelector.PTH12">PTH12</a></code> | PTH12. |
| <code><a href="#projen.python.RuleSelector.PTH120">PTH120</a></code> | PTH120. |
| <code><a href="#projen.python.RuleSelector.PTH121">PTH121</a></code> | PTH121. |
| <code><a href="#projen.python.RuleSelector.PTH122">PTH122</a></code> | PTH122. |
| <code><a href="#projen.python.RuleSelector.PTH123">PTH123</a></code> | PTH123. |
| <code><a href="#projen.python.RuleSelector.PTH124">PTH124</a></code> | PTH124. |
| <code><a href="#projen.python.RuleSelector.PTH2">PTH2</a></code> | PTH2. |
| <code><a href="#projen.python.RuleSelector.PTH20">PTH20</a></code> | PTH20. |
| <code><a href="#projen.python.RuleSelector.PTH201">PTH201</a></code> | PTH201. |
| <code><a href="#projen.python.RuleSelector.PTH202">PTH202</a></code> | PTH202. |
| <code><a href="#projen.python.RuleSelector.PTH203">PTH203</a></code> | PTH203. |
| <code><a href="#projen.python.RuleSelector.PTH204">PTH204</a></code> | PTH204. |
| <code><a href="#projen.python.RuleSelector.PTH205">PTH205</a></code> | PTH205. |
| <code><a href="#projen.python.RuleSelector.PTH206">PTH206</a></code> | PTH206. |
| <code><a href="#projen.python.RuleSelector.PTH207">PTH207</a></code> | PTH207. |
| <code><a href="#projen.python.RuleSelector.PTH208">PTH208</a></code> | PTH208. |
| <code><a href="#projen.python.RuleSelector.PTH21">PTH21</a></code> | PTH21. |
| <code><a href="#projen.python.RuleSelector.PTH210">PTH210</a></code> | PTH210. |
| <code><a href="#projen.python.RuleSelector.PTH211">PTH211</a></code> | PTH211. |
| <code><a href="#projen.python.RuleSelector.PYI">PYI</a></code> | PYI. |
| <code><a href="#projen.python.RuleSelector.PYI0">PYI0</a></code> | PYI0. |
| <code><a href="#projen.python.RuleSelector.PYI00">PYI00</a></code> | PYI00. |
| <code><a href="#projen.python.RuleSelector.PYI001">PYI001</a></code> | PYI001. |
| <code><a href="#projen.python.RuleSelector.PYI002">PYI002</a></code> | PYI002. |
| <code><a href="#projen.python.RuleSelector.PYI003">PYI003</a></code> | PYI003. |
| <code><a href="#projen.python.RuleSelector.PYI004">PYI004</a></code> | PYI004. |
| <code><a href="#projen.python.RuleSelector.PYI005">PYI005</a></code> | PYI005. |
| <code><a href="#projen.python.RuleSelector.PYI006">PYI006</a></code> | PYI006. |
| <code><a href="#projen.python.RuleSelector.PYI007">PYI007</a></code> | PYI007. |
| <code><a href="#projen.python.RuleSelector.PYI008">PYI008</a></code> | PYI008. |
| <code><a href="#projen.python.RuleSelector.PYI009">PYI009</a></code> | PYI009. |
| <code><a href="#projen.python.RuleSelector.PYI01">PYI01</a></code> | PYI01. |
| <code><a href="#projen.python.RuleSelector.PYI010">PYI010</a></code> | PYI010. |
| <code><a href="#projen.python.RuleSelector.PYI011">PYI011</a></code> | PYI011. |
| <code><a href="#projen.python.RuleSelector.PYI012">PYI012</a></code> | PYI012. |
| <code><a href="#projen.python.RuleSelector.PYI013">PYI013</a></code> | PYI013. |
| <code><a href="#projen.python.RuleSelector.PYI014">PYI014</a></code> | PYI014. |
| <code><a href="#projen.python.RuleSelector.PYI015">PYI015</a></code> | PYI015. |
| <code><a href="#projen.python.RuleSelector.PYI016">PYI016</a></code> | PYI016. |
| <code><a href="#projen.python.RuleSelector.PYI017">PYI017</a></code> | PYI017. |
| <code><a href="#projen.python.RuleSelector.PYI018">PYI018</a></code> | PYI018. |
| <code><a href="#projen.python.RuleSelector.PYI019">PYI019</a></code> | PYI019. |
| <code><a href="#projen.python.RuleSelector.PYI02">PYI02</a></code> | PYI02. |
| <code><a href="#projen.python.RuleSelector.PYI020">PYI020</a></code> | PYI020. |
| <code><a href="#projen.python.RuleSelector.PYI021">PYI021</a></code> | PYI021. |
| <code><a href="#projen.python.RuleSelector.PYI024">PYI024</a></code> | PYI024. |
| <code><a href="#projen.python.RuleSelector.PYI025">PYI025</a></code> | PYI025. |
| <code><a href="#projen.python.RuleSelector.PYI026">PYI026</a></code> | PYI026. |
| <code><a href="#projen.python.RuleSelector.PYI029">PYI029</a></code> | PYI029. |
| <code><a href="#projen.python.RuleSelector.PYI03">PYI03</a></code> | PYI03. |
| <code><a href="#projen.python.RuleSelector.PYI030">PYI030</a></code> | PYI030. |
| <code><a href="#projen.python.RuleSelector.PYI032">PYI032</a></code> | PYI032. |
| <code><a href="#projen.python.RuleSelector.PYI033">PYI033</a></code> | PYI033. |
| <code><a href="#projen.python.RuleSelector.PYI034">PYI034</a></code> | PYI034. |
| <code><a href="#projen.python.RuleSelector.PYI035">PYI035</a></code> | PYI035. |
| <code><a href="#projen.python.RuleSelector.PYI036">PYI036</a></code> | PYI036. |
| <code><a href="#projen.python.RuleSelector.PYI04">PYI04</a></code> | PYI04. |
| <code><a href="#projen.python.RuleSelector.PYI041">PYI041</a></code> | PYI041. |
| <code><a href="#projen.python.RuleSelector.PYI042">PYI042</a></code> | PYI042. |
| <code><a href="#projen.python.RuleSelector.PYI043">PYI043</a></code> | PYI043. |
| <code><a href="#projen.python.RuleSelector.PYI044">PYI044</a></code> | PYI044. |
| <code><a href="#projen.python.RuleSelector.PYI045">PYI045</a></code> | PYI045. |
| <code><a href="#projen.python.RuleSelector.PYI046">PYI046</a></code> | PYI046. |
| <code><a href="#projen.python.RuleSelector.PYI047">PYI047</a></code> | PYI047. |
| <code><a href="#projen.python.RuleSelector.PYI048">PYI048</a></code> | PYI048. |
| <code><a href="#projen.python.RuleSelector.PYI049">PYI049</a></code> | PYI049. |
| <code><a href="#projen.python.RuleSelector.PYI05">PYI05</a></code> | PYI05. |
| <code><a href="#projen.python.RuleSelector.PYI050">PYI050</a></code> | PYI050. |
| <code><a href="#projen.python.RuleSelector.PYI051">PYI051</a></code> | PYI051. |
| <code><a href="#projen.python.RuleSelector.PYI052">PYI052</a></code> | PYI052. |
| <code><a href="#projen.python.RuleSelector.PYI053">PYI053</a></code> | PYI053. |
| <code><a href="#projen.python.RuleSelector.PYI054">PYI054</a></code> | PYI054. |
| <code><a href="#projen.python.RuleSelector.PYI055">PYI055</a></code> | PYI055. |
| <code><a href="#projen.python.RuleSelector.PYI056">PYI056</a></code> | PYI056. |
| <code><a href="#projen.python.RuleSelector.PYI057">PYI057</a></code> | PYI057. |
| <code><a href="#projen.python.RuleSelector.PYI058">PYI058</a></code> | PYI058. |
| <code><a href="#projen.python.RuleSelector.PYI059">PYI059</a></code> | PYI059. |
| <code><a href="#projen.python.RuleSelector.PYI06">PYI06</a></code> | PYI06. |
| <code><a href="#projen.python.RuleSelector.PYI061">PYI061</a></code> | PYI061. |
| <code><a href="#projen.python.RuleSelector.PYI062">PYI062</a></code> | PYI062. |
| <code><a href="#projen.python.RuleSelector.PYI063">PYI063</a></code> | PYI063. |
| <code><a href="#projen.python.RuleSelector.PYI064">PYI064</a></code> | PYI064. |
| <code><a href="#projen.python.RuleSelector.PYI066">PYI066</a></code> | PYI066. |
| <code><a href="#projen.python.RuleSelector.Q">Q</a></code> | Q. |
| <code><a href="#projen.python.RuleSelector.Q0">Q0</a></code> | Q0. |
| <code><a href="#projen.python.RuleSelector.Q00">Q00</a></code> | Q00. |
| <code><a href="#projen.python.RuleSelector.Q000">Q000</a></code> | Q000. |
| <code><a href="#projen.python.RuleSelector.Q001">Q001</a></code> | Q001. |
| <code><a href="#projen.python.RuleSelector.Q002">Q002</a></code> | Q002. |
| <code><a href="#projen.python.RuleSelector.Q003">Q003</a></code> | Q003. |
| <code><a href="#projen.python.RuleSelector.Q004">Q004</a></code> | Q004. |
| <code><a href="#projen.python.RuleSelector.RET">RET</a></code> | RET. |
| <code><a href="#projen.python.RuleSelector.RET5">RET5</a></code> | RET5. |
| <code><a href="#projen.python.RuleSelector.RET50">RET50</a></code> | RET50. |
| <code><a href="#projen.python.RuleSelector.RET501">RET501</a></code> | RET501. |
| <code><a href="#projen.python.RuleSelector.RET502">RET502</a></code> | RET502. |
| <code><a href="#projen.python.RuleSelector.RET503">RET503</a></code> | RET503. |
| <code><a href="#projen.python.RuleSelector.RET504">RET504</a></code> | RET504. |
| <code><a href="#projen.python.RuleSelector.RET505">RET505</a></code> | RET505. |
| <code><a href="#projen.python.RuleSelector.RET506">RET506</a></code> | RET506. |
| <code><a href="#projen.python.RuleSelector.RET507">RET507</a></code> | RET507. |
| <code><a href="#projen.python.RuleSelector.RET508">RET508</a></code> | RET508. |
| <code><a href="#projen.python.RuleSelector.RSE">RSE</a></code> | RSE. |
| <code><a href="#projen.python.RuleSelector.RSE1">RSE1</a></code> | RSE1. |
| <code><a href="#projen.python.RuleSelector.RSE10">RSE10</a></code> | RSE10. |
| <code><a href="#projen.python.RuleSelector.RSE102">RSE102</a></code> | RSE102. |
| <code><a href="#projen.python.RuleSelector.RUF">RUF</a></code> | RUF. |
| <code><a href="#projen.python.RuleSelector.RUF0">RUF0</a></code> | RUF0. |
| <code><a href="#projen.python.RuleSelector.RUF00">RUF00</a></code> | RUF00. |
| <code><a href="#projen.python.RuleSelector.RUF001">RUF001</a></code> | RUF001. |
| <code><a href="#projen.python.RuleSelector.RUF002">RUF002</a></code> | RUF002. |
| <code><a href="#projen.python.RuleSelector.RUF003">RUF003</a></code> | RUF003. |
| <code><a href="#projen.python.RuleSelector.RUF005">RUF005</a></code> | RUF005. |
| <code><a href="#projen.python.RuleSelector.RUF006">RUF006</a></code> | RUF006. |
| <code><a href="#projen.python.RuleSelector.RUF007">RUF007</a></code> | RUF007. |
| <code><a href="#projen.python.RuleSelector.RUF008">RUF008</a></code> | RUF008. |
| <code><a href="#projen.python.RuleSelector.RUF009">RUF009</a></code> | RUF009. |
| <code><a href="#projen.python.RuleSelector.RUF01">RUF01</a></code> | RUF01. |
| <code><a href="#projen.python.RuleSelector.RUF010">RUF010</a></code> | RUF010. |
| <code><a href="#projen.python.RuleSelector.RUF012">RUF012</a></code> | RUF012. |
| <code><a href="#projen.python.RuleSelector.RUF013">RUF013</a></code> | RUF013. |
| <code><a href="#projen.python.RuleSelector.RUF015">RUF015</a></code> | RUF015. |
| <code><a href="#projen.python.RuleSelector.RUF016">RUF016</a></code> | RUF016. |
| <code><a href="#projen.python.RuleSelector.RUF017">RUF017</a></code> | RUF017. |
| <code><a href="#projen.python.RuleSelector.RUF018">RUF018</a></code> | RUF018. |
| <code><a href="#projen.python.RuleSelector.RUF019">RUF019</a></code> | RUF019. |
| <code><a href="#projen.python.RuleSelector.RUF02">RUF02</a></code> | RUF02. |
| <code><a href="#projen.python.RuleSelector.RUF020">RUF020</a></code> | RUF020. |
| <code><a href="#projen.python.RuleSelector.RUF021">RUF021</a></code> | RUF021. |
| <code><a href="#projen.python.RuleSelector.RUF022">RUF022</a></code> | RUF022. |
| <code><a href="#projen.python.RuleSelector.RUF023">RUF023</a></code> | RUF023. |
| <code><a href="#projen.python.RuleSelector.RUF024">RUF024</a></code> | RUF024. |
| <code><a href="#projen.python.RuleSelector.RUF026">RUF026</a></code> | RUF026. |
| <code><a href="#projen.python.RuleSelector.RUF027">RUF027</a></code> | RUF027. |
| <code><a href="#projen.python.RuleSelector.RUF028">RUF028</a></code> | RUF028. |
| <code><a href="#projen.python.RuleSelector.RUF029">RUF029</a></code> | RUF029. |
| <code><a href="#projen.python.RuleSelector.RUF03">RUF03</a></code> | RUF03. |
| <code><a href="#projen.python.RuleSelector.RUF030">RUF030</a></code> | RUF030. |
| <code><a href="#projen.python.RuleSelector.RUF031">RUF031</a></code> | RUF031. |
| <code><a href="#projen.python.RuleSelector.RUF032">RUF032</a></code> | RUF032. |
| <code><a href="#projen.python.RuleSelector.RUF033">RUF033</a></code> | RUF033. |
| <code><a href="#projen.python.RuleSelector.RUF034">RUF034</a></code> | RUF034. |
| <code><a href="#projen.python.RuleSelector.RUF036">RUF036</a></code> | RUF036. |
| <code><a href="#projen.python.RuleSelector.RUF037">RUF037</a></code> | RUF037. |
| <code><a href="#projen.python.RuleSelector.RUF038">RUF038</a></code> | RUF038. |
| <code><a href="#projen.python.RuleSelector.RUF039">RUF039</a></code> | RUF039. |
| <code><a href="#projen.python.RuleSelector.RUF04">RUF04</a></code> | RUF04. |
| <code><a href="#projen.python.RuleSelector.RUF040">RUF040</a></code> | RUF040. |
| <code><a href="#projen.python.RuleSelector.RUF041">RUF041</a></code> | RUF041. |
| <code><a href="#projen.python.RuleSelector.RUF043">RUF043</a></code> | RUF043. |
| <code><a href="#projen.python.RuleSelector.RUF045">RUF045</a></code> | RUF045. |
| <code><a href="#projen.python.RuleSelector.RUF046">RUF046</a></code> | RUF046. |
| <code><a href="#projen.python.RuleSelector.RUF047">RUF047</a></code> | RUF047. |
| <code><a href="#projen.python.RuleSelector.RUF048">RUF048</a></code> | RUF048. |
| <code><a href="#projen.python.RuleSelector.RUF049">RUF049</a></code> | RUF049. |
| <code><a href="#projen.python.RuleSelector.RUF05">RUF05</a></code> | RUF05. |
| <code><a href="#projen.python.RuleSelector.RUF051">RUF051</a></code> | RUF051. |
| <code><a href="#projen.python.RuleSelector.RUF052">RUF052</a></code> | RUF052. |
| <code><a href="#projen.python.RuleSelector.RUF053">RUF053</a></code> | RUF053. |
| <code><a href="#projen.python.RuleSelector.RUF054">RUF054</a></code> | RUF054. |
| <code><a href="#projen.python.RuleSelector.RUF055">RUF055</a></code> | RUF055. |
| <code><a href="#projen.python.RuleSelector.RUF056">RUF056</a></code> | RUF056. |
| <code><a href="#projen.python.RuleSelector.RUF057">RUF057</a></code> | RUF057. |
| <code><a href="#projen.python.RuleSelector.RUF058">RUF058</a></code> | RUF058. |
| <code><a href="#projen.python.RuleSelector.RUF059">RUF059</a></code> | RUF059. |
| <code><a href="#projen.python.RuleSelector.RUF06">RUF06</a></code> | RUF06. |
| <code><a href="#projen.python.RuleSelector.RUF060">RUF060</a></code> | RUF060. |
| <code><a href="#projen.python.RuleSelector.RUF061">RUF061</a></code> | RUF061. |
| <code><a href="#projen.python.RuleSelector.RUF063">RUF063</a></code> | RUF063. |
| <code><a href="#projen.python.RuleSelector.RUF064">RUF064</a></code> | RUF064. |
| <code><a href="#projen.python.RuleSelector.RUF065">RUF065</a></code> | RUF065. |
| <code><a href="#projen.python.RuleSelector.RUF1">RUF1</a></code> | RUF1. |
| <code><a href="#projen.python.RuleSelector.RUF10">RUF10</a></code> | RUF10. |
| <code><a href="#projen.python.RuleSelector.RUF100">RUF100</a></code> | RUF100. |
| <code><a href="#projen.python.RuleSelector.RUF101">RUF101</a></code> | RUF101. |
| <code><a href="#projen.python.RuleSelector.RUF102">RUF102</a></code> | RUF102. |
| <code><a href="#projen.python.RuleSelector.RUF2">RUF2</a></code> | RUF2. |
| <code><a href="#projen.python.RuleSelector.RUF20">RUF20</a></code> | RUF20. |
| <code><a href="#projen.python.RuleSelector.RUF200">RUF200</a></code> | RUF200. |
| <code><a href="#projen.python.RuleSelector.S">S</a></code> | S. |
| <code><a href="#projen.python.RuleSelector.S1">S1</a></code> | S1. |
| <code><a href="#projen.python.RuleSelector.S10">S10</a></code> | S10. |
| <code><a href="#projen.python.RuleSelector.S101">S101</a></code> | S101. |
| <code><a href="#projen.python.RuleSelector.S102">S102</a></code> | S102. |
| <code><a href="#projen.python.RuleSelector.S103">S103</a></code> | S103. |
| <code><a href="#projen.python.RuleSelector.S104">S104</a></code> | S104. |
| <code><a href="#projen.python.RuleSelector.S105">S105</a></code> | S105. |
| <code><a href="#projen.python.RuleSelector.S106">S106</a></code> | S106. |
| <code><a href="#projen.python.RuleSelector.S107">S107</a></code> | S107. |
| <code><a href="#projen.python.RuleSelector.S108">S108</a></code> | S108. |
| <code><a href="#projen.python.RuleSelector.S11">S11</a></code> | S11. |
| <code><a href="#projen.python.RuleSelector.S110">S110</a></code> | S110. |
| <code><a href="#projen.python.RuleSelector.S112">S112</a></code> | S112. |
| <code><a href="#projen.python.RuleSelector.S113">S113</a></code> | S113. |
| <code><a href="#projen.python.RuleSelector.S2">S2</a></code> | S2. |
| <code><a href="#projen.python.RuleSelector.S20">S20</a></code> | S20. |
| <code><a href="#projen.python.RuleSelector.S201">S201</a></code> | S201. |
| <code><a href="#projen.python.RuleSelector.S202">S202</a></code> | S202. |
| <code><a href="#projen.python.RuleSelector.S3">S3</a></code> | S3. |
| <code><a href="#projen.python.RuleSelector.S30">S30</a></code> | S30. |
| <code><a href="#projen.python.RuleSelector.S301">S301</a></code> | S301. |
| <code><a href="#projen.python.RuleSelector.S302">S302</a></code> | S302. |
| <code><a href="#projen.python.RuleSelector.S303">S303</a></code> | S303. |
| <code><a href="#projen.python.RuleSelector.S304">S304</a></code> | S304. |
| <code><a href="#projen.python.RuleSelector.S305">S305</a></code> | S305. |
| <code><a href="#projen.python.RuleSelector.S306">S306</a></code> | S306. |
| <code><a href="#projen.python.RuleSelector.S307">S307</a></code> | S307. |
| <code><a href="#projen.python.RuleSelector.S308">S308</a></code> | S308. |
| <code><a href="#projen.python.RuleSelector.S31">S31</a></code> | S31. |
| <code><a href="#projen.python.RuleSelector.S310">S310</a></code> | S310. |
| <code><a href="#projen.python.RuleSelector.S311">S311</a></code> | S311. |
| <code><a href="#projen.python.RuleSelector.S312">S312</a></code> | S312. |
| <code><a href="#projen.python.RuleSelector.S313">S313</a></code> | S313. |
| <code><a href="#projen.python.RuleSelector.S314">S314</a></code> | S314. |
| <code><a href="#projen.python.RuleSelector.S315">S315</a></code> | S315. |
| <code><a href="#projen.python.RuleSelector.S316">S316</a></code> | S316. |
| <code><a href="#projen.python.RuleSelector.S317">S317</a></code> | S317. |
| <code><a href="#projen.python.RuleSelector.S318">S318</a></code> | S318. |
| <code><a href="#projen.python.RuleSelector.S319">S319</a></code> | S319. |
| <code><a href="#projen.python.RuleSelector.S32">S32</a></code> | S32. |
| <code><a href="#projen.python.RuleSelector.S321">S321</a></code> | S321. |
| <code><a href="#projen.python.RuleSelector.S323">S323</a></code> | S323. |
| <code><a href="#projen.python.RuleSelector.S324">S324</a></code> | S324. |
| <code><a href="#projen.python.RuleSelector.S4">S4</a></code> | S4. |
| <code><a href="#projen.python.RuleSelector.S40">S40</a></code> | S40. |
| <code><a href="#projen.python.RuleSelector.S401">S401</a></code> | S401. |
| <code><a href="#projen.python.RuleSelector.S402">S402</a></code> | S402. |
| <code><a href="#projen.python.RuleSelector.S403">S403</a></code> | S403. |
| <code><a href="#projen.python.RuleSelector.S404">S404</a></code> | S404. |
| <code><a href="#projen.python.RuleSelector.S405">S405</a></code> | S405. |
| <code><a href="#projen.python.RuleSelector.S406">S406</a></code> | S406. |
| <code><a href="#projen.python.RuleSelector.S407">S407</a></code> | S407. |
| <code><a href="#projen.python.RuleSelector.S408">S408</a></code> | S408. |
| <code><a href="#projen.python.RuleSelector.S409">S409</a></code> | S409. |
| <code><a href="#projen.python.RuleSelector.S41">S41</a></code> | S41. |
| <code><a href="#projen.python.RuleSelector.S411">S411</a></code> | S411. |
| <code><a href="#projen.python.RuleSelector.S412">S412</a></code> | S412. |
| <code><a href="#projen.python.RuleSelector.S413">S413</a></code> | S413. |
| <code><a href="#projen.python.RuleSelector.S415">S415</a></code> | S415. |
| <code><a href="#projen.python.RuleSelector.S5">S5</a></code> | S5. |
| <code><a href="#projen.python.RuleSelector.S50">S50</a></code> | S50. |
| <code><a href="#projen.python.RuleSelector.S501">S501</a></code> | S501. |
| <code><a href="#projen.python.RuleSelector.S502">S502</a></code> | S502. |
| <code><a href="#projen.python.RuleSelector.S503">S503</a></code> | S503. |
| <code><a href="#projen.python.RuleSelector.S504">S504</a></code> | S504. |
| <code><a href="#projen.python.RuleSelector.S505">S505</a></code> | S505. |
| <code><a href="#projen.python.RuleSelector.S506">S506</a></code> | S506. |
| <code><a href="#projen.python.RuleSelector.S507">S507</a></code> | S507. |
| <code><a href="#projen.python.RuleSelector.S508">S508</a></code> | S508. |
| <code><a href="#projen.python.RuleSelector.S509">S509</a></code> | S509. |
| <code><a href="#projen.python.RuleSelector.S6">S6</a></code> | S6. |
| <code><a href="#projen.python.RuleSelector.S60">S60</a></code> | S60. |
| <code><a href="#projen.python.RuleSelector.S601">S601</a></code> | S601. |
| <code><a href="#projen.python.RuleSelector.S602">S602</a></code> | S602. |
| <code><a href="#projen.python.RuleSelector.S603">S603</a></code> | S603. |
| <code><a href="#projen.python.RuleSelector.S604">S604</a></code> | S604. |
| <code><a href="#projen.python.RuleSelector.S605">S605</a></code> | S605. |
| <code><a href="#projen.python.RuleSelector.S606">S606</a></code> | S606. |
| <code><a href="#projen.python.RuleSelector.S607">S607</a></code> | S607. |
| <code><a href="#projen.python.RuleSelector.S608">S608</a></code> | S608. |
| <code><a href="#projen.python.RuleSelector.S609">S609</a></code> | S609. |
| <code><a href="#projen.python.RuleSelector.S61">S61</a></code> | S61. |
| <code><a href="#projen.python.RuleSelector.S610">S610</a></code> | S610. |
| <code><a href="#projen.python.RuleSelector.S611">S611</a></code> | S611. |
| <code><a href="#projen.python.RuleSelector.S612">S612</a></code> | S612. |
| <code><a href="#projen.python.RuleSelector.S7">S7</a></code> | S7. |
| <code><a href="#projen.python.RuleSelector.S70">S70</a></code> | S70. |
| <code><a href="#projen.python.RuleSelector.S701">S701</a></code> | S701. |
| <code><a href="#projen.python.RuleSelector.S702">S702</a></code> | S702. |
| <code><a href="#projen.python.RuleSelector.S704">S704</a></code> | S704. |
| <code><a href="#projen.python.RuleSelector.SIM">SIM</a></code> | SIM. |
| <code><a href="#projen.python.RuleSelector.SIM1">SIM1</a></code> | SIM1. |
| <code><a href="#projen.python.RuleSelector.SIM10">SIM10</a></code> | SIM10. |
| <code><a href="#projen.python.RuleSelector.SIM101">SIM101</a></code> | SIM101. |
| <code><a href="#projen.python.RuleSelector.SIM102">SIM102</a></code> | SIM102. |
| <code><a href="#projen.python.RuleSelector.SIM103">SIM103</a></code> | SIM103. |
| <code><a href="#projen.python.RuleSelector.SIM105">SIM105</a></code> | SIM105. |
| <code><a href="#projen.python.RuleSelector.SIM107">SIM107</a></code> | SIM107. |
| <code><a href="#projen.python.RuleSelector.SIM108">SIM108</a></code> | SIM108. |
| <code><a href="#projen.python.RuleSelector.SIM109">SIM109</a></code> | SIM109. |
| <code><a href="#projen.python.RuleSelector.SIM11">SIM11</a></code> | SIM11. |
| <code><a href="#projen.python.RuleSelector.SIM110">SIM110</a></code> | SIM110. |
| <code><a href="#projen.python.RuleSelector.SIM112">SIM112</a></code> | SIM112. |
| <code><a href="#projen.python.RuleSelector.SIM113">SIM113</a></code> | SIM113. |
| <code><a href="#projen.python.RuleSelector.SIM114">SIM114</a></code> | SIM114. |
| <code><a href="#projen.python.RuleSelector.SIM115">SIM115</a></code> | SIM115. |
| <code><a href="#projen.python.RuleSelector.SIM116">SIM116</a></code> | SIM116. |
| <code><a href="#projen.python.RuleSelector.SIM117">SIM117</a></code> | SIM117. |
| <code><a href="#projen.python.RuleSelector.SIM118">SIM118</a></code> | SIM118. |
| <code><a href="#projen.python.RuleSelector.SIM2">SIM2</a></code> | SIM2. |
| <code><a href="#projen.python.RuleSelector.SIM20">SIM20</a></code> | SIM20. |
| <code><a href="#projen.python.RuleSelector.SIM201">SIM201</a></code> | SIM201. |
| <code><a href="#projen.python.RuleSelector.SIM202">SIM202</a></code> | SIM202. |
| <code><a href="#projen.python.RuleSelector.SIM208">SIM208</a></code> | SIM208. |
| <code><a href="#projen.python.RuleSelector.SIM21">SIM21</a></code> | SIM21. |
| <code><a href="#projen.python.RuleSelector.SIM210">SIM210</a></code> | SIM210. |
| <code><a href="#projen.python.RuleSelector.SIM211">SIM211</a></code> | SIM211. |
| <code><a href="#projen.python.RuleSelector.SIM212">SIM212</a></code> | SIM212. |
| <code><a href="#projen.python.RuleSelector.SIM22">SIM22</a></code> | SIM22. |
| <code><a href="#projen.python.RuleSelector.SIM220">SIM220</a></code> | SIM220. |
| <code><a href="#projen.python.RuleSelector.SIM221">SIM221</a></code> | SIM221. |
| <code><a href="#projen.python.RuleSelector.SIM222">SIM222</a></code> | SIM222. |
| <code><a href="#projen.python.RuleSelector.SIM223">SIM223</a></code> | SIM223. |
| <code><a href="#projen.python.RuleSelector.SIM3">SIM3</a></code> | SIM3. |
| <code><a href="#projen.python.RuleSelector.SIM30">SIM30</a></code> | SIM30. |
| <code><a href="#projen.python.RuleSelector.SIM300">SIM300</a></code> | SIM300. |
| <code><a href="#projen.python.RuleSelector.SIM4">SIM4</a></code> | SIM4. |
| <code><a href="#projen.python.RuleSelector.SIM40">SIM40</a></code> | SIM40. |
| <code><a href="#projen.python.RuleSelector.SIM401">SIM401</a></code> | SIM401. |
| <code><a href="#projen.python.RuleSelector.SIM9">SIM9</a></code> | SIM9. |
| <code><a href="#projen.python.RuleSelector.SIM90">SIM90</a></code> | SIM90. |
| <code><a href="#projen.python.RuleSelector.SIM905">SIM905</a></code> | SIM905. |
| <code><a href="#projen.python.RuleSelector.SIM91">SIM91</a></code> | SIM91. |
| <code><a href="#projen.python.RuleSelector.SIM910">SIM910</a></code> | SIM910. |
| <code><a href="#projen.python.RuleSelector.SIM911">SIM911</a></code> | SIM911. |
| <code><a href="#projen.python.RuleSelector.SLF">SLF</a></code> | SLF. |
| <code><a href="#projen.python.RuleSelector.SLF0">SLF0</a></code> | SLF0. |
| <code><a href="#projen.python.RuleSelector.SLF00">SLF00</a></code> | SLF00. |
| <code><a href="#projen.python.RuleSelector.SLF001">SLF001</a></code> | SLF001. |
| <code><a href="#projen.python.RuleSelector.SLOT">SLOT</a></code> | SLOT. |
| <code><a href="#projen.python.RuleSelector.SLOT0">SLOT0</a></code> | SLOT0. |
| <code><a href="#projen.python.RuleSelector.SLOT00">SLOT00</a></code> | SLOT00. |
| <code><a href="#projen.python.RuleSelector.SLOT000">SLOT000</a></code> | SLOT000. |
| <code><a href="#projen.python.RuleSelector.SLOT001">SLOT001</a></code> | SLOT001. |
| <code><a href="#projen.python.RuleSelector.SLOT002">SLOT002</a></code> | SLOT002. |
| <code><a href="#projen.python.RuleSelector.T">T</a></code> | T. |
| <code><a href="#projen.python.RuleSelector.T1">T1</a></code> | T1. |
| <code><a href="#projen.python.RuleSelector.T10">T10</a></code> | T10. |
| <code><a href="#projen.python.RuleSelector.T100">T100</a></code> | T100. |
| <code><a href="#projen.python.RuleSelector.T2">T2</a></code> | T2. |
| <code><a href="#projen.python.RuleSelector.T20">T20</a></code> | T20. |
| <code><a href="#projen.python.RuleSelector.T201">T201</a></code> | T201. |
| <code><a href="#projen.python.RuleSelector.T203">T203</a></code> | T203. |
| <code><a href="#projen.python.RuleSelector.TC">TC</a></code> | TC. |
| <code><a href="#projen.python.RuleSelector.TC0">TC0</a></code> | TC0. |
| <code><a href="#projen.python.RuleSelector.TC00">TC00</a></code> | TC00. |
| <code><a href="#projen.python.RuleSelector.TC001">TC001</a></code> | TC001. |
| <code><a href="#projen.python.RuleSelector.TC002">TC002</a></code> | TC002. |
| <code><a href="#projen.python.RuleSelector.TC003">TC003</a></code> | TC003. |
| <code><a href="#projen.python.RuleSelector.TC004">TC004</a></code> | TC004. |
| <code><a href="#projen.python.RuleSelector.TC005">TC005</a></code> | TC005. |
| <code><a href="#projen.python.RuleSelector.TC006">TC006</a></code> | TC006. |
| <code><a href="#projen.python.RuleSelector.TC007">TC007</a></code> | TC007. |
| <code><a href="#projen.python.RuleSelector.TC008">TC008</a></code> | TC008. |
| <code><a href="#projen.python.RuleSelector.TC01">TC01</a></code> | TC01. |
| <code><a href="#projen.python.RuleSelector.TC010">TC010</a></code> | TC010. |
| <code><a href="#projen.python.RuleSelector.TD">TD</a></code> | TD. |
| <code><a href="#projen.python.RuleSelector.TD0">TD0</a></code> | TD0. |
| <code><a href="#projen.python.RuleSelector.TD00">TD00</a></code> | TD00. |
| <code><a href="#projen.python.RuleSelector.TD001">TD001</a></code> | TD001. |
| <code><a href="#projen.python.RuleSelector.TD002">TD002</a></code> | TD002. |
| <code><a href="#projen.python.RuleSelector.TD003">TD003</a></code> | TD003. |
| <code><a href="#projen.python.RuleSelector.TD004">TD004</a></code> | TD004. |
| <code><a href="#projen.python.RuleSelector.TD005">TD005</a></code> | TD005. |
| <code><a href="#projen.python.RuleSelector.TD006">TD006</a></code> | TD006. |
| <code><a href="#projen.python.RuleSelector.TD007">TD007</a></code> | TD007. |
| <code><a href="#projen.python.RuleSelector.TID">TID</a></code> | TID. |
| <code><a href="#projen.python.RuleSelector.TID2">TID2</a></code> | TID2. |
| <code><a href="#projen.python.RuleSelector.TID25">TID25</a></code> | TID25. |
| <code><a href="#projen.python.RuleSelector.TID251">TID251</a></code> | TID251. |
| <code><a href="#projen.python.RuleSelector.TID252">TID252</a></code> | TID252. |
| <code><a href="#projen.python.RuleSelector.TID253">TID253</a></code> | TID253. |
| <code><a href="#projen.python.RuleSelector.TRY">TRY</a></code> | TRY. |
| <code><a href="#projen.python.RuleSelector.TRY0">TRY0</a></code> | TRY0. |
| <code><a href="#projen.python.RuleSelector.TRY00">TRY00</a></code> | TRY00. |
| <code><a href="#projen.python.RuleSelector.TRY002">TRY002</a></code> | TRY002. |
| <code><a href="#projen.python.RuleSelector.TRY003">TRY003</a></code> | TRY003. |
| <code><a href="#projen.python.RuleSelector.TRY004">TRY004</a></code> | TRY004. |
| <code><a href="#projen.python.RuleSelector.TRY2">TRY2</a></code> | TRY2. |
| <code><a href="#projen.python.RuleSelector.TRY20">TRY20</a></code> | TRY20. |
| <code><a href="#projen.python.RuleSelector.TRY201">TRY201</a></code> | TRY201. |
| <code><a href="#projen.python.RuleSelector.TRY203">TRY203</a></code> | TRY203. |
| <code><a href="#projen.python.RuleSelector.TRY3">TRY3</a></code> | TRY3. |
| <code><a href="#projen.python.RuleSelector.TRY30">TRY30</a></code> | TRY30. |
| <code><a href="#projen.python.RuleSelector.TRY300">TRY300</a></code> | TRY300. |
| <code><a href="#projen.python.RuleSelector.TRY301">TRY301</a></code> | TRY301. |
| <code><a href="#projen.python.RuleSelector.TRY4">TRY4</a></code> | TRY4. |
| <code><a href="#projen.python.RuleSelector.TRY40">TRY40</a></code> | TRY40. |
| <code><a href="#projen.python.RuleSelector.TRY400">TRY400</a></code> | TRY400. |
| <code><a href="#projen.python.RuleSelector.TRY401">TRY401</a></code> | TRY401. |
| <code><a href="#projen.python.RuleSelector.UP">UP</a></code> | UP. |
| <code><a href="#projen.python.RuleSelector.UP0">UP0</a></code> | UP0. |
| <code><a href="#projen.python.RuleSelector.UP00">UP00</a></code> | UP00. |
| <code><a href="#projen.python.RuleSelector.UP001">UP001</a></code> | UP001. |
| <code><a href="#projen.python.RuleSelector.UP003">UP003</a></code> | UP003. |
| <code><a href="#projen.python.RuleSelector.UP004">UP004</a></code> | UP004. |
| <code><a href="#projen.python.RuleSelector.UP005">UP005</a></code> | UP005. |
| <code><a href="#projen.python.RuleSelector.UP006">UP006</a></code> | UP006. |
| <code><a href="#projen.python.RuleSelector.UP007">UP007</a></code> | UP007. |
| <code><a href="#projen.python.RuleSelector.UP008">UP008</a></code> | UP008. |
| <code><a href="#projen.python.RuleSelector.UP009">UP009</a></code> | UP009. |
| <code><a href="#projen.python.RuleSelector.UP01">UP01</a></code> | UP01. |
| <code><a href="#projen.python.RuleSelector.UP010">UP010</a></code> | UP010. |
| <code><a href="#projen.python.RuleSelector.UP011">UP011</a></code> | UP011. |
| <code><a href="#projen.python.RuleSelector.UP012">UP012</a></code> | UP012. |
| <code><a href="#projen.python.RuleSelector.UP013">UP013</a></code> | UP013. |
| <code><a href="#projen.python.RuleSelector.UP014">UP014</a></code> | UP014. |
| <code><a href="#projen.python.RuleSelector.UP015">UP015</a></code> | UP015. |
| <code><a href="#projen.python.RuleSelector.UP017">UP017</a></code> | UP017. |
| <code><a href="#projen.python.RuleSelector.UP018">UP018</a></code> | UP018. |
| <code><a href="#projen.python.RuleSelector.UP019">UP019</a></code> | UP019. |
| <code><a href="#projen.python.RuleSelector.UP02">UP02</a></code> | UP02. |
| <code><a href="#projen.python.RuleSelector.UP020">UP020</a></code> | UP020. |
| <code><a href="#projen.python.RuleSelector.UP021">UP021</a></code> | UP021. |
| <code><a href="#projen.python.RuleSelector.UP022">UP022</a></code> | UP022. |
| <code><a href="#projen.python.RuleSelector.UP023">UP023</a></code> | UP023. |
| <code><a href="#projen.python.RuleSelector.UP024">UP024</a></code> | UP024. |
| <code><a href="#projen.python.RuleSelector.UP025">UP025</a></code> | UP025. |
| <code><a href="#projen.python.RuleSelector.UP026">UP026</a></code> | UP026. |
| <code><a href="#projen.python.RuleSelector.UP028">UP028</a></code> | UP028. |
| <code><a href="#projen.python.RuleSelector.UP029">UP029</a></code> | UP029. |
| <code><a href="#projen.python.RuleSelector.UP03">UP03</a></code> | UP03. |
| <code><a href="#projen.python.RuleSelector.UP030">UP030</a></code> | UP030. |
| <code><a href="#projen.python.RuleSelector.UP031">UP031</a></code> | UP031. |
| <code><a href="#projen.python.RuleSelector.UP032">UP032</a></code> | UP032. |
| <code><a href="#projen.python.RuleSelector.UP033">UP033</a></code> | UP033. |
| <code><a href="#projen.python.RuleSelector.UP034">UP034</a></code> | UP034. |
| <code><a href="#projen.python.RuleSelector.UP035">UP035</a></code> | UP035. |
| <code><a href="#projen.python.RuleSelector.UP036">UP036</a></code> | UP036. |
| <code><a href="#projen.python.RuleSelector.UP037">UP037</a></code> | UP037. |
| <code><a href="#projen.python.RuleSelector.UP039">UP039</a></code> | UP039. |
| <code><a href="#projen.python.RuleSelector.UP04">UP04</a></code> | UP04. |
| <code><a href="#projen.python.RuleSelector.UP040">UP040</a></code> | UP040. |
| <code><a href="#projen.python.RuleSelector.UP041">UP041</a></code> | UP041. |
| <code><a href="#projen.python.RuleSelector.UP042">UP042</a></code> | UP042. |
| <code><a href="#projen.python.RuleSelector.UP043">UP043</a></code> | UP043. |
| <code><a href="#projen.python.RuleSelector.UP044">UP044</a></code> | UP044. |
| <code><a href="#projen.python.RuleSelector.UP045">UP045</a></code> | UP045. |
| <code><a href="#projen.python.RuleSelector.UP046">UP046</a></code> | UP046. |
| <code><a href="#projen.python.RuleSelector.UP047">UP047</a></code> | UP047. |
| <code><a href="#projen.python.RuleSelector.UP049">UP049</a></code> | UP049. |
| <code><a href="#projen.python.RuleSelector.UP05">UP05</a></code> | UP05. |
| <code><a href="#projen.python.RuleSelector.UP050">UP050</a></code> | UP050. |
| <code><a href="#projen.python.RuleSelector.W">W</a></code> | W. |
| <code><a href="#projen.python.RuleSelector.W1">W1</a></code> | W1. |
| <code><a href="#projen.python.RuleSelector.W19">W19</a></code> | W19. |
| <code><a href="#projen.python.RuleSelector.W191">W191</a></code> | W191. |
| <code><a href="#projen.python.RuleSelector.W2">W2</a></code> | W2. |
| <code><a href="#projen.python.RuleSelector.W29">W29</a></code> | W29. |
| <code><a href="#projen.python.RuleSelector.W291">W291</a></code> | W291. |
| <code><a href="#projen.python.RuleSelector.W292">W292</a></code> | W292. |
| <code><a href="#projen.python.RuleSelector.W293">W293</a></code> | W293. |
| <code><a href="#projen.python.RuleSelector.W3">W3</a></code> | W3. |
| <code><a href="#projen.python.RuleSelector.W39">W39</a></code> | W39. |
| <code><a href="#projen.python.RuleSelector.W391">W391</a></code> | W391. |
| <code><a href="#projen.python.RuleSelector.W5">W5</a></code> | W5. |
| <code><a href="#projen.python.RuleSelector.W50">W50</a></code> | W50. |
| <code><a href="#projen.python.RuleSelector.W505">W505</a></code> | W505. |
| <code><a href="#projen.python.RuleSelector.W6">W6</a></code> | W6. |
| <code><a href="#projen.python.RuleSelector.W60">W60</a></code> | W60. |
| <code><a href="#projen.python.RuleSelector.W605">W605</a></code> | W605. |
| <code><a href="#projen.python.RuleSelector.YTT">YTT</a></code> | YTT. |
| <code><a href="#projen.python.RuleSelector.YTT1">YTT1</a></code> | YTT1. |
| <code><a href="#projen.python.RuleSelector.YTT10">YTT10</a></code> | YTT10. |
| <code><a href="#projen.python.RuleSelector.YTT101">YTT101</a></code> | YTT101. |
| <code><a href="#projen.python.RuleSelector.YTT102">YTT102</a></code> | YTT102. |
| <code><a href="#projen.python.RuleSelector.YTT103">YTT103</a></code> | YTT103. |
| <code><a href="#projen.python.RuleSelector.YTT2">YTT2</a></code> | YTT2. |
| <code><a href="#projen.python.RuleSelector.YTT20">YTT20</a></code> | YTT20. |
| <code><a href="#projen.python.RuleSelector.YTT201">YTT201</a></code> | YTT201. |
| <code><a href="#projen.python.RuleSelector.YTT202">YTT202</a></code> | YTT202. |
| <code><a href="#projen.python.RuleSelector.YTT203">YTT203</a></code> | YTT203. |
| <code><a href="#projen.python.RuleSelector.YTT204">YTT204</a></code> | YTT204. |
| <code><a href="#projen.python.RuleSelector.YTT3">YTT3</a></code> | YTT3. |
| <code><a href="#projen.python.RuleSelector.YTT30">YTT30</a></code> | YTT30. |
| <code><a href="#projen.python.RuleSelector.YTT301">YTT301</a></code> | YTT301. |
| <code><a href="#projen.python.RuleSelector.YTT302">YTT302</a></code> | YTT302. |
| <code><a href="#projen.python.RuleSelector.YTT303">YTT303</a></code> | YTT303. |

---

##### `A` <a name="A" id="projen.python.RuleSelector.A"></a>

A.

---


##### `A0` <a name="A0" id="projen.python.RuleSelector.A0"></a>

A0.

---


##### `A00` <a name="A00" id="projen.python.RuleSelector.A00"></a>

A00.

---


##### `A001` <a name="A001" id="projen.python.RuleSelector.A001"></a>

A001.

---


##### `A002` <a name="A002" id="projen.python.RuleSelector.A002"></a>

A002.

---


##### `A003` <a name="A003" id="projen.python.RuleSelector.A003"></a>

A003.

---


##### `A004` <a name="A004" id="projen.python.RuleSelector.A004"></a>

A004.

---


##### `A005` <a name="A005" id="projen.python.RuleSelector.A005"></a>

A005.

---


##### `A006` <a name="A006" id="projen.python.RuleSelector.A006"></a>

A006.

---


##### `AIR` <a name="AIR" id="projen.python.RuleSelector.AIR"></a>

AIR.

---


##### `AIR0` <a name="AIR0" id="projen.python.RuleSelector.AIR0"></a>

AIR0.

---


##### `AIR00` <a name="AIR00" id="projen.python.RuleSelector.AIR00"></a>

AIR00.

---


##### `AIR001` <a name="AIR001" id="projen.python.RuleSelector.AIR001"></a>

AIR001.

---


##### `AIR002` <a name="AIR002" id="projen.python.RuleSelector.AIR002"></a>

AIR002.

---


##### `AIR3` <a name="AIR3" id="projen.python.RuleSelector.AIR3"></a>

AIR3.

---


##### `AIR30` <a name="AIR30" id="projen.python.RuleSelector.AIR30"></a>

AIR30.

---


##### `AIR301` <a name="AIR301" id="projen.python.RuleSelector.AIR301"></a>

AIR301.

---


##### `AIR302` <a name="AIR302" id="projen.python.RuleSelector.AIR302"></a>

AIR302.

---


##### `AIR31` <a name="AIR31" id="projen.python.RuleSelector.AIR31"></a>

AIR31.

---


##### `AIR311` <a name="AIR311" id="projen.python.RuleSelector.AIR311"></a>

AIR311.

---


##### `AIR312` <a name="AIR312" id="projen.python.RuleSelector.AIR312"></a>

AIR312.

---


##### `ALL` <a name="ALL" id="projen.python.RuleSelector.ALL"></a>

ALL.

---


##### `ANN` <a name="ANN" id="projen.python.RuleSelector.ANN"></a>

ANN.

---


##### `ANN0` <a name="ANN0" id="projen.python.RuleSelector.ANN0"></a>

ANN0.

---


##### `ANN00` <a name="ANN00" id="projen.python.RuleSelector.ANN00"></a>

ANN00.

---


##### `ANN001` <a name="ANN001" id="projen.python.RuleSelector.ANN001"></a>

ANN001.

---


##### `ANN002` <a name="ANN002" id="projen.python.RuleSelector.ANN002"></a>

ANN002.

---


##### `ANN003` <a name="ANN003" id="projen.python.RuleSelector.ANN003"></a>

ANN003.

---


##### `ANN2` <a name="ANN2" id="projen.python.RuleSelector.ANN2"></a>

ANN2.

---


##### `ANN20` <a name="ANN20" id="projen.python.RuleSelector.ANN20"></a>

ANN20.

---


##### `ANN201` <a name="ANN201" id="projen.python.RuleSelector.ANN201"></a>

ANN201.

---


##### `ANN202` <a name="ANN202" id="projen.python.RuleSelector.ANN202"></a>

ANN202.

---


##### `ANN204` <a name="ANN204" id="projen.python.RuleSelector.ANN204"></a>

ANN204.

---


##### `ANN205` <a name="ANN205" id="projen.python.RuleSelector.ANN205"></a>

ANN205.

---


##### `ANN206` <a name="ANN206" id="projen.python.RuleSelector.ANN206"></a>

ANN206.

---


##### `ANN4` <a name="ANN4" id="projen.python.RuleSelector.ANN4"></a>

ANN4.

---


##### `ANN40` <a name="ANN40" id="projen.python.RuleSelector.ANN40"></a>

ANN40.

---


##### `ANN401` <a name="ANN401" id="projen.python.RuleSelector.ANN401"></a>

ANN401.

---


##### `ARG` <a name="ARG" id="projen.python.RuleSelector.ARG"></a>

ARG.

---


##### `ARG0` <a name="ARG0" id="projen.python.RuleSelector.ARG0"></a>

ARG0.

---


##### `ARG00` <a name="ARG00" id="projen.python.RuleSelector.ARG00"></a>

ARG00.

---


##### `ARG001` <a name="ARG001" id="projen.python.RuleSelector.ARG001"></a>

ARG001.

---


##### `ARG002` <a name="ARG002" id="projen.python.RuleSelector.ARG002"></a>

ARG002.

---


##### `ARG003` <a name="ARG003" id="projen.python.RuleSelector.ARG003"></a>

ARG003.

---


##### `ARG004` <a name="ARG004" id="projen.python.RuleSelector.ARG004"></a>

ARG004.

---


##### `ARG005` <a name="ARG005" id="projen.python.RuleSelector.ARG005"></a>

ARG005.

---


##### `ASYNC` <a name="ASYNC" id="projen.python.RuleSelector.ASYNC"></a>

ASYNC.

---


##### `ASYNC1` <a name="ASYNC1" id="projen.python.RuleSelector.ASYNC1"></a>

ASYNC1.

---


##### `ASYNC10` <a name="ASYNC10" id="projen.python.RuleSelector.ASYNC10"></a>

ASYNC10.

---


##### `ASYNC100` <a name="ASYNC100" id="projen.python.RuleSelector.ASYNC100"></a>

ASYNC100.

---


##### `ASYNC105` <a name="ASYNC105" id="projen.python.RuleSelector.ASYNC105"></a>

ASYNC105.

---


##### `ASYNC109` <a name="ASYNC109" id="projen.python.RuleSelector.ASYNC109"></a>

ASYNC109.

---


##### `ASYNC11` <a name="ASYNC11" id="projen.python.RuleSelector.ASYNC11"></a>

ASYNC11.

---


##### `ASYNC110` <a name="ASYNC110" id="projen.python.RuleSelector.ASYNC110"></a>

ASYNC110.

---


##### `ASYNC115` <a name="ASYNC115" id="projen.python.RuleSelector.ASYNC115"></a>

ASYNC115.

---


##### `ASYNC116` <a name="ASYNC116" id="projen.python.RuleSelector.ASYNC116"></a>

ASYNC116.

---


##### `ASYNC2` <a name="ASYNC2" id="projen.python.RuleSelector.ASYNC2"></a>

ASYNC2.

---


##### `ASYNC21` <a name="ASYNC21" id="projen.python.RuleSelector.ASYNC21"></a>

ASYNC21.

---


##### `ASYNC210` <a name="ASYNC210" id="projen.python.RuleSelector.ASYNC210"></a>

ASYNC210.

---


##### `ASYNC212` <a name="ASYNC212" id="projen.python.RuleSelector.ASYNC212"></a>

ASYNC212.

---


##### `ASYNC22` <a name="ASYNC22" id="projen.python.RuleSelector.ASYNC22"></a>

ASYNC22.

---


##### `ASYNC220` <a name="ASYNC220" id="projen.python.RuleSelector.ASYNC220"></a>

ASYNC220.

---


##### `ASYNC221` <a name="ASYNC221" id="projen.python.RuleSelector.ASYNC221"></a>

ASYNC221.

---


##### `ASYNC222` <a name="ASYNC222" id="projen.python.RuleSelector.ASYNC222"></a>

ASYNC222.

---


##### `ASYNC23` <a name="ASYNC23" id="projen.python.RuleSelector.ASYNC23"></a>

ASYNC23.

---


##### `ASYNC230` <a name="ASYNC230" id="projen.python.RuleSelector.ASYNC230"></a>

ASYNC230.

---


##### `ASYNC24` <a name="ASYNC24" id="projen.python.RuleSelector.ASYNC24"></a>

ASYNC24.

---


##### `ASYNC240` <a name="ASYNC240" id="projen.python.RuleSelector.ASYNC240"></a>

ASYNC240.

---


##### `ASYNC25` <a name="ASYNC25" id="projen.python.RuleSelector.ASYNC25"></a>

ASYNC25.

---


##### `ASYNC250` <a name="ASYNC250" id="projen.python.RuleSelector.ASYNC250"></a>

ASYNC250.

---


##### `ASYNC251` <a name="ASYNC251" id="projen.python.RuleSelector.ASYNC251"></a>

ASYNC251.

---


##### `B` <a name="B" id="projen.python.RuleSelector.B"></a>

B.

---


##### `B0` <a name="B0" id="projen.python.RuleSelector.B0"></a>

B0.

---


##### `B00` <a name="B00" id="projen.python.RuleSelector.B00"></a>

B00.

---


##### `B002` <a name="B002" id="projen.python.RuleSelector.B002"></a>

B002.

---


##### `B003` <a name="B003" id="projen.python.RuleSelector.B003"></a>

B003.

---


##### `B004` <a name="B004" id="projen.python.RuleSelector.B004"></a>

B004.

---


##### `B005` <a name="B005" id="projen.python.RuleSelector.B005"></a>

B005.

---


##### `B006` <a name="B006" id="projen.python.RuleSelector.B006"></a>

B006.

---


##### `B007` <a name="B007" id="projen.python.RuleSelector.B007"></a>

B007.

---


##### `B008` <a name="B008" id="projen.python.RuleSelector.B008"></a>

B008.

---


##### `B009` <a name="B009" id="projen.python.RuleSelector.B009"></a>

B009.

---


##### `B01` <a name="B01" id="projen.python.RuleSelector.B01"></a>

B01.

---


##### `B010` <a name="B010" id="projen.python.RuleSelector.B010"></a>

B010.

---


##### `B011` <a name="B011" id="projen.python.RuleSelector.B011"></a>

B011.

---


##### `B012` <a name="B012" id="projen.python.RuleSelector.B012"></a>

B012.

---


##### `B013` <a name="B013" id="projen.python.RuleSelector.B013"></a>

B013.

---


##### `B014` <a name="B014" id="projen.python.RuleSelector.B014"></a>

B014.

---


##### `B015` <a name="B015" id="projen.python.RuleSelector.B015"></a>

B015.

---


##### `B016` <a name="B016" id="projen.python.RuleSelector.B016"></a>

B016.

---


##### `B017` <a name="B017" id="projen.python.RuleSelector.B017"></a>

B017.

---


##### `B018` <a name="B018" id="projen.python.RuleSelector.B018"></a>

B018.

---


##### `B019` <a name="B019" id="projen.python.RuleSelector.B019"></a>

B019.

---


##### `B02` <a name="B02" id="projen.python.RuleSelector.B02"></a>

B02.

---


##### `B020` <a name="B020" id="projen.python.RuleSelector.B020"></a>

B020.

---


##### `B021` <a name="B021" id="projen.python.RuleSelector.B021"></a>

B021.

---


##### `B022` <a name="B022" id="projen.python.RuleSelector.B022"></a>

B022.

---


##### `B023` <a name="B023" id="projen.python.RuleSelector.B023"></a>

B023.

---


##### `B024` <a name="B024" id="projen.python.RuleSelector.B024"></a>

B024.

---


##### `B025` <a name="B025" id="projen.python.RuleSelector.B025"></a>

B025.

---


##### `B026` <a name="B026" id="projen.python.RuleSelector.B026"></a>

B026.

---


##### `B027` <a name="B027" id="projen.python.RuleSelector.B027"></a>

B027.

---


##### `B028` <a name="B028" id="projen.python.RuleSelector.B028"></a>

B028.

---


##### `B029` <a name="B029" id="projen.python.RuleSelector.B029"></a>

B029.

---


##### `B03` <a name="B03" id="projen.python.RuleSelector.B03"></a>

B03.

---


##### `B030` <a name="B030" id="projen.python.RuleSelector.B030"></a>

B030.

---


##### `B031` <a name="B031" id="projen.python.RuleSelector.B031"></a>

B031.

---


##### `B032` <a name="B032" id="projen.python.RuleSelector.B032"></a>

B032.

---


##### `B033` <a name="B033" id="projen.python.RuleSelector.B033"></a>

B033.

---


##### `B034` <a name="B034" id="projen.python.RuleSelector.B034"></a>

B034.

---


##### `B035` <a name="B035" id="projen.python.RuleSelector.B035"></a>

B035.

---


##### `B039` <a name="B039" id="projen.python.RuleSelector.B039"></a>

B039.

---


##### `B9` <a name="B9" id="projen.python.RuleSelector.B9"></a>

B9.

---


##### `B90` <a name="B90" id="projen.python.RuleSelector.B90"></a>

B90.

---


##### `B901` <a name="B901" id="projen.python.RuleSelector.B901"></a>

B901.

---


##### `B903` <a name="B903" id="projen.python.RuleSelector.B903"></a>

B903.

---


##### `B904` <a name="B904" id="projen.python.RuleSelector.B904"></a>

B904.

---


##### `B905` <a name="B905" id="projen.python.RuleSelector.B905"></a>

B905.

---


##### `B909` <a name="B909" id="projen.python.RuleSelector.B909"></a>

B909.

---


##### `B91` <a name="B91" id="projen.python.RuleSelector.B91"></a>

B91.

---


##### `B911` <a name="B911" id="projen.python.RuleSelector.B911"></a>

B911.

---


##### `B912` <a name="B912" id="projen.python.RuleSelector.B912"></a>

B912.

---


##### `BLE` <a name="BLE" id="projen.python.RuleSelector.BLE"></a>

BLE.

---


##### `BLE0` <a name="BLE0" id="projen.python.RuleSelector.BLE0"></a>

BLE0.

---


##### `BLE00` <a name="BLE00" id="projen.python.RuleSelector.BLE00"></a>

BLE00.

---


##### `BLE001` <a name="BLE001" id="projen.python.RuleSelector.BLE001"></a>

BLE001.

---


##### `C` <a name="C" id="projen.python.RuleSelector.C"></a>

C.

---


##### `C4` <a name="C4" id="projen.python.RuleSelector.C4"></a>

C4.

---


##### `C40` <a name="C40" id="projen.python.RuleSelector.C40"></a>

C40.

---


##### `C400` <a name="C400" id="projen.python.RuleSelector.C400"></a>

C400.

---


##### `C401` <a name="C401" id="projen.python.RuleSelector.C401"></a>

C401.

---


##### `C402` <a name="C402" id="projen.python.RuleSelector.C402"></a>

C402.

---


##### `C403` <a name="C403" id="projen.python.RuleSelector.C403"></a>

C403.

---


##### `C404` <a name="C404" id="projen.python.RuleSelector.C404"></a>

C404.

---


##### `C405` <a name="C405" id="projen.python.RuleSelector.C405"></a>

C405.

---


##### `C406` <a name="C406" id="projen.python.RuleSelector.C406"></a>

C406.

---


##### `C408` <a name="C408" id="projen.python.RuleSelector.C408"></a>

C408.

---


##### `C409` <a name="C409" id="projen.python.RuleSelector.C409"></a>

C409.

---


##### `C41` <a name="C41" id="projen.python.RuleSelector.C41"></a>

C41.

---


##### `C410` <a name="C410" id="projen.python.RuleSelector.C410"></a>

C410.

---


##### `C411` <a name="C411" id="projen.python.RuleSelector.C411"></a>

C411.

---


##### `C413` <a name="C413" id="projen.python.RuleSelector.C413"></a>

C413.

---


##### `C414` <a name="C414" id="projen.python.RuleSelector.C414"></a>

C414.

---


##### `C415` <a name="C415" id="projen.python.RuleSelector.C415"></a>

C415.

---


##### `C416` <a name="C416" id="projen.python.RuleSelector.C416"></a>

C416.

---


##### `C417` <a name="C417" id="projen.python.RuleSelector.C417"></a>

C417.

---


##### `C418` <a name="C418" id="projen.python.RuleSelector.C418"></a>

C418.

---


##### `C419` <a name="C419" id="projen.python.RuleSelector.C419"></a>

C419.

---


##### `C42` <a name="C42" id="projen.python.RuleSelector.C42"></a>

C42.

---


##### `C420` <a name="C420" id="projen.python.RuleSelector.C420"></a>

C420.

---


##### `C9` <a name="C9" id="projen.python.RuleSelector.C9"></a>

C9.

---


##### `C90` <a name="C90" id="projen.python.RuleSelector.C90"></a>

C90.

---


##### `C901` <a name="C901" id="projen.python.RuleSelector.C901"></a>

C901.

---


##### `COM` <a name="COM" id="projen.python.RuleSelector.COM"></a>

COM.

---


##### `COM8` <a name="COM8" id="projen.python.RuleSelector.COM8"></a>

COM8.

---


##### `COM81` <a name="COM81" id="projen.python.RuleSelector.COM81"></a>

COM81.

---


##### `COM812` <a name="COM812" id="projen.python.RuleSelector.COM812"></a>

COM812.

---


##### `COM818` <a name="COM818" id="projen.python.RuleSelector.COM818"></a>

COM818.

---


##### `COM819` <a name="COM819" id="projen.python.RuleSelector.COM819"></a>

COM819.

---


##### `CPY` <a name="CPY" id="projen.python.RuleSelector.CPY"></a>

CPY.

---


##### `CPY0` <a name="CPY0" id="projen.python.RuleSelector.CPY0"></a>

CPY0.

---


##### `CPY00` <a name="CPY00" id="projen.python.RuleSelector.CPY00"></a>

CPY00.

---


##### `CPY001` <a name="CPY001" id="projen.python.RuleSelector.CPY001"></a>

CPY001.

---


##### `D` <a name="D" id="projen.python.RuleSelector.D"></a>

D.

---


##### `D1` <a name="D1" id="projen.python.RuleSelector.D1"></a>

D1.

---


##### `D10` <a name="D10" id="projen.python.RuleSelector.D10"></a>

D10.

---


##### `D100` <a name="D100" id="projen.python.RuleSelector.D100"></a>

D100.

---


##### `D101` <a name="D101" id="projen.python.RuleSelector.D101"></a>

D101.

---


##### `D102` <a name="D102" id="projen.python.RuleSelector.D102"></a>

D102.

---


##### `D103` <a name="D103" id="projen.python.RuleSelector.D103"></a>

D103.

---


##### `D104` <a name="D104" id="projen.python.RuleSelector.D104"></a>

D104.

---


##### `D105` <a name="D105" id="projen.python.RuleSelector.D105"></a>

D105.

---


##### `D106` <a name="D106" id="projen.python.RuleSelector.D106"></a>

D106.

---


##### `D107` <a name="D107" id="projen.python.RuleSelector.D107"></a>

D107.

---


##### `D2` <a name="D2" id="projen.python.RuleSelector.D2"></a>

D2.

---


##### `D20` <a name="D20" id="projen.python.RuleSelector.D20"></a>

D20.

---


##### `D200` <a name="D200" id="projen.python.RuleSelector.D200"></a>

D200.

---


##### `D201` <a name="D201" id="projen.python.RuleSelector.D201"></a>

D201.

---


##### `D202` <a name="D202" id="projen.python.RuleSelector.D202"></a>

D202.

---


##### `D203` <a name="D203" id="projen.python.RuleSelector.D203"></a>

D203.

---


##### `D204` <a name="D204" id="projen.python.RuleSelector.D204"></a>

D204.

---


##### `D205` <a name="D205" id="projen.python.RuleSelector.D205"></a>

D205.

---


##### `D206` <a name="D206" id="projen.python.RuleSelector.D206"></a>

D206.

---


##### `D207` <a name="D207" id="projen.python.RuleSelector.D207"></a>

D207.

---


##### `D208` <a name="D208" id="projen.python.RuleSelector.D208"></a>

D208.

---


##### `D209` <a name="D209" id="projen.python.RuleSelector.D209"></a>

D209.

---


##### `D21` <a name="D21" id="projen.python.RuleSelector.D21"></a>

D21.

---


##### `D210` <a name="D210" id="projen.python.RuleSelector.D210"></a>

D210.

---


##### `D211` <a name="D211" id="projen.python.RuleSelector.D211"></a>

D211.

---


##### `D212` <a name="D212" id="projen.python.RuleSelector.D212"></a>

D212.

---


##### `D213` <a name="D213" id="projen.python.RuleSelector.D213"></a>

D213.

---


##### `D214` <a name="D214" id="projen.python.RuleSelector.D214"></a>

D214.

---


##### `D215` <a name="D215" id="projen.python.RuleSelector.D215"></a>

D215.

---


##### `D3` <a name="D3" id="projen.python.RuleSelector.D3"></a>

D3.

---


##### `D30` <a name="D30" id="projen.python.RuleSelector.D30"></a>

D30.

---


##### `D300` <a name="D300" id="projen.python.RuleSelector.D300"></a>

D300.

---


##### `D301` <a name="D301" id="projen.python.RuleSelector.D301"></a>

D301.

---


##### `D4` <a name="D4" id="projen.python.RuleSelector.D4"></a>

D4.

---


##### `D40` <a name="D40" id="projen.python.RuleSelector.D40"></a>

D40.

---


##### `D400` <a name="D400" id="projen.python.RuleSelector.D400"></a>

D400.

---


##### `D401` <a name="D401" id="projen.python.RuleSelector.D401"></a>

D401.

---


##### `D402` <a name="D402" id="projen.python.RuleSelector.D402"></a>

D402.

---


##### `D403` <a name="D403" id="projen.python.RuleSelector.D403"></a>

D403.

---


##### `D404` <a name="D404" id="projen.python.RuleSelector.D404"></a>

D404.

---


##### `D405` <a name="D405" id="projen.python.RuleSelector.D405"></a>

D405.

---


##### `D406` <a name="D406" id="projen.python.RuleSelector.D406"></a>

D406.

---


##### `D407` <a name="D407" id="projen.python.RuleSelector.D407"></a>

D407.

---


##### `D408` <a name="D408" id="projen.python.RuleSelector.D408"></a>

D408.

---


##### `D409` <a name="D409" id="projen.python.RuleSelector.D409"></a>

D409.

---


##### `D41` <a name="D41" id="projen.python.RuleSelector.D41"></a>

D41.

---


##### `D410` <a name="D410" id="projen.python.RuleSelector.D410"></a>

D410.

---


##### `D411` <a name="D411" id="projen.python.RuleSelector.D411"></a>

D411.

---


##### `D412` <a name="D412" id="projen.python.RuleSelector.D412"></a>

D412.

---


##### `D413` <a name="D413" id="projen.python.RuleSelector.D413"></a>

D413.

---


##### `D414` <a name="D414" id="projen.python.RuleSelector.D414"></a>

D414.

---


##### `D415` <a name="D415" id="projen.python.RuleSelector.D415"></a>

D415.

---


##### `D416` <a name="D416" id="projen.python.RuleSelector.D416"></a>

D416.

---


##### `D417` <a name="D417" id="projen.python.RuleSelector.D417"></a>

D417.

---


##### `D418` <a name="D418" id="projen.python.RuleSelector.D418"></a>

D418.

---


##### `D419` <a name="D419" id="projen.python.RuleSelector.D419"></a>

D419.

---


##### `DJ` <a name="DJ" id="projen.python.RuleSelector.DJ"></a>

DJ.

---


##### `DJ0` <a name="DJ0" id="projen.python.RuleSelector.DJ0"></a>

DJ0.

---


##### `DJ00` <a name="DJ00" id="projen.python.RuleSelector.DJ00"></a>

DJ00.

---


##### `DJ001` <a name="DJ001" id="projen.python.RuleSelector.DJ001"></a>

DJ001.

---


##### `DJ003` <a name="DJ003" id="projen.python.RuleSelector.DJ003"></a>

DJ003.

---


##### `DJ006` <a name="DJ006" id="projen.python.RuleSelector.DJ006"></a>

DJ006.

---


##### `DJ007` <a name="DJ007" id="projen.python.RuleSelector.DJ007"></a>

DJ007.

---


##### `DJ008` <a name="DJ008" id="projen.python.RuleSelector.DJ008"></a>

DJ008.

---


##### `DJ01` <a name="DJ01" id="projen.python.RuleSelector.DJ01"></a>

DJ01.

---


##### `DJ012` <a name="DJ012" id="projen.python.RuleSelector.DJ012"></a>

DJ012.

---


##### `DJ013` <a name="DJ013" id="projen.python.RuleSelector.DJ013"></a>

DJ013.

---


##### `DOC` <a name="DOC" id="projen.python.RuleSelector.DOC"></a>

DOC.

---


##### `DOC1` <a name="DOC1" id="projen.python.RuleSelector.DOC1"></a>

DOC1.

---


##### `DOC10` <a name="DOC10" id="projen.python.RuleSelector.DOC10"></a>

DOC10.

---


##### `DOC102` <a name="DOC102" id="projen.python.RuleSelector.DOC102"></a>

DOC102.

---


##### `DOC2` <a name="DOC2" id="projen.python.RuleSelector.DOC2"></a>

DOC2.

---


##### `DOC20` <a name="DOC20" id="projen.python.RuleSelector.DOC20"></a>

DOC20.

---


##### `DOC201` <a name="DOC201" id="projen.python.RuleSelector.DOC201"></a>

DOC201.

---


##### `DOC202` <a name="DOC202" id="projen.python.RuleSelector.DOC202"></a>

DOC202.

---


##### `DOC4` <a name="DOC4" id="projen.python.RuleSelector.DOC4"></a>

DOC4.

---


##### `DOC40` <a name="DOC40" id="projen.python.RuleSelector.DOC40"></a>

DOC40.

---


##### `DOC402` <a name="DOC402" id="projen.python.RuleSelector.DOC402"></a>

DOC402.

---


##### `DOC403` <a name="DOC403" id="projen.python.RuleSelector.DOC403"></a>

DOC403.

---


##### `DOC5` <a name="DOC5" id="projen.python.RuleSelector.DOC5"></a>

DOC5.

---


##### `DOC50` <a name="DOC50" id="projen.python.RuleSelector.DOC50"></a>

DOC50.

---


##### `DOC501` <a name="DOC501" id="projen.python.RuleSelector.DOC501"></a>

DOC501.

---


##### `DOC502` <a name="DOC502" id="projen.python.RuleSelector.DOC502"></a>

DOC502.

---


##### `DTZ` <a name="DTZ" id="projen.python.RuleSelector.DTZ"></a>

DTZ.

---


##### `DTZ0` <a name="DTZ0" id="projen.python.RuleSelector.DTZ0"></a>

DTZ0.

---


##### `DTZ00` <a name="DTZ00" id="projen.python.RuleSelector.DTZ00"></a>

DTZ00.

---


##### `DTZ001` <a name="DTZ001" id="projen.python.RuleSelector.DTZ001"></a>

DTZ001.

---


##### `DTZ002` <a name="DTZ002" id="projen.python.RuleSelector.DTZ002"></a>

DTZ002.

---


##### `DTZ003` <a name="DTZ003" id="projen.python.RuleSelector.DTZ003"></a>

DTZ003.

---


##### `DTZ004` <a name="DTZ004" id="projen.python.RuleSelector.DTZ004"></a>

DTZ004.

---


##### `DTZ005` <a name="DTZ005" id="projen.python.RuleSelector.DTZ005"></a>

DTZ005.

---


##### `DTZ006` <a name="DTZ006" id="projen.python.RuleSelector.DTZ006"></a>

DTZ006.

---


##### `DTZ007` <a name="DTZ007" id="projen.python.RuleSelector.DTZ007"></a>

DTZ007.

---


##### `DTZ01` <a name="DTZ01" id="projen.python.RuleSelector.DTZ01"></a>

DTZ01.

---


##### `DTZ011` <a name="DTZ011" id="projen.python.RuleSelector.DTZ011"></a>

DTZ011.

---


##### `DTZ012` <a name="DTZ012" id="projen.python.RuleSelector.DTZ012"></a>

DTZ012.

---


##### `DTZ9` <a name="DTZ9" id="projen.python.RuleSelector.DTZ9"></a>

DTZ9.

---


##### `DTZ90` <a name="DTZ90" id="projen.python.RuleSelector.DTZ90"></a>

DTZ90.

---


##### `DTZ901` <a name="DTZ901" id="projen.python.RuleSelector.DTZ901"></a>

DTZ901.

---


##### `E` <a name="E" id="projen.python.RuleSelector.E"></a>

E.

---


##### `E1` <a name="E1" id="projen.python.RuleSelector.E1"></a>

E1.

---


##### `E10` <a name="E10" id="projen.python.RuleSelector.E10"></a>

E10.

---


##### `E101` <a name="E101" id="projen.python.RuleSelector.E101"></a>

E101.

---


##### `E11` <a name="E11" id="projen.python.RuleSelector.E11"></a>

E11.

---


##### `E111` <a name="E111" id="projen.python.RuleSelector.E111"></a>

E111.

---


##### `E112` <a name="E112" id="projen.python.RuleSelector.E112"></a>

E112.

---


##### `E113` <a name="E113" id="projen.python.RuleSelector.E113"></a>

E113.

---


##### `E114` <a name="E114" id="projen.python.RuleSelector.E114"></a>

E114.

---


##### `E115` <a name="E115" id="projen.python.RuleSelector.E115"></a>

E115.

---


##### `E116` <a name="E116" id="projen.python.RuleSelector.E116"></a>

E116.

---


##### `E117` <a name="E117" id="projen.python.RuleSelector.E117"></a>

E117.

---


##### `E2` <a name="E2" id="projen.python.RuleSelector.E2"></a>

E2.

---


##### `E20` <a name="E20" id="projen.python.RuleSelector.E20"></a>

E20.

---


##### `E201` <a name="E201" id="projen.python.RuleSelector.E201"></a>

E201.

---


##### `E202` <a name="E202" id="projen.python.RuleSelector.E202"></a>

E202.

---


##### `E203` <a name="E203" id="projen.python.RuleSelector.E203"></a>

E203.

---


##### `E204` <a name="E204" id="projen.python.RuleSelector.E204"></a>

E204.

---


##### `E21` <a name="E21" id="projen.python.RuleSelector.E21"></a>

E21.

---


##### `E211` <a name="E211" id="projen.python.RuleSelector.E211"></a>

E211.

---


##### `E22` <a name="E22" id="projen.python.RuleSelector.E22"></a>

E22.

---


##### `E221` <a name="E221" id="projen.python.RuleSelector.E221"></a>

E221.

---


##### `E222` <a name="E222" id="projen.python.RuleSelector.E222"></a>

E222.

---


##### `E223` <a name="E223" id="projen.python.RuleSelector.E223"></a>

E223.

---


##### `E224` <a name="E224" id="projen.python.RuleSelector.E224"></a>

E224.

---


##### `E225` <a name="E225" id="projen.python.RuleSelector.E225"></a>

E225.

---


##### `E226` <a name="E226" id="projen.python.RuleSelector.E226"></a>

E226.

---


##### `E227` <a name="E227" id="projen.python.RuleSelector.E227"></a>

E227.

---


##### `E228` <a name="E228" id="projen.python.RuleSelector.E228"></a>

E228.

---


##### `E23` <a name="E23" id="projen.python.RuleSelector.E23"></a>

E23.

---


##### `E231` <a name="E231" id="projen.python.RuleSelector.E231"></a>

E231.

---


##### `E24` <a name="E24" id="projen.python.RuleSelector.E24"></a>

E24.

---


##### `E241` <a name="E241" id="projen.python.RuleSelector.E241"></a>

E241.

---


##### `E242` <a name="E242" id="projen.python.RuleSelector.E242"></a>

E242.

---


##### `E25` <a name="E25" id="projen.python.RuleSelector.E25"></a>

E25.

---


##### `E251` <a name="E251" id="projen.python.RuleSelector.E251"></a>

E251.

---


##### `E252` <a name="E252" id="projen.python.RuleSelector.E252"></a>

E252.

---


##### `E26` <a name="E26" id="projen.python.RuleSelector.E26"></a>

E26.

---


##### `E261` <a name="E261" id="projen.python.RuleSelector.E261"></a>

E261.

---


##### `E262` <a name="E262" id="projen.python.RuleSelector.E262"></a>

E262.

---


##### `E265` <a name="E265" id="projen.python.RuleSelector.E265"></a>

E265.

---


##### `E266` <a name="E266" id="projen.python.RuleSelector.E266"></a>

E266.

---


##### `E27` <a name="E27" id="projen.python.RuleSelector.E27"></a>

E27.

---


##### `E271` <a name="E271" id="projen.python.RuleSelector.E271"></a>

E271.

---


##### `E272` <a name="E272" id="projen.python.RuleSelector.E272"></a>

E272.

---


##### `E273` <a name="E273" id="projen.python.RuleSelector.E273"></a>

E273.

---


##### `E274` <a name="E274" id="projen.python.RuleSelector.E274"></a>

E274.

---


##### `E275` <a name="E275" id="projen.python.RuleSelector.E275"></a>

E275.

---


##### `E3` <a name="E3" id="projen.python.RuleSelector.E3"></a>

E3.

---


##### `E30` <a name="E30" id="projen.python.RuleSelector.E30"></a>

E30.

---


##### `E301` <a name="E301" id="projen.python.RuleSelector.E301"></a>

E301.

---


##### `E302` <a name="E302" id="projen.python.RuleSelector.E302"></a>

E302.

---


##### `E303` <a name="E303" id="projen.python.RuleSelector.E303"></a>

E303.

---


##### `E304` <a name="E304" id="projen.python.RuleSelector.E304"></a>

E304.

---


##### `E305` <a name="E305" id="projen.python.RuleSelector.E305"></a>

E305.

---


##### `E306` <a name="E306" id="projen.python.RuleSelector.E306"></a>

E306.

---


##### `E4` <a name="E4" id="projen.python.RuleSelector.E4"></a>

E4.

---


##### `E40` <a name="E40" id="projen.python.RuleSelector.E40"></a>

E40.

---


##### `E401` <a name="E401" id="projen.python.RuleSelector.E401"></a>

E401.

---


##### `E402` <a name="E402" id="projen.python.RuleSelector.E402"></a>

E402.

---


##### `E5` <a name="E5" id="projen.python.RuleSelector.E5"></a>

E5.

---


##### `E50` <a name="E50" id="projen.python.RuleSelector.E50"></a>

E50.

---


##### `E501` <a name="E501" id="projen.python.RuleSelector.E501"></a>

E501.

---


##### `E502` <a name="E502" id="projen.python.RuleSelector.E502"></a>

E502.

---


##### `E7` <a name="E7" id="projen.python.RuleSelector.E7"></a>

E7.

---


##### `E70` <a name="E70" id="projen.python.RuleSelector.E70"></a>

E70.

---


##### `E701` <a name="E701" id="projen.python.RuleSelector.E701"></a>

E701.

---


##### `E702` <a name="E702" id="projen.python.RuleSelector.E702"></a>

E702.

---


##### `E703` <a name="E703" id="projen.python.RuleSelector.E703"></a>

E703.

---


##### `E71` <a name="E71" id="projen.python.RuleSelector.E71"></a>

E71.

---


##### `E711` <a name="E711" id="projen.python.RuleSelector.E711"></a>

E711.

---


##### `E712` <a name="E712" id="projen.python.RuleSelector.E712"></a>

E712.

---


##### `E713` <a name="E713" id="projen.python.RuleSelector.E713"></a>

E713.

---


##### `E714` <a name="E714" id="projen.python.RuleSelector.E714"></a>

E714.

---


##### `E72` <a name="E72" id="projen.python.RuleSelector.E72"></a>

E72.

---


##### `E721` <a name="E721" id="projen.python.RuleSelector.E721"></a>

E721.

---


##### `E722` <a name="E722" id="projen.python.RuleSelector.E722"></a>

E722.

---


##### `E73` <a name="E73" id="projen.python.RuleSelector.E73"></a>

E73.

---


##### `E731` <a name="E731" id="projen.python.RuleSelector.E731"></a>

E731.

---


##### `E74` <a name="E74" id="projen.python.RuleSelector.E74"></a>

E74.

---


##### `E741` <a name="E741" id="projen.python.RuleSelector.E741"></a>

E741.

---


##### `E742` <a name="E742" id="projen.python.RuleSelector.E742"></a>

E742.

---


##### `E743` <a name="E743" id="projen.python.RuleSelector.E743"></a>

E743.

---


##### `E9` <a name="E9" id="projen.python.RuleSelector.E9"></a>

E9.

---


##### `E90` <a name="E90" id="projen.python.RuleSelector.E90"></a>

E90.

---


##### `E902` <a name="E902" id="projen.python.RuleSelector.E902"></a>

E902.

---


##### `EM` <a name="EM" id="projen.python.RuleSelector.EM"></a>

EM.

---


##### `EM1` <a name="EM1" id="projen.python.RuleSelector.EM1"></a>

EM1.

---


##### `EM10` <a name="EM10" id="projen.python.RuleSelector.EM10"></a>

EM10.

---


##### `EM101` <a name="EM101" id="projen.python.RuleSelector.EM101"></a>

EM101.

---


##### `EM102` <a name="EM102" id="projen.python.RuleSelector.EM102"></a>

EM102.

---


##### `EM103` <a name="EM103" id="projen.python.RuleSelector.EM103"></a>

EM103.

---


##### `ERA` <a name="ERA" id="projen.python.RuleSelector.ERA"></a>

ERA.

---


##### `ERA0` <a name="ERA0" id="projen.python.RuleSelector.ERA0"></a>

ERA0.

---


##### `ERA00` <a name="ERA00" id="projen.python.RuleSelector.ERA00"></a>

ERA00.

---


##### `ERA001` <a name="ERA001" id="projen.python.RuleSelector.ERA001"></a>

ERA001.

---


##### `EXE` <a name="EXE" id="projen.python.RuleSelector.EXE"></a>

EXE.

---


##### `EXE0` <a name="EXE0" id="projen.python.RuleSelector.EXE0"></a>

EXE0.

---


##### `EXE00` <a name="EXE00" id="projen.python.RuleSelector.EXE00"></a>

EXE00.

---


##### `EXE001` <a name="EXE001" id="projen.python.RuleSelector.EXE001"></a>

EXE001.

---


##### `EXE002` <a name="EXE002" id="projen.python.RuleSelector.EXE002"></a>

EXE002.

---


##### `EXE003` <a name="EXE003" id="projen.python.RuleSelector.EXE003"></a>

EXE003.

---


##### `EXE004` <a name="EXE004" id="projen.python.RuleSelector.EXE004"></a>

EXE004.

---


##### `EXE005` <a name="EXE005" id="projen.python.RuleSelector.EXE005"></a>

EXE005.

---


##### `F` <a name="F" id="projen.python.RuleSelector.F"></a>

F.

---


##### `F4` <a name="F4" id="projen.python.RuleSelector.F4"></a>

F4.

---


##### `F40` <a name="F40" id="projen.python.RuleSelector.F40"></a>

F40.

---


##### `F401` <a name="F401" id="projen.python.RuleSelector.F401"></a>

F401.

---


##### `F402` <a name="F402" id="projen.python.RuleSelector.F402"></a>

F402.

---


##### `F403` <a name="F403" id="projen.python.RuleSelector.F403"></a>

F403.

---


##### `F404` <a name="F404" id="projen.python.RuleSelector.F404"></a>

F404.

---


##### `F405` <a name="F405" id="projen.python.RuleSelector.F405"></a>

F405.

---


##### `F406` <a name="F406" id="projen.python.RuleSelector.F406"></a>

F406.

---


##### `F407` <a name="F407" id="projen.python.RuleSelector.F407"></a>

F407.

---


##### `F5` <a name="F5" id="projen.python.RuleSelector.F5"></a>

F5.

---


##### `F50` <a name="F50" id="projen.python.RuleSelector.F50"></a>

F50.

---


##### `F501` <a name="F501" id="projen.python.RuleSelector.F501"></a>

F501.

---


##### `F502` <a name="F502" id="projen.python.RuleSelector.F502"></a>

F502.

---


##### `F503` <a name="F503" id="projen.python.RuleSelector.F503"></a>

F503.

---


##### `F504` <a name="F504" id="projen.python.RuleSelector.F504"></a>

F504.

---


##### `F505` <a name="F505" id="projen.python.RuleSelector.F505"></a>

F505.

---


##### `F506` <a name="F506" id="projen.python.RuleSelector.F506"></a>

F506.

---


##### `F507` <a name="F507" id="projen.python.RuleSelector.F507"></a>

F507.

---


##### `F508` <a name="F508" id="projen.python.RuleSelector.F508"></a>

F508.

---


##### `F509` <a name="F509" id="projen.python.RuleSelector.F509"></a>

F509.

---


##### `F52` <a name="F52" id="projen.python.RuleSelector.F52"></a>

F52.

---


##### `F521` <a name="F521" id="projen.python.RuleSelector.F521"></a>

F521.

---


##### `F522` <a name="F522" id="projen.python.RuleSelector.F522"></a>

F522.

---


##### `F523` <a name="F523" id="projen.python.RuleSelector.F523"></a>

F523.

---


##### `F524` <a name="F524" id="projen.python.RuleSelector.F524"></a>

F524.

---


##### `F525` <a name="F525" id="projen.python.RuleSelector.F525"></a>

F525.

---


##### `F54` <a name="F54" id="projen.python.RuleSelector.F54"></a>

F54.

---


##### `F541` <a name="F541" id="projen.python.RuleSelector.F541"></a>

F541.

---


##### `F6` <a name="F6" id="projen.python.RuleSelector.F6"></a>

F6.

---


##### `F60` <a name="F60" id="projen.python.RuleSelector.F60"></a>

F60.

---


##### `F601` <a name="F601" id="projen.python.RuleSelector.F601"></a>

F601.

---


##### `F602` <a name="F602" id="projen.python.RuleSelector.F602"></a>

F602.

---


##### `F62` <a name="F62" id="projen.python.RuleSelector.F62"></a>

F62.

---


##### `F621` <a name="F621" id="projen.python.RuleSelector.F621"></a>

F621.

---


##### `F622` <a name="F622" id="projen.python.RuleSelector.F622"></a>

F622.

---


##### `F63` <a name="F63" id="projen.python.RuleSelector.F63"></a>

F63.

---


##### `F631` <a name="F631" id="projen.python.RuleSelector.F631"></a>

F631.

---


##### `F632` <a name="F632" id="projen.python.RuleSelector.F632"></a>

F632.

---


##### `F633` <a name="F633" id="projen.python.RuleSelector.F633"></a>

F633.

---


##### `F634` <a name="F634" id="projen.python.RuleSelector.F634"></a>

F634.

---


##### `F7` <a name="F7" id="projen.python.RuleSelector.F7"></a>

F7.

---


##### `F70` <a name="F70" id="projen.python.RuleSelector.F70"></a>

F70.

---


##### `F701` <a name="F701" id="projen.python.RuleSelector.F701"></a>

F701.

---


##### `F702` <a name="F702" id="projen.python.RuleSelector.F702"></a>

F702.

---


##### `F704` <a name="F704" id="projen.python.RuleSelector.F704"></a>

F704.

---


##### `F706` <a name="F706" id="projen.python.RuleSelector.F706"></a>

F706.

---


##### `F707` <a name="F707" id="projen.python.RuleSelector.F707"></a>

F707.

---


##### `F72` <a name="F72" id="projen.python.RuleSelector.F72"></a>

F72.

---


##### `F722` <a name="F722" id="projen.python.RuleSelector.F722"></a>

F722.

---


##### `F8` <a name="F8" id="projen.python.RuleSelector.F8"></a>

F8.

---


##### `F81` <a name="F81" id="projen.python.RuleSelector.F81"></a>

F81.

---


##### `F811` <a name="F811" id="projen.python.RuleSelector.F811"></a>

F811.

---


##### `F82` <a name="F82" id="projen.python.RuleSelector.F82"></a>

F82.

---


##### `F821` <a name="F821" id="projen.python.RuleSelector.F821"></a>

F821.

---


##### `F822` <a name="F822" id="projen.python.RuleSelector.F822"></a>

F822.

---


##### `F823` <a name="F823" id="projen.python.RuleSelector.F823"></a>

F823.

---


##### `F84` <a name="F84" id="projen.python.RuleSelector.F84"></a>

F84.

---


##### `F841` <a name="F841" id="projen.python.RuleSelector.F841"></a>

F841.

---


##### `F842` <a name="F842" id="projen.python.RuleSelector.F842"></a>

F842.

---


##### `F9` <a name="F9" id="projen.python.RuleSelector.F9"></a>

F9.

---


##### `F90` <a name="F90" id="projen.python.RuleSelector.F90"></a>

F90.

---


##### `F901` <a name="F901" id="projen.python.RuleSelector.F901"></a>

F901.

---


##### `FA` <a name="FA" id="projen.python.RuleSelector.FA"></a>

FA.

---


##### `FA1` <a name="FA1" id="projen.python.RuleSelector.FA1"></a>

FA1.

---


##### `FA10` <a name="FA10" id="projen.python.RuleSelector.FA10"></a>

FA10.

---


##### `FA100` <a name="FA100" id="projen.python.RuleSelector.FA100"></a>

FA100.

---


##### `FA102` <a name="FA102" id="projen.python.RuleSelector.FA102"></a>

FA102.

---


##### `FAST` <a name="FAST" id="projen.python.RuleSelector.FAST"></a>

FAST.

---


##### `FAST0` <a name="FAST0" id="projen.python.RuleSelector.FAST0"></a>

FAST0.

---


##### `FAST00` <a name="FAST00" id="projen.python.RuleSelector.FAST00"></a>

FAST00.

---


##### `FAST001` <a name="FAST001" id="projen.python.RuleSelector.FAST001"></a>

FAST001.

---


##### `FAST002` <a name="FAST002" id="projen.python.RuleSelector.FAST002"></a>

FAST002.

---


##### `FAST003` <a name="FAST003" id="projen.python.RuleSelector.FAST003"></a>

FAST003.

---


##### `FBT` <a name="FBT" id="projen.python.RuleSelector.FBT"></a>

FBT.

---


##### `FBT0` <a name="FBT0" id="projen.python.RuleSelector.FBT0"></a>

FBT0.

---


##### `FBT00` <a name="FBT00" id="projen.python.RuleSelector.FBT00"></a>

FBT00.

---


##### `FBT001` <a name="FBT001" id="projen.python.RuleSelector.FBT001"></a>

FBT001.

---


##### `FBT002` <a name="FBT002" id="projen.python.RuleSelector.FBT002"></a>

FBT002.

---


##### `FBT003` <a name="FBT003" id="projen.python.RuleSelector.FBT003"></a>

FBT003.

---


##### `FIX` <a name="FIX" id="projen.python.RuleSelector.FIX"></a>

FIX.

---


##### `FIX0` <a name="FIX0" id="projen.python.RuleSelector.FIX0"></a>

FIX0.

---


##### `FIX00` <a name="FIX00" id="projen.python.RuleSelector.FIX00"></a>

FIX00.

---


##### `FIX001` <a name="FIX001" id="projen.python.RuleSelector.FIX001"></a>

FIX001.

---


##### `FIX002` <a name="FIX002" id="projen.python.RuleSelector.FIX002"></a>

FIX002.

---


##### `FIX003` <a name="FIX003" id="projen.python.RuleSelector.FIX003"></a>

FIX003.

---


##### `FIX004` <a name="FIX004" id="projen.python.RuleSelector.FIX004"></a>

FIX004.

---


##### `FLY` <a name="FLY" id="projen.python.RuleSelector.FLY"></a>

FLY.

---


##### `FLY0` <a name="FLY0" id="projen.python.RuleSelector.FLY0"></a>

FLY0.

---


##### `FLY00` <a name="FLY00" id="projen.python.RuleSelector.FLY00"></a>

FLY00.

---


##### `FLY002` <a name="FLY002" id="projen.python.RuleSelector.FLY002"></a>

FLY002.

---


##### `FURB` <a name="FURB" id="projen.python.RuleSelector.FURB"></a>

FURB.

---


##### `FURB1` <a name="FURB1" id="projen.python.RuleSelector.FURB1"></a>

FURB1.

---


##### `FURB10` <a name="FURB10" id="projen.python.RuleSelector.FURB10"></a>

FURB10.

---


##### `FURB101` <a name="FURB101" id="projen.python.RuleSelector.FURB101"></a>

FURB101.

---


##### `FURB103` <a name="FURB103" id="projen.python.RuleSelector.FURB103"></a>

FURB103.

---


##### `FURB105` <a name="FURB105" id="projen.python.RuleSelector.FURB105"></a>

FURB105.

---


##### `FURB11` <a name="FURB11" id="projen.python.RuleSelector.FURB11"></a>

FURB11.

---


##### `FURB110` <a name="FURB110" id="projen.python.RuleSelector.FURB110"></a>

FURB110.

---


##### `FURB113` <a name="FURB113" id="projen.python.RuleSelector.FURB113"></a>

FURB113.

---


##### `FURB116` <a name="FURB116" id="projen.python.RuleSelector.FURB116"></a>

FURB116.

---


##### `FURB118` <a name="FURB118" id="projen.python.RuleSelector.FURB118"></a>

FURB118.

---


##### `FURB12` <a name="FURB12" id="projen.python.RuleSelector.FURB12"></a>

FURB12.

---


##### `FURB122` <a name="FURB122" id="projen.python.RuleSelector.FURB122"></a>

FURB122.

---


##### `FURB129` <a name="FURB129" id="projen.python.RuleSelector.FURB129"></a>

FURB129.

---


##### `FURB13` <a name="FURB13" id="projen.python.RuleSelector.FURB13"></a>

FURB13.

---


##### `FURB131` <a name="FURB131" id="projen.python.RuleSelector.FURB131"></a>

FURB131.

---


##### `FURB132` <a name="FURB132" id="projen.python.RuleSelector.FURB132"></a>

FURB132.

---


##### `FURB136` <a name="FURB136" id="projen.python.RuleSelector.FURB136"></a>

FURB136.

---


##### `FURB14` <a name="FURB14" id="projen.python.RuleSelector.FURB14"></a>

FURB14.

---


##### `FURB140` <a name="FURB140" id="projen.python.RuleSelector.FURB140"></a>

FURB140.

---


##### `FURB142` <a name="FURB142" id="projen.python.RuleSelector.FURB142"></a>

FURB142.

---


##### `FURB145` <a name="FURB145" id="projen.python.RuleSelector.FURB145"></a>

FURB145.

---


##### `FURB148` <a name="FURB148" id="projen.python.RuleSelector.FURB148"></a>

FURB148.

---


##### `FURB15` <a name="FURB15" id="projen.python.RuleSelector.FURB15"></a>

FURB15.

---


##### `FURB152` <a name="FURB152" id="projen.python.RuleSelector.FURB152"></a>

FURB152.

---


##### `FURB154` <a name="FURB154" id="projen.python.RuleSelector.FURB154"></a>

FURB154.

---


##### `FURB156` <a name="FURB156" id="projen.python.RuleSelector.FURB156"></a>

FURB156.

---


##### `FURB157` <a name="FURB157" id="projen.python.RuleSelector.FURB157"></a>

FURB157.

---


##### `FURB16` <a name="FURB16" id="projen.python.RuleSelector.FURB16"></a>

FURB16.

---


##### `FURB161` <a name="FURB161" id="projen.python.RuleSelector.FURB161"></a>

FURB161.

---


##### `FURB162` <a name="FURB162" id="projen.python.RuleSelector.FURB162"></a>

FURB162.

---


##### `FURB163` <a name="FURB163" id="projen.python.RuleSelector.FURB163"></a>

FURB163.

---


##### `FURB164` <a name="FURB164" id="projen.python.RuleSelector.FURB164"></a>

FURB164.

---


##### `FURB166` <a name="FURB166" id="projen.python.RuleSelector.FURB166"></a>

FURB166.

---


##### `FURB167` <a name="FURB167" id="projen.python.RuleSelector.FURB167"></a>

FURB167.

---


##### `FURB168` <a name="FURB168" id="projen.python.RuleSelector.FURB168"></a>

FURB168.

---


##### `FURB169` <a name="FURB169" id="projen.python.RuleSelector.FURB169"></a>

FURB169.

---


##### `FURB17` <a name="FURB17" id="projen.python.RuleSelector.FURB17"></a>

FURB17.

---


##### `FURB171` <a name="FURB171" id="projen.python.RuleSelector.FURB171"></a>

FURB171.

---


##### `FURB177` <a name="FURB177" id="projen.python.RuleSelector.FURB177"></a>

FURB177.

---


##### `FURB18` <a name="FURB18" id="projen.python.RuleSelector.FURB18"></a>

FURB18.

---


##### `FURB180` <a name="FURB180" id="projen.python.RuleSelector.FURB180"></a>

FURB180.

---


##### `FURB181` <a name="FURB181" id="projen.python.RuleSelector.FURB181"></a>

FURB181.

---


##### `FURB187` <a name="FURB187" id="projen.python.RuleSelector.FURB187"></a>

FURB187.

---


##### `FURB188` <a name="FURB188" id="projen.python.RuleSelector.FURB188"></a>

FURB188.

---


##### `FURB189` <a name="FURB189" id="projen.python.RuleSelector.FURB189"></a>

FURB189.

---


##### `FURB19` <a name="FURB19" id="projen.python.RuleSelector.FURB19"></a>

FURB19.

---


##### `FURB192` <a name="FURB192" id="projen.python.RuleSelector.FURB192"></a>

FURB192.

---


##### `G` <a name="G" id="projen.python.RuleSelector.G"></a>

G.

---


##### `G0` <a name="G0" id="projen.python.RuleSelector.G0"></a>

G0.

---


##### `G00` <a name="G00" id="projen.python.RuleSelector.G00"></a>

G00.

---


##### `G001` <a name="G001" id="projen.python.RuleSelector.G001"></a>

G001.

---


##### `G002` <a name="G002" id="projen.python.RuleSelector.G002"></a>

G002.

---


##### `G003` <a name="G003" id="projen.python.RuleSelector.G003"></a>

G003.

---


##### `G004` <a name="G004" id="projen.python.RuleSelector.G004"></a>

G004.

---


##### `G01` <a name="G01" id="projen.python.RuleSelector.G01"></a>

G01.

---


##### `G010` <a name="G010" id="projen.python.RuleSelector.G010"></a>

G010.

---


##### `G1` <a name="G1" id="projen.python.RuleSelector.G1"></a>

G1.

---


##### `G10` <a name="G10" id="projen.python.RuleSelector.G10"></a>

G10.

---


##### `G101` <a name="G101" id="projen.python.RuleSelector.G101"></a>

G101.

---


##### `G2` <a name="G2" id="projen.python.RuleSelector.G2"></a>

G2.

---


##### `G20` <a name="G20" id="projen.python.RuleSelector.G20"></a>

G20.

---


##### `G201` <a name="G201" id="projen.python.RuleSelector.G201"></a>

G201.

---


##### `G202` <a name="G202" id="projen.python.RuleSelector.G202"></a>

G202.

---


##### `I` <a name="I" id="projen.python.RuleSelector.I"></a>

I.

---


##### `I0` <a name="I0" id="projen.python.RuleSelector.I0"></a>

I0.

---


##### `I00` <a name="I00" id="projen.python.RuleSelector.I00"></a>

I00.

---


##### `I001` <a name="I001" id="projen.python.RuleSelector.I001"></a>

I001.

---


##### `I002` <a name="I002" id="projen.python.RuleSelector.I002"></a>

I002.

---


##### `ICN` <a name="ICN" id="projen.python.RuleSelector.ICN"></a>

ICN.

---


##### `ICN0` <a name="ICN0" id="projen.python.RuleSelector.ICN0"></a>

ICN0.

---


##### `ICN00` <a name="ICN00" id="projen.python.RuleSelector.ICN00"></a>

ICN00.

---


##### `ICN001` <a name="ICN001" id="projen.python.RuleSelector.ICN001"></a>

ICN001.

---


##### `ICN002` <a name="ICN002" id="projen.python.RuleSelector.ICN002"></a>

ICN002.

---


##### `ICN003` <a name="ICN003" id="projen.python.RuleSelector.ICN003"></a>

ICN003.

---


##### `INP` <a name="INP" id="projen.python.RuleSelector.INP"></a>

INP.

---


##### `INP0` <a name="INP0" id="projen.python.RuleSelector.INP0"></a>

INP0.

---


##### `INP00` <a name="INP00" id="projen.python.RuleSelector.INP00"></a>

INP00.

---


##### `INP001` <a name="INP001" id="projen.python.RuleSelector.INP001"></a>

INP001.

---


##### `INT` <a name="INT" id="projen.python.RuleSelector.INT"></a>

INT.

---


##### `INT0` <a name="INT0" id="projen.python.RuleSelector.INT0"></a>

INT0.

---


##### `INT00` <a name="INT00" id="projen.python.RuleSelector.INT00"></a>

INT00.

---


##### `INT001` <a name="INT001" id="projen.python.RuleSelector.INT001"></a>

INT001.

---


##### `INT002` <a name="INT002" id="projen.python.RuleSelector.INT002"></a>

INT002.

---


##### `INT003` <a name="INT003" id="projen.python.RuleSelector.INT003"></a>

INT003.

---


##### `ISC` <a name="ISC" id="projen.python.RuleSelector.ISC"></a>

ISC.

---


##### `ISC0` <a name="ISC0" id="projen.python.RuleSelector.ISC0"></a>

ISC0.

---


##### `ISC00` <a name="ISC00" id="projen.python.RuleSelector.ISC00"></a>

ISC00.

---


##### `ISC001` <a name="ISC001" id="projen.python.RuleSelector.ISC001"></a>

ISC001.

---


##### `ISC002` <a name="ISC002" id="projen.python.RuleSelector.ISC002"></a>

ISC002.

---


##### `ISC003` <a name="ISC003" id="projen.python.RuleSelector.ISC003"></a>

ISC003.

---


##### `LOG` <a name="LOG" id="projen.python.RuleSelector.LOG"></a>

LOG.

---


##### `LOG0` <a name="LOG0" id="projen.python.RuleSelector.LOG0"></a>

LOG0.

---


##### `LOG00` <a name="LOG00" id="projen.python.RuleSelector.LOG00"></a>

LOG00.

---


##### `LOG001` <a name="LOG001" id="projen.python.RuleSelector.LOG001"></a>

LOG001.

---


##### `LOG002` <a name="LOG002" id="projen.python.RuleSelector.LOG002"></a>

LOG002.

---


##### `LOG004` <a name="LOG004" id="projen.python.RuleSelector.LOG004"></a>

LOG004.

---


##### `LOG007` <a name="LOG007" id="projen.python.RuleSelector.LOG007"></a>

LOG007.

---


##### `LOG009` <a name="LOG009" id="projen.python.RuleSelector.LOG009"></a>

LOG009.

---


##### `LOG01` <a name="LOG01" id="projen.python.RuleSelector.LOG01"></a>

LOG01.

---


##### `LOG014` <a name="LOG014" id="projen.python.RuleSelector.LOG014"></a>

LOG014.

---


##### `LOG015` <a name="LOG015" id="projen.python.RuleSelector.LOG015"></a>

LOG015.

---


##### `N` <a name="N" id="projen.python.RuleSelector.N"></a>

N.

---


##### `N8` <a name="N8" id="projen.python.RuleSelector.N8"></a>

N8.

---


##### `N80` <a name="N80" id="projen.python.RuleSelector.N80"></a>

N80.

---


##### `N801` <a name="N801" id="projen.python.RuleSelector.N801"></a>

N801.

---


##### `N802` <a name="N802" id="projen.python.RuleSelector.N802"></a>

N802.

---


##### `N803` <a name="N803" id="projen.python.RuleSelector.N803"></a>

N803.

---


##### `N804` <a name="N804" id="projen.python.RuleSelector.N804"></a>

N804.

---


##### `N805` <a name="N805" id="projen.python.RuleSelector.N805"></a>

N805.

---


##### `N806` <a name="N806" id="projen.python.RuleSelector.N806"></a>

N806.

---


##### `N807` <a name="N807" id="projen.python.RuleSelector.N807"></a>

N807.

---


##### `N81` <a name="N81" id="projen.python.RuleSelector.N81"></a>

N81.

---


##### `N811` <a name="N811" id="projen.python.RuleSelector.N811"></a>

N811.

---


##### `N812` <a name="N812" id="projen.python.RuleSelector.N812"></a>

N812.

---


##### `N813` <a name="N813" id="projen.python.RuleSelector.N813"></a>

N813.

---


##### `N814` <a name="N814" id="projen.python.RuleSelector.N814"></a>

N814.

---


##### `N815` <a name="N815" id="projen.python.RuleSelector.N815"></a>

N815.

---


##### `N816` <a name="N816" id="projen.python.RuleSelector.N816"></a>

N816.

---


##### `N817` <a name="N817" id="projen.python.RuleSelector.N817"></a>

N817.

---


##### `N818` <a name="N818" id="projen.python.RuleSelector.N818"></a>

N818.

---


##### `N9` <a name="N9" id="projen.python.RuleSelector.N9"></a>

N9.

---


##### `N99` <a name="N99" id="projen.python.RuleSelector.N99"></a>

N99.

---


##### `N999` <a name="N999" id="projen.python.RuleSelector.N999"></a>

N999.

---


##### `NPY` <a name="NPY" id="projen.python.RuleSelector.NPY"></a>

NPY.

---


##### `NPY0` <a name="NPY0" id="projen.python.RuleSelector.NPY0"></a>

NPY0.

---


##### `NPY00` <a name="NPY00" id="projen.python.RuleSelector.NPY00"></a>

NPY00.

---


##### `NPY001` <a name="NPY001" id="projen.python.RuleSelector.NPY001"></a>

NPY001.

---


##### `NPY002` <a name="NPY002" id="projen.python.RuleSelector.NPY002"></a>

NPY002.

---


##### `NPY003` <a name="NPY003" id="projen.python.RuleSelector.NPY003"></a>

NPY003.

---


##### `NPY2` <a name="NPY2" id="projen.python.RuleSelector.NPY2"></a>

NPY2.

---


##### `NPY20` <a name="NPY20" id="projen.python.RuleSelector.NPY20"></a>

NPY20.

---


##### `NPY201` <a name="NPY201" id="projen.python.RuleSelector.NPY201"></a>

NPY201.

---


##### `PD` <a name="PD" id="projen.python.RuleSelector.PD"></a>

PD.

---


##### `PD0` <a name="PD0" id="projen.python.RuleSelector.PD0"></a>

PD0.

---


##### `PD00` <a name="PD00" id="projen.python.RuleSelector.PD00"></a>

PD00.

---


##### `PD002` <a name="PD002" id="projen.python.RuleSelector.PD002"></a>

PD002.

---


##### `PD003` <a name="PD003" id="projen.python.RuleSelector.PD003"></a>

PD003.

---


##### `PD004` <a name="PD004" id="projen.python.RuleSelector.PD004"></a>

PD004.

---


##### `PD007` <a name="PD007" id="projen.python.RuleSelector.PD007"></a>

PD007.

---


##### `PD008` <a name="PD008" id="projen.python.RuleSelector.PD008"></a>

PD008.

---


##### `PD009` <a name="PD009" id="projen.python.RuleSelector.PD009"></a>

PD009.

---


##### `PD01` <a name="PD01" id="projen.python.RuleSelector.PD01"></a>

PD01.

---


##### `PD010` <a name="PD010" id="projen.python.RuleSelector.PD010"></a>

PD010.

---


##### `PD011` <a name="PD011" id="projen.python.RuleSelector.PD011"></a>

PD011.

---


##### `PD012` <a name="PD012" id="projen.python.RuleSelector.PD012"></a>

PD012.

---


##### `PD013` <a name="PD013" id="projen.python.RuleSelector.PD013"></a>

PD013.

---


##### `PD015` <a name="PD015" id="projen.python.RuleSelector.PD015"></a>

PD015.

---


##### `PD1` <a name="PD1" id="projen.python.RuleSelector.PD1"></a>

PD1.

---


##### `PD10` <a name="PD10" id="projen.python.RuleSelector.PD10"></a>

PD10.

---


##### `PD101` <a name="PD101" id="projen.python.RuleSelector.PD101"></a>

PD101.

---


##### `PERF` <a name="PERF" id="projen.python.RuleSelector.PERF"></a>

PERF.

---


##### `PERF1` <a name="PERF1" id="projen.python.RuleSelector.PERF1"></a>

PERF1.

---


##### `PERF10` <a name="PERF10" id="projen.python.RuleSelector.PERF10"></a>

PERF10.

---


##### `PERF101` <a name="PERF101" id="projen.python.RuleSelector.PERF101"></a>

PERF101.

---


##### `PERF102` <a name="PERF102" id="projen.python.RuleSelector.PERF102"></a>

PERF102.

---


##### `PERF2` <a name="PERF2" id="projen.python.RuleSelector.PERF2"></a>

PERF2.

---


##### `PERF20` <a name="PERF20" id="projen.python.RuleSelector.PERF20"></a>

PERF20.

---


##### `PERF203` <a name="PERF203" id="projen.python.RuleSelector.PERF203"></a>

PERF203.

---


##### `PERF4` <a name="PERF4" id="projen.python.RuleSelector.PERF4"></a>

PERF4.

---


##### `PERF40` <a name="PERF40" id="projen.python.RuleSelector.PERF40"></a>

PERF40.

---


##### `PERF401` <a name="PERF401" id="projen.python.RuleSelector.PERF401"></a>

PERF401.

---


##### `PERF402` <a name="PERF402" id="projen.python.RuleSelector.PERF402"></a>

PERF402.

---


##### `PERF403` <a name="PERF403" id="projen.python.RuleSelector.PERF403"></a>

PERF403.

---


##### `PGH` <a name="PGH" id="projen.python.RuleSelector.PGH"></a>

PGH.

---


##### `PGH0` <a name="PGH0" id="projen.python.RuleSelector.PGH0"></a>

PGH0.

---


##### `PGH00` <a name="PGH00" id="projen.python.RuleSelector.PGH00"></a>

PGH00.

---


##### `PGH003` <a name="PGH003" id="projen.python.RuleSelector.PGH003"></a>

PGH003.

---


##### `PGH004` <a name="PGH004" id="projen.python.RuleSelector.PGH004"></a>

PGH004.

---


##### `PGH005` <a name="PGH005" id="projen.python.RuleSelector.PGH005"></a>

PGH005.

---


##### `PIE` <a name="PIE" id="projen.python.RuleSelector.PIE"></a>

PIE.

---


##### `PIE7` <a name="PIE7" id="projen.python.RuleSelector.PIE7"></a>

PIE7.

---


##### `PIE79` <a name="PIE79" id="projen.python.RuleSelector.PIE79"></a>

PIE79.

---


##### `PIE790` <a name="PIE790" id="projen.python.RuleSelector.PIE790"></a>

PIE790.

---


##### `PIE794` <a name="PIE794" id="projen.python.RuleSelector.PIE794"></a>

PIE794.

---


##### `PIE796` <a name="PIE796" id="projen.python.RuleSelector.PIE796"></a>

PIE796.

---


##### `PIE8` <a name="PIE8" id="projen.python.RuleSelector.PIE8"></a>

PIE8.

---


##### `PIE80` <a name="PIE80" id="projen.python.RuleSelector.PIE80"></a>

PIE80.

---


##### `PIE800` <a name="PIE800" id="projen.python.RuleSelector.PIE800"></a>

PIE800.

---


##### `PIE804` <a name="PIE804" id="projen.python.RuleSelector.PIE804"></a>

PIE804.

---


##### `PIE807` <a name="PIE807" id="projen.python.RuleSelector.PIE807"></a>

PIE807.

---


##### `PIE808` <a name="PIE808" id="projen.python.RuleSelector.PIE808"></a>

PIE808.

---


##### `PIE81` <a name="PIE81" id="projen.python.RuleSelector.PIE81"></a>

PIE81.

---


##### `PIE810` <a name="PIE810" id="projen.python.RuleSelector.PIE810"></a>

PIE810.

---


##### `PL` <a name="PL" id="projen.python.RuleSelector.PL"></a>

PL.

---


##### `PLC` <a name="PLC" id="projen.python.RuleSelector.PLC"></a>

PLC.

---


##### `PLC0` <a name="PLC0" id="projen.python.RuleSelector.PLC0"></a>

PLC0.

---


##### `PLC01` <a name="PLC01" id="projen.python.RuleSelector.PLC01"></a>

PLC01.

---


##### `PLC010` <a name="PLC010" id="projen.python.RuleSelector.PLC010"></a>

PLC010.

---


##### `PLC0105` <a name="PLC0105" id="projen.python.RuleSelector.PLC0105"></a>

PLC0105.

---


##### `PLC013` <a name="PLC013" id="projen.python.RuleSelector.PLC013"></a>

PLC013.

---


##### `PLC0131` <a name="PLC0131" id="projen.python.RuleSelector.PLC0131"></a>

PLC0131.

---


##### `PLC0132` <a name="PLC0132" id="projen.python.RuleSelector.PLC0132"></a>

PLC0132.

---


##### `PLC02` <a name="PLC02" id="projen.python.RuleSelector.PLC02"></a>

PLC02.

---


##### `PLC020` <a name="PLC020" id="projen.python.RuleSelector.PLC020"></a>

PLC020.

---


##### `PLC0205` <a name="PLC0205" id="projen.python.RuleSelector.PLC0205"></a>

PLC0205.

---


##### `PLC0206` <a name="PLC0206" id="projen.python.RuleSelector.PLC0206"></a>

PLC0206.

---


##### `PLC0207` <a name="PLC0207" id="projen.python.RuleSelector.PLC0207"></a>

PLC0207.

---


##### `PLC0208` <a name="PLC0208" id="projen.python.RuleSelector.PLC0208"></a>

PLC0208.

---


##### `PLC04` <a name="PLC04" id="projen.python.RuleSelector.PLC04"></a>

PLC04.

---


##### `PLC041` <a name="PLC041" id="projen.python.RuleSelector.PLC041"></a>

PLC041.

---


##### `PLC0414` <a name="PLC0414" id="projen.python.RuleSelector.PLC0414"></a>

PLC0414.

---


##### `PLC0415` <a name="PLC0415" id="projen.python.RuleSelector.PLC0415"></a>

PLC0415.

---


##### `PLC1` <a name="PLC1" id="projen.python.RuleSelector.PLC1"></a>

PLC1.

---


##### `PLC18` <a name="PLC18" id="projen.python.RuleSelector.PLC18"></a>

PLC18.

---


##### `PLC180` <a name="PLC180" id="projen.python.RuleSelector.PLC180"></a>

PLC180.

---


##### `PLC1802` <a name="PLC1802" id="projen.python.RuleSelector.PLC1802"></a>

PLC1802.

---


##### `PLC19` <a name="PLC19" id="projen.python.RuleSelector.PLC19"></a>

PLC19.

---


##### `PLC190` <a name="PLC190" id="projen.python.RuleSelector.PLC190"></a>

PLC190.

---


##### `PLC1901` <a name="PLC1901" id="projen.python.RuleSelector.PLC1901"></a>

PLC1901.

---


##### `PLC2` <a name="PLC2" id="projen.python.RuleSelector.PLC2"></a>

PLC2.

---


##### `PLC24` <a name="PLC24" id="projen.python.RuleSelector.PLC24"></a>

PLC24.

---


##### `PLC240` <a name="PLC240" id="projen.python.RuleSelector.PLC240"></a>

PLC240.

---


##### `PLC2401` <a name="PLC2401" id="projen.python.RuleSelector.PLC2401"></a>

PLC2401.

---


##### `PLC2403` <a name="PLC2403" id="projen.python.RuleSelector.PLC2403"></a>

PLC2403.

---


##### `PLC27` <a name="PLC27" id="projen.python.RuleSelector.PLC27"></a>

PLC27.

---


##### `PLC270` <a name="PLC270" id="projen.python.RuleSelector.PLC270"></a>

PLC270.

---


##### `PLC2701` <a name="PLC2701" id="projen.python.RuleSelector.PLC2701"></a>

PLC2701.

---


##### `PLC28` <a name="PLC28" id="projen.python.RuleSelector.PLC28"></a>

PLC28.

---


##### `PLC280` <a name="PLC280" id="projen.python.RuleSelector.PLC280"></a>

PLC280.

---


##### `PLC2801` <a name="PLC2801" id="projen.python.RuleSelector.PLC2801"></a>

PLC2801.

---


##### `PLC3` <a name="PLC3" id="projen.python.RuleSelector.PLC3"></a>

PLC3.

---


##### `PLC30` <a name="PLC30" id="projen.python.RuleSelector.PLC30"></a>

PLC30.

---


##### `PLC300` <a name="PLC300" id="projen.python.RuleSelector.PLC300"></a>

PLC300.

---


##### `PLC3002` <a name="PLC3002" id="projen.python.RuleSelector.PLC3002"></a>

PLC3002.

---


##### `PLE` <a name="PLE" id="projen.python.RuleSelector.PLE"></a>

PLE.

---


##### `PLE0` <a name="PLE0" id="projen.python.RuleSelector.PLE0"></a>

PLE0.

---


##### `PLE01` <a name="PLE01" id="projen.python.RuleSelector.PLE01"></a>

PLE01.

---


##### `PLE010` <a name="PLE010" id="projen.python.RuleSelector.PLE010"></a>

PLE010.

---


##### `PLE0100` <a name="PLE0100" id="projen.python.RuleSelector.PLE0100"></a>

PLE0100.

---


##### `PLE0101` <a name="PLE0101" id="projen.python.RuleSelector.PLE0101"></a>

PLE0101.

---


##### `PLE011` <a name="PLE011" id="projen.python.RuleSelector.PLE011"></a>

PLE011.

---


##### `PLE0115` <a name="PLE0115" id="projen.python.RuleSelector.PLE0115"></a>

PLE0115.

---


##### `PLE0116` <a name="PLE0116" id="projen.python.RuleSelector.PLE0116"></a>

PLE0116.

---


##### `PLE0117` <a name="PLE0117" id="projen.python.RuleSelector.PLE0117"></a>

PLE0117.

---


##### `PLE0118` <a name="PLE0118" id="projen.python.RuleSelector.PLE0118"></a>

PLE0118.

---


##### `PLE02` <a name="PLE02" id="projen.python.RuleSelector.PLE02"></a>

PLE02.

---


##### `PLE023` <a name="PLE023" id="projen.python.RuleSelector.PLE023"></a>

PLE023.

---


##### `PLE0237` <a name="PLE0237" id="projen.python.RuleSelector.PLE0237"></a>

PLE0237.

---


##### `PLE024` <a name="PLE024" id="projen.python.RuleSelector.PLE024"></a>

PLE024.

---


##### `PLE0241` <a name="PLE0241" id="projen.python.RuleSelector.PLE0241"></a>

PLE0241.

---


##### `PLE03` <a name="PLE03" id="projen.python.RuleSelector.PLE03"></a>

PLE03.

---


##### `PLE030` <a name="PLE030" id="projen.python.RuleSelector.PLE030"></a>

PLE030.

---


##### `PLE0302` <a name="PLE0302" id="projen.python.RuleSelector.PLE0302"></a>

PLE0302.

---


##### `PLE0303` <a name="PLE0303" id="projen.python.RuleSelector.PLE0303"></a>

PLE0303.

---


##### `PLE0304` <a name="PLE0304" id="projen.python.RuleSelector.PLE0304"></a>

PLE0304.

---


##### `PLE0305` <a name="PLE0305" id="projen.python.RuleSelector.PLE0305"></a>

PLE0305.

---


##### `PLE0307` <a name="PLE0307" id="projen.python.RuleSelector.PLE0307"></a>

PLE0307.

---


##### `PLE0308` <a name="PLE0308" id="projen.python.RuleSelector.PLE0308"></a>

PLE0308.

---


##### `PLE0309` <a name="PLE0309" id="projen.python.RuleSelector.PLE0309"></a>

PLE0309.

---


##### `PLE06` <a name="PLE06" id="projen.python.RuleSelector.PLE06"></a>

PLE06.

---


##### `PLE060` <a name="PLE060" id="projen.python.RuleSelector.PLE060"></a>

PLE060.

---


##### `PLE0604` <a name="PLE0604" id="projen.python.RuleSelector.PLE0604"></a>

PLE0604.

---


##### `PLE0605` <a name="PLE0605" id="projen.python.RuleSelector.PLE0605"></a>

PLE0605.

---


##### `PLE064` <a name="PLE064" id="projen.python.RuleSelector.PLE064"></a>

PLE064.

---


##### `PLE0643` <a name="PLE0643" id="projen.python.RuleSelector.PLE0643"></a>

PLE0643.

---


##### `PLE07` <a name="PLE07" id="projen.python.RuleSelector.PLE07"></a>

PLE07.

---


##### `PLE070` <a name="PLE070" id="projen.python.RuleSelector.PLE070"></a>

PLE070.

---


##### `PLE0704` <a name="PLE0704" id="projen.python.RuleSelector.PLE0704"></a>

PLE0704.

---


##### `PLE1` <a name="PLE1" id="projen.python.RuleSelector.PLE1"></a>

PLE1.

---


##### `PLE11` <a name="PLE11" id="projen.python.RuleSelector.PLE11"></a>

PLE11.

---


##### `PLE113` <a name="PLE113" id="projen.python.RuleSelector.PLE113"></a>

PLE113.

---


##### `PLE1132` <a name="PLE1132" id="projen.python.RuleSelector.PLE1132"></a>

PLE1132.

---


##### `PLE114` <a name="PLE114" id="projen.python.RuleSelector.PLE114"></a>

PLE114.

---


##### `PLE1141` <a name="PLE1141" id="projen.python.RuleSelector.PLE1141"></a>

PLE1141.

---


##### `PLE1142` <a name="PLE1142" id="projen.python.RuleSelector.PLE1142"></a>

PLE1142.

---


##### `PLE12` <a name="PLE12" id="projen.python.RuleSelector.PLE12"></a>

PLE12.

---


##### `PLE120` <a name="PLE120" id="projen.python.RuleSelector.PLE120"></a>

PLE120.

---


##### `PLE1205` <a name="PLE1205" id="projen.python.RuleSelector.PLE1205"></a>

PLE1205.

---


##### `PLE1206` <a name="PLE1206" id="projen.python.RuleSelector.PLE1206"></a>

PLE1206.

---


##### `PLE13` <a name="PLE13" id="projen.python.RuleSelector.PLE13"></a>

PLE13.

---


##### `PLE130` <a name="PLE130" id="projen.python.RuleSelector.PLE130"></a>

PLE130.

---


##### `PLE1300` <a name="PLE1300" id="projen.python.RuleSelector.PLE1300"></a>

PLE1300.

---


##### `PLE1307` <a name="PLE1307" id="projen.python.RuleSelector.PLE1307"></a>

PLE1307.

---


##### `PLE131` <a name="PLE131" id="projen.python.RuleSelector.PLE131"></a>

PLE131.

---


##### `PLE1310` <a name="PLE1310" id="projen.python.RuleSelector.PLE1310"></a>

PLE1310.

---


##### `PLE15` <a name="PLE15" id="projen.python.RuleSelector.PLE15"></a>

PLE15.

---


##### `PLE150` <a name="PLE150" id="projen.python.RuleSelector.PLE150"></a>

PLE150.

---


##### `PLE1507` <a name="PLE1507" id="projen.python.RuleSelector.PLE1507"></a>

PLE1507.

---


##### `PLE151` <a name="PLE151" id="projen.python.RuleSelector.PLE151"></a>

PLE151.

---


##### `PLE1519` <a name="PLE1519" id="projen.python.RuleSelector.PLE1519"></a>

PLE1519.

---


##### `PLE152` <a name="PLE152" id="projen.python.RuleSelector.PLE152"></a>

PLE152.

---


##### `PLE1520` <a name="PLE1520" id="projen.python.RuleSelector.PLE1520"></a>

PLE1520.

---


##### `PLE17` <a name="PLE17" id="projen.python.RuleSelector.PLE17"></a>

PLE17.

---


##### `PLE170` <a name="PLE170" id="projen.python.RuleSelector.PLE170"></a>

PLE170.

---


##### `PLE1700` <a name="PLE1700" id="projen.python.RuleSelector.PLE1700"></a>

PLE1700.

---


##### `PLE2` <a name="PLE2" id="projen.python.RuleSelector.PLE2"></a>

PLE2.

---


##### `PLE25` <a name="PLE25" id="projen.python.RuleSelector.PLE25"></a>

PLE25.

---


##### `PLE250` <a name="PLE250" id="projen.python.RuleSelector.PLE250"></a>

PLE250.

---


##### `PLE2502` <a name="PLE2502" id="projen.python.RuleSelector.PLE2502"></a>

PLE2502.

---


##### `PLE251` <a name="PLE251" id="projen.python.RuleSelector.PLE251"></a>

PLE251.

---


##### `PLE2510` <a name="PLE2510" id="projen.python.RuleSelector.PLE2510"></a>

PLE2510.

---


##### `PLE2512` <a name="PLE2512" id="projen.python.RuleSelector.PLE2512"></a>

PLE2512.

---


##### `PLE2513` <a name="PLE2513" id="projen.python.RuleSelector.PLE2513"></a>

PLE2513.

---


##### `PLE2514` <a name="PLE2514" id="projen.python.RuleSelector.PLE2514"></a>

PLE2514.

---


##### `PLE2515` <a name="PLE2515" id="projen.python.RuleSelector.PLE2515"></a>

PLE2515.

---


##### `PLE4` <a name="PLE4" id="projen.python.RuleSelector.PLE4"></a>

PLE4.

---


##### `PLE47` <a name="PLE47" id="projen.python.RuleSelector.PLE47"></a>

PLE47.

---


##### `PLE470` <a name="PLE470" id="projen.python.RuleSelector.PLE470"></a>

PLE470.

---


##### `PLE4703` <a name="PLE4703" id="projen.python.RuleSelector.PLE4703"></a>

PLE4703.

---


##### `PLR` <a name="PLR" id="projen.python.RuleSelector.PLR"></a>

PLR.

---


##### `PLR0` <a name="PLR0" id="projen.python.RuleSelector.PLR0"></a>

PLR0.

---


##### `PLR01` <a name="PLR01" id="projen.python.RuleSelector.PLR01"></a>

PLR01.

---


##### `PLR012` <a name="PLR012" id="projen.python.RuleSelector.PLR012"></a>

PLR012.

---


##### `PLR0124` <a name="PLR0124" id="projen.python.RuleSelector.PLR0124"></a>

PLR0124.

---


##### `PLR013` <a name="PLR013" id="projen.python.RuleSelector.PLR013"></a>

PLR013.

---


##### `PLR0133` <a name="PLR0133" id="projen.python.RuleSelector.PLR0133"></a>

PLR0133.

---


##### `PLR02` <a name="PLR02" id="projen.python.RuleSelector.PLR02"></a>

PLR02.

---


##### `PLR020` <a name="PLR020" id="projen.python.RuleSelector.PLR020"></a>

PLR020.

---


##### `PLR0202` <a name="PLR0202" id="projen.python.RuleSelector.PLR0202"></a>

PLR0202.

---


##### `PLR0203` <a name="PLR0203" id="projen.python.RuleSelector.PLR0203"></a>

PLR0203.

---


##### `PLR0206` <a name="PLR0206" id="projen.python.RuleSelector.PLR0206"></a>

PLR0206.

---


##### `PLR04` <a name="PLR04" id="projen.python.RuleSelector.PLR04"></a>

PLR04.

---


##### `PLR040` <a name="PLR040" id="projen.python.RuleSelector.PLR040"></a>

PLR040.

---


##### `PLR0402` <a name="PLR0402" id="projen.python.RuleSelector.PLR0402"></a>

PLR0402.

---


##### `PLR09` <a name="PLR09" id="projen.python.RuleSelector.PLR09"></a>

PLR09.

---


##### `PLR090` <a name="PLR090" id="projen.python.RuleSelector.PLR090"></a>

PLR090.

---


##### `PLR0904` <a name="PLR0904" id="projen.python.RuleSelector.PLR0904"></a>

PLR0904.

---


##### `PLR091` <a name="PLR091" id="projen.python.RuleSelector.PLR091"></a>

PLR091.

---


##### `PLR0911` <a name="PLR0911" id="projen.python.RuleSelector.PLR0911"></a>

PLR0911.

---


##### `PLR0912` <a name="PLR0912" id="projen.python.RuleSelector.PLR0912"></a>

PLR0912.

---


##### `PLR0913` <a name="PLR0913" id="projen.python.RuleSelector.PLR0913"></a>

PLR0913.

---


##### `PLR0914` <a name="PLR0914" id="projen.python.RuleSelector.PLR0914"></a>

PLR0914.

---


##### `PLR0915` <a name="PLR0915" id="projen.python.RuleSelector.PLR0915"></a>

PLR0915.

---


##### `PLR0916` <a name="PLR0916" id="projen.python.RuleSelector.PLR0916"></a>

PLR0916.

---


##### `PLR0917` <a name="PLR0917" id="projen.python.RuleSelector.PLR0917"></a>

PLR0917.

---


##### `PLR1` <a name="PLR1" id="projen.python.RuleSelector.PLR1"></a>

PLR1.

---


##### `PLR17` <a name="PLR17" id="projen.python.RuleSelector.PLR17"></a>

PLR17.

---


##### `PLR170` <a name="PLR170" id="projen.python.RuleSelector.PLR170"></a>

PLR170.

---


##### `PLR1702` <a name="PLR1702" id="projen.python.RuleSelector.PLR1702"></a>

PLR1702.

---


##### `PLR1704` <a name="PLR1704" id="projen.python.RuleSelector.PLR1704"></a>

PLR1704.

---


##### `PLR1708` <a name="PLR1708" id="projen.python.RuleSelector.PLR1708"></a>

PLR1708.

---


##### `PLR171` <a name="PLR171" id="projen.python.RuleSelector.PLR171"></a>

PLR171.

---


##### `PLR1711` <a name="PLR1711" id="projen.python.RuleSelector.PLR1711"></a>

PLR1711.

---


##### `PLR1714` <a name="PLR1714" id="projen.python.RuleSelector.PLR1714"></a>

PLR1714.

---


##### `PLR1716` <a name="PLR1716" id="projen.python.RuleSelector.PLR1716"></a>

PLR1716.

---


##### `PLR172` <a name="PLR172" id="projen.python.RuleSelector.PLR172"></a>

PLR172.

---


##### `PLR1722` <a name="PLR1722" id="projen.python.RuleSelector.PLR1722"></a>

PLR1722.

---


##### `PLR173` <a name="PLR173" id="projen.python.RuleSelector.PLR173"></a>

PLR173.

---


##### `PLR1730` <a name="PLR1730" id="projen.python.RuleSelector.PLR1730"></a>

PLR1730.

---


##### `PLR1733` <a name="PLR1733" id="projen.python.RuleSelector.PLR1733"></a>

PLR1733.

---


##### `PLR1736` <a name="PLR1736" id="projen.python.RuleSelector.PLR1736"></a>

PLR1736.

---


##### `PLR2` <a name="PLR2" id="projen.python.RuleSelector.PLR2"></a>

PLR2.

---


##### `PLR20` <a name="PLR20" id="projen.python.RuleSelector.PLR20"></a>

PLR20.

---


##### `PLR200` <a name="PLR200" id="projen.python.RuleSelector.PLR200"></a>

PLR200.

---


##### `PLR2004` <a name="PLR2004" id="projen.python.RuleSelector.PLR2004"></a>

PLR2004.

---


##### `PLR204` <a name="PLR204" id="projen.python.RuleSelector.PLR204"></a>

PLR204.

---


##### `PLR2044` <a name="PLR2044" id="projen.python.RuleSelector.PLR2044"></a>

PLR2044.

---


##### `PLR5` <a name="PLR5" id="projen.python.RuleSelector.PLR5"></a>

PLR5.

---


##### `PLR55` <a name="PLR55" id="projen.python.RuleSelector.PLR55"></a>

PLR55.

---


##### `PLR550` <a name="PLR550" id="projen.python.RuleSelector.PLR550"></a>

PLR550.

---


##### `PLR5501` <a name="PLR5501" id="projen.python.RuleSelector.PLR5501"></a>

PLR5501.

---


##### `PLR6` <a name="PLR6" id="projen.python.RuleSelector.PLR6"></a>

PLR6.

---


##### `PLR61` <a name="PLR61" id="projen.python.RuleSelector.PLR61"></a>

PLR61.

---


##### `PLR610` <a name="PLR610" id="projen.python.RuleSelector.PLR610"></a>

PLR610.

---


##### `PLR6104` <a name="PLR6104" id="projen.python.RuleSelector.PLR6104"></a>

PLR6104.

---


##### `PLR62` <a name="PLR62" id="projen.python.RuleSelector.PLR62"></a>

PLR62.

---


##### `PLR620` <a name="PLR620" id="projen.python.RuleSelector.PLR620"></a>

PLR620.

---


##### `PLR6201` <a name="PLR6201" id="projen.python.RuleSelector.PLR6201"></a>

PLR6201.

---


##### `PLR63` <a name="PLR63" id="projen.python.RuleSelector.PLR63"></a>

PLR63.

---


##### `PLR630` <a name="PLR630" id="projen.python.RuleSelector.PLR630"></a>

PLR630.

---


##### `PLR6301` <a name="PLR6301" id="projen.python.RuleSelector.PLR6301"></a>

PLR6301.

---


##### `PLW` <a name="PLW" id="projen.python.RuleSelector.PLW"></a>

PLW.

---


##### `PLW0` <a name="PLW0" id="projen.python.RuleSelector.PLW0"></a>

PLW0.

---


##### `PLW01` <a name="PLW01" id="projen.python.RuleSelector.PLW01"></a>

PLW01.

---


##### `PLW010` <a name="PLW010" id="projen.python.RuleSelector.PLW010"></a>

PLW010.

---


##### `PLW0108` <a name="PLW0108" id="projen.python.RuleSelector.PLW0108"></a>

PLW0108.

---


##### `PLW012` <a name="PLW012" id="projen.python.RuleSelector.PLW012"></a>

PLW012.

---


##### `PLW0120` <a name="PLW0120" id="projen.python.RuleSelector.PLW0120"></a>

PLW0120.

---


##### `PLW0127` <a name="PLW0127" id="projen.python.RuleSelector.PLW0127"></a>

PLW0127.

---


##### `PLW0128` <a name="PLW0128" id="projen.python.RuleSelector.PLW0128"></a>

PLW0128.

---


##### `PLW0129` <a name="PLW0129" id="projen.python.RuleSelector.PLW0129"></a>

PLW0129.

---


##### `PLW013` <a name="PLW013" id="projen.python.RuleSelector.PLW013"></a>

PLW013.

---


##### `PLW0131` <a name="PLW0131" id="projen.python.RuleSelector.PLW0131"></a>

PLW0131.

---


##### `PLW0133` <a name="PLW0133" id="projen.python.RuleSelector.PLW0133"></a>

PLW0133.

---


##### `PLW017` <a name="PLW017" id="projen.python.RuleSelector.PLW017"></a>

PLW017.

---


##### `PLW0177` <a name="PLW0177" id="projen.python.RuleSelector.PLW0177"></a>

PLW0177.

---


##### `PLW02` <a name="PLW02" id="projen.python.RuleSelector.PLW02"></a>

PLW02.

---


##### `PLW021` <a name="PLW021" id="projen.python.RuleSelector.PLW021"></a>

PLW021.

---


##### `PLW0211` <a name="PLW0211" id="projen.python.RuleSelector.PLW0211"></a>

PLW0211.

---


##### `PLW024` <a name="PLW024" id="projen.python.RuleSelector.PLW024"></a>

PLW024.

---


##### `PLW0244` <a name="PLW0244" id="projen.python.RuleSelector.PLW0244"></a>

PLW0244.

---


##### `PLW0245` <a name="PLW0245" id="projen.python.RuleSelector.PLW0245"></a>

PLW0245.

---


##### `PLW04` <a name="PLW04" id="projen.python.RuleSelector.PLW04"></a>

PLW04.

---


##### `PLW040` <a name="PLW040" id="projen.python.RuleSelector.PLW040"></a>

PLW040.

---


##### `PLW0406` <a name="PLW0406" id="projen.python.RuleSelector.PLW0406"></a>

PLW0406.

---


##### `PLW06` <a name="PLW06" id="projen.python.RuleSelector.PLW06"></a>

PLW06.

---


##### `PLW060` <a name="PLW060" id="projen.python.RuleSelector.PLW060"></a>

PLW060.

---


##### `PLW0602` <a name="PLW0602" id="projen.python.RuleSelector.PLW0602"></a>

PLW0602.

---


##### `PLW0603` <a name="PLW0603" id="projen.python.RuleSelector.PLW0603"></a>

PLW0603.

---


##### `PLW0604` <a name="PLW0604" id="projen.python.RuleSelector.PLW0604"></a>

PLW0604.

---


##### `PLW064` <a name="PLW064" id="projen.python.RuleSelector.PLW064"></a>

PLW064.

---


##### `PLW0642` <a name="PLW0642" id="projen.python.RuleSelector.PLW0642"></a>

PLW0642.

---


##### `PLW07` <a name="PLW07" id="projen.python.RuleSelector.PLW07"></a>

PLW07.

---


##### `PLW071` <a name="PLW071" id="projen.python.RuleSelector.PLW071"></a>

PLW071.

---


##### `PLW0711` <a name="PLW0711" id="projen.python.RuleSelector.PLW0711"></a>

PLW0711.

---


##### `PLW1` <a name="PLW1" id="projen.python.RuleSelector.PLW1"></a>

PLW1.

---


##### `PLW15` <a name="PLW15" id="projen.python.RuleSelector.PLW15"></a>

PLW15.

---


##### `PLW150` <a name="PLW150" id="projen.python.RuleSelector.PLW150"></a>

PLW150.

---


##### `PLW1501` <a name="PLW1501" id="projen.python.RuleSelector.PLW1501"></a>

PLW1501.

---


##### `PLW1507` <a name="PLW1507" id="projen.python.RuleSelector.PLW1507"></a>

PLW1507.

---


##### `PLW1508` <a name="PLW1508" id="projen.python.RuleSelector.PLW1508"></a>

PLW1508.

---


##### `PLW1509` <a name="PLW1509" id="projen.python.RuleSelector.PLW1509"></a>

PLW1509.

---


##### `PLW151` <a name="PLW151" id="projen.python.RuleSelector.PLW151"></a>

PLW151.

---


##### `PLW1510` <a name="PLW1510" id="projen.python.RuleSelector.PLW1510"></a>

PLW1510.

---


##### `PLW1514` <a name="PLW1514" id="projen.python.RuleSelector.PLW1514"></a>

PLW1514.

---


##### `PLW16` <a name="PLW16" id="projen.python.RuleSelector.PLW16"></a>

PLW16.

---


##### `PLW164` <a name="PLW164" id="projen.python.RuleSelector.PLW164"></a>

PLW164.

---


##### `PLW1641` <a name="PLW1641" id="projen.python.RuleSelector.PLW1641"></a>

PLW1641.

---


##### `PLW2` <a name="PLW2" id="projen.python.RuleSelector.PLW2"></a>

PLW2.

---


##### `PLW21` <a name="PLW21" id="projen.python.RuleSelector.PLW21"></a>

PLW21.

---


##### `PLW210` <a name="PLW210" id="projen.python.RuleSelector.PLW210"></a>

PLW210.

---


##### `PLW2101` <a name="PLW2101" id="projen.python.RuleSelector.PLW2101"></a>

PLW2101.

---


##### `PLW29` <a name="PLW29" id="projen.python.RuleSelector.PLW29"></a>

PLW29.

---


##### `PLW290` <a name="PLW290" id="projen.python.RuleSelector.PLW290"></a>

PLW290.

---


##### `PLW2901` <a name="PLW2901" id="projen.python.RuleSelector.PLW2901"></a>

PLW2901.

---


##### `PLW3` <a name="PLW3" id="projen.python.RuleSelector.PLW3"></a>

PLW3.

---


##### `PLW32` <a name="PLW32" id="projen.python.RuleSelector.PLW32"></a>

PLW32.

---


##### `PLW320` <a name="PLW320" id="projen.python.RuleSelector.PLW320"></a>

PLW320.

---


##### `PLW3201` <a name="PLW3201" id="projen.python.RuleSelector.PLW3201"></a>

PLW3201.

---


##### `PLW33` <a name="PLW33" id="projen.python.RuleSelector.PLW33"></a>

PLW33.

---


##### `PLW330` <a name="PLW330" id="projen.python.RuleSelector.PLW330"></a>

PLW330.

---


##### `PLW3301` <a name="PLW3301" id="projen.python.RuleSelector.PLW3301"></a>

PLW3301.

---


##### `PT` <a name="PT" id="projen.python.RuleSelector.PT"></a>

PT.

---


##### `PT0` <a name="PT0" id="projen.python.RuleSelector.PT0"></a>

PT0.

---


##### `PT00` <a name="PT00" id="projen.python.RuleSelector.PT00"></a>

PT00.

---


##### `PT001` <a name="PT001" id="projen.python.RuleSelector.PT001"></a>

PT001.

---


##### `PT002` <a name="PT002" id="projen.python.RuleSelector.PT002"></a>

PT002.

---


##### `PT003` <a name="PT003" id="projen.python.RuleSelector.PT003"></a>

PT003.

---


##### `PT006` <a name="PT006" id="projen.python.RuleSelector.PT006"></a>

PT006.

---


##### `PT007` <a name="PT007" id="projen.python.RuleSelector.PT007"></a>

PT007.

---


##### `PT008` <a name="PT008" id="projen.python.RuleSelector.PT008"></a>

PT008.

---


##### `PT009` <a name="PT009" id="projen.python.RuleSelector.PT009"></a>

PT009.

---


##### `PT01` <a name="PT01" id="projen.python.RuleSelector.PT01"></a>

PT01.

---


##### `PT010` <a name="PT010" id="projen.python.RuleSelector.PT010"></a>

PT010.

---


##### `PT011` <a name="PT011" id="projen.python.RuleSelector.PT011"></a>

PT011.

---


##### `PT012` <a name="PT012" id="projen.python.RuleSelector.PT012"></a>

PT012.

---


##### `PT013` <a name="PT013" id="projen.python.RuleSelector.PT013"></a>

PT013.

---


##### `PT014` <a name="PT014" id="projen.python.RuleSelector.PT014"></a>

PT014.

---


##### `PT015` <a name="PT015" id="projen.python.RuleSelector.PT015"></a>

PT015.

---


##### `PT016` <a name="PT016" id="projen.python.RuleSelector.PT016"></a>

PT016.

---


##### `PT017` <a name="PT017" id="projen.python.RuleSelector.PT017"></a>

PT017.

---


##### `PT018` <a name="PT018" id="projen.python.RuleSelector.PT018"></a>

PT018.

---


##### `PT019` <a name="PT019" id="projen.python.RuleSelector.PT019"></a>

PT019.

---


##### `PT02` <a name="PT02" id="projen.python.RuleSelector.PT02"></a>

PT02.

---


##### `PT020` <a name="PT020" id="projen.python.RuleSelector.PT020"></a>

PT020.

---


##### `PT021` <a name="PT021" id="projen.python.RuleSelector.PT021"></a>

PT021.

---


##### `PT022` <a name="PT022" id="projen.python.RuleSelector.PT022"></a>

PT022.

---


##### `PT023` <a name="PT023" id="projen.python.RuleSelector.PT023"></a>

PT023.

---


##### `PT024` <a name="PT024" id="projen.python.RuleSelector.PT024"></a>

PT024.

---


##### `PT025` <a name="PT025" id="projen.python.RuleSelector.PT025"></a>

PT025.

---


##### `PT026` <a name="PT026" id="projen.python.RuleSelector.PT026"></a>

PT026.

---


##### `PT027` <a name="PT027" id="projen.python.RuleSelector.PT027"></a>

PT027.

---


##### `PT028` <a name="PT028" id="projen.python.RuleSelector.PT028"></a>

PT028.

---


##### `PT029` <a name="PT029" id="projen.python.RuleSelector.PT029"></a>

PT029.

---


##### `PT03` <a name="PT03" id="projen.python.RuleSelector.PT03"></a>

PT03.

---


##### `PT030` <a name="PT030" id="projen.python.RuleSelector.PT030"></a>

PT030.

---


##### `PT031` <a name="PT031" id="projen.python.RuleSelector.PT031"></a>

PT031.

---


##### `PTH` <a name="PTH" id="projen.python.RuleSelector.PTH"></a>

PTH.

---


##### `PTH1` <a name="PTH1" id="projen.python.RuleSelector.PTH1"></a>

PTH1.

---


##### `PTH10` <a name="PTH10" id="projen.python.RuleSelector.PTH10"></a>

PTH10.

---


##### `PTH100` <a name="PTH100" id="projen.python.RuleSelector.PTH100"></a>

PTH100.

---


##### `PTH101` <a name="PTH101" id="projen.python.RuleSelector.PTH101"></a>

PTH101.

---


##### `PTH102` <a name="PTH102" id="projen.python.RuleSelector.PTH102"></a>

PTH102.

---


##### `PTH103` <a name="PTH103" id="projen.python.RuleSelector.PTH103"></a>

PTH103.

---


##### `PTH104` <a name="PTH104" id="projen.python.RuleSelector.PTH104"></a>

PTH104.

---


##### `PTH105` <a name="PTH105" id="projen.python.RuleSelector.PTH105"></a>

PTH105.

---


##### `PTH106` <a name="PTH106" id="projen.python.RuleSelector.PTH106"></a>

PTH106.

---


##### `PTH107` <a name="PTH107" id="projen.python.RuleSelector.PTH107"></a>

PTH107.

---


##### `PTH108` <a name="PTH108" id="projen.python.RuleSelector.PTH108"></a>

PTH108.

---


##### `PTH109` <a name="PTH109" id="projen.python.RuleSelector.PTH109"></a>

PTH109.

---


##### `PTH11` <a name="PTH11" id="projen.python.RuleSelector.PTH11"></a>

PTH11.

---


##### `PTH110` <a name="PTH110" id="projen.python.RuleSelector.PTH110"></a>

PTH110.

---


##### `PTH111` <a name="PTH111" id="projen.python.RuleSelector.PTH111"></a>

PTH111.

---


##### `PTH112` <a name="PTH112" id="projen.python.RuleSelector.PTH112"></a>

PTH112.

---


##### `PTH113` <a name="PTH113" id="projen.python.RuleSelector.PTH113"></a>

PTH113.

---


##### `PTH114` <a name="PTH114" id="projen.python.RuleSelector.PTH114"></a>

PTH114.

---


##### `PTH115` <a name="PTH115" id="projen.python.RuleSelector.PTH115"></a>

PTH115.

---


##### `PTH116` <a name="PTH116" id="projen.python.RuleSelector.PTH116"></a>

PTH116.

---


##### `PTH117` <a name="PTH117" id="projen.python.RuleSelector.PTH117"></a>

PTH117.

---


##### `PTH118` <a name="PTH118" id="projen.python.RuleSelector.PTH118"></a>

PTH118.

---


##### `PTH119` <a name="PTH119" id="projen.python.RuleSelector.PTH119"></a>

PTH119.

---


##### `PTH12` <a name="PTH12" id="projen.python.RuleSelector.PTH12"></a>

PTH12.

---


##### `PTH120` <a name="PTH120" id="projen.python.RuleSelector.PTH120"></a>

PTH120.

---


##### `PTH121` <a name="PTH121" id="projen.python.RuleSelector.PTH121"></a>

PTH121.

---


##### `PTH122` <a name="PTH122" id="projen.python.RuleSelector.PTH122"></a>

PTH122.

---


##### `PTH123` <a name="PTH123" id="projen.python.RuleSelector.PTH123"></a>

PTH123.

---


##### `PTH124` <a name="PTH124" id="projen.python.RuleSelector.PTH124"></a>

PTH124.

---


##### `PTH2` <a name="PTH2" id="projen.python.RuleSelector.PTH2"></a>

PTH2.

---


##### `PTH20` <a name="PTH20" id="projen.python.RuleSelector.PTH20"></a>

PTH20.

---


##### `PTH201` <a name="PTH201" id="projen.python.RuleSelector.PTH201"></a>

PTH201.

---


##### `PTH202` <a name="PTH202" id="projen.python.RuleSelector.PTH202"></a>

PTH202.

---


##### `PTH203` <a name="PTH203" id="projen.python.RuleSelector.PTH203"></a>

PTH203.

---


##### `PTH204` <a name="PTH204" id="projen.python.RuleSelector.PTH204"></a>

PTH204.

---


##### `PTH205` <a name="PTH205" id="projen.python.RuleSelector.PTH205"></a>

PTH205.

---


##### `PTH206` <a name="PTH206" id="projen.python.RuleSelector.PTH206"></a>

PTH206.

---


##### `PTH207` <a name="PTH207" id="projen.python.RuleSelector.PTH207"></a>

PTH207.

---


##### `PTH208` <a name="PTH208" id="projen.python.RuleSelector.PTH208"></a>

PTH208.

---


##### `PTH21` <a name="PTH21" id="projen.python.RuleSelector.PTH21"></a>

PTH21.

---


##### `PTH210` <a name="PTH210" id="projen.python.RuleSelector.PTH210"></a>

PTH210.

---


##### `PTH211` <a name="PTH211" id="projen.python.RuleSelector.PTH211"></a>

PTH211.

---


##### `PYI` <a name="PYI" id="projen.python.RuleSelector.PYI"></a>

PYI.

---


##### `PYI0` <a name="PYI0" id="projen.python.RuleSelector.PYI0"></a>

PYI0.

---


##### `PYI00` <a name="PYI00" id="projen.python.RuleSelector.PYI00"></a>

PYI00.

---


##### `PYI001` <a name="PYI001" id="projen.python.RuleSelector.PYI001"></a>

PYI001.

---


##### `PYI002` <a name="PYI002" id="projen.python.RuleSelector.PYI002"></a>

PYI002.

---


##### `PYI003` <a name="PYI003" id="projen.python.RuleSelector.PYI003"></a>

PYI003.

---


##### `PYI004` <a name="PYI004" id="projen.python.RuleSelector.PYI004"></a>

PYI004.

---


##### `PYI005` <a name="PYI005" id="projen.python.RuleSelector.PYI005"></a>

PYI005.

---


##### `PYI006` <a name="PYI006" id="projen.python.RuleSelector.PYI006"></a>

PYI006.

---


##### `PYI007` <a name="PYI007" id="projen.python.RuleSelector.PYI007"></a>

PYI007.

---


##### `PYI008` <a name="PYI008" id="projen.python.RuleSelector.PYI008"></a>

PYI008.

---


##### `PYI009` <a name="PYI009" id="projen.python.RuleSelector.PYI009"></a>

PYI009.

---


##### `PYI01` <a name="PYI01" id="projen.python.RuleSelector.PYI01"></a>

PYI01.

---


##### `PYI010` <a name="PYI010" id="projen.python.RuleSelector.PYI010"></a>

PYI010.

---


##### `PYI011` <a name="PYI011" id="projen.python.RuleSelector.PYI011"></a>

PYI011.

---


##### `PYI012` <a name="PYI012" id="projen.python.RuleSelector.PYI012"></a>

PYI012.

---


##### `PYI013` <a name="PYI013" id="projen.python.RuleSelector.PYI013"></a>

PYI013.

---


##### `PYI014` <a name="PYI014" id="projen.python.RuleSelector.PYI014"></a>

PYI014.

---


##### `PYI015` <a name="PYI015" id="projen.python.RuleSelector.PYI015"></a>

PYI015.

---


##### `PYI016` <a name="PYI016" id="projen.python.RuleSelector.PYI016"></a>

PYI016.

---


##### `PYI017` <a name="PYI017" id="projen.python.RuleSelector.PYI017"></a>

PYI017.

---


##### `PYI018` <a name="PYI018" id="projen.python.RuleSelector.PYI018"></a>

PYI018.

---


##### `PYI019` <a name="PYI019" id="projen.python.RuleSelector.PYI019"></a>

PYI019.

---


##### `PYI02` <a name="PYI02" id="projen.python.RuleSelector.PYI02"></a>

PYI02.

---


##### `PYI020` <a name="PYI020" id="projen.python.RuleSelector.PYI020"></a>

PYI020.

---


##### `PYI021` <a name="PYI021" id="projen.python.RuleSelector.PYI021"></a>

PYI021.

---


##### `PYI024` <a name="PYI024" id="projen.python.RuleSelector.PYI024"></a>

PYI024.

---


##### `PYI025` <a name="PYI025" id="projen.python.RuleSelector.PYI025"></a>

PYI025.

---


##### `PYI026` <a name="PYI026" id="projen.python.RuleSelector.PYI026"></a>

PYI026.

---


##### `PYI029` <a name="PYI029" id="projen.python.RuleSelector.PYI029"></a>

PYI029.

---


##### `PYI03` <a name="PYI03" id="projen.python.RuleSelector.PYI03"></a>

PYI03.

---


##### `PYI030` <a name="PYI030" id="projen.python.RuleSelector.PYI030"></a>

PYI030.

---


##### `PYI032` <a name="PYI032" id="projen.python.RuleSelector.PYI032"></a>

PYI032.

---


##### `PYI033` <a name="PYI033" id="projen.python.RuleSelector.PYI033"></a>

PYI033.

---


##### `PYI034` <a name="PYI034" id="projen.python.RuleSelector.PYI034"></a>

PYI034.

---


##### `PYI035` <a name="PYI035" id="projen.python.RuleSelector.PYI035"></a>

PYI035.

---


##### `PYI036` <a name="PYI036" id="projen.python.RuleSelector.PYI036"></a>

PYI036.

---


##### `PYI04` <a name="PYI04" id="projen.python.RuleSelector.PYI04"></a>

PYI04.

---


##### `PYI041` <a name="PYI041" id="projen.python.RuleSelector.PYI041"></a>

PYI041.

---


##### `PYI042` <a name="PYI042" id="projen.python.RuleSelector.PYI042"></a>

PYI042.

---


##### `PYI043` <a name="PYI043" id="projen.python.RuleSelector.PYI043"></a>

PYI043.

---


##### `PYI044` <a name="PYI044" id="projen.python.RuleSelector.PYI044"></a>

PYI044.

---


##### `PYI045` <a name="PYI045" id="projen.python.RuleSelector.PYI045"></a>

PYI045.

---


##### `PYI046` <a name="PYI046" id="projen.python.RuleSelector.PYI046"></a>

PYI046.

---


##### `PYI047` <a name="PYI047" id="projen.python.RuleSelector.PYI047"></a>

PYI047.

---


##### `PYI048` <a name="PYI048" id="projen.python.RuleSelector.PYI048"></a>

PYI048.

---


##### `PYI049` <a name="PYI049" id="projen.python.RuleSelector.PYI049"></a>

PYI049.

---


##### `PYI05` <a name="PYI05" id="projen.python.RuleSelector.PYI05"></a>

PYI05.

---


##### `PYI050` <a name="PYI050" id="projen.python.RuleSelector.PYI050"></a>

PYI050.

---


##### `PYI051` <a name="PYI051" id="projen.python.RuleSelector.PYI051"></a>

PYI051.

---


##### `PYI052` <a name="PYI052" id="projen.python.RuleSelector.PYI052"></a>

PYI052.

---


##### `PYI053` <a name="PYI053" id="projen.python.RuleSelector.PYI053"></a>

PYI053.

---


##### `PYI054` <a name="PYI054" id="projen.python.RuleSelector.PYI054"></a>

PYI054.

---


##### `PYI055` <a name="PYI055" id="projen.python.RuleSelector.PYI055"></a>

PYI055.

---


##### `PYI056` <a name="PYI056" id="projen.python.RuleSelector.PYI056"></a>

PYI056.

---


##### `PYI057` <a name="PYI057" id="projen.python.RuleSelector.PYI057"></a>

PYI057.

---


##### `PYI058` <a name="PYI058" id="projen.python.RuleSelector.PYI058"></a>

PYI058.

---


##### `PYI059` <a name="PYI059" id="projen.python.RuleSelector.PYI059"></a>

PYI059.

---


##### `PYI06` <a name="PYI06" id="projen.python.RuleSelector.PYI06"></a>

PYI06.

---


##### `PYI061` <a name="PYI061" id="projen.python.RuleSelector.PYI061"></a>

PYI061.

---


##### `PYI062` <a name="PYI062" id="projen.python.RuleSelector.PYI062"></a>

PYI062.

---


##### `PYI063` <a name="PYI063" id="projen.python.RuleSelector.PYI063"></a>

PYI063.

---


##### `PYI064` <a name="PYI064" id="projen.python.RuleSelector.PYI064"></a>

PYI064.

---


##### `PYI066` <a name="PYI066" id="projen.python.RuleSelector.PYI066"></a>

PYI066.

---


##### `Q` <a name="Q" id="projen.python.RuleSelector.Q"></a>

Q.

---


##### `Q0` <a name="Q0" id="projen.python.RuleSelector.Q0"></a>

Q0.

---


##### `Q00` <a name="Q00" id="projen.python.RuleSelector.Q00"></a>

Q00.

---


##### `Q000` <a name="Q000" id="projen.python.RuleSelector.Q000"></a>

Q000.

---


##### `Q001` <a name="Q001" id="projen.python.RuleSelector.Q001"></a>

Q001.

---


##### `Q002` <a name="Q002" id="projen.python.RuleSelector.Q002"></a>

Q002.

---


##### `Q003` <a name="Q003" id="projen.python.RuleSelector.Q003"></a>

Q003.

---


##### `Q004` <a name="Q004" id="projen.python.RuleSelector.Q004"></a>

Q004.

---


##### `RET` <a name="RET" id="projen.python.RuleSelector.RET"></a>

RET.

---


##### `RET5` <a name="RET5" id="projen.python.RuleSelector.RET5"></a>

RET5.

---


##### `RET50` <a name="RET50" id="projen.python.RuleSelector.RET50"></a>

RET50.

---


##### `RET501` <a name="RET501" id="projen.python.RuleSelector.RET501"></a>

RET501.

---


##### `RET502` <a name="RET502" id="projen.python.RuleSelector.RET502"></a>

RET502.

---


##### `RET503` <a name="RET503" id="projen.python.RuleSelector.RET503"></a>

RET503.

---


##### `RET504` <a name="RET504" id="projen.python.RuleSelector.RET504"></a>

RET504.

---


##### `RET505` <a name="RET505" id="projen.python.RuleSelector.RET505"></a>

RET505.

---


##### `RET506` <a name="RET506" id="projen.python.RuleSelector.RET506"></a>

RET506.

---


##### `RET507` <a name="RET507" id="projen.python.RuleSelector.RET507"></a>

RET507.

---


##### `RET508` <a name="RET508" id="projen.python.RuleSelector.RET508"></a>

RET508.

---


##### `RSE` <a name="RSE" id="projen.python.RuleSelector.RSE"></a>

RSE.

---


##### `RSE1` <a name="RSE1" id="projen.python.RuleSelector.RSE1"></a>

RSE1.

---


##### `RSE10` <a name="RSE10" id="projen.python.RuleSelector.RSE10"></a>

RSE10.

---


##### `RSE102` <a name="RSE102" id="projen.python.RuleSelector.RSE102"></a>

RSE102.

---


##### `RUF` <a name="RUF" id="projen.python.RuleSelector.RUF"></a>

RUF.

---


##### `RUF0` <a name="RUF0" id="projen.python.RuleSelector.RUF0"></a>

RUF0.

---


##### `RUF00` <a name="RUF00" id="projen.python.RuleSelector.RUF00"></a>

RUF00.

---


##### `RUF001` <a name="RUF001" id="projen.python.RuleSelector.RUF001"></a>

RUF001.

---


##### `RUF002` <a name="RUF002" id="projen.python.RuleSelector.RUF002"></a>

RUF002.

---


##### `RUF003` <a name="RUF003" id="projen.python.RuleSelector.RUF003"></a>

RUF003.

---


##### `RUF005` <a name="RUF005" id="projen.python.RuleSelector.RUF005"></a>

RUF005.

---


##### `RUF006` <a name="RUF006" id="projen.python.RuleSelector.RUF006"></a>

RUF006.

---


##### `RUF007` <a name="RUF007" id="projen.python.RuleSelector.RUF007"></a>

RUF007.

---


##### `RUF008` <a name="RUF008" id="projen.python.RuleSelector.RUF008"></a>

RUF008.

---


##### `RUF009` <a name="RUF009" id="projen.python.RuleSelector.RUF009"></a>

RUF009.

---


##### `RUF01` <a name="RUF01" id="projen.python.RuleSelector.RUF01"></a>

RUF01.

---


##### `RUF010` <a name="RUF010" id="projen.python.RuleSelector.RUF010"></a>

RUF010.

---


##### `RUF012` <a name="RUF012" id="projen.python.RuleSelector.RUF012"></a>

RUF012.

---


##### `RUF013` <a name="RUF013" id="projen.python.RuleSelector.RUF013"></a>

RUF013.

---


##### `RUF015` <a name="RUF015" id="projen.python.RuleSelector.RUF015"></a>

RUF015.

---


##### `RUF016` <a name="RUF016" id="projen.python.RuleSelector.RUF016"></a>

RUF016.

---


##### `RUF017` <a name="RUF017" id="projen.python.RuleSelector.RUF017"></a>

RUF017.

---


##### `RUF018` <a name="RUF018" id="projen.python.RuleSelector.RUF018"></a>

RUF018.

---


##### `RUF019` <a name="RUF019" id="projen.python.RuleSelector.RUF019"></a>

RUF019.

---


##### `RUF02` <a name="RUF02" id="projen.python.RuleSelector.RUF02"></a>

RUF02.

---


##### `RUF020` <a name="RUF020" id="projen.python.RuleSelector.RUF020"></a>

RUF020.

---


##### `RUF021` <a name="RUF021" id="projen.python.RuleSelector.RUF021"></a>

RUF021.

---


##### `RUF022` <a name="RUF022" id="projen.python.RuleSelector.RUF022"></a>

RUF022.

---


##### `RUF023` <a name="RUF023" id="projen.python.RuleSelector.RUF023"></a>

RUF023.

---


##### `RUF024` <a name="RUF024" id="projen.python.RuleSelector.RUF024"></a>

RUF024.

---


##### `RUF026` <a name="RUF026" id="projen.python.RuleSelector.RUF026"></a>

RUF026.

---


##### `RUF027` <a name="RUF027" id="projen.python.RuleSelector.RUF027"></a>

RUF027.

---


##### `RUF028` <a name="RUF028" id="projen.python.RuleSelector.RUF028"></a>

RUF028.

---


##### `RUF029` <a name="RUF029" id="projen.python.RuleSelector.RUF029"></a>

RUF029.

---


##### `RUF03` <a name="RUF03" id="projen.python.RuleSelector.RUF03"></a>

RUF03.

---


##### `RUF030` <a name="RUF030" id="projen.python.RuleSelector.RUF030"></a>

RUF030.

---


##### `RUF031` <a name="RUF031" id="projen.python.RuleSelector.RUF031"></a>

RUF031.

---


##### `RUF032` <a name="RUF032" id="projen.python.RuleSelector.RUF032"></a>

RUF032.

---


##### `RUF033` <a name="RUF033" id="projen.python.RuleSelector.RUF033"></a>

RUF033.

---


##### `RUF034` <a name="RUF034" id="projen.python.RuleSelector.RUF034"></a>

RUF034.

---


##### `RUF036` <a name="RUF036" id="projen.python.RuleSelector.RUF036"></a>

RUF036.

---


##### `RUF037` <a name="RUF037" id="projen.python.RuleSelector.RUF037"></a>

RUF037.

---


##### `RUF038` <a name="RUF038" id="projen.python.RuleSelector.RUF038"></a>

RUF038.

---


##### `RUF039` <a name="RUF039" id="projen.python.RuleSelector.RUF039"></a>

RUF039.

---


##### `RUF04` <a name="RUF04" id="projen.python.RuleSelector.RUF04"></a>

RUF04.

---


##### `RUF040` <a name="RUF040" id="projen.python.RuleSelector.RUF040"></a>

RUF040.

---


##### `RUF041` <a name="RUF041" id="projen.python.RuleSelector.RUF041"></a>

RUF041.

---


##### `RUF043` <a name="RUF043" id="projen.python.RuleSelector.RUF043"></a>

RUF043.

---


##### `RUF045` <a name="RUF045" id="projen.python.RuleSelector.RUF045"></a>

RUF045.

---


##### `RUF046` <a name="RUF046" id="projen.python.RuleSelector.RUF046"></a>

RUF046.

---


##### `RUF047` <a name="RUF047" id="projen.python.RuleSelector.RUF047"></a>

RUF047.

---


##### `RUF048` <a name="RUF048" id="projen.python.RuleSelector.RUF048"></a>

RUF048.

---


##### `RUF049` <a name="RUF049" id="projen.python.RuleSelector.RUF049"></a>

RUF049.

---


##### `RUF05` <a name="RUF05" id="projen.python.RuleSelector.RUF05"></a>

RUF05.

---


##### `RUF051` <a name="RUF051" id="projen.python.RuleSelector.RUF051"></a>

RUF051.

---


##### `RUF052` <a name="RUF052" id="projen.python.RuleSelector.RUF052"></a>

RUF052.

---


##### `RUF053` <a name="RUF053" id="projen.python.RuleSelector.RUF053"></a>

RUF053.

---


##### `RUF054` <a name="RUF054" id="projen.python.RuleSelector.RUF054"></a>

RUF054.

---


##### `RUF055` <a name="RUF055" id="projen.python.RuleSelector.RUF055"></a>

RUF055.

---


##### `RUF056` <a name="RUF056" id="projen.python.RuleSelector.RUF056"></a>

RUF056.

---


##### `RUF057` <a name="RUF057" id="projen.python.RuleSelector.RUF057"></a>

RUF057.

---


##### `RUF058` <a name="RUF058" id="projen.python.RuleSelector.RUF058"></a>

RUF058.

---


##### `RUF059` <a name="RUF059" id="projen.python.RuleSelector.RUF059"></a>

RUF059.

---


##### `RUF06` <a name="RUF06" id="projen.python.RuleSelector.RUF06"></a>

RUF06.

---


##### `RUF060` <a name="RUF060" id="projen.python.RuleSelector.RUF060"></a>

RUF060.

---


##### `RUF061` <a name="RUF061" id="projen.python.RuleSelector.RUF061"></a>

RUF061.

---


##### `RUF063` <a name="RUF063" id="projen.python.RuleSelector.RUF063"></a>

RUF063.

---


##### `RUF064` <a name="RUF064" id="projen.python.RuleSelector.RUF064"></a>

RUF064.

---


##### `RUF065` <a name="RUF065" id="projen.python.RuleSelector.RUF065"></a>

RUF065.

---


##### `RUF1` <a name="RUF1" id="projen.python.RuleSelector.RUF1"></a>

RUF1.

---


##### `RUF10` <a name="RUF10" id="projen.python.RuleSelector.RUF10"></a>

RUF10.

---


##### `RUF100` <a name="RUF100" id="projen.python.RuleSelector.RUF100"></a>

RUF100.

---


##### `RUF101` <a name="RUF101" id="projen.python.RuleSelector.RUF101"></a>

RUF101.

---


##### `RUF102` <a name="RUF102" id="projen.python.RuleSelector.RUF102"></a>

RUF102.

---


##### `RUF2` <a name="RUF2" id="projen.python.RuleSelector.RUF2"></a>

RUF2.

---


##### `RUF20` <a name="RUF20" id="projen.python.RuleSelector.RUF20"></a>

RUF20.

---


##### `RUF200` <a name="RUF200" id="projen.python.RuleSelector.RUF200"></a>

RUF200.

---


##### `S` <a name="S" id="projen.python.RuleSelector.S"></a>

S.

---


##### `S1` <a name="S1" id="projen.python.RuleSelector.S1"></a>

S1.

---


##### `S10` <a name="S10" id="projen.python.RuleSelector.S10"></a>

S10.

---


##### `S101` <a name="S101" id="projen.python.RuleSelector.S101"></a>

S101.

---


##### `S102` <a name="S102" id="projen.python.RuleSelector.S102"></a>

S102.

---


##### `S103` <a name="S103" id="projen.python.RuleSelector.S103"></a>

S103.

---


##### `S104` <a name="S104" id="projen.python.RuleSelector.S104"></a>

S104.

---


##### `S105` <a name="S105" id="projen.python.RuleSelector.S105"></a>

S105.

---


##### `S106` <a name="S106" id="projen.python.RuleSelector.S106"></a>

S106.

---


##### `S107` <a name="S107" id="projen.python.RuleSelector.S107"></a>

S107.

---


##### `S108` <a name="S108" id="projen.python.RuleSelector.S108"></a>

S108.

---


##### `S11` <a name="S11" id="projen.python.RuleSelector.S11"></a>

S11.

---


##### `S110` <a name="S110" id="projen.python.RuleSelector.S110"></a>

S110.

---


##### `S112` <a name="S112" id="projen.python.RuleSelector.S112"></a>

S112.

---


##### `S113` <a name="S113" id="projen.python.RuleSelector.S113"></a>

S113.

---


##### `S2` <a name="S2" id="projen.python.RuleSelector.S2"></a>

S2.

---


##### `S20` <a name="S20" id="projen.python.RuleSelector.S20"></a>

S20.

---


##### `S201` <a name="S201" id="projen.python.RuleSelector.S201"></a>

S201.

---


##### `S202` <a name="S202" id="projen.python.RuleSelector.S202"></a>

S202.

---


##### `S3` <a name="S3" id="projen.python.RuleSelector.S3"></a>

S3.

---


##### `S30` <a name="S30" id="projen.python.RuleSelector.S30"></a>

S30.

---


##### `S301` <a name="S301" id="projen.python.RuleSelector.S301"></a>

S301.

---


##### `S302` <a name="S302" id="projen.python.RuleSelector.S302"></a>

S302.

---


##### `S303` <a name="S303" id="projen.python.RuleSelector.S303"></a>

S303.

---


##### `S304` <a name="S304" id="projen.python.RuleSelector.S304"></a>

S304.

---


##### `S305` <a name="S305" id="projen.python.RuleSelector.S305"></a>

S305.

---


##### `S306` <a name="S306" id="projen.python.RuleSelector.S306"></a>

S306.

---


##### `S307` <a name="S307" id="projen.python.RuleSelector.S307"></a>

S307.

---


##### `S308` <a name="S308" id="projen.python.RuleSelector.S308"></a>

S308.

---


##### `S31` <a name="S31" id="projen.python.RuleSelector.S31"></a>

S31.

---


##### `S310` <a name="S310" id="projen.python.RuleSelector.S310"></a>

S310.

---


##### `S311` <a name="S311" id="projen.python.RuleSelector.S311"></a>

S311.

---


##### `S312` <a name="S312" id="projen.python.RuleSelector.S312"></a>

S312.

---


##### `S313` <a name="S313" id="projen.python.RuleSelector.S313"></a>

S313.

---


##### `S314` <a name="S314" id="projen.python.RuleSelector.S314"></a>

S314.

---


##### `S315` <a name="S315" id="projen.python.RuleSelector.S315"></a>

S315.

---


##### `S316` <a name="S316" id="projen.python.RuleSelector.S316"></a>

S316.

---


##### `S317` <a name="S317" id="projen.python.RuleSelector.S317"></a>

S317.

---


##### `S318` <a name="S318" id="projen.python.RuleSelector.S318"></a>

S318.

---


##### `S319` <a name="S319" id="projen.python.RuleSelector.S319"></a>

S319.

---


##### `S32` <a name="S32" id="projen.python.RuleSelector.S32"></a>

S32.

---


##### `S321` <a name="S321" id="projen.python.RuleSelector.S321"></a>

S321.

---


##### `S323` <a name="S323" id="projen.python.RuleSelector.S323"></a>

S323.

---


##### `S324` <a name="S324" id="projen.python.RuleSelector.S324"></a>

S324.

---


##### `S4` <a name="S4" id="projen.python.RuleSelector.S4"></a>

S4.

---


##### `S40` <a name="S40" id="projen.python.RuleSelector.S40"></a>

S40.

---


##### `S401` <a name="S401" id="projen.python.RuleSelector.S401"></a>

S401.

---


##### `S402` <a name="S402" id="projen.python.RuleSelector.S402"></a>

S402.

---


##### `S403` <a name="S403" id="projen.python.RuleSelector.S403"></a>

S403.

---


##### `S404` <a name="S404" id="projen.python.RuleSelector.S404"></a>

S404.

---


##### `S405` <a name="S405" id="projen.python.RuleSelector.S405"></a>

S405.

---


##### `S406` <a name="S406" id="projen.python.RuleSelector.S406"></a>

S406.

---


##### `S407` <a name="S407" id="projen.python.RuleSelector.S407"></a>

S407.

---


##### `S408` <a name="S408" id="projen.python.RuleSelector.S408"></a>

S408.

---


##### `S409` <a name="S409" id="projen.python.RuleSelector.S409"></a>

S409.

---


##### `S41` <a name="S41" id="projen.python.RuleSelector.S41"></a>

S41.

---


##### `S411` <a name="S411" id="projen.python.RuleSelector.S411"></a>

S411.

---


##### `S412` <a name="S412" id="projen.python.RuleSelector.S412"></a>

S412.

---


##### `S413` <a name="S413" id="projen.python.RuleSelector.S413"></a>

S413.

---


##### `S415` <a name="S415" id="projen.python.RuleSelector.S415"></a>

S415.

---


##### `S5` <a name="S5" id="projen.python.RuleSelector.S5"></a>

S5.

---


##### `S50` <a name="S50" id="projen.python.RuleSelector.S50"></a>

S50.

---


##### `S501` <a name="S501" id="projen.python.RuleSelector.S501"></a>

S501.

---


##### `S502` <a name="S502" id="projen.python.RuleSelector.S502"></a>

S502.

---


##### `S503` <a name="S503" id="projen.python.RuleSelector.S503"></a>

S503.

---


##### `S504` <a name="S504" id="projen.python.RuleSelector.S504"></a>

S504.

---


##### `S505` <a name="S505" id="projen.python.RuleSelector.S505"></a>

S505.

---


##### `S506` <a name="S506" id="projen.python.RuleSelector.S506"></a>

S506.

---


##### `S507` <a name="S507" id="projen.python.RuleSelector.S507"></a>

S507.

---


##### `S508` <a name="S508" id="projen.python.RuleSelector.S508"></a>

S508.

---


##### `S509` <a name="S509" id="projen.python.RuleSelector.S509"></a>

S509.

---


##### `S6` <a name="S6" id="projen.python.RuleSelector.S6"></a>

S6.

---


##### `S60` <a name="S60" id="projen.python.RuleSelector.S60"></a>

S60.

---


##### `S601` <a name="S601" id="projen.python.RuleSelector.S601"></a>

S601.

---


##### `S602` <a name="S602" id="projen.python.RuleSelector.S602"></a>

S602.

---


##### `S603` <a name="S603" id="projen.python.RuleSelector.S603"></a>

S603.

---


##### `S604` <a name="S604" id="projen.python.RuleSelector.S604"></a>

S604.

---


##### `S605` <a name="S605" id="projen.python.RuleSelector.S605"></a>

S605.

---


##### `S606` <a name="S606" id="projen.python.RuleSelector.S606"></a>

S606.

---


##### `S607` <a name="S607" id="projen.python.RuleSelector.S607"></a>

S607.

---


##### `S608` <a name="S608" id="projen.python.RuleSelector.S608"></a>

S608.

---


##### `S609` <a name="S609" id="projen.python.RuleSelector.S609"></a>

S609.

---


##### `S61` <a name="S61" id="projen.python.RuleSelector.S61"></a>

S61.

---


##### `S610` <a name="S610" id="projen.python.RuleSelector.S610"></a>

S610.

---


##### `S611` <a name="S611" id="projen.python.RuleSelector.S611"></a>

S611.

---


##### `S612` <a name="S612" id="projen.python.RuleSelector.S612"></a>

S612.

---


##### `S7` <a name="S7" id="projen.python.RuleSelector.S7"></a>

S7.

---


##### `S70` <a name="S70" id="projen.python.RuleSelector.S70"></a>

S70.

---


##### `S701` <a name="S701" id="projen.python.RuleSelector.S701"></a>

S701.

---


##### `S702` <a name="S702" id="projen.python.RuleSelector.S702"></a>

S702.

---


##### `S704` <a name="S704" id="projen.python.RuleSelector.S704"></a>

S704.

---


##### `SIM` <a name="SIM" id="projen.python.RuleSelector.SIM"></a>

SIM.

---


##### `SIM1` <a name="SIM1" id="projen.python.RuleSelector.SIM1"></a>

SIM1.

---


##### `SIM10` <a name="SIM10" id="projen.python.RuleSelector.SIM10"></a>

SIM10.

---


##### `SIM101` <a name="SIM101" id="projen.python.RuleSelector.SIM101"></a>

SIM101.

---


##### `SIM102` <a name="SIM102" id="projen.python.RuleSelector.SIM102"></a>

SIM102.

---


##### `SIM103` <a name="SIM103" id="projen.python.RuleSelector.SIM103"></a>

SIM103.

---


##### `SIM105` <a name="SIM105" id="projen.python.RuleSelector.SIM105"></a>

SIM105.

---


##### `SIM107` <a name="SIM107" id="projen.python.RuleSelector.SIM107"></a>

SIM107.

---


##### `SIM108` <a name="SIM108" id="projen.python.RuleSelector.SIM108"></a>

SIM108.

---


##### `SIM109` <a name="SIM109" id="projen.python.RuleSelector.SIM109"></a>

SIM109.

---


##### `SIM11` <a name="SIM11" id="projen.python.RuleSelector.SIM11"></a>

SIM11.

---


##### `SIM110` <a name="SIM110" id="projen.python.RuleSelector.SIM110"></a>

SIM110.

---


##### `SIM112` <a name="SIM112" id="projen.python.RuleSelector.SIM112"></a>

SIM112.

---


##### `SIM113` <a name="SIM113" id="projen.python.RuleSelector.SIM113"></a>

SIM113.

---


##### `SIM114` <a name="SIM114" id="projen.python.RuleSelector.SIM114"></a>

SIM114.

---


##### `SIM115` <a name="SIM115" id="projen.python.RuleSelector.SIM115"></a>

SIM115.

---


##### `SIM116` <a name="SIM116" id="projen.python.RuleSelector.SIM116"></a>

SIM116.

---


##### `SIM117` <a name="SIM117" id="projen.python.RuleSelector.SIM117"></a>

SIM117.

---


##### `SIM118` <a name="SIM118" id="projen.python.RuleSelector.SIM118"></a>

SIM118.

---


##### `SIM2` <a name="SIM2" id="projen.python.RuleSelector.SIM2"></a>

SIM2.

---


##### `SIM20` <a name="SIM20" id="projen.python.RuleSelector.SIM20"></a>

SIM20.

---


##### `SIM201` <a name="SIM201" id="projen.python.RuleSelector.SIM201"></a>

SIM201.

---


##### `SIM202` <a name="SIM202" id="projen.python.RuleSelector.SIM202"></a>

SIM202.

---


##### `SIM208` <a name="SIM208" id="projen.python.RuleSelector.SIM208"></a>

SIM208.

---


##### `SIM21` <a name="SIM21" id="projen.python.RuleSelector.SIM21"></a>

SIM21.

---


##### `SIM210` <a name="SIM210" id="projen.python.RuleSelector.SIM210"></a>

SIM210.

---


##### `SIM211` <a name="SIM211" id="projen.python.RuleSelector.SIM211"></a>

SIM211.

---


##### `SIM212` <a name="SIM212" id="projen.python.RuleSelector.SIM212"></a>

SIM212.

---


##### `SIM22` <a name="SIM22" id="projen.python.RuleSelector.SIM22"></a>

SIM22.

---


##### `SIM220` <a name="SIM220" id="projen.python.RuleSelector.SIM220"></a>

SIM220.

---


##### `SIM221` <a name="SIM221" id="projen.python.RuleSelector.SIM221"></a>

SIM221.

---


##### `SIM222` <a name="SIM222" id="projen.python.RuleSelector.SIM222"></a>

SIM222.

---


##### `SIM223` <a name="SIM223" id="projen.python.RuleSelector.SIM223"></a>

SIM223.

---


##### `SIM3` <a name="SIM3" id="projen.python.RuleSelector.SIM3"></a>

SIM3.

---


##### `SIM30` <a name="SIM30" id="projen.python.RuleSelector.SIM30"></a>

SIM30.

---


##### `SIM300` <a name="SIM300" id="projen.python.RuleSelector.SIM300"></a>

SIM300.

---


##### `SIM4` <a name="SIM4" id="projen.python.RuleSelector.SIM4"></a>

SIM4.

---


##### `SIM40` <a name="SIM40" id="projen.python.RuleSelector.SIM40"></a>

SIM40.

---


##### `SIM401` <a name="SIM401" id="projen.python.RuleSelector.SIM401"></a>

SIM401.

---


##### `SIM9` <a name="SIM9" id="projen.python.RuleSelector.SIM9"></a>

SIM9.

---


##### `SIM90` <a name="SIM90" id="projen.python.RuleSelector.SIM90"></a>

SIM90.

---


##### `SIM905` <a name="SIM905" id="projen.python.RuleSelector.SIM905"></a>

SIM905.

---


##### `SIM91` <a name="SIM91" id="projen.python.RuleSelector.SIM91"></a>

SIM91.

---


##### `SIM910` <a name="SIM910" id="projen.python.RuleSelector.SIM910"></a>

SIM910.

---


##### `SIM911` <a name="SIM911" id="projen.python.RuleSelector.SIM911"></a>

SIM911.

---


##### `SLF` <a name="SLF" id="projen.python.RuleSelector.SLF"></a>

SLF.

---


##### `SLF0` <a name="SLF0" id="projen.python.RuleSelector.SLF0"></a>

SLF0.

---


##### `SLF00` <a name="SLF00" id="projen.python.RuleSelector.SLF00"></a>

SLF00.

---


##### `SLF001` <a name="SLF001" id="projen.python.RuleSelector.SLF001"></a>

SLF001.

---


##### `SLOT` <a name="SLOT" id="projen.python.RuleSelector.SLOT"></a>

SLOT.

---


##### `SLOT0` <a name="SLOT0" id="projen.python.RuleSelector.SLOT0"></a>

SLOT0.

---


##### `SLOT00` <a name="SLOT00" id="projen.python.RuleSelector.SLOT00"></a>

SLOT00.

---


##### `SLOT000` <a name="SLOT000" id="projen.python.RuleSelector.SLOT000"></a>

SLOT000.

---


##### `SLOT001` <a name="SLOT001" id="projen.python.RuleSelector.SLOT001"></a>

SLOT001.

---


##### `SLOT002` <a name="SLOT002" id="projen.python.RuleSelector.SLOT002"></a>

SLOT002.

---


##### `T` <a name="T" id="projen.python.RuleSelector.T"></a>

T.

---


##### `T1` <a name="T1" id="projen.python.RuleSelector.T1"></a>

T1.

---


##### `T10` <a name="T10" id="projen.python.RuleSelector.T10"></a>

T10.

---


##### `T100` <a name="T100" id="projen.python.RuleSelector.T100"></a>

T100.

---


##### `T2` <a name="T2" id="projen.python.RuleSelector.T2"></a>

T2.

---


##### `T20` <a name="T20" id="projen.python.RuleSelector.T20"></a>

T20.

---


##### `T201` <a name="T201" id="projen.python.RuleSelector.T201"></a>

T201.

---


##### `T203` <a name="T203" id="projen.python.RuleSelector.T203"></a>

T203.

---


##### `TC` <a name="TC" id="projen.python.RuleSelector.TC"></a>

TC.

---


##### `TC0` <a name="TC0" id="projen.python.RuleSelector.TC0"></a>

TC0.

---


##### `TC00` <a name="TC00" id="projen.python.RuleSelector.TC00"></a>

TC00.

---


##### `TC001` <a name="TC001" id="projen.python.RuleSelector.TC001"></a>

TC001.

---


##### `TC002` <a name="TC002" id="projen.python.RuleSelector.TC002"></a>

TC002.

---


##### `TC003` <a name="TC003" id="projen.python.RuleSelector.TC003"></a>

TC003.

---


##### `TC004` <a name="TC004" id="projen.python.RuleSelector.TC004"></a>

TC004.

---


##### `TC005` <a name="TC005" id="projen.python.RuleSelector.TC005"></a>

TC005.

---


##### `TC006` <a name="TC006" id="projen.python.RuleSelector.TC006"></a>

TC006.

---


##### `TC007` <a name="TC007" id="projen.python.RuleSelector.TC007"></a>

TC007.

---


##### `TC008` <a name="TC008" id="projen.python.RuleSelector.TC008"></a>

TC008.

---


##### `TC01` <a name="TC01" id="projen.python.RuleSelector.TC01"></a>

TC01.

---


##### `TC010` <a name="TC010" id="projen.python.RuleSelector.TC010"></a>

TC010.

---


##### `TD` <a name="TD" id="projen.python.RuleSelector.TD"></a>

TD.

---


##### `TD0` <a name="TD0" id="projen.python.RuleSelector.TD0"></a>

TD0.

---


##### `TD00` <a name="TD00" id="projen.python.RuleSelector.TD00"></a>

TD00.

---


##### `TD001` <a name="TD001" id="projen.python.RuleSelector.TD001"></a>

TD001.

---


##### `TD002` <a name="TD002" id="projen.python.RuleSelector.TD002"></a>

TD002.

---


##### `TD003` <a name="TD003" id="projen.python.RuleSelector.TD003"></a>

TD003.

---


##### `TD004` <a name="TD004" id="projen.python.RuleSelector.TD004"></a>

TD004.

---


##### `TD005` <a name="TD005" id="projen.python.RuleSelector.TD005"></a>

TD005.

---


##### `TD006` <a name="TD006" id="projen.python.RuleSelector.TD006"></a>

TD006.

---


##### `TD007` <a name="TD007" id="projen.python.RuleSelector.TD007"></a>

TD007.

---


##### `TID` <a name="TID" id="projen.python.RuleSelector.TID"></a>

TID.

---


##### `TID2` <a name="TID2" id="projen.python.RuleSelector.TID2"></a>

TID2.

---


##### `TID25` <a name="TID25" id="projen.python.RuleSelector.TID25"></a>

TID25.

---


##### `TID251` <a name="TID251" id="projen.python.RuleSelector.TID251"></a>

TID251.

---


##### `TID252` <a name="TID252" id="projen.python.RuleSelector.TID252"></a>

TID252.

---


##### `TID253` <a name="TID253" id="projen.python.RuleSelector.TID253"></a>

TID253.

---


##### `TRY` <a name="TRY" id="projen.python.RuleSelector.TRY"></a>

TRY.

---


##### `TRY0` <a name="TRY0" id="projen.python.RuleSelector.TRY0"></a>

TRY0.

---


##### `TRY00` <a name="TRY00" id="projen.python.RuleSelector.TRY00"></a>

TRY00.

---


##### `TRY002` <a name="TRY002" id="projen.python.RuleSelector.TRY002"></a>

TRY002.

---


##### `TRY003` <a name="TRY003" id="projen.python.RuleSelector.TRY003"></a>

TRY003.

---


##### `TRY004` <a name="TRY004" id="projen.python.RuleSelector.TRY004"></a>

TRY004.

---


##### `TRY2` <a name="TRY2" id="projen.python.RuleSelector.TRY2"></a>

TRY2.

---


##### `TRY20` <a name="TRY20" id="projen.python.RuleSelector.TRY20"></a>

TRY20.

---


##### `TRY201` <a name="TRY201" id="projen.python.RuleSelector.TRY201"></a>

TRY201.

---


##### `TRY203` <a name="TRY203" id="projen.python.RuleSelector.TRY203"></a>

TRY203.

---


##### `TRY3` <a name="TRY3" id="projen.python.RuleSelector.TRY3"></a>

TRY3.

---


##### `TRY30` <a name="TRY30" id="projen.python.RuleSelector.TRY30"></a>

TRY30.

---


##### `TRY300` <a name="TRY300" id="projen.python.RuleSelector.TRY300"></a>

TRY300.

---


##### `TRY301` <a name="TRY301" id="projen.python.RuleSelector.TRY301"></a>

TRY301.

---


##### `TRY4` <a name="TRY4" id="projen.python.RuleSelector.TRY4"></a>

TRY4.

---


##### `TRY40` <a name="TRY40" id="projen.python.RuleSelector.TRY40"></a>

TRY40.

---


##### `TRY400` <a name="TRY400" id="projen.python.RuleSelector.TRY400"></a>

TRY400.

---


##### `TRY401` <a name="TRY401" id="projen.python.RuleSelector.TRY401"></a>

TRY401.

---


##### `UP` <a name="UP" id="projen.python.RuleSelector.UP"></a>

UP.

---


##### `UP0` <a name="UP0" id="projen.python.RuleSelector.UP0"></a>

UP0.

---


##### `UP00` <a name="UP00" id="projen.python.RuleSelector.UP00"></a>

UP00.

---


##### `UP001` <a name="UP001" id="projen.python.RuleSelector.UP001"></a>

UP001.

---


##### `UP003` <a name="UP003" id="projen.python.RuleSelector.UP003"></a>

UP003.

---


##### `UP004` <a name="UP004" id="projen.python.RuleSelector.UP004"></a>

UP004.

---


##### `UP005` <a name="UP005" id="projen.python.RuleSelector.UP005"></a>

UP005.

---


##### `UP006` <a name="UP006" id="projen.python.RuleSelector.UP006"></a>

UP006.

---


##### `UP007` <a name="UP007" id="projen.python.RuleSelector.UP007"></a>

UP007.

---


##### `UP008` <a name="UP008" id="projen.python.RuleSelector.UP008"></a>

UP008.

---


##### `UP009` <a name="UP009" id="projen.python.RuleSelector.UP009"></a>

UP009.

---


##### `UP01` <a name="UP01" id="projen.python.RuleSelector.UP01"></a>

UP01.

---


##### `UP010` <a name="UP010" id="projen.python.RuleSelector.UP010"></a>

UP010.

---


##### `UP011` <a name="UP011" id="projen.python.RuleSelector.UP011"></a>

UP011.

---


##### `UP012` <a name="UP012" id="projen.python.RuleSelector.UP012"></a>

UP012.

---


##### `UP013` <a name="UP013" id="projen.python.RuleSelector.UP013"></a>

UP013.

---


##### `UP014` <a name="UP014" id="projen.python.RuleSelector.UP014"></a>

UP014.

---


##### `UP015` <a name="UP015" id="projen.python.RuleSelector.UP015"></a>

UP015.

---


##### `UP017` <a name="UP017" id="projen.python.RuleSelector.UP017"></a>

UP017.

---


##### `UP018` <a name="UP018" id="projen.python.RuleSelector.UP018"></a>

UP018.

---


##### `UP019` <a name="UP019" id="projen.python.RuleSelector.UP019"></a>

UP019.

---


##### `UP02` <a name="UP02" id="projen.python.RuleSelector.UP02"></a>

UP02.

---


##### `UP020` <a name="UP020" id="projen.python.RuleSelector.UP020"></a>

UP020.

---


##### `UP021` <a name="UP021" id="projen.python.RuleSelector.UP021"></a>

UP021.

---


##### `UP022` <a name="UP022" id="projen.python.RuleSelector.UP022"></a>

UP022.

---


##### `UP023` <a name="UP023" id="projen.python.RuleSelector.UP023"></a>

UP023.

---


##### `UP024` <a name="UP024" id="projen.python.RuleSelector.UP024"></a>

UP024.

---


##### `UP025` <a name="UP025" id="projen.python.RuleSelector.UP025"></a>

UP025.

---


##### `UP026` <a name="UP026" id="projen.python.RuleSelector.UP026"></a>

UP026.

---


##### `UP028` <a name="UP028" id="projen.python.RuleSelector.UP028"></a>

UP028.

---


##### `UP029` <a name="UP029" id="projen.python.RuleSelector.UP029"></a>

UP029.

---


##### `UP03` <a name="UP03" id="projen.python.RuleSelector.UP03"></a>

UP03.

---


##### `UP030` <a name="UP030" id="projen.python.RuleSelector.UP030"></a>

UP030.

---


##### `UP031` <a name="UP031" id="projen.python.RuleSelector.UP031"></a>

UP031.

---


##### `UP032` <a name="UP032" id="projen.python.RuleSelector.UP032"></a>

UP032.

---


##### `UP033` <a name="UP033" id="projen.python.RuleSelector.UP033"></a>

UP033.

---


##### `UP034` <a name="UP034" id="projen.python.RuleSelector.UP034"></a>

UP034.

---


##### `UP035` <a name="UP035" id="projen.python.RuleSelector.UP035"></a>

UP035.

---


##### `UP036` <a name="UP036" id="projen.python.RuleSelector.UP036"></a>

UP036.

---


##### `UP037` <a name="UP037" id="projen.python.RuleSelector.UP037"></a>

UP037.

---


##### `UP039` <a name="UP039" id="projen.python.RuleSelector.UP039"></a>

UP039.

---


##### `UP04` <a name="UP04" id="projen.python.RuleSelector.UP04"></a>

UP04.

---


##### `UP040` <a name="UP040" id="projen.python.RuleSelector.UP040"></a>

UP040.

---


##### `UP041` <a name="UP041" id="projen.python.RuleSelector.UP041"></a>

UP041.

---


##### `UP042` <a name="UP042" id="projen.python.RuleSelector.UP042"></a>

UP042.

---


##### `UP043` <a name="UP043" id="projen.python.RuleSelector.UP043"></a>

UP043.

---


##### `UP044` <a name="UP044" id="projen.python.RuleSelector.UP044"></a>

UP044.

---


##### `UP045` <a name="UP045" id="projen.python.RuleSelector.UP045"></a>

UP045.

---


##### `UP046` <a name="UP046" id="projen.python.RuleSelector.UP046"></a>

UP046.

---


##### `UP047` <a name="UP047" id="projen.python.RuleSelector.UP047"></a>

UP047.

---


##### `UP049` <a name="UP049" id="projen.python.RuleSelector.UP049"></a>

UP049.

---


##### `UP05` <a name="UP05" id="projen.python.RuleSelector.UP05"></a>

UP05.

---


##### `UP050` <a name="UP050" id="projen.python.RuleSelector.UP050"></a>

UP050.

---


##### `W` <a name="W" id="projen.python.RuleSelector.W"></a>

W.

---


##### `W1` <a name="W1" id="projen.python.RuleSelector.W1"></a>

W1.

---


##### `W19` <a name="W19" id="projen.python.RuleSelector.W19"></a>

W19.

---


##### `W191` <a name="W191" id="projen.python.RuleSelector.W191"></a>

W191.

---


##### `W2` <a name="W2" id="projen.python.RuleSelector.W2"></a>

W2.

---


##### `W29` <a name="W29" id="projen.python.RuleSelector.W29"></a>

W29.

---


##### `W291` <a name="W291" id="projen.python.RuleSelector.W291"></a>

W291.

---


##### `W292` <a name="W292" id="projen.python.RuleSelector.W292"></a>

W292.

---


##### `W293` <a name="W293" id="projen.python.RuleSelector.W293"></a>

W293.

---


##### `W3` <a name="W3" id="projen.python.RuleSelector.W3"></a>

W3.

---


##### `W39` <a name="W39" id="projen.python.RuleSelector.W39"></a>

W39.

---


##### `W391` <a name="W391" id="projen.python.RuleSelector.W391"></a>

W391.

---


##### `W5` <a name="W5" id="projen.python.RuleSelector.W5"></a>

W5.

---


##### `W50` <a name="W50" id="projen.python.RuleSelector.W50"></a>

W50.

---


##### `W505` <a name="W505" id="projen.python.RuleSelector.W505"></a>

W505.

---


##### `W6` <a name="W6" id="projen.python.RuleSelector.W6"></a>

W6.

---


##### `W60` <a name="W60" id="projen.python.RuleSelector.W60"></a>

W60.

---


##### `W605` <a name="W605" id="projen.python.RuleSelector.W605"></a>

W605.

---


##### `YTT` <a name="YTT" id="projen.python.RuleSelector.YTT"></a>

YTT.

---


##### `YTT1` <a name="YTT1" id="projen.python.RuleSelector.YTT1"></a>

YTT1.

---


##### `YTT10` <a name="YTT10" id="projen.python.RuleSelector.YTT10"></a>

YTT10.

---


##### `YTT101` <a name="YTT101" id="projen.python.RuleSelector.YTT101"></a>

YTT101.

---


##### `YTT102` <a name="YTT102" id="projen.python.RuleSelector.YTT102"></a>

YTT102.

---


##### `YTT103` <a name="YTT103" id="projen.python.RuleSelector.YTT103"></a>

YTT103.

---


##### `YTT2` <a name="YTT2" id="projen.python.RuleSelector.YTT2"></a>

YTT2.

---


##### `YTT20` <a name="YTT20" id="projen.python.RuleSelector.YTT20"></a>

YTT20.

---


##### `YTT201` <a name="YTT201" id="projen.python.RuleSelector.YTT201"></a>

YTT201.

---


##### `YTT202` <a name="YTT202" id="projen.python.RuleSelector.YTT202"></a>

YTT202.

---


##### `YTT203` <a name="YTT203" id="projen.python.RuleSelector.YTT203"></a>

YTT203.

---


##### `YTT204` <a name="YTT204" id="projen.python.RuleSelector.YTT204"></a>

YTT204.

---


##### `YTT3` <a name="YTT3" id="projen.python.RuleSelector.YTT3"></a>

YTT3.

---


##### `YTT30` <a name="YTT30" id="projen.python.RuleSelector.YTT30"></a>

YTT30.

---


##### `YTT301` <a name="YTT301" id="projen.python.RuleSelector.YTT301"></a>

YTT301.

---


##### `YTT302` <a name="YTT302" id="projen.python.RuleSelector.YTT302"></a>

YTT302.

---


##### `YTT303` <a name="YTT303" id="projen.python.RuleSelector.YTT303"></a>

YTT303.

---


### TrustedPublishing <a name="TrustedPublishing" id="projen.python.TrustedPublishing"></a>

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.TrustedPublishing.ALWAYS">ALWAYS</a></code> | always. |
| <code><a href="#projen.python.TrustedPublishing.NEVER">NEVER</a></code> | never. |

---

##### `ALWAYS` <a name="ALWAYS" id="projen.python.TrustedPublishing.ALWAYS"></a>

always.

---


##### `NEVER` <a name="NEVER" id="projen.python.TrustedPublishing.NEVER"></a>

never.

---


### VersionComparator <a name="VersionComparator" id="projen.python.VersionComparator"></a>

#### Members <a name="Members" id="Members"></a>

| **Name** | **Description** |
| --- | --- |
| <code><a href="#projen.python.VersionComparator.LE">LE</a></code> | *No description.* |
| <code><a href="#projen.python.VersionComparator.LT">LT</a></code> | *No description.* |
| <code><a href="#projen.python.VersionComparator.NE">NE</a></code> | *No description.* |
| <code><a href="#projen.python.VersionComparator.EQ">EQ</a></code> | *No description.* |
| <code><a href="#projen.python.VersionComparator.GE">GE</a></code> | *No description.* |
| <code><a href="#projen.python.VersionComparator.GT">GT</a></code> | *No description.* |
| <code><a href="#projen.python.VersionComparator.COMPATIBLE_RELEASE">COMPATIBLE_RELEASE</a></code> | *No description.* |
| <code><a href="#projen.python.VersionComparator.ARBITRARY_EQ">ARBITRARY_EQ</a></code> | *No description.* |

---

##### `LE` <a name="LE" id="projen.python.VersionComparator.LE"></a>

---


##### `LT` <a name="LT" id="projen.python.VersionComparator.LT"></a>

---


##### `NE` <a name="NE" id="projen.python.VersionComparator.NE"></a>

---


##### `EQ` <a name="EQ" id="projen.python.VersionComparator.EQ"></a>

---


##### `GE` <a name="GE" id="projen.python.VersionComparator.GE"></a>

---


##### `GT` <a name="GT" id="projen.python.VersionComparator.GT"></a>

---


##### `COMPATIBLE_RELEASE` <a name="COMPATIBLE_RELEASE" id="projen.python.VersionComparator.COMPATIBLE_RELEASE"></a>

---


##### `ARBITRARY_EQ` <a name="ARBITRARY_EQ" id="projen.python.VersionComparator.ARBITRARY_EQ"></a>

---

