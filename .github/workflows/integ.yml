# ~~ Generated by projen. To modify, edit .projenrc.ts and run "node ./projen.js".

name: integ
on:
  pull_request: {}
  workflow_dispatch: {}
  merge_group: {}
jobs:
  integ-node:
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    env:
      CI: "0"
    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v7
      - id: auto_60fc0a66
        uses: actions/setup-node@v7
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Install dependencies
        id: install_dependencies
        run: npm ci
      - name: Compile
        id: compile
        run: node ./projen.js compile
      - name: Bundle task runner
        id: bundle_task_runner
        run: node ./projen.js bundle:task-runner
      - name: Package
        id: package
        run: node ./projen.js package:js
      - id: auto_60fc0a66_2
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node-version }}
          package-manager-cache: false
      - name: Run Node integration test
        id: run_node_integration_test
        run: node ./projen.js integ:node
      - name: Run Windows task shell syntax tests
        id: run_windows_task_shell_syntax_tests
        run: node ./projen.js integ:windows-shell-syntax
      - name: Run eject integration test
        id: run_eject_integration_test
        run: node ./projen.js integ:eject
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 18.18.0
          - 20.9.0
          - lts/-2
          - 22.0.0
          - lts/-1
          - 24.0.0
          - lts/*
        os:
          - ubuntu-latest
          - windows-latest
  integ-python:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      CI: "0"
    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v7
      - id: auto_d7db6400
        uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
      - id: auto_60fc0a66
        uses: actions/setup-node@v7
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Install dependencies
        id: install_dependencies
        run: npm ci
      - name: Compile
        id: compile
        run: node ./projen.js compile
      - name: Package
        id: package
        run: node ./projen.js package:python
      - name: Run Python integration test
        id: run_python_integration_test
        run: node ./projen.js integ:python
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.10"
          - "3.11"
          - "3.12"
          - "3.13"
          - "3.14"
  integ-go:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      CI: "0"
    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v7
      - id: auto_a7dbad2d
        uses: actions/setup-go@v7
        with:
          go-version: ${{ matrix.go-version }}
      - id: auto_60fc0a66
        uses: actions/setup-node@v7
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Install dependencies
        id: install_dependencies
        run: npm ci
      - name: Compile
        id: compile
        run: node ./projen.js compile
      - name: Package
        id: package
        run: node ./projen.js package:go
      - name: Run Go integration test
        id: run_go_integration_test
        run: node ./projen.js integ:go
    strategy:
      fail-fast: false
      matrix:
        go-version:
          - 1.25.x
          - 1.26.x
  integ-java:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      CI: "0"
    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v7
      - id: auto_caa81516
        uses: actions/setup-java@v5
        with:
          java-version: ${{ matrix.java-version }}
          distribution: ${{ matrix.java-distribution }}
      - id: auto_60fc0a66
        uses: actions/setup-node@v7
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Install dependencies
        id: install_dependencies
        run: npm ci
      - name: Compile
        id: compile
        run: node ./projen.js compile
      - name: Package
        id: package
        run: node ./projen.js package:java
      - name: Run Java integration test
        id: run_java_integration_test
        run: node ./projen.js integ:java
    strategy:
      fail-fast: false
      matrix:
        include:
          - java-version: "8"
            java-distribution: corretto
          - java-version: "11"
            java-distribution: corretto
          - java-version: "17"
            java-distribution: corretto
          - java-version: "21"
            java-distribution: corretto
          - java-version: "25"
            java-distribution: corretto
        java-version:
          - "8"
          - "11"
          - "17"
          - "21"
          - "25"
  integ:
    needs:
      - integ-node
      - integ-python
      - integ-go
      - integ-java
    runs-on: ubuntu-latest
    permissions: {}
    if: always()
    steps:
      - name: integ-node result
        id: integ-node_result
        run: echo ${{ needs.integ-node.result }}
      - name: integ-python result
        id: integ-python_result
        run: echo ${{ needs.integ-python.result }}
      - name: integ-go result
        id: integ-go_result
        run: echo ${{ needs.integ-go.result }}
      - name: integ-java result
        id: integ-java_result
        run: echo ${{ needs.integ-java.result }}
      - name: Set status based on integ tests
        id: set_status_based_on_integ_tests
        if: ${{ needs.integ-node.result != 'success' || needs.integ-python.result != 'success' || needs.integ-go.result != 'success' || needs.integ-java.result != 'success' }}
        run: exit 1
